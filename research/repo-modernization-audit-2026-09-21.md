---
title: "Repo Modernization Audit — JUCE, Build, Claude Tooling, Gates, Hygiene, Instruments"
created: 2026-09-21
domain: tooling
type: research
keywords:
  - juce
  - build-system
  - ci
  - claude-code
  - tooling
  - repo-hygiene
  - modernization
  - audit
---

# Repo Modernization Audit — 2026-09-21

## Executive Summary

| # | Action | Why now | Effort | Risk |
|---|--------|---------|--------|------|

## 1. JUCE Version & Toolchain

All upstream facts below were retrieved **2026-09-21** from
`https://github.com/juce-framework/JUCE` via `gh api` and `raw.githubusercontent.com`.

| Finding | Evidence | Recommendation | Effort | Risk |
|---------|----------|----------------|--------|------|
| The pin is two releases behind, and one of them is a new major. Pinned JUCE is 8.0.14 (published 2026-06-22); upstream shipped 8.0.15 on 2026-07-21 and then JUCE **9.0.0** the same day, with 9.0.2 on 2026-09-07. The framing "latest 8.x" is now a dead end — 8.0.15 is the terminal 8.x release. | `.github/juce-version.txt` = `8.0.14`; local tree `project(JUCE VERSION 8.0.14 ...)` at `/Users/taylorbrook/JUCE/CMakeLists.txt:35`; `gh api repos/juce-framework/JUCE/releases` → `9.0.2 2026-09-07`, `9.0.1 2026-08-10`, `9.0.0 2026-07-21`, `8.0.15 2026-07-21`, `8.0.14 2026-06-22` (retrieved 2026-09-21) | Treat 8.0.15 and 9.x as two separate decisions. Take 8.0.15 first as a low-risk patch-level move; hold 9.x until the override hazard below is gated. | S | Low |
| **The vendored-override copy is an unguarded clobber, and 8.0.15 already proves it bites.** CI does `cp -R vendored/JUCE-overrides/modules/. JUCE/modules/` over the freshly downloaded JUCE with no version check. Upstream changed one of the two override targets between 8.0.14 and 8.0.15 — a one-line `getPosition()` fix removing a `jmax(0, projectTimeSamples)` clamp. Bumping the pin today would silently revert that upstream fix in every CI binary. | `.github/workflows/build-and-release.yml:160` and `:525`; `.github/workflows/ci-tests.yml:128` and `:251`; `gh api repos/juce-framework/JUCE/compare/8.0.14...8.0.15` → `modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp` `additions=1 deletions=1`; `grep -rn "8\.0\.1[0-9]" vendored/` returns nothing, so no version is tied to the override | Before any bump, add a guard step that fails when the override's base JUCE version ≠ `.github/juce-version.txt`, and re-derive the two override files from the new tag rather than copying the old ones forward. | M | High |
| Two independent patch mechanisms exist for the same fork, and nothing proves they agree. Local dev applies a real patch; CI copies whole files. They happen to be in sync **today** — byte-identical modulo CRLF — but by coincidence, not by any gate. | `scripts/apply-juce-patches.sh` applies `scripts/juce-patches/note-expression-juce-8.0.14.patch`; `grep -n "apply-juce-patches" .github/workflows/*.yml` returns **nothing**; `diff <(tr -d '\r' < vendored/JUCE-overrides/.../juce_audio_plugin_client_VST3.cpp) /Users/taylorbrook/JUCE/.../juce_audio_plugin_client_VST3.cpp` → 0 diff lines (vendored copy is CRLF, local is LF) | Make one mechanism authoritative: generate `vendored/JUCE-overrides/` from the patch in a script, and add a CI step that regenerates and diffs so drift fails loudly. | M | Med |
| The Note Expression fork is still genuinely required — upstream has **not** absorbed it. But it has shrunk by an order of magnitude across the 8.0.9 → 8.0.14 re-derivation, so re-deriving it against 9.x is a much smaller job than the older patch suggests. | Master's `modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h` (HTTP 200, retrieved 2026-09-21) contains **0** occurrences of `NoteExpression`/`onVst3RawEvent`/`Vst3RawEvent`; `grep -c '^@@'` → `note-expression-juce-8.0.9.patch` 27 hunks / 1114 lines vs `note-expression-juce-8.0.14.patch` **2 hunks / 117 lines** | Keep the fork. Budget the 9.x re-derivation at roughly the 2-hunk scale, not the 27-hunk scale — both hunks are purely additive. | S | Low |
| **JUCE 9 deprecation exposure is effectively zero.** Every removed or changed symbol named in upstream `BREAKING_CHANGES.md` for 9.0.0–9.0.2 was grepped against `plugins/` and `modules/`; all return 0 files. The WebView-first UI architecture is why — nothing here touches `juce::Drawable` or the SVG/OpenGL/MIDI-selector surfaces that 9.x changed. | `grep -rIl` across `plugins modules CMakeLists.txt` → `createFromSVG` 0, `Drawable` 0, `getStrokeType` 0, `getDashLengths` 0, `setDashLengths` 0, `getMidiInputSelectorListBox` 0, `JUCE_INCLUDE_ZLIB_CODE` 0, `MultiTouch` 0, `OpenGL` 0 source files; symbols extracted from `https://raw.githubusercontent.com/juce-framework/JUCE/master/BREAKING_CHANGES.md` (retrieved 2026-09-21) | The 9.x source-compatibility risk is not the blocker people assume. The real blocker is the override mechanism in row 2, not the API surface. | S | Low |
| The one JUCE-9 surface this repo does touch is the WebView JS package relocation (9.0.1), and even that is comment-only. `modules/juce_gui_extra/native/javascript` moved to `modules/juce_gui_extra/native/typescript/webview-interop`. | `grep -rn "juce_gui_extra/native/javascript"` → 3 hits, all comments: `plugins/O-Octagon/CMakeLists.txt:80`, `plugins/O-Octagon/Source/ui/public/js/app.js:81`, `plugins/O-Contrabass/.planning/mockups/v1-CMakeLists.txt:73`; upstream `BREAKING_CHANGES.md` "Version 9.0.1" entry (retrieved 2026-09-21) | Fix the three stale comments whenever 9.x is taken. No code change required. | S | Low |
| **34 plugins each vendor a private copy of JUCE's WebView interop `index.js`, and the copies have drifted into 3 distinct versions.** JUCE 9.0.1 published this as `@juce-framework/webview` on npm specifically to end hand-copying. | `find plugins -path '*/Source/ui/public/js/juce' -not -path '*/backups/*' &#124; wc -l` → **34**; `shasum` of `index.js` → `2e45ed40` ×27, `fcb126fa` ×5 (O-Bass, O-IntonationPad, O-MultiBandCompressor, O-SimpleReverb, O-Tremolo), `fea0d398` ×2 (O-Chorus, O-TextureForge); `CHANGE_LIST.md` "Version 9.0.1 — Added a new TypeScript npm package for WebView integrarion" (retrieved 2026-09-21) | Reconcile the 3 variants to one before considering npm consumption. The drift is a live correctness question today, independent of any JUCE bump. | M | Med |
| A hardcoded absolute home directory is the non-Windows JUCE fallback in a public repo. Any contributor without `JUCE_DIR` set, on macOS or Linux, gets a configure failure pointing at a stranger's home directory. | `CMakeLists.txt:41` → `add_subdirectory(/Users/taylorbrook/JUCE JUCE)`; the same literal is the default in `scripts/apply-juce-patches.sh` (`JUCE_DIR="${JUCE_DIR:-/Users/taylorbrook/JUCE}"`) | Replace the fallback with a `find_package`/`FetchContent` path or a clear `message(FATAL_ERROR ...)` naming `JUCE_DIR`. Cheap, and it is the first thing an outside contributor hits. | S | Low |
| The bundled VST3 SDK is current — this is a non-finding worth recording so it is not re-audited. | `kVstVersionString "VST 3.8.0"` at `/Users/taylorbrook/JUCE/modules/juce_audio_processors_headless/format_types/VST3_SDK/pluginterfaces/vst/vsttypes.h:27` | No action. | S | Low |
| Toolchain spread between local and CI is wide, and the macOS deployment floor predates every compiler now in play. Local is macOS 26.6.2 / Xcode 26.3 / CMake 4.2.1 / Ninja 1.13.2; CI builds on `macos-14`. The 10.13 floor is set unconditionally. | `CMakeLists.txt:5` → `CMAKE_OSX_DEPLOYMENT_TARGET "10.13"`; `.github/workflows/build-and-release.yml:141` and `.github/workflows/ci-tests.yml:94` → `runs-on: macos-14`; local `cmake --version` 4.2.1, `ninja --version` 1.13.2, `xcodebuild -version` Xcode 26.3, `sw_vers` 26.6.2 | Raise the floor to something a current Xcode actually supports and move CI to a newer macOS image — but verify against real DAW-host minimums first, since this is the one row here that changes shipped binaries. | M | Med |
| No CLAP format is built, for any of the 44 plugins. All build the same three formats. | `grep -h FORMATS plugins/*/CMakeLists.txt` → `FORMATS VST3 AU Standalone` ×44; `grep -rli clap plugins/*/CMakeLists.txt CMakeLists.txt modules/` → no matches | Optional. `clap-juce-extensions` adds CLAP as a wrapper target without touching plugin source; worth it only if a target DAW (Bitwig, Reaper) matters commercially. | M | Low |

**Upgrade risk/benefit, stated plainly.** A bump to **8.0.15** buys a handful of Windows GUI-scaling fixes, an Oboe update, and the `getPosition()` clamp fix — and costs almost nothing, because `BREAKING_CHANGES.md` lists **no** entries between 8.0.13 and 9.0.0, so 8.0.14 → 8.0.15 is breaking-change-free. A bump to **9.x** buys a new SVG parser, variable fonts, a rewritten CoreAudio backend, and the npm WebView package — none of which this repo currently needs — and costs re-deriving the 2-hunk Note Expression patch plus re-validating 44 plugins. **The binding constraint in both cases is the same:** the unguarded `vendored/JUCE-overrides/` clobber (row 2). Fix that gate first and both bumps become ordinary; leave it and even the "free" 8.0.15 bump silently reverts an upstream audio-position fix.

## 2. Build Efficiency

No build was run for this audit. Every number below is read from committed configuration,
an existing build graph, or real CI run history.

| Finding | Evidence | Recommendation | Effort | Risk |
|---------|----------|----------------|--------|------|
| **No compiler cache is configured anywhere — not locally, not in CI.** Across the root CMake file, all 44 plugin CMake files, `modules/`, both workflows and every build script, there is not one `ccache`, `sccache` or `*_COMPILER_LAUNCHER` reference. Every rebuild recompiles from scratch. | `grep -rn "ccache&#124;sccache&#124;COMPILER_LAUNCHER" CMakeLists.txt plugins/*/CMakeLists.txt modules/ .github/workflows/*.yml scripts/*.sh scripts/*.ps1` → **no matches** | Add `ccache` locally via `CMAKE_CXX_COMPILER_LAUNCHER`. This is the single highest-leverage build change available and it cannot change emitted code. | S | Low |
| **Neither workflow caches anything at all.** There is no `actions/cache` step in either file, so every CI run re-downloads the whole JUCE zip and rebuilds every object from zero. O-Texture additionally re-fetches `anira` (and through it ONNX Runtime) over the network per configure. | `grep -n "actions/cache" .github/workflows/*.yml` → **no matches**; `.github/workflows/build-and-release.yml:151` and `:516` curl the JUCE zip per job; `plugins/O-Texture/CMakeLists.txt:11-20` → `FetchContent_Declare(anira ... GIT_TAG v2.1.0)` | Cache the JUCE download keyed on `.github/juce-version.txt`, and cache the `FetchContent` deps dir. Both are pure-win: keyed on a pinned version, so a stale cache is impossible. | S | Low |
| No unity builds and no precompiled headers, across 44 plugin targets that all include the same JUCE headers. | `grep -rn "CMAKE_UNITY_BUILD&#124;UNITY_BUILD&#124;target_precompile_headers"` across root + plugin CMake files + `modules/` → **no matches** | Consider `target_precompile_headers` on the JUCE includes. Lower priority than ccache and measurably riskier (PCH can mask missing includes), so do it second. | M | Med |
| The root `file(GLOB plugins/*)` auto-discovery adds all 44 plugins to every configure, producing a build graph with **8,911 targets** and a 66,070-line `build.ninja` even when one plugin is wanted. A narrower path already exists but the glob is not gated on it. | `CMakeLists.txt:48` → `file(GLOB PLUGIN_DIRS ...)`; `ninja -C build -t targets all &#124; wc -l` → **8911**; `wc -l build/build.ninja` → **66070**; `build-release` (release-configured) → 4355 targets / 30623 lines; `SKIP_PLUGINS` cache var exists at `CMakeLists.txt:46` and `scripts/resolve-target.sh` is present | Have `build-and-install.sh` pass `-DSKIP_PLUGINS=` with the complement of the requested plugin. The machinery is already there; nothing calls it. | M | Low |
| Local build trees total **6.2 GB** across three configurations that are never garbage-collected. | `du -sh build build-asan build-release` → `4.9G` / `700M` / `607M` | Housekeeping only — all three are correctly gitignored (see §5). Prune when disk pressure appears. | S | Low |
| `build-and-install.sh` correctly skips reconfiguration when `build/` exists, so the glob cost above is a one-time-per-tree cost rather than per-invocation. Worth recording so it is not "optimised" twice. | `scripts/build-and-install.sh:261-270` → "One-time configure at root (only if build/ doesn't exist)", with `--reconfigure` as the explicit opt-in | No action. | S | Low |
| Release builds are universal binaries (`arm64;x86_64`), which doubles compile time for every release; CI test builds are deliberately single-arch. The asymmetry is intentional and documented, but it means release wall-clock is roughly twice what the test leg suggests. | `.github/workflows/build-and-release.yml:170` → `-DCMAKE_OSX_ARCHITECTURES="arm64;x86_64"`; `.github/workflows/ci-tests.yml:148` → "Single-arch DELIBERATELY: no CMAKE_OSX_ARCHITECTURES" | Keep. Universal binaries are a distribution requirement, not an oversight. Caching (row 2) is the right lever here, not dropping an arch. | S | Low |
| Real CI wall-clock, measured from run history rather than estimated: `ci-tests.yml` runs 5–12 min; `build-and-release.yml` runs 7–21 min, with the recent spread widening as plugin count grew. | `gh run list --workflow=ci-tests.yml --limit 15` → 5,5,5,5,6,6,6,6,8,8,8,8,12 min; `gh run list --workflow=build-and-release.yml --limit 10` → 7,8,8,9,9,14,16,19,20,21 min (retrieved 2026-09-21) | These are not pathological. Fix caching first and re-measure before investing anywhere else. | S | Low |
| No link-time optimisation is configured on any target. | `grep -rn "INTERPROCEDURAL_OPTIMIZATION"` across root + plugin CMake files + `modules/` + workflows → **no matches** | Leave off. LTO on 44 plugin targets would cost far more build time than it buys in a real-time audio path that is already hand-optimised. Recorded so it is not mistaken for an oversight. | S | Low |

## 3. Claude Workflow & Tooling

| Finding | Evidence | Recommendation | Effort | Risk |
|---------|----------|----------------|--------|------|

## 4. Testing & Quality Gates

| Finding | Evidence | Recommendation | Effort | Risk |
|---------|----------|----------------|--------|------|
| **`ci-tests.yml` never fires on its own.** Its only trigger is `workflow_dispatch`, so no commit, push, tag or pull request runs the probes or the Windows pluginval gate. Every quality gate in this repo is opt-in and manual. | `.github/workflows/ci-tests.yml:71-81` → `on: workflow_dispatch:` with no `push:`/`pull_request:` key; file comment at `:12` states "This workflow is therefore `workflow_dispatch` only" | Add a `pull_request` trigger, or at minimum a nightly `schedule`. Nothing else in this section matters until a gate runs without being asked. | S | Low |
| **The dispatch input restricts CI to 2 of 44 plugins.** The `plugin` choice offers only `O-Octagon` and `O-Strata`. The other 42 plugins — including every shipped instrument — have no CI path at all, on any OS. | `.github/workflows/ci-tests.yml:74-81` → `options: [O-Octagon, O-Strata]`, `default: O-Octagon`; `ls -d plugins/*/ &#124; wc -l` → 44 | Widen the choice list to all plugins (it is a literal enum, so this is a mechanical edit), then pick a rotating subset for the scheduled run. | S | Low |
| **CI has been red for 7 days, and both failures are load-sensitive thresholds rather than regressions.** The macOS render harness reports `GATES FAILED — 162 check(s), 2 failure(s), 292.0 s`. One failure compares against an absolute `+ 2.0` slack; the other asserts a wall-clock `<= 120 ms` republish budget on a shared runner. | `gh run list --workflow=ci-tests.yml` → `failure` on runs `34809565500`, `34808930140`, `34808363835` (all 2026-09-14, branch `main`), last success 2026-08-25; `gh run view 34809565500 --log-failed` → `FAIL [H7] Bandlimited delta 10.37 % <= 2x delta 8.23 % + 2.0` and `FAIL [sched] ... re-publishes the set in 154 ms (need <= 120 under pump)` | Re-express both as ratios against a same-run baseline instead of absolute constants. A gate that fails on runner load teaches the team to ignore red, which is worse than having no gate. | M | Med |
| A substantial automated test suite **does** exist and does run — 162 checks in the macOS leg — but it is a bespoke render harness, not a standard framework. There is no CTest, Catch2 or GoogleTest anywhere, so `ctest` finds nothing and the harness is invisible to any conventional tooling. | `.github/workflows/ci-tests.yml:171-202` → steps "Run geometry unit probes", "Run render-harness probes", "Build render harness", "Run render harness (--gate all)", "Orbit golden"; `grep -rn "enable_testing&#124;add_test&#124;Catch2&#124;gtest"` across root + plugin CMake files + `modules/` → **no matches** | Keep the harness — it is doing real work. Register it with `add_test()` so `ctest` becomes the single entry point and the gate stops depending on hand-written workflow steps. | M | Low |
| **pluginval runs on Windows only, and macOS gets no automated plugin validation of any kind.** `auval` — which CLAUDE.md requires after every install — never executes in CI. Given that AU is a shipped format, the format with the strictest host validator has the weakest automated coverage. | `.github/workflows/ci-tests.yml:292-303` → pluginval `--strictness-level 10` in the `windows-vst3` job; `.github/workflows/build-and-release.yml:571-586` → same, Windows only; `grep -n auval .github/workflows/*.yml` → **no matches** | Add an `auval -v aumu <subtype> <manu>` step to the macOS leg. The runner already builds the AU; this is a few lines and closes the largest coverage gap in the matrix. | S | Low |
| pluginval is pinned to **v1.0.3 (2023-03-03)**; upstream v1.0.4 shipped 2024-12-04. Strictness 10 is correctly the maximum, and the choice is deliberate — it opens the editor, which is what surfaces a blank WebView. | `.github/workflows/ci-tests.yml:296` and `.github/workflows/build-and-release.yml:578` → `.../pluginval/releases/download/v1.0.3/pluginval_Windows.zip`; `gh api repos/Tracktion/pluginval/releases` → `v1.0.4 2024-12-04`, `v1.0.3 2023-03-03` (retrieved 2026-09-21) | Bump the pin to v1.0.4 and keep strictness 10. Low risk, and it is a one-line change in two files. | S | Low |
| The ASan build is entirely local — `build-asan/` occupies 700 MB on disk but no workflow or script references a sanitizer. Sanitizer coverage depends on someone remembering to run it. | `grep -rn "sanitize&#124;asan&#124;ASAN" .github/workflows/ scripts/` → **no matches**; `du -sh build-asan` → `700M` | Note: per project knowledge, ASan currently hangs at init on macOS 26 / Xcode 26.3, so a CI ASan leg would need a pinned older image. Treat as blocked, not as an easy win. | L | Med |

**The single cheapest high-value gate to add is `auval` on the macOS leg.** It is cheapest because every prerequisite already exists: the macOS runner already builds the AU target, `auval` ships with macOS so there is nothing to install or pin, and it needs no new harness, fixture or golden. It is high-value because AU is the one shipped format with zero automated validation today, and `auval` catches exactly the class of defect — I/O configuration and registry problems — that this project has hit repeatedly. Every other candidate gate requires either new infrastructure or unblocking something first.

## 5. Repo Hygiene

| Finding | Evidence | Recommendation | Effort | Risk |
|---------|----------|----------------|--------|------|
| **The repository's own checksum-only golden policy is being violated, and it is the dominant cause of a 1.1 GB `.git`.** 21 golden WAVs are tracked *alongside* a tracked `.sha256` sibling — both mechanisms committed for the same fixture. Those 21 files are 204.4 MB at HEAD alone, before history. | `git count-objects -vH` → `size-pack: 752.57 MiB`, `du -sh .git` → `1.1G`; `git ls-files -- '*.wav'` → 33 files totalling **226.4 MB**; 21 of them have a tracked `.sha256` sibling (204.4 MB); `git ls-files -- '*.sha256'` → 35 | Delete the 21 redundant WAVs with `git rm --cached` and let the `.sha256` files be the contract, as the policy already intends. History rewrite is a separate, larger decision — do the HEAD cleanup first. | M | Med |
| The largest blobs in history are the same goldens, stored multiple times. `sub-harmonics-stability.wav` alone appears as **4 distinct 49.96 MB blobs** — roughly 200 MB of pack for one fixture. | `git rev-list --objects --all &#124; git cat-file --batch-check` sorted by size → top 25 blobs are all `plugins/O-Contrabass/tests/render-harness/golden/*.wav`, led by `sub-harmonics-stability.wav` ×4 at 49.96 MB, then `string-A/-D/-G`, `stiffness-zero-pre`, `saturator-tail-comparison` at 16.40 MB each | Only a history rewrite (`git filter-repo`) reclaims this. Worth doing once, deliberately, after the HEAD cleanup above — and it invalidates every existing clone, so it needs a clear announcement. | L | High |
| **`logs/` is not gitignored** — it holds 50 per-plugin directories and 51 MB of build logs, currently untracked only because commits here are path-scoped. One `git add -A` would commit all of it. CLAUDE.md's path-scoping rule is load-bearing, and this is what it is bearing. | `git check-ignore -v logs/` → no match; `ls logs/ &#124; wc -l` → 50; `du -sh logs` → `51M`; `git ls-files -- logs/` → 0 tracked | Add `logs/` to `.gitignore`. One line, closes a 51 MB foot-gun. | S | Low |
| `.gitignore` coverage is otherwise sound, tested against reality rather than read. Seven of eight checked paths resolve to a real rule, including the 12 loose root PNG screenshots, which are covered by a deliberate root-anchored rule. | `git check-ignore -v` → `build/` `.gitignore:86`, `build-release/` `:87`, `build-asan/` `:89`, `backups/` `:36`, `scratchpad/` `:279`, `.playwright-mcp/` `:250`, `.cache/` via `*.cache` `:218`; `grain-before.png` → `/*.png` `:246`; `git ls-files -- '*.png' &#124; grep -vc /` → 0 tracked root PNGs; `git ls-files &#124; grep -c DS_Store` → 0 | No action beyond `logs/`. Recorded so the `.gitignore` is not re-audited. | S | Low |
| **CLAUDE.md documents the release-tag format that never triggers CI.** It specifies version-first (`vX.Y.Z-<PluginName>`, example `v3.1.1-O-Bells`), but the release workflow triggers on `'*-v*'`, which a version-first tag cannot match. Current practice is correct plugin-first, so this is a live documentation trap rather than a live outage. | `.github/workflows/build-and-release.yml:40` → `- '*-v*'  # Matches: PluginName-vX.Y.Z`; `CLAUDE.md` "Release tags — named `vX.Y.Z-<PluginName>`, version first"; `git tag` census → 240 plugin-first, 125 version-first; `git for-each-ref --sort=-creatordate refs/tags &#124; head -12` → all plugin-first, most recent `O-Bells-v4.8.0` (2026-09-21) | Correct the CLAUDE.md sentence to plugin-first. Cheapest fix in this report and it prevents a silent no-op release. *(Not applied here — CLAUDE.md is a protected path for this read-only audit.)* | S | Low |
| Two generated `.claude/` artifacts are tracked, so they produce diff noise on every regeneration. | `git ls-files --error-unmatch` → **tracked**: `.claude/resource-index.json` (60 K) and `.claude/frontmatter-issues.txt` | Decide deliberately: either gitignore both as build products, or keep them and accept the churn. The current state looks accidental. | S | Low |
| Branch, worktree and tag state fully matches the trunk-based policy — one local branch, one worktree, no strays. | `git branch -a` → `* main` plus `origin/main`, `private/main`, `private/pre-cleanup-backup-20260201`; `git worktree list` → 1 entry; `git tag &#124; wc -l` → 398 | No action. Confirms the policy is holding. | S | Low |
| **The SAF submodule is pinned ~8 months behind upstream `master`.** It sits on release tag `v1.3.4` (2025-10-04); upstream master has moved to a commit dated 2026-01-03. | `git submodule status` → `b6fe188288ecd59fef9e46bf6bfe3ed57f0ee9af plugins/O-Orbit/libs/SAF (v1.3.4)`; `gh api repos/leomccormack/Spatial_Audio_Framework/commits/master` → `18fd5aba...` `2026-01-03`; latest release still `v1.3.4 2025-10-04` (retrieved 2026-09-21) | Leave it. The pin is on the newest *tagged release*, which is the correct discipline for a vendored DSP dependency; master is simply ahead of the last tag. | S | Low |
| The only other third-party pin is `anira v2.1.0`, fetched at configure time by O-Texture. | `plugins/O-Texture/CMakeLists.txt:11-20` → `FetchContent_Declare(anira GIT_REPOSITORY https://github.com/anira-project/anira.git GIT_TAG v2.1.0)` | Pinned to a tag, which is correct. Pair with the FetchContent cache recommended in §2. | S | Low |

## 6. Instrument-Specific Best Practices

Census basis: **22 instruments**, defined as the plugins declaring `IS_SYNTH TRUE`
(`grep -l "IS_SYNTH TRUE" plugins/*/CMakeLists.txt`). Module adoption is read from
`modules/registry.yaml` `used_by` and intersected with that set, so every "N of 22" below is
instrument-scoped rather than repo-wide.

| Finding | Evidence | Recommendation | Effort | Risk |
|---------|----------|----------------|--------|------|
| **A registry entry points at a module that does not exist on disk — and it is the one with the highest declared reuse score.** `resource-provider` declares `reuse_score: 9` and `path: core/resource-provider`, but that directory is absent. Every other registry path resolves. | `modules/registry.yaml` → `name: resource-provider`, `path: core/resource-provider`, `reuse_score: 9`, `used_by: []`; `ls modules/core/` → only `webview-relay-manager`, `webview-drop-streaming`; path-resolution check across all 11 modules → 10 `OK`, 1 `MISS` | Either restore the module or delete the entry. A registry that lies about what exists is worse than a smaller registry — it will be "reused" by a future session that then has to invent it. | S | Low |
| **Three more modules exist on disk with zero adopters**, together ~88 KB of code nothing consumes. Combined with the missing one, 4 of 11 registry entries have no consumer. | `modules/registry.yaml` `used_by: []` for `webview-relay-manager` (3 files, 24 K), `vu-meter` (4 files, 36 K), `playable-keyboard` (3 files, 28 K); `resource-provider` absent | Decide per module: adopt, or retire. `vu-meter` and `playable-keyboard` are exactly the kind of thing being hand-rolled per plugin, so adoption is likelier than retirement. | M | Low |
| **Voice management is reimplemented in 20 of 22 instruments with no shared module covering it.** This is the single largest duplicated surface in the instrument suite, and the registry has no `synthesis` entry for it beyond the narrow `bow-friction`. | `grep -rqI SynthesiserVoice plugins/<p>/Source` → **20 of 22** instruments (all but O-Texture and O-TextureForge); `modules/registry.yaml` synthesis category contains only `bow-friction` (3 instruments) | Extract a shared voice-management module — voice allocation, stealing, and the note/pitch bookkeeping. 20 consumers is the strongest reuse case in the repo. | L | Med |
| **Preset handling is split three ways.** 12 of 22 instruments consume `preset-manager v1.0.7`; 4 hand-roll preset code instead; 6 have no preset system at all. | `registry.yaml` `preset-manager` `used_by` ∩ instruments → **12/22** (28 repo-wide); local `loadPreset&#124;savePreset&#124;PresetManager&#124;factoryPreset` file counts among the 10 non-consumers → O-Marimba 9, O-IntonationPad 7, O-MicrotonalSampler 1, O-simpleAdditive 1; remaining 6 → 0 | Migrate the 4 hand-rolled instruments to `preset-manager`. They are carrying a maintained module's bugs without its fixes — and the module is at v1.0.7, so the drift is already seven releases deep. | M | Med |
| **One instrument carries Note Expression code without consuming the shared module.** 12 instruments have NE code; the module lists 11. O-Strata is the outlier. | `grep -rqI NoteExpression plugins/<p>/Source` → 12/22; `registry.yaml` `note-expression v1.1.1` `used_by` ∩ instruments → **11/22**; set difference → **O-Strata** | Check whether O-Strata's NE code predates the module, and fold it in. A single divergent copy of the Dorico-critical path is exactly where a silent behavioural difference hides. | S | Low |
| **MPE handling is inconsistent and mostly absent.** 4 of 22 instruments use `MPESynthesiser`, only 1 uses `MPEInstrument`, and **no instrument calls `setMPEZoneLayout`** — so zone configuration is either implicit or unhandled everywhere. | `grep -rqI` across instrument `Source` trees → `MPESynthesiser` **4/22** (O-Bowed, O-Contrabass, O-Formant, O-Reed), `MPEInstrument` **1/22** (O-Contrabass), `setMPEZoneLayout` **0/22** | Decide the suite-wide MPE position explicitly. Zero `setMPEZoneLayout` calls across 22 instruments is a finding worth confirming by hand before acting — it may be correct for VST3 note-ID routing, but it should be a decision, not an accident. | M | Med |
| Scala tuning adoption is 12 of 22, matching the repo-wide count exactly — every `scala-tuning-engine` consumer is an instrument, which is the right shape. | `registry.yaml` `scala-tuning-engine v3.1.0` `used_by` ∩ instruments → **12/22**, total 12 | No action. Recorded as the healthy comparison case for the rows above. | S | Low |
| Block-size invariance is universally handled: all 22 instruments reference `samplesPerBlock`. Offline-render determinism is not: only 1 of 22 references `isNonRealtime`, while 17 of 22 use `juce::Random`. | `grep -rqI samplesPerBlock` → **22/22**; `isNonRealtime` → **1/22** (O-Texture); `juce::Random` → **17/22** | Audit RNG seeding in the 17. Per project knowledge, clock- or `this`-seeded `Random` breaks render-diff determinism — which is precisely what the §4 golden harness depends on. | M | Med |

## Apply Later
