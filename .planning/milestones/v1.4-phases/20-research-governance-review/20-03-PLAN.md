---
phase: 20-research-governance-review
plan: 03
type: execute
wave: 3
depends_on: [20-02]
files_modified:
  - research/*.md
  - .claude/resource-index.json
autonomous: false
requirements: [RSCH-02]

must_haves:
  truths:
    - "New research documents exist for each user-approved coverage gap"
    - "Each new doc is a full deep-dive matching existing doc quality with JUCE C++ examples and broader techniques"
    - "Each new doc has valid frontmatter and is indexed in resource-index.json"
    - "Each new doc was individually approved by the user before creation"
  artifacts:
    - path: ".claude/resource-index.json"
      provides: "Updated index including all new gap-fill docs"
      min_lines: 50
  key_links:
    - from: "research/coverage-audit.md"
      to: "research/*.md (new gap-fill docs)"
      via: "Gaps identified in audit, filled by new docs"
      pattern: "gap"
---

<objective>
Create new research documents for each user-approved coverage gap identified in Plan 02. Each doc is a full deep-dive with JUCE C++ examples and broader techniques, created one at a time with user approval between each.

Purpose: Fill identified research coverage gaps with high-quality documentation (RSCH-02).
Output: New research .md files in research/, updated resource-index.json.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-research-governance-review/20-CONTEXT.md
@.planning/phases/20-research-governance-review/20-02-SUMMARY.md
@research/coverage-audit.md

<interfaces>
<!-- From Plan 02 checkpoint, executor will have: -->
<!-- User's approved list of gaps to fill -->
<!-- coverage-audit.md with gap descriptions and priorities -->

Frontmatter standard (from CONTEXT.md decisions):
- Required: title, created, domain, type, keywords
- Domain vocab: dsp, ui, architecture, tooling, market-research, ml, spatial-audio, cross-platform
- Type vocab: research, algorithm, guide, market-research
- Optional: summary, last_verified, juce_version, stages, agents, status

Quality standard (from CONTEXT.md decisions):
- Full deep-dives matching existing doc quality
- Include JUCE C++ code examples but also broader techniques
- Use web research for accuracy and currency
- Comprehensive coverage with references and implementation notes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gap-fill research documents with per-doc user approval</name>
  <files>research/*.md (new files, names TBD based on approved gaps)</files>
  <action>
For each user-approved gap from the Plan 02 checkpoint:

1. **Present** the gap topic to the user and confirm they want this specific doc created
2. **Research** the topic using web research / deep-research for accuracy and currency
3. **Write** a comprehensive research document in research/{topic-slug}.md with:
   - Valid YAML frontmatter (5 required fields + optional summary, juce_version, last_verified, stages, agents)
   - Domain and type from the controlled vocabulary
   - 3-10 keywords
   - Comprehensive technical content:
     * Overview/introduction of the technique or domain
     * Key algorithms and approaches (with mathematical foundations where relevant)
     * JUCE C++ implementation examples (but not limited to JUCE -- include broader techniques)
     * Performance considerations for real-time audio
     * Common pitfalls and best practices
     * References and further reading
   - Quality bar: match or exceed existing docs like delay-effects-comprehensive-guide.md or reverb-comprehensive-research.md
4. **Validate** frontmatter passes: `python3 .claude/hooks/validators/validate-research-frontmatter.py research/{file}.md`
5. **Commit** each doc individually

After all approved docs are created:
6. **Regenerate** resource index: `python3 .claude/scripts/generate-resource-index.py`
7. **Update** research/coverage-audit.md to reflect newly filled gaps (mark them as covered)

NOTE: If the user deferred all gaps in Plan 02, this task produces no new docs -- just update coverage-audit.md to note "no gaps filled" and regenerate the index to confirm it is current.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development && python3 .claude/scripts/generate-resource-index.py && python3 -c "import json; d=json.load(open('.claude/resource-index.json')); print(f'Total indexed: {len(d[\"documents\"])}')" && for f in research/*.md research/stutter-effects/*.md; do [[ "$(basename $f)" == "README.md" ]] && continue; python3 .claude/hooks/validators/validate-research-frontmatter.py "$f" 2>&1 | grep -i error && echo "FAIL: $f"; done; echo "Validation complete"</automated>
  </verify>
  <done>New research documents exist for each user-approved gap. All have valid frontmatter. resource-index.json is updated to include them. coverage-audit.md reflects the new coverage status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final verification of gap-fill research docs</name>
  <files>research/coverage-audit.md</files>
  <action>
Present the final state to the user for verification:
1. Show updated coverage-audit.md with gaps marked as filled
2. Show resource-index.json entry count (should be 49 + number of new docs)
3. Run `python3 .claude/scripts/generate-resource-index.py` to confirm 0 skipped
4. List all new research docs created with their titles

Ask the user to review and approve or request revisions.
  </action>
  <verify>User has responded with "approved" or revision requests</verify>
  <done>User has verified and approved the complete gap-fill research docs. Phase 20 is complete.</done>
</task>

</tasks>

<verification>
1. All user-approved gap topics have corresponding research docs in research/
2. Each new doc passes frontmatter validation
3. resource-index.json total count = original 49 + new docs created
4. coverage-audit.md is updated to show gaps as filled
5. User has verified and approved the final state
</verification>

<success_criteria>
- New research documents exist for identified coverage gaps (RSCH-02)
- All new docs have valid frontmatter and are indexed
- Coverage audit reflects the updated state
- User has approved the final result
</success_criteria>

<output>
After completion, create `.planning/phases/20-research-governance-review/20-03-SUMMARY.md`
</output>
