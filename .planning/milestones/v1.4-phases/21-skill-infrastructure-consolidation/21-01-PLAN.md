---
phase: 21-skill-infrastructure-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/plugin-workflow/SKILL.md
  - .claude/skills/plugin-phases/SKILL.md
  - .claude/commands/plugin-discuss.md
  - .claude/commands/plugin-research.md
  - .claude/commands/plugin-plan.md
  - .claude/commands/plugin-execute.md
  - .claude/commands/plugin-verify.md
  - .claude/skills/improve-milestone/BOUNDARIES.md
autonomous: true
requirements:
  - SKIL-01
  - SKIL-02

must_haves:
  truths:
    - "The plugin-phases skill directory no longer exists"
    - "All 5 phase commands (plugin-discuss, plugin-research, plugin-plan, plugin-execute, plugin-verify) reference plugin-workflow as their skill"
    - "No file in .claude/ references plugin-phases by name"
    - "The plugin-workflow SKILL.md contains all phase-specific content that was unique to plugin-phases"
  artifacts:
    - path: ".claude/skills/plugin-workflow/SKILL.md"
      provides: "Merged skill with phase commands, skip flags, phase state transitions, and express mode integration from plugin-phases"
      contains: "plugin-discuss"
    - path: ".claude/commands/plugin-discuss.md"
      provides: "Discuss phase command"
      contains: "skill: plugin-workflow"
    - path: ".claude/commands/plugin-research.md"
      provides: "Research phase command"
      contains: "skill: plugin-workflow"
    - path: ".claude/commands/plugin-plan.md"
      provides: "Plan phase command"
      contains: "skill: plugin-workflow"
    - path: ".claude/commands/plugin-execute.md"
      provides: "Execute phase command"
      contains: "skill: plugin-workflow"
    - path: ".claude/commands/plugin-verify.md"
      provides: "Verify phase command"
      contains: "skill: plugin-workflow"
  key_links:
    - from: ".claude/commands/plugin-*.md"
      to: ".claude/skills/plugin-workflow/SKILL.md"
      via: "skill: plugin-workflow frontmatter"
      pattern: "skill: plugin-workflow"
---

<objective>
Merge the plugin-phases skill into plugin-workflow and update all references.

Purpose: Eliminate the overlapping plugin-phases skill directory. plugin-workflow already contains the full orchestration logic; plugin-phases duplicates the same 5-phase cycle (discuss/research/plan/execute/verify) as standalone commands. The merge consolidates into a single source of truth.

Output: Merged plugin-workflow SKILL.md, updated command files, deleted plugin-phases directory.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-skill-infrastructure-consolidation/21-CONTEXT.md

<interfaces>
<!-- The plugin-phases SKILL.md defines commands in its frontmatter that must be preserved in the merged result. -->

From .claude/skills/plugin-phases/SKILL.md (frontmatter):
```yaml
commands:
  - name: plugin-discuss
  - name: plugin-research
  - name: plugin-plan
  - name: plugin-execute
  - name: plugin-verify
```

From .claude/skills/plugin-workflow/SKILL.md (frontmatter):
```yaml
name: plugin-workflow
description: Orchestrates JUCE plugin implementation through stages 1-4 with GSD phase cycles.
```

Each command file (.claude/commands/plugin-*.md) has frontmatter with `skill: plugin-phases` that must become `skill: plugin-workflow`.

From .claude/skills/improve-milestone/BOUNDARIES.md line 116:
```
| plugin-phases | plugin-phases for implementation stage phases; improve-milestone for post-completion improvement phases. |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge plugin-phases content into plugin-workflow SKILL.md and delete plugin-phases</name>
  <files>
    .claude/skills/plugin-workflow/SKILL.md
    .claude/skills/plugin-phases/SKILL.md
  </files>
  <action>
Read both SKILL.md files fully. The plugin-workflow SKILL.md already contains the orchestration loop, phase delegation, all phase execution details, state management, decision menus, error handling, and express/auto mode. Compare with plugin-phases SKILL.md to identify content unique to plugin-phases that plugin-workflow lacks.

Unique plugin-phases content to merge into plugin-workflow SKILL.md:
1. The 5 individual command entries in the SKILL.md frontmatter `commands:` list (plugin-discuss, plugin-research, plugin-plan, plugin-execute, plugin-verify with their args). Add these to the existing plugin-workflow frontmatter commands section alongside the existing /implement and /continue entries.
2. The "Skip Flags" section with `--skip-discuss`, `--skip-research`, `--skip-verify` examples using individual commands (plugin-workflow has skip flag docs but focused on /implement usage). Add a brief note under the existing Skip Flags section that these flags also work with individual phase commands.
3. The "Phase State Transitions" ASCII diagram showing the full stage flow with skippable phases. Plugin-workflow has a simpler one-line diagram. Replace or augment the existing diagram with the more detailed one from plugin-phases.
4. The "Error Handling" section has a "Phase out of order" error pattern that plugin-workflow does not have. Add this as an additional error handling case.
5. The "Express Mode Integration" note that express mode auto-advances phases but presents handoff at stage boundaries. This content already exists in plugin-workflow but verify the wording is complete.

After merging, update the plugin-workflow SKILL.md description to mention it handles both orchestration (/implement) AND individual phase commands.

Then delete the entire `.claude/skills/plugin-phases/` directory (it only contains SKILL.md).

Per user decision: use "best of both" merge approach -- review both prompts and combine strongest elements. Preserve all stage-specific execute agent references. Inherit plugin-workflow's directory structure.
  </action>
  <verify>
    Run: test ! -d .claude/skills/plugin-phases && echo "PASS: plugin-phases deleted" || echo "FAIL: plugin-phases still exists"
    Run: grep -c "plugin-discuss\|plugin-research\|plugin-plan\|plugin-execute\|plugin-verify" .claude/skills/plugin-workflow/SKILL.md
    Expected: plugin-phases directory gone, all 5 command names present in merged SKILL.md
  </verify>
  <done>plugin-phases directory deleted. plugin-workflow SKILL.md contains all merged content including the 5 phase command definitions, skip flags for individual commands, detailed phase state transition diagram, and phase-out-of-order error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Update all references from plugin-phases to plugin-workflow</name>
  <files>
    .claude/commands/plugin-discuss.md
    .claude/commands/plugin-research.md
    .claude/commands/plugin-plan.md
    .claude/commands/plugin-execute.md
    .claude/commands/plugin-verify.md
    .claude/skills/improve-milestone/BOUNDARIES.md
  </files>
  <action>
In each of the 5 command files, change the frontmatter field `skill: plugin-phases` to `skill: plugin-workflow`. Do not change any other content in these files.

In `.claude/skills/improve-milestone/BOUNDARIES.md`, find line 116 which contains `| plugin-phases | plugin-phases for implementation stage phases; improve-milestone for post-completion improvement phases. |` and update it to `| plugin-workflow | plugin-workflow for implementation stage phases; improve-milestone for post-completion improvement phases. |`.

After all edits, run a comprehensive grep across the entire `.claude/` directory to confirm zero remaining references to "plugin-phases" as a skill name. The string may still appear in historical summaries or planning docs (which is fine), but it must not appear in any active skill, command, or boundary file.
  </action>
  <verify>
    Run: grep -r "plugin-phases" .claude/commands/ .claude/skills/ 2>/dev/null | grep -v "SUMMARY\|PLAN\|CONTEXT" | wc -l
    Expected: 0 (no active references to plugin-phases remain)
    Run: grep "skill: plugin-workflow" .claude/commands/plugin-discuss.md .claude/commands/plugin-research.md .claude/commands/plugin-plan.md .claude/commands/plugin-execute.md .claude/commands/plugin-verify.md | wc -l
    Expected: 5 (all 5 command files reference plugin-workflow)
  </verify>
  <done>All 5 command files have skill: plugin-workflow. The improve-milestone BOUNDARIES.md reference is updated. Zero active files reference plugin-phases.</done>
</task>

</tasks>

<verification>
1. `test ! -d .claude/skills/plugin-phases` -- directory is gone
2. `grep -r "skill: plugin-phases" .claude/commands/` returns nothing
3. `grep -r "plugin-phases" .claude/skills/ .claude/commands/` returns nothing
4. `grep "plugin-discuss" .claude/skills/plugin-workflow/SKILL.md` returns matches (content merged)
</verification>

<success_criteria>
- plugin-phases skill directory does not exist
- plugin-workflow SKILL.md contains all phase command definitions and unique plugin-phases content
- All 5 command files reference plugin-workflow
- No active .claude/ file references plugin-phases
</success_criteria>

<output>
After completion, create `.planning/phases/21-skill-infrastructure-consolidation/21-01-SUMMARY.md`
</output>
