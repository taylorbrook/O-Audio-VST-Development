---
phase: 21-skill-infrastructure-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agent-memory/dsp-agent.md
  - .claude/agent-memory/gui-agent.md
  - .claude/agent-memory/troubleshoot-agent.md
  - .claude/agent-memory/validation-agent.md
autonomous: false
requirements:
  - INFR-01

must_haves:
  truths:
    - "Each agent memory file contains specific, actionable patterns drawn from real project experience"
    - "No agent memory file contains the placeholder text 'No patterns recorded yet'"
    - "Memory files follow the same structure as research-planning-agent.md (Learned Patterns, Common Issues, Last Updated)"
  artifacts:
    - path: ".claude/agent-memory/dsp-agent.md"
      provides: "DSP implementation patterns from MEMORY.md, research docs, and project history"
      contains: "Learned Patterns"
    - path: ".claude/agent-memory/gui-agent.md"
      provides: "WebView/GUI patterns from MEMORY.md, research docs, and project history"
      contains: "Learned Patterns"
    - path: ".claude/agent-memory/troubleshoot-agent.md"
      provides: "Build troubleshooting patterns from MEMORY.md, research docs, and project history"
      contains: "Learned Patterns"
    - path: ".claude/agent-memory/validation-agent.md"
      provides: "Plugin validation patterns from MEMORY.md, research docs, and project history"
      contains: "Learned Patterns"
  key_links:
    - from: ".claude/hooks/inject-agent-memory.py"
      to: ".claude/agent-memory/*.md"
      via: "Hook reads memory files and injects into agent context"
      pattern: "agent-memory"
---

<objective>
Populate the 4 empty agent memory placeholder files with real seed patterns from project experience.

Purpose: The inject-agent-memory.py hook reads these files and injects them into agent context at runtime. Currently 4 of 5 memory files contain only "No patterns recorded yet" placeholders, making the injection useless. Seeding them with known patterns from MEMORY.md, research docs, and multi-plugin experience gives agents a warm start.

Output: 4 populated agent memory files with specific, actionable patterns.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-skill-infrastructure-consolidation/21-CONTEXT.md

<interfaces>
<!-- The gold standard memory file format (from research-planning-agent.md): -->

```markdown
# [Agent Name] Memory

## Learned Patterns
- [Plugin]: [Specific pattern with technical detail]
- [Plugin]: [Another pattern]

## Common Issues
- [Issue description with context]

## Last Updated
YYYY-MM-DD ([context])
```

<!-- Source patterns come from: -->
<!-- 1. MEMORY.md (user's auto-memory -- WebView2, canvas gotcha, ANIRA, JUCE 8 patterns) -->
<!-- 2. research/ docs (64 research documents covering DSP, GUI, troubleshooting) -->
<!-- 3. Multi-plugin project experience across 35+ plugins -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Curate and propose seed patterns for all 4 agent memory files</name>
  <files>
    .claude/agent-memory/dsp-agent.md
    .claude/agent-memory/gui-agent.md
    .claude/agent-memory/troubleshoot-agent.md
    .claude/agent-memory/validation-agent.md
  </files>
  <action>
Read the following sources to extract patterns relevant to each agent:
1. The user's MEMORY.md (loaded via project context -- contains WebView2, canvas gotcha, JUCE 8, ANIRA patterns)
2. `.claude/agent-memory/research-planning-agent.md` (the gold standard format to follow)
3. Research documents from `research/` directory -- scan titles and read the most relevant ones for each agent domain

For each of the 4 agents, curate 5-10 specific, actionable patterns. Per user decision: include high-impact one-off discoveries (canvas replaced element gotcha, WebView2 static linking) not just multi-plugin confirmed patterns. Assign each pattern to the appropriate agent.

**DSP Agent patterns** (from MEMORY.md + research):
- JUCE 8 getLatencySamples() is NOT virtual -- use setLatencySamples(N) in prepareToPlay()
- Wavetable mipmap anti-aliasing (from research-planning-agent.md -- also relevant to DSP)
- StateVariableTPTFilter: only LP/BP/HP natively; notch = LP+HP sum
- 24dB filter slopes: cascade two SVF instances, resonance first stage only
- polyBLEP for classic waveforms (near-zero-cost anti-aliasing)
- IS_SYNTH TRUE + NEEDS_MIDI_INPUT TRUE required for synth plugins
- ANIRA shared library distribution (embed dylibs in Frameworks/)
- Relevant patterns from DSP-focused research docs

**GUI Agent patterns** (from MEMORY.md + research):
- WebView2 static linking: MUST use JUCE_USE_WIN_WEBVIEW2_WITH_STATIC_LINKING=1
- Cross-platform URL schemes: juce:// on macOS, https:// on Windows
- Resource provider receives PATHS not full URLs -- direct equality checks
- Canvas replaced element gotcha: explicit width/height, not left+right
- DPR-aware backing store for Retina rendering
- WebView2 user data folder: always set withUserDataFolder() to temp dir
- WebView2 silent fallback to IE backend if construction fails
- 34/35 plugins missing NEEDS_WEBVIEW2 TRUE (audit finding)

**Troubleshoot Agent patterns** (from MEMORY.md + research):
- Plugin cache clearing sequence (AU cache, remove old, install fresh)
- WebView2 blank page diagnosis: check static vs dynamic linking
- WebView2 blank page diagnosis: check user data folder permissions
- stale plugin behavior: close DAW completely and restart
- ANIRA rpath and install_name_tool fixes for dylib distribution
- auval verification command for AU validation

**Validation Agent patterns** (from MEMORY.md + research):
- Build verification: ninja targets for VST3 + AU on macOS, VST3 only on Windows
- Parameter validation: check APVTS parameter IDs match between C++ and UI
- Cross-platform: verify both juce:// and https:// scheme handling
- Plugin audit pattern: NEEDS_WEBVIEW2 TRUE + static linking flags
- Check for IS_SYNTH/NEEDS_MIDI_INPUT in synth plugin CMakeLists.txt

Write all 4 files with the proposed content. Use the same format as research-planning-agent.md. Set Last Updated to 2026-03-07 (seed patterns from Phase 21).
  </action>
  <verify>
    Run: grep -c "No patterns recorded yet" .claude/agent-memory/*.md
    Expected: 0 occurrences (no placeholders remain)
    Run: for f in .claude/agent-memory/dsp-agent.md .claude/agent-memory/gui-agent.md .claude/agent-memory/troubleshoot-agent.md .claude/agent-memory/validation-agent.md; do echo "$f: $(grep -c '^\- ' "$f") patterns"; done
    Expected: Each file has 5+ patterns (bullet points)
  </verify>
  <done>All 4 memory files populated with real, specific patterns. No placeholder text remains. Each file has 5-10 actionable patterns matching the research-planning-agent.md format.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User review and approval of seed patterns</name>
  <files>
    .claude/agent-memory/dsp-agent.md
    .claude/agent-memory/gui-agent.md
    .claude/agent-memory/troubleshoot-agent.md
    .claude/agent-memory/validation-agent.md
  </files>
  <action>
CHECKPOINT: User reviews the 4 populated agent memory files for accuracy and completeness.

What was built: Populated 4 agent memory files (dsp-agent, gui-agent, troubleshoot-agent, validation-agent) with seed patterns curated from MEMORY.md, research docs, and project experience.

How to verify:
1. Review each file to confirm patterns are accurate and useful:
   - `cat .claude/agent-memory/dsp-agent.md`
   - `cat .claude/agent-memory/gui-agent.md`
   - `cat .claude/agent-memory/troubleshoot-agent.md`
   - `cat .claude/agent-memory/validation-agent.md`
2. Check that patterns are assigned to the correct agent (DSP patterns in dsp-agent, GUI patterns in gui-agent, etc.)
3. Verify no critical patterns are missing that you know about from experience
4. Compare format with the gold standard: `cat .claude/agent-memory/research-planning-agent.md`

Resume signal: Type "approved" to accept the seed patterns, or describe any changes needed (add/remove/move patterns between agents).
  </action>
  <verify>User confirms "approved" or provides feedback for revisions.</verify>
  <done>User has reviewed and approved all 4 agent memory seed pattern files.</done>
</task>

</tasks>

<verification>
1. `grep -rL "Learned Patterns" .claude/agent-memory/` returns no files (all have the section)
2. `grep -c "No patterns recorded yet" .claude/agent-memory/*.md` returns 0 for each file
3. Each file has 5+ specific bullet point patterns
4. User has approved the proposed patterns
</verification>

<success_criteria>
- All 4 placeholder memory files contain real, specific patterns
- Pattern format matches research-planning-agent.md (Learned Patterns, Common Issues, Last Updated)
- Patterns are accurate and drawn from real project experience
- User has reviewed and approved the seed patterns
</success_criteria>

<output>
After completion, create `.planning/phases/21-skill-infrastructure-consolidation/21-02-SUMMARY.md`
</output>
