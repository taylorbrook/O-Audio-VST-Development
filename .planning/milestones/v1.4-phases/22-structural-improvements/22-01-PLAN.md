---
phase: 22-structural-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/hooks/validators/validation-cache.py
  - .claude/utils/validation-cache.md
  - .claude/utils/validation-cache.sh
  - .claude/cache/validation-results.json
  - .claude/commands/clear-cache.md
  - .claude/agents/validation-agent.md
  - .claude/skills/plugin-workflow/references/precondition-checks.md
  - .claude/hooks/SessionStart.py
  - .claude/scripts/canary-test.sh
autonomous: true
requirements: [STRC-02, STRC-03]

must_haves:
  truths:
    - "No validation-cache files exist anywhere in the repository"
    - "No references to validation-cache remain in any skill, command, agent, or hook file"
    - "The /clear-cache command no longer exists"
    - "canary-test.sh does not exist -- only canary-test.py remains"
    - "SessionStart.py no longer references validation-cache.sh"
  artifacts:
    - path: ".claude/hooks/validators/validation-cache.py"
      provides: "DELETED"
    - path: ".claude/utils/validation-cache.md"
      provides: "DELETED"
    - path: ".claude/utils/validation-cache.sh"
      provides: "DELETED"
    - path: ".claude/cache/validation-results.json"
      provides: "DELETED"
    - path: ".claude/commands/clear-cache.md"
      provides: "DELETED"
    - path: ".claude/scripts/canary-test.sh"
      provides: "DELETED"
    - path: ".claude/agents/validation-agent.md"
      provides: "Updated -- validation-cache references removed"
    - path: ".claude/skills/plugin-workflow/references/precondition-checks.md"
      provides: "Updated -- validation-cache.sh source line removed"
    - path: ".claude/hooks/SessionStart.py"
      provides: "Updated -- validation-cache.sh reference removed"
  key_links:
    - from: ".claude/agents/validation-agent.md"
      to: "validation-cache.py"
      via: "Reference removal"
      pattern: "No remaining validation-cache references"
---

<objective>
Remove all dead validation cache infrastructure and duplicate canary test script.

Purpose: The validation cache system (3 files + command + references) was never activated and adds dead code. The canary-test.sh is a macOS-only duplicate of the cross-platform canary-test.py. Removing both cleans up the last dead infrastructure in the system.

Output: 6 files deleted, 3 files scrubbed of references, zero validation-cache or canary-test.sh artifacts remaining.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-structural-improvements/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete validation cache files and scrub all references</name>
  <files>
    .claude/hooks/validators/validation-cache.py
    .claude/utils/validation-cache.md
    .claude/utils/validation-cache.sh
    .claude/cache/validation-results.json
    .claude/commands/clear-cache.md
    .claude/agents/validation-agent.md
    .claude/skills/plugin-workflow/references/precondition-checks.md
    .claude/hooks/SessionStart.py
  </files>
  <action>
    Delete these 5 files entirely:
    1. `.claude/hooks/validators/validation-cache.py` (278-line cache manager -- never used)
    2. `.claude/utils/validation-cache.md` (documentation for dead system)
    3. `.claude/utils/validation-cache.sh` (173-line bash cache utility -- never sourced in practice)
    4. `.claude/cache/validation-results.json` (empty `{}` cache file)
    5. `.claude/commands/clear-cache.md` (slash command for dead cache system -- entire file)

    After deleting the 5 files, also delete the `.claude/cache/` directory if it is now empty.

    Then scrub references from these 3 files:

    6. `.claude/agents/validation-agent.md` -- Remove the validation cache section (lines referencing `validation-cache.py check`, `validation-cache.py store`). These are in the "Cache Integration" or similar section. Remove the cache check/store commands but keep the rest of the validation agent definition intact. Replace the cache integration section with a note: "Validation runs fresh each time (no caching layer)."

    7. `.claude/skills/plugin-workflow/references/precondition-checks.md` -- Remove the line `source .claude/utils/validation-cache.sh` (line 9) and any subsequent `is_cached` / cache-related calls in that file. Replace cache check logic with a direct comment: `# No caching -- run checks fresh each time`.

    8. `.claude/hooks/SessionStart.py` -- Remove lines 32-35 (the cache_file reference to validation-cache.sh and the comment about cache cleaning). These 4 lines do nothing (the pass statement skips it) but the reference itself is dead code.

    Use `git rm` for tracked files to ensure they are properly removed from git.
  </action>
  <verify>
    Run: `grep -r "validation.cache" .claude/ --include="*.py" --include="*.md" --include="*.sh" --include="*.json" 2>/dev/null | grep -v __pycache__ | wc -l` -- must return 0.
    Run: `ls .claude/hooks/validators/validation-cache.py .claude/utils/validation-cache.md .claude/utils/validation-cache.sh .claude/cache/ .claude/commands/clear-cache.md 2>&1 | grep -c "No such file"` -- must return 5 (all gone).
  </verify>
  <done>Zero validation-cache files exist. Zero references to validation-cache in any .claude/ file. The /clear-cache command no longer exists. SessionStart.py has no dead cache reference.</done>
</task>

<task type="auto">
  <name>Task 2: Delete duplicate canary-test.sh</name>
  <files>
    .claude/scripts/canary-test.sh
  </files>
  <action>
    The canary-test.sh (95 lines, macOS-only, hardcoded paths) is a strict subset of canary-test.py (174 lines, cross-platform, Windows support, relative paths). They test the same two plugins (O-SimpleReverb and O-AnalogEQ) with the same build/install/auval flow.

    Delete `.claude/scripts/canary-test.sh` using `git rm`.

    After deletion, verify canary-test.py still exists and is the single source of truth. Run a quick syntax check: `python3 -c "import ast; ast.parse(open('.claude/scripts/canary-test.py').read())"` to confirm the Python version parses cleanly.

    Search for any references to `canary-test.sh` elsewhere in the repo: `grep -r "canary-test.sh" .claude/ .planning/ 2>/dev/null`. If any references exist, update them to point to `canary-test.py` instead.
  </action>
  <verify>
    Run: `test ! -f .claude/scripts/canary-test.sh && test -f .claude/scripts/canary-test.py && echo "PASS"` -- must print PASS.
    Run: `grep -r "canary-test.sh" .claude/ .planning/ 2>/dev/null | wc -l` -- must return 0.
  </verify>
  <done>canary-test.sh is deleted. canary-test.py is the sole canary test script. No references to canary-test.sh remain anywhere.</done>
</task>

</tasks>

<verification>
1. `grep -r "validation.cache" .claude/ 2>/dev/null | grep -v __pycache__` returns empty
2. `ls .claude/scripts/canary-test.sh 2>&1` shows "No such file"
3. `ls .claude/scripts/canary-test.py` exists
4. `python3 -c "import ast; ast.parse(open('.claude/hooks/SessionStart.py').read())"` parses without error
5. `grep -r "canary-test.sh" . 2>/dev/null` returns empty
</verification>

<success_criteria>
- All 6 dead files deleted (3 validation-cache files, 1 empty cache file, 1 clear-cache command, 1 canary-test.sh)
- All 3 scrubbed files have zero remaining validation-cache references
- canary-test.py remains as sole canary test
- No grep hits for "validation.cache" or "canary-test.sh" in the repo
</success_criteria>

<output>
After completion, create `.planning/phases/22-structural-improvements/22-01-SUMMARY.md`
</output>
