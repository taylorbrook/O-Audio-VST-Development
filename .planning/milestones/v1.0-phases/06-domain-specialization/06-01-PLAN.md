---
phase: 06-domain-specialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/dsp-agent.md
  - .claude/critics/critic-dsp.md
autonomous: true

must_haves:
  truths:
    - "DSP agent rejects code with allocations in processBlock"
    - "DSP agent rejects std::function in processBlock path"
    - "DSP agent rejects locks and syscalls in processBlock"
    - "DSP critic has exhaustive violation checklist"
  artifacts:
    - path: ".claude/agents/dsp-agent.md"
      provides: "Real-time safety rules section"
      contains: "std::function"
    - path: ".claude/critics/critic-dsp.md"
      provides: "Expanded violation checklist"
      contains: "std::function"
  key_links:
    - from: ".claude/agents/dsp-agent.md"
      to: ".claude/critics/critic-dsp.md"
      via: "Shared real-time safety rules"
      pattern: "std::function.*REJECT"
---

<objective>
Encode exhaustive real-time safety rules into DSP agent and critic

Purpose: DSP agent and critic currently have basic real-time rules. This plan adds comprehensive coverage including std::function rejection, lambda capture analysis, and MessageManager patterns per CONTEXT.md decisions.

Output: Enhanced dsp-agent.md with zero-tolerance real-time rules and critic-dsp.md with expanded violation checklist
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-domain-specialization/06-CONTEXT.md
@.planning/phases/06-domain-specialization/06-RESEARCH.md
@.claude/agents/dsp-agent.md
@.claude/critics/critic-dsp.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance DSP agent with exhaustive real-time safety rules</name>
  <files>.claude/agents/dsp-agent.md</files>
  <action>
Add a new `<realtime_safety_rules>` section to dsp-agent.md with comprehensive violation patterns from RESEARCH.md:

1. **Memory Allocation (REJECT):**
   - `new`, `delete`, `malloc`, `calloc`, `realloc`, `free`
   - `std::make_unique`, `std::make_shared`
   - `std::vector::push_back`, `emplace_back`, `resize`
   - `std::string` operations (construction, concatenation, append)
   - `juce::String` operations in processBlock
   - `juce::Array` dynamic operations

2. **std::function (REJECT entirely):**
   - Any `std::function` usage in processBlock path
   - Type erasure may allocate on construction
   - Even small captures can exceed small object optimization threshold
   - No exceptions - reject all std::function in audio path

3. **Lambda Rules (Conditional):**
   - Capture-less lambdas `[]` - ALLOWED (compile to function pointers)
   - Lambdas with captures - REJECT unless:
     - Not passed through std::function
     - Capture is proven non-allocating (e.g., `[this]` or small primitives)
     - Lambda is created at compile time, not runtime

4. **Locks and Synchronization (REJECT):**
   - `std::mutex`, `std::recursive_mutex`
   - `std::lock_guard`, `std::unique_lock`, `std::scoped_lock`
   - `juce::ScopedLock`, `juce::CriticalSection`
   - `pthread_mutex_lock` or any platform lock primitives
   - Any blocking wait on condition variables

5. **System Calls and I/O (REJECT):**
   - `printf`, `fprintf`, `std::cout`, `std::cerr`
   - `DBG()` in release builds (allowed in debug only)
   - File operations: `fopen`, `fread`, `fwrite`, `juce::File`
   - Network operations, registry/plist access

6. **Exception Handling (REJECT):**
   - `throw` statements
   - `try/catch` blocks in processBlock
   - Any code that may throw (unchecked)

7. **Unbounded Operations (REJECT):**
   - `while` loops with external condition
   - Recursion without proven bounds
   - Algorithm calls with unbounded time complexity

8. **MessageManager Communication:**
   - REJECT: `MessageManager::callAsync` from audio thread
   - SUGGEST: Atomic + Timer polling pattern instead
   - SUGGEST: AsyncUpdater for infrequent updates (but warn about potential blocking)

9. **Pre-allocation Patterns (REQUIRED):**
   - All buffers allocated in prepareToPlay()
   - juce::ScopedNoDenormals at start of processBlock
   - Channel count validation before iteration
   - Zero-length buffer early exit

Add this section after `<workflow>` section. Reference CONTEXT.md decisions for zero-tolerance approach.
  </action>
  <verify>
grep -c "std::function" .claude/agents/dsp-agent.md should return 5+ occurrences
grep "REJECT.*std::function" .claude/agents/dsp-agent.md should match
grep "MessageManager::callAsync" .claude/agents/dsp-agent.md should match
  </verify>
  <done>
dsp-agent.md contains exhaustive real-time safety rules section with all violation patterns from RESEARCH.md
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance DSP critic with expanded violation checklist</name>
  <files>.claude/critics/critic-dsp.md</files>
  <action>
Update the Real-Time Safety category in critic-dsp.md to include comprehensive violation detection:

1. **Expand the checklist** to include all patterns from Task 1:
   - Add std::function detection as separate check item
   - Add lambda capture analysis check
   - Add MessageManager::callAsync detection
   - Add explicit regex patterns for detection

2. **Add new subsection: std::function Detection**
   ```markdown
   ### std::function Analysis (Zero Tolerance)
   - Search for `std::function` anywhere in processBlock call path
   - Even if defined outside processBlock, called from within = violation
   - Trace function calls from processBlock to identify hidden std::function usage
   ```

3. **Add new subsection: Lambda Capture Analysis**
   ```markdown
   ### Lambda Capture Rules
   - `[]` (capture-less): ALLOWED - compiles to function pointer
   - `[this]`: ALLOWED if not passed through std::function
   - `[&var]` or `[var]`: SUSPICIOUS - evaluate capture size
   - Any lambda passed to std::function: REJECT regardless of capture
   ```

4. **Add MessageManager pattern detection:**
   ```markdown
   ### Audio Thread to GUI Communication
   - REJECT: MessageManager::callAsync in processBlock path
   - ACCEPTABLE: Atomic + Timer polling pattern
   - ACCEPTABLE: Lock-free FIFO for complex data
   - CAUTION: AsyncUpdater may block on some platforms
   ```

5. **Update scoring criteria** to reflect zero-tolerance:
   - 10: No violations, all safety patterns present
   - 8-9: Minor warnings (DBG in debug builds)
   - 6-7: 1-2 violations that could cause glitches
   - 4-5: std::function found OR multiple allocation violations
   - 1-3: Locks in processBlock OR MessageManager::callAsync from audio thread

6. **Add detection regex patterns:**
   ```markdown
   **Regex patterns for automated detection:**
   - Allocation: `new\\s+\\w+`, `std::make_unique|std::make_shared`
   - std::function: `std::function\\s*<`
   - Lambda with capture: `\\[(?!\\])[^\\]]+\\]\\s*\\(`
   - Locks: `std::mutex|lock_guard|ScopedLock`
   - MessageManager: `MessageManager::callAsync`
   ```
  </action>
  <verify>
grep "std::function" .claude/critics/critic-dsp.md should match multiple times
grep "Lambda Capture" .claude/critics/critic-dsp.md should match
grep "MessageManager::callAsync" .claude/critics/critic-dsp.md should match
grep "Zero Tolerance" .claude/critics/critic-dsp.md should match
  </verify>
  <done>
critic-dsp.md has expanded violation checklist covering std::function, lambda captures, and MessageManager patterns with detection regex
  </done>
</task>

</tasks>

<verification>
1. dsp-agent.md contains `<realtime_safety_rules>` section with all 9 categories
2. critic-dsp.md Real-Time Safety category expanded with std::function, lambda, MessageManager checks
3. Both files reference each other's patterns for consistency
4. Zero-tolerance language present for critical violations
</verification>

<success_criteria>
- DSP agent has explicit std::function rejection rule
- DSP agent has lambda capture analysis guidance
- DSP agent has MessageManager communication patterns
- DSP critic has expanded detection checklist with regex patterns
- Both files aligned on what constitutes real-time violations
</success_criteria>

<output>
After completion, create `.planning/phases/06-domain-specialization/06-01-SUMMARY.md`
</output>
