---
phase: 13-maintenance-tooling-hardening
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - research/circuit-modeling-fundamentals.md
  - research/cross-platform-webview-best-practices.md
  - research/custom-fft-implementations.md
  - research/delay-effects-comprehensive-guide.md
  - research/dsp-click-prevention-debugging.md
  - research/fft-artifact-prevention.md
  - research/fft-processing-best-practices.md
  - research/generative-audio-algorithms-reference.md
  - research/generative-plugins-research-synthesis.md
  - research/microtonality-commercial-performance.md
  - research/microtonality-comprehensive-database.md
  - research/microtonality-implementation-juce.md
  - research/microtonality-theory-formats.md
  - research/modal-synthesis-bells-academic-research.md
  - research/multi-stage-decay-envelopes-comparison.md
  - research/O-Detune-market-research.md
  - research/physical-modeling-commercial-analog-modeling-ml-approaches.md
  - research/physical-modeling-research-agent-3-physical-modelling-optimization.md
  - research/reverb-comprehensive-research.md
  - research/spectral-sequencer-research.md
  - research/spectral-toolbox-synopses.md
  - research/spectral-transient-shaper-research.md
  - research/webgl-spectrogram-patterns.md
  - research/stutter-effects/path-a-granular-stutter-engine.md
  - research/stutter-effects/path-b-beat-repeater.md
  - research/stutter-effects/path-c-playhead-modulator.md
  - research/stutter-effects/stutter-effects-research-findings.md
  - .claude/resource-index.json
autonomous: true

must_haves:
  truths:
    - "Every research document (excluding README.md) has created, last_verified, and juce_version in its YAML frontmatter"
    - "The created date reflects when the document was first committed to git (historical accuracy)"
    - "The manifest regenerates successfully with all 27 documents indexed (0 skipped)"
    - "The frontmatter validator passes for every research document"
  artifacts:
    - path: ".claude/resource-index.json"
      provides: "Complete manifest with 27 documents including temporal fields"
      contains: "last_verified"
    - path: ".claude/frontmatter-issues.txt"
      provides: "Empty or deleted (all docs have valid frontmatter)"
  key_links:
    - from: "research/*.md frontmatter"
      to: ".claude/resource-index.json"
      via: "generate-resource-index.py extracts and propagates temporal fields"
      pattern: "last_verified.*juce_version"
---

<objective>
Retrofit all 27 research documents with the 3 new frontmatter fields (created, last_verified, juce_version), then regenerate the manifest to confirm full indexing with zero skipped files.

Purpose: MAINT-02 requires all research docs to have created date, last_verified date, and JUCE version fields. The created date must be historically accurate (from git log), not the retrofit date. After retrofit, the manifest generator (updated in Plan 01) should produce a complete index.

Output: All 27 research docs have 10-field frontmatter. Manifest rebuilt with all 27 docs indexed. Frontmatter issues file empty/deleted.

**Document count: 27.** This is the 23 root-level research docs + 4 stutter-effects docs listed in files_modified above. Excluded: research/stutter-effects/README.md (index file, not a research doc) and research/plugin-development-without-juce.md (untracked file). After retrofit, the manifest should report exactly 27 documents indexed.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-maintenance-tooling-hardening/13-RESEARCH.md
@.planning/phases/13-maintenance-tooling-hardening/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Determine historical created dates via git log</name>
  <files>
    (no files modified -- data gathering only)
  </files>
  <action>
    For each of the 27 research documents (23 root + 4 stutter-effects, excluding README.md and the untracked plugin-development-without-juce.md), determine the `created` date by running:

    ```bash
    git log --follow --format=%aI -- <filepath> | tail -1
    ```

    This gives the author date of the first commit that introduced each file. Extract just the date portion (YYYY-MM-DD) from the ISO datetime.

    Build a mapping of filepath -> created_date for use in Task 2.

    **Special cases:**
    - If a file has no git history (e.g., untracked), use today's date (2026-02-06)
    - If multiple stutter-effects files share the same commit date, that is acceptable (they were likely created together)
    - The `plugin-development-without-juce.md` file is untracked (`??` in git status) -- skip it for now. It will be naturally excluded from the manifest since it has no frontmatter. It can be added later when someone decides to track it.
  </action>
  <verify>
    Print the mapping to stdout. Verify:
    1. All 27 tracked research docs have a created date
    2. Dates look historically plausible (not all the same date unless files were batch-committed)
    3. No errors from git log commands
  </verify>
  <done>A mapping of 27 filepaths to their historical created dates exists for use in the frontmatter retrofit.</done>
</task>

<task type="auto">
  <name>Task 2: Add temporal fields to all research document frontmatter and regenerate manifest</name>
  <files>
    research/circuit-modeling-fundamentals.md
    research/cross-platform-webview-best-practices.md
    research/custom-fft-implementations.md
    research/delay-effects-comprehensive-guide.md
    research/dsp-click-prevention-debugging.md
    research/fft-artifact-prevention.md
    research/fft-processing-best-practices.md
    research/generative-audio-algorithms-reference.md
    research/generative-plugins-research-synthesis.md
    research/microtonality-commercial-performance.md
    research/microtonality-comprehensive-database.md
    research/microtonality-implementation-juce.md
    research/microtonality-theory-formats.md
    research/modal-synthesis-bells-academic-research.md
    research/multi-stage-decay-envelopes-comparison.md
    research/O-Detune-market-research.md
    research/physical-modeling-commercial-analog-modeling-ml-approaches.md
    research/physical-modeling-research-agent-3-physical-modelling-optimization.md
    research/reverb-comprehensive-research.md
    research/spectral-sequencer-research.md
    research/spectral-toolbox-synopses.md
    research/spectral-transient-shaper-research.md
    research/webgl-spectrogram-patterns.md
    research/stutter-effects/path-a-granular-stutter-engine.md
    research/stutter-effects/path-b-beat-repeater.md
    research/stutter-effects/path-c-playhead-modulator.md
    research/stutter-effects/stutter-effects-research-findings.md
    .claude/resource-index.json
  </files>
  <action>
    For each of the 27 research documents, insert 3 new frontmatter fields after `title` and before `summary`:

    ```yaml
    created: YYYY-MM-DD      # From git log (Task 1 mapping)
    last_verified: 2026-02-06  # Today's date (this retrofit is a verification act)
    juce_version: "8.0.4"     # Current JUCE version per MEMORY.md
    ```

    **Field ordering convention** (per RESEARCH.md): Place temporal fields after `title` and before `summary`. The resulting frontmatter order should be:
    1. title
    2. created
    3. last_verified
    4. juce_version
    5. summary
    6. domain
    7. type
    8. keywords
    9. stages
    10. agents

    **Important:** Use quotes around juce_version (`"8.0.4"`) for explicitness. Dates can be unquoted (YAML handles them natively as date objects, and the validator/generator handle both str and date types per Plan 01).

    **Do NOT modify** `research/stutter-effects/README.md` (index file, excluded from manifest per 10-03 decision) or `research/plugin-development-without-juce.md` (untracked file).

    After all 27 files are updated, regenerate the manifest:
    ```bash
    python3 .claude/scripts/generate-resource-index.py
    ```

    Expected outcome: Exactly 27 documents indexed, 0 skipped. The 27 count comprises the 23 root-level research docs + 4 stutter-effects docs listed in files_modified.

    Verify the frontmatter issues file is empty or deleted (meaning 0 skipped files).
  </action>
  <verify>
    1. Run the frontmatter validator on every research doc:
    ```bash
    for f in research/*.md research/stutter-effects/*.md; do
      [[ "$(basename "$f")" == "README.md" ]] && continue
      python3 .claude/hooks/validators/validate-research-frontmatter.py "$f"
      if [ $? -ne 0 ]; then echo "FAILED: $f"; fi
    done
    ```
    All should pass (exit 0).

    2. Regenerate and check manifest:
    ```bash
    python3 .claude/scripts/generate-resource-index.py
    python3 -c "import json; m=json.load(open('.claude/resource-index.json')); print(f'Documents: {len(m[\"documents\"])}'); assert len(m['documents']) == 27, f'Expected 27 documents, got {len(m[\"documents\"])}'; assert all('created' in d and 'last_verified' in d and 'juce_version' in d for d in m['documents']), 'Missing temporal fields!'; print('All 27 documents have temporal fields')"
    ```

    3. Check frontmatter issues file:
    ```bash
    cat .claude/frontmatter-issues.txt 2>/dev/null || echo "No issues file (good)"
    ```
    Should be empty or not exist.

    4. Spot-check a document to confirm field ordering:
    ```bash
    head -15 research/fft-processing-best-practices.md
    ```
    Should show title, created, last_verified, juce_version, summary in that order.
  </verify>
  <done>
    - All 27 research documents have 10-field frontmatter with historically accurate created dates
    - Manifest regenerated with exactly 27 documents indexed and temporal fields present
    - Frontmatter issues file is empty or deleted
    - Frontmatter validator passes for every document
  </done>
</task>

</tasks>

<verification>
1. `python3 .claude/scripts/generate-resource-index.py` reports exactly 27 documents indexed, 0 skipped
2. Validator passes for all research docs (batch check)
3. Manifest JSON contains `created`, `last_verified`, `juce_version` for every document entry
4. `created` dates vary across documents (not all the same -- proves historical accuracy)
5. `.claude/frontmatter-issues.txt` is empty or absent
</verification>

<success_criteria>
Every research document has complete 10-field YAML frontmatter with historically accurate creation dates. The manifest is fully regenerated with exactly 27 documents including temporal metadata. MAINT-02 is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/13-maintenance-tooling-hardening/13-02-SUMMARY.md`
</output>
