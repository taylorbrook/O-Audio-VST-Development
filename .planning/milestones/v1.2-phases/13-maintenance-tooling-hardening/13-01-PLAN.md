---
phase: 13-maintenance-tooling-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/resource-index.schema.json
  - .claude/scripts/generate-resource-index.py
  - .claude/hooks/validators/validate-research-frontmatter.py
  - .claude/scripts/discover-resources.py
autonomous: true

must_haves:
  truths:
    - "Generator produces manifest entries with created, last_verified, and juce_version fields"
    - "Validator rejects research docs missing any of the 10 required fields (including 3 new temporal fields)"
    - "Generator skips docs with missing required fields and writes per-file issue details to .claude/frontmatter-issues.txt"
    - "Generator uses atomic write pattern (temp file + os.replace) to prevent manifest corruption"
    - "discover-resources.py proceeds with empty results instead of crashing when jsonschema import fails"
    - "discover-resources.py returns empty results when resource-index.json is completely missing"
    - "generate-resource-index.py proceeds with empty results instead of crashing when jsonschema import fails"
  artifacts:
    - path: ".claude/resource-index.schema.json"
      provides: "DocumentEntry with created, last_verified, juce_version required fields"
      contains: "last_verified"
    - path: ".claude/scripts/generate-resource-index.py"
      provides: "Manifest generation with 3 new fields, atomic writes, bug report file, graceful degradation"
      contains: "os.replace"
    - path: ".claude/hooks/validators/validate-research-frontmatter.py"
      provides: "Validation of 10 required fields including date format and juce_version semver"
      contains: "last_verified"
    - path: ".claude/scripts/discover-resources.py"
      provides: "Graceful degradation on missing jsonschema and missing manifest file"
      contains: "return \\[\\]"
  key_links:
    - from: ".claude/resource-index.schema.json"
      to: ".claude/scripts/generate-resource-index.py"
      via: "Schema validates generator output"
      pattern: "jsonschema\\.validate"
    - from: ".claude/scripts/generate-resource-index.py"
      to: ".claude/frontmatter-issues.txt"
      via: "Generator writes skipped file details to bug report"
      pattern: "frontmatter-issues"
---

<objective>
Extend the resource index schema, generator, validator, and discovery script with three new temporal fields (created, last_verified, juce_version), add graceful degradation for jsonschema import failures, implement atomic manifest writes to prevent corruption from concurrent hook triggers, and create a trackable bug report file for documents with missing/malformed frontmatter.

Purpose: This is the foundation for all Phase 13 work. The schema/generator/validator must be updated atomically (per RESEARCH.md Pitfall 1 -- additionalProperties: false means any desync breaks validation). Graceful degradation fixes ensure the pipeline never crashes on missing dependencies.

Output: Updated schema, generator, validator, and discovery script. All 4 files modified in sync so existing 27-doc manifest can still be regenerated after frontmatter retrofit (Plan 02).
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-maintenance-tooling-hardening/13-RESEARCH.md

@.claude/resource-index.schema.json
@.claude/scripts/generate-resource-index.py
@.claude/hooks/validators/validate-research-frontmatter.py
@.claude/scripts/discover-resources.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend schema, generator, and validator with temporal fields + graceful degradation</name>
  <files>
    .claude/resource-index.schema.json
    .claude/scripts/generate-resource-index.py
    .claude/hooks/validators/validate-research-frontmatter.py
    .claude/scripts/discover-resources.py
  </files>
  <action>
    **ALL FOUR FILES MUST BE UPDATED IN ONE PASS** -- the schema has `additionalProperties: false`, so the generator will fail validation if schema and generator are out of sync.

    **Field naming clarification:** The CONTEXT.md locked decision lists "category" as a required field. This refers to the existing `domain` field in the schema (values: dsp, ui, build, workflow). No new "category" field is needed -- the CONTEXT.md's use of "category" maps to the existing `domain` field. The 5 mandatory fields from the user decision (created, last_verified, juce_version, keywords, category) correspond to schema fields: created, last_verified, juce_version, keywords, domain.

    **1. resource-index.schema.json:**
    - Add `created`, `last_verified`, `juce_version` to DocumentEntry `required` array (now 11 required fields)
    - Add property definitions for each:
      - `created`: `{"type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$", "description": "Date document was originally created (YYYY-MM-DD)"}`
      - `last_verified`: `{"type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$", "description": "Date content was last verified against current JUCE version (YYYY-MM-DD)"}`
      - `juce_version`: `{"type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$", "description": "JUCE version content was last verified against (e.g., 8.0.4)"}`
    - Place the 3 new properties after `title` and before `summary` in the properties object for logical grouping

    **2. generate-resource-index.py:**
    - Add `"created"`, `"last_verified"`, `"juce_version"` to `REQUIRED_FIELDS` list
    - In `build_document_entry()`: extract the 3 new fields from frontmatter, handling PyYAML date auto-parsing (isinstance check for datetime.date -- convert to .isoformat() string). Place them after title and before summary in the output dict. Import `from datetime import date` at top.
    - Handle `juce_version` with `str()` cast (PyYAML may parse "8.0.4" as string already, but be safe)
    - **Atomic write pattern**: Replace the direct `json.dump` to OUTPUT_PATH with: write to a NamedTemporaryFile in OUTPUT_PATH.parent, then `os.replace(temp_path, OUTPUT_PATH)`. Import `tempfile` and `os`. This prevents race conditions from concurrent hook invocations.
    - **Bug report file**: After building the manifest, if any files were skipped, write `.claude/frontmatter-issues.txt` with one line per skipped file in format: `[YYYY-MM-DD] relative/path.md: {reason}` (reason = "missing fields: X, Y, Z" or "invalid YAML" or "no frontmatter"). If no files were skipped, delete the issues file if it exists (`Path.unlink(missing_ok=True)`). The issues file path: `CLAUDE_DIR / "frontmatter-issues.txt"`.
    - **Graceful degradation for jsonschema**: Change lines 20-24 from `sys.exit(1)` to: set a module-level flag `HAS_JSONSCHEMA = False`, and in `generate_manifest()`, skip the validation step if jsonschema is not available (print warning to stderr: "Warning: jsonschema not available, skipping manifest validation"). The manifest is still written -- just unvalidated.

    **3. validate-research-frontmatter.py:**
    - Add `"created"`, `"last_verified"`, `"juce_version"` to `REQUIRED_FIELDS` set (now 10 fields)
    - Add date validation function `validate_date_field(fm, field_name, errors)` that handles both `datetime.date` objects (PyYAML auto-parsed) and strings (validate with `date.fromisoformat()`). Import `from datetime import date`.
    - Add `validate_juce_version(fm, errors)` that checks the value matches `^\d+\.\d+\.\d+$` pattern (cast to str first since PyYAML may return string)
    - Call both new validators from `validate_frontmatter()` after the existing field checks
    - Per user decision: warn per missing field so authors know exactly what to fix (the existing `missing` check already does this -- just ensure the 3 new fields appear in the missing set output)

    **4. discover-resources.py:**
    - Change lines 22-26 from `sys.exit(1)` on missing jsonschema to: set `HAS_JSONSCHEMA = False` flag, print warning to stderr. In `load_and_validate_manifest()`, if jsonschema is not available, skip the validation step (still load the manifest JSON, just don't validate it). Return the manifest. This satisfies MAINT-04 (graceful degradation).
    - Note: The existing `load_and_validate_manifest()` already returns `None` (which causes `discover()` to return `[]`) when `MANIFEST_PATH.exists()` is false (line 82-83). This correctly handles the missing manifest scenario per MAINT-04. No code change needed for that path, but the verify section will explicitly confirm this behavior.
  </action>
  <verify>
    Run the generator to confirm it still works with existing frontmatter (will produce a manifest with the new fields missing from existing docs, which means all docs will be skipped since they lack the 3 new fields -- this is expected and correct before Plan 02 retrofits them):
    ```bash
    python3 .claude/scripts/generate-resource-index.py
    ```
    The generator should:
    1. Run without errors
    2. Report 0 documents indexed (all skipped for missing fields)
    3. Create .claude/frontmatter-issues.txt listing all files with "missing fields: created, last_verified, juce_version"
    4. Still produce a valid (empty) manifest

    Validate the schema is valid JSON:
    ```bash
    python3 -c "import json; json.load(open('.claude/resource-index.schema.json'))"
    ```

    Verify atomic write pattern is implemented:
    ```bash
    grep 'os.replace' .claude/scripts/generate-resource-index.py
    ```
    Should match at least once (the os.replace call in the atomic write logic).

    Test the validator accepts existing frontmatter format (will fail on missing new fields -- expected):
    ```bash
    python3 .claude/hooks/validators/validate-research-frontmatter.py research/fft-processing-best-practices.md
    echo "Exit code: $?"
    ```
    Should exit 1 with errors listing missing fields.

    Test discover-resources.py graceful degradation -- jsonschema import failure:
    ```bash
    python3 -c "
    import sys
    sys.modules['jsonschema'] = None
    exec(open('.claude/scripts/discover-resources.py').read())
    " 2>&1 || echo "Script should not crash"
    ```

    Test discover-resources.py missing manifest behavior (MAINT-04):
    ```bash
    mv .claude/resource-index.json .claude/resource-index.json.bak 2>/dev/null; \
    python3 -c "
    import sys; sys.path.insert(0, '.claude/scripts')
    from discover_resources import discover
    results = discover(stage=2, agent_role='dsp')
    assert results == [], f'Expected empty list, got {results}'
    print('Missing manifest: correctly returned empty results')
    "; \
    mv .claude/resource-index.json.bak .claude/resource-index.json 2>/dev/null
    ```
    Should print "Missing manifest: correctly returned empty results".
  </verify>
  <done>
    - Schema requires 11 fields per DocumentEntry (8 original + created, last_verified, juce_version)
    - "category" from CONTEXT.md correctly maps to existing "domain" field (no new field added)
    - Generator extracts 3 new fields, handles date type coercion, uses atomic writes, writes frontmatter-issues.txt for skipped files, degrades gracefully without jsonschema
    - Validator checks 10 required fields with date format and semver validation
    - Discovery script degrades gracefully without jsonschema (returns unvalidated manifest) and with missing manifest (returns empty results)
    - All 4 files are in sync and the generator can produce a valid (possibly empty) manifest
  </done>
</task>

</tasks>

<verification>
1. `python3 .claude/scripts/generate-resource-index.py` runs without error
2. `python3 -c "import json; json.load(open('.claude/resource-index.schema.json'))"` succeeds
3. `grep 'os.replace' .claude/scripts/generate-resource-index.py` matches (atomic write confirmed)
4. `.claude/frontmatter-issues.txt` exists and lists skipped files with specific missing fields
5. `python3 .claude/hooks/validators/validate-research-frontmatter.py research/fft-processing-best-practices.md` exits 1 with clear error about missing created, last_verified, juce_version
6. `python3 -c "import json; m=json.load(open('.claude/resource-index.json')); print(len(m['documents']))"` shows 0 (all docs skipped pre-retrofit)
7. Missing manifest returns empty results from discover() (MAINT-04 verified)
</verification>

<success_criteria>
Schema, generator, validator, and discovery script are atomically updated with 3 new temporal fields. Generator uses atomic writes and produces a bug report file. Both generator and discovery degrade gracefully without jsonschema. Discovery returns empty on missing manifest. The "category" field from CONTEXT.md is clarified as the existing "domain" field. The system is ready for Plan 02 to retrofit frontmatter on all research docs.
</success_criteria>

<output>
After completion, create `.planning/phases/13-maintenance-tooling-hardening/13-01-SUMMARY.md`
</output>
