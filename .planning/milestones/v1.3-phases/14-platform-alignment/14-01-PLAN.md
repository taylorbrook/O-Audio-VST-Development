---
phase: 14-platform-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/dsp-agent.md
  - .claude/agents/research-planning-agent.md
  - .claude/agents/foundation-shell-agent.md
  - .claude/agents/gui-agent.md
  - .claude/agents/ui-design-agent.md
  - .claude/agents/ui-finalization-agent.md
  - .claude/agents/aesthetics-agent.md
  - .claude/agents/music-theory-agent.md
  - .claude/agents/troubleshoot-agent.md
  - .claude/agents/validation-agent.md
  - .claude/agents/polish-agent.md
  - .claude/critics/critic-dsp.md
  - .claude/critics/critic-ui.md
  - .claude/agent-profiles.json
autonomous: true

must_haves:
  truths:
    - "No agent or critic file contains a `model:` frontmatter field"
    - "No agent file contains `<extended_thinking>`, `<model_selection>`, or `<model_and_thinking>` XML sections"
    - "No agent file references `budget_tokens` as an API parameter"
    - "No agent or skill file prefills an assistant message (PLAT-03 — Opus 4.6 returns 400 on prefills)"
    - "agent-profiles.json exists with all 13 agents mapped to effort levels"
    - "dsp-agent and research-planning-agent are mapped to effort: max"
  artifacts:
    - path: ".claude/agent-profiles.json"
      provides: "Central effort profile configuration for all agents"
      contains: "dsp-agent.*max"
  key_links:
    - from: ".claude/agent-profiles.json"
      to: "All agent definitions"
      via: "Convention document — documents intended effort for each agent role"
      pattern: "profiles.*dsp-agent.*max"
---

<objective>
Remove all deprecated thinking configuration, model selection logic, and binary Sonnet/Opus switching from agent and critic definitions. Create the central agent-profiles.json effort configuration file.

Purpose: Satisfy PLAT-01 (adaptive thinking migration), PLAT-03 (zero assistant prefills — verify already satisfied), PLAT-04 (effort profiles replace model selection), and PLAT-07 (DSP/research agents always on Opus) for all agent/critic files.
Output: 13 cleaned agent/critic files + 1 new agent-profiles.json
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-platform-alignment/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip model frontmatter and deprecated thinking sections from all agents and critics</name>
  <files>
    .claude/agents/dsp-agent.md
    .claude/agents/research-planning-agent.md
    .claude/agents/foundation-shell-agent.md
    .claude/agents/gui-agent.md
    .claude/agents/ui-design-agent.md
    .claude/agents/ui-finalization-agent.md
    .claude/agents/aesthetics-agent.md
    .claude/agents/music-theory-agent.md
    .claude/agents/troubleshoot-agent.md
    .claude/agents/validation-agent.md
    .claude/agents/polish-agent.md
    .claude/critics/critic-dsp.md
    .claude/critics/critic-ui.md
  </files>
  <action>
    For each agent/critic file listed below, make these changes:

    **Frontmatter changes (all 13 files):**
    - Remove the entire `model: sonnet` or `model: opus` line from YAML frontmatter
    - Do NOT add an `effort:` field (Claude Code does not support it in frontmatter)
    - Leave all other frontmatter fields (name, description, tools, color) untouched

    **Body text changes (only files with deprecated sections):**

    **dsp-agent.md:**
    - Delete the entire `<model_selection>...</model_selection>` section (around line 221)
    - Delete the entire `<model_and_thinking>...</model_and_thinking>` section (around line 1225-1242)
    - Keep the surrounding content intact

    **research-planning-agent.md:**
    - Delete ALL `<extended_thinking>...</extended_thinking>` tag pairs (lines ~146-166, 170-180, 188-199, 224-233, 269-278, 295-309, 783-790)
    - KEEP the inner instruction text — just unwrap it from the tags
    - Example: `<extended_thinking>Think deeply to analyze...</extended_thinking>` becomes `Think deeply to analyze...`
    - Also remove the "extended thinking" reference at line ~980

    **troubleshoot-agent.md:**
    - Delete the "Use Extended Thinking" section (around lines 335-348)
    - Remove "extended thinking" references at lines ~699 and ~728
    - Keep surrounding diagnostic/analysis instructions intact

    **validation-agent.md:**
    - Remove `model: opus` from frontmatter
    - Update any body text that references "Opus model" or "model selection" — change to "Opus 4.6 with adaptive thinking"

    **polish-agent.md:**
    - No frontmatter change needed (has no model: field per research)
    - Check for any extended_thinking references in body text and remove if found

    **critic-dsp.md and critic-ui.md:**
    - Remove `model: opus` from frontmatter only
    - No body text changes expected

    **Verification grep after all changes:**
    ```bash
    grep -rn "^model:" .claude/agents/ .claude/critics/
    grep -rn "<extended_thinking>" .claude/agents/
    grep -rn "<model_selection>" .claude/agents/
    grep -rn "<model_and_thinking>" .claude/agents/
    grep -rn "budget_tokens" .claude/agents/
    ```
    All five greps should return zero results.
  </action>
  <verify>
    Run these commands — ALL must return empty (zero matches):
    ```bash
    grep -rn "^model:" .claude/agents/ .claude/critics/
    grep -rn "<extended_thinking>" .claude/agents/
    grep -rn "<model_selection>" .claude/agents/
    grep -rn "<model_and_thinking>" .claude/agents/
    grep -rn "budget_tokens" .claude/agents/ .claude/critics/
    ```
  </verify>
  <done>Zero agent or critic files contain model: frontmatter, extended_thinking tags, model_selection sections, model_and_thinking sections, or budget_tokens references.</done>
</task>

<task type="auto">
  <name>Task 2: Create agent-profiles.json central effort configuration</name>
  <files>
    .claude/agent-profiles.json
  </files>
  <action>
    Create `.claude/agent-profiles.json` with the exact structure from the research document:

    ```json
    {
      "_comment": "Agent effort profiles for Opus 4.6. All agents run on Opus 4.6 — effort level is the only tuning knob.",
      "_effort_levels": "max > high > medium > low",
      "_usage": "This is a convention/documentation file, not runtime config. Claude Code does not support per-agent effort in frontmatter. Effort is controlled at the API/session level. This file documents the INTENDED effort for each agent role.",
      "profiles": {
        "dsp-agent":                { "effort": "max",    "rationale": "Complex DSP algorithm design" },
        "research-planning-agent":  { "effort": "max",    "rationale": "Deep research and architecture" },
        "troubleshoot-agent":       { "effort": "max",    "rationale": "Complex debugging synthesis" },
        "validation-agent":         { "effort": "high",   "rationale": "Semantic judgment layer" },
        "music-theory-agent":       { "effort": "high",   "rationale": "Specialized domain reasoning" },
        "critic-dsp":               { "effort": "high",   "rationale": "Nuanced safety evaluation" },
        "critic-ui":                { "effort": "high",   "rationale": "Visual quality judgment" },
        "foundation-shell-agent":   { "effort": "medium", "rationale": "Template-based code generation" },
        "gui-agent":                { "effort": "medium", "rationale": "Follows established patterns" },
        "ui-design-agent":          { "effort": "medium", "rationale": "Creative but structured" },
        "ui-finalization-agent":    { "effort": "medium", "rationale": "Scaffolding generation" },
        "aesthetics-agent":         { "effort": "medium", "rationale": "Specification only" },
        "polish-agent":             { "effort": "medium", "rationale": "Follows plan directives" }
      }
    }
    ```

    Per user decision: dsp-agent and research-planning-agent MUST be "max". This is the central config the user can glance at to see all effort levels.
  </action>
  <verify>
    ```bash
    cat .claude/agent-profiles.json | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['profiles']['dsp-agent']['effort']=='max'; assert d['profiles']['research-planning-agent']['effort']=='max'; assert len(d['profiles'])==13; print('VALID: 13 profiles, DSP+research at max')"
    ```
  </verify>
  <done>agent-profiles.json exists with 13 agent profiles. dsp-agent and research-planning-agent are at max effort. All agents documented with effort level and rationale.</done>
</task>

</tasks>

<verification>
1. `grep -rn "^model:" .claude/agents/ .claude/critics/` returns nothing
2. `grep -rn "<extended_thinking>\|<model_selection>\|<model_and_thinking>" .claude/agents/` returns nothing
3. `grep -rn "budget_tokens" .claude/agents/ .claude/critics/` returns nothing
4. `.claude/agent-profiles.json` exists and validates as JSON with 13 profiles
5. dsp-agent and research-planning-agent both mapped to effort: max
6. `grep -rn "prefill\|role.*assistant.*content" .claude/agents/ .claude/critics/ .claude/skills/` returns nothing (PLAT-03 — confirms zero assistant message prefills)
</verification>

<success_criteria>
- All 13 agent/critic files have no model: frontmatter and no deprecated thinking sections
- agent-profiles.json is the single source of truth for effort configuration
- Zero assistant message prefills exist in any agent, critic, or skill file (PLAT-03)
- PLAT-01, PLAT-03, PLAT-04, and PLAT-07 are satisfied for agent/critic definitions
</success_criteria>

<output>
After completion, create `.planning/phases/14-platform-alignment/14-01-SUMMARY.md`
</output>
