---
phase: 14-platform-alignment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/O-AnalogSaturation/.planning/
  - plugins/O-MultiBandCompressor/.planning/
  - plugins/O-Comp/.planning/
  - plugins/O-Marimba/.planning/
  - plugins/O-DigiDelay/.planning/
  - plugins/O-AnalogEQ/.planning/
  - plugins/O-SimpleReverb/.planning/
  - plugins/O-Lyrica/.planning/
  - plugins/O-Polystutter/.planning/
  - plugins/O-Tremolo/.planning/
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "No plugin directory contains a .ideas/ subdirectory"
    - "No plugin directory contains a .continue-here.md file"
    - "All previously .ideas/ content now lives in .planning/ for each affected plugin"
    - ".gitignore prevents .ideas/ and .continue-here.md from reappearing"
  artifacts:
    - path: "plugins/O-SimpleReverb/.planning/"
      provides: "Migrated planning directory for canary plugin"
    - path: "plugins/O-AnalogEQ/.planning/"
      provides: "Migrated planning directory for WebView canary plugin"
    - path: ".gitignore"
      provides: "Prevention rules for legacy paths"
      contains: "plugins/*/.ideas/"
  key_links:
    - from: "plugins/*/.planning/"
      to: "Hook scripts (PreCompact.sh, UserPromptSubmit.sh)"
      via: "Directory path convention — scripts will reference .planning/ after Plan 04"
      pattern: ".planning/"
---

<objective>
Migrate all plugin content from legacy .ideas/ directories and .continue-here.md files to the standardized .planning/ directory structure. Add .gitignore rules to prevent legacy paths from reappearing.

Purpose: Satisfy PLAT-02 content migration prerequisite — content must exist at .planning/ paths before scripts are updated to reference them (Plan 04).
Output: 10 plugins with .planning/ directories (content migrated from .ideas/), 4 plugins with .continue-here.md content absorbed, .gitignore updated
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-platform-alignment/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate .ideas/ directories to .planning/ for all 10 plugins</name>
  <files>
    plugins/O-AnalogSaturation/.planning/
    plugins/O-MultiBandCompressor/.planning/
    plugins/O-Comp/.planning/
    plugins/O-Marimba/.planning/
    plugins/O-DigiDelay/.planning/
    plugins/O-AnalogEQ/.planning/
    plugins/O-SimpleReverb/.planning/
    plugins/O-Lyrica/.planning/
    plugins/O-Polystutter/.planning/
    plugins/O-Tremolo/.planning/
  </files>
  <action>
    For each of the 10 plugins that have `.ideas/` directories, migrate content to `.planning/`:

    **Standard migration (9 plugins — all except O-DigiDelay):**
    Plugins: O-AnalogSaturation, O-MultiBandCompressor, O-Comp, O-Marimba, O-AnalogEQ, O-SimpleReverb, O-Lyrica, O-Polystutter, O-Tremolo

    For each:
    ```bash
    mkdir -p plugins/{PLUGIN}/.planning
    # Copy all files from .ideas/ to .planning/, preserving structure
    cp -R plugins/{PLUGIN}/.ideas/* plugins/{PLUGIN}/.planning/
    # Remove old directory
    rm -rf plugins/{PLUGIN}/.ideas/
    ```

    **Merge migration (O-DigiDelay — has BOTH .ideas/ and .planning/):**
    ```bash
    # List both directories to understand what exists
    ls -la plugins/O-DigiDelay/.ideas/
    ls -la plugins/O-DigiDelay/.planning/
    # Copy .ideas/ files that DON'T already exist in .planning/
    # For any filename conflicts, keep the .planning/ version (newer)
    cp -Rn plugins/O-DigiDelay/.ideas/* plugins/O-DigiDelay/.planning/ 2>/dev/null || true
    rm -rf plugins/O-DigiDelay/.ideas/
    ```

    **File naming convention:** If .ideas/ contains files like `creative-brief.md`, they become `.planning/creative-brief.md` (or rename to `BRIEF.md` if following the convention of other plugins that already use .planning/). Check what naming convention the existing .planning/ plugins use and follow it consistently.

    **Important:** Use `git mv` where possible to preserve git history. If git mv fails (different directory structures), use cp + rm and note in the commit.

    After all moves, verify:
    ```bash
    # Should find zero .ideas/ directories
    find plugins/ -name ".ideas" -type d
    ```
  </action>
  <verify>
    ```bash
    # Zero .ideas/ directories remain
    find plugins/ -name ".ideas" -type d | wc -l

    # All 10 plugins now have .planning/
    for p in O-AnalogSaturation O-MultiBandCompressor O-Comp O-Marimba O-DigiDelay O-AnalogEQ O-SimpleReverb O-Lyrica O-Polystutter O-Tremolo; do
      [ -d "plugins/$p/.planning" ] && echo "OK: $p" || echo "MISSING: $p"
    done
    ```
    First command: 0. Second: all 10 show "OK".
  </verify>
  <done>All 10 plugins have .planning/ directories with migrated content. Zero .ideas/ directories remain anywhere in plugins/.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate .continue-here.md files and add .gitignore prevention rules</name>
  <files>
    plugins/O-MultiBandCompressor/.planning/STATUS.md
    plugins/O-AnalogEQ/.planning/STATUS.md
    plugins/O-SimpleReverb/.planning/STATUS.md
    plugins/O-Polystutter/.planning/STATUS.md
    .gitignore
  </files>
  <action>
    **Migrate .continue-here.md for 4 plugins:**
    Plugins: O-MultiBandCompressor, O-AnalogEQ, O-SimpleReverb, O-Polystutter

    For each:
    1. Read the `.continue-here.md` content
    2. Create or append to `.planning/STATUS.md` with the content (wrap in a "## Continuation Context" section if creating new)
    3. Delete the `.continue-here.md` file

    ```bash
    for p in O-MultiBandCompressor O-AnalogEQ O-SimpleReverb O-Polystutter; do
      if [ -f "plugins/$p/.continue-here.md" ]; then
        mkdir -p "plugins/$p/.planning"
        # If STATUS.md already exists, append
        if [ -f "plugins/$p/.planning/STATUS.md" ]; then
          echo -e "\n## Continuation Context (migrated from .continue-here.md)\n" >> "plugins/$p/.planning/STATUS.md"
          cat "plugins/$p/.continue-here.md" >> "plugins/$p/.planning/STATUS.md"
        else
          echo "## Continuation Context (migrated from .continue-here.md)" > "plugins/$p/.planning/STATUS.md"
          echo "" >> "plugins/$p/.planning/STATUS.md"
          cat "plugins/$p/.continue-here.md" >> "plugins/$p/.planning/STATUS.md"
        fi
        rm "plugins/$p/.continue-here.md"
      fi
    done
    ```

    **Add .gitignore rules:**
    Append to the project root `.gitignore`:
    ```
    # Legacy paths (migrated to .planning/ in Phase 14)
    plugins/*/.ideas/
    plugins/*/.continue-here.md
    ```

    Verify:
    ```bash
    find plugins/ -name ".continue-here.md" | wc -l
    grep ".ideas" .gitignore
    ```
  </action>
  <verify>
    ```bash
    # Zero .continue-here.md files remain
    find plugins/ -name ".continue-here.md" | wc -l

    # .gitignore has prevention rules
    grep "plugins/\*/.ideas/" .gitignore
    grep "plugins/\*/.continue-here.md" .gitignore

    # STATUS.md exists for all 4 plugins
    for p in O-MultiBandCompressor O-AnalogEQ O-SimpleReverb O-Polystutter; do
      [ -f "plugins/$p/.planning/STATUS.md" ] && echo "OK: $p" || echo "MISSING: $p"
    done
    ```
    First: 0. Second/Third: match found. Fourth: all 4 show "OK".
  </verify>
  <done>All 4 .continue-here.md files migrated to .planning/STATUS.md. .gitignore prevents legacy paths from reappearing. Zero .continue-here.md files remain.</done>
</task>

</tasks>

<verification>
1. `find plugins/ -name ".ideas" -type d | wc -l` returns 0
2. `find plugins/ -name ".continue-here.md" | wc -l` returns 0
3. All 10 affected plugins have `.planning/` directory with migrated content
4. `.gitignore` contains prevention rules for both `.ideas/` and `.continue-here.md`
5. No content was lost — all files moved, not deleted without replacement
</verification>

<success_criteria>
- All plugin content lives exclusively in .planning/ directories
- Legacy .ideas/ and .continue-here.md are fully eliminated
- .gitignore prevents regression
- PLAT-02 content migration is complete (scripts updated in Plan 04)
</success_criteria>

<output>
After completion, create `.planning/phases/14-platform-alignment/14-03-SUMMARY.md`
</output>
