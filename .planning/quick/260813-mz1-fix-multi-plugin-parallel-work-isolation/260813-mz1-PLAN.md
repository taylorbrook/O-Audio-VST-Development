---
phase: quick-260813-mz1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - CLAUDE.md
  - .planning/workflow/active-plugin.json
  - .planning/workflow/registry.json
  - .claude/commands/continue.md
  - .claude/commands/improve-milestone.md
  - .claude/commands/module-add.md
  - .claude/commands/module-remove.md
  - .claude/commands/module-upgrade.md
  - .claude/commands/plugin-focus.md
  - .claude/commands/plugin-handoff.md
  - .claude/commands/reconcile.md
  - .claude/skills/state-recovery/SKILL.md
  - .claude/skills/state-validation/SKILL.md
  - .claude/skills/improve-milestone/SKILL.md
  - .claude/skills/improve-milestone/BOUNDARIES.md
  - .claude/skills/improve-milestone/references/README.md
  - .claude/skills/improve-milestone/references/state-management.md
  - .claude/skills/improve-milestone/references/version-integration.md
autonomous: true
requirements: [TASK-1, TASK-2, TASK-3, TASK-4, TASK-5, TASK-6]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 63000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`git check-attr merge -- PLUGINS.md` reports `union` in the main worktree (TASK-1)"
    - "`CLAUDE.md` carries a `## Parallel Plugin Development` section covering branch-per-plugin, worktree-per-plugin, the `VST-development-<slug>` sibling naming rule, outside-the-repo placement, the shared-row PLUGINS.md rule, and the worktree add/remove commands (TASK-5)"
    - "Both retired focus-state JSON files are gone from disk AND from the git index (TASK-4)"
    - "No file under `.claude/` reads a retired focus-state file as a live source — every survivor either points at PLUGINS.md / plugin-local STATUS.md / modules/registry.yaml, or is explicitly marked retired (TASK-4)"
    - "On `feat/o-octagon`, the PLUGINS.md O-SpectralShaper row states the same version its CHANGELOG.md states (1.3.2 / 2026-07-07), and the CHANGELOG is byte-unchanged (TASK-2)"
    - "`git worktree list` reports exactly 2 worktrees and branch `improve/o-contrabass-v1.1` no longer exists (TASK-3)"
    - "SUMMARY.md carries an ahead/behind + contents report for the 4 stale unmerged branches, with no merge or delete performed on any of them (TASK-6)"
  artifacts:
    - /Users/taylorbrook/Dev/VST-development/.gitattributes
    - /Users/taylorbrook/Dev/VST-development/CLAUDE.md
    - /Users/taylorbrook/Dev/VST-development/.planning/quick/260813-mz1-fix-multi-plugin-parallel-work-isolation/260813-mz1-SUMMARY.md
  key_links:
    - ".gitattributes `merge=union` is the ONLY mechanism preventing parallel-branch PLUGINS.md collisions — if the attribute does not resolve, the primary fix is inert"
    - "Every `.claude/` reader redirect must land on a file that exists (PLUGINS.md, plugins/*/.planning/STATUS.md, modules/registry.yaml) — a dangling pointer silently breaks the command that reads it"
    - "feat/o-octagon PLUGINS.md row ↔ plugins/O-SpectralShaper/CHANGELOG.md — the registry must not claim a version the changelog does not"
---

<objective>
Close the multi-plugin parallel-work isolation gap: install the `merge=union` driver that stops PLUGINS.md from colliding across plugin branches, retire the dead `/plugin-focus` scoping layer that stopped being written on 2026-07-16, repair one false version bump on `feat/o-octagon`, remove one dead worktree, and document the parallel-work convention in CLAUDE.md.

Purpose: PLUGINS.md is a single global registry table that every plugin branch edits (123 edits in 3 months, most-contended non-plugin file). Parallel plugin branches collide there by construction. A row-scoped union merge driver resolves that at the git layer.

Output: `.gitattributes` (new), a `## Parallel Plugin Development` section in `CLAUDE.md`, a retired focus layer with all readers redirected, one corrected registry row on `feat/o-octagon`, one fewer worktree, and a stale-branch report in SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<verified_facts>
The diagnosis is COMPLETE. Do NOT re-investigate. Execute against these facts, all re-measured 2026-08-13 during planning:

**Root cause:** PLUGINS.md is a single global registry table (39 plugin rows, one row per plugin) that every plugin branch edits. No `.gitattributes` exists at the repo root. Parallel plugin branches collide there by construction.

**The regression:** `.planning/workflow/active-plugin.json` + `.planning/workflow/registry.json` (the `/plugin-focus` scoping layer) stopped being written 2026-07-16 and are still pinned to `O-simpleSampler`. `registry.json` holds 21 plugin entries against a 39-plugin repo, and its `focused` field reads `O-simpleSampler`.

**Branch hygiene is FINE.** Every branch touches exactly one `plugins/*` dir and was cut cleanly from main. Do NOT plan or perform any branch-structure fixes.

**Worktrees (measured, all three clean):**
| Path | Branch | State |
|------|--------|-------|
| /Users/taylorbrook/Dev/VST-development | improve/o-freqpulse-tooltip-measure | live, 1 ahead |
| /Users/taylorbrook/Dev/VST-development-octagon | feat/o-octagon | live, 13 ahead |
| /Users/taylorbrook/Dev/VST-contrabass-v1.1 | improve/o-contrabass-v1.1 | 0 ahead / 1 behind — fully merged, DEAD |

**CORRECTION to the briefed base SHA:** local `main` is at **`3b5205cf`** ("merge: integrate improve/o-contrabass-v1.1 — O-Contrabass v1.2.0 → v1.4.0"), NOT `fa38ff46`. `fa38ff46` is 3 commits behind current main. That merge is precisely why `improve/o-contrabass-v1.1` now reads 0 ahead. **Cut the new branch from the `main` ref, never from a hard-coded SHA.**

**CORRECTION to the briefed reader list (TASK-4):** the skills named in the brief (`plugin-context`, `context-resume`, `plugin-list`, `plugin-status`, `plugin-pause`, `plugin-resume`) contain **zero** references to either retired file — `plugin-context/SKILL.md` already treats plugin-local `STATUS.md` as authoritative. The measured reader set is 15 different files, listed in Task 2. Re-run the grep as the completeness gate; do not assume the briefed list.

**`registry.json` carries TWO unrelated concerns** — this drives the redirect targets:
1. Focus/plugin state (`focused`, `plugins.*`) → dead. Redirect to `PLUGINS.md` + `plugins/<Name>/.planning/STATUS.md`.
2. Module tracking (`modules`, `plugins.*.modules`) → superseded. The live source is `modules/registry.yaml` (v1.0.1, updated 2026-07-02, regenerated from disk truth by `scripts/regen-registry-used-by.sh`). Sampled `plugins.O-Contrabass.modules` in the JSON is an empty array, confirming it is stale. Redirect module readers to `modules/registry.yaml`.

**Both retired files are git-TRACKED** (`git ls-files` confirms). `.claude/` is tracked too (417 files). All deletions and edits must be committed.
</verified_facts>

<tasks>

<task type="tracer">
  <name>Task 1: Cut the infra branch, install the union merge driver, document the convention</name>
  <precondition>The main worktree at /Users/taylorbrook/Dev/VST-development has no modified TRACKED files (`git status --porcelain --untracked-files=no` is empty — the untracked `.planning/quick/260813-mz1-.../` planning dir is expected and is NOT a blocker), and branch `chore/parallel-work-isolation` does not yet exist. Halt if either is untrue.</precondition>
  <files>
    /Users/taylorbrook/Dev/VST-development/.gitattributes
    /Users/taylorbrook/Dev/VST-development/CLAUDE.md
  </files>
  <action>
Work in the MAIN worktree `/Users/taylorbrook/Dev/VST-development` only. It is currently on `improve/o-freqpulse-tooltip-measure`, a plugin branch. Infra work must NOT land on a plugin branch.

Step 1 — cut the branch. Run `git switch -c chore/parallel-work-isolation main`. Cut from the `main` REF, not from a SHA literal; the briefed `fa38ff46` is stale (see verified_facts). Record the resolved base SHA for SUMMARY.md.

Note on the planning artifacts: this PLAN.md was deliberately left UNCOMMITTED and untracked so it would not land on the plugin branch the main worktree currently sits on. Untracked files survive `git switch -c`, so the plan travels with you onto the infra branch. Do NOT commit it here — **Task 3 Step 3d owns writing SUMMARY.md and committing the whole planning directory** on `chore/parallel-work-isolation` as the 4th and final commit. It must never land on `improve/o-freqpulse-tooltip-measure`. Because that directory stays untracked until Step 3d, use `git status --porcelain --untracked-files=no` whenever you check for a clean tree before then.

Step 2 — create `.gitattributes` at the repo root (per TASK-1). No such file exists today. It contains a brief comment block (2-4 lines) followed by the rule mapping PLUGINS.md to the built-in union merge driver. The comment explains WHY: PLUGINS.md is a row-scoped global registry table, one row per plugin, edited by every plugin branch; a union merge resolves parallel-branch row edits without a conflict. `union` is a git built-in — no `merge.<driver>` config stanza is needed anywhere. Keep the file scoped to this single rule; do not add unrelated attribute rules.

Step 3 — commit `.gitattributes` alone as `chore(parallel-work): add union merge driver for PLUGINS.md registry table`.

Step 4 — add a `## Parallel Plugin Development` section to `CLAUDE.md` (per TASK-5). Insert it as a new top-level `##` section immediately BEFORE the existing `## Project Structure` section. This edit is PURELY ADDITIVE — every pre-existing line in CLAUDE.md stays byte-identical (the numstat gate in verify enforces zero deletions). The section covers all six required points:
  a. One branch per plugin, always cut from `main` — never from another plugin's feature branch.
  b. One worktree per concurrently-developed plugin.
  c. Naming convention: `VST-development-<slug>`, created as a SIBLING of the repo inside `~/Dev`. State plainly that `VST-contrabass-v1.1` broke this convention and was removed.
  d. Worktree directories live OUTSIDE the repo so they never appear as untracked paths in `git status`.
  e. PLUGINS.md is a shared registry, union-merged — only ever edit YOUR plugin's row. Note the tradeoff honestly: union merge trades a conflict for a possible DUPLICATE row when two branches touch the same hunk, so editing another plugin's row is what actually breaks it. Note also that the merge attribute is read from the working tree of the branch being merged INTO, so `main` must carry `.gitattributes` for the driver to apply.
  f. The concrete commands, as a fenced bash block: `git worktree add ../VST-development-<slug> -b <branch> main`, `git worktree list`, `git worktree remove ../VST-development-<slug>`, and `git branch -d <branch>`.

Step 5 — commit CLAUDE.md alone as `docs(parallel-work): document branch/worktree convention and shared PLUGINS.md rule`.

Leave the main worktree ON `chore/parallel-work-isolation` when done; record in SUMMARY.md that it was switched from `improve/o-freqpulse-tooltip-measure` and how to switch back.
  </action>
  <verify>
    <automated>
cd /Users/taylorbrook/Dev/VST-development
# Branch is correct
test "$(git rev-parse --abbrev-ref HEAD)" = "chore/parallel-work-isolation" || exit 1
# PRIMARY GATE: the attribute actually resolves (proves the driver, not just the text)
git check-attr merge -- PLUGINS.md | grep -q 'merge: union' || exit 1
# CLAUDE.md section present with all six required points
grep -c '^## Parallel Plugin Development' CLAUDE.md | grep -qx 1 || exit 1
grep -q 'VST-development-<slug>' CLAUDE.md || exit 1
grep -q 'git worktree add' CLAUDE.md || exit 1
grep -q 'git worktree remove' CLAUDE.md || exit 1
grep -q 'git worktree list' CLAUDE.md || exit 1
# CLAUDE.md edit is purely additive — zero deletions across the two commits
test "$(git diff main HEAD --numstat -- CLAUDE.md | cut -f2)" = "0" || exit 1
# Two atomic commits, each touching exactly one file
test "$(git show --name-only --format= HEAD~1)" = ".gitattributes" || { git show --name-only --format= HEAD~1; exit 1; }
test "$(git show --name-only --format= HEAD)" = "CLAUDE.md" || { git show --name-only --format= HEAD; exit 1; }
# Nothing was pushed: the infra branch has no remote-tracking ref
git rev-parse --verify --quiet origin/chore/parallel-work-isolation && { echo "BRANCH WAS PUSHED"; exit 1; }
echo "TASK 1 PASS"
    </automated>
  </verify>
  <done>Branch `chore/parallel-work-isolation` exists off current `main`; `git check-attr merge -- PLUGINS.md` resolves to `union`; CLAUDE.md gained a `## Parallel Plugin Development` section covering all six required points with zero deleted lines; two single-file atomic commits landed.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the dead focus layer and redirect every reader</name>
  <precondition>Task 1 completed and the main worktree HEAD is `chore/parallel-work-isolation`. `modules/registry.yaml` exists at the repo root (it is the redirect target for module readers).</precondition>
  <files>
    .planning/workflow/active-plugin.json (deleted)
    .planning/workflow/registry.json (deleted)
    .claude/commands/continue.md
    .claude/commands/improve-milestone.md
    .claude/commands/module-add.md
    .claude/commands/module-remove.md
    .claude/commands/module-upgrade.md
    .claude/commands/plugin-focus.md
    .claude/commands/plugin-handoff.md
    .claude/commands/reconcile.md
    .claude/skills/state-recovery/SKILL.md
    .claude/skills/state-validation/SKILL.md
    .claude/skills/improve-milestone/SKILL.md
    .claude/skills/improve-milestone/BOUNDARIES.md
    .claude/skills/improve-milestone/references/README.md
    .claude/skills/improve-milestone/references/state-management.md
    .claude/skills/improve-milestone/references/version-integration.md
  </files>
  <action>
Per TASK-4. Still on `chore/parallel-work-isolation` in the main worktree.

Step 1 — delete both retired state files with `git rm` (both are TRACKED; a plain `rm` leaves them staged in the index):
`git rm .planning/workflow/active-plugin.json .planning/workflow/registry.json`
Delete ONLY these two. Everything else under `.planning/workflow/` (`checkpoints/`, `schemas/`, `scripts/`, `templates/`, `module-deps.json`, `professional-quality-standards.md`) stays.

Step 2 — re-run the completeness grep yourself; it is the authoritative gate, not the list below:
`grep -rln "active-plugin\|registry\.json" .claude/`
As measured during planning it returns these 15 files, which is your starting worklist:
  commands: continue.md, improve-milestone.md, module-add.md, module-remove.md, module-upgrade.md, plugin-focus.md, plugin-handoff.md, reconcile.md
  skills: state-recovery/SKILL.md, state-validation/SKILL.md, improve-milestone/SKILL.md, improve-milestone/BOUNDARIES.md, improve-milestone/references/{README.md, state-management.md, version-integration.md}
If your grep returns a file not on this list, it is in scope too — update it.

Step 3 — redirect every hit by concern. The two concerns have DIFFERENT targets:
  - **Plugin focus / plugin state** (`focused`, `active-plugin.json`, `plugins.*` stage/phase/status, `activeMilestone`) → `PLUGINS.md` for the roster and status column, and `plugins/<Name>/.planning/STATUS.md` for per-plugin state. `.claude/skills/plugin-context/SKILL.md` already declares STATUS.md authoritative — match that wording rather than inventing a new contract.
  - **Module tracking** (`modules` section, `plugins.{plugin}.modules`) → `modules/registry.yaml`. This affects module-add.md, module-remove.md, module-upgrade.md, and improve-milestone.md. Note that `modules/registry.yaml` inverts the relation: it stores `used_by` per module rather than a `modules` array per plugin, and `scripts/regen-registry-used-by.sh` regenerates it from disk truth — reword the affected steps to match that shape instead of describing an array write that no longer exists.

For `reconcile.md`, the block validating a file against the retired active-plugin schema and the `registry.focused === active-plugin.plugin` cross-check both describe state that no longer exists — remove those checks rather than repointing them.

For `plugin-focus.md`, whose entire purpose is the retired scoping layer: TASK-4 permits marking the command as removed. Choose ONE and apply it consistently — either (a) rewrite it to focus via `PLUGINS.md` + STATUS.md with no JSON writes, or (b) mark the command retired at the top and strip the dead mechanics. Record which option you chose, and why, in SUMMARY.md.

Step 4 — every surviving textual mention of a retired filename must sit on a line that marks it as retired (use the word "retired", "removed", or "no longer" on that same line). Any mention NOT so marked is treated as a live dangling pointer and fails the gate. Leave no reader pointing at a deleted file.

Step 5 — commit as `chore(parallel-work): retire dead plugin-focus state layer, redirect readers to PLUGINS.md and modules/registry.yaml`. Include the two deletions and all `.claude/` edits in one commit so the removal and its redirects are never separately checked out.

Step 6 — report-only residual (do NOT delete): `.planning/workflow/schemas/active-plugin.schema.json` and `.planning/workflow/schemas/registry.schema.json` now describe deleted files. They are outside the authorized deletion scope — note them in SUMMARY.md as orphaned residuals for a future cleanup.
  </action>
  <verify>
    <automated>
cd /Users/taylorbrook/Dev/VST-development
# Files gone from disk AND from the index
test ! -e .planning/workflow/active-plugin.json || exit 1
test ! -e .planning/workflow/registry.json || exit 1
test -z "$(git ls-files .planning/workflow/active-plugin.json .planning/workflow/registry.json)" || exit 1
# Sibling contents of .planning/workflow/ survived
test -d .planning/workflow/schemas && test -d .planning/workflow/checkpoints || exit 1
test -f .planning/workflow/module-deps.json || exit 1
# COMPLETENESS GATE: no unmarked reference to a retired file survives anywhere under .claude/
# (marked = the same line says retired / removed / no longer)
# <!-- planner-discipline-allow: active-plugin -->
# <!-- planner-discipline-allow: registry.json -->
UNMARKED=$(grep -rn "active-plugin\|registry\.json" .claude/ | grep -viE "retired|removed|no longer" | wc -l | tr -d ' ')
test "$UNMARKED" = "0" || { echo "DANGLING REFS:"; grep -rn "active-plugin\|registry\.json" .claude/ | grep -viE "retired|removed|no longer"; exit 1; }
# Redirect targets exist and are actually referenced by the module readers
test -f modules/registry.yaml || exit 1
grep -q 'modules/registry.yaml' .claude/commands/module-add.md || exit 1
grep -q 'modules/registry.yaml' .claude/commands/module-remove.md || exit 1
grep -q 'modules/registry.yaml' .claude/commands/module-upgrade.md || exit 1
# Focus readers point at a file that exists
test -f PLUGINS.md || exit 1
grep -q 'PLUGINS.md' .claude/commands/continue.md || exit 1
# Deletions and redirects are in ONE commit
git show --name-only --format= HEAD | grep -q 'active-plugin.json' || exit 1
git show --name-only --format= HEAD | grep -q '.claude/' || exit 1
echo "TASK 2 PASS"
    </automated>
  </verify>
  <done>Both retired JSON files are absent from disk and index; the rest of `.planning/workflow/` is intact; `grep -rn "active-plugin\|registry\.json" .claude/` yields zero lines that are not explicitly marked retired; module readers resolve to the existing `modules/registry.yaml` and focus readers to the existing `PLUGINS.md`; one atomic commit carries the deletions plus every redirect; orphaned schemas recorded in SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the octagon registry row, remove the dead worktree, report stale branches</name>
  <precondition>The octagon worktree /Users/taylorbrook/Dev/VST-development-octagon and the contrabass worktree /Users/taylorbrook/Dev/VST-contrabass-v1.1 both report a clean `git status --porcelain`. Halt on either being dirty — do NOT use `git worktree remove --force`.</precondition>
  <files>
    /Users/taylorbrook/Dev/VST-development-octagon/PLUGINS.md
    (worktree removal — no file edit)
    .planning/quick/260813-mz1-fix-multi-plugin-parallel-work-isolation/260813-mz1-SUMMARY.md
  </files>
  <action>
Three independent sub-steps. None of them touch the `chore/parallel-work-isolation` branch.

**3a — Revert the false version bump on feat/o-octagon (TASK-2).**
Commit `fba35081` on `feat/o-octagon` bumped the O-SpectralShaper row in PLUGINS.md to `1.4.0 | ... | 2026-08-12`, but that branch's `plugins/O-SpectralShaper/CHANGELOG.md` still tops out at `[1.3.2] - 2026-07-07`. The registry claims a release the changelog does not.

Work IN the octagon worktree `/Users/taylorbrook/Dev/VST-development-octagon` (already checked out on `feat/o-octagon`) — do not check that branch out anywhere else.

Capture the CHANGELOG state BEFORE any edit:
`git show feat/o-octagon:plugins/O-SpectralShaper/CHANGELOG.md | head -5` — record the output, and its sha256, in SUMMARY.md.

Edit PLUGINS.md line 45 in that worktree, replacing exactly that one table row with:
`| O-SpectralShaper | 📦 Installed | 1.3.2 | Audio Effect (Spectral Transient Shaper) | 2026-07-07 |`
This restores the row to what `main` still carries. Touch NOTHING else in the file — not another row, not a heading, not the CHANGELOG. Do NOT revert commit `fba35081` wholesale; that commit carries unrelated octagon work.

Commit in that worktree as `fix(o-octagon): restore O-SpectralShaper registry row to 1.3.2 to match its CHANGELOG`.

Re-capture `git show feat/o-octagon:plugins/O-SpectralShaper/CHANGELOG.md | head -5` after the commit and confirm it is identical to the before-capture.

**3b — Remove the dead contrabass worktree (TASK-3).**
Re-confirm zero divergence FIRST: `git rev-list --count main..improve/o-contrabass-v1.1` must print `0`. Measured 0 during planning (it was merged into main at `3b5205cf`), but re-confirm — halt and report if it is anything other than 0.

Then, from the main worktree:
`git worktree remove /Users/taylorbrook/Dev/VST-contrabass-v1.1`
`git branch -d improve/o-contrabass-v1.1`   (use `-d`, never `-D` — `-d` refuses if unmerged, which is the safety net)

Leave the other two worktrees completely alone.

**3c — Stale-branch report, REPORT ONLY (TASK-6).**
Gather live counts (do not reuse planning-time numbers; they drift) for these four branches and write the table into SUMMARY.md:
  improve/o-spectralshaper-botanical, improve/o-spectralshaper-tooltips, improve/o-freqpulse-tooltip-measure, docs/logic-multichannel-dbap-research

For each, run `git rev-list --left-right --count main...<branch>` (left = behind main, right = ahead of main) and `git log --oneline main..<branch>` plus `git diff --stat main...<branch>`. For the two o-spectralshaper branches also record the top version in `git show <branch>:plugins/O-SpectralShaper/CHANGELOG.md | head -5` — the brief expects botanical at 1.4.0 and tooltips at 1.5.0; report what you actually measure and flag any mismatch.

Gather the data and STOP acting on those branches. Do NOT merge, rebase, delete, or push any of the four.

**3d — Write SUMMARY.md and commit the planning artifacts (the 4th and final commit).**
This step is the ONLY place the planning directory gets committed; nothing earlier commits it.

Return to the main worktree `/Users/taylorbrook/Dev/VST-development` and confirm you are on `chore/parallel-work-isolation` (`git rev-parse --abbrev-ref HEAD`). If you are not, switch to it — this commit must not land on `improve/o-freqpulse-tooltip-measure` or on `feat/o-octagon`.

Write `.planning/quick/260813-mz1-fix-multi-plugin-parallel-work-isolation/260813-mz1-SUMMARY.md` covering every item listed in this plan's `<output>` block, including the 3c stale-branch table you just gathered and the 3a before/after CHANGELOG capture.

Then stage the whole planning directory (PLAN.md and SUMMARY.md together — PLAN.md has been untracked for the entire run) and commit:
`git add .planning/quick/260813-mz1-fix-multi-plugin-parallel-work-isolation/`
`git commit -m "docs(quick-260813-mz1): plan and summary for parallel-work isolation fix"`

After this commit `git log --oneline main..HEAD` reads 4 commits: `.gitattributes`, `CLAUDE.md`, the focus-layer retirement, and this one.

**Push prohibition:** no `git push` anywhere in this plan, on any branch, in any worktree.
  </action>
  <verify>
    <automated>
cd /Users/taylorbrook/Dev/VST-development
# 3a — row corrected, and it matches what main carries
git show feat/o-octagon:PLUGINS.md | grep -q '^| O-SpectralShaper | 📦 Installed | 1.3.2 | Audio Effect (Spectral Transient Shaper) | 2026-07-07 |$' || exit 1
diff <(git show main:PLUGINS.md | grep '^| O-SpectralShaper |') <(git show feat/o-octagon:PLUGINS.md | grep '^| O-SpectralShaper |') || exit 1
# 3a — CHANGELOG untouched by the fix commit, still 1.3.2
git show feat/o-octagon:plugins/O-SpectralShaper/CHANGELOG.md | head -5 | grep -q '\[1.3.2\] - 2026-07-07' || exit 1
# 3a — the fix commit is EXACTLY one line changed in exactly one file
test "$(git show --numstat --format= feat/o-octagon)" = "$(printf '1\t1\tPLUGINS.md')" || { git show --numstat --format= feat/o-octagon; exit 1; }
# 3b — worktree and branch gone, others intact
test ! -d /Users/taylorbrook/Dev/VST-contrabass-v1.1 || exit 1
test "$(git worktree list | wc -l | tr -d ' ')" = "2" || { git worktree list; exit 1; }
test -z "$(git branch --list improve/o-contrabass-v1.1)" || exit 1
git worktree list | grep -q 'VST-development-octagon' || exit 1
# 3c — the four branches still exist, untouched (report-only)
for b in improve/o-spectralshaper-botanical improve/o-spectralshaper-tooltips improve/o-freqpulse-tooltip-measure docs/logic-multichannel-dbap-research; do
  git rev-parse --verify "$b" >/dev/null 2>&1 || { echo "MISSING (must not be deleted): $b"; exit 1; }
done
# No remote was pushed: the octagon fix commit is local-only, exactly 1 ahead of its remote-tracking ref
test "$(git rev-list --count origin/feat/o-octagon..feat/o-octagon)" = "1" || { echo "octagon push/divergence unexpected"; git rev-list --count origin/feat/o-octagon..feat/o-octagon; exit 1; }
# 3d — planning artifacts written and committed on the infra branch as the 4th commit
test "$(git rev-parse --abbrev-ref HEAD)" = "chore/parallel-work-isolation" || exit 1
D=.planning/quick/260813-mz1-fix-multi-plugin-parallel-work-isolation
test -f "$D/260813-mz1-SUMMARY.md" || exit 1
git ls-files --error-unmatch "$D/260813-mz1-SUMMARY.md" >/dev/null 2>&1 || { echo "SUMMARY.md not tracked"; exit 1; }
git ls-files --error-unmatch "$D/260813-mz1-PLAN.md" >/dev/null 2>&1 || { echo "PLAN.md not tracked"; exit 1; }
test -z "$(git status --porcelain "$D")" || { echo "planning dir not fully committed"; git status --porcelain "$D"; exit 1; }
test "$(git rev-list --count main..HEAD)" = "4" || { echo "expected 4 commits on infra branch"; git log --oneline main..HEAD; exit 1; }
# the 4th commit carries ONLY the planning dir
test -z "$(git show --name-only --format= HEAD | grep -v "^$D/")" || { git show --name-only --format= HEAD; exit 1; }
echo "TASK 3 PASS"
    </automated>
    <human-check>Confirm the SUMMARY.md stale-branch table lists all four branches with live ahead/behind counts and contents, and that no merge or delete was performed on any of them.</human-check>
  </verify>
  <done>`feat/o-octagon`'s O-SpectralShaper registry row reads 1.3.2 / 2026-07-07, byte-matching main's row, delivered by a commit whose numstat is exactly `1 1 PLUGINS.md`; that branch's CHANGELOG still reads `[1.3.2] - 2026-07-07` and was never staged; `git worktree list` shows 2 entries with the octagon worktree intact and branch `improve/o-contrabass-v1.1` deleted via `-d`; all four stale branches still exist and their live ahead/behind counts and contents are tabulated in SUMMARY.md with no merge, delete, or push performed; and SUMMARY.md plus PLAN.md are committed as a planning-dir-only 4th commit on `chore/parallel-work-isolation`, leaving that directory with no uncommitted or untracked residue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main worktree → octagon worktree | A commit intended for one branch landing on the other |
| local repo → remote origin | An unintended push publishing infra churn or a half-done revert |
| tracked-file deletion → skill/command readers | A deleted state file leaving live readers pointing at nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-mz1-01 | Tampering | Infra commits landing on `improve/o-freqpulse-tooltip-measure` instead of a fresh infra branch | high | mitigate | Task 1 Step 1 cuts `chore/parallel-work-isolation` from `main` BEFORE any edit; verify asserts `rev-parse --abbrev-ref HEAD` |
| T-mz1-02 | Tampering | Octagon PLUGINS.md edit spilling beyond the single O-SpectralShaper row | high | mitigate | Numstat gate asserts the fix commit is exactly `1 1 PLUGINS.md`; a wholesale revert of `fba35081` is explicitly forbidden |
| T-mz1-03 | Denial of Service | `git worktree remove` destroying unmerged work in the contrabass worktree | high | mitigate | Precondition requires a clean worktree; `rev-list --count main..` must be 0; `--force` forbidden; `git branch -d` (not `-D`) refuses an unmerged branch |
| T-mz1-04 | Tampering | Deleting the focus-state files while readers still point at them | high | mitigate | Task 2 completeness grep gate — zero unmarked references anywhere under `.claude/`; deletions and redirects ship in ONE commit |
| T-mz1-05 | Information Disclosure | Pushing infra or a partial revert to the PUBLIC origin repo | medium | mitigate | No push in any task; reflog gate asserts no push occurred |
| T-mz1-06 | Tampering | CLAUDE.md whole-file rewrite silently dropping existing build guidance | high | mitigate | Numstat gate asserts zero deleted lines in CLAUDE.md across the branch |
| T-mz1-07 | Tampering | Task 6 escalating from report to action (merging/deleting stale branches) | medium | mitigate | Existence gate asserts all four branches still resolve after the task |
| T-mz1-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan — nothing is fetched from any registry |
</threat_model>

<verification>
Run from `/Users/taylorbrook/Dev/VST-development` after all three tasks:

1. `git check-attr merge -- PLUGINS.md` → `PLUGINS.md: merge: union`
2. `git rev-parse --abbrev-ref HEAD` → `chore/parallel-work-isolation`
3. `git log --oneline main..HEAD` → 4 commits (gitattributes, CLAUDE.md, focus-layer retirement, planning artifacts)
4. `git status --porcelain --untracked-files=no` → empty in both remaining worktrees
5. `git ls-files .planning/workflow/` → neither retired JSON file listed
6. `grep -rn "active-plugin\|registry\.json" .claude/ | grep -viE "retired|removed|no longer"` → zero lines
7. `git worktree list` → 2 entries
8. `git show feat/o-octagon:PLUGINS.md | grep O-SpectralShaper` → `1.3.2 ... 2026-07-07`
9. `git branch --list` → `improve/o-contrabass-v1.1` absent; the four stale branches present
10. No `git push` ran anywhere
</verification>

<success_criteria>
- `.gitattributes` exists at the repo root and `git check-attr` resolves `PLUGINS.md` to the `union` merge driver (TASK-1)
- `feat/o-octagon`'s O-SpectralShaper registry row reads 1.3.2 / 2026-07-07 with its CHANGELOG provably untouched, via a 1-line-in-1-file commit (TASK-2)
- The contrabass worktree is gone and `improve/o-contrabass-v1.1` is deleted, with the other two worktrees intact (TASK-3)
- Both focus-layer JSON files are removed from disk and index, and every `.claude/` reader now points at PLUGINS.md, plugin-local STATUS.md, or `modules/registry.yaml` — zero dangling pointers (TASK-4)
- CLAUDE.md carries a `## Parallel Plugin Development` section covering all six required points, added with zero deleted lines (TASK-5)
- SUMMARY.md carries the four-branch stale-branch report, gathered live, with no merge/delete/push performed (TASK-6)
- SUMMARY.md and PLAN.md are committed together as a planning-dir-only 4th commit on `chore/parallel-work-isolation` (Task 3 Step 3d) — never on a plugin branch
</success_criteria>

<output>
Create `.planning/quick/260813-mz1-fix-multi-plugin-parallel-work-isolation/260813-mz1-SUMMARY.md` when done.

It MUST include:
- The resolved base SHA the infra branch was cut from (and the note that briefed `fa38ff46` was stale)
- The main worktree's original branch (`improve/o-freqpulse-tooltip-measure`) and the command to return to it
- Which option was taken for `plugin-focus.md` (rewritten vs marked retired) and why
- The full list of `.claude/` files actually updated, versus the 15 measured at planning time
- The note that the briefed skills (`plugin-context`, `context-resume`, `plugin-list`, `plugin-status`, `plugin-pause`, `plugin-resume`) contained zero references and needed no change
- The orphaned-schema residual (`active-plugin.schema.json`, `registry.schema.json`)
- The before/after CHANGELOG capture for O-SpectralShaper on `feat/o-octagon`
- The TASK-6 stale-branch table: branch, ahead, behind, contents summary, and CHANGELOG version for the two o-spectralshaper branches
</output>
