---
phase: quick-260908-a8w
plan: 01
type: execute
wave: 1
depends_on: []
subsystem: i18n
tags: [i18n, zh-Hans, glossary, corpus-hygiene, deferred-closure, O-GrainScatter, O-simpleGrain, O-simpleSampler]
autonomous: true
requirements: [ZHG-01, ZHG-02, ZHG-03]
files_modified:
  - scripts/i18n-zh-glossary.js
  - .planning/quick/260907-qda-stage-4-wave-4g-of-the-zh-hans-rollout-l/deferred-items.md

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`scripts/i18n-zh-glossary.js` renders `dist lpf` as the DISTANCE low-pass, agreeing with the only corpus site (O-GrainScatter)."
    - "`scripts/i18n-zh-glossary.js` renders `load your own` with a head noun, agreeing byte-for-byte with what both corpus sites (O-simpleGrain, O-simpleSampler) already ship."
    - "The repo-wide zh gate still passes at zero findings across 44 plugins, and its self-test still proves 10 of 10 rules fire."
    - "No file under `plugins/` is modified — the renderings shipped to users are byte-identical before and after."
    - "The 260907-qda ledger no longer carries I1 or the `'load your own'` G4 row as open work."
  artifacts:
    - scripts/i18n-zh-glossary.js
    - .planning/quick/260907-qda-stage-4-wave-4g-of-the-zh-hans-rollout-l/deferred-items.md
  key_links:
    - "TERMS root -> TERM_META.charCount: the count is COMPUTED at require time, so a changed root must move the derived count with it (7 for `load your own`, up from 5)."
    - "TERMS -> lint rule Z5: Z5 compels the root on every non-exempt row carrying that English. Both English keys' only corpus rows carry a termNote, so the root change moves zero findings — that invariance is the proof the change is corpus-safe."
    - "TERMS keys -> the lint's `Z6 coverage: N of 552 glossary terms` line: the key COUNT must not move, which is what proves a value was edited and not a key."
---

<objective>
Correct the two zh-Hans glossary roots that the wave-4f and wave-4g reviews proved wrong, and
re-prove the corpus against the corrected glossary.

Both roots are wrong in the same way: each was derived from ONE plugin's caption+title pair, which
looks measured (2 occurrences) but had no second site to check it against.

- `'dist lpf'` renders the *distortion* sense of the abbreviation. The only corpus site,
  O-GrainScatter, means **distance** — its own tooltip body reads "Distance LPF sets how much
  Distance darkens the cloud". Oldest open item in the ledger (4f D1 / 4g I1), carried by five waves
  because a shared-script edit is outside every per-plugin task's path scope.
- `'load your own'` ends on a possessive with **no head noun** — grammatically incomplete in
  Chinese. Both corpus sites already ship the corrected form with the head noun (4g G4 row 4).

Purpose: the glossary is the thing 44 plugins are linted against. A wrong root is a standing
instruction to render a control incorrectly, and it costs a per-entry `termNote` at every site that
declines it.

Output: two corrected values in `scripts/i18n-zh-glossary.js`, four green gates at their measured
baselines, and two ledger items closed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@scripts/i18n-zh-glossary.js
@scripts/i18n-zh-lint.js
@.planning/quick/260907-qda-stage-4-wave-4g-of-the-zh-hans-rollout-l/deferred-items.md
</context>

<preflight_facts>
Measured at planning time on `main` at `935a6c71`, working tree clean apart from
`.claude/agent-memory/research-planning-agent.md`. These are the BASELINE numbers every verify
below compares against — do not re-derive them, reproduce them.

**The two lines to change** (`scripts/i18n-zh-glossary.js`):

| line | key | current value | corrected value |
|---|---|---|---|
| 212 | `'load your own'` | `['载入自己的']` | `['载入自己的素材']` |
| 433 | `'dist lpf'` | `['失真低通']` | `['距离低通']` |

**Corpus site census — complete, both keys, `plugins/` only:**

- `Load your own` — 3 hits: `O-simpleGrain/.../index.html:181` (an `aria-label`, English, no zh),
  `O-simpleGrain/.../js/i18n.js:285` (en), `O-simpleSampler/.../js/i18n.js:221` (en). Both zh rows
  ship `载入自己的素材` at `reviewed:'bt'` and **both carry a `termNote`**.
- `Dist LPF` — 6 hits in O-GrainScatter and nowhere else: `index.html:910`, comments at
  `i18n.js:188` and `:1081`, a tooltip body at `:725`, `tip.distLpf` at `:785`, `label.distLpf` at
  `:1161`. Both zh rows ship `距离低通` at `reviewed:'bt'` and **both carry a `termNote`**.

**Why the change cannot move a lint finding.** `scripts/i18n-zh-lint.js:420-423` gates Z5 on
`!exempt`, and `exempt` is true for any row with a non-empty `termNote`. All four corpus rows are
exempt. So the corrected roots agree with rows the lint was already not checking — the finding total
must stay at zero, and if it does not, something unrelated moved and the run is a genuine RED.

**Neither key is in `BUDGETS`** (which holds exactly `depth`, `save`, `spread`), so Z6 is inert on
both and the 5-to-7 character growth on `'load your own'` cannot fire a budget finding.

**No comment inside `scripts/i18n-zh-glossary.js` names either superseded rendering**, and neither
key appears in `SAME_AS_EN`, `FORBIDDEN_IN_LABELS` or `FORBIDDEN_IN_PROSE`. There is no version or
date stamp in the file header that this edit invalidates: the header's "Regenerated live on
2026-09-01" statement describes the walk that measured WHICH strings are shared, not their
renderings.

**Baseline gate output, all four measured green:**

```
node scripts/i18n-zh-lint.js --self-test   ->  SELF-TEST: 10/10                (exit 0)
node scripts/i18n-zh-lint.js               ->  GATE PASSED — exit 0. 0 findings across 44 plugin(s).
                                               zh-Hans entries checked: 4329   plugins with findings: 0 / 44
                                               straight copies zh === en (info): 34   termNote exemptions (info): 74
                                               Z6 coverage: 3 of 552 glossary terms carry a measured budget
node scripts/check-i18n.js                 ->  ALL CHECKS PASS — 44 localized plugin(s)   (exit 0)
```

`node scripts/i18n-zh-lint.js` emits a `MODULE_TYPELESS_PACKAGE_JSON` warning about
O-TextureForge on stderr. It is pre-existing, unrelated, and not a finding.
</preflight_facts>

<tasks>

<task type="tracer">
  <name>Task 1: Correct both glossary roots and re-prove the whole corpus against them</name>
  <files>scripts/i18n-zh-glossary.js</files>
  <precondition>The working tree has no uncommitted change under `scripts/` or `plugins/` — `git status --short -- scripts plugins` prints nothing. If it does not, STOP: another session is mid-edit and a path-scoped commit here would carry its work.</precondition>
  <action>
Edit exactly two values in `scripts/i18n-zh-glossary.js`, changing no keys and adding no entries.

Line 212, key `'load your own'`: replace the single-element array so the rendering carries the head
noun the page loads — the same seven-character form both O-simpleGrain and O-simpleSampler already
ship (see the census table in `<preflight_facts>` for the exact target string). One accepted
rendering, not two: do not keep the superseded form as an alternate, because an alternate is for a
width-pinned cell and no cell here is pinned.

Line 433, key `'dist lpf'`: replace the single-element array with the DISTANCE reading — the exact
four-character form O-GrainScatter's two rows already ship. Again one member, not two: the
distortion reading is not a narrower alternate of the distance reading, it is a different control.

Add a trailing `//` comment to each corrected line, matching the house style already used on the
`'mid'` entry at line 169: name the corpus site that settled it and the sense it carries, and cite
this quick task's ID as the correction's provenance. **The comment must NOT spell either superseded
rendering.** Writing the old string into the comment would defeat the file-scoped absence check in
`<verify>` and would leave the wrong rendering greppable in the very file that is supposed to no
longer contain it. Name the sense in English ("the distortion reading", "the bare possessive")
instead.

Preserve the file's column alignment: values in `TERMS` line up in a column, so keep the existing
whitespace padding between the key and the value.

Change nothing else in the file. Do not touch `BUDGETS`, `SAME_AS_EN`, either forbidden table, the
header block, or the module-load assertions. Do not add a `charCount` beside either rendering — the
file computes it at require time on purpose (the "Derived, never stored" block at line 691), and a
written count is the mirrored constant this repo has already been bitten by.

**Do not touch any file under `plugins/`.** The four corpus rows already ship the corrected
renderings; they need no edit. Their `termNote` fields become obsolete-but-harmless — that
observation is Task 2's to record, not this task's to act on.

Then run the four gates and reproduce every baseline number in `<preflight_facts>`. The lint walk
over 44 plugins takes on the order of a minute; run it in the background if it approaches the
executor watchdog, and read the log rather than re-running.

Commit path-scoped, naming the one file explicitly. Never `git add -A`, never `git commit -a` —
another session shares this index and HEAD. Re-check `git branch --show-current` and
`git status --short` in the same step as the commit, not minutes earlier.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development && node -e "const g=require('./scripts/i18n-zh-glossary.js');const eq=(a,b)=>JSON.stringify(a)===JSON.stringify(b);if(!eq(g.TERMS['dist lpf'],['距离低通']))throw new Error('dist lpf root: '+JSON.stringify(g.TERMS['dist lpf']));if(!eq(g.TERMS['load your own'],['载入自己的素材']))throw new Error('load your own root: '+JSON.stringify(g.TERMS['load your own']));if(g.TERM_META['dist lpf'].charCount!==4)throw new Error('derived charCount dist lpf: '+g.TERM_META['dist lpf'].charCount);if(g.TERM_META['load your own'].charCount!==7)throw new Error('derived charCount load your own: '+g.TERM_META['load your own'].charCount);if(Object.keys(g.TERMS).length!==552)throw new Error('TERMS key count moved: '+Object.keys(g.TERMS).length);console.log('ROOTS OK, DERIVED OK, 552 KEYS')"</automated>
    <automated>cd /Users/taylorbrook/Dev/VST-development && test "$(grep -c '失真低通' scripts/i18n-zh-glossary.js)" = "0" && test "$(grep -c '距离低通' scripts/i18n-zh-glossary.js)" = "1" && test "$(grep -c '载入自己的素材' scripts/i18n-zh-glossary.js)" = "1" && echo "FILE-SCOPED RENDERING CENSUS OK"</automated>
    <automated>cd /Users/taylorbrook/Dev/VST-development && node scripts/i18n-zh-lint.js --self-test 2>&1 | tee /tmp/a8w-selftest.log | tail -3 && grep -q 'SELF-TEST: 10/10' /tmp/a8w-selftest.log && test "$(grep -c 'BROKEN' /tmp/a8w-selftest.log)" = "0" && echo "SELF-TEST 10/10, NO BROKEN RULE"</automated>
    <automated>cd /Users/taylorbrook/Dev/VST-development && node scripts/i18n-zh-lint.js > /tmp/a8w-lint.log 2>&1; echo "exit=$?" && grep -q 'GATE PASSED — exit 0. 0 findings across 44 plugin(s).' /tmp/a8w-lint.log && grep -q 'zh-Hans entries checked: 4329' /tmp/a8w-lint.log && grep -q 'plugins with findings: 0 / 44' /tmp/a8w-lint.log && grep -q 'termNote exemptions (info): 74' /tmp/a8w-lint.log && grep -q 'Z6 coverage: 3 of 552 glossary terms' /tmp/a8w-lint.log && echo "ZH GATE GREEN AT BASELINE"</automated>
    <automated>cd /Users/taylorbrook/Dev/VST-development && node scripts/check-i18n.js > /tmp/a8w-checki18n.log 2>&1; echo "exit=$?" && grep -q 'ALL CHECKS PASS — 44 localized plugin(s)' /tmp/a8w-checki18n.log && echo "MECHANISM GATE GREEN"</automated>
    <automated>cd /Users/taylorbrook/Dev/VST-development && node scripts/i18n-fr-lint.js > /tmp/a8w-frlint.log 2>&1; test $? -eq 0 && echo "FR LINT STILL EXIT 0"</automated>
    <automated>cd /Users/taylorbrook/Dev/VST-development && test -z "$(git diff HEAD~1 --name-only -- plugins/)" && test "$(git diff HEAD~1 --numstat -- scripts/i18n-zh-glossary.js | awk '{print $1, $2}')" = "2 2" && echo "TWO LINES IN ONE FILE, ZERO PLUGIN FILES"</automated>
  </verify>
  <done>
`scripts/i18n-zh-glossary.js` renders `dist lpf` as the distance low-pass and `load your own` with
its head noun; `TERM_META` reports the recomputed counts 4 and 7; `TERMS` still holds 552 keys; the
superseded distortion rendering is absent from that file; all four gates reproduce their baseline
numbers exactly (self-test 10/10 with no BROKEN line, `0 findings across 44 plugin(s)`, 4329 entries
checked, 74 termNote exemptions, `ALL CHECKS PASS — 44 localized plugin(s)`, fr lint exit 0); the
commit touches exactly one file with a 2-insertion / 2-deletion diff and no file under `plugins/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Close I1 and the G4 row in the 4g ledger, and record the four newly-obsolete termNotes</name>
  <files>.planning/quick/260907-qda-stage-4-wave-4g-of-the-zh-hans-rollout-l/deferred-items.md</files>
  <action>
Mark the two now-closed items in the wave-4g ledger, editing in place and **preserving every word of
the original finding** — the ledger is a history, so a closed item gains a resolution note, it does
not lose its body.

Locate each site by grepping a token, never by line number (this ledger states that convention in
its own preamble, and the numbers in `<preflight_facts>` are hours old already). Four sites:

1. **The G4 table row.** Grep the row that opens with the `'load your own'` key. Leave the row
   itself untouched and add a bolded resolution paragraph immediately AFTER the G4 table and BEFORE
   the paragraph that begins `**A shared-script edit is outside a per-plugin task's path scope**`.
   State: row 4 closed by this quick task's ID; the glossary root now carries the head noun and
   matches what both sites already shipped; the remaining rows of G4 are unchanged.

2. **The I1 section.** Grep the heading that begins `### I1. 4f D1`. Append a resolution line to the
   end of that section's body, before the next `###`. Careful: an evidence line placed past the next
   heading orphans itself and reads as belonging to I2. State that the root now reads the distance
   sense, that O-GrainScatter's two rows already shipped it, and that the corpus gate was re-proved
   green at the same baseline numbers rather than merely re-run.

3. **Next-wave item 2.** Grep the numbered item that begins `**I1 / 4f D1 — the wrong glossary
   root`. Prefix its text with a bolded resolution marker naming this quick task. It called for its
   own commit; that is what Task 1 gave it, so say so.

4. **Next-wave item 3.** Grep the numbered item that begins `**G4 — five more single-sited roots**`.
   The count is now one lower: reword it to four, and name the one that closed. Do not touch its
   surrounding argument about a glossary-level pass over every root with a corpus site count of 1 —
   that recommendation is unchanged and is still the highest-value remaining localization work.

Then add ONE new carry-forward item to the ledger, in the same house style (bold lead, a grep-able
token, no line numbers), recording the side effect Task 1 deliberately left in place: four
entry-scoped `termNote` fields — two in O-GrainScatter's `tip.distLpf` and `label.distLpf`, two in
O-simpleGrain's and O-simpleSampler's `loadSource` — were written to explain a divergence from a
root that no longer diverges. They are harmless to every gate (a termNote is INFO, never a finding)
but they are not inert: a termNote exempts its entry from BOTH Z5 and F1, so those four rows now sit
outside term coverage they would pass on merit. Note that O-GrainScatter's file header block also
narrates the exemption. Plugin files were held out of scope here on purpose — retiring the notes
means rewriting three authored header blocks for no rendering change — so this belongs to the
glossary-level pass that item 3 already calls for. Give it a grep-able token so the next wave can
find all four rows in one search.

Resolve marker, used verbatim at all four sites so one grep finds them: `RESOLVED 260908-a8w`.

**Edit only this one ledger file.** The 260907-qda and 260907-ja8 SUMMARY and TASK-SUMMARY files
also record these items as open. Those are sealed records of completed work — rewriting them would
be falsifying history, and the ledger is the live surface. Leave them.

Commit path-scoped, naming the one file. Re-check branch and staging in the same step as the commit.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development && L=.planning/quick/260907-qda-stage-4-wave-4g-of-the-zh-hans-rollout-l/deferred-items.md && test "$(grep -c 'RESOLVED 260908-a8w' "$L")" -ge 4 && echo "RESOLUTION MARKER AT >=4 SITES"</automated>
    <automated>cd /Users/taylorbrook/Dev/VST-development && L=.planning/quick/260907-qda-stage-4-wave-4g-of-the-zh-hans-rollout-l/deferred-items.md && test "$(grep -c 'five more single-sited roots' "$L")" = "0" && test "$(grep -c 'four more single-sited roots' "$L")" = "1" && echo "G4 COUNT DECREMENTED"</automated>
    <automated>cd /Users/taylorbrook/Dev/VST-development && L=.planning/quick/260907-qda-stage-4-wave-4g-of-the-zh-hans-rollout-l/deferred-items.md && awk '/^### I1\./{f=1;next} /^### /{f=0} f' "$L" | grep -q 'RESOLVED 260908-a8w' && echo "I1 RESOLUTION INSIDE ITS OWN SECTION, NOT ORPHANED"</automated>
    <automated>cd /Users/taylorbrook/Dev/VST-development && L=.planning/quick/260907-qda-stage-4-wave-4g-of-the-zh-hans-rollout-l/deferred-items.md && grep -q "载入自己的" "$L" && grep -q '距离低通' "$L" && grep -q 'termNote' "$L" && echo "ORIGINAL FINDING BODIES AND THE NEW CARRY-FORWARD BOTH PRESENT"</automated>
    <automated>cd /Users/taylorbrook/Dev/VST-development && test "$(git diff HEAD~1 --name-only | tr -d ' ')" = ".planning/quick/260907-qda-stage-4-wave-4g-of-the-zh-hans-rollout-l/deferred-items.md" && echo "LEDGER-ONLY COMMIT"</automated>
  </verify>
  <done>
The ledger carries `RESOLVED 260908-a8w` at the G4 table, inside the I1 section body, and on
next-wave items 2 and 3; item 3 now reads four rather than five and names the closed root; a new
carry-forward records the four obsolete-but-not-inert `termNote` exemptions with a grep-able token;
every original finding body survives verbatim; the commit touches that one file and nothing else.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared checkout -> git index | Another concurrent session's staged work can join a commit made here (shared `.git/index` and HEAD) |
| glossary data -> 44 plugin lint | `scripts/i18n-zh-glossary.js` is the single source every zh gate reads; a bad edit there is a suite-wide instruction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-a8w-01 | Tampering | git index shared across sessions | medium | mitigate | Path-scoped `git commit -- <file>` naming one file per task; branch + `git status --short` re-checked in the same step as each commit; `git diff HEAD~1 --name-only` asserted in `<verify>` after each commit |
| T-a8w-02 | Tampering | `scripts/i18n-zh-glossary.js` key set | high | mitigate | Module-load assertions run at require time; the 552-key count and the two exact root values are asserted by a `node -e` check, and the diff is pinned to `2 2` on one file |
| T-a8w-03 | Denial of Service | 44-plugin lint walk vs the executor watchdog | low | accept | Runs measured at planning time well inside the limit; action instructs backgrounding and reading the log if it approaches the ceiling |
| T-a8w-04 | Information Disclosure | shipped plugin renderings | low | accept | No file under `plugins/` is edited; asserted empty by `git diff HEAD~1 --name-only -- plugins/` |
| T-a8w-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan — nothing is installed, so the legitimacy gate has no surface |
</threat_model>

<verification>
1. `node -e` assertion on the required glossary module: both roots exact, both derived `charCount`
   values recomputed, key count still 552.
2. File-scoped rendering census on `scripts/i18n-zh-glossary.js`: the superseded distortion form
   absent, each corrected form present exactly once. Scoped to that one file deliberately — a
   repo-wide grep for the distortion form still returns 3 hits in O-GrainScatter's comment and two
   termNotes, which this plan does not touch.
3. `node scripts/i18n-zh-lint.js --self-test`: `SELF-TEST: 10/10` and zero `BROKEN` lines. This is
   the liveness evidence for Z5 — the self-test already proves Z5 fires on a violating rendering and
   is silenced by a `termNote`, so no injection into a plugin file is needed or wanted.
4. `node scripts/i18n-zh-lint.js`: exit 0 and every baseline number reproduced —
   `0 findings across 44 plugin(s)`, `4329` entries checked, `0 / 44` plugins with findings, `74`
   termNote exemptions, `3 of 552` Z6 coverage. The unchanged 74 and 552 are what prove no plugin
   file moved and no glossary key moved; the unchanged 0 is what proves the root change was
   corpus-safe.
5. `node scripts/check-i18n.js`: exit 0, `ALL CHECKS PASS — 44 localized plugin(s)`. This is also
   the syntax gate — it dynamically imports every table, so a malformed edit surfaces here.
6. `node scripts/i18n-fr-lint.js`: exit 0. The standing invariant is that BOTH repo-wide lints are
   green; the fr lint reads a different glossary and must be untouched by this change.
7. Diff shape asserted after each commit: one file, `2 2` for the glossary; ledger-only for Task 2.
</verification>

<success_criteria>
- `scripts/i18n-zh-glossary.js` renders `dist lpf` as the distance low-pass and `load your own` with
  a head noun, each as a single accepted rendering with a provenance comment that does not spell the
  superseded form.
- The corpus is re-proved, not merely re-run: all five baseline numbers reproduce exactly.
- Zero files under `plugins/` are modified; the renderings users see are byte-identical.
- The wave-4g ledger's oldest open item (I1) and G4 row 4 are closed, with a new carry-forward
  naming the four `termNote` exemptions the fix made obsolete.
- Two path-scoped commits on `main`, each touching exactly the file its task declares.
</success_criteria>

<output>
Create `.planning/quick/260908-a8w-fix-the-two-wrong-zh-glossary-roots-dist/260908-a8w-SUMMARY.md` when done.
</output>