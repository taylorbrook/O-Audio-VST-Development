---
phase: quick-260810-j78
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - QUICK-260810-j78
tags:
  - documentation
  - readme

must_haves:
  truths:
    - "A reader of README.md can click through to the oaudio.io product page for all 22 published plugins, not just the 15 that were already linked."
    - "The 7 newly published plugins (O-MicrotonalSampler, O-simpleAdditive, O-simpleFM, O-simpleGrain, O-simplePhysicalModelSynth, O-simpleBeatmaker, O-ReverseDelay) render as links in their existing table rows."
    - "The 17 plugins with no product page still render as plain unlinked names, so the README's own line 13 promise ('unlinked names build from source here') stays true."
    - "Every Description column in every README table is byte-identical to before the edit."
    - "The 15 pre-existing product links are untouched."
  artifacts:
    - "README.md — 7 rows in the Plugin column converted from plain code-span to linked code-span"
  key_links:
    - "Each new URL must exactly match the verified sitemap slug — a typo produces a 404 on the public landing page for the repo."
    - "Link markup must match the existing style [`O-Name`](url) with backticks INSIDE the link text — a mismatch renders the backticks literally."
---

<objective>
Add oaudio.io product-page links to the 7 newly published plugins in README.md's `## Plugins` section, matching the existing link style exactly.

Purpose: README.md is the public landing page for the repo. Line 13 tells readers that linked names go to a downloadable release and unlinked names build from source. That contract is currently stale for 7 plugins that now HAVE published pages — a reader looking for a download for O-ReverseDelay or the `simple` series is told, incorrectly, that no release exists.

Output: README.md with 22 linked plugin rows (up from 15), all Description columns unchanged.

**Tracer opt-out (deliberate):** this plan uses horizontal task shape rather than a leading `type="tracer"` slice. The "architecture" here is one markdown link pattern that is already proven 15 times in this exact file at this exact table position — a thin slice would add zero information and would split one 7-line edit across two commits. Task 1 IS the whole vertical.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@CLAUDE.md

**Verified URL table (ground truth — do NOT re-fetch the web, do NOT guess slugs).**
The orchestrator already fetched https://oaudio.io/sitemap.xml and confirmed each URL below returns HTTP 200 with a matching `<title>`:

| README plugin name | URL | Verified page title | Table |
|---|---|---|---|
| `O-MicrotonalSampler` | https://oaudio.io/products/ouaricon-microtonal-sampler | Ouaricon Microtonal Sampler | Instruments & Synths |
| `O-simpleAdditive` | https://oaudio.io/products/ouaricon-simple-additive | Ouaricon Simple Additive | `simple` series |
| `O-simpleFM` | https://oaudio.io/products/ouaricon-simple-fm | Ouaricon Simple FM | `simple` series |
| `O-simpleGrain` | https://oaudio.io/products/ouaricon-simple-grain | Ouaricon Simple Grain | `simple` series |
| `O-simplePhysicalModelSynth` | https://oaudio.io/products/ouaricon-simple-modeling-synth | Ouaricon Simple Modeling Synth | `simple` series |
| `O-simpleBeatmaker` | https://oaudio.io/products/ouaricon-simple-beatmaker | Ouaricon Simple Beatmaker | `simple` series |
| `O-ReverseDelay` | https://oaudio.io/products/ouaricon-reverse-delay | Ouaricon Reverse Delay | Effects & Processors |

Note the slug mismatch on row 5: the plugin is `O-simplePhysicalModelSynth` but the published slug is `ouaricon-simple-modeling-synth`. Use the slug from this table, not one derived from the plugin name.

**Out of scope:** the sitemap also lists `https://oaudio.io/products/ohands` — a separate product with no row in these tables. Do not add it.

**Execution note:** this run is SEQUENTIAL (worktree isolation disabled). Edit the main checkout directly.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Link the 7 newly published plugins in the README plugin tables</name>
  <files>README.md</files>
  <action>
In README.md's `## Plugins` section, convert exactly 7 rows from a plain code-span in the Plugin column to a linked code-span, using the URL table in `<context>`. Use the Edit tool with the full row as the match string so each replacement is unambiguous.

Markup style is fixed by the 15 rows already present in this file: an opening square bracket, then a backtick-wrapped plugin name, then a closing square bracket, then the URL in parentheses — backticks go INSIDE the link text, never outside. Copy the shape of the existing `O-Bells` row (Instruments table, first data row) verbatim and swap the name and URL.

The 7 rows to change, by table:
- Instruments &amp; Synths: the `O-MicrotonalSampler` row.
- The `simple` series: the `O-simpleAdditive`, `O-simpleFM`, `O-simpleGrain`, `O-simplePhysicalModelSynth`, and `O-simpleBeatmaker` rows. Note this table gains 5 of its 7 rows as links; only `O-simpleSubtractive` and `O-simpleSampler` keep plain names.
- Effects &amp; Processors: the `O-ReverseDelay` row.

Hard constraints:
- Change ONLY the Plugin column. Every Description column stays byte-identical, including its surrounding single spaces and the trailing pipe. Do not re-wrap, re-align, or re-pad any table.
- Do not reorder rows, do not add rows, do not remove rows. The edit is 7 lines replaced in place — 7 insertions and 7 deletions, nothing else.
- Do not touch the 15 rows that are already linked; their URLs were verified in an earlier task and are correct.
- Do not touch any other file. PLUGINS.md, source, and build files are out of scope for this plan.
- Do not modify the explanatory sentence above the tables that describes what linked versus unlinked names mean — it is already accurate and becomes MORE accurate after this edit.
  </action>
  <verify>
    <automated>test "$(grep -c '^| \[.O-' README.md)" = "22" &amp;&amp; echo "LINKED-ROWS-22-OK"</automated>
    <automated>for u in ouaricon-microtonal-sampler ouaricon-simple-additive ouaricon-simple-fm ouaricon-simple-grain ouaricon-simple-modeling-synth ouaricon-simple-beatmaker ouaricon-reverse-delay; do n=$(grep -cF "oaudio.io/products/$u)" README.md); [ "$n" = "1" ] || { echo "FAIL $u count=$n"; exit 1; }; done; echo "ALL-7-URLS-PRESENT-ONCE"</automated>
    <automated>[ "$(git diff --numstat -- README.md | awk '{print $1"/"$2"/"$3}')" = "7/7/README.md" ] || { git diff --numstat; echo "FAIL: expected exactly 7 insertions / 7 deletions in README.md only"; exit 1; }</automated>
    <automated>[ -z "$(git diff --name-only | grep -v '^README.md$')" ] || { echo "FAIL: files other than README.md are dirty"; git diff --name-only; exit 1; }</automated>
  </verify>
  <done>README.md has 22 linked plugin rows; each of the 7 new slugs appears exactly once; `git diff --numstat` reports exactly `7 7 README.md` and no other file is dirty.</done>
</task>

<task type="auto">
  <name>Task 2: No-regression audit — unlinked set intact and every description byte-identical</name>
  <files>README.md</files>
  <action>
Run the audit gates below against the committed state of Task 1 and confirm all three pass. This task adds a guard against the two ways a bulk table edit silently goes wrong: a row losing its Description column to re-alignment, and a plugin without a product page accidentally acquiring a link.

The description-integrity gate extracts the third pipe-delimited field from every table row in the file (which covers every table in README.md, not only the plugin tables) at the pre-edit commit and at HEAD, and diffs them. It must produce zero output. This is a stronger assertion than eyeballing the 7 changed rows: it proves no Description anywhere in the file moved.

The unlinked-set gate asserts each of the 17 plugins with no product page still matches the plain-name row shape. If a row were converted to a link, the two extra characters before the plugin name would break the match and the gate would fail.

If any gate fails, do not patch over it — restore README.md from the pre-edit commit and redo Task 1's edits one row at a time.

If all three gates pass, record the audit result in the plan SUMMARY. No file changes are expected from this task; it is a read-only gate.
  </action>
  <verify>
    <automated>BASE="$(git log -1 --format=%H -- README.md)^"; diff <(git show "$BASE:README.md" | awk -F'|' 'NF>=4{print $3}') <(awk -F'|' 'NF>=4{print $3}' README.md) &amp;&amp; echo "DESCRIPTIONS-BYTE-IDENTICAL"</automated>
    <automated>for p in O-Marimba O-Wind O-Reed O-Bowed O-Contrabass O-Bassoon O-Formant O-TextureForge O-Texture O-simpleSubtractive O-simpleSampler O-MultiBandCompressor O-Bass O-GrainScatter O-SpectralShaper O-Orbit O-Gain; do grep -q "^| .${p}. |" README.md || { echo "FAIL: $p no longer matches the plain unlinked row shape"; exit 1; }; done; echo "17-UNLINKED-INTACT"</automated>
    <automated>BASE="$(git log -1 --format=%H -- README.md)^"; [ "$(git show "$BASE:README.md" | wc -l)" = "$(wc -l < README.md)" ] &amp;&amp; echo "LINE-COUNT-UNCHANGED"</automated>
  </verify>
  <done>All three audit gates pass: every Description column is byte-identical to the pre-edit commit, all 17 unpublished plugins still render as plain names, and the file's total line count is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo README → public web | Every URL added here is a destination the repo sends an untrusted-to-us reader to. A wrong slug is a public 404; a wrong domain would be a redirect off the project's own property. |

No new dependencies, no package-manager installs, no executable code paths, and no user input are introduced by this plan, so the supply-chain (`T-*-SC`) row is not applicable.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-j78-01 | Spoofing | outbound product URLs in README.md | low | mitigate | Slugs are copied verbatim from the orchestrator's sitemap-verified table (each already confirmed HTTP 200 with a matching page title). Task 1 forbids re-fetching or deriving slugs from plugin names — the `ouaricon-simple-modeling-synth` slug is called out precisely because deriving it from `O-simplePhysicalModelSynth` would produce a wrong destination. |
| T-j78-02 | Tampering | unrelated README content | low | mitigate | Task 1's numstat gate pins the change to exactly 7 insertions / 7 deletions in README.md alone; Task 2 diffs every Description column and the total line count against the pre-edit commit. |
| T-j78-03 | Information disclosure | README.md content | low | accept | The file is already public and this plan adds only public product-page URLs. No secrets, paths, or internal hosts are introduced. |
</threat_model>

<verification>
Overall phase checks, run from the repo root after both tasks:

1. `grep -c '^| \[.O-' README.md` returns `22`.
2. `grep -c 'oaudio.io/products/' README.md` returns `22` — the 15 pre-existing plus the 7 new, with no stray product URL elsewhere in the file.
3. `git log --oneline -1 -- README.md` shows a single commit for this task, and `git show --stat HEAD` for it lists README.md only.
4. Rendered spot-check: the `simple` series table shows 5 of its 7 rows as links, with `O-simpleSubtractive` and `O-simpleSampler` still plain.
</verification>

<success_criteria>
- [ ] All 7 target rows link to their verified oaudio.io product page using the `[` + backticked-name + `](url)` style already used by the 15 existing links.
- [ ] `O-simplePhysicalModelSynth` points at the `ouaricon-simple-modeling-synth` slug, not a name-derived guess.
- [ ] The 17 plugins without product pages remain unlinked.
- [ ] Zero Description columns changed anywhere in README.md.
- [ ] Exactly one commit, touching README.md only, with a 7/7 numstat.
- [ ] No other file in the repo modified.
</success_criteria>

<output>
Create `.planning/quick/260810-j78-update-the-readme-with-links-to-the-oaud/260810-j78-SUMMARY.md` when done.
</output>
