---
phase: quick-260701-j3b
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/settings.json
  - .claude/hooks/inject-agent-memory.py
  - .claude/hooks/task-validator-dispatch.py
  - .claude/settings.local.json          # untracked / gitignored — verify on disk, do NOT commit
  - .claude/hooks/Stop.py                 # deleted
  - .claude/hooks/UserPromptSubmit.py     # deleted
autonomous: true
requirements: [BUG-02, BUG-03, BUG-04, IMP-01]
must_haves:
  truths:
    - "inject-agent-memory.py and task-validator-dispatch.py are wired to events the INSTALLED Claude Code (>= 2.1.197) supports, read the correct payload fields, and print an env-gated debug echo proving they execute."
    - "The SubagentStart agent-memory matcher matches exactly the agents that have a .claude/agent-memory/*.md file (dorico-agent included, research-lead excluded)."
    - "The auto-approve allow-list no longer grants blanket recursive deletion; git/cd allows are subcommand/path scoped."
    - "No stale/dead orphan hook scripts remain in .claude/hooks/; the referenced CLI utilities merge-critic-reports.py and detect-research-conflicts.py are retained."
  artifacts:
    - .claude/settings.json
    - .claude/hooks/inject-agent-memory.py
    - .claude/hooks/task-validator-dispatch.py
    - .claude/settings.local.json
  key_links:
    - "settings.json SubagentStart matcher <-> .claude/agent-memory/<agent>.md filenames (1:1 for firing agents)"
    - "inject-agent-memory.py reads agent_type <-> SubagentStart payload field"
    - "task-validator-dispatch.py reads task_subject/task_description <-> TaskCompleted payload fields"
    - ".claude/agents/critic-orchestrator.md <-> merge-critic-reports.py (retained CLI utility reference)"
    - ".claude/agents/research-lead.md:64 <-> detect-research-conflicts.py (retained CLI utility reference; also research-team-protocol.md:57)"
---

<objective>
Fix .claude hook wiring and settings hygiene per REVIEW.md Section A (BUG-02, BUG-03, BUG-04, IMP-01).

CRITICAL PREMISE CORRECTION (verified during planning — executor MUST honor this):
The review's BUG-02 claims inject-agent-memory.py (SubagentStart) and task-validator-dispatch.py
(TaskCompleted) are wired to "non-existent" events. That reflects the OLD 9-event Anthropic hooks
docs. The INSTALLED Claude Code is >= 2.1.197 (2.1.198 observed during planning), whose binary registers `executeSubagentStartHooks` and
contains `SubagentStart` (31 occurrences), `TaskCompleted` (35), `TaskCreated`, plus the exact
payload fields these hooks read: `agent_type` (SubagentStart) and `task_subject`/`task_description`
(TaskCompleted — the binary literally documents "JSON with task_id, task_subject, task_description,
teammate_name, and team_name"). Therefore both hooks are wired to REAL events with CORRECT input
contracts. Rewiring task-validator-dispatch to SubagentStop (which provides agent_type/transcript,
NOT task_subject/task_description) or inject-agent-memory to PreToolUse/SessionStart (no agent_type)
would BREAK the input contracts and permanently no-op the hooks — a regression. So BUG-02 is handled
as VERIFY + INSTRUMENT, NOT rewire.

Purpose: Restore trust in the .claude automation surface — the real defects are the agent-memory
matcher mismatch (BUG-04), a dangerous auto-approve entry (BUG-03), and stale orphan scripts
(IMP-01). BUG-02 is resolved by proving the existing wiring fires under the installed build (>= 2.1.197).
Output: Corrected settings.json matcher, env-gated debug instrumentation on both event hooks,
a de-fanged settings.local.json allow-list, and a cleaned .claude/hooks/ directory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md
@.claude/settings.json
@.claude/hooks/inject-agent-memory.py
@.claude/hooks/task-validator-dispatch.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify + instrument the two event hooks; fix the agent-memory matcher (BUG-02 + BUG-04)</name>
  <files>.claude/settings.json, .claude/hooks/inject-agent-memory.py, .claude/hooks/task-validator-dispatch.py</files>
  <action>
DO NOT change the event names in settings.json. Leave the `SubagentStart` block (inject-agent-memory.py)
and the `TaskCompleted` block (task-validator-dispatch.py) intact — both are valid events in the
installed Claude Code (>= 2.1.197) and the hooks already read their correct payload fields (agent_type;
task_subject/task_description). Confirm the installed version first with `claude --version` and record
the ACTUAL version in the SUMMARY (expect >= 2.1.197 — 2.1.198 was observed during planning; do NOT
stall on an exact-match mismatch). If the installed version is OLDER than the version where SubagentStart
was introduced (i.e. `claude --version` reports a build whose binary lacks `executeSubagentStartHooks`),
STOP and flag — only then would a rewire be warranted; otherwise proceed as below.

BUG-04 (matcher fix): In settings.json, edit the SubagentStart matcher (currently
`troubleshoot-agent|dsp-agent|gui-agent|research-planning-agent|validation-agent|research-lead`) to add
`dorico-agent` (its memory file .claude/agent-memory/dorico-agent.md exists) and remove `research-lead`
(no .claude/agent-memory/research-lead.md exists; the real research agent is research-planning-agent,
already listed). Final matcher: `troubleshoot-agent|dsp-agent|gui-agent|research-planning-agent|validation-agent|dorico-agent`.
Do not touch any other block (SessionStart, PostToolUse, PreCompact, SubagentStop, enabledPlugins).

Instrumentation (satisfies "verify each fires with a debug echo"): Add an env-gated debug echo to BOTH
hooks, guarded by the CLAUDE_HOOK_DEBUG environment variable, printed to STDERR only.
- inject-agent-memory.py: after agent_type is parsed, if CLAUDE_HOOK_DEBUG is set, write a one-line
  stderr diagnostic naming the event, the resolved agent_type, and whether a matching memory file was
  found. CRITICAL: the echo MUST go to sys.stderr, never stdout — stdout is reserved for the
  hookSpecificOutput JSON contract and any pollution corrupts the additionalContext payload.
- task-validator-dispatch.py: immediately after task_content is assembled (before the non-code skip),
  if CLAUDE_HOOK_DEBUG is set, write a one-line stderr diagnostic naming the event and a short prefix
  of task_content. Normal (unset) runs must remain silent so no per-event noise is added.

Keep both hooks fail-safe (never raise, never block on unexpected input) exactly as they are today.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development && claude --version && python3 -c "import json;d=json.load(open('.claude/settings.json'));m=d['hooks']['SubagentStart'][0]['matcher'];assert 'dorico-agent' in m and 'research-lead' not in m, m;assert d['hooks']['SubagentStart'][0]['hooks'][0]['command'].endswith('inject-agent-memory.py');assert d['hooks']['TaskCompleted'][0]['hooks'][0]['command'].endswith('task-validator-dispatch.py');print('settings OK:',m)" && printf '%s' '{"agent_type":"dsp-agent","hook_event_name":"SubagentStart"}' | CLAUDE_HOOK_DEBUG=1 python3 .claude/hooks/inject-agent-memory.py 1>/tmp/j3b_inject.out 2>/tmp/j3b_inject.err; python3 -c "import json;print('dsp additionalContext present:', 'additionalContext' in (json.load(open('/tmp/j3b_inject.out')) if open('/tmp/j3b_inject.out').read().strip() else {}))" && test -s /tmp/j3b_inject.err && echo "dsp debug echo -> stderr OK" && printf '%s' '{"agent_type":"dorico-agent"}' | CLAUDE_HOOK_DEBUG=1 python3 .claude/hooks/inject-agent-memory.py | python3 -c "import sys,json;print('dorico additionalContext present:', 'additionalContext' in (json.loads(sys.stdin.read() or '{}')))" && printf '%s' '{"agent_type":"research-lead"}' | CLAUDE_HOOK_DEBUG=1 python3 .claude/hooks/inject-agent-memory.py 1>/tmp/j3b_rl.out 2>/dev/null; test ! -s /tmp/j3b_rl.out && echo "research-lead emits no injection (no memory file) OK" && printf '%s' '{"task_subject":"update user docs","task_description":"edit the readme"}' | CLAUDE_HOOK_DEBUG=1 python3 .claude/hooks/task-validator-dispatch.py 2>/tmp/j3b_task.err; echo "task-validator exit=$?"; test -s /tmp/j3b_task.err && echo "task-validator debug echo -> stderr OK"</automated>
  </verify>
  <done>
`claude --version` recorded (>= 2.1.197; record the actual version, do not require an exact match). settings.json is valid JSON; SubagentStart matcher
includes dorico-agent and excludes research-lead; SubagentStart/TaskCompleted event blocks unchanged.
Under CLAUDE_HOOK_DEBUG=1: inject-agent-memory emits stdout additionalContext for dsp-agent and
dorico-agent and a stderr debug line; emits NO stdout injection for research-lead; task-validator-dispatch
prints a stderr debug line and exits 0 on a non-code task. Normal (env unset) runs stay silent.
  </done>
</task>

<!-- planner-discipline-allow: rm -rf -->
<task type="auto">
  <name>Task 2: De-fang the auto-approve allow-list (BUG-03)</name>
  <files>.claude/settings.local.json</files>
  <action>
This file is gitignored/untracked (confirmed: git check-ignore matches **/.claude/settings.local.json).
Edit it in place; do NOT git add or commit it — verify changes on disk only.

Remove the blanket destructive entry on line ~12 (the recursive force-delete wildcard,
"Bash(rm -rf *)") from the permissions.allow array entirely — do not replace it with a narrower delete
glob (deletes should be per-command approved).

Narrow the over-broad globs to canonical Claude Code subcommand/path scoping while preserving normal
monorepo workflow convenience:
- "Bash(git add *)"        -> "Bash(git add:*)"
- "Bash(git commit -m ' *)" -> "Bash(git commit:*)"
- "Bash(git push *)"       -> "Bash(git push:*)"
- "Bash(cd *)"             -> "Bash(cd /Users/taylorbrook/Dev/VST-development:*)"  (scope cd to project root)

Leave every other entry untouched (including the read-only "Bash(git check-ignore *)"). Preserve JSON
validity and 2-space indentation. Note in the SUMMARY that git add/commit/push remain subcommand-scoped
(not path-scoped) by design — path-scoping git across a 38-plugin monorepo would cause constant prompts;
the load-bearing security fix is removal of the recursive-delete wildcard.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development && python3 -c "import json;a=json.load(open('.claude/settings.local.json'))['permissions']['allow'];danger=[x for x in a if 'rm -rf' in x];assert not danger, danger;assert 'Bash(git add:*)' in a and 'Bash(git commit:*)' in a and 'Bash(git push:*)' in a, 'git globs not scoped';assert 'Bash(cd *)' not in a and any(x.startswith('Bash(cd /Users/taylorbrook/Dev/VST-development') for x in a), 'cd not scoped';print('allow-list clean, entries:',len(a))" && test -z "$(git status --porcelain .claude/settings.local.json)" && echo "settings.local.json still untracked/ignored OK"</automated>
  </verify>
  <done>
permissions.allow contains no entry with a recursive-delete wildcard; git add/commit/push are
subcommand-scoped (colon form); cd is scoped to the project root; all other entries intact; file is
valid JSON and remains untracked (git status shows nothing for it).
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean stale orphan hook scripts; retain the referenced CLI utilities (IMP-01)</name>
  <files>.claude/hooks/Stop.py, .claude/hooks/UserPromptSubmit.py</files>
  <action>
Delete two stale/dead scripts (both recoverable via git; neither is wired in settings.json):
- .claude/hooks/Stop.py — enforces the pre-GSD PLUGINS.md construction-marker stage-commit convention
  (scans for the in-progress emoji + "Stage N" in last commit). Obsolete under the current GSD phase
  workflow; not wired to the Stop event.
- .claude/hooks/UserPromptSubmit.py — built on a wrong input contract: it reads os.environ["USER_PROMPT"],
  which Claude Code does not set (real UserPromptSubmit delivers the prompt via stdin JSON `prompt`).
  Superseded by the context-resume skill (/continue). Would no-op even if wired.

DO NOT delete two referenced CLI utilities that happen to live under hooks/ — NEITHER is an orphan;
leave both in place and do not wire either as an event hook:
- .claude/hooks/merge-critic-reports.py — invoked by .claude/agents/critic-orchestrator.md:22
  ("run merge-critic-reports.py to produce unified report").
- .claude/hooks/detect-research-conflicts.py — an argparse CLI utility (--dir/--files) that IS actively
  invoked, as Step 4 of the research-lead orchestration workflow: .claude/agents/research-lead.md:64 runs
  `python3 ${CLAUDE_PROJECT_DIR}/.claude/hooks/detect-research-conflicts.py --dir [findings-directory]`,
  and it is documented in .claude/skills/plugin-workflow/references/research-team-protocol.md:57. Same
  retained-CLI disposition as merge-critic-reports.py — it is NOT a stdin-driven event hook, but it is
  live code. (Correct the review's mislabel of BOTH utilities as orphans in the SUMMARY. Note: dropping
  research-lead from the SubagentStart memory matcher in Task 1/BUG-04 is unrelated and remains correct —
  that concerns memory injection, not whether the research-lead agent is live.)

Optionally remove matching stale entries under .claude/hooks/__pycache__ for the two deleted scripts.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development && test ! -e .claude/hooks/Stop.py && test ! -e .claude/hooks/UserPromptSubmit.py && echo "2 orphans deleted OK" && test -f .claude/hooks/merge-critic-reports.py && grep -q 'merge-critic-reports.py' .claude/agents/critic-orchestrator.md && echo "merge-critic-reports.py retained + still referenced OK" && test -f .claude/hooks/detect-research-conflicts.py && grep -q 'detect-research-conflicts.py' .claude/agents/research-lead.md && echo "detect-research-conflicts.py retained + still referenced OK" && python3 -c "import json;s=json.load(open('.claude/settings.json'));import re;cmds=re.findall(r'hooks/([A-Za-z0-9_-]+\.py)', json.dumps(s));bad=[c for c in cmds if c in ('Stop.py','UserPromptSubmit.py')];assert not bad, bad;print('no settings.json refs to deleted scripts OK')"</automated>
  </verify>
  <done>
Stop.py and UserPromptSubmit.py no longer exist; merge-critic-reports.py (referenced by
critic-orchestrator.md) and detect-research-conflicts.py (referenced by research-lead.md:64 and
research-team-protocol.md:57) both still exist and are retained as CLI utilities; settings.json
contains no reference to any deleted script.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude auto-approval -> host shell | Entries in .claude/settings.local.json permissions.allow run unattended, without a human prompt |
| Subagent spawn -> injected context | SubagentStart additionalContext (agent-memory files) is fed into spawned subagents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-j3b-01 | Elevation of Privilege / Destruction | settings.local.json `Bash(rm -rf *)` auto-approve | high | mitigate | Task 2 removes the recursive-delete wildcard so no destructive delete is auto-approved; deletes now require per-command approval |
| T-j3b-02 | Tampering | Over-broad git/cd auto-approve globs | medium | mitigate | Task 2 scopes git to subcommands and cd to the project root, shrinking the unattended-exec surface |
| T-j3b-03 | Information Disclosure | Agent-memory matcher injecting into unintended agents | low | accept | Matcher is an explicit allowlist of internal agents; Task 1 aligns it 1:1 with existing memory files (adds dorico-agent, drops research-lead) |
| T-j3b-SC | Tampering | package installs | n/a | accept | No npm/pip/cargo installs in this plan; no third-party packages added |
</threat_model>

<verification>
- `claude --version` recorded; SubagentStart/TaskCompleted confirmed valid for the installed build (binary registers `executeSubagentStartHooks`; premise correction documented).
- Both event hooks fire under CLAUDE_HOOK_DEBUG=1 with synthetic payloads and behave correctly (Task 1 automated checks).
- settings.json is valid JSON; matcher = `troubleshoot-agent|dsp-agent|gui-agent|research-planning-agent|validation-agent|dorico-agent`.
- settings.local.json: no recursive-delete wildcard; git/cd scoped; valid JSON; still untracked.
- .claude/hooks/ contains no stale orphan; merge-critic-reports.py and detect-research-conflicts.py retained and referenced.

## Manual (fresh-session) end-to-end confirmation — NOT part of automated verify
Full firing of SubagentStart/TaskCompleted hooks can only be confirmed inside a fresh Claude Code
session (the synthetic-payload tests prove the SCRIPTS execute correctly under the documented schemas,
not that the running CLI dispatches these events in this project's workflows). After merge, in a new
session with CLAUDE_HOOK_DEBUG=1 exported, spawn one of the matched subagents (e.g. dsp-agent) and
confirm the inject-agent-memory stderr debug line appears and the agent-memory content is present; note
that TaskCompleted is part of Claude Code's task/teammate system and may fire only when a task is marked
complete.
</verification>

<success_criteria>
- BUG-02: Existing SubagentStart/TaskCompleted wiring verified valid for Claude Code >= 2.1.197 (actual version recorded) and proven to fire via env-gated debug echoes; NOT rewired (regression avoided). Premise correction documented.
- BUG-04: agent-memory matcher aligned 1:1 with existing memory files (dorico-agent added, research-lead removed).
- BUG-03: `Bash(rm -rf *)` removed from the allow-list; git/cd globs scoped; file remains valid + untracked.
- IMP-01: two stale orphan scripts deleted (Stop.py, UserPromptSubmit.py); referenced CLI utilities merge-critic-reports.py and detect-research-conflicts.py retained.
</success_criteria>

<output>
Create `.planning/quick/260701-j3b-fix-dead-claude-hook-wiring-and-settings/260701-j3b-SUMMARY.md` when done.
Record the installed `claude --version` (actual value), the premise correction (SubagentStart/TaskCompleted
are valid in >= 2.1.197 — review reflected the old 9-event docs), and the keep-rationale for BOTH retained
CLI utilities (merge-critic-reports.py and detect-research-conflicts.py).
</output>
