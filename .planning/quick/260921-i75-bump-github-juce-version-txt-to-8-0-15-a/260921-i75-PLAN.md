---
phase: quick-260921-i75
plan: 01
type: execute
wave: 1
depends_on: [quick-260921-hno]
subsystem: build-infrastructure
tags: [juce, ci, provenance, supply-chain, version-bump]
autonomous: true

files_modified:
  - .github/juce-version.txt
  - scripts/juce-patches/note-expression-juce-8.0.15.patch
  - vendored/JUCE-overrides/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp
  - vendored/JUCE-overrides/MANIFEST.txt
  - README.md

requirements: [Q-i75-01, Q-i75-02, Q-i75-03]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 3
  confidence: low        # sample_count=0, factor=1.0 (estimate-calibration, 2026-09-21)

must_haves:
  truths:
    - "`.github/juce-version.txt` reads 8.0.15, and every gate that reads it passes."
    - "`bash scripts/gen-juce-overrides.sh --check` exits 0 — the vendored overrides regenerate from an 8.0.15-named patch applied to pristine JUCE 8.0.15."
    - "`bash scripts/check-juce-overrides.sh` exits 0 — MANIFEST.txt agrees with both the on-disk overrides and upstream 8.0.15."
    - "The vendored VST3 wrapper carries upstream's 8.0.15 getPosition() fix; the bump re-derives the overrides rather than copying 8.0.14 bytes forward."
    - "The exact CI sequence (guard -> regeneration check -> copy -> marker greps) passes against the official juce-8.0.15-osx.zip, not only against the raw git tag."
    - "No file outside the declared set changed; the pre-existing `.claude/agent-memory/research-planning-agent.md` modification is still uncommitted and untouched."
  artifacts:
    - scripts/juce-patches/note-expression-juce-8.0.15.patch
    - vendored/JUCE-overrides/MANIFEST.txt (juce_version 8.0.15)
  key_links:
    - "pin -> patch filename: gen-juce-overrides.sh:197 and apply-juce-patches.sh:39 both derive `note-expression-juce-<pinned>.patch` from the pin. The bump is inert unless the 8.0.15-named patch exists."
    - "pin -> MANIFEST.txt juce_version: check-juce-overrides.sh CHECK 1 fails when they disagree. This is the bump tripwire and it must be cleared deliberately, by --update, not by editing the manifest."
    - "raw git tag -> release zip: the manifest is fingerprinted from raw.githubusercontent.com; CI fingerprints the unpacked release zip. If the two distributions disagree CR-stripped, CI goes red on a green local tree."
---

<objective>
Move the JUCE pin from 8.0.14 to 8.0.15 and re-derive the two vendored override files from
the 8.0.15 tag using the generator built in quick task 260921-hno. This is that generator's
first real use.

Purpose: 8.0.15 is the terminal 8.x release. More importantly, upstream changed one of the two
override targets between 8.0.14 and 8.0.15 — a one-line `getPosition()` fix. Bumping the pin
without re-deriving the overrides would silently revert that upstream fix in every CI binary
(`research/repo-modernization-audit-2026-09-21.md`, row flagged High). Re-deriving is the whole
point of the bump.

Output: pin at 8.0.15, an 8.0.15-named note-expression patch, regenerated vendored bytes, a
re-fingerprinted manifest, and README's three stale version references corrected — with all
three gates (`check-juce-overrides.sh`, `gen-juce-overrides.sh`, `--check-wiring`) green on both
the raw-tag axis and the release-zip axis CI actually consumes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260921-hno-generate-vendored-juce-overrides-from-sc/260921-hno-SUMMARY.md
@scripts/gen-juce-overrides.sh
@scripts/check-juce-overrides.sh
@scripts/apply-juce-patches.sh
@scripts/juce-patches/note-expression-juce-8.0.14.patch
@vendored/JUCE-overrides/MANIFEST.txt
</context>

<planning_time_observations>
Everything below was measured live at planning time (2026-09-21) against the repo and upstream.
Treat it as a starting picture, not as edit authority: re-observe anything you are about to act on.

| # | Observation | How it was measured |
|---|---|---|
| **P1** | JUCE tag `8.0.15` exists upstream, resolving to commit **`91ad83ae34a81e0833b1a2b0866f54846370ae53`** (8.0.14 = `2cdfca8feb300fb424002ba2c2751569e5bacb64`) | `git ls-remote --tags https://github.com/juce-framework/JUCE 8.0.15` |
| **P2** | Both release assets CI downloads exist: `juce-8.0.15-osx.zip` (52,636,418 bytes) and `juce-8.0.15-windows.zip`, HTTP 200 | `gh api repos/juce-framework/JUCE/releases/tags/8.0.15`, `curl -sIL` |
| **P3** | The existing 8.0.14 patch applies to a pristine 8.0.15 tree **cleanly**: `patch -p1 -F0 -N -t --dry-run` exits 0, zero fuzz, zero offset, zero rejects | dry-run against LF-normalized 8.0.15 sources in scratch |
| **P4** | `juce_VST3ClientExtensions.h` is **byte-identical** between 8.0.14 and 8.0.15 upstream | `cmp` on the two raw-tag fetches |
| **P5** | `juce_audio_plugin_client_VST3.cpp` differs by **exactly one line** — upstream removed the `jmax ((Steinberg::int64) 0, ...)` clamp inside `getPosition()` at upstream line ~2996 | `diff -u` on CR-stripped raw-tag fetches |
| **P6** | Simulated regeneration: applying the patch to pristine 8.0.15 yields a `.h` **byte-identical** to the committed vendored `.h`, and a `.cpp` differing from the committed vendored `.cpp` by **exactly that one line, in that direction** | full apply + CR-stripped `diff -u` against `vendored/` |
| **P7** | **No workflow carries a version literal.** Every CI reference derives from the pin (`ci-tests.yml:112,335`; `build-and-release.yml:103`), including both zip URLs (`ci-tests.yml:122,345`; `build-and-release.yml:151,539`). Nothing in `.github/workflows/` needs editing | `grep -n` for `juce-version.txt` and the download URLs |
| **P8** | The only `8.0.14` literals in the repo outside `.git`/`build`/`.planning` are: `.github/juce-version.txt:1`, `README.md:92,1026,1032`, `research/repo-modernization-audit-2026-09-21.md` (many), `.claude/resource-index.json:939,968,1000`, `.claude/agent-memory/research-planning-agent.md:55`, and the 8.0.14 patch's own header | repo-wide `grep -rn "8\.0\.14"` |

### Consumer disposition — decided at planning time

| Consumer | Disposition | Why |
|---|---|---|
| `.github/juce-version.txt` | **UPDATE** | The pin. The task. |
| `scripts/juce-patches/note-expression-juce-8.0.15.patch` | **CREATE** | Both `gen-juce-overrides.sh:197` and `apply-juce-patches.sh:39` derive this filename from the pin. Without it the bump fails instantly and offline (negative control NC3 in 260921-hno). |
| `vendored/JUCE-overrides/**` | **REGENERATE** (never hand-edit) | Product of `--write`. |
| `README.md:92,1026,1032` | **UPDATE** | Live prose about the current pin and the current patch filename. |
| `scripts/juce-patches/note-expression-juce-8.0.14.patch` | **KEEP, untouched** | `note-expression-juce-8.0.9.patch` is still present — the established convention is to retain superseded patches as the record of what each pin used. |
| `research/repo-modernization-audit-2026-09-21.md` | **DO NOT TOUCH** | A dated audit. It is a historical record of what was true on 2026-09-21, and this bump is the audit's own recommendation being carried out. Rewriting it would destroy the evidence for this task. |
| `.claude/resource-index.json` | **DO NOT TOUCH** | GENERATED (`.claude/scripts/generate-resource-index.py:114`) from each research doc's own `juce_version:` frontmatter — a per-document "verified against" record, not the build pin. Most entries already read `8.0.4`. Editing it would make a derived file contradict its sources. |
| `.claude/agent-memory/research-planning-agent.md` | **DO NOT TOUCH, DO NOT STAGE** | The pre-existing unrelated working-tree modification. Its `8.0.14` reference describes the local `/Users/taylorbrook/JUCE` tree, which this task deliberately leaves at 8.0.14. |
| `/Users/taylorbrook/JUCE` | **DO NOT TOUCH** | Out of scope by instruction. See the disclosed divergence below. |

### Disclosed divergence (record it; do not fix it here)

After this bump the pin says 8.0.15 while the local build tree `/Users/taylorbrook/JUCE` remains
8.0.14 + NE patch. `scripts/apply-juce-patches.sh` will now resolve the 8.0.15-named patch, but it
exits 0 at its step-3 idempotency check (`JUCE-NE-PATCH` marker already present in both targets,
`apply-juce-patches.sh:66-69`) and never applies anything — the local tree is unaffected. Local
builds are therefore one patch release behind CI until `/Users/taylorbrook/JUCE` is reinstalled.
Task 2 measures this rather than assuming it.

### Disclosed non-claim

This task proves the override **derivation** is correct for 8.0.15. It does **not** prove JUCE
8.0.15 compiles the plugin suite — that is a CI-only property, first exercised on the next CI run,
and it cannot be established locally without replacing `/Users/taylorbrook/JUCE`. No plugin is
built here, so CLAUDE.md's AU-cache/install sequence does not apply.
</planning_time_observations>

<tasks>

<task type="tracer">
  <name>Task 1: Bump the pin and re-derive the overrides end-to-end</name>
  <files>.github/juce-version.txt, scripts/juce-patches/note-expression-juce-8.0.15.patch, vendored/JUCE-overrides/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp, vendored/JUCE-overrides/MANIFEST.txt</files>
  <precondition>Upstream JUCE tag 8.0.15 resolves (`git ls-remote --tags https://github.com/juce-framework/JUCE 8.0.15` prints a SHA). If it does not, HALT and report — nothing downstream is derivable.</precondition>
  <action>
The single path from pin to committed bytes, wired end to end.

**Step A — re-confirm the tag and record its SHA.** Run `git ls-remote --tags https://github.com/juce-framework/JUCE 8.0.15` and record the resolved commit SHA verbatim for the SUMMARY (threat T-i75-01 provenance evidence). Planning time saw `91ad83ae34a81e0833b1a2b0866f54846370ae53`; if it differs now, STOP and report — a moved tag is a supply-chain signal, not a rebase.

**Step B — create the 8.0.15 patch.** `cp` the 8.0.14 patch to `scripts/juce-patches/note-expression-juce-8.0.15.patch`. Do NOT `git mv`; the 8.0.14 patch is retained. Then correct its header comment on exactly three lines — line 2 (the filename self-reference), line 3 (`Target: JUCE 8.0.14`), and line 12 (the example `patch -p1 <` invocation) — so each names 8.0.15.

**TRAP — do not blanket-substitute.** Line 8 reads `(moved to juce_audio_processors_headless/utilities/ at 8.0.14)`. That is a historical statement about which JUCE release relocated the header, and it stays 8.0.14. A `sed -i 's/8\.0\.14/8.0.15/g'` over this file would make it assert a false fact. Edit the three lines individually.

Everything from line 14 (`--- a/...`) down is patch payload and must be byte-identical to the 8.0.14 patch — P3 established it applies to 8.0.15 with zero fuzz and zero offset, so no rebase is needed. `gen-juce-overrides.sh:217` only ever reads `--- a/` lines, so the header is inert to the generator; correcting it closes a version mirror rather than changing behavior.

**If the payload turns out NOT to apply cleanly** (Step D fails with a patch error): STOP and report. Do not add `-F`, do not relax `-N`, do not hand-resolve a hunk into the vendored files. Known trap from 260921-hno NC6: `patch -t` without `-N` silently reverse-applies; the shipped generator already carries `-N` and its `DIAGNOSIS:` block will name a non-pristine base if that is the cause.

**Step C — bump the pin.** Write `8.0.15` into `.github/juce-version.txt`, preserving the file's existing single-line-plus-newline shape.

**Step D — regenerate.** Run `bash scripts/gen-juce-overrides.sh --write` from the repo root, with no `--juce-root` (network path: it fetches pristine 8.0.15 from the tag). Never hand-edit anything under `vendored/` — the vendored bytes are a product of this command and nothing else (threat T-i75-03).

**Step E — re-fingerprint the manifest.** Run `bash scripts/check-juce-overrides.sh --update`, again with no `--juce-root`, matching how the manifest records upstream (raw tag). `--write` deliberately does not touch MANIFEST.txt and prints this reminder itself.

**Step F — assert the delta is exactly the expected one.** Per P6 the regeneration must produce: `vendored/.../juce_VST3ClientExtensions.h` unchanged (0 lines), and `vendored/.../juce_audio_plugin_client_VST3.cpp` changed on exactly one line — gaining upstream's `getPosition()` fix, i.e. losing the `jmax` clamp. In MANIFEST.txt, `juce_version` flips and the `.cpp` override line's two sha256 values change, while the `.h` override line is byte-identical to before. If the observed delta is larger than this, STOP and report before committing — a wider diff means the patch payload and the 8.0.15 sources interact differently than measured, and that is a finding, not a formality.

**Step G — commit, path-scoped.** The new patch file is untracked and a pathspec commit takes tracked files only, so `git add` exactly that one path first — never `git add -A`, never `-a`. Re-check `git branch --show-current` and `git status --short` immediately before committing (not once at task start: another session shares this index and HEAD). Confirm `.claude/agent-memory/research-planning-agent.md` is still present and unstaged in that status output. Then commit the four declared paths only.
  </action>
  <verify>
    <automated>
cd /Users/taylorbrook/Dev/VST-development &amp;&amp; \
test "$(tr -d '[:space:]' &lt; .github/juce-version.txt)" = "8.0.15" &amp;&amp; \
test -f scripts/juce-patches/note-expression-juce-8.0.15.patch &amp;&amp; \
test -f scripts/juce-patches/note-expression-juce-8.0.14.patch &amp;&amp; \
diff &lt;(sed -n '14,$p' scripts/juce-patches/note-expression-juce-8.0.15.patch) &lt;(sed -n '14,$p' scripts/juce-patches/note-expression-juce-8.0.14.patch) &amp;&amp; \
test "$(grep -c '8\.0\.14' scripts/juce-patches/note-expression-juce-8.0.15.patch)" = "1" &amp;&amp; \
grep -n '8\.0\.14' scripts/juce-patches/note-expression-juce-8.0.15.patch | grep -q 'moved to' &amp;&amp; \
grep -q '^juce_version 8\.0\.15$' vendored/JUCE-overrides/MANIFEST.txt &amp;&amp; \
grep -q '^override modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h 5488c7ef6b2af884e91af7c23ac74bb352ab49863fb0e68ab7e1ace149c2a4c0 4e2b9023f01aedd0256a7752ba1c66a3a2e90278652c858d150a5032739ed720$' vendored/JUCE-overrides/MANIFEST.txt &amp;&amp; \
bash scripts/gen-juce-overrides.sh --check &amp;&amp; \
bash scripts/check-juce-overrides.sh &amp;&amp; \
echo "TASK1_PASS"
    </automated>
    <automated>
# Delta bound: the .h is untouched and the .cpp moved by exactly one line, in the right direction.
# `git` status is captured BEFORE any pipe — a `git show` piped straight into `wc -l`
# would report 0 on a broken command and read as "the .h did not change".
cd /Users/taylorbrook/Dev/VST-development
NUMSTAT="$(git show --numstat --format= HEAD -- vendored/JUCE-overrides/)" || exit 1
test -n "$NUMSTAT" || { echo "FAIL: HEAD touches nothing under vendored/"; exit 1; }
if printf '%s\n' "$NUMSTAT" | grep -q 'juce_VST3ClientExtensions\.h'; then
  echo "FAIL: the .h changed; upstream 8.0.15 left it byte-identical so it must not"; exit 1
fi
test "$(printf '%s\n' "$NUMSTAT" | awk '$3 ~ /juce_audio_plugin_client_VST3\.cpp$/ {print $1"/"$2}')" = "1/1" || \
  { echo "FAIL: .cpp delta is not exactly 1 insertion / 1 deletion"; exit 1; }
CPPDIFF="$(git show HEAD -- vendored/JUCE-overrides/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp)" || exit 1
printf '%s\n' "$CPPDIFF" | grep -q '^-.*jmax ((Steinberg::int64) 0, processContext.projectTimeSamples)' || \
  { echo "FAIL: the removed line is not the 8.0.14 jmax clamp"; exit 1; }
printf '%s\n' "$CPPDIFF" | grep -q '^+.*setTimeInSamples (processContext.projectTimeSamples)' || \
  { echo "FAIL: the added line is not upstream's 8.0.15 getPosition() fix"; exit 1; }
echo "DELTA_BOUND_PASS"
    </automated>
  </verify>
  <done>
`.github/juce-version.txt` reads 8.0.15. `scripts/juce-patches/note-expression-juce-8.0.15.patch`
exists, its payload from line 14 down is byte-identical to the 8.0.14 patch, and its header carries
exactly one surviving 8.0.14 reference — the "moved to ... at 8.0.14" historical note. MANIFEST.txt
reads `juce_version 8.0.15` with the `.h` override line byte-unchanged. `gen-juce-overrides.sh
--check` and `check-juce-overrides.sh` both exit 0. The committed vendored delta is exactly: `.h`
untouched, `.cpp` 1 insertion / 1 deletion carrying upstream's `getPosition()` fix. One path-scoped
commit on `main`; `.claude/agent-memory/research-planning-agent.md` still unstaged.
  </done>
  <reversibility rating="reversible">The pin, the patch, and the vendored bytes are all recoverable by `git restore --source=&lt;sha&gt; -- &lt;paths&gt;`; the superseded 8.0.14 patch is retained on disk, so the bump can be undone without re-deriving anything.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Prove the bump against the distribution CI actually consumes</name>
  <files>(verification only — no repo file changes expected; see the contingency below)</files>
  <precondition>`curl -sIL -o /dev/null -w '%{http_code}' https://github.com/juce-framework/JUCE/releases/download/8.0.15/juce-8.0.15-osx.zip` returns 200. Planning time measured 200 and a 52,636,418-byte asset. If it does not resolve, HALT — CI cannot fetch what does not exist, and no local gate would have noticed.</precondition>
  <action>
Task 1's gates all read `raw.githubusercontent.com` at the tag. CI reads a **different distribution**:
it unzips the macOS release asset and runs both guards with `--juce-root JUCE`. Those two
distributions agreeing CR-stripped is load-bearing and is the one axis nothing local has yet
exercised at 8.0.15 (threat T-i75-04). This task closes it by replaying the CI sequence verbatim.

**Work entirely inside the session scratchpad directory.** Never unpack a JUCE tree anywhere under
the repo: `vendored/`, `scripts/`, and the two gates' inventory checks all walk repo paths, and a
stray tree would both pollute `git status` and risk being swept into a pathspec commit. Create a
subdirectory of the session scratchpad and do everything there.

**Step A — fetch and unpack.** Download `https://github.com/juce-framework/JUCE/releases/download/8.0.15/juce-8.0.15-osx.zip` with `curl --fail --show-error --location --proto '=https' --tlsv1.2` into the scratch dir and `unzip -q` it, yielding a pristine `JUCE/` tree. The official `juce-framework/JUCE` release URL is the only acceptable source (threat T-i75-01). If the download or unzip runs long, launch it as a background command rather than letting the executor watchdog stall on it.

**Step B — replay the CI step order against that tree**, in exactly the order both workflows use
(`ci-tests.yml` probes-macos, `build-and-release.yml` build-macos):

1. `bash scripts/check-juce-overrides.sh --juce-root &lt;scratch&gt;/JUCE`
2. `bash scripts/gen-juce-overrides.sh --check --juce-root &lt;scratch&gt;/JUCE`
3. `cp -R vendored/JUCE-overrides/modules/. &lt;scratch&gt;/JUCE/modules/`
4. the two post-copy marker greps for `JUCE-NE-PATCH`, one per override target

Record each exit code. Step 1 is the one that discriminates: it recomputes the upstream sha256 from
the zip bytes and compares it against the manifest values Task 1 wrote from the raw tag. If it
passes, the two distributions agree at 8.0.15 and the bump is CI-safe.

**Contingency — if step 1 fails on CHECK 3 (upstream fingerprint).** The two distributions disagree.
That is a real finding, not a nuisance. CI is authoritative because it is what ships, so re-run
`bash scripts/check-juce-overrides.sh --update --juce-root &lt;scratch&gt;/JUCE` to re-fingerprint from the
zip, re-run the whole sequence, commit the corrected manifest path-scoped, and report the divergence
prominently in the SUMMARY — including which bytes differ. Do not silently prefer one axis.

**Step C — measure the local-tree consequence rather than assuming it.** Run
`bash scripts/apply-juce-patches.sh` and capture its output and exit code. Expect it to resolve the
8.0.15-named patch and then exit 0 at the idempotency check without applying anything, leaving
`/Users/taylorbrook/JUCE` untouched. Prove untouched by comparing `sha256` of the two target files in
that tree before and after the run. **If it does apply anything, that is a stop-and-report** — the
local tree must not be mutated by this task.

**Step D — wiring is still intact.** Run `bash scripts/gen-juce-overrides.sh --check-wiring` and
`bash scripts/check-juce-overrides.sh --check-wiring`. Neither workflow was edited (P7), so both must
still exit 0; this is a regression check against the possibility that the bump touched something it
should not have.

**Step E — clean up.** Remove the scratch JUCE tree and the zip. Confirm `git status --short` shows
nothing beyond the pre-existing `.claude/agent-memory/research-planning-agent.md`.

No commit unless the contingency fired.
  </action>
  <verify>
    <automated>
# Full CI replay against the official release zip. SCRATCH must point at the session scratchpad.
set -e
cd /Users/taylorbrook/Dev/VST-development
J="${SCRATCH:?set SCRATCH to the session scratchpad dir}/juce8015/JUCE"
test -d "$J"
bash scripts/check-juce-overrides.sh --juce-root "$J"
bash scripts/gen-juce-overrides.sh --check --juce-root "$J"
cp -R vendored/JUCE-overrides/modules/. "$J/modules/"
grep -q "JUCE-NE-PATCH" "$J/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h"
grep -q "JUCE-NE-PATCH" "$J/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp"
echo "CI_REPLAY_PASS"
    </automated>
    <automated>
cd /Users/taylorbrook/Dev/VST-development &amp;&amp; \
bash scripts/gen-juce-overrides.sh --check-wiring &amp;&amp; \
bash scripts/check-juce-overrides.sh --check-wiring &amp;&amp; \
echo "WIRING_PASS"
    </automated>
    <automated>
# The local JUCE tree is a read-only input to this task.
cd /Users/taylorbrook/Dev/VST-development &amp;&amp; \
BEFORE="$(shasum -a 256 /Users/taylorbrook/JUCE/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h /Users/taylorbrook/JUCE/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp)" &amp;&amp; \
bash scripts/apply-juce-patches.sh &amp;&amp; \
AFTER="$(shasum -a 256 /Users/taylorbrook/JUCE/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h /Users/taylorbrook/JUCE/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp)" &amp;&amp; \
test "$BEFORE" = "$AFTER" &amp;&amp; \
echo "LOCAL_JUCE_UNTOUCHED"
    </automated>
  </verify>
  <done>
The four-step CI sequence passes against the unpacked official `juce-8.0.15-osx.zip`, proving the
manifest's raw-tag fingerprints hold for the release-zip distribution too. Both `--check-wiring`
modes exit 0. `apply-juce-patches.sh` resolves the 8.0.15-named patch, exits 0 at the idempotency
check, and leaves both target files in `/Users/taylorbrook/JUCE` byte-identical. Scratch tree removed;
`git status --short` shows only the pre-existing unrelated modification. Exit codes for all steps and
the local-tree divergence are recorded for the SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct README's stale version references and close the consumer audit</name>
  <files>README.md</files>
  <action>
Update the three live references in `README.md` measured at P8, and nothing else:

- **line 92** — the patch filename cited for the `modules/tuning/note-expression` module must name
  the 8.0.15 patch, since that is the one the pin now resolves.
- **line 1026** — the stack list entry naming the framework version.
- **line 1032** — the cross-platform build note stating which pin Windows builds share.

These are prose statements about the *current* pin, which is why they move; the other surviving
8.0.14 references in the repo are historical or generated and were dispositioned at planning time
(see the consumer table above). Do not touch `research/repo-modernization-audit-2026-09-21.md`,
`.claude/resource-index.json`, or `.claude/agent-memory/research-planning-agent.md`.

Then re-run the consumer audit to confirm the disposition still holds after the bump: a repo-wide
`grep -rn '8\.0\.14'` excluding `.git/`, `build/`, and `.planning/` should return only the retained
8.0.14 patch (its own header and filename), the dated research audit, `.claude/resource-index.json`,
and `.claude/agent-memory/research-planning-agent.md`. Any hit outside that set is an unplanned
consumer — report it rather than editing it on the spot.

**Scope every negative grep to a named file.** A repo-wide "no 8.0.14 anywhere" gate is
self-invalidating here: this PLAN, the SUMMARY, the retained patch, and the research audit all
legitimately contain the string. The gates below therefore target `README.md` specifically.

Commit path-scoped, re-checking `git branch --show-current` and `git status --short` immediately
before. `README.md` is tracked, so no `git add` is needed — but confirm the status output still shows
`.claude/agent-memory/research-planning-agent.md` unstaged and that nothing else is staged.
  </action>
  <verify>
    <automated>
cd /Users/taylorbrook/Dev/VST-development &amp;&amp; \
test "$(grep -c '8\.0\.14' README.md)" = "0" &amp;&amp; \
test "$(grep -c 'note-expression-juce-8\.0\.15\.patch' README.md)" = "1" &amp;&amp; \
grep -q 'JUCE 8\.0\.15 (audio plugin framework)' README.md &amp;&amp; \
grep -q 'same JUCE 8\.0\.15 pin' README.md &amp;&amp; \
echo "README_PASS"
    </automated>
    <automated>
# Consumer audit: every surviving 8.0.14 reference is one of the four dispositioned keeps.
cd /Users/taylorbrook/Dev/VST-development &amp;&amp; \
UNEXPECTED="$(grep -rln '8\.0\.14' . 2>/dev/null \
  | grep -v '^\./\.git/' | grep -v '^\./build/' | grep -v '^\./\.planning/' \
  | grep -v '^\./scripts/juce-patches/note-expression-juce-8\.0\.14\.patch$' \
  | grep -v '^\./research/repo-modernization-audit-2026-09-21\.md$' \
  | grep -v '^\./\.claude/resource-index\.json$' \
  | grep -v '^\./\.claude/agent-memory/research-planning-agent\.md$' )" &amp;&amp; \
test -z "$UNEXPECTED" &amp;&amp; echo "CONSUMER_AUDIT_CLEAN" || { echo "UNPLANNED CONSUMERS:"; echo "$UNEXPECTED"; exit 1; }
    </automated>
    <automated>
# Commit hygiene: the unrelated working-tree modification never entered a commit.
# Keyed on the task scope in the commit message, NOT on a fixed HEAD~N — Task 2
# commits only under its contingency, so the commit count is 2, 3, or 4.
cd /Users/taylorbrook/Dev/VST-development
LOG="$(git log --format='%H %s' -30)" || exit 1
SHAS="$(printf '%s\n' "$LOG" | grep 'quick-260921-i75' | awk '{print $1}')"
test -n "$SHAS" || { echo "FAIL: no commit carries the task scope"; exit 1; }
NAMES=""
for s in $SHAS; do
  ONE="$(git show --format= --name-only "$s")" || exit 1
  NAMES="$NAMES
$ONE"
done
test -n "$NAMES" || { echo "FAIL: task commits touch no files"; exit 1; }
if printf '%s\n' "$NAMES" | grep -q 'agent-memory'; then
  echo "FAIL: the unrelated agent-memory file entered a task commit"; exit 1
fi
STATUS="$(git status --short)" || exit 1
printf '%s\n' "$STATUS" | grep -q 'agent-memory/research-planning-agent.md' || \
  { echo "FAIL: the pre-existing unrelated modification is gone from the working tree"; exit 1; }
STAGED="$(git diff --cached --name-only)" || exit 1
test -z "$STAGED" || { echo "FAIL: index is not clean"; exit 1; }
echo "COMMIT_HYGIENE_PASS ($(printf '%s\n' "$SHAS" | wc -l | tr -d ' ') commits)"
    </automated>
  </verify>
  <done>
`README.md` carries no 8.0.14 reference, cites `note-expression-juce-8.0.15.patch` once, and states
JUCE 8.0.15 in both the stack list and the cross-platform build note. The repo-wide consumer audit
returns only the four dispositioned keeps. `.claude/agent-memory/research-planning-agent.md` appears
in no commit from this task and is still modified-but-unstaged; the index is clean. One path-scoped
commit on `main`.
  </done>
</task>

</tasks>

<threat_model>
ASVS level 1; block on `high`. Scope: a build-input version bump with an upstream source fetch.
No package-manager installs are in scope (no npm/pip/cargo), so no package-legitimacy audit applies.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream GitHub -> local derivation | Pristine JUCE 8.0.15 bytes cross into the repo as the base the patch is applied to. Untrusted until pinned and fingerprinted. |
| raw git tag -> release zip | Two distinct upstream distributions of the "same" version. The manifest is fingerprinted from one; CI consumes the other. |
| patch file -> vendored override bytes | A transformation whose output is committed and later copied over CI's build input. |
| repo -> CI build input | `cp -R vendored/JUCE-overrides/modules/.` overwrites downloaded JUCE sources with no further validation after the two pre-copy gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-i75-01 | Tampering | upstream fetch of pristine 8.0.15 (`gen-juce-overrides.sh` curl; Task 2 zip download) | high | mitigate | Fetch only from `juce-framework/JUCE` addressed by the immutable tag `8.0.15` / its release asset, over `--proto '=https' --tlsv1.2 --fail`. Re-confirm and record the resolved tag SHA (`91ad83ae34a81e0833b1a2b0866f54846370ae53` at planning time) in the SUMMARY; a changed SHA is a stop-and-report, not a rebase. The regeneration gate then binds derived bytes to the committed patch, so substituted upstream content surfaces as a diff rather than as silent input. |
| T-i75-03 | Tampering | `vendored/JUCE-overrides/**` | high | mitigate | Vendored bytes are produced ONLY by `gen-juce-overrides.sh --write`; hand-editing is forbidden. The expected delta is bounded in advance to one known line (P6) and asserted by a dedicated `<verify>` gate, so a hand-edit or a wider-than-expected regeneration halts the task before commit. |
| T-i75-02 | Tampering | `patch(1)` application inside the generator | medium | mitigate | Use the shipped generator unmodified: `-F0` forbids fuzz, `-N` forbids the silent reverse-apply proven in 260921-hno NC6, `-t` forbids prompts. The executor must not add relaxing flags or hand-resolve a rejected hunk; a failed apply is a stop-and-report. |
| T-i75-04 | Spoofing | raw-tag bytes standing in for release-zip bytes | medium | mitigate | Task 2 replays the exact CI sequence against the unpacked official `juce-8.0.15-osx.zip`, so the manifest's raw-tag fingerprints are proven to hold for the distribution CI consumes; a disagreement re-fingerprints from the zip and is reported. |
| T-i75-05 | Tampering | commit scope in a shared checkout | medium | mitigate | Every commit is path-scoped to declared paths (`git add` only the one untracked patch file; never `git add -A` / `-a`), with `git branch --show-current` and `git status --short` re-checked immediately before each commit. A dedicated gate asserts `.claude/agent-memory/research-planning-agent.md` entered no commit and the index is clean. |
| T-i75-06 | Tampering | `/Users/taylorbrook/JUCE` (the local build tree) | low | mitigate | Out of scope by instruction. Task 2 proves non-mutation by sha256 before/after the only command that could touch it (`apply-juce-patches.sh`), which exits at its idempotency check. |
| T-i75-07 | Repudiation | `--write` / `--update` can launder drift | low | accept | Documented limitation of both scripts (see each header's "WHAT THIS GATE DOES NOT CLAIM"). Both produce plain-text diffs reviewed in the commit; these gates target silent drift, not a deliberate insider. Unchanged by this task. |
| T-i75-08 | Denial of Service | network unavailable during derivation | low | accept | Inherited from 260921-hno. A fetch failure is a hard error naming `--juce-root` as the offline path; no check is ever skipped. |
</threat_model>

<verification>
Run from the repo root after all three tasks:

```bash
bash scripts/check-juce-overrides.sh          # provenance, raw-tag axis
bash scripts/gen-juce-overrides.sh --check    # regeneration, raw-tag axis
bash scripts/check-juce-overrides.sh --check-wiring
bash scripts/gen-juce-overrides.sh --check-wiring
```

All four exit 0. Task 2 additionally discharges the release-zip axis, which no local default-mode
invocation reaches.

Not verified here, by design: that JUCE 8.0.15 compiles the plugin suite. That is CI-only and is
first exercised on the next CI run. No plugin is built by this task.
</verification>

<success_criteria>
- [ ] `.github/juce-version.txt` reads `8.0.15`
- [ ] `scripts/juce-patches/note-expression-juce-8.0.15.patch` exists; payload byte-identical to the 8.0.14 patch from line 14 down; header names 8.0.15 on lines 2, 3, 12 and retains the 8.0.14 module-move note on line 8
- [ ] `scripts/juce-patches/note-expression-juce-8.0.14.patch` still present and unmodified
- [ ] Vendored `.h` unchanged; vendored `.cpp` changed by exactly 1 insertion / 1 deletion, gaining upstream's `getPosition()` fix
- [ ] `MANIFEST.txt` reads `juce_version 8.0.15`; the `.h` override line byte-unchanged; the `.cpp` override line's two sha256 values updated
- [ ] All four gate invocations exit 0 on the raw-tag axis
- [ ] The four-step CI sequence exits 0 against the unpacked official `juce-8.0.15-osx.zip`
- [ ] `apply-juce-patches.sh` leaves both target files in `/Users/taylorbrook/JUCE` byte-identical
- [ ] `README.md` free of 8.0.14; cites the 8.0.15 patch and pin
- [ ] Consumer audit returns only the four dispositioned keeps
- [ ] Three path-scoped commits on `main`; no tags; `.claude/agent-memory/research-planning-agent.md` in none of them and still unstaged; index clean
- [ ] Resolved 8.0.15 tag SHA recorded in the SUMMARY (T-i75-01 evidence)
- [ ] SUMMARY records the disclosed divergence (local JUCE tree stays 8.0.14) and the disclosed non-claim (8.0.15 compilation unproven locally) as carry-forward
</success_criteria>

<output>
Create `.planning/quick/260921-i75-bump-github-juce-version-txt-to-8-0-15-a/260921-i75-SUMMARY.md` when done.
</output>
