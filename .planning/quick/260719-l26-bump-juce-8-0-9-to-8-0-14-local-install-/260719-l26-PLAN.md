---
phase: 260719-l26-bump-juce
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/apply-juce-patches.sh
  - .github/workflows/build-and-release.yml
autonomous: true
requirements: [BUMP-01, BUMP-02, BUMP-03]
user_setup: []

must_haves:
  truths:
    - "Local /Users/taylorbrook/JUCE is pristine JUCE 8.0.14 (CMakeLists project VERSION 8.0.14) carrying the JUCE-NE-PATCH marker in both patched files at the 8.0.14 headless-module layout."
    - "CI JUCE_VERSION in build-and-release.yml is 8.0.14, matching the local install exactly (per the 'must match' rule)."
    - "All 37 plugins compile and link cleanly (VST3 + AU) against patched JUCE 8.0.14 from a fresh CMake configure — zero compile fallout."
    - "A rollback path to 8.0.9 exists: the prior local JUCE tree is preserved aside, not overwritten in place."
  artifacts:
    - "/Users/taylorbrook/JUCE (pristine 8.0.14, NE-patched)"
    - "/Users/taylorbrook/JUCE-8.0.9-backup (preserved prior tree)"
    - "scripts/apply-juce-patches.sh (repointed to note-expression-juce-8.0.14.patch + headless header path)"
    - ".github/workflows/build-and-release.yml (JUCE_VERSION 8.0.14)"
    - "build/plugins/*/*_artefacts/Release/{VST3,AU}/* (37 VST3 + 37 AU bundles)"
  key_links:
    - "apply-juce-patches.sh -> scripts/juce-patches/note-expression-juce-8.0.14.patch -> JUCE-NE-PATCH markers in juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h AND juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp"
    - "CI JUCE_VERSION <-> local /Users/taylorbrook/JUCE version (must be identical)"
    - "vendored/JUCE-overrides/ headless-module paths <-> 8.0.14 module layout (already re-based on this branch; CI copy step + grep gates already point there)"
---

<objective>
Bump JUCE from 8.0.9 to 8.0.14 across the local dev install and CI together, apply the
already-re-based note-expression patch (JUCE-NE-PATCH) to the fresh 8.0.14 tree, then prove the
full 37-plugin macOS suite (VST3 + AU) still compiles and links with zero fallout.

This is the version-bump commit-set the `quick/260719-k5o-juce-ne-rebase-8014` branch was waiting
for: the NE patch was re-based onto 8.0.14 (vendored header relocated to the new
`juce_audio_processors_headless/utilities/` module; `note-expression-juce-8.0.14.patch` regenerated)
but local JUCE + CI are still pinned to 8.0.9. Both must move to 8.0.14 in the same commit-set.

Purpose: land the JUCE 8.0.14 upgrade that underpins all 37 plugins, verified green, on this branch.
Output: patched local 8.0.14 JUCE, CI pinned to 8.0.14, `apply-juce-patches.sh` repointed to the
8.0.14 patch, and a full-suite build report. Per research/framework-updates-2026-07.md the
breaking-change grep found ZERO repo exposure, so fallout should be nil — treat any compile failure
as a re-base defect in the NE patch first, not a JUCE breaking change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@research/framework-updates-2026-07.md
@CLAUDE.md
@scripts/apply-juce-patches.sh
@scripts/juce-patches/note-expression-juce-8.0.14.patch
@.github/workflows/build-and-release.yml

# Verified during planning (do not re-discover):
# - Local /Users/taylorbrook/JUCE is an UNPACKED release, NOT a git checkout (project VERSION 8.0.9).
#   Bump = download pristine 8.0.14 osx zip, swap tree, preserve old tree aside for rollback.
# - CI is ALREADY re-based: copy step + grep gates (build-and-release.yml lines 102-105 / 451-454)
#   reference juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h. Only JUCE_VERSION
#   (line 39) + its comment banner (lines 34-38) still say 8.0.9.
# - vendored/JUCE-overrides/ already carries the 8.0.14 headless layout (both patched files present).
# - apply-juce-patches.sh is STALE: PATCH_FILE points at note-expression-juce-8.0.9.patch (line 22)
#   and HEADER_FILE at the OLD path juce_audio_processors/utilities/ (line 39). Against pristine
#   8.0.14 the 8.0.9 patch REJECTS (old header path is a 404 in 8.0.14). Must repoint to the 8.0.14
#   patch + headless header path. CPP_FILE path is unchanged in 8.0.14.
# - Build scope: 39 plugins/* dirs. tache_plugins has NO top-level CMakeLists (auto-skipped by the
#   root GLOB foreach). O-Orbit is CI-skipped via SKIP_PLUGINS. 39 - 2 = 37 buildable plugins.
# - O-Texture's juce_add_plugin target is literally "${PROJECT_NAME}" (a CMake variable), so
#   grepping CMakeLists for target names is fragile — derive VST3/AU target names from the configured
#   `ninja -t targets` list instead (ninja sees the expanded name).
# - Official JUCE download URL pattern (same as CI): https://github.com/juce-framework/JUCE/releases/download/8.0.14/juce-8.0.14-osx.zip
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap local JUCE to pristine 8.0.14, re-apply NE patch, bump CI + apply-script (version-bump commit-set)</name>
  <files>/Users/taylorbrook/JUCE (external tree — replaced), /Users/taylorbrook/JUCE-8.0.9-backup (external — preserved), scripts/apply-juce-patches.sh, .github/workflows/build-and-release.yml</files>
  <action>
    Land the atomic 8.0.9 -> 8.0.14 bump. Local JUCE and CI must match — do all of the following before building.

    1. PRESERVE ROLLBACK: rename the existing install `/Users/taylorbrook/JUCE` to `/Users/taylorbrook/JUCE-8.0.9-backup` (mv, do NOT delete). This is the rollback tree. If a `-8.0.9-backup` already exists, append a timestamp rather than clobbering it.

    2. INSTALL PRISTINE 8.0.14: download the official release zip from the same source CI uses — `https://github.com/juce-framework/JUCE/releases/download/8.0.14/juce-8.0.14-osx.zip` (HTTPS, juce-framework/JUCE official). Download into the scratchpad, unzip (it expands to a `JUCE/` folder), then move that folder to `/Users/taylorbrook/JUCE`. Confirm the swap: `grep 'project(JUCE VERSION' /Users/taylorbrook/JUCE/CMakeLists.txt` must report 8.0.14, and the relocated header `/Users/taylorbrook/JUCE/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h` must exist (the pristine 8.0.14 file, no marker yet).

    3. REPOINT apply-juce-patches.sh (it is stale — still targets the 8.0.9 patch and old header path): set `PATCH_FILE` to `$PATCH_DIR/note-expression-juce-8.0.14.patch`; set `HEADER_FILE` to `$JUCE_DIR/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h`; update the header-comment line that names the 8.0.9 patch and the final post-apply verify echo path to the headless `juce_audio_processors_headless/utilities/` directory. Leave `CPP_FILE` unchanged (that path is identical in 8.0.14). Leave the 8.0.9 patch file on disk (rollback/history reference).

    4. APPLY THE PATCH via the script: run `bash scripts/apply-juce-patches.sh`. It applies `note-expression-juce-8.0.14.patch` with `patch -p1` against the pristine tree (two additive hunks: the Vst3RawEvent struct + onVst3RawEvent hook in the headless header, and the raw-event forward loop before MidiEventList::toMidiBuffer in the VST3 wrapper). Confirm success — the JUCE-NE-PATCH marker must be present in BOTH `/Users/taylorbrook/JUCE/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h` and `/Users/taylorbrook/JUCE/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp`.

    5. BUMP CI: in .github/workflows/build-and-release.yml change `JUCE_VERSION: '8.0.9'` (line 39) to `'8.0.14'` and update the preceding comment banner (lines 34-38) that says "JUCE 8.0.9 — matches the local dev install" to 8.0.14. Do NOT touch the override copy step or the grep-gate paths — they are already on the headless layout. Confirm no `8.0.9` string survives anywhere in that workflow file.

    6. Commit the repo changes (script + workflow) as the version-bump commit-set on the current branch `quick/260719-k5o-juce-ne-rebase-8014`. The `/Users/taylorbrook/JUCE` tree is external and not committed. Do NOT switch branches.
  </action>
  <verify>
    <automated>test -d /Users/taylorbrook/JUCE-8.0.9-backup && grep -q 'project(JUCE VERSION 8.0.14' /Users/taylorbrook/JUCE/CMakeLists.txt && grep -q 'JUCE-NE-PATCH' /Users/taylorbrook/JUCE/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h && grep -q 'JUCE-NE-PATCH' /Users/taylorbrook/JUCE/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp && grep -q "JUCE_VERSION: '8.0.14'" .github/workflows/build-and-release.yml && ! grep -q '8\.0\.9' .github/workflows/build-and-release.yml && grep -q 'note-expression-juce-8.0.14.patch' scripts/apply-juce-patches.sh && grep -q 'juce_audio_processors_headless/utilities' scripts/apply-juce-patches.sh && echo BUMP-OK</automated>
  </verify>
  <done>
    /Users/taylorbrook/JUCE is pristine 8.0.14 with JUCE-NE-PATCH markers in both patched files at the
    headless layout; /Users/taylorbrook/JUCE-8.0.9-backup preserved; apply-juce-patches.sh repointed to
    the 8.0.14 patch + headless header path; build-and-release.yml JUCE_VERSION is 8.0.14 with no
    surviving 8.0.9 string; script + workflow committed on the current branch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fresh full-suite build (VST3 + AU, all 37 plugins) against patched 8.0.14 — capture any fallout, no install</name>
  <files>build/ (fresh CMake configure + ninja output — not committed), scratchpad build log</files>
  <action>
    Prove the patched 8.0.14 tree compiles and links the whole suite. This is BUILD-VERIFICATION ONLY —
    do NOT clear caches or install to system plugin folders (no VST3/AU copies to ~/Library).

    1. FRESH CONFIGURE (mandatory — the existing build/ CMake cache references JUCE 8.0.9 and stale
       module paths): remove the build tree (`rm -rf build`) and configure clean:
       `cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DSKIP_PLUGINS="O-Orbit"`.
       JUCE_DIR is unset locally, so the root CMakeLists resolves `/Users/taylorbrook/JUCE` (now 8.0.14).
       Leave OUARICON_RELEASE unset (dev branding is forced OFF locally; branding is irrelevant since
       nothing is installed). Native single-arch is fine and faster — do not force universal arch.
       Configure must complete with no "JUCE 8.0.9" or missing-module errors.

    2. DERIVE THE VST3+AU TARGET LIST from the configured graph (robust against O-Texture's
       `${PROJECT_NAME}` target — do NOT grep CMakeLists for names): run `ninja -C build -t targets all`,
       extract convenience target names ending in `_VST3` or `_AU` (the phony per-plugin format targets;
       ninja reports O-Texture's expanded name here), dedupe, and confirm the count is 74 (37 plugins x
       VST3+AU). Save the list to the scratchpad.

    3. BUILD IN THE BACKGROUND with keep-going so ALL failures surface in one pass (not just the first):
       run `ninja -C build -k 0 <target-list>` redirected to `build/ninja-build.log` (concrete path the
       verify step greps; not committed), using the Bash tool's run_in_background with a generous
       timeout — the full suite (37 plugins x 2 formats, many WebView-heavy) can take 30-60+ minutes on
       a fresh configure. Monitor to completion; do not poll with foreground sleeps.

    4. TRIAGE FALLOUT: after the build finishes, grep the log for `FAILED:` and `error:`. Expectation
       per research/framework-updates-2026-07.md is ZERO fallout (breaking-change grep = 0 repo
       exposure). If ANY target fails, treat it as a note-expression re-base defect FIRST (the
       vendored patch / applied hunks), not a JUCE public-API break — capture the failing target
       name(s) + the first error line for each (not full logs), diagnose against the applied
       JUCE-NE-PATCH hunks, fix, and re-build the affected targets. Only after ruling out the re-base
       should a JUCE break be considered.

    5. Report: list any failing targets + first errors, or confirm a clean full-suite build. This task
       produces no source changes on a clean pass (nothing to commit); commit only if fallout forced a
       fix.
  </action>
  <verify>
    <automated>VST3=$(ls -d build/plugins/*/*_artefacts/Release/VST3/*.vst3 2>/dev/null | wc -l | tr -d ' '); AU=$(ls -d build/plugins/*/*_artefacts/Release/AU/*.component 2>/dev/null | wc -l | tr -d ' '); FAILED=$(grep -c '^FAILED:' build/ninja-build.log 2>/dev/null || echo 0); echo "VST3=$VST3 AU=$AU FAILED=$FAILED"; [ "$VST3" -ge 37 ] && [ "$AU" -ge 37 ] && [ "$FAILED" -eq 0 ] && echo SUITE-BUILD-OK
  </verify>
  <done>
    Fresh CMake configure against patched JUCE 8.0.14 succeeds; ninja builds all 37 plugins' VST3 + AU
    targets with no `FAILED:` in the log; >=37 .vst3 and >=37 .component artefacts exist under
    build/plugins/*/*_artefacts/Release/. Zero fallout, or any fallout diagnosed as an NE re-base
    defect, fixed, and re-built green. No plugins installed to system folders.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network -> local build toolchain | Pristine JUCE 8.0.14 source zip is downloaded and compiled into every plugin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-BUMP-01 | Tampering | JUCE 8.0.14 osx zip download | low | mitigate | Download only from the official pinned tag URL (github.com/juce-framework/JUCE/releases/download/8.0.14/...) over HTTPS — the identical source CI already trusts. Confirm the unpacked tree reports `project(JUCE VERSION 8.0.14`; markers are added ONLY via the vendored `note-expression-juce-8.0.14.patch`, never hand-edited. |
| T-BUMP-02 | Tampering | Version skew between local install and CI | low | mitigate | Move `/Users/taylorbrook/JUCE` and CI `JUCE_VERSION` to 8.0.14 in the same commit-set; verify step asserts both are 8.0.14 and no 8.0.9 string survives in the workflow. |
| T-BUMP-03 | Denial (rollback loss) | Overwriting the working 8.0.9 tree in place | low | mitigate | Rename the old tree to `/Users/taylorbrook/JUCE-8.0.9-backup` (mv, never rm) before installing 8.0.14, preserving a one-move rollback. |
</threat_model>

<verification>
Overall phase checks:
- Local `/Users/taylorbrook/JUCE` CMakeLists reports project VERSION 8.0.14.
- JUCE-NE-PATCH marker present in both patched files at the 8.0.14 headless layout.
- CI `JUCE_VERSION: '8.0.14'`; no `8.0.9` string anywhere in build-and-release.yml.
- `scripts/apply-juce-patches.sh` targets `note-expression-juce-8.0.14.patch` + the headless header path.
- `/Users/taylorbrook/JUCE-8.0.9-backup` exists (rollback intact).
- Fresh `cmake -B build` configure succeeds; `ninja -k 0` builds all 37 plugins' VST3 + AU with no `FAILED:`.
- >=37 `.vst3` and >=37 `.component` artefacts under `build/plugins/*/*_artefacts/Release/`.
- No plugins copied to `~/Library/Audio/Plug-Ins/` (build-verification only).
</verification>

<success_criteria>
- JUCE bumped 8.0.9 -> 8.0.14 on both the local install and CI, matching, in one commit-set on branch `quick/260719-k5o-juce-ne-rebase-8014`.
- NE patch re-applied to the fresh 8.0.14 tree via the repointed `apply-juce-patches.sh`; markers verified.
- Rollback path preserved (old 8.0.9 tree renamed aside).
- Full 37-plugin macOS suite compiles and links (VST3 + AU) against patched 8.0.14 with zero fallout — or any fallout diagnosed as an NE re-base defect, fixed, and re-built green.
- No system-folder installs performed.
</success_criteria>

<output>
Create `.planning/quick/260719-l26-bump-juce-8-0-9-to-8-0-14-local-install-/260719-l26-SUMMARY.md` when done.
</output>
