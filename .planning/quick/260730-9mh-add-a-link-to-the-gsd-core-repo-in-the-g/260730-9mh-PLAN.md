---
phase: quick-260730-9mh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - QT-9MH-01
must_haves:
  truths:
    - "A reader of README.md's 'Plugin-Local Planning (GSD-Integrated)' section can click through to the gsd-core repository."
    - "The GSD Integration paragraph still reads as a single sentence describing the discuss → research → plan → execute → verify cycle."
  artifacts:
    - "README.md (modified, line ~175)"
  key_links:
    - "README.md GSD Integration paragraph -> https://github.com/open-gsd/gsd-core"
---

<objective>
Add a markdown link to the gsd-core repository in the GSD Integration paragraph of README.md.

Purpose: readers of the System Architecture section currently see "GSD" as an unexplained acronym with no path to the upstream project. A single inline link gives attribution and a way to learn what GSD is.
Output: one modified line in README.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Link the GSD Integration label to the gsd-core repo</name>
  <files>README.md</files>
  <action>
In README.md, locate the paragraph at line 175 in the `### Plugin-Local Planning (GSD-Integrated)` section. It currently begins with a bold label reading GSD Integration, followed by a colon and the sentence about the discuss → research → plan → execute → verify cycle.

Turn the acronym inside that bold label into an inline markdown link pointing at `https://github.com/open-gsd/gsd-core`, keeping the rest of the label and the full sentence byte-identical. The resulting line must be:

`**[GSD](https://github.com/open-gsd/gsd-core) Integration**: Each stage follows the discuss → research → plan → execute → verify cycle with explicit documentation at each phase.`

Use the Edit tool with a scoped replacement of that single line — do not rewrite the file, do not reflow the surrounding paragraph, and do not touch the other GSD mentions elsewhere in README.md (lines 45, 151, 165, 636, 689-693, 759, 769). This is a one-line change; the file's total line count must not change.

URL note: `https://github.com/open-gsd/gsd-core` is the canonical repository, already verified against the `@opengsd/gsd-core` npm registry entry — do not substitute a different org or a docs mirror.
  </action>
  <verify>
    <automated>R=/Users/taylorbrook/Dev/VST-development/README.md; test "$(grep -c 'https://github.com/open-gsd/gsd-core' $R)" = "1" && grep -q '^\*\*\[GSD\](https://github.com/open-gsd/gsd-core) Integration\*\*: Each stage follows the discuss → research → plan → execute → verify cycle with explicit documentation at each phase\.$' $R && grep -q '^### Plugin-Local Planning (GSD-Integrated)$' $R && grep -q '^\*\*Zero drift\*\*' $R && test "$(git -C /Users/taylorbrook/Dev/VST-development diff --numstat -- README.md | awk '{print $1"/"$2}')" = "1/1" && echo VERIFY_OK</automated>
  </verify>
  <done>
README.md line 175 renders the bold GSD Integration label with the acronym hyperlinked to https://github.com/open-gsd/gsd-core; the trailing sentence is unchanged; `git diff --numstat README.md` reports exactly 1 insertion and 1 deletion; the adjacent "Zero drift" and section-heading lines are untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → reader's browser | README renders an outbound hyperlink that readers may follow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9MH-01 | Spoofing | outbound README link target | low | mitigate | URL pinned to the canonical `open-gsd/gsd-core` repo verified via the `@opengsd/gsd-core` npm registry entry; task action forbids substituting another org or mirror |
| T-9MH-02 | Tampering | README.md whole-file rewrite | low | mitigate | Edit-only, single-line scoped replacement; verify gate asserts `git diff --numstat` equals 1 insertion / 1 deletion |
</threat_model>

<verification>
1. `grep -n 'open-gsd/gsd-core' README.md` returns exactly one hit, on the GSD Integration line.
2. `git diff -- README.md` shows a single changed line with no whitespace or reflow noise.
3. Markdown renders as bold text whose first word is a link (verify visually in any markdown preview or on the GitHub blob view after commit).
</verification>

<success_criteria>
- README.md's GSD Integration paragraph links "GSD" to https://github.com/open-gsd/gsd-core
- Exactly one line changed in README.md
- No other GSD mentions or surrounding content modified
- Change committed atomically
</success_criteria>

<output>
Create `.planning/quick/260730-9mh-add-a-link-to-the-gsd-core-repo-in-the-g/260730-9mh-SUMMARY.md` when done
</output>
