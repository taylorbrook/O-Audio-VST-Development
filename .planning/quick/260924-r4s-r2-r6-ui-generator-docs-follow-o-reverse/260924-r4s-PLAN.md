---
phase: quick-260924-r4s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/ui-mockup/references/html-generation.md
  - .claude/skills/ui-mockup/references/ui-design-rules.md
  - .claude/agents/ui-finalization-agent.md
  - modules/ui/instrument-footer-panel/          # deleted (9 tracked files)
  - modules/ui/playable-keyboard/               # deleted (3 tracked files)
  - modules/registry.yaml
  - modules/tuning/scala-tuning-engine/README.md
  - docs/module-system.md
autonomous: true
requirements: [R2, R6]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An agent that follows ui-finalization-agent.md + html-generation.md emits a Family A knob: a conic-gradient seed ring plus a .knob-stem that rotates from getNormalisedValue(), with readouts from getScaledValue(). It emits no dark SVG knob and no JS-side min/max range map."
    - "The documented knob binding has the full O-ReverseDelay interaction: tabindex=0, role=slider, aria-valuetext, arrow-key nudges, and setPointerCapture with pointerup, pointercancel, and lostpointercapture all ending the gesture exactly once. Wheel and double-click reset are bracketed gestures."
    - "The generator copies the canonical i18n block verbatim from scripts/i18n-canon.js. It emits a v[N]-i18n.js table module, exactly one #tips-toggle hover-help switch, and data-tip only through TIP_BINDINGS. The C++ template registers getUiLanguage, setUiLanguage, and getParameterDefaults, and serves /js/i18n.js."
    - "The generated-CSS guidance sets a 9 px text floor and requires a prefers-reduced-motion block for every transition or animation."
    - "ui-design-rules.md Rule 4 defaults to a fixed frame no taller than 800 px. Resizable is reserved for frames that cannot fit, using a fixed aspect ratio and whole-page scaling. Viewport-unit sizing is removed."
    - "The Stage 3 integration checklist names five real gate commands with correct flags and exit semantics: check-i18n, i18n-fr-lint, i18n-zh-lint, check-ui-labels, and boot-all-uis --strict-tips."
    - "modules/ui/instrument-footer-panel and modules/ui/playable-keyboard and the playable-keyboard registry entry are gone. They are deleted only after a live working-tree grep finds 0 consumers."
    - "No path under plugins/ is modified, staged, or committed by this task."
  artifacts:
    - path: ".claude/skills/ui-mockup/references/html-generation.md"
      provides: "Production UI contract (O-ReverseDelay patterns)"
      contains: "lostpointercapture"
    - path: ".claude/skills/ui-mockup/references/ui-design-rules.md"
      provides: "Rule 4 sizing, Rule 8 Family A stem rotation, updated validation checklist"
      contains: "knob-stem"
    - path: ".claude/agents/ui-finalization-agent.md"
      provides: "Generator steps, C++ natives, gate checklist, path-scoped commit"
      contains: "node scripts/check-i18n.js --plugin"
    - path: "modules/registry.yaml"
      provides: "Registry without the dead playable-keyboard entry"
  key_links:
    - from: ".claude/agents/ui-finalization-agent.md"
      to: ".claude/skills/ui-mockup/references/html-generation.md"
      via: "Required reading + Phase 6 generate against the contract"
      pattern: "html-generation\\.md"
    - from: ".claude/skills/ui-mockup/references/html-generation.md"
      to: "scripts/i18n-canon.js"
      via: "canon block copied verbatim at generation time, never retyped"
      pattern: "I18N_CANON_V2"
    - from: ".claude/agents/ui-finalization-agent.md"
      to: "scripts/{check-i18n,i18n-fr-lint,i18n-zh-lint,check-ui-labels,boot-all-uis}.js"
      via: "Phase 9 integration checklist gate section"
      pattern: "node scripts/(check-i18n|i18n-fr-lint|i18n-zh-lint|check-ui-labels|boot-all-uis)\\.js --plugin"
    - from: "canon initI18n() + knob dblclick reset"
      to: "PluginEditor template native functions"
      via: "Phase 7 withNativeFunction getUiLanguage / setUiLanguage / getParameterDefaults"
      pattern: "getUiLanguage|getParameterDefaults"
---

<objective>
This plan carries out **R2** and **R6** from `.planning/quick/260924-nho-review-of-the-ui-and-design-approach-in-/260924-nho-UI-DESIGN-REVIEW.md`.

**R2: teach the UI generator the current contract.** This work is documentation only. It touches three docs:
- `.claude/agents/ui-finalization-agent.md`
- `.claude/skills/ui-mockup/references/html-generation.md`
- `.claude/skills/ui-mockup/references/ui-design-rules.md`

After the change, a newly generated plugin UI follows O-ReverseDelay from its first commit:
- Family A stem + conic knob;
- full pointer-capture lifecycle, including lostpointercapture;
- arrow keys, role=slider, and aria-valuetext;
- getScaledValue() readouts computed from live state.properties;
- the canonical i18n block from `scripts/i18n-canon.js`;
- a single `#tips-toggle` hover-help switch, with data-tip set only through TIP_BINDINGS;
- a 9 px text floor;
- prefers-reduced-motion support.

R2 also removes three things:
- the dark SVG knob;
- range-mirroring value formatting;
- the stale Rule 4 resizable guidance.

The five UI gates are added to the finalization checklist.

**R6: delete the dead `modules/ui/*` modules** (instrument-footer-panel, playable-keyboard) and the playable-keyboard registry entry. This happens only after a live consumer grep at execution time finds zero consumers.

**Adjacent fix.** The agent's Phase 10.5 staging block is edited anyway (it gains v[N]-i18n.js). Its bare `git commit -m` therefore becomes a path-scoped commit, as the project CLAUDE.md requires. It is the only change outside the R2 list, and the SUMMARY must say so.

Purpose: stop new-plugin drift at the source. The review found that the generator's own template produces the knob and readout anti-patterns, and that none of the mandatory requirements added since August was taught to it.

Output:
- three updated docs;
- 12 tracked files deleted;
- registry and two dangling doc pointers fixed;
- `260924-r4s-SUMMARY.md`.

**Hard boundaries:**
- Nothing under `plugins/` is edited or staged.
- O-ReverseDelay's working tree carries another session's uncommitted edits. Read its committed version with `git show HEAD:<path>`, and never modify or stage it.
- Every commit is path-scoped, with branch and status re-checked immediately before it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/quick/260924-nho-review-of-the-ui-and-design-approach-in-/260924-nho-UI-DESIGN-REVIEW.md
@.claude/skills/ui-mockup/references/html-generation.md
@.claude/skills/ui-mockup/references/ui-design-rules.md
@.claude/agents/ui-finalization-agent.md
@scripts/i18n-canon.js

<interfaces>
<!-- Facts verified at planning time (2026-09-24). Cite by NAME in the docs, never by line number: O-ReverseDelay is under active edit. -->

**O-ReverseDelay reference** (read with `git show HEAD:<path>`):
- **`Source/ui/public/js/app.js`**
  - Module-state top block: KNOB_MIN_DEG −135, KNOB_MAX_DEG 135, DRAG_TRAVEL_PX 220, FINE_DRAG_RATE 0.2, NUDGE_STEP 0.02, WHEEL_PX_PER_NUDGE 100, WHEEL_LINE_PX 33, WHEEL_PAGE_PX 400, WHEEL_MAX_NUDGES 4, WHEEL_GESTURE_MS 250.
  - Functions: normToDeg, scaledToNorm(st, scaled) (uses st.properties start/end/skew), updateKnobVisual (FORMAT[id](st.getScaledValue()), stem rotate, aria-valuetext), nudgeStep (floored at properties.interval/span), stepBy, nudge, resetToDefault, bindKnob, bindSelectCombo (properties.choices), loadParameterDefaults, initSettingsPopover, the canon region (trLabel … initI18n), handleTooltipOver/Out, showTooltip, hideTooltip, applyTipsEnabled, initTipsToggle (localStorage key "ord.tipsEnabled", default ON), initTooltips, init().
  - Structure: a single `init();` call at the bottom of the file.
- **`Source/ui/public/css/styles.css`**
  - `:root` tokens: --bg-paper*, --brown-border/-frame/-text, --green-*, --knob-ring/-divider/-core/-border/-seg-1/-seg-2.
  - `--serif: 'Garamond', 'EB Garamond', 'Times New Roman', 'PingFang SC', 'Microsoft YaHei', serif`.
  - Knob rules: `.knob` (radial ring + 20-segment conic-gradient + radial core, 2px border), `.knob:hover/:active` scale, `.knob:focus-visible` (2px dotted --green-mid, offset 3px), `.knob-stem` (absolute, 50%/50%, 2.5px wide, 24px at a 56px knob, transform-origin 50% 100%, translate(-50%,-100%) rotate).
  - `.knob-value` is reserved for knob readouts, because gates census it.
  - One `@media (prefers-reduced-motion: reduce)` block.
- **`Source/ui/public/index.html`**
  - `<html lang="en">`.
  - Knob markup: `.knob-cell` > `.knob#knob-<id>[data-param]` > `.knob-stem`, then `.knob-label[data-i18n]`, then `.knob-value#val-<id>`.
  - Settings popover: `#gear-btn` + `#settings-popover` holding `#lang-select` and `#tips-toggle` (button, aria-pressed, data-i18n-aria, data-i18n="ui.on", data-tip-always).
  - `#tooltip` (role=tooltip).
- **`Source/ui/public/js/i18n.js`**
  - Exports only: LANGUAGES ['en','fr','zh-Hans'], I18N ({t,b} per language), LABELS ({t}), I18N_EXEMPT, TIP_BINDINGS (includes ['#tips-toggle','tips-toggle']), and tr().
- **`Source/PluginEditor.cpp`**
  - Native functions: getParameterDefaults (param->convertFrom0to1(param->getDefaultValue()) per slider id), getUiLanguage, and setUiLanguage (the processor holds `std::atomic<int> uiLanguage`, with languageCode/languageIndex mapping unknown codes to en).
  - getResource branch "/js/i18n.js" served as "application/javascript; charset=utf-8".
  - `.withNativeIntegrationEnabled()` and `.withKeepPageLoadedWhenBrowserIsHidden()`.

**`scripts/i18n-canon.js`**
- Exports I18N_CANON_V2, I18N_CANON_V2_IMPORT (`import { LANGUAGES, I18N, LABELS, TIP_BINDINGS, tr } from './i18n.js';`), I18N_CANON_BODY_START (`let uiLanguage = 'en';`), and I18N_CANON_BODY_END_FN ('initI18n').
- The canon sits between sentinels inside a comment.
- It needs `Juce` in scope, plus the natives getUiLanguage and setUiLanguage.
- The controller must be ONE place: an inline `<script type="module">` at the UI root is accepted (the O-Bitrot shape). Its import specifier is `'./js/i18n.js'` (check-i18n.js controller-resolution comment).

**Gate commands** (usage lines verified in each script header):
- `node scripts/check-i18n.js --plugin <Name>`: exit code = number of failed assertions [1–16].
- `node scripts/i18n-fr-lint.js --plugin <Name>`: exit 2 on any finding.
- `node scripts/i18n-zh-lint.js --plugin <Name>`: exit 2 on any finding; 'mt' rows are counted, not failed.
- `node scripts/check-ui-labels.js --plugin <Name>`: needs Playwright. Exit 77 means "could not verify", which is not a pass.
- `node scripts/boot-all-uis.js --plugin <Name> --strict-tips`: report-only unless --strict-tips; 2 = a DEAD tip binding; 77 = no Playwright.
- The first three run in CI on every push to main (`.github/workflows/ui-static-gates.yml`, commit 85da346f).

**R6 planning-time facts:**
- modules/ui holds 12 tracked files.
- Only playable-keyboard is in modules/registry.yaml (entry under a "UI MODULES" banner, `used_by: []`, registry `version: 1.0.4`). instrument-footer-panel has no entry.
- The `ui` category stays in `categories:`.
- A working-tree grep found 0 build/runtime consumers.
- Non-consumer mentions:
  - `.claude/commands/{module-list,module-upgrade,plugin-focus}.md` and `.claude/skills/module-system/SKILL.md`: illustrative sample output;
  - `docs/module-system.md` (category table);
  - `modules/tuning/scala-tuning-engine/README.md` (one "see instrument-footer-panel" sentence);
  - `plugins/O-Bells/{CHANGELOG,CODE_REVIEW,NOTES}.md` (history);
  - `research/repo-modernization-audit-2026-09-21.md`;
  - `.planning/**`.

**Downstream contract to preserve:**
- gui-agent reads `v[N]-ui.html` as the complete HTML/CSS/JS page. Keep it complete: inline style plus an inline module controller.
- Add exactly one new artifact, `v[N]-i18n.js`, because check-i18n [7]/[8] require i18n.js to be a separate embedded and served file.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): End-to-end knob contract through all three docs, one control family only</name>
  <files>.claude/skills/ui-mockup/references/html-generation.md, .claude/skills/ui-mockup/references/ui-design-rules.md, .claude/agents/ui-finalization-agent.md</files>
  <precondition>`git show HEAD:plugins/O-ReverseDelay/Source/ui/public/js/app.js` succeeds and contains lostpointercapture. Read the committed reference only. The working tree holds another session's uncommitted O-ReverseDelay edits: never edit or stage them.</precondition>
  <action>
**Goal.** Wire ONE path end to end: the continuous-parameter knob. It must run from the reference contract (html-generation.md), through the design rule (ui-design-rules.md Rule 8), to the generator steps and self-check (ui-finalization-agent.md), with the C++ native it needs. Task 2 adds i18n, hover-help, typography, motion, sizing, and gates. It extends this slice, so leave those areas unchanged here, except where step 2b's example replacement touches them.

**Step 1 — read the reference.** `git show HEAD:` each O-ReverseDelay file listed in the interfaces block into the session scratchpad, and read them there. Include the processor header only for the uiLanguage codec. Every citation you write goes by function or selector name (bindKnob, updateKnobVisual, normToDeg, scaledToNorm, nudgeStep, resetToDefault, loadParameterDefaults, `.knob`, `.knob-stem`, `.knob:focus-visible`), never by line number.

**Step 2 — html-generation.md.**

- **(a) Key Principles.**
  - Keep "production-ready" and "no external dependencies".
  - Replace the message-passing API principle with the JUCE 8 relay-state API: `import * as Juce from './js/juce/index.js'`, then `Juce.getSliderState(id)`, `Juce.getToggleButtonState(id)`, `Juce.getComboBoxState(id)`.
  - Add a principle naming O-ReverseDelay as the reference implementation. List its four UI files and `Source/PluginEditor.cpp`, and say to read them via `git show HEAD:` because the working tree can carry another session's edits.
  - Replace the "Responsive" principle with "fixed frame by default, see ui-design-rules.md Rule 4".

- **(b) Structure and binding sections.**
  - In the HTML Structure example, replace the classic `<script>` block with an inline `<script type="module">` skeleton:
    - the Juce import;
    - a module-state top block holding KNOB_IDS, FORMAT, sliderState, paramDefaults and the knob constants listed in the interfaces block;
    - function declarations;
    - one `init()` call at the very bottom, which binds knobs.
  - State the reason for that layout: a top-level reference to a not-yet-initialised binding throws out of module evaluation and kills the whole UI (TDZ).
  - Replace the example's generic control div with one `.knob-cell`.
  - Leave the example's `<style>` block for Task 2.
  - Replace the existing sections "Parameter Binding Conventions", "WebView Communication" and "Value Formatting" with one section, "Parameter Binding and Readouts". It must say:
    - the C++ NormalisableRange is the ONLY source of range and skew, and the page carries no range numbers;
    - FORMAT[id] holds units and decimals only, and is applied to `st.getScaledValue()`;
    - the knob angle is `normToDeg(st.getNormalisedValue())`;
    - defaults convert back through `scaledToNorm(st, value)` using live `st.properties` (start, end, skew);
    - the nudge step is floored at one parameter interval (nudgeStep);
    - choice options come from `st.properties.choices`;
    - every binder listens to BOTH `valueChangedEvent` and `propertiesChangedEvent`;
    - no hand-mirrored min/max attributes and no JS linear range formula, because skewed parameters drift from them.

- **(c) Replace "Control Implementations > Rotary Knob" with "Rotary Knob — Family A (seed cross-section)".**
  - Markup: the O-ReverseDelay `.knob-cell` shape from the interfaces block. The value element carries an em-dash as its initial text until the first update.
  - CSS: the `--knob-*` tokens, plus the `.knob`, `.knob:hover`/`.knob:active`, `.knob:focus-visible` and `.knob-stem` rules, copied verbatim from the committed styles.css. Retyping the 20-stop conic-gradient invites error.
  - Note that `.knob-value` is reserved for knob readouts.
  - Delete the dark SVG knob completely. Do not keep it as a counter-example.

- **(d) Add "Knob Interaction (bindKnob)", listing each behaviour with its reason.** Every item is present in the committed bindKnob.
  - **Focus and ARIA:** `tabindex="0"` and `role="slider"`; updateKnobVisual writes `aria-valuetext` with the same text as the readout.
  - **Arrow keys:** keydown ArrowUp/ArrowRight nudge +1 and ArrowDown/ArrowLeft nudge −1. Each nudge is bracketed by `sliderDragStarted()`/`sliderDragEnded()`, then `preventDefault()`.
  - **Pointer down:** close any open wheel gesture, record the origin, call `sliderDragStarted()`, call `setPointerCapture(e.pointerId)` inside try/catch, and attach pointermove, pointerup, pointercancel and lostpointercapture ON THE KNOB, never on window.
  - **Ending the gesture:** one idempotent onUp handler, guarded by the dragging flag. It calls `sliderDragEnded()` exactly once, detaches all four listeners, and calls `releasePointerCapture` inside try/catch.
  - **Why capture matters:** a release outside the window, or a host modal grab, otherwise leaves the gesture open. In Logic and Live that latches automation write, and the knob keeps following the cursor.
  - **Drag:** relative vertical drag; Shift drags at FINE_DRAG_RATE and re-bases the origin whenever the modifier flips.
  - **Wheel:** ignore horizontal-dominant events and do not preventDefault them. Register the listener with `{ passive: false }`. One gesture bracket stays open, held by a WHEEL_GESTURE_MS timer.
  - **Double-click:** resetToDefault uses the engineering default from the `getParameterDefaults` native function, converted through scaledToNorm. Explain why: the properties payload carries no default, and a JS default table would drift.

- **(e) Replace the Linear Slider and Button/Toggle examples.**
  - **Toggles:** `<button type="button" aria-pressed>` bound to getToggleButtonState.
  - **Choices:** a `<select>` rebuilt from `properties.choices` (the bindSelectCombo pattern).
  - **Linear fader, only when the design demands one:** a native `<input type="range" min="0" max="1" step="any">` over the normalised domain. Its readout comes from getScaledValue(), and pointerdown/pointerup bracket the gesture.

**Step 3 — ui-design-rules.md.**
- **Rule 8:** rewrite it as "Rotary Control Rotation — Family A stem".
  - The seed ring and its lighting stay fixed, which is consistent with Rule 9.
  - Only `.knob-stem` rotates. Its base is anchored at the knob centre (transform-origin 50% 100% plus translate(-50%, -100%)), so it reads as a pointer from the centre.
  - The angle runs −135° to +135° from `getNormalisedValue()` via normToDeg().
  - Remove the old WRONG/CORRECT pair, which inverted the house pattern, and the mirrored-range formula.
  - Point to html-generation.md for the binding.
- **Rule 7:** add to "Critical requirements": listen to propertiesChangedEvent; the knob contract lives in html-generation.md.
- **Rule 11 (debug monitor, test HTML):** the monitor shows the knob's own aria-valuetext (its readout text) and the normalised percentage from getNormalisedValue(). Remove the range-mapping formatter call and the linear range formula.
- **Summary checklist:** rename "Interactive Controls (Test HTML)" to "Interactive Controls", because the rules apply to test and production HTML. Replace the parent-rotation line with a Family A stem line. Add three lines:
  - pointer capture with pointerup, pointercancel and lostpointercapture;
  - arrows + role=slider + aria-valuetext;
  - readouts from getScaledValue().

**Step 4 — ui-finalization-agent.md.**
- **`<required_reading>`:**
  - Add html-generation.md as the production contract.
  - Add the O-ReverseDelay files, read via `git show HEAD:plugins/O-ReverseDelay/...`.
  - Add key pattern 6: readouts and angles come from the SliderState (getScaledValue/getNormalisedValue), never from mirrored ranges.
  - Add key pattern 7: the Family A knob with the full pointer lifecycle and keyboard/ARIA support.
- **Phase 6:**
  - The base is the html-generation.md contract. The `assets/webview-templates/index-template.html` asset is no longer the base, because it carries the retired dark palette. Leave the asset file untouched.
  - Parse the test HTML only for parameter IDs, types and layout. Regenerate all control code from the contract, and never carry the test HTML's control JS or CSS forward.
  - Add Family A and relay-state bullets to "Critical constraints" and to "Verification".
- **Phase 7:**
  - Step 7.6 key sections gain `withNativeIntegrationEnabled()` and a `getParameterDefaults` native function. It returns a DynamicObject mapping each slider parameter id to `convertFrom0to1(getDefaultValue())`.
  - It is registered with `options.withNativeFunction`. Every native the page calls must be registered, or its promise never settles.
- **Self-Validation Checklist:**
  - Add a "Control contract (v[N]-ui.html)" block of grep checks that must find: knob-stem, conic-gradient, setPointerCapture, lostpointercapture, pointercancel, aria-valuetext, getScaledValue, and the slider role.
  - The same block must find NO hand-mirrored min/max data attributes and NO SVG knob, written as regexes.
  - Add `getParameterDefaults` to the .cpp checks.

**Step 5 — commit** (per CLAUDE.md). Immediately before committing, run `git branch --show-current` (expect main) and `git status --short`. Then `git add --` the three docs, and commit with `git commit -m "docs(quick-260924-r4s): R2 knob contract — Family A, pointer lifecycle, ARIA, getScaledValue readouts" --` plus the three doc paths. Never use `-A` or `-a`. Record the SHA from `git log -1 --format=%H -- .claude/agents/ui-finalization-agent.md`.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development && H=.claude/skills/ui-mockup/references/html-generation.md R=.claude/skills/ui-mockup/references/ui-design-rules.md A=.claude/agents/ui-finalization-agent.md && RD=$(git show HEAD:plugins/O-ReverseDelay/Source/ui/public/js/app.js) && case "$RD" in *lostpointercapture*) ;; *) echo "reference lacks lostpointercapture"; exit 1;; esac && for t in knob-stem conic-gradient setPointerCapture lostpointercapture pointercancel aria-valuetext getScaledValue getNormalisedValue propertiesChangedEvent sliderDragStarted ArrowUp getParameterDefaults scaledToNorm O-ReverseDelay; do grep -q -- "$t" $H || { echo "H missing $t"; exit 1; }; done && for t in knob-stem getNormalisedValue lostpointercapture aria-valuetext; do grep -q -- "$t" $R || { echo "R missing $t"; exit 1; }; done && for t in html-generation.md 'git show HEAD:plugins/O-ReverseDelay' getParameterDefaults lostpointercapture knob-stem; do grep -q -- "$t" $A || { echo "A missing $t"; exit 1; }; done && ! grep -nE 'fill="#333"|formatValue\(|data-min=|data-max=|postMessage|receiveParameterUpdate' $H && ! grep -nE 'formatValue\(|max - min' $R && echo TRACER-OK</automated>
  </verify>
  <done>
- TRACER-OK prints.
- html-generation.md documents the Family A knob markup and CSS, and the full bindKnob lifecycle.
- html-generation.md has no dark SVG knob, no `formatValue(`, no `data-min=`/`data-max=`, and no message-passing API.
- Rule 8 describes stem rotation from getNormalisedValue().
- Rules 8 and 11 carry no linear range formula.
- The agent's Required Reading, Phase 6, Phase 7 (getParameterDefaults) and Self-Validation all point at the contract.
- One path-scoped commit touches exactly the three docs: `git show --name-only --format= <sha>` lists only them, and nothing under plugins/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand the contract with page shell, canonical i18n, hover-help, 9 px floor, reduced motion, Rule 4 sizing, and the gate checklist</name>
  <files>.claude/skills/ui-mockup/references/html-generation.md, .claude/skills/ui-mockup/references/ui-design-rules.md, .claude/agents/ui-finalization-agent.md</files>
  <action>
**Goal.** Build out from Task 1's knob slice to cover every remaining R2 item.

**Step 1 — read.**
- `scripts/i18n-canon.js`: its export names and sentinels.
- The `scripts/check-i18n.js` header: assertions 1–16, plus the controller-resolution comment on the inline-module import specifier.
- From O-ReverseDelay at HEAD:
  - i18n.js export shape;
  - app.js initSettingsPopover, initTipsToggle, applyTipsEnabled, initTooltips, showTooltip/hideTooltip, and the init() ordering;
  - index.html gear popover and `#tooltip` markup;
  - styles.css `:root` tokens, `--serif`, and the reduced-motion block;
  - PluginEditor.cpp getUiLanguage/setUiLanguage and the `/js/i18n.js` getResource branch.

**Step 2 — html-generation.md.**

- **(a) HTML Structure example.**
  - The `<style>` block uses O-ReverseDelay's paper tokens (`--bg-paper`, `--brown-text`, `--brown-border`, `--brown-frame`, `--green-*`) and the `--serif` stack with its CJK fallbacks, which zh-Hans needs.
  - Name the brand source: `.claude/aesthetics/ouaricon-naturalist-001/aesthetic.md`.
  - Use `<html lang="en">`.
  - The markup gains the header `#gear-btn` + `#settings-popover` (holding `#lang-select` and `#tips-toggle`) and a `#tooltip role="tooltip"` element.
  - Every visible label is `data-i18n="key"`, and every aria-label is `data-i18n-aria`.
  - Zero authored data-tip or data-tip-title attributes (check-i18n [3]), and zero native title attributes (check-i18n [11]).

- **(b) New section "i18n — the canonical block".**
  - The controller copies the canon VERBATIM from `scripts/i18n-canon.js`, from I18N_CANON_BODY_START through the close of initI18n. Extract it at generation time with a node one-liner that prints `require('./scripts/i18n-canon.js').I18N_CANON_V2`.
  - Never retype the canon. This doc must NOT hold a copy, because a second copy is a drift source.
  - The import line: an inline controller at the UI root imports LANGUAGES, I18N, LABELS, TIP_BINDINGS and tr from `'./js/i18n.js'`. A `js/app.js` controller uses I18N_CANON_V2_IMPORT verbatim.
  - The canon needs `Juce` in scope and the natives getUiLanguage/setUiLanguage.
  - Table-module rules:
    - exports only (check-i18n [7]); no innerHTML and no `<` in any string ([9]);
    - LANGUAGES en/fr/zh-Hans;
    - I18N `{t,b}` per language; LABELS `{t}`;
    - fr carries boolean `reviewed: false`; zh-Hans carries `reviewed: 'mt'`;
    - fr ≠ en unless `sameAsEn: true` ([4]);
    - drafts use `scripts/i18n-fr-glossary.js` and `scripts/i18n-zh-glossary.js`;
    - tr() is copied from O-ReverseDelay's i18n.js unchanged.
  - JS-written labels go through `setLabel(el, 'literal.key')`: two calls in two branches, never a ternary ([13]).
  - Pin widths at the widest language, and let check-ui-labels prove the fit.

- **(c) New section "Hover help and tooltips".**
  - **The switch:**
    - exactly one switch, `id="tips-toggle"`, beside `#lang-select` in the popover (check-i18n [16]);
    - a `<button type="button">` with aria-pressed, data-i18n-aria, `data-i18n="ui.on"` and data-tip-always;
    - data-tip-always goes on that switch and `#gear-btn` only;
    - bound by the TIP_BINDINGS row for `#tips-toggle`, with a matching I18N entry;
    - state lives in localStorage under a per-plugin key (`<prefix>.tipsEnabled`), defaults ON, and survives a private-mode throw;
    - applyTipsEnabled relabels the switch with setLabel ui.on / ui.off, in two branches.
  - **Tooltip copy:**
    - data-tip and data-tip-title are written only by applyI18n from TIP_BINDINGS;
    - every knob gets a TIP_BINDINGS row.
  - **Renderer:**
    - delegated document mouseover/mouseout resolving `closest('[data-tip]')`;
    - textContent only;
    - suppressed from a capture-phase pointerdown until pointerup;
    - measured, then clamped inside the frame.
  - **init() order:** initSettingsPopover → initI18n → initTooltips → initTipsToggle, each in its own try/catch.

- **(d) Styling Guidelines.**
  - Replace the dark custom-property example with the O-ReverseDelay token block.
  - Replace the 12 px minimum with a "9 px" text floor for every text element. Cite aesthetic.md: parameter labels are 9–11 px.
  - Keep the WCAG AA line.
  - Text uses `--brown-text`; `--brown-border` is for borders and decoration.

- **(e) New section "Motion".**
  - Every transition or animation the page emits needs a counterpart inside `@media (prefers-reduced-motion: reduce)`. That includes the knob hover/active scale.
  - O-ReverseDelay's block is the reference: it swaps a pulse for a static state.

- **(f) Replace "Responsive Design" with a short "Sizing" section.** Point to Rule 4. Fixed frames get no breakpoints.

- **(g)** Keep "Performance".

**Step 3 — ui-design-rules.md.**

- **(a) Rewrite Rule 4 decision criteria with suite evidence** from review §5.6:
  - 40/44 plugins are fixed.
  - Only O-MicrotonalSampler (720×480–1600×1080), O-Orbit, O-simpleBeatmaker and O-simpleSubtractive are resizable.
  - O-Prism (1200×800, five tabs) and O-Octagon (a visualizer) are both fixed.
- **(b) State the new rule.** Parameter count and page count do not decide sizing.
  - Default to a fixed frame no taller than 800 px. A 13-inch laptop at its default scaled resolution leaves about 800 px below the menu bar and a DAW's plugin-window title bar, and five shipped frames of 820–980 px fall off-screen.
  - If a design cannot fit, make it resizable per review R7:
    - `setResizable(true, true)`, plus `getConstrainer()->setFixedAspectRatio(designW / designH)`, plus `setResizeLimits`;
    - the page scales its fixed composition as a whole, with CSS zoom or a transform scale on the root computed from `window.innerWidth / designWidth` in a resize listener;
    - it does not reflow;
    - no shipped plugin uses this scale approach yet, so the Stage 3 checklist carries a hands-on DAW resize check.
- **(c) Resizable Pattern CSS.** Delete the viewport-unit clamp() sizing (it contradicts Rule 1), leaving body at `width: 100%; height: 100%`. Update both "When to use" lists to match.
- **(d) Summary checklist.**
  - The "Sizing strategy" line becomes: fixed ≤ 800 px tall, or resizable with fixed-aspect scale per Rule 4.
  - Add Required items:
    - canonical i18n block copied verbatim;
    - exactly one `#tips-toggle`;
    - data-tip only via TIP_BINDINGS;
    - 9 px text floor;
    - prefers-reduced-motion counterpart for every transition or animation.

**Step 4 — ui-finalization-agent.md.**

- **(a) Required reading.** Add `scripts/i18n-canon.js` and key patterns 8–11: canon verbatim; one `#tips-toggle`; 9 px floor; reduced motion.

- **(b) Phase 6.**
  - Output is `v[N]-ui.html`, a complete page with inline `<style>` and an inline `<script type="module">` controller. That is the shape gui-agent reads and check-i18n resolves.
  - Plus ONE new artifact, `v[N]-i18n.js`, the table module:
    - en is authored from the parameter-spec.md descriptions;
    - fr and zh-Hans are machine-drafted with the two glossaries, at `reviewed: false` and `reviewed: 'mt'`.
  - If the mockup has no gear popover, add it top-right in the header and say so in the integration checklist.
  - Add the canon-copy procedure.

- **(c) Phase 7.**
  - Add getUiLanguage/setUiLanguage natives. The processor holds the language as an atomic int, persisted in getStateInformation/setStateInformation, with unknown codes mapped to en. List that processor-side work as an integration-checklist item for gui-agent.
  - Add the `/js/i18n.js` getResource branch served as `application/javascript; charset=utf-8`.
  - The window size follows Rule 4.

- **(d) Phase 8.** SOURCES gains `Source/ui/public/js/i18n.js`, because check-i18n [8] requires it to be both embedded and served.

- **(e) Phase 9 checklist template.**
  - Step 1 gains: copy `v[N]-i18n.js` to `Source/ui/public/js/i18n.js`.
  - Add section "8. UI gates". These run from the repo root and all must pass before the Stage 3 commit. List the five commands exactly as in the interfaces block, with `[PluginName]`, and give each one's exit semantics.
  - Note that 77 means NOT verified.
  - Note that the first three also run in CI on every push to main (`.github/workflows/ui-static-gates.yml`), so a failure turns main red.
  - Add section "9. Hands-on (Standalone or DAW)":
    - Tab reaches every knob, and the arrow keys move it;
    - dragging out of the window and releasing ends the gesture, so the knob stops following the cursor;
    - double-click resets to the default;
    - the hover-help switch toggles and persists across reopen;
    - switching the language to fr and zh-Hans relabels the page with no clipping;
    - with macOS Reduce Motion on, no animation runs;
    - if the frame is resizable, resizing keeps the aspect ratio and scales the page.

- **(f) Phase 10.5.**
  - Build a FILES bash array of the generated files, including `v[N]-i18n.js`, plus `../parameter-spec.md` when it was created.
  - Stage with `git add -- "${FILES[@]}"`.
  - Before committing, re-check `git branch --show-current` and `git status --short`, per the project CLAUDE.md concurrent-session rule.
  - Commit with `git commit -m "..." -- "${FILES[@]}"`.
  - Update the message's file count.

- **(g) Consistency sweep.**
  - The file counts in the role, task, success criteria and commit message move up by one for `v[N]-i18n.js`: 6 implementation files, or 7 when parameter-spec.md is also generated.
  - The JSON report `files_created` examples gain `v[N]-i18n.js`.
  - The Self-Validation "File generation" list gains it.
  - Self-Validation gains an "i18n, hover-help, type, motion" block:
    - a canon containment check: a node one-liner that collapses whitespace in both the page and the canon body (sliced from I18N_CANON_BODY_START out of I18N_CANON_V2) and asserts the page contains the body;
    - zero data-tip/data-tip-title attributes in `v[N]-ui.html`;
    - exactly one `id="tips-toggle"`;
    - no CSS font-size below 9 px;
    - prefers-reduced-motion present.

**Step 5 — commit.** Same discipline as Task 1: re-check branch and status, `git add --` the three docs, and commit with `git commit -m "docs(quick-260924-r4s): R2 i18n canon, hover-help, 9 px floor, reduced motion, Rule 4 sizing, UI gate checklist" --` plus the three paths. Record the SHA.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development && H=.claude/skills/ui-mockup/references/html-generation.md R=.claude/skills/ui-mockup/references/ui-design-rules.md A=.claude/agents/ui-finalization-agent.md && for t in i18n-canon.js I18N_CANON_V2 I18N_CANON_BODY_START tips-toggle TIP_BINDINGS data-tip-always setLabel prefers-reduced-motion getUiLanguage '9 px' ouaricon-naturalist-001; do grep -qF -- "$t" $H || { echo "H missing $t"; exit 1; }; done && for t in prefers-reduced-motion tips-toggle O-MicrotonalSampler setFixedAspectRatio '800 px' '9 px'; do grep -qF -- "$t" $R || { echo "R missing $t"; exit 1; }; done && for t in 'node scripts/check-i18n.js --plugin' 'node scripts/i18n-fr-lint.js --plugin' 'node scripts/i18n-zh-lint.js --plugin' 'node scripts/check-ui-labels.js --plugin' 'node scripts/boot-all-uis.js --plugin' '--strict-tips' 'ui-static-gates.yml' 'v[N]-i18n.js' getUiLanguage setUiLanguage '/js/i18n.js' 'git branch --show-current' '"${FILES[@]}"' i18n-canon.js; do grep -qF -- "$t" $A || { echo "A missing $t"; exit 1; }; done && ! grep -nE '4a9eff|#2b2b2b|Inter, system-ui|Minimum font size: 12px|@media \(max-width|function initI18n\(\)' $H && ! grep -nE 'font-size: *[0-8](\.[0-9]+)?px' $H $R && ! (grep -E '[0-9.]+vw' $R | grep -vE '100d?vw' | grep -q .) && ! grep -qF '| 13+ parameters | Complex | **Resizable** |' $R && M=$(grep -ohE 'scripts/[A-Za-z0-9_-]+\.js' $H $R $A | sort -u | while read -r p; do test -f "$p" || echo "$p"; done) && test -z "$M" && test -f .github/workflows/ui-static-gates.yml && grep -q -- '--strict-tips' scripts/boot-all-uis.js && echo EXPANSION-OK</automated>
  </verify>
  <done>
- EXPANSION-OK prints.
- **html-generation.md:**
  - carries the canon procedure (by export name, with no pasted canon body), the hover-help contract, the 9 px floor, and the reduced-motion rule;
  - has no dark palette, no 12 px minimum, and no breakpoint CSS.
- **ui-design-rules.md:**
  - Rule 4 defaults to fixed ≤ 800 px with fixed-aspect scaling as the exception;
  - it has no viewport-unit clamp() and no "13+ parameters means resizable" row.
- **The agent doc:**
  - emits `v[N]-i18n.js`;
  - registers getUiLanguage/setUiLanguage and serves `/js/i18n.js`;
  - lists the five gate commands with exit semantics, plus the hands-on section;
  - commits with a pathspec.
- Every `scripts/*.js` path cited in the three docs exists.
- The one path-scoped commit touches only the three docs.
  </done>
</task>

<task type="auto">
  <name>Task 3: R6 — delete the dead modules/ui modules after a live zero-consumer check</name>
  <files>modules/ui/instrument-footer-panel/, modules/ui/playable-keyboard/, modules/registry.yaml, modules/tuning/scala-tuning-engine/README.md, docs/module-system.md</files>
  <action>
<!-- planner-discipline-allow: playable-keyboard -->
<!-- planner-discipline-allow: instrument-footer-panel -->
This task is independent of Tasks 1–2. MUTABLE-SCOPE AUTHORITY applies: the planning-time "0 consumers" result is NOT evidence. Re-derive it live, now.

**Step 1 — live consumer check over the WORKING TREE.** Plain `grep -rnE`, not `git grep`, so that other sessions' uncommitted files and gitignored configure-time copies under `plugins/*/Source/ui/public/modules/` are seen.
- Pattern: `instrument-footer-panel|playable-keyboard|instrument_footer_panel|playable_keyboard|modules/ui/`
- Save all output to the session scratchpad.

**CONSUMER locations.** Any hit here ABORTS the deletion:
- `plugins/*/CMakeLists.txt`, `plugins/*/Source`, `plugins/*/Resources`, `plugins/*/tests`;
- the root `CMakeLists.txt`, `modules/cmake`;
- `scripts/`, `.github/`, `tools/`, `vendored/`;
- any file under `.claude/` whose hit is the `modules/ui/` path, meaning something loads files from it;
- a non-empty `used_by` for playable-keyboard in `modules/registry.yaml`, checked with `yq '.modules[] | select(.name == "playable-keyboard") | .used_by'`.

**KNOWN NON-CONSUMER mentions.** These do not abort:
- `.claude/commands/module-list.md`, `.claude/commands/module-upgrade.md`, `.claude/commands/plugin-focus.md`, `.claude/skills/module-system/SKILL.md`: illustrative sample output of commands that read the registry live;
- `docs/module-system.md`;
- `modules/registry.yaml`;
- `modules/tuning/scala-tuning-engine/README.md`;
- `plugins/O-Bells/CHANGELOG.md`, `plugins/O-Bells/CODE_REVIEW.md`, `plugins/O-Bells/NOTES.md`: history, and no plugin edits are allowed;
- `research/repo-modernization-audit-2026-09-21.md`;
- anything under `.planning/`.

**Any other file** that names a module, outside `modules/ui/` itself, counts as a possible consumer. Abort and report it.

**On abort:**
- delete nothing and edit nothing;
- write the hits into the SUMMARY under "R6 aborted — consumers found";
- finish the plan. Tasks 1–2 stand.

**Step 2 — delete.** `git rm -r -q -- modules/ui/instrument-footer-panel modules/ui/playable-keyboard`. This removes the files from both the index and the working tree, and must not be an index-only removal.
- Then run `git status --short --ignored -- modules/ui`.
- If only OS litter such as `.DS_Store` remains, remove it.
- If anything else remains, stop and report it.

**Step 3 — modules/registry.yaml.**
- Remove the playable-keyboard entry together with the "UI MODULES" banner comment block above it: from the banner's first rule line through the entry's `used_by: []`. Leave exactly one blank line before the "HOW TO USE THIS REGISTRY" block.
- KEEP the `ui` category under `categories:`. It is taxonomy, not a module.
- Bump `version` by one minor step, per the header note ("bump minor/major by hand for significant changes"). It is 1.0.4 → 1.1.0 if unchanged since planning; otherwise bump the minor of whatever is present.
- Set `last_updated` to today's date (`date +%F`).
- Do NOT run `scripts/regen-registry-used-by.sh`. It rewrites every `used_by` block from disk truth, which is out of scope.

**Step 4 — dangling pointers only.**
- In `modules/tuning/scala-tuning-engine/README.md`, delete the single sentence that sends readers to the deleted footer-panel module for "simpler modules that integrate INTO existing UI".
- In `docs/module-system.md`, set the category table's `ui` row Examples cell to `— (none registered)`.
- Leave the illustrative `.claude` sample outputs, plugin history files and the research audit unchanged. List them in the SUMMARY with that reason.

**Step 5 — commit.**
- Re-check `git branch --show-current` (expect main) and `git status --short`.
- Stage the two docs and the registry with `git add --`. The deletions are already staged by git rm.
- Commit: `git commit -m "chore(quick-260924-r4s): R6 delete dead modules/ui (instrument-footer-panel, playable-keyboard) — 0 consumers" -- modules/ui/instrument-footer-panel modules/ui/playable-keyboard modules/registry.yaml modules/tuning/scala-tuning-engine/README.md docs/module-system.md`.
- Record the SHA with `git log -1 --format=%H -- modules/registry.yaml`.
- Confirm `git show --name-only --format= <sha>` lists no `plugins/` path and shows the 12 deletions.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development && test ! -e modules/ui/instrument-footer-panel && test ! -e modules/ui/playable-keyboard && L=$(git ls-files modules/ui) && test -z "$L" && ! grep -nE 'playable-keyboard|instrument-footer-panel' modules/registry.yaml modules/tuning/scala-tuning-engine/README.md docs/module-system.md && python3 -c "import yaml; d=yaml.safe_load(open('modules/registry.yaml')); assert all(m['name']!='playable-keyboard' for m in d['modules']); assert any(c['name']=='ui' for c in d['categories'])" && ! grep -rnE 'instrument-footer-panel|playable-keyboard|modules/ui/' plugins/*/CMakeLists.txt plugins/*/Source plugins/*/Resources scripts .github CMakeLists.txt modules/cmake 2>/dev/null && echo R6-OK</automated>
  </verify>
  <done>
**If no consumer was found:**
- R6-OK prints.
- Both module directories are gone, from the index and from disk.
- The registry parses and has no playable-keyboard entry. It keeps the `ui` category, and its version and last_updated are bumped.
- Neither dangling doc pointer names a deleted module.
- One path-scoped commit contains exactly the 12 deletions plus 3 doc/registry edits, and no `plugins/` path.

**If a consumer was found:** nothing was deleted, and the SUMMARY records the grep hits under "R6 aborted — consumers found".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → future agents | ui-finalization-agent.md and the ui-mockup references are executed as instructions by future agents. A wrong command or pattern here spreads into every plugin generated afterwards. |
| this session ↔ concurrent sessions | Sessions share one checkout, `.git/index` and HEAD. Other sessions have uncommitted edits in plugins/O-Prism and plugins/O-ReverseDelay. |
| module tree → plugin builds | Deleting a module that a CMakeLists or UI still references breaks that plugin's configure or build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-r4s-01 | Tampering | git index / commits | high | mitigate | Every commit is `git commit -m ... -- <explicit paths>`, with `git branch --show-current` and `git status --short` re-checked immediately before. There is no `-A`/`-a`. After each commit, `git show --name-only` must list no `plugins/` path. |
| T-r4s-02 | Tampering | O-ReverseDelay working tree (another session's edits) | high | mitigate | The reference is read only via `git show HEAD:<path>` into the scratchpad. The executor never opens those working-tree files for writing and never stages them. |
| T-r4s-03 | Denial of Service | plugin builds after R6 | high | mitigate | A live working-tree grep runs over CMake, plugin Source/Resources/tests (including gitignored module copies), scripts, .github and .claude. Any consumer hit aborts before `git rm`. The post-commit verify re-greps. |
| T-r4s-04 | Tampering | generated agent instructions | medium | mitigate | Every cited `scripts/*.js` path is checked with `test -f`. Flags are checked against the script usage headers. The canon is referenced by export name and copied at generation time, never pasted into docs, so no second drifting copy exists. |
| T-r4s-05 | Information Disclosure / Tampering | generated WebView pages (tooltip and i18n copy) | low | mitigate | The contract keeps textContent-only tooltips and bans innerHTML and `<` in i18n.js (check-i18n [9]). It deletes native title attributes (check-i18n [11]). These are cited in html-generation.md. |
| T-r4s-06 | Repudiation | R6 decision trail | low | mitigate | The full consumer-grep output and the non-consumer allowlist rationale go into the SUMMARY. |
</threat_model>

<verification>
Run all three task verify commands after the final commit. They must print TRACER-OK, EXPANSION-OK, and R6-OK (or the documented R6 abort).

For each of this task's commit SHAs, capture `N=$(git show --name-only --format= <sha>)` first (a failed git must not read as clean). Then check that `N` is non-empty and that `printf '%s\n' "$N" | grep '^plugins/'` returns nothing.

Spot-read the three docs end to end once, checking two things:
- No section still describes the retired pattern in prose. That means no dark knob, no parent-rotation rule, no parameter-count-driven resizing, and no message-passing API.
- The agent's file counts, JSON examples, staging array and Self-Validation list all agree on `v[N]-i18n.js`.
</verification>

<success_criteria>
- **R2 complete.** All three docs teach the O-ReverseDelay contract:
  - Family A knob;
  - pointer lifecycle including lostpointercapture;
  - arrow keys + role=slider + aria-valuetext;
  - getScaledValue()/state.properties readouts;
  - canonical i18n block from scripts/i18n-canon.js;
  - single `#tips-toggle`;
  - data-tip via TIP_BINDINGS;
  - 9 px floor;
  - prefers-reduced-motion.
- **Retired patterns gone.** The dark knob and range-mirroring formatter are deleted. Rule 4 is corrected.
- **Gates in the checklist.** The finalization checklist names the three static gates, check-ui-labels.js, and boot-all-uis.js --strict-tips, with real invocations.
- **R6 complete.** Both dead modules and the registry entry are deleted after a live zero-consumer check, or the plan aborts with evidence.
- **Clean history.** Three path-scoped commits (two for R2, one for R6), none touching `plugins/`.
</success_criteria>

<output>
Create `.planning/quick/260924-r4s-r2-r6-ui-generator-docs-follow-o-reverse/260924-r4s-SUMMARY.md`. It must contain:
- per-task commit SHAs;
- the R6 consumer-grep output and the non-consumer allowlist, with the reason for each entry;
- the adjacent fix (the agent's path-scoped commit) flagged as outside the R2 list;
- items left for a later task:
  - `.claude/skills/ui-mockup/assets/webview-templates/index-template.html` and `assets/ui-yaml-template.yaml` still carry the retired dark palette, and are no longer the Phase 6 base;
  - `ui-design-agent.md` was not updated;
  - `ui-mockup/SKILL.md` file counts and names already disagreed with the agent before this task.
</output>
