---
phase: quick-260921-in9
plan: 01
type: execute
wave: 1
depends_on: [quick-260921-hno, quick-260921-i75]
subsystem: build-infrastructure
tags: [juce, toolchain, note-expression, supply-chain, version-bump]
autonomous: true

files_modified: []
# Deliberately empty. This task's work is OUTSIDE the repo (/Users/taylorbrook/JUCE).
# Repo source edits are NOT expected — see <non_goals>. The only commit is the
# quick-task planning artifacts under .planning/quick/260921-in9-*/.

requirements: [Q-in9-01, Q-in9-02, Q-in9-03]

user_setup: []

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 3
  confidence: low        # sample_count=0, factor=1.0 (estimate-calibration, 2026-09-21)

must_haves:
  truths:
    - "`/Users/taylorbrook/JUCE` is JUCE 8.0.15 — `modules/juce_core/juce_core.h` reads `version: 8.0.15`, matching `.github/juce-version.txt`."
    - "That tree carries the JUCE-NE-PATCH note-expression surface in both patched files."
    - "The two patched files in the live tree are byte-identical (CR-normalized) to `vendored/JUCE-overrides/modules/...` — the local patch mechanism and the CI copy mechanism agree at 8.0.15."
    - "The prior 8.0.14 tree survives as `/Users/taylorbrook/JUCE-8.0.14-backup` — nothing outside git was deleted."
    - "O-Lyrica rebuilds, installs, and passes auval against the 8.0.15 tree, and the installed binary reports JUCE v8.0.15 (not the prior point release)."
    - "`bash scripts/check-juce-overrides.sh` (network mode) is still exit 0 after the swap."
    - "No repo source file was edited; the pre-existing uncommitted `.claude/agent-memory/research-planning-agent.md` is still untouched."
  artifacts:
    - /Users/taylorbrook/JUCE (patched 8.0.15 tree)
    - /Users/taylorbrook/JUCE-8.0.14-backup (the displaced tree, kept)
    - ~/Library/Audio/Plug-Ins/VST3/O-Lyrica-dev.vst3 (rebuilt against 8.0.15)
  key_links:
    - "pin -> patch filename: `apply-juce-patches.sh:39` derives `note-expression-juce-<pinned>.patch` from `.github/juce-version.txt`. The pin is already 8.0.15 and `scripts/juce-patches/note-expression-juce-8.0.15.patch` already exists, so the script resolves the RIGHT patch with no argument."
    - "patched tree -> compiled plugin: `modules/tuning/note-expression/cpp/NoteExpression.h:186` declares `void onVst3RawEvent (const Vst3RawEvent& e) override`, pulled into O-Lyrica by `ouaricon_add_module(OLyrica note-expression)` (plugins/O-Lyrica/CMakeLists.txt:80). If the swapped tree lost the patch, `Vst3RawEvent` is an unknown type and `override` has no base virtual — the build FAILS. A green build is the note-expression proof."
    - "new tree mtimes -> ninja: `add_subdirectory(/Users/taylorbrook/JUCE JUCE)` (CMakeLists.txt:41) means ninja tracks JUCE sources by mtime. A zip unpack preserves ARCHIVE mtimes, which can predate the existing build/ objects — ninja would then skip every JUCE-derived object and the '8.0.15 build' would silently be 8.0.14 machine code. T2 refreshes mtimes; T3's version-string check is the independent proof the refresh worked."
    - "pristine tree -> both gates: `check-juce-overrides.sh` CHECK 3 and `gen-juce-overrides.sh --check` both require a PRISTINE `--juce-root`. They must run BEFORE the patch, in T1. After T2 the live tree is patched and `--juce-root /Users/taylorbrook/JUCE` MUST fail by design — see <traps>."
---

<objective>
Bring the local JUCE checkout at `/Users/taylorbrook/JUCE` up to 8.0.15 so it matches the pin
that quick task 260921-i75 already moved, and prove the Ouaricon note-expression fork still
works on it end to end: fresh pristine 8.0.15 tree -> both provenance gates green against it ->
patch applied -> byte-equal to `vendored/JUCE-overrides/` -> swapped in with the 8.0.14 tree kept
as a renamed backup -> O-Lyrica rebuilt, installed, and auval-clean.

Purpose: the pin, the patch, the vendored overrides and CI are all at 8.0.15; only the local
dev toolchain is still 8.0.14. Until this closes, every local build is compiled against a
different JUCE than every CI build — the exact silent divergence the 260921-hno/i75 gates were
built to prevent, just on the one axis those gates cannot see.

Output: a patched 8.0.15 tree at `/Users/taylorbrook/JUCE`, an untouched 8.0.14 tree at
`/Users/taylorbrook/JUCE-8.0.14-backup`, and recorded command output proving the swap held.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Repo mechanisms this task drives (read the headers, not the whole scripts)
@scripts/apply-juce-patches.sh
@scripts/check-juce-overrides.sh
@scripts/gen-juce-overrides.sh

# The two preceding quick tasks this one completes
@.planning/quick/260921-hno-generate-vendored-juce-overrides-from-sc/
@.planning/quick/260921-i75-bump-github-juce-version-txt-to-8-0-15-a/

Project skill: `Skill("spike-findings-VST-development")` covers the VST3 Note Expression patch —
read it before judging any note-expression symptom.
</context>

<facts_measured_at_planning_time>
Measured 2026-09-21 in the working tree. Re-verify anything you act on; do not assume.

| Fact | Value |
|---|---|
| `.github/juce-version.txt` | `8.0.15` |
| `/Users/taylorbrook/JUCE` version | `8.0.14` (`modules/juce_core/juce_core.h` -> `version: 8.0.14`) |
| `/Users/taylorbrook/JUCE` is a git repo? | **No** — `git -C` fails. Not git-recoverable. |
| Local tree patched? | Yes — `JUCE-NE-PATCH` present in both target files |
| Existing backup precedent | `/Users/taylorbrook/JUCE-8.0.9-backup` (141M) — follow this naming |
| JUCE tree size / free space | 153M tree; 50Gi free on `/System/Volumes/Data` |
| CMake JUCE resolution | `CMakeLists.txt:36-41` — `$ENV{JUCE_DIR}` if set, else hardcoded `/Users/taylorbrook/JUCE` on macOS |
| `build/CMakeCache.txt` | `JUCE_SOURCE_DIR=/Users/taylorbrook/JUCE`; stale version-derived strings `JUCE_INSTALL_DESTINATION=lib/cmake/JUCE-8.0.14`, `JUCE_TOOL_INSTALL_DIR=bin/JUCE-8.0.14` (install-only, never invoked — cosmetic) |
| Patch resolves from the pin | `apply-juce-patches.sh:23` honors `$JUCE_DIR`; `:39` derives the patch name from the pin; `:56` skips when the marker is already present; `:72` normalizes CRLF->LF first |
| `8.0.14` vs `8.0.15` patch files | identical except the header/self-reference lines |
| O-Lyrica CMake target | `OLyrica` (folder `O-Lyrica`) — `build-and-install.sh` resolves this itself, pass the FOLDER name |
| O-Lyrica NE wiring | `plugins/O-Lyrica/CMakeLists.txt:80` `ouaricon_add_module(OLyrica note-expression)`; `PluginProcessor.h:134` overrides `getVST3ClientExtensions()` |
| Installed baseline binary | `~/Library/Audio/Plug-Ins/VST3/O-Lyrica-dev.vst3` -> `strings` contains exactly **1** occurrence of the 8.0.14 JUCE version string |
| `juce-8.0.15-osx.zip` (recorded by 260921-i75) | sha256 `4ed8602cc3e7bfa7cac7d61280ff2a0f6e1e0faf17824adf1243ad4e7a6de19e`, size `52636418` bytes |
| CI's own JUCE source | the same `juce-<pin>-osx.zip` release asset (`.github/workflows/ci-tests.yml:122`) |
</facts_measured_at_planning_time>

<tasks>

<task type="tracer">
  <name>Task 1: Stage a pristine JUCE 8.0.15 tree and prove both provenance gates green against it — live tree untouched</name>
  <files>
    /Users/taylorbrook/JUCE-8.0.15-staging/   (created; outside the repo)
    (no repo files modified)
  </files>
  <precondition>Network reachable for github.com release downloads, and `curl`, `unzip`, `shasum`, `patch` are on PATH. At least 1 GiB free on the volume holding /Users/taylorbrook.</precondition>
  <reversibility rating="reversible">Creates a new staging directory only; `/Users/taylorbrook/JUCE` and the repo are not touched. Undo is `rm -rf /Users/taylorbrook/JUCE-8.0.15-staging`.</reversibility>
  <action>
    This is the tracer: prove the whole chain on a staged tree before anything destructive happens.
    If any step here fails, STOP and report — do not touch `/Users/taylorbrook/JUCE`.

    Preflight. Confirm free space and that `curl`, `unzip`, `shasum` and `patch` resolve. Confirm
    `.github/juce-version.txt` still reads `8.0.15` — derive the version into a shell variable from
    that file rather than typing a literal, matching how every script in this repo resolves it.

    Download. Fetch the official release asset
    `https://github.com/juce-framework/JUCE/releases/download/8.0.15/juce-8.0.15-osx.zip` with
    `curl -L --fail --show-error --proto '=https' --tlsv1.2`, into `/Users/taylorbrook/JUCE-8.0.15-staging/`.
    This specific asset is chosen over a git clone because it is the byte-identical artifact CI
    consumes (ci-tests.yml:122), so the local toolchain and CI end up on the same bytes.

    Verify the download before unpacking (T-in9-01 mitigation). Record `shasum -a 256` and the byte
    size, and compare both against the values 260921-i75 recorded from an independent session:
    sha256 `4ed8602cc3e7bfa7cac7d61280ff2a0f6e1e0faf17824adf1243ad4e7a6de19e`, size `52636418`.
    A MISMATCH on either is a HARD HALT — delete the download, do not unpack, report it. Do not
    "re-download and see if it matches the second time".

    Unpack with `unzip -q` into the staging directory; the archive contains a top-level `JUCE/`,
    so the pristine tree lands at `/Users/taylorbrook/JUCE-8.0.15-staging/JUCE`. Confirm the tree
    identifies itself as 8.0.15 by reading the `version:` field of
    `modules/juce_core/juce_core.h` — the tree's own self-report, not the filename you downloaded.

    Gate A — upstream provenance:
    `bash scripts/check-juce-overrides.sh --juce-root /Users/taylorbrook/JUCE-8.0.15-staging/JUCE`
    must exit 0. This re-fingerprints the two override-source files from THIS download and matches
    them against MANIFEST.txt, whose upstream fingerprints were taken from raw.githubusercontent.com
    in a prior session. Two independent distribution channels agreeing is the real supply-chain
    check, and it is stronger than the sha256 alone.

    Gate B — patch-derivation:
    `bash scripts/gen-juce-overrides.sh --check --juce-root /Users/taylorbrook/JUCE-8.0.15-staging/JUCE`
    must exit 0. It applies `note-expression-juce-8.0.15.patch` to a temp copy of this pristine tree
    and asserts the result equals `vendored/JUCE-overrides/` exactly (CR-normalized). Green here is
    advance proof that T2's real patch application will apply cleanly and produce the right bytes.

    Confirm the staged tree is still pristine after both gates — Gate B works in a temp dir and must
    not have written into the staging tree. Assert zero `JUCE-NE-PATCH` occurrences in the two
    target paths. A non-zero count here means the gate wrote in place and the tree can no longer be
    used as a pristine baseline.

    Capture the literal output of both gates for the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development && S=/Users/taylorbrook/JUCE-8.0.15-staging/JUCE && test -d "$S" && grep -q "version: *8\.0\.15" "$S/modules/juce_core/juce_core.h" && bash scripts/check-juce-overrides.sh --juce-root "$S" && bash scripts/gen-juce-overrides.sh --check --juce-root "$S" && test "$(grep -rl 'JUCE-NE-PATCH' "$S/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp" "$S/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h" 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo T1_PASS</automated>
  </verify>
  <done>
    `/Users/taylorbrook/JUCE-8.0.15-staging/JUCE` exists, self-reports 8.0.15, is unpatched, and both
    `check-juce-overrides.sh --juce-root` and `gen-juce-overrides.sh --check --juce-root` exit 0
    against it. The downloaded zip's sha256 and byte size match the values recorded by 260921-i75.
    `/Users/taylorbrook/JUCE` is untouched and still 8.0.14. Verifier prints `T1_PASS`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch the staged tree, confirm it equals the vendored overrides, swap it in keeping 8.0.14 as a renamed backup, reconfigure CMake</name>
  <files>
    /Users/taylorbrook/JUCE               (replaced — the 8.0.15 patched tree)
    /Users/taylorbrook/JUCE-8.0.14-backup (created — the displaced 8.0.14 tree)
    build/CMakeCache.txt, build/build.ninja (regenerated by cmake reconfigure)
    (no repo source files modified)
  </files>
  <precondition>Task 1 completed with both gates green, and `/Users/taylorbrook/JUCE-8.0.14-backup` does not already exist.</precondition>
  <reversibility rating="costly">Replaces a toolchain directory that lives outside the repo and is not a git repo, so git cannot restore it. Made reversible on purpose by renaming rather than deleting: rollback is `mv /Users/taylorbrook/JUCE /Users/taylorbrook/JUCE-8.0.15-failed &amp;&amp; mv /Users/taylorbrook/JUCE-8.0.14-backup /Users/taylorbrook/JUCE`, then re-touch and reconfigure as below. That rollback is only available because this task never deletes.</reversibility>
  <action>
    Apply the patch to the staged tree, not the live one:
    `JUCE_DIR=/Users/taylorbrook/JUCE-8.0.15-staging/JUCE bash scripts/apply-juce-patches.sh`.
    The script honors `$JUCE_DIR` (line 23), derives the patch name from the pin (line 39), skips when
    the marker is already present (line 56), and normalizes CRLF to LF before applying (line 72).
    That marker skip is the guard against the known reverse-apply hazard: `patch` here runs without
    `-N`, so a second application on an already-patched tree would silently undo the fork. Never run
    this script twice against the same tree to "make sure" — read the marker instead.

    Prove the patched files equal the vendored overrides. For each of the two paths in
    `vendored/JUCE-overrides/modules/`, diff the CR-stripped bytes of the vendored file against the
    CR-stripped bytes of the same path in the staged tree; both diffs must be empty. The vendored
    copies are CRLF and the patched tree is LF, so a raw byte compare would fail on line endings
    alone — CR-stripping is the same normalization MANIFEST.txt documents. This is the task's
    literal "confirm the patched files match vendored/JUCE-overrides/" requirement, measured
    directly rather than inferred from Task 1's temp-dir regeneration. Also assert the
    `JUCE-NE-PATCH` marker is now present in both files.

    Swap, refusing to destroy anything. In order:
    (1) assert `/Users/taylorbrook/JUCE` exists and self-reports 8.0.14;
    (2) assert `/Users/taylorbrook/JUCE-8.0.14-backup` does NOT exist — if it does, HALT and report;
        never overwrite a pre-existing backup;
    (3) `mv /Users/taylorbrook/JUCE /Users/taylorbrook/JUCE-8.0.14-backup`;
    (4) assert the backup is present and still self-reports 8.0.14 before going further;
    (5) `mv /Users/taylorbrook/JUCE-8.0.15-staging/JUCE /Users/taylorbrook/JUCE`;
    (6) remove the now-empty staging directory and the downloaded zip.
    Staging deliberately sits on the same volume as the destination so step 5 is a rename, not a
    153 MB cross-device copy. There is no `rm -rf` of a JUCE tree anywhere in this task
    (T-in9-02 mitigation).

    Refresh mtimes across the new tree: `find /Users/taylorbrook/JUCE -type f -exec touch {} +`.
    This is load-bearing, not hygiene. A zip unpack carries the ARCHIVE's timestamps, which can
    predate the objects already in `build/`; ninja compares mtimes, would judge every JUCE source
    up to date, and would relink O-Lyrica from 8.0.14 objects while every human-visible signal said
    8.0.15. Task 3's version-string assertion is the independent check that this worked.

    Reconfigure CMake IN PLACE: `cmake -S . -B build`. Do NOT delete `build/` and do NOT pass
    `--reconfigure` to build-and-install.sh, which deletes it: another Claude session may share this
    checkout and be mid-build, and wiping `build/` destroys its state. The two stale version-derived
    cache strings (`JUCE_INSTALL_DESTINATION`, `JUCE_TOOL_INSTALL_DIR`) feed JUCE's own `install()`
    targets, which this repo never invokes; they are cosmetic and are not worth a full wipe. If and
    only if the in-place configure FAILS, report the failure and ask before wiping `build/` —
    do not wipe it unilaterally.

    Confirm the reconfigure kept pointing at the same path: `JUCE_SOURCE_DIR` in
    `build/CMakeCache.txt` must still read `/Users/taylorbrook/JUCE`.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development && J=/Users/taylorbrook/JUCE && V=vendored/JUCE-overrides/modules && grep -q "version: *8\.0\.15" "$J/modules/juce_core/juce_core.h" && test -d /Users/taylorbrook/JUCE-8.0.14-backup && grep -q "version: *8\.0\.14" /Users/taylorbrook/JUCE-8.0.14-backup/modules/juce_core/juce_core.h && test ! -d /Users/taylorbrook/JUCE-8.0.15-staging && for f in juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h; do diff <(tr -d '\r' < "$V/$f") <(tr -d '\r' < "$J/modules/$f") >/dev/null || exit 1; grep -q 'JUCE-NE-PATCH' "$J/modules/$f" || exit 1; done && grep -q '^JUCE_SOURCE_DIR:STATIC=/Users/taylorbrook/JUCE$' build/CMakeCache.txt && echo T2_PASS</automated>
  </verify>
  <done>
    `/Users/taylorbrook/JUCE` is a patched 8.0.15 tree whose two fork files are CR-normalized
    byte-identical to `vendored/JUCE-overrides/` and carry the marker.
    `/Users/taylorbrook/JUCE-8.0.14-backup` holds the displaced 8.0.14 tree intact. The staging
    directory and downloaded zip are gone. `cmake -S . -B build` exited 0 and `JUCE_SOURCE_DIR` is
    unchanged. Verifier prints `T2_PASS`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rebuild, install and auval O-Lyrica against the 8.0.15 tree — prove the note-expression fork survived</name>
  <files>
    build/plugins/O-Lyrica/OLyrica_artefacts/Release/  (rebuilt)
    ~/Library/Audio/Plug-Ins/VST3/O-Lyrica-dev.vst3     (reinstalled)
    ~/Library/Audio/Plug-Ins/Components/O-Lyrica-dev.component (reinstalled)
    (no repo files modified)
  </files>
  <precondition>Task 2 completed: /Users/taylorbrook/JUCE is the patched 8.0.15 tree and `cmake -S . -B build` succeeded.</precondition>
  <action>
    Build and install with `./scripts/build-and-install.sh O-Lyrica`. Pass the FOLDER name — the
    script resolves the `juce_add_plugin()` target `OLyrica` itself (lines 129-162), and its Phase 4
    already performs the dual-variant sweep (`-dev` and unsuffixed) that CLAUDE.md requires, with
    Phase 6 clearing the AU caches. Do not hand-roll the install sequence and do not pass
    `--reconfigure`.

    RUN THIS IN THE BACKGROUND AND POLL. A JUCE point-release bump invalidates every JUCE-derived
    object, so this is a near-full rebuild and will exceed the 600 s executor watchdog if run in the
    foreground.

    Note what the build itself proves. `modules/tuning/note-expression/cpp/NoteExpression.h:186`
    declares `void onVst3RawEvent (const Vst3RawEvent& e) override`, and that module is compiled into
    O-Lyrica via `ouaricon_add_module(OLyrica note-expression)`. If the swapped tree had lost the
    fork, `Vst3RawEvent` would be an unknown type and the `override` would have no base virtual — a
    hard compile error, not a silent degradation. A successful build IS the note-expression proof;
    say so explicitly in the SUMMARY rather than leaving it implicit.

    Confirm the binary is actually built against the new framework. Run `strings` over the installed
    VST3 executable inside `~/Library/Audio/Plug-Ins/VST3/O-Lyrica-dev.vst3/Contents/MacOS/` and
    assert it contains the 8.0.15 JUCE version banner at least once and the prior point release's
    banner zero times. Baseline measured 2026-09-21: the pre-swap binary contained exactly one
    occurrence of the prior banner, so this is a genuine discriminator and it is what catches the
    ninja-mtime trap described in Task 2.
    <!-- planner-discipline-allow: JUCE v8.0.14 -->

    Validate the AU. Read `PLUGIN_MANUFACTURER_CODE` and `PLUGIN_CODE` out of
    `plugins/O-Lyrica/CMakeLists.txt` and confirm from the same file whether `IS_SYNTH` is TRUE
    (component type `aumu`) or not (`aufx`); construct `auval -v <type> <subtype> <manufacturer>`
    from what you read — do not guess the codes. Run it in the BACKGROUND too: a cold auval after a
    fresh install rescans the whole registry, which is slow on first run and is not a hang. Also run
    `auval -a | grep -i lyrica` to confirm registration. Record the literal final verdict line.

    Record the actual command output — build result line, the `strings` counts, and the auval
    verdict — verbatim in the SUMMARY. Do not paraphrase a pass.

    If the build fails on a note-expression symptom, read `Skill("spike-findings-VST-development")`
    before diagnosing. If it fails for a reason you cannot resolve inside this task, do NOT leave a
    half-swapped toolchain: perform the Task 2 rollback, re-touch and reconfigure, and report.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development && B=$(ls -d ~/Library/Audio/Plug-Ins/VST3/O-Lyrica-dev.vst3 2>/dev/null | head -1) && test -n "$B" && test "$B/Contents/MacOS" -nt /Users/taylorbrook/JUCE/modules/juce_core/juce_core.h && S=$(strings "$B/Contents/MacOS/"* 2>/dev/null) && test "$(printf '%s' "$S" | grep -c 'JUCE v8\.0\.15')" -ge 1 && test "$(printf '%s' "$S" | grep -c 'JUCE v8\.0\.1[0-4]')" -eq 0 && test -d ~/Library/Audio/Plug-Ins/Components/O-Lyrica-dev.component && auval -a 2>/dev/null | grep -qi lyrica && echo T3_PASS</automated>
    <human-check>The recorded `auval -v` output for O-Lyrica ends in a PASS verdict, and the recorded build log shows JUCE sources actually recompiling rather than "no work to do".</human-check>
  </verify>
  <done>
    O-Lyrica built clean against the patched 8.0.15 tree (which compile-proves `onVst3RawEvent`
    still exists), installed as both VST3 and AU with the dual-variant sweep, the installed binary
    reports the 8.0.15 JUCE banner and zero occurrences of any earlier 8.0.1x banner, and `auval -v`
    returns a PASS recorded verbatim. Verifier prints `T3_PASS`.
  </done>
</task>

</tasks>

<traps>
Known failure modes for this specific task. Each is a real mechanism verified at planning time.

1. **Do NOT run `check-juce-overrides.sh --juce-root /Users/taylorbrook/JUCE` after Task 2.** That
   tree is patched, so CHECK 3 (upstream provenance) MUST fail — by design. A failure there after
   the swap is the gate working, not a regression. The valid post-swap runs are the network-mode
   `bash scripts/check-juce-overrides.sh` (no `--juce-root`) and `bash scripts/gen-juce-overrides.sh --check`,
   both of which should stay exit 0 because neither depends on the local tree.
2. **Never run `apply-juce-patches.sh` twice against the same tree.** It calls `patch -p1` without
   `-N`; the marker check at line 56 is the only thing standing between a second run and a silent
   reverse-apply that would remove the fork while reporting success.
3. **A zip unpack can leave ninja blind.** Archive mtimes may predate `build/` objects. Task 2's
   `touch` sweep is mandatory; Task 3's version-string assertion is how you find out if it was
   skipped.
4. **Do not delete `build/`.** A concurrent session may share this checkout and be mid-build.
5. **`build-and-install.sh` takes the plugin FOLDER name**, and resolves the differing CMake target
   itself. Passing `OLyrica` is not what the script expects.
6. **Builds and cold auval runs exceed the 600 s watchdog.** Background them and poll.
</traps>

<non_goals>
Do NOT do any of the following. Each is a deliberate exclusion, not an oversight.

- **No repo source edits are expected from this task.** Every in-repo occurrence of `8.0.14`
  measured at planning time is historical provenance — `research/framework-updates-2026-07.md`,
  `research/repo-modernization-audit-2026-09-21.md`, `juce_version:` frontmatter in `research/`
  docs recording what each was verified against, and `plugins/O-Strata/.planning/` research notes.
  Quick task 260921-i75 explicitly decided those must survive a bump untouched. Leave them.
  If you find a doc asserting the LOCAL tree is *currently* 8.0.14 as a live fact, REPORT it in the
  SUMMARY — do not silently edit it.
- No git tags, no pushes, no plugin version bumps.
- No changes to `.github/juce-version.txt`, `vendored/JUCE-overrides/`, or
  `scripts/juce-patches/` — 260921-i75 already moved all three to 8.0.15 and both gates are green.
- No rebuild of plugins other than O-Lyrica. One Note Expression plugin is the proof this task asks
  for; a suite-wide rebuild is a separate decision.
- Do not touch the pre-existing uncommitted `.claude/agent-memory/research-planning-agent.md`.
- Do not stage anything under the `plugins/O-Orbit/libs/SAF` submodule.
</non_goals>

<threat_model>
ASVS level 1; block on `high`. No npm/pip/cargo package installs occur in this task, so no
Package Legitimacy Audit applies — the third-party surface here is a source-archive download and a
toolchain directory replacement.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| github.com -> local disk | A 52 MB third-party source archive crosses into the build toolchain and is then compiled into every shipped binary. |
| staging tree -> `/Users/taylorbrook/JUCE` | A directory outside the repo, not under git, is replaced. Git cannot restore it. |
| new JUCE sources -> existing `build/` | Fresh source mtimes decide whether ninja rebuilds or silently reuses prior-version objects. |
| this session -> shared checkout/index | Another Claude session may share `.git/index`, HEAD and `build/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-in9-01 | Tampering | `juce-8.0.15-osx.zip` fetched from github.com (T1) | high | mitigate | Exact pinned tag URL over `--proto '=https' --tlsv1.2 --fail`; sha256 + byte size compared against values recorded in an independent prior session (260921-i75), mismatch = hard halt before unpacking; plus `check-juce-overrides.sh --juce-root` re-fingerprinting the two override-source files against MANIFEST values taken from a *different* distribution channel (raw.githubusercontent tag). Two-channel agreement, not a single checksum. |
| T-in9-02 | Denial of Service | `/Users/taylorbrook/JUCE` replacement (T2) | high | mitigate | Rename-never-delete: the 8.0.14 tree becomes `JUCE-8.0.14-backup`; the swap refuses to run if that path already exists; the backup is asserted present and version-correct before the new tree is moved in; no `rm -rf` of any JUCE tree appears in the plan; free-space preflight in T1; explicit rollback recorded in T2's `<reversibility>`. |
| T-in9-03 | Tampering | ninja reusing 8.0.14 objects under archive mtimes (T2/T3) | medium | mitigate | `find … -exec touch {} +` across the new tree, plus an independent post-build assertion on the JUCE version banner inside the installed binary — the build cannot report green on stale objects without failing that check. |
| T-in9-04 | Tampering | shared `.git/index` and `build/` with a concurrent session | medium | mitigate | Path-scoped commits only, `git branch --show-current` and `git status --short` re-checked immediately before each commit, never `git add -A`; `build/` is reconfigured in place and never deleted without asking. |
| T-in9-05 | Elevation of Privilege | `note-expression-juce-8.0.15.patch` applied to the toolchain | low | accept | The patch is version-controlled in this repo, was re-derived and reviewed in 260921-i75, and `gen-juce-overrides.sh --check` independently re-derives the vendored bytes from it in T1 before it is ever applied for real. |
</threat_model>

<verification>
Run after all three tasks, from the repo root:

1. `grep 'version:' /Users/taylorbrook/JUCE/modules/juce_core/juce_core.h | head -1` -> `8.0.15`
2. `grep 'version:' /Users/taylorbrook/JUCE-8.0.14-backup/modules/juce_core/juce_core.h | head -1` -> `8.0.14`
3. `bash scripts/check-juce-overrides.sh` -> exit 0 (network mode; NOT `--juce-root` against the
   live patched tree — see `<traps>` item 1)
4. `bash scripts/gen-juce-overrides.sh --check` -> exit 0
5. Both CR-normalized diffs between `vendored/JUCE-overrides/modules/*` and the same paths under
   `/Users/taylorbrook/JUCE/modules/` -> empty
6. `auval -v <type> <subtype> <manufacturer>` for O-Lyrica -> PASS, output recorded verbatim
7. `git status --short` -> shows only `.planning/quick/260921-in9-*/` additions plus the
   pre-existing untouched `.claude/agent-memory/research-planning-agent.md`

Record the literal output of 3, 4, 6 and 7 in the SUMMARY. A paraphrased pass is not evidence.
</verification>

<success_criteria>
- `/Users/taylorbrook/JUCE` is JUCE 8.0.15, patched, and CR-normalized byte-identical to
  `vendored/JUCE-overrides/` on both fork files.
- `/Users/taylorbrook/JUCE-8.0.14-backup` exists and is the intact prior tree.
- O-Lyrica builds (compile-proving `onVst3RawEvent`), installs as VST3 + AU, reports the 8.0.15
  JUCE banner in its binary with zero earlier-8.0.1x banners, and passes `auval`.
- `check-juce-overrides.sh` and `gen-juce-overrides.sh --check` are both still exit 0.
- Zero repo source files modified; the commit contains only `.planning/quick/260921-in9-*/`
  artifacts, path-scoped.
</success_criteria>

<output>
Create `.planning/quick/260921-in9-upgrade-the-local-juce-checkout-at-users/260921-in9-SUMMARY.md` when done.

Record in it: the downloaded zip's measured sha256 and byte size (and whether they matched the
260921-i75 values), the verbatim output of both provenance gates from Task 1, the two diff results
from Task 2, the build result line, the `strings` banner counts, and the verbatim `auval` verdict.
Note explicitly that a green O-Lyrica build is itself the note-expression proof, and state whether
any repo file needed editing (expected: none).
</output>
