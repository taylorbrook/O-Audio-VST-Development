---
phase: 260921-fy9-audit-repo-for-juce-version-workflow-and
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [FY9-R1, FY9-R2, FY9-R3, FY9-R4, FY9-R5, FY9-R6, FY9-R7, FY9-R8]
files_modified:
  - research/repo-modernization-audit-2026-09-21.md
  - .planning/quick/260921-fy9-audit-repo-for-juce-version-workflow-and/check-report.sh
  - .planning/quick/260921-fy9-audit-repo-for-juce-version-workflow-and/preflight-status.txt

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "FY9-R1: The report states the JUCE version actually pinned (from `.github/juce-version.txt` and the root `CMakeLists.txt` JUCE resolution block), the latest upstream JUCE 8.x release as retrieved today with its retrieval source, what the vendored Note Expression patch touches, and whether upstream has absorbed it."
    - "FY9-R2: Build-efficiency findings name the measured or configured fact (ccache/sccache present or absent, unity/PCH present or absent, target count the root glob produces, CI cache steps, CI wall-clock from real runs) — never a generic 'builds could be faster'."
    - "FY9-R3: Claude-tooling findings are grounded in counted or existence-tested facts about `.claude/` — file counts, byte sizes, referenced paths that do or do not exist, MCP tool names that do or do not match the registered ones, and hook event names verified against the official hooks reference."
    - "FY9-R4: Testing findings state the pluginval strictness level and pinned pluginval version, which workflow runs what on which OS, and whether any unit or render-harness test executes in CI — each with a `file:line`."
    - "FY9-R5: Repo-hygiene findings cite measured sizes (`.git` size, largest tracked blobs), `.gitignore` gaps tested against actual tracked paths, submodule status, and branch/worktree/tag state."
    - "FY9-R6: Instrument-practice findings compare `modules/registry.yaml` against a per-topic census across `plugins/`, so each row says how many plugins reuse a shared module versus reimplement locally."
    - "FY9-R7: The report opens with an Executive Summary of exactly 5 ranked actions, each carrying Effort S/M/L and Risk Low/Med/High; every dimension section carries a findings table; the report closes with an Apply Later checklist of copy-pasteable follow-up commands, each referencing the section it came from."
    - "FY9-R8: Nothing outside `research/repo-modernization-audit-2026-09-21.md` and this quick-task directory is created or modified — the protected-path git status is byte-identical to the preflight baseline at the end of every task."
  artifacts:
    - "research/repo-modernization-audit-2026-09-21.md"
    - ".planning/quick/260921-fy9-audit-repo-for-juce-version-workflow-and/check-report.sh"
    - ".planning/quick/260921-fy9-audit-repo-for-juce-version-workflow-and/preflight-status.txt"
  key_links:
    - "report frontmatter -> .claude/hooks/validators/validate-research-frontmatter.py (required fields title/created/domain/type/keywords; domain must be `tooling`, type must be `research`)"
    - "check-report.sh -> research/repo-modernization-audit-2026-09-21.md (the gate reads the report's own tables; a stub section cannot pass)"
    - "check-report.sh readonly -> preflight-status.txt (the read-only guarantee is a diff against the baseline, not an assumption)"
    - "JUCE pin -> .github/juce-version.txt + CMakeLists.txt JUCE resolution block + scripts/juce-patches/ (three places that must agree before any upgrade is recommended)"
---

<objective>
Produce an evidence-backed modernization audit of this repository — JUCE version and
toolchain, build efficiency, the `.claude/` Plugin Freedom System tooling, quality gates,
repo hygiene, and instrument-specific shared-module coverage — as a single prioritized
report the developer can pick from later.

Purpose: The developer asked what updates this repo needs "to make for the best possible
results when building VST instruments using Claude." They are regression-averse, so this
run produces a *decision document*, not changes.

Output: `research/repo-modernization-audit-2026-09-21.md` plus the gate script that proves
the report is substantive rather than a skeleton.

**THIS TASK CHANGES NOTHING BUT THE REPORT.** It is read-only against `plugins/`,
`modules/`, `.github/`, `scripts/`, `.claude/`, `CMakeLists.txt`, `CLAUDE.md`, `.gitignore`
and the JUCE tree. Do not bump a version, edit a workflow, prune a skill, fix a `.gitignore`,
delete a stale file, or run a build or install. Catalogue everything; apply nothing.

Requirements this plan satisfies: FY9-R1 through FY9-R8.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.github/juce-version.txt
@CMakeLists.txt
@.claude/settings.json
</context>

<report_contract>
Applies to every task. The report lives at `research/repo-modernization-audit-2026-09-21.md`.

**Frontmatter** (enforced by the `PostToolUse` hook
`.claude/hooks/validators/validate-research-frontmatter.py`, which fires on every Write/Edit
under `research/`): `title`, `created: 2026-09-21`, `domain: tooling`, `type: research`,
`keywords` (a YAML list). `domain` and `type` are closed enums — `tooling` and `research`
are the correct members. A malformed value fails the hook, so validate after the first write
before going further.

**Headings**, in this exact order and exact spelling — the gate script greps them literally:
`## Executive Summary`, `## 1. JUCE Version & Toolchain`, `## 2. Build Efficiency`,
`## 3. Claude Workflow & Tooling`, `## 4. Testing & Quality Gates`, `## 5. Repo Hygiene`,
`## 6. Instrument-Specific Best Practices`, `## Apply Later`.

**Findings table** in each numbered section, columns exactly
`| Finding | Evidence | Recommendation | Effort | Risk |`. Effort is exactly one of `S`,
`M`, `L`. Risk is exactly one of `Low`, `Med`, `High`. The Evidence cell must carry a
verifiable fact — a `path:line`, a version string, a count, or a retrieved URL plus today's
date for anything fetched from the network. "Looks outdated" is not evidence.

**Executive Summary table**, columns exactly `| # | Action | Why now | Effort | Risk |`, with
exactly 5 rows ranked 1–5. Same Effort/Risk vocabularies, so one regex gates both tables.

**Apply Later**: a checklist where each line starts `- [ ] ` and carries a copy-pasteable
command plus the section it came from, e.g. a `/gsd-quick` invocation followed by `(§1)`.

**Evidence discipline.** Every upstream claim (latest JUCE release, absorbed patches,
deprecations, Claude Code features) cites the URL it came from and the date retrieved. Do
not assert from recollection — the known baseline in the task framing is a hypothesis to
confirm or refute, not a fact to restate.
</report_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Report skeleton, read-only gate, and the JUCE/toolchain dimension end-to-end</name>
  <precondition>`gh auth status` succeeds — Tasks 1 and 2 read the JUCE releases API and this repo's CI run history through `gh api`. If it fails, halt and report rather than substituting recollection for upstream facts.</precondition>
  <files>
    .planning/quick/260921-fy9-audit-repo-for-juce-version-workflow-and/preflight-status.txt,
    .planning/quick/260921-fy9-audit-repo-for-juce-version-workflow-and/check-report.sh,
    research/repo-modernization-audit-2026-09-21.md
  </files>
  <action>
Do these four things in order, because each one proves a layer the later tasks depend on.

**(a) Capture the read-only baseline.** Write `preflight-status.txt` containing the output of
`git status --porcelain -- plugins modules .github scripts CMakeLists.txt CLAUDE.md .gitignore .claude/skills .claude/commands .claude/hooks .claude/agents .claude/settings.json`
taken *before* any other work. `.claude/agent-memory/` is deliberately outside this protected
set — the `write-back-agent-memory.py` SubagentStop hook writes there on its own and one of
its files is already dirty at session start, so including it would make the gate fail for
reasons unrelated to this audit.

**(b) Write `check-report.sh`**, a bash gate over the report, taking one or more mode
arguments and exiting non-zero on the first failure with a message naming what failed.
Define one shared row regex, `^\| [^|]+ \| [^|]+ \| [^|]+ \| [SML] \| (Low|Med|High) \|`,
used via `grep -Ec` — it matches findings rows and Executive Summary rows alike and cannot
match a markdown heading, a separator row, or the header row. Modes:
`skeleton` runs `python3 .claude/hooks/validators/validate-research-frontmatter.py` on the
report and asserts all eight contract headings are present via `grep -qF`;
a numeric mode `1`–`6` slices that section with `sed -n` from its own `^## N.` heading to the
next `^## ` heading and asserts at least 4 rows match the row regex, and that at least 3 of
those rows' Evidence cells (field 3 under `awk -F'|'`) match
`[A-Za-z0-9_./-]+\.(yml|txt|sh|cmake|json|md|cpp|h|patch)|[0-9]+\.[0-9]+\.[0-9]+|https?://`;
`summary` asserts the Executive Summary slice has exactly 5 matching rows;
`apply` asserts the Apply Later slice has at least 6 lines matching `^- \[ \] ` and that each
of those lines contains a `(§` section reference;
`readonly` re-runs the protected-path `git status --porcelain` from (a) and asserts the output
is byte-identical to `preflight-status.txt` via `diff -q` against a process substitution.
Make the script executable.

**(c) Write the report skeleton** at `research/repo-modernization-audit-2026-09-21.md` with
the frontmatter and all eight headings from `<report_contract>`, each numbered section
carrying its table header and separator row and nothing else yet, then immediately run
`check-report.sh skeleton` — this is the cheapest possible proof that the frontmatter
validator hook accepts the file, before six dimensions of evidence have been invested in it.

**(d) Fill `## 1. JUCE Version & Toolchain` completely.** Gather, and cite, at minimum:
the pinned version in `.github/juce-version.txt` and the download URLs in
`.github/workflows/build-and-release.yml` and `.github/workflows/ci-tests.yml` that consume
it; the JUCE resolution block in the root `CMakeLists.txt` (env `JUCE_DIR`, then `C:/JUCE`,
then a hardcoded absolute macOS path — note the portability consequence of a hardcoded home
directory in a public repo); the version of the local tree at that path, read from its own
`project(JUCE VERSION ...)` declaration; the latest JUCE releases via
`gh api repos/juce-framework/JUCE/releases --jq '.[0:8][] | "\(.tag_name) \(.published_at)"'`,
recording tag and date; what changed between the pinned version and latest, via WebFetch of
the upstream `CHANGELOG.md` and `BREAKING-CHANGES.md` on the JUCE default branch; the two
vendored patches in `scripts/juce-patches/` (`note-expression-juce-8.0.14.patch` and the much
larger `note-expression-juce-8.0.9.patch`) — list which JUCE source files each touches by
reading the `+++` lines, note the size delta between them as a signal of how much upstream
already absorbed, and check whether upstream has since absorbed the 8.0.14 patch by searching
the changed symbols in the upstream tree; how `scripts/apply-juce-patches.sh` is wired into
CI and whether it would still apply against a newer JUCE; the bundled VST3 SDK version,
found by grepping for an SDK version or `kVstVersionString`-style constant under the pinned
JUCE's `modules/juce_audio_processors/format_types/VST3_SDK/`; which plugin formats are built
(`grep -n FORMATS plugins/*/CMakeLists.txt`) and whether CLAP is absent (grep for `clap`
across the CMake files) with `clap-juce-extensions` named as the option if so; the
deprecation exposure, derived by extracting symbol names from the upstream
`BREAKING-CHANGES.md` entries for 8.0.x and grepping this repo for each one rather than
guessing a list; and the toolchain version spread — local `cmake --version`, `ninja --version`,
`xcodebuild -version`, `sw_vers` against the `runs-on:` images and any pinned tool versions in
both workflow files, plus the `CMAKE_OSX_DEPLOYMENT_TARGET` set at `CMakeLists.txt` line 5 and
whether a 10.13 floor is still buying anything.
Close the section with an explicit risk/benefit line for the upgrade: what a bump costs
(re-deriving the Note Expression patch, re-validating 40 plugins) versus what it buys.

Commit path-scoped per `CLAUDE.md`. Re-check `git branch --show-current` and
`git status --short` immediately before committing — another session shares this index — and
name the paths explicitly. Never `git add -A`.
  </action>
  <verify>
    <automated>
cd /Users/taylorbrook/Dev/VST-development && \
bash .planning/quick/260921-fy9-audit-repo-for-juce-version-workflow-and/check-report.sh skeleton 1 readonly
    </automated>
  </verify>
  <done>The report exists at the contract path, passes the research-frontmatter hook validator, carries all eight headings, and `## 1. JUCE Version & Toolchain` holds at least 4 findings rows whose Evidence cells cite real paths, version strings, or retrieved URLs. The protected-path git status matches the preflight baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Build efficiency, testing gates, repo hygiene, and instrument-practice dimensions</name>
  <files>research/repo-modernization-audit-2026-09-21.md</files>
  <action>
Fill sections 2, 4, 5 and 6. These four share one evidence set — the CMake files, the two
workflows, `scripts/`, and git itself — so gather once and write once. Run any command that
could exceed the executor's 600-second watchdog in the background rather than in the
foreground; the largest-blob scan below is the likely candidate on a repo this size.

**`## 2. Build Efficiency`.** Establish: whether a compiler cache is configured anywhere
(`grep -rn "ccache\|sccache\|COMPILER_LAUNCHER"` across `CMakeLists.txt`,
`plugins/*/CMakeLists.txt`, `modules/cmake/`, both workflow files, and `scripts/*.sh`/`*.ps1`);
whether unity builds or precompiled headers are used (`CMAKE_UNITY_BUILD`, `UNITY_BUILD`,
`target_precompile_headers`); the cost of the root `file(GLOB plugins/*)` auto-discovery at
`CMakeLists.txt` lines 47–58 — every configure adds all ~40 plugin subdirectories even when
one plugin is wanted, so measure it with `ninja -C build -t targets all | wc -l` and
`wc -l build/build.ninja` and `du -sh build build-asan build-release`, and check whether
`SKIP_PLUGINS` or `scripts/resolve-target.sh` already gives a narrower path; what
`scripts/build-and-install.sh` actually re-does on each invocation (read it, note whether it
re-configures unconditionally and what its Phase 4 dual-variant sweep costs); CI caching
(`grep -n "actions/cache"` in both workflows — note specifically whether the downloaded JUCE
zip and the build tree are cached across runs, since each run currently curls a fresh JUCE);
real CI wall-clock from
`gh run list --workflow=ci-tests.yml --limit 15 --json displayTitle,conclusion,createdAt,updatedAt`
and the same for `build-and-release.yml`, converted to durations; and whether LTO
(`INTERPROCEDURAL_OPTIMIZATION`) or universal-binary `CMAKE_OSX_ARCHITECTURES` settings exist
and what they cost. Use `ninja -n` dry runs for work-volume evidence. **Do not run a real
build** — an actual compile is out of scope and would dirty `build/`.

**`## 4. Testing & Quality Gates`.** Establish: what `ci-tests.yml` triggers on and what it
actually runs on each OS (the macOS leg around lines 119–214, the Windows leg around lines
244–310); the pinned pluginval version and strictness level (`ci-tests.yml:292-303` shows
v1.0.3 at strictness 10 on Windows — confirm and state whether macOS gets the same treatment
or something weaker); whether `auval` runs anywhere in CI (`grep -n auval .github/workflows/`);
whether any unit or render-harness test executes in CI — check for `enable_testing`, `add_test`,
`catch2`, `gtest` across the CMake files, then check what `scripts/verify-suite-battery.sh`
and any `plugins/*/tests/` directories contain and whether a workflow ever invokes them; and
whether the sanitizer build implied by the root `build-asan/` directory is wired into CI or
is purely local (`grep -rn "sanitize\|asan" .github/workflows/ scripts/`). Close the section
by naming the single cheapest high-value gate to add, with the concrete reason it is cheapest
given what already exists — not a wish list.

**`## 5. Repo Hygiene`.** Establish: `.git` size via `git count-objects -vH`; the largest
tracked blobs via
`git rev-list --objects --all | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$1=="blob"' | sort -k3 -rn | head -25`
run in the background; whether the seven loose PNG screenshots sitting at the repo root are
tracked (`git ls-files -- '*.png'`), and likewise `.DS_Store` (`git ls-files | grep DS_Store`),
`.claude/resource-index.json`, and `.claude/frontmatter-issues.txt`; `.gitignore` coverage
tested against reality rather than read — for each of `build/`, `build-asan/`, `build-release/`,
`backups/`, `logs/`, `scratchpad/`, `.playwright-mcp/`, `.cache/` use `git check-ignore -v`,
remembering that `check-ignore` is index-aware so a path already tracked will report as not
ignored and that distinction is itself a finding; branch, tag and worktree state
(`git branch -a`, `git worktree list`, `git tag | wc -l`) checked against the trunk-based
policy in `CLAUDE.md`; submodule health for `plugins/O-Orbit/libs/SAF` — `git submodule status`
for the pinned SHA, then `gh api repos/leomccormack/Spatial_Audio_Framework/commits/master --jq .sha`
and the commit date, to state how far behind the pin is; and any other third-party pin
(grep the CMake files for `FetchContent`, `anira`, `ANIRA`, `ExternalProject`) with its pinned
version against upstream.

**`## 6. Instrument-Specific Best Practices`.** Start from `modules/registry.yaml` — list what
shared modules exist and which plugins each claims. Then build a census: identify the
instruments with `grep -l "IS_SYNTH TRUE" plugins/*/CMakeLists.txt`, and for each of these
topics count how many of those instruments carry a local implementation versus consume a
shared module — MIDI/MPE handling (`MPEInstrument`, `MPESynthesiser`, `setMPEZoneLayout`),
Note Expression (`NoteExpression`), voice management and stealing (`SynthesiserVoice`,
`numVoices`, `renderNextBlock`), the preset system (the `preset-manager` module versus
hand-rolled preset code), sample-rate and block-size invariance (what `prepareToPlay` bodies
guard), and offline-render determinism (`isNonRealtime`, `juce::Random` seeding). Each row
must read as "N of M instruments reimplement X; shared module Y exists / does not exist",
which is what makes the recommendation actionable.

Commit path-scoped, re-checking branch and staging immediately beforehand.
  </action>
  <verify>
    <automated>
cd /Users/taylorbrook/Dev/VST-development && \
bash .planning/quick/260921-fy9-audit-repo-for-juce-version-workflow-and/check-report.sh 2 4 5 6 readonly
    </automated>
  </verify>
  <done>Sections 2, 4, 5 and 6 each hold at least 4 findings rows with cited evidence; §5 states a measured `.git` size and a tested `.gitignore` gap list; §6 states shared-module reuse counts against the instrument census. The protected-path git status still matches the preflight baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Claude workflow tooling dimension, Executive Summary, and Apply Later checklist</name>
  <files>research/repo-modernization-audit-2026-09-21.md</files>
  <action>
**`## 3. Claude Workflow & Tooling`** — the dimension with the most leverage, because it is
what actually drives plugin development here. Establish, with counts and existence tests:

*Scale and context cost.* `du -sh .claude` and per-subtree sizes; `find .claude/skills -name
SKILL.md | wc -l` against the 236 files under `.claude/skills`; the 50 files under
`.claude/commands`, 32 under `.claude/hooks`, 12 agents; the largest markdown files via
`find .claude -name '*.md' | xargs wc -c | sort -rn | head -20`; and `wc -c CLAUDE.md`. State
which of these load into every session versus on demand, and quantify the per-session tax.

*Dead and stale references.* Harvest every `.claude/...` path mentioned inside
`.claude/commands/`, `.claude/agents/` and `.claude/skills/` with
`grep -rho '\.claude/[A-Za-z0-9._/-]*'`, deduplicate, and `test -e` each — report the ones
that do not resolve. Do the same for the path `CLAUDE.md` itself cites,
`.claude/references/handoff-protocol.md`, against the three files actually present in
`.claude/references/`. Also test the skill and agent *names* invoked in commands against the
directories that exist.

*MCP tool-name drift.* Six agent files reference `mcp__context7` —
`.claude/agents/{troubleshoot,dorico,research-planning,foundation-shell,gui,dsp}-agent.md`.
Grep the exact tool names each declares and compare against the tools this server actually
registers, which are `mcp__context7__resolve-library-id` and `mcp__context7__query-docs`. Any
other spelling is a tool that silently does not exist for that agent; report each file:line.

*Hook events that may never fire.* `.claude/settings.json` registers handlers under the event
names `SessionStart`, `PostToolUse`, `PreCompact`, `SubagentStop`, and also `SubagentStart`
and `TaskCompleted`. Verify each name against the official hooks reference by WebFetching the
Claude Code hooks documentation, and report which registered events are real. Any name the
docs do not list means its handler never executes — which would make
`.claude/hooks/inject-agent-memory.py` and `.claude/hooks/task-validator-dispatch.py` dead
code despite being wired. Confirm from the docs; do not conclude from the filename.

*Per-session and per-subagent latency.* List each hook with its configured timeout from
`settings.json` and what the script does (read each; do not execute hooks that write state —
`SessionStart.py`, `regenerate-manifest.py` and `write-back-agent-memory.py` have side
effects). Flag the 30-second `SubagentStop.py` and the 15-second `task-validator-dispatch.py`
budgets specifically: with heavy subagent use those are a recurring tax, so state what each
buys.

*The user memory file.* `/Users/taylorbrook/.claude/projects/-Users-taylorbrook-Dev-VST-development/memory/MEMORY.md`
— report `wc -c` and the entry count, then extract every `](*.md)` link target and `test -e`
it in that same memory directory, reporting how many resolve and how many dangle. Separately
identify entries whose content is already stated in `CLAUDE.md` (the trunk-based policy, the
cache-clearing sequence, the handoff protocol) and are therefore paying twice. Recommend a
concrete pruning or tiering rule, not "consider trimming".

*Newer Claude Code capability this repo is not using.* WebFetch the Claude Code release notes
or changelog and identify features landed since this tooling was last touched (the mtimes give
the window: `.claude/settings.json` 2026-07-20, `.claude/skills/` 2026-06-23,
`.claude/commands/` 2026-08-21). Only list features that map onto a real friction found above.
The worktree question is already settled — `CLAUDE.md` mandates trunk-based on `main` and
`.planning/config.json` sets `use_worktrees: false` — so do not reopen it.

**`## Executive Summary`.** Read back the findings tables now in sections 1–6 and pick exactly
5 actions, ranked by (impact on plugin-building quality) ÷ (effort × risk). Each row carries
`| # | Action | Why now | Effort | Risk |` with the same Effort and Risk vocabularies. The
"Why now" cell names the finding it comes from. Rank 1 must be the thing the developer should
do first; if that is "nothing urgent — the toolchain is current", say so plainly and rank the
cheap wins beneath it. Do not inflate a low-value item to fill five slots; if fewer than five
matter, state that in the row's Why-now cell.

**`## Apply Later`.** At least 6 checklist lines, each `- [ ] ` followed by a copy-pasteable
command and a `(§N)` reference back to the section that motivated it — e.g. a `/gsd-quick`
invocation naming the specific change. Order them so independent items come first and anything
that must follow a JUCE bump is grouped and labelled as such. These are the developer's menu:
each must be self-contained enough to paste into a fresh session.

Commit path-scoped, re-checking branch and staging immediately beforehand.
  </action>
  <verify>
    <automated>
cd /Users/taylorbrook/Dev/VST-development && \
bash .planning/quick/260921-fy9-audit-repo-for-juce-version-workflow-and/check-report.sh skeleton 1 2 3 4 5 6 summary apply readonly
    </automated>
  </verify>
  <done>All six dimension sections and the Executive Summary and Apply Later sections pass the gate: 5 ranked summary rows, at least 4 evidenced findings rows per dimension, at least 6 section-referenced checklist items. The protected-path git status still matches the preflight baseline, proving nothing outside the report and this quick directory changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream web/API → report | JUCE releases, changelogs and Claude Code docs are untrusted content quoted as fact into a document the developer will act on |
| local config → report | `.claude/settings.local.json`, workflow files and CI logs may contain secret names or values that would become public in a tracked report |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-fy9-01 | Tampering | upstream version/changelog claims in §1 and §3 | medium | mitigate | Every upstream claim cites the retrieved URL and today's date; `gh api` output is quoted rather than paraphrased from recollection |
| T-fy9-02 | Information disclosure | report text drawn from `.claude/settings.local.json`, workflow `secrets.*` refs, `gh run` logs | high | mitigate | Cite secret and env-var *names* only; never quote a value, token, or signing identity into `research/`, which is a tracked public-repo path |
| T-fy9-03 | Tampering | accidental edit of build, CI, hook or plugin config during a read-only audit | high | mitigate | `check-report.sh readonly` diffs the protected-path git status against `preflight-status.txt` and runs in every task's verify |
| T-fy9-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs occur — this plan adds no dependency; all tooling used (`git`, `gh`, `grep`, `cmake`, `ninja`, `python3`) is already present |
</threat_model>

<verification>
- `bash .planning/quick/260921-fy9-audit-repo-for-juce-version-workflow-and/check-report.sh skeleton 1 2 3 4 5 6 summary apply readonly` exits 0.
- `python3 .claude/hooks/validators/validate-research-frontmatter.py research/repo-modernization-audit-2026-09-21.md` exits 0.
- `git status --porcelain -- plugins modules .github scripts CMakeLists.txt CLAUDE.md .gitignore .claude/skills .claude/commands .claude/hooks .claude/agents .claude/settings.json` is byte-identical to `preflight-status.txt`.
- No build ran: `build/`, `build-asan/` and `build-release/` are untouched, and no plugin was installed to `~/Library/Audio/Plug-Ins/`.
</verification>

<success_criteria>
The developer can open `research/repo-modernization-audit-2026-09-21.md`, read five ranked
actions with effort and risk on the first screen, drill into any of six dimensions to see the
`path:line` or URL behind each claim, and paste a command from the Apply Later checklist to
start whichever one they choose — with nothing in the repo having changed to get there.
</success_criteria>

<output>
Create `.planning/quick/260921-fy9-audit-repo-for-juce-version-workflow-and/260921-fy9-SUMMARY.md` when done.
</output>
