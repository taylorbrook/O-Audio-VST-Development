---
phase: quick-260719-q0o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/O-Texture/CMakeLists.txt
autonomous: true
requirements: [QT-q0o-01]
must_haves:
  truths:
    - "O-Texture configures and builds against ANIRA tag v2.1.0 (ORT still 1.19.2)"
    - "The built plugin binary resolves libonnxruntime from the embedded Contents/Frameworks copy via @rpath"
    - "pluginval passes on the freshly built O-Texture VST3"
  artifacts:
    - "plugins/O-Texture/CMakeLists.txt with GIT_TAG v2.1.0 (ONNXRUNTIME_VERSION unchanged at 1.19.2)"
    - "build/plugins/O-Texture/OuariconTexture_artefacts/Release/VST3/O-Texture-dev.vst3 with Contents/Frameworks/libonnxruntime.1.19.2.dylib"
  key_links:
    - "INSTALL_RPATH @loader_path/../Frameworks -> Contents/Frameworks/libonnxruntime.1.19.2.dylib (unchanged post-build embed step)"
    - "ONNXRUNTIME_VERSION 1.19.2 string couples the lib path (:24), link line (:75), and the embedded dylib filename/symlink (:168-169) — must remain 1.19.2 because v2.1.0 provisions ORT 1.19.2"
---

<objective>
Bump the ANIRA FetchContent pin in O-Texture from GIT_TAG v2.0.3 to v2.1.0 — a zero-string-change drop-in, since v2.1.0 provisions the same ONNX Runtime 1.19.2 (per research/framework-updates-2026-07.md). Do NOT take v2.2.x (that couples ORT 1.26.0 and would require editing the hardcoded ONNXRUNTIME_VERSION and re-validating the dylib-embed/rpath step).

Purpose: Stay current on the ANIRA ORT-download provisioning logic with the smallest possible change surface, and prove the embedded-dylib + @rpath post-build step still resolves after the bump.
Output: One-line CMakeLists edit, a clean ANIRA re-fetch at v2.1.0, a rebuilt+installed O-Texture, and a three-part verification (build succeeds, otool/rpath resolves the embedded ORT dylib, pluginval passes).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@research/framework-updates-2026-07.md
@plugins/O-Texture/CMakeLists.txt

# Ground truth confirmed during planning:
# - CMake target is OuariconTexture (set(PROJECT_NAME OuariconTexture) at :34) — NOT the folder name.
#   Targets: OuariconTexture_VST3, OuariconTexture_AU. Artefacts: build/plugins/O-Texture/OuariconTexture_artefacts/.
#   scripts/build-and-install.sh resolves this via resolve_cmake_target() when passed the folder name "O-Texture".
# - PRODUCT_NAME is "O-Texture${OUARICON_DEV_SUFFIX}" -> local builds produce O-Texture-dev.{vst3,component}.
# - ANIRA FetchContent cache lives at build/_deps/anira-{src,build,subbuild}; currently checked out at v2.0.3.
# - Current embedded Frameworks payload: libonnxruntime.1.19.2.dylib + libonnxruntime.dylib symlink.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump ANIRA GIT_TAG to v2.1.0 and invalidate the ANIRA FetchContent cache</name>
  <files>plugins/O-Texture/CMakeLists.txt</files>
  <action>
In plugins/O-Texture/CMakeLists.txt, change the ANIRA FetchContent_Declare GIT_TAG from v2.0.3 to v2.1.0 (the line reads GIT_TAG v2.0.3 inside the FetchContent_Declare(anira ...) block near line 15). This is the ONLY source edit in this plan.

Do NOT touch set(ONNXRUNTIME_VERSION 1.19.2) at line 22 — v2.1.0 provisions ORT 1.19.2, so the lib path (:24), the link line (:75), and the embedded dylib filename + symlink (:168-169) all stay 1.19.2. A v2.2.x bump would move ORT to 1.26.0 and is explicitly out of scope per the framework-updates research.

A FetchContent GIT_TAG change does not reliably re-checkout an already-populated dependency, so invalidate the cache: remove build/_deps/anira-src, build/_deps/anira-build, and build/_deps/anira-subbuild so the next configure fetches v2.1.0 cleanly. ANIRA is consumed only by O-Texture, so removing these directories affects no other plugin.
  </action>
  <verify>
    <automated>grep -q 'GIT_TAG v2.1.0' plugins/O-Texture/CMakeLists.txt && grep -q 'set(ONNXRUNTIME_VERSION 1.19.2)' plugins/O-Texture/CMakeLists.txt && ! grep -q 'GIT_TAG v2.0.3' plugins/O-Texture/CMakeLists.txt && ! test -d build/_deps/anira-src</automated>
  </verify>
  <done>GIT_TAG reads v2.1.0, ONNXRUNTIME_VERSION still 1.19.2, no residual v2.0.3 pin, and the build/_deps/anira-* directories are removed so the next configure re-fetches at the new tag.</done>
</task>

<task type="auto">
  <name>Task 2: Reconfigure, re-fetch ANIRA v2.1.0, rebuild and install O-Texture</name>
  <files>build/ (generated), ~/Library/Audio/Plug-Ins/VST3/, ~/Library/Audio/Plug-Ins/Components/</files>
  <action>
Force a CMake reconfigure so the ANIRA subbuild is regenerated at v2.1.0 and re-fetched (build-and-install.sh skips its own configure when build/ already exists, so run configure explicitly first):

Run: cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release

This repopulates build/_deps/anira-src at tag v2.1.0. Then build + install O-Texture with the project's canonical script, which resolves the OuariconTexture target from the folder name, builds VST3 + AU, and performs the mandatory AU cache-clear + dual-variant (dev/unsuffixed) bundle sweep + fresh install per CLAUDE.md:

Run: ./scripts/build-and-install.sh O-Texture

Do not hand-roll ninja target names — the target is OuariconTexture_VST3 / OuariconTexture_AU, not O-Texture_*. If build-and-install.sh reports it skipped configure, that is fine — the explicit cmake -B build above already reconfigured and re-fetched; Ninja will also re-run CMake automatically because CMakeLists.txt changed.
  </action>
  <verify>
    <automated>git -C build/_deps/anira-src describe --tags | grep -q '^v2.1.0' && test -f build/plugins/O-Texture/OuariconTexture_artefacts/Release/VST3/O-Texture-dev.vst3/Contents/MacOS/O-Texture-dev && test -f build/plugins/O-Texture/OuariconTexture_artefacts/Release/AU/O-Texture-dev.component/Contents/MacOS/O-Texture-dev</automated>
  </verify>
  <done>ANIRA source is checked out at v2.1.0, and both the VST3 and AU bundles build successfully with their Mach-O binaries present.</done>
</task>

<task type="auto">
  <name>Task 3: Verify embedded ORT dylib @rpath resolution, AU link, and pluginval</name>
  <files>build/plugins/O-Texture/OuariconTexture_artefacts/Release/ (read-only inspection)</files>
  <action>
Prove the three required gates against the freshly built VST3 bundle at build/plugins/O-Texture/OuariconTexture_artefacts/Release/VST3/O-Texture-dev.vst3.

(a) rpath / embedded-dylib resolution — confirm the binary links ORT via @rpath, carries the @loader_path/../Frameworks rpath, and the embedded 1.19.2 dylib + symlink are present in the bundle. Use otool -L and otool -l on Contents/MacOS/O-Texture-dev, and test for Contents/Frameworks/libonnxruntime.1.19.2.dylib and the libonnxruntime.dylib symlink.

(b) AU link gate — run scripts/verify-au-link.sh O-Texture (the canonical auval-based AU-link check; catches any Steinberg/ORT symbol-resolution regression introduced by the re-fetch).

(c) pluginval — run the installed/built VST3 through pluginval at project strictness. Binary is at /Applications/pluginval.app/Contents/MacOS/pluginval. Use --strictness-level 8 --validate-in-process to match the post-JUCE-8.0.14 battery convention; add --skip-gui-tests if the WebView editor stalls the headless run.

If any gate fails, do not paper over it — report which gate failed and the otool/pluginval output; a resolution failure here means the ORT re-fetch or embed step regressed.
  </action>
  <verify>
    <automated>B=build/plugins/O-Texture/OuariconTexture_artefacts/Release/VST3/O-Texture-dev.vst3; otool -L "$B/Contents/MacOS/O-Texture-dev" | grep -q '@rpath/libonnxruntime.1.19.2.dylib' && otool -l "$B/Contents/MacOS/O-Texture-dev" | grep -A2 LC_RPATH | grep -q '@loader_path/../Frameworks' && test -f "$B/Contents/Frameworks/libonnxruntime.1.19.2.dylib" && test -L "$B/Contents/Frameworks/libonnxruntime.dylib" && /Applications/pluginval.app/Contents/MacOS/pluginval --strictness-level 8 --validate-in-process "$B" | tail -5 | grep -qi 'ALL TESTS PASSED\|Success'</automated>
  </verify>
  <done>otool shows the binary links @rpath/libonnxruntime.1.19.2.dylib with an @loader_path/../Frameworks rpath, the embedded 1.19.2 dylib + symlink exist in Contents/Frameworks, verify-au-link.sh O-Texture PASSes, and pluginval reports all tests passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| github.com/anira-project/anira @ v2.1.0 -> local build | FetchContent pulls a new git tag of an already-vetted dependency into the build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q0o-01 | Tampering | ANIRA GIT_TAG re-fetch (supply chain) | low | accept | Same upstream repo already in use; v2.1.0 is a pinned tag vetted in research/framework-updates-2026-07.md; no new package registry install, ORT stays 1.19.2. Re-validated by pluginval + verify-au-link (Task 3). |
| T-q0o-02 | Denial of Service | Embedded ORT dylib fails to resolve at load | low | mitigate | Task 3 otool/@rpath + Frameworks presence check proves the dylib resolves from the bundle before the plugin is trusted in a host. |
</threat_model>

<verification>
1. Source: GIT_TAG v2.1.0, ONNXRUNTIME_VERSION still 1.19.2, no v2.0.3 residue (Task 1).
2. Build: ANIRA src at v2.1.0, VST3 + AU bundles build (Task 2).
3. Resolution: otool confirms @rpath/libonnxruntime.1.19.2.dylib + @loader_path/../Frameworks rpath + embedded dylib/symlink present (Task 3a).
4. AU link: scripts/verify-au-link.sh O-Texture PASS (Task 3b).
5. pluginval: all tests pass on the built VST3 (Task 3c).
</verification>

<success_criteria>
- plugins/O-Texture/CMakeLists.txt pins ANIRA at v2.1.0 with ONNXRUNTIME_VERSION unchanged (1.19.2).
- O-Texture rebuilds and installs cleanly (VST3 + AU) against the re-fetched ANIRA v2.1.0.
- The built binary resolves libonnxruntime.1.19.2.dylib from the embedded Contents/Frameworks copy via @rpath.
- verify-au-link.sh passes and pluginval passes on the built VST3.
</success_criteria>

<output>
Create `.planning/quick/260719-q0o-bump-anira-git-tag-v2-0-3-to-v2-1-0-in-o/260719-q0o-SUMMARY.md` when done.
</output>
