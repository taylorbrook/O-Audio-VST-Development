---
phase: quick-260901-fr8
plan: 01
type: execute
wave: 1
depends_on: [quick-260901-c3s]
autonomous: true
requirements: [P8, Z1, Z2, Z3, Z4, Z5, Z6, Z7, F1, R1, BT, LIFECYCLE]
files_modified:
  - scripts/i18n-zh-glossary.js
  - scripts/i18n-zh-lint.js
  - scripts/i18n-zh-backtranslate.js
files_deleted: []

estimate:
  tokens: 200000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node scripts/i18n-zh-lint.js` walks all 43 plugins, reports `0 zh entries found, nothing to check` as an explicit VACUITY result (not a pass), and exits 0."
    - "`node scripts/i18n-zh-lint.js --self-test` proves each of the nine rules Z1 Z2 Z3 Z4 Z5 Z6 Z7 F1 R1 FIRES on a deliberate violation AND is SILENT on a clean control, and prints `SELF-TEST: 9/9`."
    - "`node scripts/i18n-zh-lint.js --codes` prints exactly the nine zh codes and contains no French code (no T1..T7, no C1, no G1)."
    - "The Z3 Traditional-only character set is DERIVED from OpenCC dictionary data (TSCharacters.txt minus STCharacters.txt keys), not hand-written, and the file records the source URL, the upstream sha256, the derivation command and the fetch date."
    - "`scripts/i18n-zh-glossary.js` requires cleanly under CommonJS and exports TERMS covering every shared English string (>1 site) computed from the live 43-plugin corpus — 551 by measurement on 2026-09-01."
    - "Every glossary term exposes a zh-Hans rendering, a DERIVED charCount, and a maxChars budget that is either a measured number or an explicit null; the tool prints how many are unbudgeted so Z6's coverage is disclosed, never silent."
    - "`node scripts/i18n-zh-backtranslate.js` reports zh coverage per plugin and exits 0; its --emit batch withholds the English, and --ingest REFUSES to report a triple whose back-translation provenance is unrecorded."
    - "All three tools exit 0 regardless of findings and each header states the report-to-gate lifecycle (exit 2 only once the pilot is at zero findings)."
    - "`git status --short` shows ZERO files under plugins/ changed and ZERO changes to scripts/i18n-fr-glossary.js or scripts/i18n-fr-lint.js."
  artifacts:
    - scripts/i18n-zh-glossary.js
    - scripts/i18n-zh-lint.js
    - scripts/i18n-zh-backtranslate.js
  key_links:
    - "i18n-zh-lint.js requires i18n-zh-glossary.js via CommonJS `require`, exactly as i18n-fr-lint.js requires i18n-fr-glossary.js (scripts/i18n-fr-lint.js:84). Stage-2 tooling must be able to consume fr and zh glossaries uniformly, so the zh TERMS map keeps fr's shape — English key (lower-cased, trimmed) to an array of accepted renderings, ROOT FIRST — and adds BUDGETS/SAME_AS_EN as SEPARATE exports rather than changing the TERMS value type."
    - "Both lints read a plugin's table by dynamic ESM import of <uiRoot>/js/i18n.js over the two UI roots ['Source/ui/public/js/i18n.js','Resources/ui/js/i18n.js'] (scripts/i18n-fr-lint.js:151-158). The zh lint reuses that walk verbatim; a hand-rolled parser would drift from the 10 Resources/ui plugins."
    - "Z6 depends on a MEASURED cell width. Stage 1 has no zh content and no rendered geometry, so the only measured budgets available today are the two O-Chorus cliffs recorded in plugins/O-Chorus/Source/ui/public/js/i18n.js:39-42 (62 px wrap, 50 px gate) at the 10 px caption size given in IMPLEMENTATION-PLAN Stage 2 item 6. Every other term ships maxChars: null and Z6 is inert on it — Stages 2-4 fill budgets in from the check-ui-labels zh arm as each plugin is measured. The tool must PRINT the unbudgeted count, because an unreported inert rule is the 'gate green on unchecked content' failure this rollout exists to avoid."
---

<!-- planner-discipline-allow: T1 T7 C1 G1 — these French codes are named in prose because the
     whole point of the stage is that they are NOT ported. The negative grep in Task 1 runs against
     the runtime output of `--codes` (a generated temp file), never against a plan or source file,
     so naming them here cannot self-invalidate the gate. -->

<objective>
Stage 1 of the zh-Hans rollout — prerequisite P8. Build the three zh tools so the terminology is
settled BEFORE any translator is dispatched. Touch no plugin.

Purpose: the French rollout produced 267 divergent renderings because no glossary existed when the
per-plugin dispatch began. The shared-string set is computable today (measured 2026-09-01: 551
English strings appearing at more than one site, covering 2538 / 3789 occurrences = 67.0%). Settling
it now is the entire reason this stage precedes Stage 2.

Output: `scripts/i18n-zh-glossary.js`, `scripts/i18n-zh-lint.js`, `scripts/i18n-zh-backtranslate.js`.
Three path-scoped commits (one per task — each preceded by `git add`, because the files are
untracked and `git commit -- <path>` sees only tracked files).

HARD SCOPE BOUNDARY — non-negotiable:
- THREE NEW FILES ONLY. Zero files under `plugins/`. Zero edits to `scripts/i18n-fr-glossary.js`,
  `scripts/i18n-fr-lint.js`, `scripts/check-i18n.js`, `scripts/check-ui-labels.js`,
  `scripts/i18n-canon.js`, `scripts/i18n-extract.js`.
- No npm install, no root `package.json`, no `node_modules`. This repo has neither and this task
  does not introduce one. The Z3 data comes in as a one-time fetch whose OUTPUT is embedded; there
  is no runtime dependency. (If the executor concludes a dependency is unavoidable, STOP and report
  rather than installing — a package-legitimacy audit table does not exist for this task.)
- All three tools are REPORTS: exit 0 regardless of findings.
- Do NOT port French `T1`-`T7` (French typography) or `C1` (casing). Han has no case and the French
  U+00A0 rules are the deliberate inverse of Z2. Porting them would be actively wrong, not merely
  useless.
</objective>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260901-akh-research-and-plan-chinese-localization-a/260901-akh-IMPLEMENTATION-PLAN.md
@research/i18n-zh-hans-localization.md
@scripts/i18n-fr-glossary.js
@scripts/i18n-fr-lint.js
</context>

<facts_measured_at_planning_time>
Observed live on 2026-09-01, branch `main`, worktree clean apart from
`.gsd/dispatch-isolation-sentinel.json`:

| Fact | Value | How it was obtained |
|---|---|---|
| node | v24.19.0 | `node --version` |
| plugins matching `O-*` | 43 | `ls plugins \| grep -c '^O-'` |
| plugins carrying `i18n.js` under either UI root | 43 / 43 | ESM-import walk over `Source/ui/public/js/i18n.js` and `Resources/ui/js/i18n.js` |
| entries needing a zh value (LABELS.t + I18N.t) | 3789 (2367 label + 1422 title) | same walk |
| unique short strings | 1802 | same walk, normalised `\s+`->' ', trim, lower-case |
| **shared strings (>1 site)** | **551**, covering **2538 / 3789 = 67.0 %** | same walk |
| root `package.json` | none | `find . -maxdepth 3 -name package.json` |
| global npm packages | `corepack`, `npm` only | `ls $(npm root -g)` |
| `scripts/i18n-zh-*.js` | none exist | `ls scripts/i18n-zh-*.js` |
| OpenCC `TSCharacters.txt` reachable | yes, 5062 lines, sha256 `737c21c66f55a419dd6956cb3089476cdefc5a36877452631617696df1e5d925` | `curl` from `raw.githubusercontent.com/BYVoid/OpenCC/master/data/dictionary/` |
| Traditional-only set = TS keys minus ST keys | **4139 chars**, ~12.8 KB UTF-8 | computed; spot-checked 這 in-set, 这 out, 音 out, 後 in-set |

The research document says 543 / 64 %; the live measurement says 551 / 67.0 %. The difference is
normalisation, not drift. **Use the live number.** Re-run the measurement at execution time (Task 3
step 1) rather than trusting this table — it is a planning-time observation, not edit authority.
</facts_measured_at_planning_time>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end skeleton — all three tools wired, two rules proven, 43-plugin vacuity run</name>
  <files>scripts/i18n-zh-glossary.js, scripts/i18n-zh-lint.js, scripts/i18n-zh-backtranslate.js</files>
  <read_first>
    scripts/i18n-fr-lint.js (whole file — the plugin walk at lines 151-158, the `norm()` at 110-114,
    the `sameAsEn` / `termNote` INFO handling at 186-206, the per-plugin table printer and the
    summary block are the shapes to reuse verbatim);
    scripts/i18n-fr-glossary.js lines 1-100 and 380-425 (the AGPL header block, the TERMS comment
    contract, and the `module.exports` line).
  </read_first>
  <action>
Create all three files with the FULL skeleton wired end to end, but only two rules implemented. The
point of this task is to prove the architecture — module shape, plugin walk, report lifecycle,
self-test fixture mechanism — on the cheapest possible content, before the 551-term glossary is
written.

**Every file starts with the repo's AGPL-3.0 header block, copied verbatim from
`scripts/i18n-fr-glossary.js` lines 1-19.**

**Every file's header comment states the report-to-gate lifecycle, in these terms:** this tool ships
as a REPORT and exits 0 regardless of findings; it is promoted to a GATE (exit 2 on any finding)
only once the Stage 2 pilot (O-Chorus) is at zero findings; that is the exact lifecycle
`scripts/i18n-fr-lint.js` went through, and it is what keeps a half-built lint from blocking Stage 2.

**`scripts/i18n-zh-glossary.js`** — CommonJS data module, `'use strict'`. Exports, in this order:
- `TERMS` — English key (lower-cased, trimmed, trailing period dropped) to an ARRAY of accepted
  zh-Hans renderings, ROOT FIRST. Same value type as the French glossary so Stage-2 tooling can
  consume both uniformly. Seed it in this task with roughly a dozen chrome terms only (settings,
  interface language, hover help, save, load, delete, reset, on, off, mix, depth, spread, rate,
  size, level, input, output) — the full fill is Task 3.
- `BUDGETS` — English key to `{ maxChars, cellWidthPx, fontSizePx, source }`, or the key absent
  meaning UNBUDGETED. Seed exactly three, from the O-Chorus cliffs recorded in
  `plugins/O-Chorus/Source/ui/public/js/i18n.js` lines 39-42 and the 10 px caption size given in
  IMPLEMENTATION-PLAN Stage 2 item 6: `depth` -> 62/10 = 6, `save` -> 62/10 = 6, `spread` -> 50/10 = 5.
  Record `source` as a citation string naming that header and that plan line.
- `SAME_AS_EN` — an array of the English tokens that must stay English in a zh table:
  `LFO`, `MIDI`, `dB`, `Hz`, `kHz`, `ms`, `BPM`, and any others the corpus proves in Task 3.
- `FORBIDDEN_IN_LABELS`, `FORBIDDEN_IN_PROSE` — same mechanism as French (a wrong rendering mapping
  to what should have been written), empty objects in this task, filled in Task 3.
- `charCount(s)` — `[...String(s)].length`. Code-point iteration, NOT `.length`: CJK extension
  characters are surrogate pairs and `.length` double-counts them.
- `TERM_META` — built at require time from TERMS + BUDGETS: `{ [en]: { zh, charCount, maxChars } }`
  where `zh` is the ROOT rendering, `charCount` is `charCount(zh)` and `maxChars` is the budget or
  `null`. Explain in a comment why charCount is DERIVED and not stored: a stored count is a mirrored
  constant and mirrored constants drift silently.

The glossary header must additionally state: the project has no native Chinese reader; the rollout
ships at `reviewed: 'bt'` (an independent back-translation the developer read against the English
source); `'native'` stays open as a later upgrade and is not a blocker; and `'mt'` never ships.

**`scripts/i18n-zh-lint.js`** — the structure of `scripts/i18n-fr-lint.js`, with the French rules
removed. Reuse verbatim: the `UI_ROOTS` pair, the dynamic-ESM `lintPlugin` walk, the row shapes
(`label` / `title` / `body`), the `sameAsEn` and `termNote` INFO handling (both are INFO, never a
finding — a `termNote` exempts the entry from the term rules exactly as it does in French), the
per-plugin table printer, the details block, and the summary block. `I18N_EXEMPT` stays
`check-i18n.js`'s concern; this lint neither reads nor re-interprets it.

Implement TWO rules in this task:
- `Z5` glossary conformance — a LABEL or tooltip TITLE whose English is a `TERMS` key must render
  as one of its accepted zh forms. The G1 analogue; same normalisation, same `termNote` exemption.
- `R1` reviewed enum — a `zh-Hans` entry whose `reviewed` is absent or not one of `'mt' | 'bt' |
  'native'` is a finding; an entry at `'mt'` is counted and reported separately as BELOW SHIP BAR
  (info tier), which is the value this rule adds over `check-i18n.js` assertion [5].

Stub the other seven codes so they are declared but never fire yet, and print them in the table.

Flags: `--plugin <Name>`, `--verbose`, `--codes`, `--self-test`.
- `--codes` prints the rule codes, space-separated, one line, nothing else:
  `Z1 Z2 Z3 Z4 Z5 Z6 Z7 F1 R1`
- `--self-test` runs the fixture harness described below and exits 0.

**Self-test harness** — this is the mechanism the exit criterion depends on, so build it now, in
this task, with only Z5 and R1 wired into it. For each rule it runs the rule against a deliberate
VIOLATION and against a clean CONTROL, and prints one line per rule:
`SELF-TEST <code> FIRES on violation, silent on control` or `SELF-TEST <code> BROKEN: <which half failed>`.
It ends with `SELF-TEST: <n>/9`. Rules that need a plugin-shaped ESM module (Z5, R1) get one
written to `os.tmpdir()` at run time and deleted after; string-level rules use in-memory fixtures.
**No fixture is ever written under `plugins/`, and none is committed.** A rule that cannot be shown
to fire is not implemented — it is decorative.

**`scripts/i18n-zh-backtranslate.js`** — same plugin walk. Default mode (no flags) prints a
per-plugin coverage report: rows, zh entries present, and the `reviewed` breakdown across
`mt`/`bt`/`native`. `--verbose` prints every row rather than a capped sample. Exit 0.

Wire the emit/ingest round trip in this task with a stub scoring function; the independence
mechanism and provenance refusal are Task 2.

Then run all three over the 43 plugins. There is no zh content yet, so the correct output is an
explicit VACUITY line — the lint and the back-translate tool must each print a line of the form
`VACUITY: 0 zh-Hans entries found across 43 plugins — nothing was checked` and must NOT print a
success banner. A vacuity result is not a pass, and the tools must say so in their own words.

Commit. `git branch --show-current` and `git status --short` immediately before, then
`git add` the three paths and `git commit -- ` the same three paths. Path-scope it; another session
shares this checkout's index.
  </action>
  <verify>
    <automated>bash -c '
set -u; cd /Users/taylorbrook/Dev/VST-development
fail() { echo "FAIL: $*"; exit 1; }

node -e "
const g = require(\"./scripts/i18n-zh-glossary.js\");
const n = Object.keys(g.TERMS).length;
if (n < 10) throw new Error(\"TERMS too small: \" + n);
if (typeof g.charCount !== \"function\") throw new Error(\"no charCount export\");
for (const k of [\"TERMS\",\"BUDGETS\",\"SAME_AS_EN\",\"FORBIDDEN_IN_LABELS\",\"FORBIDDEN_IN_PROSE\",\"TERM_META\"])
  if (!g[k]) throw new Error(\"missing export: \" + k);
if (g.BUDGETS[\"depth\"].maxChars !== 6 || g.BUDGETS[\"save\"].maxChars !== 6 || g.BUDGETS[\"spread\"].maxChars !== 5)
  throw new Error(\"O-Chorus budgets not seeded\");
console.log(\"glossary OK, terms=\" + n);
" || fail "glossary contract"

node scripts/i18n-zh-lint.js --codes > /tmp/zhcodes.txt || fail "--codes exited non-zero"
grep -qx "Z1 Z2 Z3 Z4 Z5 Z6 Z7 F1 R1" /tmp/zhcodes.txt || { cat /tmp/zhcodes.txt; fail "code list wrong"; }
grep -qE "(T[1-7]|C1|G1)" /tmp/zhcodes.txt && fail "French code leaked into the zh code list"

node scripts/i18n-zh-lint.js --self-test > /tmp/zhselftest1.txt || fail "--self-test exited non-zero"
n=$(grep -c "FIRES on violation, silent on control" /tmp/zhselftest1.txt)
[ "$n" -eq 2 ] || { cat /tmp/zhselftest1.txt; fail "expected Z5 and R1 proven, got $n"; }
grep -q "SELF-TEST Z5 FIRES" /tmp/zhselftest1.txt || fail "Z5 not proven"
grep -q "SELF-TEST R1 FIRES" /tmp/zhselftest1.txt || fail "R1 not proven"

node scripts/i18n-zh-lint.js > /tmp/zhlint1.txt; rc=$?
[ "$rc" -eq 0 ] || fail "lint exit=$rc (must be 0 — it is a report)"
grep -q "VACUITY: 0 zh-Hans entries found across 43 plugins" /tmp/zhlint1.txt || { tail -20 /tmp/zhlint1.txt; fail "no VACUITY line from lint"; }

node scripts/i18n-zh-backtranslate.js > /tmp/zhbt1.txt; rc=$?
[ "$rc" -eq 0 ] || fail "backtranslate exit=$rc (must be 0)"
grep -q "VACUITY" /tmp/zhbt1.txt || fail "no VACUITY line from backtranslate"

git status --short | grep -qE "^.. (plugins/|scripts/i18n-fr-|scripts/check-)" && fail "out-of-scope file touched"
[ -f package.json ] && fail "a root package.json appeared — no npm dependency is permitted"
echo "TASK 1 VERIFY OK — scope clean"
'</automated>
    <human-check>Read the header comment of each of the three files and confirm each one states, in its own words, that it is a report today and becomes a gate only after the pilot reaches zero findings.</human-check>
  </verify>
  <done>
All three files exist, carry the AGPL header, and state the report-to-gate lifecycle. The glossary
requires cleanly and exports TERMS / BUDGETS / SAME_AS_EN / FORBIDDEN_IN_LABELS /
FORBIDDEN_IN_PROSE / TERM_META / charCount, with the three O-Chorus budgets seeded.
`--codes` prints the nine zh codes and no French code. `--self-test` shows Z5 and R1 firing on a
violation and silent on a control. All three tools run over 43 plugins, print an explicit VACUITY
line, and exit 0. `git status --short` shows nothing under `plugins/` and no French script touched.
One path-scoped commit landed on `main`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The remaining seven rules (Z1 Z2 Z3 Z4 Z6 Z7 F1) and back-translation independence</name>
  <files>scripts/i18n-zh-lint.js, scripts/i18n-zh-backtranslate.js, scripts/i18n-zh-glossary.js</files>
  <read_first>
    research/i18n-zh-hans-localization.md section 6.3 (the rule table and the rationale column) and
    section 3.3 (why the French U+00A0 rules invert, and why C1 has no zh analogue);
    scripts/i18n-fr-lint.js lines 128-150 (the `typography()` and `forbidden()` shapes to mirror
    structurally, NOT to copy the rules from).
  </read_first>
  <behavior>
Each rule below is written as its self-test pair — the violation that must FIRE and the control
that must stay SILENT. Write the pair into `--self-test` first, watch it fail, then implement.

- Z1 ASCII punctuation in Han prose. FIRES: `混音, 深度.` CONTROL: `混音，深度。` and
  `延迟 (delay) 20 ms` — an ASCII parenthesis or period INSIDE a Latin/unit token is legal and must
  not fire. This exemption is the rule's whole difficulty; build the Latin/unit-token mask first.
- Z2 U+00A0 before `: ; ! ? %`. FIRES: `深度 ：` CONTROL: `深度：` and `50%`.
- Z3 Traditional-only character in a zh-Hans table. FIRES: `這個聲音` CONTROL: `这个声音`.
- Z4 Latin/CJK spacing consistency. FIRES: a table that writes `20 ms 延迟` in one entry and
  `20ms延迟` in another — inconsistency is the finding, not either form on its own; and any thin
  space U+2009 / U+200A between a Latin run and a Han run FIRES unconditionally (a thin space has
  no glyph in some faces and renders as a box where no gate looks — the same reasoning that chose
  U+00A0 over U+202F for French). CONTROL: a table that is consistently spaced throughout.
- Z6 character budget. FIRES: a rendering of 7 code points for an English key whose `BUDGETS`
  entry is `maxChars: 6`. CONTROL: the same key at 6, and any key with no `BUDGETS` entry at any
  length (UNBUDGETED is inert by design).
- Z7 full-width Latin or digits. FIRES: `ＬＦＯ ２` CONTROL: `LFO 2`.
- F1 forbidden rendering. FIRES: a label rendering that is a key of `FORBIDDEN_IN_LABELS`.
  CONTROL: the accepted rendering the glossary lists for the same English. Mirror the French
  precedent exactly: a rendering the glossary itself ACCEPTS for this English must never draw F1,
  and a `termNote` exempts the entry from both F1 and Z5.
  </behavior>
  <action>
**Z3's character set is derived, never hand-written.** Write a throwaway generator under
`/private/tmp/claude-501/-Users-taylorbrook-Dev-VST-development/6a5e7373-1814-4a30-8a86-40bdf3e7ac65/scratchpad/`
(NOT under `scripts/`, NOT committed) that:
1. `curl`s `https://raw.githubusercontent.com/BYVoid/OpenCC/master/data/dictionary/TSCharacters.txt`
   and `.../STCharacters.txt`;
2. records the sha256 of each fetched file;
3. computes `Traditional-only = keys(TSCharacters) \ keys(STCharacters)` — planning-time
   measurement gave 4139 characters, ~12.8 KB, with 這 and 後 in-set and 这 and 音 out. Treat those
   four as the generator's own assertion; if the live fetch disagrees, STOP and report rather than
   shipping a set you cannot spot-check;
4. **writes the resulting literal into `scripts/i18n-zh-lint.js` by replacing a sentinel line** the
   skeleton left there. The executor must not type 4139 CJK characters by hand — a script writes
   them, which is also what makes the set reproducible.

Immediately above the generated literal, record the provenance as a comment: the two source URLs,
both upstream sha256 values, the derivation rule in words, the exact regeneration command, and the
fetch date. A character set whose provenance is unrecorded proves nothing, which is the same
standard this rollout applies to a back-translation.

Iterate the set and every scanned string with `[...str]`, never `charAt`/`.length` — CJK extension
characters are surrogate pairs and index-based iteration splits them.

**Fill `FORBIDDEN_IN_LABELS` and `FORBIDDEN_IN_PROSE`** in the glossary with the zh content the
corpus warrants — keep the French mechanism, replace the content. Where the set is genuinely empty
at this stage, leave it empty with a comment saying so rather than inventing entries; F1's self-test
uses a fixture, so an empty production table does not make the rule decorative.

**Back-translation independence.** Complete `scripts/i18n-zh-backtranslate.js`:
- `--emit <PluginName|--all> --out <path>` writes a TSV of `id \t zh` — **the English is withheld**.
  Withholding it is the independence mechanism: a reverse pass that can see the source round-trips
  the source's own vocabulary and reads clean while the Chinese is wrong. `--out` has no default;
  the tool refuses to guess a write path.
- `--ingest <file> --provenance "<what produced the en-prime, and when>"` joins the returned
  `id \t en'` back to the stored `en` and `zh` and prints the `en -> zh -> en'` triple, worst drift
  first, with a lexical-overlap score. If `--provenance` is missing, or if it matches the
  provenance recorded at emit time, the tool prints
  `REFUSED: back-translation provenance is missing or identical to the forward pass — this triple proves nothing`
  and reports nothing further. It still exits 0 (it is a report), but it reports a refusal, not a result.
- The header must state plainly what the tool does mechanically (enumerate, withhold, join, diff,
  score, record provenance) and what it cannot do (produce the `en'` itself — that requires a
  separate, independent pass by a different agent or session).

Commit. Re-check `git branch --show-current` and `git status --short` immediately before, then
`git add` and `git commit -- ` the same three paths.
  </action>
  <verify>
    <automated>bash -c '
set -u; cd /Users/taylorbrook/Dev/VST-development
fail() { echo "FAIL: $*"; exit 1; }

node scripts/i18n-zh-lint.js --self-test > /tmp/zhselftest2.txt || fail "--self-test exited non-zero"
grep -q "SELF-TEST: 9/9" /tmp/zhselftest2.txt || { cat /tmp/zhselftest2.txt; fail "self-test is not 9/9"; }
grep -q "BROKEN" /tmp/zhselftest2.txt && { cat /tmp/zhselftest2.txt; fail "a rule reported BROKEN"; }
n=$(grep -c "FIRES on violation, silent on control" /tmp/zhselftest2.txt)
[ "$n" -eq 9 ] || { cat /tmp/zhselftest2.txt; fail "rules proven: $n/9"; }
for c in Z1 Z2 Z3 Z4 Z5 Z6 Z7 F1 R1; do
  grep -q "SELF-TEST $c FIRES on violation, silent on control" /tmp/zhselftest2.txt || fail "$c not proven"
done

node -e "
const s = require(\"fs\").readFileSync(\"scripts/i18n-zh-lint.js\", \"utf8\");
if (!/raw\.githubusercontent\.com\/BYVoid\/OpenCC/.test(s)) throw new Error(\"no OpenCC provenance URL\");
if ((s.match(/[0-9a-f]{64}/g) || []).length < 2) throw new Error(\"fewer than two upstream sha256 values recorded\");
console.log(\"Z3 provenance OK\");
" || fail "Z3 provenance"

node scripts/i18n-zh-backtranslate.js --ingest /dev/null > /tmp/zhbt2.txt 2>&1; rc=$?
[ "$rc" -eq 0 ] || fail "backtranslate --ingest exit=$rc (must be 0 — it is a report)"
grep -q "REFUSED" /tmp/zhbt2.txt || { cat /tmp/zhbt2.txt; fail "unprovenanced ingest was not refused"; }

node scripts/i18n-zh-lint.js > /dev/null; rc=$?; [ "$rc" -eq 0 ] || fail "lint exit=$rc"
node scripts/i18n-zh-backtranslate.js > /dev/null; rc=$?; [ "$rc" -eq 0 ] || fail "backtranslate exit=$rc"

git status --short | grep -qE "^.. (plugins/|scripts/i18n-fr-|scripts/check-)" && fail "out-of-scope file touched"
[ -f package.json ] && fail "a root package.json appeared — no npm dependency is permitted"
[ -d node_modules ] && fail "node_modules appeared — no npm dependency is permitted"
echo "TASK 2 VERIFY OK — 9/9 rules proven, scope clean"
'</automated>
    <human-check>Read the Z3 provenance comment block. Confirm it names both source URLs, both upstream sha256 values, the derivation rule, the regeneration command and the fetch date — and that the four spot-check characters are recorded.</human-check>
  </verify>
  <done>
`--self-test` prints `SELF-TEST: 9/9` with every rule firing on its violation and silent on its
control, and no `BROKEN` line. The Z3 set is machine-generated from OpenCC data with full provenance
recorded in the file. `--ingest` without `--provenance` prints `REFUSED`. Both tools still exit 0 on
the 43-plugin corpus. No file under `plugins/`, no French script and no `check-*` script changed.
Second path-scoped commit landed on `main`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Settle the full shared-string glossary and close the stage</name>
  <files>scripts/i18n-zh-glossary.js, scripts/i18n-zh-lint.js</files>
  <action>
This is the long pole. It is 551 terms, not code, and it must be produced in a way that survives a
single session.

**Step 1 — regenerate the term list mechanically, at execution time.** Write a throwaway script in
the scratchpad that walks all 43 plugins by the same dynamic-ESM route the lint uses, collects every
`LABELS[k].en.t` and `I18N[k].en.t`, normalises (collapse whitespace, trim, lower-case), counts
occurrences, keeps every string appearing at more than one site, and emits a TSV of
`occurrences \t english \t plugins-it-appears-in`, sorted by occurrences descending. Planning-time
measurement was 551 terms / 2538 of 3789 occurrences / 67.0 % — treat a materially different number
as a signal to stop and look, not as a number to paper over. The generator is scratchpad-only and
is NOT committed.

**Step 2 — fill the renderings in batches, highest occurrence count first.** Work down the sorted
list in batches of roughly 50 and write each batch into `TERMS` before starting the next, so a
context exhaustion loses one batch rather than the session. Group the file by the same kind of
section comments the French glossary uses (chrome shared by all 43, transport, dynamics, modulation,
filter, tuning, and so on), so a later reader can find a term.

Rules for the renderings:
- ROOT FIRST, then any accepted alternate. The alternate list is how a term with a tight cell gets a
  shorter option: Chinese has no abbreviations, so the lever is a 2-character rendering instead of a
  3- or 4-character one. Where you list a shorter alternate, say in a trailing comment which cell
  forced it.
- The `SAME_AS_EN` tokens (`LFO`, `MIDI`, `dB`, `Hz`, `kHz`, `ms`, `BPM`, and anything else the
  corpus proves) stay English and are declared there rather than given a Chinese rendering. Any term
  in `SAME_AS_EN` must NOT also carry a `TERMS` rendering — add an assertion at module load that
  fails loudly on an overlap.
- Prefer the rendering a Chinese-language DAW actually shows over a dictionary-literal one, and where
  two defensible renderings exist, pick one and record why in a trailing comment. The point of the
  glossary is that 43 reviewers do not each pick differently.
- Do not invent a budget. A term gets a `BUDGETS` entry only where a cell was MEASURED — which today
  is the three O-Chorus seeds from Task 1. Everything else stays out of `BUDGETS` and Z6 is inert on
  it, by design, until Stage 2's `check-ui-labels` zh arm measures the cell.

**Step 3 — make the budget coverage visible.** The lint's summary block must print a line of the
form `Z6 coverage: <n> of <total> glossary terms carry a measured budget; <m> are UNBUDGETED and Z6
is inert on them — Stages 2-4 fill these from the check-ui-labels zh arm`. An inert rule that does
not announce itself is the exact "gate goes green on unchecked content" failure this rollout exists
to prevent, so this line is a deliverable, not decoration.

**Step 4 — close the stage.** Run all three tools over the 43 plugins one final time. The corpus
still has no zh content, so the correct result is still the VACUITY line and exit 0 on all three.
Re-run `--self-test` and confirm 9/9 still holds against the now-full glossary (a real 551-term
`TERMS` is a different input from the twelve-term seed, and Z5's fixture must still fire).

Commit. Re-check `git branch --show-current` and `git status --short` immediately before, then
`git add` and `git commit -- ` the three paths.

Finally, update `.planning/STATE.md` `last_activity` to record Stage 1 complete and Stage 2
(pilot O-Chorus) as next — a separate path-scoped commit on `.planning/STATE.md` alone.
  </action>
  <verify>
    <automated>bash -c '
set -u; cd /Users/taylorbrook/Dev/VST-development
fail() { echo "FAIL: $*"; exit 1; }

node -e "
const g = require(\"./scripts/i18n-zh-glossary.js\");
const terms = Object.keys(g.TERMS);
if (terms.length < 500) throw new Error(\"TERMS has only \" + terms.length + \" entries; the measured shared-string set is ~551\");
const same = g.SAME_AS_EN.map((s) => String(s).toLowerCase());
const overlap = terms.filter((t) => same.includes(t));
if (overlap.length) throw new Error(\"SAME_AS_EN token also carries a rendering: \" + overlap.join(\",\"));
const noHan = terms.filter((t) => !/\\p{Script=Han}/u.test(g.TERMS[t][0]));
if (noHan.length) throw new Error(noHan.length + \" root renderings carry no Han, e.g. \" + noHan.slice(0, 5).join(\" | \"));
const meta = g.TERM_META;
const bad = terms.filter((t) => meta[t].charCount !== [...meta[t].zh].length);
if (bad.length) throw new Error(\"charCount is not derived for \" + bad.length + \" terms\");
const budgeted = terms.filter((t) => meta[t].maxChars !== null).length;
console.log(\"glossary OK: \" + terms.length + \" terms, \" + budgeted + \" budgeted, \" + (terms.length - budgeted) + \" unbudgeted\");
" || fail "glossary contract"

node scripts/i18n-zh-lint.js --self-test > /tmp/zhselftest3.txt || fail "--self-test exited non-zero"
grep -q "SELF-TEST: 9/9" /tmp/zhselftest3.txt || { cat /tmp/zhselftest3.txt; fail "self-test regressed against the full glossary"; }

node scripts/i18n-zh-lint.js > /tmp/zhlint3.txt; rc=$?
[ "$rc" -eq 0 ] || fail "lint exit=$rc"
grep -q "VACUITY: 0 zh-Hans entries found across 43 plugins" /tmp/zhlint3.txt || fail "no VACUITY line"
grep -qE "Z6 coverage: [0-9]+ of [0-9]+ glossary terms carry a measured budget" /tmp/zhlint3.txt || { tail -20 /tmp/zhlint3.txt; fail "Z6 coverage not disclosed"; }

node scripts/i18n-zh-backtranslate.js > /dev/null; rc=$?; [ "$rc" -eq 0 ] || fail "backtranslate exit=$rc"
node scripts/check-i18n.js > /tmp/checki18n3.txt 2>&1; rc=$?
[ "$rc" -eq 0 ] || { tail -30 /tmp/checki18n3.txt; fail "check-i18n exit=$rc — Stage 0 baseline broken"; }
node scripts/i18n-fr-lint.js > /tmp/frlint3.txt 2>&1; rc=$?
[ "$rc" -eq 0 ] || { tail -30 /tmp/frlint3.txt; fail "i18n-fr-lint exit=$rc — the French rollout regressed"; }

git status --short | grep -qE "^.. plugins/" && fail "a plugin file changed"
[ -f package.json ] && fail "a root package.json appeared"
[ -d node_modules ] && fail "node_modules appeared"
git log --oneline -4
echo "TASK 3 VERIFY OK — zero plugin files touched, French and check-i18n baselines intact"
'</automated>
    <human-check>Spot-read twenty glossary entries spread across the file. Confirm each root rendering is plausible Chinese for the English key, that alternates are shorter (not merely different), and that any comment explaining a choice actually explains it.</human-check>
  </verify>
  <done>
`scripts/i18n-zh-glossary.js` carries a rendering for every shared English string measured from the
live corpus (~551), with derived charCounts, a `SAME_AS_EN` set that does not overlap `TERMS`, and
budgets only where a cell was measured. The lint prints its Z6 coverage line naming the unbudgeted
count. `--self-test` is still 9/9 against the full glossary. All three tools exit 0 with the VACUITY
result on 43 plugins, and `node scripts/check-i18n.js` still exits 0 — Stage 0's baseline is intact.
Zero files under `plugins/` changed. Third path-scoped commit landed on `main`, plus a separate
STATE.md commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network -> repo source | The Z3 character set is fetched from `raw.githubusercontent.com` at authoring time and its OUTPUT is embedded in a committed script. Untrusted bytes cross into tracked source. |
| shared git index | Two sessions share `.git/index` and HEAD in this single checkout; another session's staging can join a commit in the gap between checking and committing. |
| scratchpad -> repo | Throwaway generators and self-test fixtures live outside the repo; a leak of either into `scripts/` or `plugins/` would ship unreviewed content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-fr8-01 | Tampering | OpenCC data fetch in Task 2 | medium | mitigate | Record both upstream sha256 values and the fetch date in the file; assert four known spot-check characters (這/後 in-set, 这/音 out) before writing; STOP and report on any disagreement. The fetched file is never committed and never executed — only a derived character set is embedded. |
| T-fr8-02 | Tampering | shared `.git/index` across concurrent sessions | high | mitigate | Path-scope every commit (`git add <3 paths>` then `git commit -- <3 paths>`); re-check `git branch --show-current` and `git status --short` immediately before each of the three commits, not once at task start. Never `git add -A`, never `git commit -a`. |
| T-fr8-03 | Information disclosure | back-translation `--emit` batch | low | accept | The batch contains only plugin UI strings that already ship in the binaries. `--out` has no default so the tool never writes to an unexpected path. |
| T-fr8-04 | Elevation of privilege | npm dependency for Z3 | high | mitigate | No install occurs. The repo has no root `package.json` and none is created; the Z3 route is a one-time data fetch with an embedded output and zero runtime dependency. If the executor concludes a dependency is unavoidable it STOPS and reports — there is no package-legitimacy audit table for this task, so an install cannot be approved inside it. |
| T-fr8-05 | Repudiation | back-translation whose reverse pass is unrecorded | high | mitigate | `--ingest` refuses to report a triple without a `--provenance` string, and refuses one whose provenance matches the forward pass. A triple whose provenance is unrecorded proves nothing (research risk table, final row). |
| T-fr8-SC | Tampering | npm/pip/cargo installs | high | mitigate | None occur in this task; the scope boundary in `<objective>` forbids them and the Task 1/2/3 scope verifications fail the task if `package.json` or `node_modules` appears. |
</threat_model>

<verification>
Stage-level, run after Task 3:

```bash
cd /Users/taylorbrook/Dev/VST-development
node scripts/i18n-zh-lint.js;            echo "lint exit=$?"          # 0, with the VACUITY line
node scripts/i18n-zh-lint.js --self-test | tail -1                     # SELF-TEST: 9/9
node scripts/i18n-zh-lint.js --codes                                   # Z1 Z2 Z3 Z4 Z5 Z6 Z7 F1 R1
node scripts/i18n-zh-backtranslate.js;   echo "bt exit=$?"             # 0
node scripts/check-i18n.js;              echo "check-i18n exit=$?"     # 0 — Stage 0 baseline intact
node scripts/i18n-fr-lint.js;            echo "fr-lint exit=$?"        # 0 — French untouched
git status --short                                                     # nothing under plugins/
git log --oneline -4
```

The French lint run is the regression guard: this task must not have disturbed the French rollout,
and `fr-lint` is a real gate (exit 2 on any finding), so a non-zero exit there is a hard failure.
</verification>

<success_criteria>
- Three new files exist under `scripts/`, all report-only (exit 0), all stating the report-to-gate
  lifecycle in their headers.
- `--self-test` proves all nine rules (Z1 Z2 Z3 Z4 Z5 Z6 Z7 F1 R1) fire on a deliberate violation
  and stay silent on a control. No rule is decorative.
- Z3's character set is derived from OpenCC data with recorded provenance, not hand-written.
- The glossary covers the measured shared-string set (~551 terms) with derived charCounts and
  measured-only budgets, and the lint discloses its Z6 coverage.
- The back-translate tool withholds the English on emit and refuses an unprovenanced ingest.
- `check-i18n` and `i18n-fr-lint` both still exit 0; zero files under `plugins/` changed; no npm
  dependency, no `package.json`, no `node_modules`.
- Three path-scoped commits plus a STATE.md commit, all on `main`.
</success_criteria>

<output>
Create `.planning/quick/260901-fr8-stage-1-of-the-zh-hans-rollout-p8-create/260901-fr8-SUMMARY.md` when done.
</output>
</content>
</invoke>
