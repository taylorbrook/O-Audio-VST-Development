---
phase: quick-260904-q4j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/i18n-zh-lint.js
  - plugins/O-MicrotonalSampler/Resources/ui/js/i18n.js
autonomous: true
requirements: [QUICK-260904-q4j-Z1, QUICK-260904-q4j-Z8]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running `node scripts/i18n-zh-lint.js` across all 43 plugins prints zero finding lines of any code (D-01) — the live baseline measured at planning time is 8 Z1 findings, all on O-MicrotonalSampler, all false positives."
    - "A zh string carrying a leading-dot file extension is SILENT under Z1, and a Han-prose string carrying a genuine ASCII comma/period still FIRES Z1 (D-01)."
    - "`--self-test` reports 10/10, with Z8 among the proven rules (D-02)."
    - "Z8 fires on a Han-space-Han string through the CORPUS path (lintPlugin), not only through the fixture path (D-02)."
    - "Z8's repo-wide zero column agrees with an independent standalone Han-space-Han scan run across all 43 plugins (D-02)."
    - "The plugins/ tree is byte-identical to HEAD except for the one intentional documentation edit in O-MicrotonalSampler's i18n.js — the Z8 corpus positive control leaves no residue."
  artifacts:
    - scripts/i18n-zh-lint.js
    - plugins/O-MicrotonalSampler/Resources/ui/js/i18n.js
  key_links:
    - "CODES[] <-> lintRows() <-> SELF_TESTS{} — a code listed in CODES with no push in lintRows is an inert column that reads as a clean zero; a code with no SELF_TESTS entry prints `NOT YET IMPLEMENTED` and is excluded from the proven count. All three must be edited together or the rule is decorative."
    - "maskLatin() <-> ruleZ1() — maskLatin is the whole of Z1's difficulty; widening the mask can only REDUCE findings, so every widening needs a violation fixture that still fires."
---

<objective>
Repair `scripts/i18n-zh-lint.js` so it can be promoted to a gate in Stage 4 without failing on strings the glossary itself compels, and add the Han-space-Han rule (Z8) that Stage 3 found by reading and no rule can see.

Purpose: Stage 4 flips this lint from report-only (exit 0) to a gate (exit 2). Today the corpus is NOT at zero — 8 Z1 findings stand, every one a tool defect. Flipping the gate on this code would block 8 plugins on strings that are byte-identical to the glossary's own roots. Z5 compels the root; Z1 then flags it. Two rules in the same file disagree.

Output: a lint at 10/10 self-test and 0 repo-wide findings, with every change proven by a control that was observed RED before the fix landed.

## Locked decisions

- **D-01** — Fix `maskLatin()` so leading-dot file extensions (`.scl`, `.kbm`, `.omspreset`) are masked before the Han punctuation scan, and add self-test controls that discriminate: a `.scl`-carrying string must be SILENT under Z1, a genuine ASCII-punctuation leak must still FIRE. The controls are observed RED before the fix, per `pattern_probe_must_target_the_branch_the_fix_changed`.
- **D-02** — Add rule Z8: a plain U+0020 between two Han code points. Its repo-wide zero column must agree with an INDEPENDENT standalone scan across all 43 plugins before it counts as validated, per `pattern_new_lint_zero_must_agree_with_independent_scan`.

## In scope beyond the literal brief — flagged, not slipped in

The brief names six leading-dot renderings. The live lint run at planning time returns **8** Z1 findings, not 6. The other two are `label.floTokensBefore` and `label.floTokensBefore2` on O-MicrotonalSampler, both `否则使用文件名标记 (` — a label deliberately split around markup, so its open paren has no close paren in the same string. `maskLatin` masks only BALANCED parens, so the lone `(` survives.

This is the same defect class, diagnosed in the same Stage-3 finding (§14), and it is load-bearing for the stated goal: leaving it unfixed means the corpus is at 2, not 0, and Stage 4 still cannot flip the gate. A `termNote` cannot rescue it either — termNote exempts an entry from Z5 and F1 only, never from Z1. It is therefore fixed here, under D-01, with its own RED-first control.

## Explicitly NOT in scope

- **Do NOT flip the lint to a gate (exit 2).** The header comment's promotion criterion is met after this task, but the flip is Stage 4's decision and Stage 4's commit. Leave the REPORT ONLY banner and the `exit 0` behaviour exactly as they are.
- **Do NOT edit any zh-Hans rendering.** The Stage-3 precedent (Z3's 像 false positive) is settled: if the lint fires on the glossary's own rendering, the TOOL is wrong, not the string. The only plugin-file edit authorised here is retiring a now-stale comment.
- **Do NOT add a leading-`)` branch to `maskLatin`.** The sibling labels (`label.floTokensAfterLayer` / `label.floTokensAfterTech`) already use the full-width `）`, so no such string exists in the corpus. An unexercised branch is a rule with no control.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@scripts/i18n-zh-lint.js
@.planning/quick/260904-g5l-stage-3-of-the-zh-hans-rollout-the-hard-/260904-g5l-SUMMARY.md
@.planning/quick/260904-g5l-stage-3-of-the-zh-hans-rollout-the-hard-/260904-g5l-SUMMARY-task1.md
</context>

<baseline>
Every number below was measured live at planning time on `main`, working tree clean apart from `.gsd/dispatch-isolation-sentinel.json`. They are the before-picture the verifies compare against. Re-measure rather than trust them if the tree has moved.

| Measurement | Value at planning time |
|---|---|
| `node scripts/i18n-zh-lint.js` repo-wide TOTAL | 8 (all Z1, all O-MicrotonalSampler) |
| Z1 findings: leading-dot extension shape | 6 — `label.loadScl`, `label.loadKbm`, `label.saveScl`, `label.saveKbm`, `aria.savePreset`, `aria.loadPreset` |
| Z1 findings: unbalanced trailing paren shape | 2 — `label.floTokensBefore`, `label.floTokensBefore2` |
| `node scripts/i18n-zh-lint.js --self-test` | `SELF-TEST: 9/9` |
| zh-Hans entries checked | 612 across 43 plugins |
| entries at `reviewed:'mt'` | 0 |
| Independent Han-space-Han scan, all 43 plugins | 0 occurrences |
| Plugins carrying `.scl`/`.kbm` in their i18n table | 8 — O-Bells, O-Contrabass, O-Formant, O-IntonationPad, O-Lyrica, O-Marimba, O-MicrotonalSampler, O-Prism (the brief says 12; the live count is 8, and 8 is the number to report) |

`plugins/O-MicrotonalSampler/Resources/ui/js/i18n.js` is under `Resources/ui/js/`, NOT `Source/ui/public/js/`. The lint resolves either root via `UI_ROOTS`; do not assume the other path.

**The Task 1 mask changes were simulated at planning time against all five Z1 controls and all three violations, plus the four real corpus strings, and came back 11/11 as specified.** That simulation is a design check, not a substitute for the RED observation — Task 1 STEP 2 still has to be run and recorded on the real file. If the executor's reading disagrees with 11/11, the file has drifted and the divergence is the finding.
</baseline>

<tasks>

<task type="tracer">
  <name>Task 1: Z1 end-to-end — RED control, maskLatin fix, corpus at zero (D-01)</name>
  <files>scripts/i18n-zh-lint.js</files>
  <precondition>The working tree under `scripts/` and `plugins/` is clean at HEAD; `node scripts/i18n-zh-lint.js --self-test` currently ends in `SELF-TEST: 9/9`. If either is false, stop and report — the RED observation below is only meaningful from a known-good baseline.</precondition>
  <behavior>
    This task walks one rule through all three layers the file has — fixture, rule function, corpus — and it starts RED.

    - Control A (new, must end SILENT under Z1): a label reading `载入 .scl`. Before the fix this FIRES, which is the defect.
    - Control B (new, must end SILENT under Z1): a label reading `否则使用文件名标记 (` — a markup-split label whose close paren lives in a sibling DOM node. Before the fix this FIRES.
    - Violation C (new, must FIRE under Z1 both before AND after the fix): a body reading `混音 ( 深度` — an unmatched open paren that is NOT at end-of-string. This is the discriminator that proves the paren mask did not go too far.
    - Violation D (existing, must FIRE under Z1 both before AND after): the current fixture `混音, 深度.` — the discriminator that proves the leading-dot mask did not swallow genuine Han-prose punctuation.
    - Violation E (new, must FIRE under Z1 both before AND after): a body reading `混音 (深度)` — a BALANCED ASCII paren pair with Han inside. The existing balanced-paren branch deliberately keeps this, because Han in parens should take the full-width forms; the new end-of-string branch must not reach around it.
  </behavior>
  <action>
    Work in this order. The order IS the deliverable — a control added after the fix proves nothing (per D-01 and `pattern_probe_must_target_the_branch_the_fix_changed`).

    STEP 1 — fixtures only, no rule change. In `SELF_TESTS.Z1`, append Control A and Control B to the existing `control` array and append Violation C to the `violation` side. Note that `violation` is currently a single object, not an array; `selfTest()` normalises with `[].concat(t.violation)`, so promote it to an array holding the existing `混音, 深度.` body plus the new `混音 ( 深度` body. Use `BODY(...)` for both so Z1's Han-prose precondition holds and no label-only rule (Z5, Z6, R1) is dragged in. Write the fixture strings as JS unicode escapes, matching the style already used throughout `SELF_TESTS`.

    STEP 2 — observe RED. Run the self-test and record the Z1 line verbatim in the summary. The expected reading is `SELF-TEST Z1 BROKEN: fires on a control` with a denominator line of `SELF-TEST: 8/9`. That specific wording matters: `fires on a control` (not `silent on a violation`) is what proves Controls A and B are pointed at the branch the fix will change, while Violations C and D were already firing and stay firing. If the line reads anything else, STOP and report — the fixtures are not discriminating and the fix must not proceed.

    STEP 3 — fix `maskLatin()`. Two edits, both narrow.
    (a) Leading-dot extensions: the Latin/unit token pattern currently requires an alphanumeric first, so a dot with no alphanumeric before it survives into the Han scan. Allow one optional leading dot before the first alphanumeric run. The trailing `%?` and the existing internal-punctuation group stay exactly as they are.
    (b) Markup-split open paren: after the existing balanced-paren gloss replacement, add a second replacement that masks a single unmatched `(` occurring at END of string, allowing trailing whitespace. It must run AFTER the balanced replacement so a genuine balanced pair is never half-consumed. Anchoring at end-of-string is the whole discriminator — an unmatched paren anywhere else still fires, which is Violation C.

    STEP 4 — record the trade-off in the file. Add a short comment beside edit (a) naming the one shape the widened mask now goes blind to: a Han character followed immediately by an ASCII period and then Latin with no space, which now reads as an extension token. This is a narrow, deliberate blindness and the file must say so — the Z6 disclosure precedent in this same file is the house style.

    STEP 5 — observe GREEN at both layers. Re-run the self-test; then run the full corpus lint. Both verifies below must pass.
  </action>
  <verify>
    <automated>node scripts/i18n-zh-lint.js --self-test 2>/dev/null | tail -1   # must read exactly: SELF-TEST: 9/9</automated>
    <automated>node scripts/i18n-zh-lint.js --self-test 2>/dev/null | grep -c 'SELF-TEST Z1 FIRES on violation, silent on control'   # must print 1</automated>
    <automated>node scripts/i18n-zh-lint.js --verbose 2>/dev/null | grep -E '^  (Z[0-9]|F1|R1)  ' | wc -l   # must print 0 — every finding detail line across all 43 plugins</automated>
    <automated>node scripts/i18n-zh-lint.js --verbose 2>/dev/null | grep -c 'REPORT ONLY'   # must print 1 — the gate flip is NOT this task's job</automated>
  </verify>
  <done>The self-test reads 9/9 with Z1 among the proven rules; the repo-wide lint prints zero finding detail lines across all 43 plugins, down from the 8 measured at planning time; and the RED line `SELF-TEST Z1 BROKEN: fires on a control` was observed and recorded BEFORE the maskLatin edit. The REPORT ONLY banner is untouched.</done>
  <reversibility rating="reversible">A regex widening in one function, guarded by four fixtures; revert is a two-line edit.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rule Z8 — RED-first, corpus positive control, independent-scan agreement (D-02)</name>
  <files>scripts/i18n-zh-lint.js</files>
  <precondition>Task 1 is complete and its findings are committed, so the working tree is clean before the corpus positive control temporarily dirties one plugin file. A clean tree is what makes the residue check in STEP 5 meaningful.</precondition>
  <behavior>
    - Violation (must FIRE Z8): a label reading `写入 源` — one plain U+0020 between two Han code points, the shape of the real defect Stage 3 found by reading O-Octagon's `puck` body.
    - Control 1 (must be SILENT): `20 ms 延迟` — a space at a Latin/Han boundary is Z4's business, and Z8 must not poach it.
    - Control 2 (must be SILENT): `混音，深度` — correct full-width punctuation, no space anywhere.
    - Control 3 (must be SILENT): the body `延迟 (delay) 20 ms` — the space after `迟` precedes an ASCII paren, not a Han character.
    - Corpus behaviour: Z8 must fire through `lintPlugin`, not only through the fixture path. A rule proven only on fixtures can still be missing its push in `lintRows` and read as a clean zero column.
  </behavior>
  <action>
    Scope Z8 to exactly one predicate: a Han code point, one plain U+0020, another Han code point — the same predicate as the standalone scan recorded in Stage 3's task-1 summary §12. Do NOT widen it to U+00A0, U+2009, U+200A or U+3000. The widening would be defensible on its own but it would break the agreement test D-02 requires, because the independent scan is defined on the plain space. Say so in the header comment, and name Z2 and Z4 as the rules that already police the other whitespace classes at their own boundaries.

    Work in this order.

    STEP 1 — declare the code and its fixture, but do NOT implement the rule. Append `'Z8'` to `CODES`, and add a `SELF_TESTS.Z8` entry carrying the violation and three controls from the behavior block. Both halves are required for the RED reading: a code in `CODES` with no `SELF_TESTS` entry prints `NOT YET IMPLEMENTED` and is skipped rather than counted as broken, which is a weaker signal.

    STEP 2 — observe RED. Run the self-test. The expected reading is `SELF-TEST Z8 BROKEN: silent on a violation` with a denominator line of `SELF-TEST: 9/10`. Record it verbatim. If the line reads `NOT YET IMPLEMENTED`, STEP 1 is half-done — the fixture is missing.

    STEP 3 — implement. Add `ruleZ8(zh)` beside the other rule functions, using a `\p{Script=Han}` unicode-property regex on both sides of the literal space. Add the corresponding push in `lintRows` alongside the Z1/Z2/Z3/Z7 pushes, so it evaluates on every row including bodies — the real defect was in a body. Give it a note naming what it found: intra-Han whitespace that the Latin/Han boundary census cannot see. Then update the two places that enumerate the rules for a reader: the `── The checks ──` block in the file header, and the one-line `codes:` legend printed in the summary. The table header, separator width and totals row all derive from `CODES.length` and need no edit.

    STEP 4 — corpus positive control. A zero column that agrees with a zero scan is two zeros, and a rule that never runs produces one of them. Prove the corpus path is live: read `plugins/O-Chorus/Resources/ui/js/i18n.js` (or its `Source/ui/public/js/` sibling — resolve which exists rather than assuming), pick one zh-Hans rendering that is Han-only with no spaces, and insert a single U+0020 between two of its Han characters using a targeted edit. Record the key and the exact before/after strings. Run the lint scoped to that one plugin and confirm Z8 reads 1 for it.

    STEP 5 — revert the injection by a REVERSE TARGETED EDIT, removing the one space you added. Do NOT use `git checkout --`, `git restore`, or any other whole-file revert: Task 2's own uncommitted changes to `scripts/i18n-zh-lint.js` are in the same tree, and the memory pattern `pattern_negative_control_checkout_wipes_uncommitted_fix` is exactly this trap. Then prove no residue with the diff verify below.

    STEP 6 — independent-scan agreement. Run the standalone scan (verify block below) across all 43 plugins and confirm it prints 0, matching the lint's Z8 column. Report BOTH numbers side by side in the summary, and state plainly that the agreement is only load-bearing because STEP 4 proved the rule is not inert.
  </action>
  <verify>
    <automated>node scripts/i18n-zh-lint.js --self-test 2>/dev/null | tail -1   # must read exactly: SELF-TEST: 10/10</automated>
    <automated>node scripts/i18n-zh-lint.js --self-test 2>/dev/null | grep -c 'SELF-TEST Z8 FIRES on violation, silent on control'   # must print 1</automated>
    <automated>node scripts/i18n-zh-lint.js --codes 2>/dev/null | tr ' ' '\n' | grep -c '^Z8$'   # must print 1</automated>
    <automated>node scripts/i18n-zh-lint.js --verbose 2>/dev/null | grep -E '^  (Z[0-9]|F1|R1)  ' | wc -l   # must print 0 across all 43 plugins, Z8 included</automated>
    <automated>cd /Users/taylorbrook/Dev/VST-development && node --input-type=module -e "import fs from 'fs'; import path from 'path'; import {pathToFileURL} from 'url'; const R=['Source/ui/public/js/i18n.js','Resources/ui/js/i18n.js']; const P=fs.readdirSync('plugins').filter(n=>n.startsWith('O-')).sort(); let tot=0; for(const p of P){ const rel=R.find(r=>fs.existsSync(path.join('plugins',p,r))); if(!rel) continue; const m=await import(pathToFileURL(path.resolve('plugins',p,rel)).href); const rows=[]; for(const v of Object.values(m.LABELS||{})) rows.push(v['zh-Hans']?.t??''); for(const v of Object.values(m.I18N||{})){ rows.push(v['zh-Hans']?.t??''); rows.push(v['zh-Hans']?.b??''); } for(const s of rows){ const re=/\p{Script=Han} \p{Script=Han}/gu; while(re.exec(s)!==null) tot++; } } console.log('independent Han-space-Han across all 43:', tot);" 2>/dev/null | tail -1   # must print 0, and must equal the lint's Z8 column</automated>
    <automated>git -C /Users/taylorbrook/Dev/VST-development diff --stat -- plugins/ | wc -l   # must print 0 — the STEP 4 injection left no residue</automated>
  </verify>
  <done>Self-test reads 10/10 with Z8 proven; `--codes` lists Z8; the repo-wide lint prints zero finding lines with Z8 active; the independent standalone scan prints 0 and matches the Z8 column; the corpus positive control was observed firing on one named plugin and then fully reverted, with `git diff --stat -- plugins/` empty; and the RED line `SELF-TEST Z8 BROKEN: silent on a violation` was observed and recorded BEFORE `ruleZ8` existed.</done>
  <reversibility rating="reversible">One added rule, one added fixture, one added CODES entry.</reversibility>
</task>

<task type="auto">
  <name>Task 3: retire the stale tool-defect note and run the adjacent-gate regression sweep</name>
  <files>plugins/O-MicrotonalSampler/Resources/ui/js/i18n.js</files>
  <action>
    The header comment block in O-MicrotonalSampler's i18n.js (around lines 180-200) records the Z1 false positives as a live, unfixed tool defect and tells the reader the strings were left as authored because the lint is report-only. After Tasks 1 and 2 that note is false on its central claim. Read it, then rewrite it in place to say what is now true: the defect was diagnosed here, fixed in `scripts/i18n-zh-lint.js` under quick task 260904-q4j, and the strings were correctly left alone — the tool was the thing that was wrong. Keep the diagnosis prose, which is the durable part and is what let the fix be surgical; replace only the disposition. Keep it to the same order of length; this is a correction, not an expansion.

    Do NOT touch any `en`, `fr` or `zh-Hans` rendering in this file. The only edit is inside the comment block.

    Then run the adjacent-gate regression sweep. Nothing in this task should have moved `check-i18n.js` or the French lint, and the point of running them is to prove that rather than assume it. Record all three results in the summary. If either sibling gate has moved off its known-good reading, STOP and report before committing — a zh lint repair that broke the French gate is a net loss.
  </action>
  <verify>
    <automated>node scripts/i18n-zh-lint.js --self-test 2>/dev/null | tail -1   # must read exactly: SELF-TEST: 10/10</automated>
    <automated>node scripts/i18n-zh-lint.js --verbose 2>/dev/null | grep -E '^  (Z[0-9]|F1|R1)  ' | wc -l   # must print 0</automated>
    <automated>node scripts/check-i18n.js >/dev/null 2>&1; echo "check-i18n exit=$?"   # must be exit=0</automated>
    <automated>node scripts/i18n-fr-lint.js >/dev/null 2>&1; echo "fr-lint exit=$?"   # must be exit=0</automated>
    <automated>git -C /Users/taylorbrook/Dev/VST-development diff --name-only -- plugins/ | tr '\n' ' '   # must name exactly plugins/O-MicrotonalSampler/Resources/ui/js/i18n.js and nothing else</automated>
  </verify>
  <done>The O-MicrotonalSampler note reports the defect as fixed and names where; no rendering in that file changed; `check-i18n.js` and `i18n-fr-lint.js` both still exit 0; and the only plugin file touched across the whole task is O-MicrotonalSampler's i18n.js.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files -> lint process | `lintPlugin` and `materialise` both use dynamic `import()`, so any file they reach is EXECUTED, not parsed. Pre-existing; unchanged by this task. |
| temporary fixture -> os.tmpdir() | `materialise` writes ESM fixtures to a `mkdtemp` directory and removes them in a `finally`. Pre-existing; this task adds only `rows`-shaped fixtures, which never touch the filesystem. |
| Task 2 STEP 4 -> shipped plugin source | A deliberate, temporary mutation of a real plugin's i18n table, made to prove the corpus path is live. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q4j-01 | Tampering | Task 2 STEP 4 corpus positive control | high | mitigate | Revert by reverse targeted edit, never by `git checkout`/`git restore` — a whole-file revert would also wipe the uncommitted `scripts/i18n-zh-lint.js` work in the same tree (`pattern_negative_control_checkout_wipes_uncommitted_fix`). Proven by a `git diff --stat -- plugins/` verify that must print 0. |
| T-q4j-02 | Tampering | concurrent session sharing `.git/index` and HEAD | high | mitigate | Commit path-scoped only — name `scripts/i18n-zh-lint.js` and `plugins/O-MicrotonalSampler` explicitly. Never `git add -A`, never `git commit -a`. Re-check `git branch --show-current` and `git status --short` immediately before each commit, not once at task start. |
| T-q4j-03 | Information disclosure | widened `maskLatin` masking a real violation | medium | mitigate | Violations C and D in Task 1 are the discriminators and must fire after the fix. The one residual blindness (Han + ASCII period + Latin, unspaced) is documented in the file rather than left silent. |
| T-q4j-04 | Denial of service | Z8 declared in `CODES` but never pushed in `lintRows` | high | mitigate | A missing push produces an all-zero column indistinguishable from a clean pass. Task 2 STEP 4's corpus positive control is the specific defence, on top of the fixture-level self-test. |
| T-q4j-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs. Every change is to committed repo files run under the system `node`; no dependency is added, resolved or fetched. |
</threat_model>

<verification>
Run from the repo root after all three tasks:

1. `node scripts/i18n-zh-lint.js --self-test` — ends in `SELF-TEST: 10/10`, with Z1 and Z8 both on the `FIRES on violation, silent on control` line.
2. `node scripts/i18n-zh-lint.js --verbose` — zero finding detail lines across 43 plugins, down from 8. The `REPORT ONLY — exit 0` banner is still printed.
3. The independent Han-space-Han scan prints 0 and equals the lint's Z8 column.
4. `node scripts/check-i18n.js` exits 0; `node scripts/i18n-fr-lint.js` exits 0.
5. `git status --short` shows only `scripts/i18n-zh-lint.js` and `plugins/O-MicrotonalSampler/Resources/ui/js/i18n.js` (plus the pre-existing `.gsd/dispatch-isolation-sentinel.json`).

Both RED observations must appear verbatim in the summary. A summary that reports only the green readings has not demonstrated that either control discriminates, and the task is not done:
- `SELF-TEST Z1 BROKEN: fires on a control` — observed before the maskLatin edit.
- `SELF-TEST Z8 BROKEN: silent on a violation` — observed before `ruleZ8` existed.
</verification>

<success_criteria>
- Repo-wide zh lint finding count goes 8 -> 0, and the 8 that disappear are the 8 named in the baseline table — no other finding was silenced.
- Self-test goes 9/9 -> 10/10, with the denominator rising because Z8 was added and proven, not because a rule was dropped.
- Both RED readings recorded verbatim.
- Z8's zero agrees with an independent scan AND the rule is proven non-inert on the corpus path.
- No zh-Hans, French or English rendering changed anywhere in `plugins/`.
- The lint is still report-only; Stage 4 owns the gate flip.
</success_criteria>

<output>
Create `.planning/quick/260904-q4j-fix-i18n-zh-lint-before-stage-4-masklati/260904-q4j-SUMMARY.md` when done.

The summary must carry, at minimum: the two verbatim RED lines; the before/after repo-wide finding counts; the Z8 column and independent-scan numbers side by side; the plugin, key and exact strings used for the Task 2 STEP 4 corpus positive control plus confirmation of its clean revert; the documented `maskLatin` blindness; and the note that the live count of `.scl`/`.kbm`-carrying plugins is 8, not the 12 the brief assumed.
</output>

