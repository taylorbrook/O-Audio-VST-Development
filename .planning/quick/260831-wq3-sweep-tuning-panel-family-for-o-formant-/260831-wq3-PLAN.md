---
phase: 260831-wq3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/O-MicrotonalSampler/Source/PluginEditor.cpp
  - plugins/O-MicrotonalSampler/Resources/ui/js/tuning-panel.js
  - plugins/O-MicrotonalSampler/CHANGELOG.md
  - plugins/O-MicrotonalSampler/CMakeLists.txt
  - PLUGINS.md
autonomous: true
requirements: [item-22-sweep, item-71]

estimate:
  tokens: 95000
  raw_tokens: 63000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O-MicrotonalSampler: a session saved with A4 at 442 Hz reopens with the panel's A4 REF readout showing 442.0 Hz, not 440.0 Hz."
    - "O-MicrotonalSampler: a second A4 drag continues from where the first ended instead of restarting at 440."
    - "O-Reed's tuning-tab disconnection is PROVEN by an exact native-fn set difference and recorded as found-not-fixed — not silently absorbed into the item-22 sweep."
    - "The five clean plugins (O-Bells, O-IntonationPad, O-Lyrica, O-Prism, O-Wind) carry re-verified, executor-observed file:line citations and no version bump."
    - "Exactly one plugin is bumped (O-MicrotonalSampler 1.25.1 -> 1.25.2) and exactly one PLUGINS.md row changes."
  artifacts:
    - plugins/O-MicrotonalSampler/Source/PluginEditor.cpp
    - plugins/O-MicrotonalSampler/Resources/ui/js/tuning-panel.js
    - plugins/O-MicrotonalSampler/CHANGELOG.md
    - plugins/O-MicrotonalSampler/CMakeLists.txt
    - PLUGINS.md
    - <scratch>/O-MicrotonalSampler/probe-a4.js
    - <scratch>/O-Reed/native-fn-inventory.txt
  key_links:
    - "editor native fn getMasterTune -> TuningEngine::getMasterTune() -> panel loadInitialState() -> updateRefPitchKnob -> #ref-pitch-value textContent"
    - "updateKnob writes currentHz -> mousedown reads startValue = currentHz -> second drag continues from the first"
    - "captureTuningValueTree (PluginProcessor.cpp:2844-2845) / restoreTuningFromValueTree (:2876-2879) already carry the STATE half — the panel read is the only missing link"
---

<objective>
Sweep the tuning-panel family for the O-Formant item 22 gap (SUMMARY item 71). The survey
already established the matrix by grep: six of seven plugins are clean or a different
defect; exactly one — O-MicrotonalSampler — has the PANEL half of the gap and nothing else.

Purpose: the A4 REF knob on O-MicrotonalSampler's tuning panel has no read path. The state
half is already correct (`captureTuningValueTree` saves the engine's `masterTune`), so a
reopened session restores 442 Hz in the engine and paints `440.0 Hz` on the knob — the UI
lies about the pitch the plugin is actually sounding. Each drag also restarts from a literal
`440`, so a second drag throws the first away.

Output: O-MicrotonalSampler v1.25.2 (editor `getMasterTune` native fn + panel read +
`currentHz` drag continuity), a page-level probe that fails before and passes after with a
control arm, a proven-and-reported O-Reed finding with no code change, re-verified citations
for the five clean plugins, and one PLUGINS.md row commit.
</objective>

<context>
@.planning/quick/260831-wq3-sweep-tuning-panel-family-for-o-formant-/260831-wq3-SURVEY.md
@CLAUDE.md
@plugins/O-MicrotonalSampler/CHANGELOG.md

Reference fix (read both hunks before editing anything):
`git show 420cfe49 -- plugins/O-Formant/Source/PluginEditor.cpp plugins/O-Formant/Source/ui/public/js/tuning-panel.js`

CHANGELOG prose model: `plugins/O-Formant/CHANGELOG.md` lines 20-45.
Shared-module panel shape (READ ONLY, never edit): `modules/tuning/scala-tuning-engine/js/tuning-panel.js:943-986`.
</context>

<house_rules>
These bind every task. A violation is a stop, not a warning.

- **Commits are path-scoped.** `git commit -- <paths>` only. Never `git add -A`, never
  `git commit -a`, never `--amend`. No push. No tag.
- **Re-check location and staging IMMEDIATELY before each commit** (concurrent sessions
  share `.git/index` and HEAD — a session-start snapshot is minutes stale):
  ```bash
  git branch --show-current   # expect: main
  git status --short
  ```
  Then commit, then `git show --stat HEAD` and confirm the file list is exactly what you
  intended. `git commit -- <paths>` takes only TRACKED files — an untracked new file is
  silently omitted.
- **Never edit `scripts/` or `modules/`.** Both are read-only inputs here. The vendored
  copy under `plugins/O-MicrotonalSampler/Resources/ui/js/` is the file that changes.
- **Never edit `PLUGINS.md` per plugin.** One commit at the end (Task 3).
- **Builds take the mutex** `/tmp/claude-501/stagek-build.lock`:
  ```bash
  until mkdir /tmp/claude-501/stagek-build.lock 2>/dev/null; do sleep 20; done
  # ... touch CMakeLists.txt, build, install, auval ...
  rmdir /tmp/claude-501/stagek-build.lock
  ```
  Release it after `auval`, including on failure. `touch` the plugin's CMakeLists.txt
  before building (the version is baked at configure time). Read the **INSTALLED**
  `Info.plist` for the version, never the build tree's.
- **Never revert to measure.** No `git stash`, no `git checkout -- <file>`, no
  `git restore` to produce the "before" arm. Run the probe FIRST, record the failure,
  then edit. A revert here wipes the uncommitted fix.
- **Scratchpad root** (referred to below as `<scratch>`):
  `/private/tmp/claude-501/-Users-taylorbrook-Dev-VST-development/8bc00eca-b311-4564-94c9-500c2cb40695/scratchpad`
- Do not edit `.planning/ROADMAP.md`.
</house_rules>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: O-MicrotonalSampler v1.25.2 — port the PANEL half of 420cfe49</name>

  <files>
    plugins/O-MicrotonalSampler/Source/PluginEditor.cpp
    plugins/O-MicrotonalSampler/Resources/ui/js/tuning-panel.js
    plugins/O-MicrotonalSampler/CHANGELOG.md
    plugins/O-MicrotonalSampler/CMakeLists.txt
    &lt;scratch&gt;/O-MicrotonalSampler/probe-a4.js
  </files>

  <precondition>
    `plugins/O-MicrotonalSampler/Source/PluginProcessor.cpp` still saves and restores the
    engine's `masterTune` (grep `setProperty ("masterTune"` and `hasProperty ("masterTune")`
    — survey cites :2844-2845 and :2876-2879). If either is gone, the STATE half is also
    missing and this task's scope is wrong — stop and report.
  </precondition>

  <behavior>
    Three assertions, driven by a page-level probe that mounts the vendored panel module
    against a hand-written `juce` stub. Written and RUN BEFORE any source edit.

    - **Arm A (the fix, must fail first):** stub `getMasterTune` resolves `442`. After
      `await panel.init()`, `#ref-pitch-value`.textContent === `"442.0 Hz"`.
      BEFORE: `"440.0 Hz"` (FAIL). AFTER: `"442.0 Hz"` (PASS).
    - **Arm B (control, must pass BOTH ways):** stub `getMasterTune` resolves `440`. Same
      assertion path, expected `"440.0 Hz"`. Passing before and after proves Arm A's
      assertion is discriminating and not always-red.
    - **Arm C (drag continuity, must fail first):** with `getMasterTune` = 442, dispatch
      `mousedown` on `#ref-pitch-knob` at `clientY: 300`, `mousemove` on `document` at
      `clientY: 280` (delta = (300-280)*0.5 = +10 Hz), `mouseup`; then repeat the identical
      pair. Assert `endOfDrag2 > endOfDrag1` strictly.
      BEFORE: 450.0 then 450.0 (equal — FAIL, `startValue` is the literal 440 both times).
      AFTER: 452.0 then 462.0 (PASS).
      The `mousemove` handler is `async` and awaits the stub's `setMasterTune`, so yield a
      tick (`await new Promise(r =&gt; setTimeout(r, 30))`) after each dispatch before reading.
  </behavior>

  <action>
Step 1 — write and run the probe BEFORE editing any source. Create
`&lt;scratch&gt;/O-MicrotonalSampler/probe-a4.js`, a CommonJS Node script:

  - `const S = require('/Users/taylorbrook/Dev/VST-development/scripts/serve-ui.js')`
  - `const built = S.buildRoot('O-MicrotonalSampler')` — assembles the served tree into a
    fresh `os.tmpdir()` mkdtemp directory (untracked; this is why no repo file is touched).
  - Write `probe.html` into `built.root`. It is an ES-module page that imports
    `TuningPanel` from `/js/tuning-panel.js`, builds a container div, and constructs
    `new TuningPanel(container, juceStub)` where `juceStub.getNativeFunction(name)` returns
    an async fn resolving: `getMasterTune` -&gt; the value from `location.search`'s `hz` param;
    `getOctaveStretch` -&gt; `1.0` (a number — `loadInitialState` calls `.toFixed(2)` on it);
    `getTuningIntervals` -&gt; the 12-TET cents JSON string; `getTuningName` -&gt;
    `"12-TET Standard"`; `getTonicNote` -&gt; `0`; `getEmbeddedTuningList` -&gt; `"[]"`;
    `setMasterTune` -&gt; pushes the arg onto `window.__setCalls` and resolves `true`;
    anything else -&gt; `null`. Expose `window.__panel` and a `window.__ready` promise.
  - `const { port, close } = await S.serve(built.root)`, then
    `const { chromium } = S.resolvePlaywright()` (it resolves from the npx cache on this
    machine — if it returns `null`, stop and report rather than skipping the probe;
    a skipped probe is not a pass).
  - Load `http://127.0.0.1:${port}/probe.html?hz=442` for arms A and C and `?hz=440` for
    arm B. Print one line per arm: `ARM A: PASS|FAIL expected=... actual=...`. Exit
    non-zero if any arm fails.

  Do NOT drive the plugin's real page for this — the tuning panel mounts lazily behind a
  tab and `tests/ui-stub/generic-overrides.json` already pins `getMasterTune: 440.0`;
  editing that tracked file to 442 would move the check-ui-labels baseline. The direct
  module mount gives total control and touches nothing tracked.

  Run it. Record verbatim: expect **ARM A FAIL** (`440.0 Hz`), **ARM B PASS**,
  **ARM C FAIL** (450.0 == 450.0). If Arm A passes before the edit, the premise is wrong —
  stop and report.

Step 2 — `Source/PluginEditor.cpp`. This editor registers native fns as a TABLE of
`{ "name", lambda }` pairs, NOT `.withNativeFunction` chaining. Add a `getMasterTune` entry
immediately after the `getOctaveStretch` entry (survey cites :824), matching the
neighbouring entries' shape exactly: capture `[this]`, take
`(const juce::Array&lt;juce::var&gt;&amp;, std::function&lt;void(juce::var)&gt; complete)`, fetch
`auto* engine = processorRef.getTuningEngine();` and
`complete (juce::var (engine != nullptr ? engine-&gt;getMasterTune() : 440.0));`.
Add a short comment citing v1.25.2 and the reason (the knob had no read path, so a reopened
session showed 440.0 over an engine restored at 442). Do NOT touch the existing
`setMasterTune` entry (survey cites :1601-1615) — it already writes the engine under the
persistence lock and is correct.

Step 3 — `Resources/ui/js/tuning-panel.js`, three edits mirroring 420cfe49's hunks:

  (a) In `loadInitialState()`, after the octave-stretch block and BEFORE the
      `updateIntervalList()` / `updateVisualization()` calls, add an A4 read in its **own
      try/catch** so a failure cannot skip the interval list: `await` the
      `getMasterTune` native fn, and only when the result is a finite number AND
      `this.updateRefPitchKnob` exists, call it with the value clamped to [400, 480]. Log
      the failure with `console.error` on the catch path. The guard matters because the
      ui-stub invents a value for any native fn it does not know; a non-number must leave
      the knob at its default.
  (b) In `setupRefPitchKnob()`, declare `let currentHz = 440;` beside the existing
      `startValue`, assign `currentHz = hz;` as the first statement of `updateKnob`, and
      publish `this.updateRefPitchKnob = updateKnob;` immediately after `updateKnob` is
      defined. `attachEventListeners()` (which calls `setupRefPitchKnob`) runs before
      `loadInitialState()` in `init()`, so the hook is always present by then.
  (c) In the `mousedown` handler, add `startValue = currentHz;` so each drag starts from
      the value the knob currently shows.

  Do not rename `startValue`, do not restructure the handlers, do not touch any other
  method. No user-visible English or French copy changes here, so no i18n edit is needed.

Step 4 — re-run the probe. All three arms must PASS. Record the numbers.

Step 5 — `CMakeLists.txt` line 19: `VERSION "1.25.1"` -&gt; `VERSION "1.25.2"`. That token
must stay spelled `VERSION` (a `PLUGIN_VERSION` keyword is silently ignored by
`juce_add_plugin` and the bundle ships as 1.0.0).

Step 6 — `CHANGELOG.md`: a new `## [1.25.2] - 2026-08-31` section at the top, below the
title and above `## [1.25.1]`. Prose in the O-Formant model (CHANGELOG.md:20-45): name the
defect, name the two code paths, state the probe's before/after numbers including the
control arm, and state explicitly that the STATE half was already correct
(`captureTuningValueTree` / `restoreTuningFromValueTree`) so only the panel read was missing.

Step 7 — gates, in order. Do not proceed past a failure.
Step 8 — commit, path-scoped, after the location and staging re-check.
  </action>

  <verify>
    <automated>
node &lt;scratch&gt;/O-MicrotonalSampler/probe-a4.js
# all three arms PASS; arms A and C were recorded FAIL in step 1 of the same session
    </automated>
    <automated>
cd /Users/taylorbrook/Dev/VST-development
node scripts/check-i18n.js --plugin O-MicrotonalSampler
node scripts/check-ui-labels.js --plugin O-MicrotonalSampler   # 0 moved vs baseline
node scripts/boot-all-uis.js                                    # 43/43, 0 DEAD, O-MicrotonalSampler late count unchanged
    </automated>
    <automated>
cd /Users/taylorbrook/Dev/VST-development
until mkdir /tmp/claude-501/stagek-build.lock 2>/dev/null; do sleep 20; done
touch plugins/O-MicrotonalSampler/CMakeLists.txt
./scripts/build-and-install.sh O-MicrotonalSampler
auval -v aumu OMtS OuDv
rmdir /tmp/claude-501/stagek-build.lock
/usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" \
  ~/Library/Audio/Plug-Ins/Components/O-MicrotonalSampler*.component/Contents/Info.plist
# expect 1.25.2
    </automated>
    <automated>
cd /Users/taylorbrook/Dev/VST-development
# the shipped bundle must actually carry the two new symbols
grep -c "getMasterTune" plugins/O-MicrotonalSampler/Source/PluginEditor.cpp   # >= 2 (get + set)
grep -c "currentHz"      plugins/O-MicrotonalSampler/Resources/ui/js/tuning-panel.js  # >= 4
    </automated>
    <automated>
cd /Users/taylorbrook/Dev/VST-development
git branch --show-current   # main
git status --short
git commit -- plugins/O-MicrotonalSampler
git show --stat HEAD        # exactly 4 files, all under plugins/O-MicrotonalSampler/
    </automated>
  </verify>

  <done>
    `#ref-pitch-value` reads `442.0 Hz` after load against a stub returning 442 (was
    `440.0 Hz`); the control arm reads `440.0 Hz` both before and after; the second drag
    ends strictly above the first (was equal). check-i18n PASS, check-ui-labels 0 moved,
    boot-all-uis 43/43 with 0 DEAD and O-MicrotonalSampler's late count unchanged, build
    installed, `auval -v aumu OMtS OuDv` PASS, installed plist reads 1.25.2. One commit,
    four files, all under `plugins/O-MicrotonalSampler/`. `scripts/`, `modules/` and
    `PLUGINS.md` untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: O-Reed — prove the larger gap, report it, change nothing</name>

  <files>
    &lt;scratch&gt;/O-Reed/native-fn-inventory.txt
    (no file under plugins/O-Reed is modified)
  </files>

  <action>
The survey found O-Reed carries a DIFFERENT and larger defect than item 22: `index.html`
mounts the SHARED module panel with `Juce`, but the editor registers only preset and
language native fns. The panel's `loadInitialState()` throws on its FIRST call
(`getTuningIntervals`) and never reaches any A4 read, so porting `getMasterTune` alone
would be decorative. Prove it, report it, do not fix it and do not bump.

  1. **Editor inventory — search the WHOLE Source tree, not just PluginEditor.cpp.** A
     second registration site elsewhere would falsify the finding:
     ```bash
     grep -rhoE 'withNativeFunction\s*\(\s*"[A-Za-z]+"' plugins/O-Reed/Source/ \
       | sed -E 's/.*"([A-Za-z]+)"/\1/' | sort -u > <scratch>/O-Reed/registered.txt
     ```
  2. **Panel inventory** (read-only on `modules/`):
     ```bash
     grep -ohE "getNativeFunction\('[A-Za-z]+'\)" modules/tuning/scala-tuning-engine/js/tuning-panel.js \
       | sed -E "s/.*'([A-Za-z]+)'.*/\1/" | sort -u > <scratch>/O-Reed/called.txt
     ```
  3. **Set difference** — the names the panel calls that the plugin does not register:
     ```bash
     comm -23 <scratch>/O-Reed/called.txt <scratch>/O-Reed/registered.txt \
       | tee <scratch>/O-Reed/missing.txt | wc -l
     ```
     Write all three lists plus the count into
     `<scratch>/O-Reed/native-fn-inventory.txt` with the date and the two source paths.
  4. **Confirm the mount** — that the shared panel really is constructed with `Juce` on
     this page (survey cites `index.html:1643`; record the line number you OBSERVE, and
     confirm the CMake embed at `plugins/O-Reed/CMakeLists.txt:75` and the resource branch
     at `plugins/O-Reed/Source/PluginEditor.cpp:410`).
  5. **Runtime arm:** `node scripts/boot-all-uis.js --plugin O-Reed` and capture the
     console-error / unknownNativeFns evidence that the panel's load aborts. Append the
     captured lines to the inventory file. If the run shows the load NOT aborting, the
     finding is wrong — say so plainly rather than forcing the conclusion.
  6. **The false claim:** quote `plugins/O-Reed/CHANGELOG.md` line 37's assertion that
     `referencePitch` "is driven by `#ref-pitch-knob` inside the shared panel" and record
     that it does not hold today.
  7. Record in the SUMMARY as **found, not fixed**, with the recommended fix shape: register
     the full set from step 3 on the editor, modelled on `plugins/O-Wind/Source/PluginEditor.cpp`
     lines 300-460; the minimal item-22 pair (`getMasterTune` reading the engine +
     `setMasterTune` routed through the `referencePitch` param, O-Wind WR-11 shape at
     `plugins/O-Wind/Source/PluginEditor.cpp:337-352`) is NOT sufficient on its own because
     the load aborts before either is reached.

No edit, no version bump, no CHANGELOG entry, no commit for O-Reed.
  </action>

  <verify>
    <automated>
cd /Users/taylorbrook/Dev/VST-development
test -s &lt;scratch&gt;/O-Reed/missing.txt
grep -qx "getTuningIntervals" &lt;scratch&gt;/O-Reed/missing.txt
grep -qx "getMasterTune"      &lt;scratch&gt;/O-Reed/missing.txt
grep -qx "setMasterTune"      &lt;scratch&gt;/O-Reed/missing.txt
test -s &lt;scratch&gt;/O-Reed/native-fn-inventory.txt
# and O-Reed must be untouched:
git status --short -- plugins/O-Reed   # empty output
    </automated>
  </verify>

  <done>
    `<scratch>/O-Reed/native-fn-inventory.txt` holds the registered set, the called set,
    the exact set difference (every tuning native, `getTuningIntervals` first) and the
    boot-all-uis runtime evidence. `git status --short -- plugins/O-Reed` is empty. The
    SUMMARY carries the finding as found-not-fixed with the O-Wind-shaped recommendation
    and the note that the minimal pair would be decorative.
  </done>
</task>

<task type="auto">
  <name>Task 3: re-verify the five clean plugins, then one PLUGINS.md commit</name>

  <files>
    PLUGINS.md
  </files>

  <precondition>
    Task 1 committed successfully. If O-MicrotonalSampler was not bumped, there is no row
    to change — skip the PLUGINS.md commit entirely rather than committing an empty or
    speculative edit.
  </precondition>

  <action>
**Part A — re-verify, do not trust.** The survey's citations were taken by grep at
2026-08-31; line numbers drift. Re-run each and record the line number you OBSERVE (not the
survey's) in the SUMMARY. A citation that no longer resolves is a finding, not a typo —
report it rather than quietly substituting a nearby line.

  - **O-Bells** (both halves already present): `setMasterTune` -&gt; `tuning_masterTune`
    param and `getMasterTune` in `plugins/O-Bells/Source/PluginEditor.cpp` (survey: 501-511
    and 497); the vendored panel's `getMasterTune` load read and `currentHz` in
    `plugins/O-Bells/Resources/ui/js/tuning-panel.js` (survey: 1012, 993, 1022); the
    processor listener forwarding to the engine at `PluginProcessor.cpp:1626`.
  - **O-IntonationPad** (SliderState shape — no native fn needed):
    `getSliderState('tuning_masterTune')` in
    `plugins/O-IntonationPad/Source/ui/public/js/tuning-panel.js` (survey: 85, 1088-1103)
    and `tuning_octaveStretch` at :84.
  - **O-Lyrica** (own param-bound knob): `bindSlider('masterTune')` in
    `plugins/O-Lyrica/Resources/ui/js/app.js` (survey: 141); param-&gt;engine push in
    `PluginProcessor.cpp` (survey: 773 and 781).
  - **O-Prism** (own param-bound knob): `bindKnob('masterTune'` and `bindKnob('octaveStretch'`
    in `plugins/O-Prism/Source/ui/public/index.html` (survey: 2922-2923).
  - **O-Wind** (shared module panel, both halves): `getMasterTune` / `setMasterTune` in
    `plugins/O-Wind/Source/PluginEditor.cpp` (survey: 337, 341-352); engine
    `octaveStretch` save/restore in `PluginProcessor.cpp` (survey: 487, 537).

  None of these five is edited. None is bumped. `git status --short -- plugins/O-Bells
  plugins/O-IntonationPad plugins/O-Lyrica plugins/O-Prism plugins/O-Wind` must be empty
  at the end of this task.

**Part B — the single PLUGINS.md commit.** Only O-MicrotonalSampler's row changes: version
`1.25.1` -&gt; `1.25.2`, date `2026-08-31` (survey observed the row at line 59 — locate it by
`grep -n`, not by line number). Edit that one row and nothing else. Then run the duplicate
check the union-merge driver makes necessary and confirm it prints nothing:

```bash
grep "^| O-" PLUGINS.md | awk -F'|' '{print $2}' | sort | uniq -d
```

Re-check location and staging, then `git commit -- PLUGINS.md`. Confirm with
`git show --stat HEAD` that the commit contains exactly one file.
  </action>

  <verify>
    <automated>
cd /Users/taylorbrook/Dev/VST-development
grep -q "^| O-MicrotonalSampler | .* | 1\.25\.2 | .* | 2026-08-31 |$" PLUGINS.md
test -z "$(grep '^| O-' PLUGINS.md | awk -F'|' '{print $2}' | sort | uniq -d)"
test -z "$(git status --short -- plugins/O-Bells plugins/O-IntonationPad plugins/O-Lyrica plugins/O-Prism plugins/O-Wind plugins/O-Reed)"
    </automated>
    <automated>
cd /Users/taylorbrook/Dev/VST-development
git branch --show-current   # main
git status --short
git commit -- PLUGINS.md
git show --stat HEAD        # exactly 1 file: PLUGINS.md
    </automated>
  </verify>

  <done>
    All five clean plugins' citations re-verified by grep with executor-observed line
    numbers recorded in the SUMMARY, and all five (plus O-Reed) show an empty
    `git status --short`. PLUGINS.md carries exactly one changed row
    (O-MicrotonalSampler 1.25.2, 2026-08-31), the duplicate check prints nothing, and the
    commit contains exactly one file. Total commits for this quick task: two.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView JS -> C++ native fn | The panel passes a drag-derived Hz across `setMasterTune`; the new `getMasterTune` returns engine state back. Both are same-process, developer-authored, no untrusted input. |
| Saved session state -> processor | `restoreTuningFromValueTree` reads a `masterTune` that round-trips XML as a string var. Pre-existing, unchanged by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-wq3-01 | Tampering | `TuningPanel.loadInitialState` A4 read | low | mitigate | Value is accepted only when `Number.isFinite`, then clamped to the knob's own [400, 480] domain — a hostile or stub-invented value cannot drive the indicator rotation out of range. |
| T-wq3-02 | Denial of Service | `TuningPanel.loadInitialState` | medium | mitigate | The A4 read sits in its OWN try/catch so a throwing native fn cannot skip `updateIntervalList()` / `updateVisualization()` and blank the tuning tab — the exact failure mode O-Reed exhibits today. |
| T-wq3-03 | Information Disclosure | probe harness | low | accept | The probe serves an assembled UI tree on an ephemeral localhost port from `os.tmpdir()`; it is torn down in the same process and ships nothing. |
| T-wq3-SC | Tampering | package installs | n/a | accept | No npm/pip/cargo install in this plan. Playwright is resolved from the existing npx cache by `scripts/serve-ui.js`; nothing is installed. |
</threat_model>

<verification>
1. The probe's three arms were run BEFORE any source edit and A and C were recorded FAIL;
   re-run after the edit, all three PASS. No `git stash` / `git checkout --` / `git restore`
   was used to produce the "before" arm.
2. `auval -v aumu OMtS OuDv` PASS and the INSTALLED `Info.plist` reads `1.25.2`.
3. `boot-all-uis` 43/43, 0 DEAD, O-MicrotonalSampler's late count unchanged from its
   pre-edit value (record both numbers).
4. `check-i18n --plugin O-MicrotonalSampler` PASS; `check-ui-labels --plugin` 0 moved
   (expected: the stub pins `getMasterTune: 440.0`, so the readout still renders
   `440.0 Hz` and no geometry shifts).
5. `git log --oneline -2` shows exactly two new commits — one `plugins/O-MicrotonalSampler`
   (4 files), one `PLUGINS.md` (1 file). No tag, no push, no `--amend`.
6. `git status --short -- plugins scripts modules` shows no stray modification under
   `scripts/`, `modules/`, or any of the six unbumped plugins.
</verification>

<success_criteria>
- O-MicrotonalSampler v1.25.2 shipped: editor `getMasterTune` native fn, panel load-time
  read, `currentHz` drag continuity, CHANGELOG entry, CMakeLists VERSION bump.
- A page-level probe demonstrably failed before and passed after, with a control arm that
  passed both ways.
- O-Reed's tuning-tab disconnection proven by an exact native-fn set difference plus a
  runtime arm, recorded as found-not-fixed with the O-Wind-shaped recommendation. No edit.
- O-Bells, O-IntonationPad, O-Lyrica, O-Prism, O-Wind re-verified clean with observed
  file:line citations. No edits, no bumps.
- Exactly two commits, both path-scoped, on `main`. PLUGINS.md carries one changed row and
  no duplicates.
</success_criteria>

<output>
Write the SUMMARY to
`.planning/quick/260831-wq3-sweep-tuning-panel-family-for-o-formant-/260831-wq3-SUMMARY.md`
covering: the per-plugin verdict table with executor-observed citations; the probe's
before/after numbers for all three arms; the gate results; the O-Reed finding with its set
difference and recommended fix shape; and the two commit SHAs.
</output>
