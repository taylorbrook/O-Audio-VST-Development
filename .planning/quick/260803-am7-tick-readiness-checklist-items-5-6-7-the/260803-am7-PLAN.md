---
phase: quick-260803-am7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PUBLIC-RELEASE-READINESS.md
autonomous: true
requirements: [AM7-01, AM7-02, AM7-03]
must_haves:
  truths:
    - "Section 6 checklist steps 5, 6, and 7 read as done, in the same visual style as the nine already-ticked steps."
    - "A reader who lands on section 2.3, 2.4, or 3.4 sees a resolved heading AND a body note that does not contradict it."
    - "The build-release/ gitignore gap is recorded as a named residual under step 6 and in section 2.4 — not buried under a checkmark."
    - "Section 2.3 cites .gitignore line 9, the line the entry actually occupies."
    - "No file other than PUBLIC-RELEASE-READINESS.md is modified."
  artifacts:
    - PUBLIC-RELEASE-READINESS.md
  key_links:
    - "Each tick cites commit ecf3fa39, so the claim stays checkable against git rather than merely asserted."
    - "Each resolved heading has a matching in-body resolved-note; heading and body move together."
---

<objective>
Tick readiness checklist steps 5, 6, and 7 in `PUBLIC-RELEASE-READINESS.md`, and bring the three sections they cite (2.3, 2.4, 3.4) into agreement with those ticks — while recording, rather than hiding, the one thing that did not actually get fixed.

Purpose: the untracking work landed in commit `ecf3fa39` (42 files, 5897 deletions, 0 insertions) but the boxes were never checked, so the document currently understates its own progress. A naive tick would be worse than no tick: `build-release/` was untracked but is still NOT covered by `.gitignore`, it is sitting on disk as an untracked directory right now, and a future `git add -A` would re-commit the whole tree including the compiled `O-Bowed_vst3_helper` binary. That gap must survive the checkmark.

Output: one modified file, `PUBLIC-RELEASE-READINESS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@PUBLIC-RELEASE-READINESS.md
</context>

<measured_facts>
Do NOT re-derive these. They were measured by the orchestrator against git and are authoritative.

- Commit `ecf3fa39` — "chore: untrack gitignored artifacts before public flip — system-config.json, build-release/, logs/ (readiness steps 5-7)", committed **2026-08-03**. Diff: **42 files changed, 5897 deletions, 0 insertions**, all `git rm --cached` untrackings.
  - `.claude/system-config.json` — 1 file (step 5)
  - `build-release/**` — 10 files, including the compiled `build-release/plugins/O-Bowed/O-Bowed_vst3_helper` (step 6)
  - `logs/**/build_*.log` — 31 files (step 7)
  - 1 + 10 + 31 = 42, matching the diff exactly.
- `git ls-files` now returns zero tracked files for each of the three paths. The untracking is real and complete.
- `ecf3fa39` did not touch `.gitignore` (0 changed).
- The counts already written into the document are **CORRECT** and must not be "fixed": §2.4's "Ten tracked files" and "seven files matching `JUCE/*.cmake`", and §3.4's "31 files".
- `.claude/system-config.json` is ignored by `.gitignore:9` — the document's §2.3 claim of "line 1" is wrong and is the one factual correction in scope.
- `logs/**/*.log` is ignored by `.gitignore:196:*.log`.
- **`build-release/` is NOT ignored.** `git check-ignore -v build-release/CPackConfig.cmake` returns nothing, and `git status --porcelain` currently shows `?? build-release/`.
- Section 1 "Verdict" (lines 7–20) does not reference system-config, build-release, or the logs; a grep over that range returns no match. It needs no edit — Task 3 re-confirms this rather than assuming it.
- Section 6 currently holds **9** ticked steps; after this work it holds **12**.
</measured_facts>

<constraints>
- `PUBLIC-RELEASE-READINESS.md` is the ONLY file that may be modified. Do not touch `.gitignore`, source, CI, or scripts.
- **The `.gitignore` scope decision is LOCKED**: the developer asked for boxes ticked, not for `.gitignore` edited. §2.4 line 110 already says the document does not edit `.gitignore`. Record the gap; do not close it.
- Use `Edit` for every change. Never `Write` this file — a whole-file rewrite of a 395-line audit document risks silent loss outside the diff window.
- Match the existing tick convention exactly, copied from lines 363–374. Do not invent a new one.
</constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Tick checklist steps 5, 6, and 7 in section 6</name>
  <files>PUBLIC-RELEASE-READINESS.md</files>
  <read_first>
    Read lines 363–374 of `PUBLIC-RELEASE-READINESS.md` before editing. Those nine lines are the style source: checkbox, full-line strikethrough that closes after the sentence's trailing clause (see step 8 for the pattern where prose continues past the bolded title), the ✅, bold `**Done YYYY-MM-DD**`, an em-dash, prose, and a trailing `*(Section X.Y — [TAG].)*` citation. Copy that convention character-for-character.
  </read_first>
  <action>
    Three `Edit` calls, one per line (currently lines 369, 370, 371). Each replaces the unticked line with a ticked one. Keep each step's existing title text and its existing trailing section/tag citation verbatim — only the checkbox, the strikethrough markers, and the inserted ✅/Done/prose are new.

    Step 5 becomes: checkbox `x`, the existing title and its `with \`git rm --cached\`.` clause wrapped in strikethrough, then `✅ **Done 2026-08-03**` — em-dash — prose reading: untracked in `ecf3fa39` (1 file, part of a 42-file, 5897-deletion untracking commit); `git ls-files` now returns nothing for the path and the file remains on disk. `.gitignore:9` keeps it from being re-added. Then the existing `*(Section 2.3 — [S3].)*` citation.

    Step 6 becomes: checkbox `x`, the existing title and its `including the compiled \`O-Bowed_vst3_helper\`.` clause wrapped in strikethrough, then `✅ **Done 2026-08-03**` — em-dash — prose reading: all 10 tracked files untracked in `ecf3fa39`, the compiled helper binary among them; `git ls-files build-release` now returns nothing. Then the existing `*(Section 2.4 — [E3].)*` citation. Then, still on the same line, a bold residual sentence in the shape step 3b uses for its carry-forward condition: state that `build-release/` is still not covered by `.gitignore`, that the directory currently sits on disk untracked, and that a future `git add -A` would therefore re-commit the whole tree, compiled binary included. Name adding the rule as the open follow-up. Do not present it as done, and do not soften it — this residual is the reason the task exists.

    Step 7 becomes: checkbox `x`, the existing title and its `under \`logs/\`.` clause wrapped in strikethrough, then `✅ **Done 2026-08-03**` — em-dash — prose reading: 31 `logs/**/build_*.log` files untracked in `ecf3fa39`; `.gitignore:196` (`*.log`) already covers them, so unlike `build-release/` this one cannot silently return. Then the existing `*(Section 3.4 — [S6].)*` citation.

    Leave steps 4, 11, 12, 13, and 14 untouched — they are genuinely open.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development &amp;&amp; test "$(grep -c '^- \[x\]' PUBLIC-RELEASE-READINESS.md)" = 12 &amp;&amp; test "$(grep -cE '^- \[ \] \*\*(5|6|7)\.' PUBLIC-RELEASE-READINESS.md)" = 0 &amp;&amp; test "$(grep -cE '^- \[ \]' PUBLIC-RELEASE-READINESS.md)" = 5 &amp;&amp; test "$(grep -cE '^- \[x\] ~~\*\*(5|6|7)\.' PUBLIC-RELEASE-READINESS.md)" = 3 &amp;&amp; grep -qE '^- \[x\] ~~\*\*6\..*ecf3fa39.*add -A' PUBLIC-RELEASE-READINESS.md &amp;&amp; echo TASK1_OK</automated>
  </verify>
  <done>Section 6 holds 12 ticked steps and 5 open ones (4, 11, 12, 13, 14). Steps 5–7 each carry ✅, a Done date of 2026-08-03, a citation of `ecf3fa39`, and their original section/tag citation. Step 6's line additionally names the unclosed `.gitignore` gap and the `git add -A` re-commit consequence.</done>
</task>

<task type="auto">
  <name>Task 2: Mark sections 2.3, 2.4, and 3.4 resolved — headings and bodies together</name>
  <files>PUBLIC-RELEASE-READINESS.md</files>
  <read_first>
    Read line 23 (`### 2.1 …`), line 35 (`### 2.2 …`), and line 342 (`### 5.4 …`). Those three show the resolved-heading convention. 2.1 and 2.2 are the dominant form: the scout tag stays where it is and the suffix follows it. Match that placement.
  </read_first>
  <action>
    Six `Edit` calls — one heading and one body note per section.

    **Headings.** Append ` — ✅ RESOLVED 2026-08-03` to the end of each of these three headings, after the existing scout tag, matching the 2.1/2.2 form exactly: `### 2.3 …[S3]`, `### 2.4 …[E3]`, `### 3.4 Tracked build logs [S6]`.

    **Body notes.** A resolved heading over an unchanged present-tense body is the failure mode to avoid — a reader hitting §2.4's "Ten tracked files live under `build-release/`" after a checkmark heading learns the wrong thing. So insert a short bolded resolution note into each section body. Place it immediately after the fenced `git rm` command block in §2.3 and §2.4 (before the `**Effort.**` line) and after the fenced block in §3.4. Leave the existing What/Why/What-is-needed prose and the fenced commands in place as the historical record — the note is what re-tenses them.

    §2.3's note: lead with bold `**Resolved 2026-08-03.**` then state that `ecf3fa39` untracked the file (1 of 42 files in that commit) and that `git ls-files` now returns nothing for the path while the file remains on disk. Separately, in the §2.3 "What." paragraph (line 87), correct the line-number claim: the entry is on `.gitignore` **line 9**, not line 1. This is the only factual correction in scope — the "Ten tracked files", "seven files matching `JUCE/*.cmake`", and "31 files" counts elsewhere are correct and stay as written.

    §2.4's note: lead with bold `**Resolved 2026-08-03 — with one residual.**` Record that `ecf3fa39` untracked all 10 files including the compiled `O-Bowed_vst3_helper`, and that `git ls-files build-release` now returns nothing. Then give the residual its own emphasis, in full — this section is where it belongs at length: `.gitignore` still has no `build-release/` rule, `git check-ignore` returns nothing for the tree, the directory is present on disk right now as an untracked `??` entry in `git status --porcelain`, and the practical consequence is that a single `git add -A` re-commits the whole tree with the compiled helper binary back into a now-public repository. Note that closing it is a one-line `.gitignore` addition that was deliberately left out of scope, consistent with this section's existing statement that the document does not edit `.gitignore`.

    §3.4's note: lead with bold `**Resolved 2026-08-03.**` State that `ecf3fa39` untracked all 31 matching files and that `.gitignore:196` (`*.log`) already covers the pattern, so — unlike `build-release/` — these cannot silently return to tracking.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development &amp;&amp; test "$(grep -c '✅ RESOLVED 2026-08-03' PUBLIC-RELEASE-READINESS.md)" = 3 &amp;&amp; grep -qE '^### 2\.3 .*\[S3\] — ✅ RESOLVED 2026-08-03$' PUBLIC-RELEASE-READINESS.md &amp;&amp; grep -qE '^### 2\.4 .*\[E3\] — ✅ RESOLVED 2026-08-03$' PUBLIC-RELEASE-READINESS.md &amp;&amp; grep -qE '^### 3\.4 Tracked build logs \[S6\] — ✅ RESOLVED 2026-08-03$' PUBLIC-RELEASE-READINESS.md &amp;&amp; test "$(grep -c 'Resolved 2026-08-03' PUBLIC-RELEASE-READINESS.md)" -ge 3 &amp;&amp; grep -q '`.gitignore` line 9 lists' PUBLIC-RELEASE-READINESS.md &amp;&amp; grep -q 'Ten tracked files' PUBLIC-RELEASE-READINESS.md &amp;&amp; grep -q '\*\*31 files\*\*' PUBLIC-RELEASE-READINESS.md &amp;&amp; echo TASK2_OK</automated>
  </verify>
  <done>All three headings carry the resolved suffix in the 2.1/2.2 form. Each of the three section bodies carries a bolded resolution note citing `ecf3fa39`. §2.4's note carries the `.gitignore`/`git add -A` residual in full. §2.3 cites line 9. The pre-existing 10/seven/31 counts are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Adjudicate section 1, then sweep the document for self-contradiction</name>
  <files>PUBLIC-RELEASE-READINESS.md</files>
  <action>
    Re-read section 1 "Verdict" (lines 7–20) and decide whether it needs an edit. The measured expectation is **no**: it enumerates only the two legal blockers (L1, L4) and the history-rewrite decision, all three already closed, and closes with "Everything else in this document is hardening, repository size, or a disclosure decision that is yours to make — not a blocker." It does not enumerate the three untracking items, so ticking them changes nothing there. Confirm that by grepping the range for `system-config`, `build-release`, and `logs` before concluding. If the grep returns a hit, edit only the sentence that hit; if it returns nothing, make no edit and say so in the summary. Do not add a new paragraph to section 1 — it is a verdict, not a changelog.

    Then sweep the whole document for contradictions introduced or newly exposed by Tasks 1–2:
    - No sentence outside a fenced command block or a "What it was"/"What." historical paragraph should still assert in the present tense that any of these three paths is tracked.
    - The three fenced `git rm` blocks stay — they are the historical remedy record, now covered by the resolution notes above them.
    - Section 6's closing note (lines 390–395) says "Steps 1 through 3 are the ones that actually block you." That remains true and needs no change.
    - Confirm nothing outside `PUBLIC-RELEASE-READINESS.md` was modified.

    Commit with message `docs: tick readiness steps 5-7 and mark sections 2.3/2.4/3.4 resolved` and a body recording `ecf3fa39` as the underlying work plus the open `build-release/` gitignore residual.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/VST-development &amp;&amp; test -z "$(git diff --name-only -- .gitignore)" &amp;&amp; test "$(git diff --name-only HEAD)" = "PUBLIC-RELEASE-READINESS.md" &amp;&amp; test "$(git diff --numstat HEAD -- PUBLIC-RELEASE-READINESS.md | cut -f2)" -le 8 &amp;&amp; test "$(grep -c '^- \[x\]' PUBLIC-RELEASE-READINESS.md)" = 12 &amp;&amp; test "$(grep -c '✅ RESOLVED 2026-08-03' PUBLIC-RELEASE-READINESS.md)" = 3 &amp;&amp; echo TASK3_OK</automated>
  </verify>
  <done>Section 1 has been read and adjudicated (expected outcome: unchanged, with the grep result stated in the summary). The working tree shows exactly one modified file. Deleted lines in the diff are bounded — this is an additive/in-place edit, not a rewrite. Ticks and resolved headings survive the sweep. Work is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private working tree → public repo `github.com/taylorbrook/O-Audio-VST-Development` | Anything committed and pushed crosses into permanent public visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-am7-01 | Information Disclosure | `build-release/` untracked-but-unignored on disk | high | mitigate | A checkmark that reads as "handled" would let the next `git add -A` re-publish 10 build files plus the compiled `O-Bowed_vst3_helper` to a public repo. Task 1 forces the gap into step 6's own tick text and Task 2 records it at length in §2.4, so the follow-up stays visible instead of closed-looking. |
| T-am7-02 | Tampering | `PUBLIC-RELEASE-READINESS.md` | medium | mitigate | Whole-file `Write` on a 395-line audit could silently drop findings outside the diff window. Constrained to `Edit` only; Task 3 bounds diff deletions at ≤ 8 lines and asserts single-file scope. |
| T-am7-03 | Repudiation | resolution claims in §2.3/§2.4/§3.4 | low | mitigate | Every claim cites `ecf3fa39` and a `git ls-files` result, so a reader can re-verify rather than trust the document. |
</threat_model>

<verification>
Run from the repository root after all three tasks:

```bash
grep -c '^- \[x\]' PUBLIC-RELEASE-READINESS.md          # 12
grep -cE '^- \[ \]' PUBLIC-RELEASE-READINESS.md          # 5  (steps 4, 11, 12, 13, 14)
grep -c '✅ RESOLVED 2026-08-03' PUBLIC-RELEASE-READINESS.md   # 3
grep -c 'ecf3fa39' PUBLIC-RELEASE-READINESS.md           # >= 6
git status --porcelain                                   # PUBLIC-RELEASE-READINESS.md only (plus the pre-existing ?? build-release/)
git diff --name-only -- .gitignore                       # empty
```

Read back lines 363–375 and confirm steps 5–7 are visually indistinguishable in style from steps 8–10.
</verification>

<success_criteria>
- Checklist steps 5, 6, 7 ticked in the exact existing convention, each dated 2026-08-03 and each citing `ecf3fa39`.
- Sections 2.3, 2.4, 3.4 carry resolved headings AND matching in-body resolution notes — no ✅ heading sits over an uncorrected present-tense body.
- The `build-release/` gitignore gap appears in two places (step 6 tick, §2.4 note) and is described as open, with the `git add -A` consequence spelled out.
- §2.3 cites `.gitignore` line 9.
- The pre-existing correct counts (10 files, seven `JUCE/*.cmake`, 31 logs) are untouched.
- Exactly one file modified; `.gitignore` untouched.
</success_criteria>

<output>
Create `.planning/quick/260803-am7-tick-readiness-checklist-items-5-6-7-the/260803-am7-SUMMARY.md` when done, and commit it in the same tree.
</output>
