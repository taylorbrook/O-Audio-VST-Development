---
phase: quick-260701-k0a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/resolve-target.sh
  - scripts/build-and-install.sh
  - .github/workflows/build-and-release.yml
  - scripts/juce-patches/note-expression-juce-8.0.9.patch
  - scripts/apply-juce-patches.sh
  - modules/registry.yaml
autonomous: true
requirements: [BUG-01, IMP-03, UPD-01, BUG-05, BUG-06]

must_haves:
  truths:
    - "scripts/resolve-target.sh resolves O-Texture -> OuariconTexture (never the literal PROJECT_NAME variable), O-Chorus -> OuariconChorus, O-Gain -> O-Gain"
    - "The CI workflow no longer contains any naive same-line juce_add_plugin target parse; it calls the shared resolver instead"
    - "build-and-install.sh sources the shared resolver, runs clean under set -euo pipefail, and execute() returns the real left-hand command exit status"
    - "apply-juce-patches.sh points at note-expression-juce-8.0.9.patch and a partial prior apply is skipped (not re-applied or reversed), verified by a post-apply marker recount"
    - "No CI run is triggered: no pushes, tags, or workflow_dispatch; all verification is static + local"
  artifacts:
    - scripts/resolve-target.sh
    - scripts/juce-patches/note-expression-juce-8.0.9.patch
  key_links:
    - "CI steps at yml:87, :439 (cmake-target) and :235, :453, :566 (product-name) invoke scripts/resolve-target.sh"
    - "build-and-install.sh sources scripts/resolve-target.sh and resolve_cmake_target() delegates to it"
    - "apply-juce-patches.sh PATCH_FILE -> renamed 8.0.9 patch; modules/registry.yaml:288 reference matches the new filename"
---

<objective>
Fix four build-script/CI defects surfaced by the 260701-in8 system review:
- BUG-01 / IMP-03: CI target + PRODUCT_NAME resolution uses naive `sed` that cannot resolve `juce_add_plugin(${PROJECT_NAME})` (breaks O-Texture releases). Factor the robust resolver out of `scripts/build-and-install.sh` into a shared `scripts/resolve-target.sh` sourced by both the local script and the CI workflow.
- UPD-01: rename `note-expression-juce-8.0.4.patch` -> `...-8.0.9.patch` (content is an 8.0.9 diff) and update its two script references + the registry reference.
- BUG-05: add a `patch --forward --dry-run` preflight + a `--forward` real apply so a partial prior patch application is not re-applied or reversed.
- BUG-06: harden `build-and-install.sh` with `set -uo pipefail` and fix the `eval | tee` exit-masking path.

Purpose: eliminate the local/CI drift that will break a real O-Texture GitHub release, and remove three latent script-hygiene hazards.
Output: one new shared script, four edited files, one renamed patch file. No CI run is triggered.

Evidence: `.planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md` (BUG-01, IMP-03, UPD-01, BUG-05, BUG-06).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md
@scripts/build-and-install.sh
@scripts/apply-juce-patches.sh
@.github/workflows/build-and-release.yml

# Verified resolver behavior (confirmed during planning against real CMakeLists):
#   O-Texture:  set(PROJECT_NAME OuariconTexture) ; juce_add_plugin(${PROJECT_NAME}  -> target OuariconTexture ; PRODUCT_NAME "O-Texture${OUARICON_DEV_SUFFIX}" -> O-Texture
#   O-Chorus:   juce_add_plugin(OuariconChorus                                        -> target OuariconChorus  (target != folder)
#   O-Gain:     juce_add_plugin(O-Gain                                                -> target O-Gain          (target == folder, literal)
# Apple `patch 2.0-12u11-Apple` supports --forward and --dry-run; on an already-applied hunk --forward prints
#   "Ignoring previously applied (or reversed) patch." and exits 1, so the real apply needs `|| true` + an authoritative marker recount.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared resolve-target.sh, wire build-and-install.sh, apply BUG-06 hardening</name>
  <files>scripts/resolve-target.sh (new), scripts/build-and-install.sh</files>
  <action>
Create `scripts/resolve-target.sh` — a bash library that is BOTH sourceable and directly executable. It must assume CWD = repo root (same contract as build-and-install.sh) and reference `plugins/<folder>/CMakeLists.txt` / `build/plugins/<folder>` with relative paths.

Define two pure functions that echo their single result to stdout and emit only warnings to stderr (never pollute stdout):

1. `resolve_cmake_target_for` (arg $1 = plugin folder). Port the exact resolution logic currently in `scripts/build-and-install.sh` lines 186-238: the awk first-arg extraction that handles the target on the same line as `juce_add_plugin(` OR on the next line; the CMake-variable branch that greps `set(<VAR> ...)` and falls back to `project(<name>)` when VAR is PROJECT_NAME; the `*_artefacts`-dir fallback under `build/plugins/<folder>`; the last-resort folder-name fallback. Parameterize on the `$1` folder arg instead of the `PLUGIN_NAME` global. On the last-resort folder-name fallback, print the warning to STDERR (so `$(...)` capture stays clean) and still echo the folder name. Echo the final resolved target with `printf '%s\n'`.

2. `resolve_product_name_for` (arg $1 = plugin folder). Replicate CI's current source-parse: grep the first `PRODUCT_NAME "..."` line from the plugin CMakeLists, extract the quoted value, strip any CMake-variable `${...}` substrings, and default to the folder name when empty. Echo with `printf '%s\n'`. (This mirrors the CI PRODUCT_NAME parse at yml:235/453/566; it intentionally does NOT change build-and-install.sh Phase 3, which reads PRODUCT_NAME from built artefacts and is strictly more robust — leave Phase 3 untouched.)

Add a CLI dispatch block guarded by `if [ "${BASH_SOURCE[0]}" = "${0}" ]; then ... fi` supporting two subcommands: `cmake-target <folder>` calls resolve_cmake_target_for; `product-name <folder>` calls resolve_product_name_for; anything else prints a usage line to stderr and exits 1. The guard ensures sourcing does not run the dispatch. Run `chmod +x scripts/resolve-target.sh`.

Then edit `scripts/build-and-install.sh`:
- BUG-06a: change line 2 from `set -e` to `set -euo pipefail`.
- Source the shared library once near the top (right after the `set` line): compute `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` and then `source "$SCRIPT_DIR/resolve-target.sh"`.
- Replace the BODY of the existing `resolve_cmake_target()` function (lines ~186-238) with a thin wrapper: set `CMAKE_TARGET="$(resolve_cmake_target_for "$PLUGIN_NAME")"` and append a log line to `$LOG_FILE` such as `echo "CMake target: $CMAKE_TARGET" >> "$LOG_FILE"`. Keep the function name and the `CMAKE_TARGET` global contract identical so Phases 2/3/5 are unchanged.
- BUG-06b (set -u guard): change the `JUCE_DIR` use at line ~170 from `if [ -n "$JUCE_DIR" ]` to `if [ -n "${JUCE_DIR:-}" ]` (JUCE_DIR is an optional external env var; unset would abort under set -u).
- BUG-06c (eval|tee exit masking, execute() lines ~113-119): capture the status immediately after each branch instead of relying on a trailing `return ${PIPESTATUS[0]}` that a later command can clobber. In the verbose branch (`eval "$cmd" 2>&1 | tee -a "$LOG_FILE"`) set `local rc=${PIPESTATUS[0]}` on the following line; in the non-verbose branch (`eval "$cmd" >> "$LOG_FILE" 2>&1`) set `local rc=$?`; then `return "$rc"`. (execute() is only ever called in `if ! execute ...` condition context, so set -e stays suppressed inside it.)

Do NOT change any other Phase logic, path, or message.

Commit staging ONLY these two files (never `git add -A`): `git add scripts/resolve-target.sh scripts/build-and-install.sh` then commit `fix(quick-260701-k0a): factor shared resolve-target.sh + harden build-and-install.sh (BUG-01/IMP-03/BUG-06)`.
  </action>
  <verify>
    <automated>bash -n scripts/resolve-target.sh && bash -n scripts/build-and-install.sh && test -x scripts/resolve-target.sh && [ "$(bash scripts/resolve-target.sh cmake-target O-Texture)" = "OuariconTexture" ] && [ "$(bash scripts/resolve-target.sh cmake-target O-Chorus)" = "OuariconChorus" ] && [ "$(bash scripts/resolve-target.sh cmake-target O-Gain)" = "O-Gain" ] && [ "$(bash scripts/resolve-target.sh product-name O-Texture)" = "O-Texture" ] && [ "$(bash scripts/resolve-target.sh product-name O-Chorus)" = "O-Chorus" ] && [ "$(bash scripts/resolve-target.sh product-name O-Gain)" = "O-Gain" ] && [ "$(grep -c 'set -euo pipefail' scripts/build-and-install.sh)" = "1" ] && grep -q 'resolve-target.sh' scripts/build-and-install.sh && grep -q 'JUCE_DIR:-' scripts/build-and-install.sh && echo PASS</automated>
  </verify>
  <done>scripts/resolve-target.sh exists (executable) and returns the three verified targets and three verified product names; build-and-install.sh sources it, runs under `set -euo pipefail`, guards `${JUCE_DIR:-}`, and execute() captures rc per-branch. Both files committed with only those two paths staged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CI workflow to the shared resolver (remove naive sed at 5 sites)</name>
  <files>.github/workflows/build-and-release.yml</files>
  <action>
Replace the five naive parse sites in `.github/workflows/build-and-release.yml` with calls to the shared resolver. CI checks out the repo (submodules recursive) and every affected step runs from repo root, so `bash scripts/resolve-target.sh ...` resolves correctly. The two Windows steps already declare `shell: bash`; keep that.

CMAKE_TARGET (BUG-01), two sites:
- Step "Resolve CMake Target" (macOS, ~line 87): replace the `CMAKE_TARGET=$(grep ... juce_add_plugin ... | ... | sed ...)` line with `CMAKE_TARGET=$(bash scripts/resolve-target.sh cmake-target "$PLUGIN")`. Leave the surrounding `PLUGIN=...`, `echo "cmake_target=..."`, and the human-readable echo intact.
- Step "Resolve CMake Target" (Windows, ~line 439): make the identical replacement.

PRODUCT_NAME (IMP-03), three sites — collapse each multi-line grep|sed + `${...}`-strip + default into a single resolver call:
- Step "Create PKG Installer" (macOS, ~line 235): replace the three PRODUCT_NAME lines (grep|sed parse, the `sed 's/\${[^}]*}//g'` strip, and the `PRODUCT_NAME="${PRODUCT_NAME:-$PLUGIN}"` default) with a single `PRODUCT_NAME=$(bash scripts/resolve-target.sh product-name "$PLUGIN")`.
- Step "Get Product Name" (Windows, ~line 453): replace the `sed -n ...` parse line and the `${...}`-strip line with `PRODUCT_NAME=$(bash scripts/resolve-target.sh product-name "$PLUGIN")`, keeping the following `echo "name=$PRODUCT_NAME" >> $GITHUB_OUTPUT`.
- Step "Get Product Name" (release job, ~line 566): replace the grep|sed parse line and the `${...}`-strip line with `PRODUCT_NAME=$(bash scripts/resolve-target.sh product-name "$PLUGIN")`, keeping the following `echo "name=$PRODUCT_NAME" >> $GITHUB_OUTPUT` and the `Product name:` echo.

Do NOT touch triggers (`on: push: tags`), env, signing, or any other step. Do NOT add `workflow_dispatch`. Do NOT push, tag, or dispatch anything.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/build-and-release.yml')); print('yaml-ok')" && [ "$(grep -c 'resolve-target.sh' .github/workflows/build-and-release.yml)" = "5" ] && [ "$(grep -c 'juce_add_plugin(' .github/workflows/build-and-release.yml)" = "0" ] && [ "$(grep -c "PRODUCT_NAME \"" .github/workflows/build-and-release.yml)" = "0" ] && ! grep -q 'workflow_dispatch' .github/workflows/build-and-release.yml && echo PASS</automated>
  </verify>
  <done>The workflow still parses as valid YAML; `resolve-target.sh` appears exactly 5 times; no `juce_add_plugin(` sed parse and no `PRODUCT_NAME "` grep parse remain; no `workflow_dispatch` was introduced. Committed with only the workflow file staged: `git add .github/workflows/build-and-release.yml` then `fix(quick-260701-k0a): CI resolves cmake target + product name via shared resolve-target.sh (BUG-01/IMP-03)`.</done>
</task>

<task type="auto">
  <name>Task 3: Rename JUCE patch to 8.0.9 (UPD-01) + apply-juce-patches.sh --forward preflight (BUG-05)</name>
  <files>scripts/juce-patches/note-expression-juce-8.0.9.patch (renamed), scripts/apply-juce-patches.sh, modules/registry.yaml</files>
  <action>
UPD-01 — rename the tracked patch preserving history: `git mv scripts/juce-patches/note-expression-juce-8.0.4.patch scripts/juce-patches/note-expression-juce-8.0.9.patch`. Then update every `8.0.4` reference so `grep -R "note-expression-juce-8.0.4" scripts modules` returns nothing:
- `scripts/apply-juce-patches.sh` line 11 (behavior comment) and line 22 (`PATCH_FILE=...`) -> `note-expression-juce-8.0.9.patch`.
- `modules/registry.yaml` line 288 -> `note-expression-juce-8.0.9.patch`.

BUG-05 — in `scripts/apply-juce-patches.sh`, replace the bare apply at line 66 (`( cd "$JUCE_DIR" && patch -p1 < "$PATCH_FILE" )`) with a forward-only, self-verifying sequence (the script runs `set -e`, so guard the patch invocations with `|| true`):
- A dry-run preflight: `( cd "$JUCE_DIR" && patch -p1 --forward --dry-run < "$PATCH_FILE" ) || true` with a YELLOW "Preflighting (dry-run)" echo.
- The real apply with `--forward` (skips already-applied hunks instead of reverse-applying them): `( cd "$JUCE_DIR" && patch -p1 --forward < "$PATCH_FILE" ) || true`.
- An authoritative post-apply verification that recounts the `JUCE-NE-PATCH` marker in BOTH `$HEADER_FILE` and `$CPP_FILE` (reuse the existing `for f in ... grep -q "$MARKER"` counting idiom into a `POST` counter): if `POST -ge 2`, GREEN success echo; else RED failure echo + `exit 1`. This makes correctness depend on the marker recount, not on patch's exit code.

Keep the existing JUCE_DIR preflight, patch-file existence check, `FOUND -ge 2` idempotency skip, and the CRLF->LF normalization step unchanged.

Staging note (isolate the pre-existing unrelated WIP hunk in registry.yaml at ~line 342 — the O-simplePhysicalModelSynth used_by addition — so it is NOT bundled into this commit):
1. `git mv` the patch (stages the rename).
2. Edit `scripts/apply-juce-patches.sh` (references + BUG-05 block).
3. `git stash push -- modules/registry.yaml` (removes the unrelated WIP so registry.yaml returns to HEAD; the line-288 reference is far from the stashed line-342 hunk and is unaffected).
4. Edit `modules/registry.yaml` line 288 (the patch-filename reference).
5. `git add scripts/apply-juce-patches.sh scripts/juce-patches/note-expression-juce-8.0.9.patch modules/registry.yaml`.
6. Commit `fix(quick-260701-k0a): rename JUCE patch to 8.0.9 + --forward preflight in apply-juce-patches (UPD-01/BUG-05)`.
7. `git stash pop` to restore the unrelated registry.yaml WIP hunk to the working tree.
  </action>
  <verify>
    <automated>bash -n scripts/apply-juce-patches.sh && test -f scripts/juce-patches/note-expression-juce-8.0.9.patch && ! test -f scripts/juce-patches/note-expression-juce-8.0.4.patch && [ -z "$(grep -Rn 'note-expression-juce-8.0.4' scripts modules 2>/dev/null)" ] && grep -q 'note-expression-juce-8.0.9.patch' scripts/apply-juce-patches.sh && grep -q 'note-expression-juce-8.0.9.patch' modules/registry.yaml && grep -q 'patch -p1 --forward --dry-run' scripts/apply-juce-patches.sh && grep -q 'patch -p1 --forward <' scripts/apply-juce-patches.sh && grep -q 'O-simplePhysicalModelSynth' modules/registry.yaml && echo PASS</automated>
  </verify>
  <done>Patch renamed to 8.0.9 with no lingering 8.0.4 references anywhere in scripts/ or modules/; apply-juce-patches.sh has the `--forward --dry-run` preflight, the `--forward` real apply, and a marker-recount gate, and still passes `bash -n`; the unrelated registry.yaml O-simplePhysicalModelSynth WIP hunk is restored to the working tree (stash popped) and was not committed with this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> repo shell scripts | GitHub Actions executes `scripts/resolve-target.sh` on runner; input is the parse-tag plugin name (repo-controlled tag) and in-repo CMakeLists. No external/untrusted network input. |
| Local dev -> JUCE tree mutation | `apply-juce-patches.sh` mutates files under `$JUCE_DIR` (developer's local JUCE install / CI download). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-k0a-01 | Tampering | apply-juce-patches.sh partial/repeat apply corrupting JUCE source | medium | mitigate | `--forward` skip-applied + `--dry-run` preflight + authoritative marker recount gate (BUG-05, Task 3) |
| T-k0a-02 | Denial (of release) | CI target resolution failing on `${PROJECT_NAME}` plugins | high | mitigate | Shared robust resolver replaces naive sed at all 5 CI sites (BUG-01/IMP-03, Task 2); verified locally against O-Texture/O-Chorus/O-Gain |
| T-k0a-03 | Elevation / unbound-var footgun | build-and-install.sh silent exit-masking + unset-var use | low | mitigate | `set -euo pipefail` + `${JUCE_DIR:-}` guard + per-branch rc capture (BUG-06, Task 1) |
| T-k0a-04 | Tampering | Accidental CI dispatch during verification | high | mitigate | All verification is static (bash -n, PyYAML parse, grep) + local resolver execution; no push/tag/workflow_dispatch; verify asserts `workflow_dispatch` absent |

No package-manager installs (npm/pip/cargo) are introduced — Package Legitimacy Gate not applicable.
</threat_model>

<verification>
All verification is static + local — NO CI run is triggered (no push, no tag, no workflow_dispatch):
- `bash -n` on all three edited/created shell scripts.
- PyYAML `safe_load` static parse of the workflow YAML (confirmed available during planning).
- Shared resolver executed locally against O-Texture (`${PROJECT_NAME}` -> OuariconTexture), O-Chorus (literal target != folder -> OuariconChorus), and O-Gain (literal target == folder -> O-Gain), plus product-name for all three.
- grep gates: `resolve-target.sh` referenced exactly 5x in the workflow; zero `juce_add_plugin(` sed parses and zero `PRODUCT_NAME "` grep parses remain; zero `note-expression-juce-8.0.4` references remain; `--forward --dry-run` preflight present; unrelated registry WIP hunk restored.
</verification>

<success_criteria>
- CI target/product-name resolution is delegated to a single shared `scripts/resolve-target.sh` used by both the local script and all 5 CI sites; O-Texture would resolve to `OuariconTexture` in CI (BUG-01/IMP-03).
- `build-and-install.sh` runs under `set -euo pipefail` without unbound-var aborts and reports true command exit status (BUG-06).
- JUCE patch renamed to `note-expression-juce-8.0.9.patch` with all references updated; apply is `--forward`-safe against partial prior state (UPD-01/BUG-05).
- Three atomic commits, each staging ONLY its own files; the pre-existing O-Formant/O-Gain/registry WIP is untouched; no CI run triggered.
</success_criteria>

<output>
Create `.planning/quick/260701-k0a-fix-build-script-and-ci-defects-from-the/260701-k0a-SUMMARY.md` when done.
</output>
