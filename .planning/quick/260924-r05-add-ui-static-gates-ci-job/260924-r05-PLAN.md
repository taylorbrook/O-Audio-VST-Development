---
quick_id: 260924-r05
description: add ui-static-gates CI job
source: .planning/quick/260924-nho-review-of-the-ui-and-design-approach-in-/260924-nho-UI-DESIGN-REVIEW.md (R1)
---

Add a `ui-static-gates` GitHub Actions job (push to `main` + `workflow_dispatch`) running
`node scripts/check-i18n.js`, `node scripts/i18n-fr-lint.js`, `node scripts/i18n-zh-lint.js`;
fail on any non-zero exit. No pre-commit hook.

## Task 1 — verify gates green locally
- Run all three on committed HEAD (git archive, so other sessions' WIP is excluded).
- Done: all exit 0.

## Task 2 — add workflow
- New file `.github/workflows/ui-static-gates.yml` (separate from `ci-tests.yml`, which is dispatch-only
  by the no-build-on-push rule, and `build-and-release.yml`, which holds signing secrets).
- `permissions: contents: read`, setup-node 24, no npm install (gates use Node built-ins only).
- Steps 2–3 `if: !cancelled()` so every red gate is reported in one run.
- Done: YAML parses; one path-scoped commit.
