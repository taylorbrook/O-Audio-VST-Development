---
quick_id: 260924-r05
status: complete
commit: 85da346f
---

# 260924-r05 — ui-static-gates CI job

**Added** `.github/workflows/ui-static-gates.yml`: push to `main` + `workflow_dispatch`, ubuntu-latest,
Node 24, runs `check-i18n.js`, `i18n-fr-lint.js`, `i18n-zh-lint.js`. Any non-zero exit fails the job; each
gate runs even if an earlier one failed. No build, no secrets, `contents: read`. No pre-commit hook (as asked).

**Verified locally** on a `git archive HEAD` copy: all three exit 0, on both macOS APFS and a
case-sensitive APFS volume (a stand-in for the Linux runner).

**Heads-up:** the live working tree currently FAILS `check-i18n` [15] — O-ReverseDelay has 2 dead LABELS keys
(`label.duck`, `label.colour`). That's uncommitted WIP from another session, not HEAD; the first CI run after
it lands will go red unless those keys are removed or used again.

Not yet run on GitHub (23 local commits unpushed).
