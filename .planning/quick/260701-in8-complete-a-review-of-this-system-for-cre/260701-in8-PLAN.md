---
phase: 260701-in8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md
autonomous: true
requirements: [REVIEW-01]
quick_task: true
must_haves:
  truths:
    - "A severity-ranked REVIEW.md report exists for the Plugin Freedom System"
    - "Every reported bug cites concrete file:line (or file + grep-locatable token) evidence"
    - "Report separates bugs, improvement suggestions, and update recommendations into distinct sections"
    - "No source, script, config, or plugin file is modified (read-only analysis)"
  artifacts:
    - ".planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md"
  key_links:
    - "Findings trace back to inspected files: scripts/, .claude/, modules/registry.yaml, CMake, PLUGINS.md/NOTES.md/CHANGELOG signals"
---

<objective>
Produce a read-only review of the Plugin Freedom System (PFS) — the agentic workflow that drives this JUCE VST/AU monorepo — and deliver a single severity-ranked REVIEW.md documenting (a) bugs with file:line evidence, (b) improvement suggestions, and (c) update recommendations (stale versions, outdated docs/configs, cross-cutting drift).

Review the SYSTEM (scripts, .claude skills/agents/hooks/commands, module registry, CMake infrastructure, cross-cutting conventions), NOT the DSP of individual plugins. The 39 plugins under `plugins/` are OUTPUTS of the system; inspect them only for cross-cutting drift signals (e.g. missing shared flags, version-pin skew, known-issue backlog).

Purpose: surface actionable bugs plus improvement/update recommendations for the maintainer before the next milestone.
Output: `.planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md`. NO code changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md
@PLUGINS.md
@modules/registry.yaml

# Read-only review. Do NOT edit any inspected file. The ONLY file this plan writes is the REVIEW.md named in <output>.
# Prior art: quick task 14 (2026-03-05) ran a full PFS system review — this is a fresh pass, not a diff against it.
# Known drift already in maintainer memory (confirm current state, cite evidence, do not assume fixed):
#   - scripts/juce-patches/note-expression-juce-8.0.4.patch filename says 8.0.4 but is a JUCE 8.0.9 diff (rename pending)
#   - JUCE pinned to 8.0.9 locally + in CI (build-and-release.yml)
#   - Historically 34/35 plugins missing NEEDS_WEBVIEW2 TRUE + JUCE_USE_WIN_WEBVIEW2_WITH_STATIC_LINKING=1 (re-quantify across current 39)
#   - Some juce_add_plugin targets != folder name (e.g. O-Chorus -> OuariconChorus, O-Texture -> ${PROJECT_NAME})
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit automation and orchestration layer (scripts + CI + .claude)</name>
  <files>.planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md</files>
  <action>
Inspect the automation/orchestration surface read-only and record findings. Cover:

1. Shell + CI scripts in `scripts/`: build-and-install.sh, build-and-install.ps1, apply-juce-patches.sh, verify-au-link.sh, verify-backup.sh, generate_placeholder_models.py, and `.github/workflows/build-and-release.yml`. Look for: unquoted expansions, missing strict-mode flags, hard-coded paths and target-name assumptions (target-not-equal-folder is a known class — see build_script_target_name_vs_folder in memory), dual-variant (-dev vs release) install/sweep gaps, cache-clear sequence correctness, and CI/local drift (JUCE pin, build targets, matrix coverage).

2. `.claude/` consistency: cross-reference `.claude/skills/` (26), `.claude/commands/` (47), and `.claude/agents/` (12) against each other and against `.claude/references/`, `.claude/critics/`, and the resource index (resource-index.json vs resource-index.schema.json). Flag: commands referencing skills or agents that do not exist, skills referencing missing references, agent-memory/inject-context wiring gaps, and stale artifacts (e.g. frontmatter-issues.txt, compaction-snapshot.md).

3. `.claude/hooks/`: verify each hook referenced by settings/lifecycle actually exists and its imports resolve at a glance (SessionStart, Stop, SubagentStop, PostToolUse, PreCompact, task-validator-dispatch, inject-agent-memory, write-back-agent-memory, validators/). Flag dangling references and duplicate/no-op hooks.

Evidence rule: every bug records the file path plus a line number OR a unique grep-locatable token so the maintainer can jump to it. Assign each finding a severity: CRITICAL / HIGH / MEDIUM / LOW.

Create the REVIEW.md now with an H1 title, a short "Scope and Method" note (read-only; what was inspected), a "Severity Legend", and an H2 "Section A — Automation and Orchestration" holding these findings. Use per-finding sub-bullets: Severity, Location (file:line), Observation, Recommendation. Do NOT reformat or edit any inspected file.
  </action>
  <verify>
    <automated>test -f .planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md && grep -qi 'Section A' .planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md && grep -Eqi 'CRITICAL|HIGH|MEDIUM|LOW' .planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md</automated>
  </verify>
  <done>REVIEW.md exists with title, scope note, severity legend, and Section A findings; each Section A bug has a severity label and a file:line (or grep-locatable) location. No inspected file was modified.</done>
</task>

<task type="auto">
  <name>Task 2: Audit module system, CMake infrastructure, and cross-cutting drift</name>
  <files>.planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md</files>
  <action>
Append findings to the existing REVIEW.md. Cover:

1. Module system: read `modules/registry.yaml` and reconcile it against reality. For each registered module (core, persistence, metering, tuning, synthesis, effects, ui, cmake, scripts), check that its declared `used_by` list matches actual consumers on disk (grep plugin CMakeLists / module.cmake includes), and that declared versions match the module's own headers/CHANGELOG. Flag: registry `version`/`last_updated` staleness (top of file says 1.0.0 / 2026-01-14 — verify against activity), modules referenced by plugins but missing from registry, and registry entries with no on-disk module. Check `modules/cmake/OuariconModules.cmake` per-format source-routing convention is internally consistent.

2. CMake infrastructure: sample plugin `CMakeLists.txt` files plus root CMake for cross-cutting patterns — JUCE version pin consistency (local + CI + any hard-coded), NEEDS_WEBVIEW2 TRUE and JUCE_USE_WIN_WEBVIEW2_WITH_STATIC_LINKING=1 coverage (quantify how many of the 39 plugins set BOTH; this is a known Windows-blank-WebView regression class), residual licensing/Supabase references after the v1.5 Path B removal, and dual binary-data NAMESPACE collision risk.

3. Cross-cutting drift and known-issue backlog: the JUCE patch filename mismatch (scripts/juce-patches/note-expression-juce-8.0.4.patch vs actual 8.0.9 content), stale docs/config (PLUGINS.md status accuracy vs on-disk plugins — 39 dirs; NOTES.md and per-plugin CHANGELOG patterns for recurring/unresolved issues; `.planning/STATE.md` carry-forward BL-01), and any TODO/FIXME/deferred-items signals in the system layer (not plugin DSP).

Evidence rule unchanged: file:line or grep-locatable token, plus CRITICAL/HIGH/MEDIUM/LOW severity per finding.

Append an H2 "Section B — Modules, CMake and Cross-Cutting Drift" with the same per-finding sub-bullet format. Do NOT edit any inspected file.
  </action>
  <verify>
    <automated>grep -qi 'Section B' .planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md && grep -Eqi 'NEEDS_WEBVIEW2|registry.yaml|8\.0\.4|8\.0\.9' .planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md</automated>
  </verify>
  <done>Section B present with module-registry reconciliation, CMake/WebView2 coverage count, and named cross-cutting drift items (JUCE patch filename mismatch, version pins, stale docs). Every finding has severity + evidence. No inspected file modified.</done>
</task>

<task type="auto">
  <name>Task 3: Synthesize severity-ranked deliverable sections and verify report</name>
  <files>.planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md</files>
  <action>
Assemble the raw Section A/B findings into the three required deliverable sections and finish the report:

1. Add an "Executive Summary" near the top (after the scope note): 3-6 bullets naming the highest-severity themes and the single most urgent item.

2. Add the three canonical deliverable H2 sections, each a severity-ordered table or list (highest severity first), with columns/fields ID, Severity, Location, Finding, Recommendation:
   - "## Bugs" — defects with concrete file:line evidence (pull the CRITICAL/HIGH items from Sections A and B to the top).
   - "## Improvement Suggestions" — non-defect enhancements to the workflow/tooling/module system.
   - "## Update Recommendations" — stale versions, outdated docs/configs, drift to reconcile (JUCE patch filename, version pins, registry last_updated, PLUGINS.md accuracy, WebView2 flag rollout).

3. Give every bug/improvement/update a stable ID (e.g. BUG-01, IMP-01, UPD-01) and confirm each Bugs-section row carries a file:line (or grep-locatable token). Add a one-line "Method: read-only; no files changed" footer.

Keep Sections A and B as the detailed appendix beneath the three synthesis sections (or fold them in — maintainer preference is concise, so a single coherent structure is fine as long as bugs/improvements/updates are clearly separated). Do NOT edit any inspected file. Run the whole-report self-check: the three H2 sections exist, severities are present, and at least the Bugs section cites file paths.
  </action>
  <verify>
    <automated>f=.planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md; grep -qi '## Bugs' "$f" && grep -qi '## Improvement' "$f" && grep -qi '## Update' "$f" && grep -Eqi 'BUG-0|IMP-0|UPD-0' "$f" && grep -Eq '\.(sh|ya?ml|py|cmake|md|txt|json)([:# ]|$)' "$f"</automated>
  </verify>
  <done>REVIEW.md contains the three deliverable H2 sections (Bugs, Improvement Suggestions, Update Recommendations), an executive summary, stable IDs, and at least the Bugs section cites file:line evidence. Read-only footer present. No inspected file modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer -> filesystem | Read-only inspection of scripts, .claude, modules, CMake, and plugin metadata. No writes outside the REVIEW.md deliverable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260701-in8-01 | Tampering | inspected system files | low | accept | Task actions are read-only by contract; the only writable path is the REVIEW.md deliverable. No `<action>` edits inspected files. |
| T-260701-in8-02 | Information Disclosure | REVIEW.md contents | low | accept | Report stays inside `.planning/quick/` and cites only paths already in the repo; no secrets are copied into it. |
| T-260701-in8-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs — pure read-only analysis, no dependency changes. |
</threat_model>

<verification>
- REVIEW.md exists at the output path and is the only file created/modified by this plan.
- Report contains: title, scope/method note, severity legend, executive summary, Section A (automation/orchestration), Section B (modules/CMake/drift), and the three deliverable sections (Bugs, Improvement Suggestions, Update Recommendations).
- Every bug carries a severity (CRITICAL/HIGH/MEDIUM/LOW) and a file:line or grep-locatable evidence token.
- `git status --porcelain` shows no modifications to inspected source/script/config/plugin files (only the new REVIEW.md under .planning/quick/).
</verification>

<success_criteria>
A maintainer can open 260701-in8-REVIEW.md and, for each bug, jump straight to the cited file:line; improvement suggestions and update recommendations are clearly separated from bugs and severity-ranked. No code was changed.
</success_criteria>

<output>
Create `.planning/quick/260701-in8-complete-a-review-of-this-system-for-cre/260701-in8-REVIEW.md` (the review report). This plan writes no other files.
</output>