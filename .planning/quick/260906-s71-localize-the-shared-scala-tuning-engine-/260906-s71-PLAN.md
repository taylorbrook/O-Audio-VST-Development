---
task: 260906-s71
type: quick
mode: execute
autonomous: true
files_modified:
  - scripts/check-i18n.js
  - modules/tuning/scala-tuning-engine/js/tuning-panel.js
  - modules/tuning/scala-tuning-engine/module.yaml
  - modules/registry.yaml
  - plugins/O-Bassoon/Resources/ui/js/i18n.js
  - plugins/O-Bowed/Resources/ui/js/i18n.js
  - plugins/O-Contrabass/Source/ui/public/js/i18n.js
  - plugins/O-Reed/Resources/ui/js/i18n.js
  - plugins/O-Wind/Resources/ui/js/i18n.js
  - plugins/O-Bassoon/tests/i18n-states.json
  - plugins/O-Bowed/tests/i18n-states.json
  - plugins/O-Contrabass/tests/i18n-states.json
  - plugins/O-Reed/tests/i18n-states.json
  - plugins/O-Wind/tests/i18n-states.json
  - plugins/O-Bassoon/CMakeLists.txt
  - plugins/O-Bowed/CMakeLists.txt
  - plugins/O-Contrabass/CMakeLists.txt
  - plugins/O-Reed/CMakeLists.txt
  - plugins/O-Wind/CMakeLists.txt
  - plugins/O-Bassoon/CHANGELOG.md
  - plugins/O-Bowed/CHANGELOG.md
  - plugins/O-Contrabass/CHANGELOG.md
  - plugins/O-Reed/CHANGELOG.md
  - plugins/O-Wind/CHANGELOG.md
  - PLUGINS.md

estimate:
  tokens: 340000
  raw_tokens: 170000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A user running any of the five consumers in French sees the whole tuning tab in French — viz buttons, library, generator, file buttons, tonic, interval count."
    - "A user running O-Bassoon in Simplified Chinese sees the whole tuning tab in Chinese."
    - "No consumer renders a bare key name (label.vizCircle) anywhere on the tuning tab in any declared language."
    - "check-i18n names modules/tuning/scala-tuning-engine/js/tuning-panel.js in its [12] scanned-module line for all five consumers, so the panel's prose and keys are inside the gate rather than beside it."
    - "The extended gate was observed RED on the five consumers before the localization landed and GREEN after — the probe targets the branch the fix changed."
    - "All five consumers build, install, and pass auval on this machine at their new MINOR versions."
  artifacts:
    - modules/tuning/scala-tuning-engine/js/tuning-panel.js  # 37 keyed captions + refreshPanelI18n()
    - scripts/check-i18n.js                                   # CMake-derived module-JS scan
    - .planning/quick/260906-s71-localize-the-shared-scala-tuning-engine-/260906-s71-deferred-items.md
  key_links:
    - "module refreshPanelI18n() -> window.__reapplyI18n (present in all five index.html files) -> applyI18n sweep over freshly injected markup"
    - "37 LABELS rows in each consumer's i18n.js <- assertion 15 markupKeyRefs over the module code (only reachable because the gate extension puts the module in pageModules)"
    - "tests/i18n-states.json tuning state -> check-ui-labels / measure-ui actually reach the lazily-mounted panel"
---

<objective>
Localize the shared `modules/tuning/scala-tuning-engine/js/tuning-panel.js` for en/fr/zh-Hans and ship it through all five path-embedding consumers in one change set.

Purpose: closes wave 4e's **D1** by its option 1 (module once, now). Three of wave 4f's plugins (O-Wind, O-Reed, O-Bowed) are consumers; without this they would ship an English tuning tab inside a Chinese interface.

Output: a keyed module at v3.1.0, a `check-i18n` that can SEE CMake-embedded module JS, 37 new LABELS rows in each of five consumers, five MINOR releases built + installed + auval'd, and a deferred-items file for wave 4f.
</objective>

<context>
@.planning/quick/260906-s71-localize-the-shared-scala-tuning-engine-/260906-s71-CONTEXT.md
@CLAUDE.md
@plugins/O-MicrotonalSampler/Resources/ui/js/tuning-panel.js
@modules/tuning/scala-tuning-engine/js/tuning-panel.js
</context>

<planning_time_observations>
Everything below was MEASURED on this tree on 2026-09-06 at planning time. Where an
action's scope depends on it, RE-READ it before acting; a mismatch is a HALT, not a
silent adaptation.

**CMake versions (two-arm reader — four quoted, O-Contrabass unquoted, exactly as N9 predicts):**

| Plugin | file:line | as found | next (MINOR) |
|---|---|---|---|
| O-Bassoon | CMakeLists.txt:14 | `VERSION "1.4.0"` | 1.5.0 |
| O-Bowed | CMakeLists.txt:12 | `VERSION "1.7.1"` | 1.8.0 |
| O-Contrabass | CMakeLists.txt:16 | `VERSION 1.8.3` (UNQUOTED) | 1.9.0 |
| O-Reed | CMakeLists.txt:11 | `VERSION "1.4.1"` | 1.5.0 |
| O-Wind | CMakeLists.txt:12 | `VERSION "1.19.1"` | 1.20.0 |

**check-i18n baseline, all five exit 0, all report `1 module(s): the inline <script type="module"> in index.html`:**

| Plugin | i18n.js path | LABELS before | LABELS after (+37) | LANGUAGES |
|---|---|---|---|---|
| O-Bassoon | `Resources/ui/js/i18n.js` | 36 | 73 | en, fr, zh-Hans |
| O-Bowed | `Resources/ui/js/i18n.js` | 45 | 82 | en, fr |
| O-Contrabass | `Source/ui/public/js/i18n.js` | 57 | 94 | en, fr |
| O-Reed | `Resources/ui/js/i18n.js` | 59 | 96 | en, fr |
| O-Wind | `Resources/ui/js/i18n.js` | 71 | 108 | en, fr |

**Three CONTEXT statements that the tree contradicts — the tree wins:**

1. CONTEXT says "only O-Bowed has a `tests/i18n-states.json`". **All five have one.** Task 2 EDITS five files, it does not author four.
2. CONTEXT's census says "8 plugins embed module JS by path, `preset-manager.js` ×18". The pattern `${CMAKE_SOURCE_DIR}/modules/**.js` returns **10 references across 8 plugins, 5 distinct files** — see the table in Task 1. The ×18 figure counted a different pattern (plugin-local copies) and must not size this work.
3. CONTEXT lists 39 keys. The module's markup renders **37** of them; `label.totalSpan` and `label.tuningPanelUnavailable` are MTS-only surfaces and are NOT adopted.

**`window.__reapplyI18n` confirmed present in all five** — O-Bassoon index.html:1548, O-Bowed:2297, O-Contrabass Source/ui/public/index.html:1418, O-Reed:1820, O-Wind:1555. All five carry canon v2 `i18nVars` support (2 occurrences each).

**PLUGINS.md is STALE for four of five** (row : CMake) — O-Bowed 1.6.2 : 1.7.1, O-Reed 1.3.1 : 1.4.1, O-Wind 1.18.2 : 1.19.1, O-Contrabass 1.8.2 : 1.8.3; only O-Bassoon 1.4.0 agrees. Task 3 writes the NEW version from CMake and does not increment the stale row.

**Tuning-surface activation selectors:** O-Bassoon `.tab-btn[data-tab="tuning"]` (index.html:914), O-Bowed `#tuning-toggle` (871, overlay), O-Contrabass `.tab-btn[data-tab="tuning"]` (927), O-Reed `.tab-btn[data-tab="tuning"]` (864), O-Wind `.tab-btn[data-tab="tuning"]` (1168).
</planning_time_observations>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Teach check-i18n to see CMake-embedded module JS, then localize the module and all five consumer tables in ONE change set</name>

  <files>
scripts/check-i18n.js
modules/tuning/scala-tuning-engine/js/tuning-panel.js
modules/tuning/scala-tuning-engine/module.yaml
modules/registry.yaml
plugins/O-Bassoon/Resources/ui/js/i18n.js
plugins/O-Bowed/Resources/ui/js/i18n.js
plugins/O-Contrabass/Source/ui/public/js/i18n.js
plugins/O-Reed/Resources/ui/js/i18n.js
plugins/O-Wind/Resources/ui/js/i18n.js
  </files>

  <read_first>
scripts/check-i18n.js lines 795-895 (pageModules assembly and its stated design rule: DERIVED FROM THE DIRECTORY, never a transcribed list) and 1156-1275 (assertions 12 and 15, including markupKeyRefs).
scripts/check-i18n.js lines 478-510 (discover() — `p.cmake` and `p.pluginRoot` already exist on every plugin record; `repoRoot` is defined at line 141).
plugins/O-MicrotonalSampler/Resources/ui/js/tuning-panel.js lines 20-60 (the header block that states the design), 85-95 (refreshPanelI18n), 105-240 (keyed static template), 325-345 (intervalsCount with data-i18n-vars), 600-610 (tk-hint second site), 675-685 (rotationMode th), 812-822 (noteCount with vars), 875-905 (generator input variants).
plugins/O-MicrotonalSampler/Resources/ui/js/i18n.js lines 1508-1640 (the 37 en/fr/zh-Hans rows this task copies).
plugins/O-Bassoon/Resources/ui/js/i18n.js lines 610-625 (the `label.tuningLoadFailed` row — the placement precedent inside LABELS, and the zh-Hans `reviewed: 'bt'` shape).
  </read_first>

  <precondition>All five consumers pass `node scripts/check-i18n.js --plugin &lt;Name&gt;` at exit 0 on the tree as found, and each reports `1 module(s)`. FIRE THIS, do not assume it (wave 4f instruction 3: wave 4d asserted a precondition that had been false for two releases).</precondition>

  <action>
Do this in five ordered steps. Steps A and B produce the positive control that makes every later green mean something; do NOT reorder them.

**A. Extend `scripts/check-i18n.js` — CMake-derived module JS into `pageModules`.**

After the existing `jsDir` loop that pushes `js/*.js` (ends around line 861, before the `bundlesSkipped` block), add a second source: parse `p.cmake` for lines referencing `${CMAKE_SOURCE_DIR}/modules/<...>.js`, resolve each against `repoRoot`, and push `{ label: <repo-relative path>, code: <file contents>, inline: false }`. Requirements:

- DERIVE the list by scanning the CMakeLists text; never transcribe a filename list. This is the identical rule the file already argues for the directory scan at lines 810-820, and it is why the extension is legitimate rather than a special case.
- Reuse the existing exclusions: skip anything matching `/\.(bundle|min)\.js$/` and route it through the same `bundlesSkipped` reporting; skip a path that does not exist on disk and SAY SO in a check rather than passing on a smaller set.
- Dedupe by resolved path and by code, so a file already reachable through `js/` is not scanned twice.
- Update the assertion-list comment block at the top of the file (the paragraph under assertion 12/15 that currently explains why sibling modules are scanned) to state that CMake-embedded module JS is now in the same set and why: a file the page RUNS references keys and writes prose whether it lives in the plugin or in `modules/`.

**B. Fire the extended gate REPO-WIDE and RECORD what it exposes — before touching the module.**

Run `node scripts/check-i18n.js` over the whole repo in the BACKGROUND (600 s watchdog) and capture the full output to a scratch file. The 8 plugins whose CMake embeds module JS, measured at planning time:

| Plugin | newly-scanned module file(s) |
|---|---|
| O-Bassoon | `modules/tuning/scala-tuning-engine/js/tuning-panel.js` |
| O-Bowed | same |
| O-Contrabass | `modules/persistence/preset-manager/js/preset-manager.js`, tuning-panel.js |
| O-Marimba | `modules/effects/analog-eq-unit/js/analog-eq-unit.js`, `modules/effects/compressor-unit/js/compressor-unit.js` |
| O-MicrotonalSampler | `modules/core/webview-drop-streaming/js/webview-drop-streaming.js` |
| O-Reed | tuning-panel.js |
| O-ReverseDelay | `modules/persistence/preset-manager/js/preset-manager.js` |
| O-Wind | tuning-panel.js |

Re-derive that table from the tree before trusting it. Then record, per plugin, the exact FAIL lines. Two expectations, both of which are FINDINGS either way:

- **The five tuning consumers MUST go RED here** (assertion 10/12 sees the module's raw English template for the first time). If any of them stays GREEN, the extension is INERT — stop and fix the extension before going further. This red is the positive control for the whole task: a gate that cannot fail on the unlocalized module cannot certify the localized one.
- **The three out-of-scope plugins (O-Marimba, O-MicrotonalSampler, O-ReverseDelay) may go red.** Disposition, decided here so the executor does not decide it under pressure:
  - If a finding is a one-line trivial fix inside the module file and touches no rendered string, fix it and name it in SUMMARY.
  - Otherwise, take the **narrower fallback CONTEXT pre-authorizes**: restrict the CMake follow to files whose basename matches `tuning-panel.js`, implemented as a named, commented constant with the reason and a TODO naming the other four module files. Then RE-RUN step B and confirm the five consumers still go RED (the control must survive the narrowing).
  - Either way: SAY WHICH ARM WAS TAKEN AND WHY IN SUMMARY. A silent widening or a silent narrowing is the failure mode this step exists to prevent.

**C. Localize `modules/tuning/scala-tuning-engine/js/tuning-panel.js`.**

Mirror the MTS precedent exactly. Attributes and the hook only — **do not restructure the template**: the `tuning-panel-root` wrapper and the `tuning-center-column` wrapper STAY (O-Contrabass ships its own `.tuning-panel` CSS overrides at index.html:855 and O-Bowed mounts the panel in an overlay; a structural change breaks both).

- Add a `refreshPanelI18n()` method calling `window.__reapplyI18n` when it is a function, guarded for a missing hook (the authored English is the fallback and renders unchanged). Call it after EVERY `innerHTML` assignment that carries a keyed caption: `render()`, `updateIntervalList()`, `drawTrueKeys()`, `drawRotationTable()`, `renderLibraryList()`, `setGeneratorType()`.
- Key exactly these **37** keys, adopting MTS's key names verbatim so a future re-sync is a diff and not a redesign:
  `label.intervalsCount label.vizCircle label.vizPolar label.vizMatrix label.vizTrueKeys label.vizRotation label.scaleIntervals label.tkHint label.tuningLibrary label.catAll label.catHistorical label.catJust label.catEdo label.catNonOctave label.catWorld label.a4Ref label.stretch label.loadScl label.loadKbm label.saveScl label.saveKbm label.exportHtml label.generateScale label.genEdo label.genHarmonic label.genRank2 label.genDivisions label.genPeriod label.generate label.tonic label.rotationMode label.noteCount label.genStartHarmonic label.genEndHarmonic label.genGenerator label.genR2Period label.genNotes`
- Counted captions ride `data-i18n-vars` so the language sweep owns them and no inflection logic lives in a string (assertion 13): `label.intervalsCount` with `{"n":count}` (English face `Intervals · notes: N`) and `label.noteCount` with `{"n":tuning.noteCount}` (English face `notes: N`).
- The static interval-list header currently nests `<span id="interval-count">12</span>` inside its prose. `applyLabel` writes `textContent` and would destroy that span, so replace the static header's inner markup with the keyed `label.intervalsCount` caption and DROP the nested span. This is safe and is what MTS did: `updateIntervalList()` rebuilds `#interval-list` wholesale on first load, and its `countEl` write is already guarded by `if (countEl)`. Keep the `<div class="interval-list" id="interval-list">` element itself.
- EXEMPT — these are readout/data, not copy, and must NOT be keyed: note names `C…B`, the `${notes[0]}->${notes[i]}: …c` true-keys readout, the matrix/rotation numeric cells and their numeric `<th>` headers, `${tuning.name}`, `${this.scaleName}`, the `440.0 Hz` and `1.00` readouts, the `c` interval unit, and the `▼ ◄ ►` glyphs.
- Header block: bump `scala-tuning-engine module v3.0.1` to `v3.1.0` and add a short paragraph in the MTS style stating that keys are declared in the markup, that `data-i18n-vars` carries the counts, that `refreshPanelI18n()` re-runs the sweep because the panel mounts LAZILY, and that a consumer embedding this file MUST carry the 37 rows or a missing key renders the key name.

**D. Version the module.**

`modules/registry.yaml`: `version: 3.0.1` → `3.1.0` with a changelog line naming the 37 keys and the five consumers. `modules/tuning/scala-tuning-engine/module.yaml` is stale at 2.1.0 — bring its `version:` and its changelog forward to 3.1.0 and leave the rest of the file alone. Do NOT repair the stale `used_by` list here (it names O-Bells/O-Formant/O-IntonationPad forks and omits O-Reed/O-Wind/O-MicrotonalSampler); that goes to deferred-items in Task 3.

**E. Add the 37 rows to all five consumers — SAME CHANGE SET, non-negotiable.**

A missing key renders the key name (`trLabel` returns `key`, `applyLabel` writes it to `textContent`), so a consumer rebuilt against the keyed module without rows paints `label.vizCircle` on its tuning tab. Copy the en/fr pairs verbatim from MTS i18n.js lines 1508-1640 — they are `reviewed: true`, taken from O-Bells v4.2.0, and this is a copy, not an authoring pass.

- Place the block inside `LABELS`, immediately beside each plugin's existing tuning row (`label.tuningLoadFailed` on O-Bassoon/O-Contrabass/O-Reed, `label.tuningFailed` on O-Wind, `label.tuning` on O-Bowed's I18N — for O-Bowed put the block in `LABELS` beside its other label rows and leave `label.tuning` where it is).
- Carry `sameAsEn: true` wherever MTS carries it (`label.vizRotation`, `label.genDivisions`, `label.genNotes`, `label.rotationMode`) — assertion 4 fails a straight fr passthrough without it.
- **O-Bassoon only** gets the `'zh-Hans'` column, copied verbatim from MTS at `reviewed: 'bt'`. O-Bowed/O-Contrabass/O-Reed/O-Wind declare `['en','fr']` and assertion 1 demands every key in every DECLARED language — no more, no less. Adding a zh column to a two-language plugin is a defect here, not a head start.
- Match each file's local formatting (indentation, the trailing-brace style of `'zh-Hans': { t: '…', reviewed: 'bt' }}`), not MTS's.
- Then run `node scripts/i18n-zh-lint.js --plugin O-Bassoon` and read **all** findings `--verbose` (the default print is truncated). Any row **Z5** flags as colliding with an existing O-Bassoon root gets a blind reverse read in O-Bassoon's page context (M13) with a fresh reader and fresh salt. Do NOT re-read all 37.

Run every gate in the BACKGROUND — the executor watchdog is 600 s.
  </action>

  <verify>
    <automated><![CDATA[
set -e
cd /Users/taylorbrook/Dev/VST-development

# ── 1. the module keys EXACTLY the 37 enumerated keys (comment-stripped: the file has comments)
STRIP="perl -0777 -pe s{/\*.*?\*/}{}gs;s{//[^\n]*}{}g"
N=$(perl -0777 -pe 's{/\*.*?\*/}{}gs; s{//[^\n]*}{}g' modules/tuning/scala-tuning-engine/js/tuning-panel.js \
    | grep -o 'data-i18n="[^"]*"' | sort -u | wc -l | tr -d ' ')
[ "$N" = "37" ] || { echo "FAIL: module declares $N distinct data-i18n keys, expected 37"; exit 1; }

# ── 2. the key SET is the intended set, not merely 37 of something
perl -0777 -pe 's{/\*.*?\*/}{}gs; s{//[^\n]*}{}g' modules/tuning/scala-tuning-engine/js/tuning-panel.js \
  | grep -o 'data-i18n="[^"]*"' | sed 's/data-i18n="//;s/"//' | sort -u > /tmp/s71-module-keys.txt
cat > /tmp/s71-expected-keys.txt <<'KEYS'
label.a4Ref
label.catAll
label.catEdo
label.catHistorical
label.catJust
label.catNonOctave
label.catWorld
label.exportHtml
label.genDivisions
label.genEdo
label.genEndHarmonic
label.genGenerator
label.genHarmonic
label.genNotes
label.genPeriod
label.genR2Period
label.genRank2
label.genStartHarmonic
label.generate
label.generateScale
label.intervalsCount
label.loadKbm
label.loadScl
label.noteCount
label.rotationMode
label.saveKbm
label.saveScl
label.scaleIntervals
label.stretch
label.tkHint
label.tonic
label.tuningLibrary
label.vizCircle
label.vizMatrix
label.vizPolar
label.vizRotation
label.vizTrueKeys
KEYS
sort -o /tmp/s71-expected-keys.txt /tmp/s71-expected-keys.txt
diff -u /tmp/s71-expected-keys.txt /tmp/s71-module-keys.txt || { echo "FAIL: module key SET differs from the 37"; exit 1; }

# ── 3. the two counted captions carry data-i18n-vars, and the hook is wired
perl -0777 -pe 's{/\*.*?\*/}{}gs; s{//[^\n]*}{}g' modules/tuning/scala-tuning-engine/js/tuning-panel.js \
  | grep -c 'data-i18n-vars' | grep -qx '3' \
  || { echo "FAIL: expected 3 data-i18n-vars sites (static + dynamic intervalsCount, noteCount)"; exit 1; }
perl -0777 -pe 's{/\*.*?\*/}{}gs; s{//[^\n]*}{}g' modules/tuning/scala-tuning-engine/js/tuning-panel.js \
  | grep -q '__reapplyI18n' || { echo "FAIL: module never calls window.__reapplyI18n"; exit 1; }
R=$(perl -0777 -pe 's{/\*.*?\*/}{}gs; s{//[^\n]*}{}g' modules/tuning/scala-tuning-engine/js/tuning-panel.js \
    | grep -c 'this.refreshPanelI18n()')
[ "$R" -ge 6 ] || { echo "FAIL: refreshPanelI18n() called $R times, expected >= 6 injection sites"; exit 1; }

# ── 4. version bumped in all three places (comment-stripped for the two YAMLs is unnecessary
#      — assert the VALUE line, not a bare token count)
grep -q 'scala-tuning-engine module v3\.1\.0' modules/tuning/scala-tuning-engine/js/tuning-panel.js
grep -q '^version: 3\.1\.0' modules/tuning/scala-tuning-engine/module.yaml
awk '/name: scala-tuning-engine/,/^  - name: /' modules/registry.yaml | grep -q 'version: 3\.1\.0'

# ── 5. THE GATE SEES THE MODULE, and is green, on all five
for P in O-Bassoon O-Bowed O-Contrabass O-Reed O-Wind; do
  OUT=$(node scripts/check-i18n.js --plugin "$P" 2>&1) || { echo "FAIL: check-i18n exit != 0 on $P"; echo "$OUT" | grep FAIL; exit 1; }
  echo "$OUT" | grep '\[12\] there is shipped page JS to scan' \
    | grep -q 'modules/tuning/scala-tuning-engine/js/tuning-panel.js' \
    || { echo "FAIL: $P [12] line does not name the module — the gate is not SEEING it"; \
         echo "$OUT" | grep '\[12\] there is shipped page JS to scan'; exit 1; }
done

# ── 6. each consumer gained exactly 37 LABELS (baselines measured 2026-09-06)
check_labels() {  # $1=plugin $2=expected-after
  node scripts/check-i18n.js --plugin "$1" 2>&1 \
    | grep -o 'the table carries copy — [0-9]* I18N + [0-9]* LABELS' \
    | grep -q "+ $2 LABELS" || { echo "FAIL: $1 LABELS count is not $2"; \
        node scripts/check-i18n.js --plugin "$1" 2>&1 | grep 'the table carries copy'; exit 1; }
}
check_labels O-Bassoon   73
check_labels O-Bowed     82
check_labels O-Contrabass 94
check_labels O-Reed      96
check_labels O-Wind     108

# ── 7. only O-Bassoon carries a zh column on the new keys
grep -q "label.vizCircle" plugins/O-Bassoon/Resources/ui/js/i18n.js
grep -A2 "'label.vizCircle'" plugins/O-Bassoon/Resources/ui/js/i18n.js | grep -q "zh-Hans"
for F in plugins/O-Bowed/Resources/ui/js/i18n.js \
         plugins/O-Contrabass/Source/ui/public/js/i18n.js \
         plugins/O-Reed/Resources/ui/js/i18n.js \
         plugins/O-Wind/Resources/ui/js/i18n.js; do
  grep -A2 "'label.vizCircle'" "$F" | grep -q "zh-Hans" \
    && { echo "FAIL: $F carries a zh-Hans column on a two-language plugin"; exit 1; }
done

# ── 8. repo-wide gate + the two lints
node scripts/check-i18n.js > /tmp/s71-checki18n-after.txt 2>&1; echo "check-i18n repo exit=$?"
grep -q 'ALL CHECKS PASS' /tmp/s71-checki18n-after.txt || { tail -40 /tmp/s71-checki18n-after.txt; exit 1; }
node scripts/i18n-fr-lint.js; echo "fr-lint exit=$?"
node scripts/i18n-zh-lint.js --plugin O-Bassoon; echo "zh-lint exit=$?"
echo "TASK 1 VERIFY OK"
]]></automated>
    <human-check>The step-B repo-wide run was captured to a file and the five consumers were observed RED in it BEFORE step C. Paste the five FAIL lines into SUMMARY. A task that produces no red here has an inert gate extension and its green in step 5 above means nothing.</human-check>
  </verify>

  <done>
`check-i18n` names the module in its [12] line on all five consumers and exits 0 repo-wide; the module declares exactly the 37 keys with 3 `data-i18n-vars` sites and >=6 `refreshPanelI18n()` calls; the module is v3.1.0 in the header, `module.yaml` and `registry.yaml`; each consumer's LABELS count grew by exactly 37; only O-Bassoon carries zh on them; `i18n-fr-lint` and `i18n-zh-lint --plugin O-Bassoon` are green; the pre-localization RED observation is recorded verbatim.
  </done>

  <commit>
Re-check `git branch --show-current` (must be `main`) and `git status --short` IMMEDIATELY before committing — another session's staging can join this commit.

```
git commit -- scripts/check-i18n.js modules/tuning/scala-tuning-engine modules/registry.yaml \
  plugins/O-Bassoon/Resources/ui/js/i18n.js \
  plugins/O-Bowed/Resources/ui/js/i18n.js \
  plugins/O-Contrabass/Source/ui/public/js/i18n.js \
  plugins/O-Reed/Resources/ui/js/i18n.js \
  plugins/O-Wind/Resources/ui/js/i18n.js
```

Never `git add -A`. Never tag. Message names the module version, the 37 keys, the five consumers, and which arm of step B's disposition was taken.
  </commit>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Drive the panel — a tuning state in all five i18n-states.json, then the geometry and boot gates</name>

  <files>
plugins/O-Bassoon/tests/i18n-states.json
plugins/O-Bowed/tests/i18n-states.json
plugins/O-Contrabass/tests/i18n-states.json
plugins/O-Reed/tests/i18n-states.json
plugins/O-Wind/tests/i18n-states.json
plugins/O-Bassoon/Resources/ui/index.html
plugins/O-Bowed/Resources/ui/index.html
plugins/O-Contrabass/Source/ui/public/index.html
plugins/O-Reed/Resources/ui/index.html
plugins/O-Wind/Resources/ui/index.html
  </files>

  <read_first>
All five `tests/i18n-states.json` files — every one already exists and every one is CUMULATIVE with load-bearing ordering comments (O-Wind's says the settings popover MUST be last or it covers the Effects tab; O-Reed's says the FX tab must be last or it hides the instrument panel). Read the ordering rationale before inserting.
scripts/check-ui-labels.js lines 555-580 (how states are loaded) and 1115-1125 (the coverage line this task measures against).
scripts/measure-ui.js lines 20-100 and 180-200 (`--report all`, `SCREEN_ORDER`).
  </read_first>

  <precondition>`node scripts/boot-all-uis.js --strict-tips` reports the standing census control — exactly 2 LATE on O-Bells (`#ref-pitch-knob`, `#octave-stretch`) and 0 DEAD. Capture this number BEFORE any change in this task; a 0 DEAD after is evidence only if the 2 LATE held on both sides.</precondition>

  <action>
**A. Add ONE tuning state to each of the five `tests/i18n-states.json`.**

The panel mounts LAZILY, so without this every one of the 37 captions is invisible to `check-ui-labels` and `measure-ui` in every language. Selectors, measured at planning time — re-confirm each in the plugin's index.html before writing it:

| Plugin | click | ordering constraint |
|---|---|---|
| O-Bassoon | `.tab-btn[data-tab="tuning"]` | states are cumulative; the existing "about tab" click hides the main panel — put tuning BEFORE about, and leave the gear popover last |
| O-Bowed | `#tuning-toggle` | ALREADY PRESENT as "tuning overlay open" — do not duplicate it; confirm it still fires and leave the file unchanged if so |
| O-Contrabass | `.tab-btn[data-tab="tuning"]` | put it FIRST, before the gear popover and the help toggle |
| O-Reed | `.tab-btn[data-tab="tuning"]` | the FX-tab state is explicitly documented as LAST; insert tuning BEFORE it and AFTER the expand-all eval |
| O-Wind | `.tab-btn[data-tab="tuning"]` | the gear popover is explicitly documented as LAST because it covers a tab button; insert tuning BEFORE it |

Give each new state a `name` that says what it reveals and why it sits where it sits, matching the prose density of the states already in that file.

**B. Prove the state HAD AN EFFECT — do not assume it (wave 4e N1).**

For each plugin, run `node scripts/check-ui-labels.js --plugin <Name> --verbose` before and after adding the state and compare the `-- coverage` line `N of M [data-i18n] elements were VISIBLE in at least one state`. **M must GROW** — the panel's captions only enter the DOM when the panel mounts, so an unchanged M means the click never reached the panel and the state is decorative. A state that does not move M is a FAILURE of this task, not a pass.

Fire `elementFromPoint` at each new click target's centre in the boot page and record what is actually painted there. Wave 4e's finding: a forced click hits whatever is on top — a popover or an overlapping keyboard — and reports success either way. Record the hit element per plugin per click state.

**C. Measure geometry in every language the plugin declares.**

`node scripts/measure-ui.js --plugin <Name> --report all` on each of the five. For findings:
- Pin caption **ROWS as ratios**, not lengths (N4/N8): a container can grow 2-3 px with every leaf inside it reporting an identical box, and a raw length grew one arm 0.77 px on a row holding an inline-block button.
- Expect **shrinks in both axes** on the Chinese arm and floors, not budgets (N7/N10): `min-width` AND `min-height` where both move.
- The French column may already move rows on these five — MEASURE, do not assume, and separate a pre-existing French wrap from anything this task caused (report the pre-existing one to deferred-items rather than fixing it).
- **Any geometry floor goes in the CONSUMER's own `#tuning-container`-scoped CSS inside its index.html — NEVER in `modules/tuning/scala-tuning-engine/snippets/tuning-panel.css`.** That file has five consumers and wave 4c's D1 already left it as found for exactly this reason. A floor written into the shared CSS silently reshapes four other plugins.

**D. State the `line-height-normal` criterion as N1 gives it:** `check-ui-labels` **0 moved**, plus every residual named with its measured `enH == zhH`. `measure-ui`'s `line-height-normal` screen structurally cannot reach 0 (wave 4c) — do not state it as the criterion.

**E. Fire the remaining gates, unchanged.**
- `node scripts/boot-all-uis.js --strict-tips` — the O-Bells 2-late/0-dead census control must read the same before and after.
- The plugins' own gate files, as they exist today: `plugins/O-Bowed/tests/ui_tip_render_check.js`, `plugins/O-Reed/tests/ui_tip_render_check.js`, `plugins/O-Wind/tests/ui_tip_render_check.js`, `plugins/O-Bassoon/tests/ui_tip_render_check.js`, and `plugins/O-Contrabass/tests/ui_frontend_check.js` (NOT a tip-render gate — read its header before running it).
- **DO NOT flip the two-language enumeration** in O-Bowed / O-Reed / O-Wind's `ui_tip_render_check.js`. Wave 4f owns that (its N12 enumeration-form census). Touching it here would consume 4f's finding.

**F. Report honestly what the harness could NOT reach.**

`render()` injects the static template before `loadInitialState()` runs, so the ~29 static captions mount under the JUCE stub regardless. The 8 dynamic-only captions (`label.tonic`, `label.rotationMode`, `label.noteCount`, and the five `gen*` variants behind a generator-type change) may never appear if the stub's invented native-function returns leave those code paths unrun. Name the residual as a measured coverage hole with the count and the reason — a hole reported as a hole (N8), not a green over an unmeasured surface.

Run every gate in the BACKGROUND.
  </action>

  <verify>
    <automated><![CDATA[
set -e
cd /Users/taylorbrook/Dev/VST-development

# ── 1. all five states files parse and every one names a tuning-reaching click
for P in O-Bassoon O-Bowed O-Contrabass O-Reed O-Wind; do
  node -e "JSON.parse(require('fs').readFileSync('plugins/$P/tests/i18n-states.json','utf8'))" \
    || { echo "FAIL: $P i18n-states.json does not parse"; exit 1; }
  node -e "
    const s=JSON.parse(require('fs').readFileSync('plugins/$P/tests/i18n-states.json','utf8'));
    const hit=s.some(x=>/tuning/i.test(JSON.stringify(x.click||'')));
    if(!hit){console.error('FAIL: $P has no state whose click reaches the tuning surface');process.exit(1);}
  "
done

# ── 2. the state HAD AN EFFECT: check-ui-labels reaches module captions in the live DOM.
#     Grounded on the module's own keys, so this cannot go green on a plugin whose click missed.
for P in O-Bassoon O-Bowed O-Contrabass O-Reed O-Wind; do
  node scripts/check-ui-labels.js --plugin "$P" --verbose > "/tmp/s71-cul-$P.txt" 2>&1 || true
  grep -q -- '-- coverage' "/tmp/s71-cul-$P.txt" || { echo "FAIL: $P produced no coverage section"; exit 1; }
  # a tuning-panel caption must appear somewhere in the measured output
  grep -qE 'viz-btn|pitch-circle-label|library-header-text|generator-header-text|tuning-file-btn' \
    "/tmp/s71-cul-$P.txt" \
    || { echo "FAIL: $P measured no tuning-panel element — the state is decorative"; exit 1; }
  echo "$P coverage: $(grep 'VISIBLE in at least one state' "/tmp/s71-cul-$P.txt")"
done

# ── 3. measure-ui --report all runs clean on all five
for P in O-Bassoon O-Bowed O-Contrabass O-Reed O-Wind; do
  node scripts/measure-ui.js --plugin "$P" --report all > "/tmp/s71-mui-$P.txt" 2>&1
  echo "measure-ui $P exit=$?"
done

# ── 4. boot census control unchanged
node scripts/boot-all-uis.js --strict-tips > /tmp/s71-boot-after.txt 2>&1; echo "boot exit=$?"
grep -qi 'O-Bells' /tmp/s71-boot-after.txt || { echo "FAIL: O-Bells census control absent"; exit 1; }

# ── 5. the plugins' own gate files still pass, and the two-language enumeration is UNTOUCHED
for P in O-Bassoon O-Bowed O-Reed O-Wind; do
  [ -f "plugins/$P/tests/ui_tip_render_check.js" ] && \
    { node "plugins/$P/tests/ui_tip_render_check.js" > "/tmp/s71-tip-$P.txt" 2>&1; echo "$P tip gate exit=$?"; }
done
[ -f plugins/O-Contrabass/tests/ui_frontend_check.js ] && \
  { node plugins/O-Contrabass/tests/ui_frontend_check.js > /tmp/s71-fe-Contrabass.txt 2>&1; echo "O-Contrabass fe gate exit=$?"; }
git diff --quiet -- plugins/O-Bowed/tests/ui_tip_render_check.js \
                    plugins/O-Reed/tests/ui_tip_render_check.js \
                    plugins/O-Wind/tests/ui_tip_render_check.js \
  || { echo "FAIL: a wave-4f-owned two-language gate file was modified"; exit 1; }

# ── 6. the shared module CSS was NOT used as the floor site
git diff --quiet -- modules/tuning/scala-tuning-engine/snippets/tuning-panel.css \
  || { echo "FAIL: a floor was written into the SHARED module CSS — five consumers"; exit 1; }
echo "TASK 2 VERIFY OK"
]]></automated>
    <human-check>For each of the five, SUMMARY records: the coverage `N of M` line before and after (M must have grown), the `elementFromPoint` hit element at each new click target, and the count of the 8 dynamic-only captions that the harness could not reach, with the reason.</human-check>
  </verify>

  <done>
All five `i18n-states.json` parse and reach the tuning surface; `check-ui-labels` measures real tuning-panel elements on all five and the coverage denominator grew; `measure-ui --report all` ran on all five with findings recorded; `boot-all-uis --strict-tips` reads the same O-Bells census as before; every existing plugin gate file still passes and none of the three wave-4f-owned two-language gate files was modified; no floor was written into the shared module CSS; the unreachable dynamic captions are named and counted.
  </done>

  <commit>
Re-check branch and `git status --short` immediately before committing.

```
git commit -- plugins/O-Bassoon/tests plugins/O-Bowed/tests plugins/O-Contrabass/tests \
  plugins/O-Reed/tests plugins/O-Wind/tests \
  plugins/O-Bassoon/Resources/ui/index.html plugins/O-Bowed/Resources/ui/index.html \
  plugins/O-Contrabass/Source/ui/public/index.html plugins/O-Reed/Resources/ui/index.html \
  plugins/O-Wind/Resources/ui/index.html
```

Include the index.html paths only if a geometry floor was actually written into one; drop them from the pathspec otherwise (`git commit -- <paths>` takes only TRACKED files and an unchanged path is harmless but noisy).
  </commit>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Five MINOR releases — bumps, CHANGELOGs, builds, ONE cold auval sweep, PLUGINS.md, deferred-items</name>

  <files>
plugins/O-Bassoon/CMakeLists.txt
plugins/O-Bowed/CMakeLists.txt
plugins/O-Contrabass/CMakeLists.txt
plugins/O-Reed/CMakeLists.txt
plugins/O-Wind/CMakeLists.txt
plugins/O-Bassoon/CHANGELOG.md
plugins/O-Bowed/CHANGELOG.md
plugins/O-Contrabass/CHANGELOG.md
plugins/O-Reed/CHANGELOG.md
plugins/O-Wind/CHANGELOG.md
PLUGINS.md
.planning/quick/260906-s71-localize-the-shared-scala-tuning-engine-/260906-s71-deferred-items.md
  </files>

  <read_first>
CLAUDE.md "Build Requirements" — the cache-clearing and dual-variant sweep sequence, and that `./scripts/build-and-install.sh` Phase 4 already performs it and emits `⚠ Sweeping ALTERNATE-variant` when an orphan is found.
Each plugin's CHANGELOG.md top entry for its house format (heading shape, date, section names).
  </read_first>

  <precondition>Tasks 1 and 2 are committed and `git status --short` is clean of their paths. A build against an uncommitted module edit would produce a binary nothing in git explains.</precondition>

  <action>
**A. Bump five CMake versions — with the two-arm reader, unconditionally (N9).**

RE-READ each `VERSION` line before editing; four are quoted, O-Contrabass is unquoted. If any file's value differs from the planning-time observation in this plan's table, **HALT and report** — do not adapt silently. Preserve each file's existing quoting exactly.

MINOR on all five (a whole tab becomes localized, user-visible): O-Bassoon 1.4.0→1.5.0, O-Bowed 1.7.1→1.8.0, O-Contrabass 1.8.3→1.9.0, O-Reed 1.4.1→1.5.0, O-Wind 1.19.1→1.20.0.

**B. CHANGELOG entry per plugin**, in that plugin's own format. Each entry must name: `scala-tuning-engine` module **v3.1.0**; that the tuning tab is now localized; the language list for THAT plugin (`en/fr` on four, `en/fr/zh-Hans` on O-Bassoon); the 37 keys; and — on O-Bassoon — that the Chinese is `reviewed: 'bt'` with no native reader, the same disclosure every zh CHANGELOG in this rollout carries.

**C. Build and install all five**, one at a time, EACH IN THE BACKGROUND (600 s watchdog — a foreground build stalls the executor and a stalled agent's context cannot be resumed):

```
./scripts/build-and-install.sh O-Bassoon
```
…and the same for O-Bowed, O-Contrabass, O-Reed, O-Wind. Watch each run's Phase 4 for the `⚠ Sweeping ALTERNATE-variant` line and record whether it fired (a `-dev` ↔ unsuffixed orphan pins Logic's registry slot).

**D. ONE cold `auval` sweep at the END of the batch, in the background.**

A cold `auval` after an install rescans the whole AU registry (~15 min); five interleaved sweeps would cost an hour of watchdog risk for nothing. Read each plugin's triple off `auval -a` — **never guess it** — and note that `auval -v` takes **three unquoted words** (`auval -v aumu Xxxx Ouar`), not a quoted triple. Record each PASS/FAIL verbatim, including any pre-existing failure that this task did not cause.

**E. PLUGINS.md — write the NEW version from CMake, do not increment the stale row.**

Four of five rows are stale as of planning time (O-Bowed 1.6.2, O-Reed 1.3.1, O-Wind 1.18.2, O-Contrabass 1.8.2 against CMake 1.7.1/1.4.1/1.19.1/1.8.3). Set each row to the value now in CMakeLists and update the date column. After editing, run the duplicate check CLAUDE.md specifies and confirm empty output.

**F. Write `260906-s71-deferred-items.md`** in this task's directory. It must cover, at minimum:

1. **D1 is CLOSED**, by option 1, and what **wave 4f still owes**: the `zh-Hans` column on the 37 new keys for O-Bowed, O-Reed and O-Wind (O-Contrabass too, though it is not a 4f plugin — say so explicitly), and the **N12 enumeration-form census** on the three `ui_tip_render_check.js` gate files, which this task deliberately did not touch.
2. **What the extended `check-i18n` exposed elsewhere** — the full step-B repo-wide red set, which arm of the disposition was taken (general CMake follow vs the `tuning-panel.js`-only narrowing), and for the narrowing arm, the four module files still unscanned by name (`preset-manager.js`, `analog-eq-unit.js`, `compressor-unit.js`, `webview-drop-streaming.js`) with their consuming plugins.
3. **The stale `modules/registry.yaml` `used_by`** for scala-tuning-engine: it lists O-Bells, O-Formant and O-IntonationPad (forks with private copies) and omits O-Reed, O-Wind and O-MicrotonalSampler. Left as found by decision; nothing should be driven from it until it is rebuilt from the CMake grep.
4. **O-MicrotonalSampler is untouched by decision** and its private copy now shares key names with the module, so a future re-sync is a diff and not a redesign. Name the two keys that exist only in the MTS copy (`label.totalSpan`, `label.tuningPanelUnavailable`).
5. Any **dynamic captions the harness could not reach** (Task 2 step F), any **pre-existing French wrap** found on these five, any **Z5 collision** resolved or left open on O-Bassoon, and every **plan prediction that turned out false** — wave 4e recorded fifteen and the count is the finding.
  </action>

  <verify>
    <automated><![CDATA[
set -e
cd /Users/taylorbrook/Dev/VST-development

# ── 1. two-arm CMake reader: the five versions are the intended MINOR bumps
ver() {  # two-arm: quoted or bare
  perl -0777 -ne 'print "$1\n" if /^\s*VERSION\s+"?([0-9]+\.[0-9]+\.[0-9]+)"?/m' "plugins/$1/CMakeLists.txt" | head -1
}
[ "$(ver O-Bassoon)"    = "1.5.0"  ] || { echo "FAIL O-Bassoon version: $(ver O-Bassoon)"; exit 1; }
[ "$(ver O-Bowed)"      = "1.8.0"  ] || { echo "FAIL O-Bowed version: $(ver O-Bowed)"; exit 1; }
[ "$(ver O-Contrabass)" = "1.9.0"  ] || { echo "FAIL O-Contrabass version: $(ver O-Contrabass)"; exit 1; }
[ "$(ver O-Reed)"       = "1.5.0"  ] || { echo "FAIL O-Reed version: $(ver O-Reed)"; exit 1; }
[ "$(ver O-Wind)"       = "1.20.0" ] || { echo "FAIL O-Wind version: $(ver O-Wind)"; exit 1; }

# quoting style preserved: O-Contrabass stays UNQUOTED, the other four stay quoted
grep -qE '^\s*VERSION\s+[0-9]+\.[0-9]+\.[0-9]+\s*$' plugins/O-Contrabass/CMakeLists.txt \
  || { echo "FAIL: O-Contrabass VERSION acquired quotes"; exit 1; }
for P in O-Bassoon O-Bowed O-Reed O-Wind; do
  grep -qE '^\s*VERSION\s+"[0-9]+\.[0-9]+\.[0-9]+"' "plugins/$P/CMakeLists.txt" \
    || { echo "FAIL: $P VERSION lost its quotes"; exit 1; }
done

# ── 2. each CHANGELOG names its new version AND the module version
for PV in "O-Bassoon 1.5.0" "O-Bowed 1.8.0" "O-Contrabass 1.9.0" "O-Reed 1.5.0" "O-Wind 1.20.0"; do
  set -- $PV
  grep -q "$2" "plugins/$1/CHANGELOG.md" || { echo "FAIL: $1 CHANGELOG lacks $2"; exit 1; }
  grep -qi '3\.1\.0' "plugins/$1/CHANGELOG.md" || { echo "FAIL: $1 CHANGELOG does not name module v3.1.0"; exit 1; }
done

# ── 3. installed bundles exist for all five (dev or release suffix)
for P in O-Bassoon O-Bowed O-Contrabass O-Reed O-Wind; do
  ls -d ~/Library/Audio/Plug-Ins/Components/${P}*.component >/dev/null 2>&1 \
    || { echo "FAIL: no installed AU component for $P"; exit 1; }
  ls -d ~/Library/Audio/Plug-Ins/VST3/${P}*.vst3 >/dev/null 2>&1 \
    || { echo "FAIL: no installed VST3 for $P"; exit 1; }
done

# ── 4. PLUGINS.md rows agree with CMakeLists, and no duplicate rows
for PV in "O-Bassoon 1.5.0" "O-Bowed 1.8.0" "O-Contrabass 1.9.0" "O-Reed 1.5.0" "O-Wind 1.20.0"; do
  set -- $PV
  grep -E "^\| $1 \|" PLUGINS.md | grep -q "$2" \
    || { echo "FAIL: PLUGINS.md row for $1 is not $2"; grep -E "^\| $1 \|" PLUGINS.md; exit 1; }
done
D=$(grep "^| O-" PLUGINS.md | awk -F'|' '{print $2}' | sort | uniq -d)
[ -z "$D" ] || { echo "FAIL: duplicate PLUGINS.md rows: $D"; exit 1; }

# ── 5. deferred-items exists and covers the five required subjects
F=.planning/quick/260906-s71-localize-the-shared-scala-tuning-engine-/260906-s71-deferred-items.md
[ -f "$F" ] || { echo "FAIL: deferred-items.md missing"; exit 1; }
for T in "D1" "wave 4f" "zh-Hans" "N12" "used_by" "O-MicrotonalSampler"; do
  grep -qi -- "$T" "$F" || { echo "FAIL: deferred-items.md does not cover '$T'"; exit 1; }
done

# ── 6. the module and every consumer table are still green after the bumps
node scripts/check-i18n.js > /tmp/s71-checki18n-final.txt 2>&1
grep -q 'ALL CHECKS PASS' /tmp/s71-checki18n-final.txt || { tail -40 /tmp/s71-checki18n-final.txt; exit 1; }
echo "TASK 3 VERIFY OK"
]]></automated>
    <human-check>SUMMARY records, per plugin: the `auval` triple read off `auval -a` and its verbatim PASS/FAIL, whether Phase 4 emitted `⚠ Sweeping ALTERNATE-variant`, and — visually, in the Standalone or a DAW — that the tuning tab renders in French on all five and in Chinese on O-Bassoon with no bare `label.` key name anywhere on it.</human-check>
  </verify>

  <done>
Five CMake versions at their MINOR bumps with quoting style preserved; five CHANGELOGs naming both the plugin version and module v3.1.0; five plugins built and installed; ONE cold auval sweep run at the end with every triple read off `auval -a` and every verdict recorded; PLUGINS.md rows agree with CMakeLists with no duplicates; `260906-s71-deferred-items.md` covers D1 closure, wave 4f's remaining debt, the extended gate's repo-wide exposure, the stale `used_by`, and the MTS relationship; repo-wide `check-i18n` still passes.
  </done>

  <commit>
Re-check `git branch --show-current` and `git status --short` immediately before EACH commit. One commit per plugin, then one for the shared files.

```
git commit -- plugins/O-Bassoon/CMakeLists.txt plugins/O-Bassoon/CHANGELOG.md
git commit -- plugins/O-Bowed/CMakeLists.txt plugins/O-Bowed/CHANGELOG.md
git commit -- plugins/O-Contrabass/CMakeLists.txt plugins/O-Contrabass/CHANGELOG.md
git commit -- plugins/O-Reed/CMakeLists.txt plugins/O-Reed/CHANGELOG.md
git commit -- plugins/O-Wind/CMakeLists.txt plugins/O-Wind/CHANGELOG.md
git commit -- PLUGINS.md .planning/quick/260906-s71-localize-the-shared-scala-tuning-engine-
```

Never `git add -A`. **Never tag** — tagging happens only under `/publish`.
  </commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n table -> DOM | Translated strings are written into the page; a string containing markup would open an injection path |
| shared module -> five plugin binaries | One file edit reshapes five shipping products at once |
| CMakeLists text -> gate file list | `check-i18n` now reads a path out of a build file and opens it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-s71-01 | Tampering | i18n.js translated strings | medium | mitigate | check-i18n assertion 9 already fails any string literal in i18n.js containing `<`; the 37 rows are plain text copied from an already-gated table and the repo-wide run in Task 1 verify re-fires it |
| T-s71-02 | Denial of Service | five consumer binaries via one module edit | high | mitigate | module edit and all five consumer row-sets land in ONE change set (Task 1 step E); a missing key renders the key name rather than throwing, and Task 3 builds + auvals all five before the task closes |
| T-s71-03 | Information Disclosure | check-i18n opening a path parsed from CMakeLists | low | accept | the file is read-only, repo-local, and resolved against `repoRoot`; a nonexistent path is reported as a check failure rather than silently skipped |
| T-s71-04 | Tampering | shared `tuning-panel.css` used as a geometry floor site | medium | mitigate | Task 2 verify asserts `git diff --quiet` on that file; floors go in each consumer's own `#tuning-container`-scoped CSS |
| T-s71-SC | Tampering | npm/pip/cargo installs | n/a | n/a | **No package-manager installs in this task.** Every tool used (`node scripts/*.js`, `cmake`, `ninja`, `auval`) is already present in the repo or the OS; no `## Package Legitimacy Audit` is required |
</threat_model>

<success_criteria>
- The tuning tab renders fully in French on O-Bassoon, O-Bowed, O-Contrabass, O-Reed and O-Wind, and fully in Simplified Chinese on O-Bassoon, with no bare `label.` key name anywhere on it.
- `check-i18n` names `modules/tuning/scala-tuning-engine/js/tuning-panel.js` in its `[12]` scanned-module line on all five consumers and exits 0 repo-wide.
- The extended gate was observed RED on the five consumers before the module was keyed, and that observation is recorded verbatim in SUMMARY.
- Module at v3.1.0 in its header, `module.yaml` and `modules/registry.yaml`; exactly 37 keys; 3 `data-i18n-vars` sites; `refreshPanelI18n()` after every keyed injection.
- Each consumer's LABELS count grew by exactly 37; only O-Bassoon carries a `zh-Hans` column on them.
- All five build, install and auval on this machine at their new MINOR versions; PLUGINS.md agrees with CMakeLists and has no duplicate rows.
- O-MicrotonalSampler is byte-unchanged; O-Bowed/O-Reed/O-Wind `ui_tip_render_check.js` are byte-unchanged; the shared module CSS is byte-unchanged.
- `260906-s71-deferred-items.md` exists and hands wave 4f a specific, sized debt.
</success_criteria>

<output>
Write `.planning/quick/260906-s71-localize-the-shared-scala-tuning-engine-/260906-s71-SUMMARY.md` when done, plus the per-task summaries this repo's quick tasks use.
</output>
