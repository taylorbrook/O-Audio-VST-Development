---
phase: quick-260719-hn4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - research/framework-updates-2026-07.md
  - .planning/quick/260719-hn4-check-juce-and-framework-updates-review-/260719-hn4-EVIDENCE.md
autonomous: true
requirements: [QT-hn4-frameworks]
must_haves:
  truths:
    - "research/framework-updates-2026-07.md exists and reviews every pinned framework dependency (JUCE, ANIRA + ONNX Runtime, nanoflann, umappp, pluginval) with a version delta and a recommended action"
    - "The JUCE note-expression vendored-patch re-base risk is quantified from an ACTUAL 8.0.9 to 8.0.14 diff of the two override files, not assumed"
    - "JUCE breaking-change exposure is evidenced by concrete repo greps (createEditor direct calls, AlertWindow::show, getPlatformSpecificData, VSTPluginFormatHeadless)"
    - "Recommended upgrade order, verification strategy (build matrix, auval, verify-au-link.sh, pluginval, render harnesses, Dorico 3-point microtonal smoke test), and rollback strategy are documented"
    - "Up-to-date items (pluginval) and defer items are explicitly called out; no upgrade is performed this session"
  artifacts:
    - research/framework-updates-2026-07.md
    - .planning/quick/260719-hn4-check-juce-and-framework-updates-review-/260719-hn4-EVIDENCE.md
  key_links:
    - "vendored/JUCE-overrides/ override files <-> upstream JUCE 8.0.14 counterparts (patch re-base sizing)"
    - "ANIRA GIT_TAG <-> the ONNX Runtime version it provisions"
    - ".github/workflows/build-and-release.yml JUCE_VERSION <-> local /Users/taylorbrook/JUCE pin"
---

<objective>
Produce a single review + integration-plan document that assesses upstream updates to every framework this repo pins (JUCE, ANIRA + ONNX Runtime, nanoflann, umappp, pluginval) and lays out how to integrate them safely into the VST development system.

The upstream version deltas are already established (see orchestrator scout findings, reproduced in <context>). This plan does NOT re-discover versions and does NOT perform any upgrade. It gathers deeper changelog / breaking-change evidence, then synthesizes a decision-ready document.

Purpose: Give the developer a grounded, risk-ranked integration plan before any JUCE bump is attempted — the highest-risk item being the re-base of the vendored note-expression patch (JUCE-NE-PATCH) onto JUCE 8.0.14.
Output: research/framework-updates-2026-07.md (the deliverable) plus an EVIDENCE.md working file in the quick-task directory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Ground-truth version deltas (from orchestrator scout, 2026-07-19). Do NOT re-discover these.
#
# JUCE (core framework, all 37 plugins)
#   pinned: 8.0.9  (local /Users/taylorbrook/JUCE AND CI JUCE_VERSION in .github/workflows/build-and-release.yml)
#   upstream latest: 8.0.14 (2026-06-22). Intermediates: 8.0.10, 8.0.11, 8.0.12, 8.0.13.
#   Known breaking changes since 8.0.9 (BREAKING_CHANGES.md, v8.0.13):
#     1. AudioProcessor::createEditor() made private — direct CALLS break; plugin OVERRIDES unaffected.
#     2. AlertWindow::show() return-value semantics changed.
#     3. AudioPluginInstance::getPlatformSpecificData() removed (hosting-side; likely unused here).
#     4. VSTPluginFormatHeadless API signature changes/removals (hosting-side; likely unused here).
#   CRITICAL repo constraint: vendored/JUCE-overrides/ holds 2 patched files carrying the note-expression
#     patch (JUCE-NE-PATCH), copied over the vanilla JUCE download in CI:
#       modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp   (4215 lines locally)
#       modules/juce_audio_processors/utilities/juce_VST3ClientExtensions.h  (307 lines locally)
#     Upgrading JUCE requires RE-BASING this patch onto the 8.0.14 versions of those files. The .cpp
#     churns frequently between releases -> highest-risk item. Local-dev patch source:
#     scripts/juce-patches/note-expression-juce-8.0.4.patch (actually an 8.0.9 diff; filename rename pending).
#     Re-apply wrapper: scripts/apply-juce-patches.sh. AU-link gate: scripts/verify-au-link.sh.
#
# ANIRA (O-Texture only; used solely for its ONNX Runtime per-platform download logic)
#   pinned: v2.0.3 (plugins/O-Texture/CMakeLists.txt GIT_TAG); ORT provisioned = 1.19.2
#   upstream latest: v2.2.1 (intermediates v2.1.0, v2.2.0); upstream ORT is at 1.27.1 but is coupled to the ANIRA tag.
#
# nanoflann (O-TextureForge): pinned v1.6.2, upstream v1.10.0. Header-only KD-tree; low risk.
# umappp (O-TextureForge): pinned v3.2.0, upstream v3.3.2. Header-only UMAP; low risk.
# pluginval: 1.0.4 installed == upstream latest. UP TO DATE — no action.
# Toolchain (CMake 4.2.1, Xcode 26.3, ninja 1.13.2): working; out of scope.
#
# REPO STATE CAVEAT: working tree has extensive uncommitted changes; HEAD is 5 commits ahead of origin/main.
# This plan is DOCUMENT-ONLY. Do NOT modify JUCE, CMakeLists, vendored/, or any plugin build in this session.

# Repo files the document must reference (already confirmed to exist):
@.github/workflows/build-and-release.yml
@scripts/apply-juce-patches.sh
@scripts/verify-au-link.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather upstream evidence via curl + repo grep</name>
  <files>.planning/quick/260719-hn4-check-juce-and-framework-updates-review-/260719-hn4-EVIDENCE.md</files>
  <action>
Collect the deeper evidence needed to write the integration plan and record it in EVIDENCE.md. Use Bash/curl against pinned version tags (never branch tips) so results are reproducible. Structure EVIDENCE.md with these H2 sections, one per evidence area:

## JUCE Override Diff — Size the note-expression patch re-base.
Fetch the upstream 8.0.14 versions of the two override files from raw.githubusercontent.com at tag 8.0.14:
  - modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp
  - modules/juce_audio_processors/utilities/juce_VST3ClientExtensions.h
Also fetch the 8.0.9 versions of the same two files (the base the local vendored overrides were patched from). Produce two diffs per file: (a) upstream 8.0.9 vs upstream 8.0.14 (isolates pure upstream churn the re-base must absorb), and (b) local vendored override (which is the 8.0.9 base plus JUCE-NE-PATCH) vs upstream 8.0.14 (approximates re-base effort). Record for each file: total changed-line count, hunk count, and whether the note-expression insertion regions (search for the JUCE-NE-PATCH markers in the local vendored copy, then locate the surrounding upstream context) still exist unmodified in 8.0.14 or fall inside churned regions. Conclude with a re-base difficulty rating (trivial / moderate / high) per file, grounded in those numbers.

## JUCE BREAKING_CHANGES — Fetch BREAKING_CHANGES.md at tag 8.0.14 and extract every entry dated/versioned 8.0.10 through 8.0.14. Confirm the four scout-listed breaking changes and capture any additional ones. Quote the exact wording for each.

## Repo Exposure Grep — Prove (do not assume) whether the repo is affected by each JUCE breaking change. Run repo-wide greps and record hit counts plus file paths:
  - direct createEditor() calls (pattern: \.createEditor()  and  ->createEditor()) — expected 0; this de-risks breaking change 1.
  - AlertWindow::show usages.
  - getPlatformSpecificData usages.
  - VSTPluginFormatHeadless usages.
Note that plugin createEditor() OVERRIDES are unaffected (only direct calls break), so a raw grep for the bare token would over-count — report the direct-call pattern separately from override declarations.

## ANIRA to ONNX Runtime — For ANIRA tags v2.1.0, v2.2.0, v2.2.1, determine which ONNX Runtime version each provisions. Inspect the ANIRA repo (anira-project/anira) at each tag: fetch the cmake download-logic file(s) that set the ORT version (grep the tag's cmake directory for an onnxruntime version string) and/or the release notes via the GitHub releases API. Record a tag -> ORT-version table and note whether the O-Texture bundle/rpath post-build step (embeds libanira + libonnxruntime, per MEMORY) would need version-string updates.

## Header-only Deps — Fetch nanoflann CHANGELOG (jlblancoc/nanoflann, v1.6.2 -> v1.10.0) and umappp changelog (check both libscran/umappp and LTLA/umappp, v3.2.0 -> v3.3.2). Record only: any API/signature breaks affecting O-TextureForge usage, and a header-only-compat verdict (drop-in / needs-changes).

## Up-to-date and Out-of-scope — Record that pluginval 1.0.4 == latest (no action) and that the toolchain (CMake/Xcode/ninja) is out of scope.

If any curl fails (rate limit, moved repo, renamed file), record the failure and the fallback used; do not silently omit a section. Keep raw command output trimmed to the load-bearing numbers. Do NOT modify any JUCE, vendored/, CMake, or plugin build file — this task only reads upstream and writes EVIDENCE.md.
  </action>
  <verify>
    <automated>test -f .planning/quick/260719-hn4-check-juce-and-framework-updates-review-/260719-hn4-EVIDENCE.md && test $(grep -Ec '^## ' .planning/quick/260719-hn4-check-juce-and-framework-updates-review-/260719-hn4-EVIDENCE.md) -ge 6</automated>
  </verify>
  <done>EVIDENCE.md exists with at least 6 H2 evidence sections: JUCE override-diff line/hunk counts + re-base rating per file, BREAKING_CHANGES 8.0.10-8.0.14 excerpts, repo exposure grep hit counts, an ANIRA-tag -> ORT-version table, nanoflann/umappp compat verdicts, and the pluginval up-to-date / toolchain out-of-scope note.</done>
</task>

<task type="auto">
  <name>Task 2: Author research/framework-updates-2026-07.md review + integration plan</name>
  <files>research/framework-updates-2026-07.md</files>
  <action>
Synthesize EVIDENCE.md (Task 1) into the decision-ready deliverable at research/framework-updates-2026-07.md. Lead with a one-line precondition banner: this is a review + plan only; no upgrade is performed; the working tree is dirty and HEAD is 5 commits ahead of origin/main, so no JUCE/CMake/vendored/plugin build changes may be made until that is resolved and the developer green-lights execution.

Write these H2 sections in order:

## Summary — A table with columns: dependency | pinned | upstream latest | delta | risk | recommended action. One row each for JUCE, ANIRA (+ ORT), nanoflann, umappp, pluginval. Actions are one of: upgrade now / stage + verify / defer / up-to-date (no action).

## Per-Dependency Review — A subsection per dependency covering what changed and why it does or does not matter here. JUCE: summarize the headline 8.0.13/8.0.14 changes (gui_basics compile-time + Component painting perf, Windows min target 1607, Windows rendering/resizing, reduced build-tree depth, MIDI fixes) and their relevance to a webview-heavy, macOS-primary + Windows-CI suite. ANIRA: the v2.0.3 -> v2.2.1 delta, the coupled ORT version from Task 1, and that O-Texture uses ANIRA only for ORT provisioning (not linked as an inference engine). nanoflann + umappp: header-only, drop-in verdict from Task 1. pluginval: at latest.

## JUCE Breaking-Change Review — Table each of the four breaking changes with its repo-exposure grep result from Task 1 and a verdict (blocks upgrade / no exposure / needs code change). Explicitly state that direct createEditor() calls were grepped and the count found (expected 0, meaning plugin overrides are unaffected), and that AlertWindow::show / getPlatformSpecificData / VSTPluginFormatHeadless are hosting-side APIs with the measured exposure.

## Risk Assessment — Rank the risks; the note-expression vendored-patch re-base (JUCE-NE-PATCH onto 8.0.14) is the headline high-risk item, quantified with the Task 1 diff numbers (line/hunk counts + per-file re-base rating). Describe the re-base procedure concretely: regenerate the patch against fresh JUCE 8.0.14 sources for the two files, update the vendored copies under vendored/JUCE-overrides/, re-run scripts/apply-juce-patches.sh, and confirm the JUCE-NE-PATCH markers survive the CI grep in build-and-release.yml (the workflow greps both override files for the marker). Include the follow-up housekeeping: rename scripts/juce-patches/note-expression-juce-8.0.4.patch to reflect the true base version, and regenerate it against 8.0.14 once the re-base lands.

## Recommended Upgrade Order — Ordered list with rationale. JUCE first (touches all 37 plugins; do it on a dedicated branch, bump both the local /Users/taylorbrook/JUCE pin and CI JUCE_VERSION together). Then ANIRA/ORT for O-Texture only (staged, since it changes the embedded dylib versions + rpath post-build step). nanoflann/umappp are optional low-risk bumps that can ride along or be deferred. pluginval: no action.

## Verification Strategy — The gate battery to run after the JUCE bump: full build matrix (macOS VST3 + AU locally, Windows VST3 via CI build-and-release.yml), auval + scripts/verify-au-link.sh per plugin (the AU-link gate that caught the Steinberg-symbol leak), pluginval, the offline render harnesses (compiled under JUCE_WEB_BROWSER=0 — guard createEditor with #if JUCE_WEB_BROWSER per the known harness-vs-webview pitfall), and the Dorico 3-point microtonal smoke test (quarter-sharp ~269 Hz, no attack zipper, polyphonic isolation) as the note-expression regression gate — this is the true acceptance test that the patch re-base preserved microtonal playback. Add DAW smoke tests (Logic/Ableton) with cache-clear per CLAUDE.md.

## Rollback Strategy — How to back out: revert JUCE_VERSION in build-and-release.yml and the local pin, restore the prior vendored/JUCE-overrides/ files and the prior patch file from git, and git-revert the branch. Per-dependency rollback for ANIRA (restore GIT_TAG + dylib version strings) and header-only deps (revert the pin). Note the branch-based approach makes rollback a branch discard.

## Deferred and Up-to-date — Explicitly list pluginval (up to date, no action), toolchain (out of scope), and any dep the review recommends deferring (e.g. nanoflann/umappp if low value), with the reason.

Ground every claim in Task 1 evidence or the confirmed repo facts in <context>; do not invent version numbers. Do NOT modify any build file — write only research/framework-updates-2026-07.md.
  </action>
  <verify>
    <automated>test -f research/framework-updates-2026-07.md && test $(grep -Ec '^## (Summary|Per-Dependency Review|JUCE Breaking-Change Review|Risk Assessment|Recommended Upgrade Order|Verification Strategy|Rollback Strategy|Deferred and Up-to-date)' research/framework-updates-2026-07.md) -eq 8</automated>
  </verify>
  <done>research/framework-updates-2026-07.md exists with all 8 required H2 sections, a per-dependency summary table, the JUCE breaking-change exposure verdicts backed by Task 1 greps, the quantified note-expression patch re-base risk, and documented upgrade order / verification / rollback strategies. No build file is modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream GitHub -> local text | curl fetches upstream JUCE/ANIRA/nanoflann/umappp source + changelogs into EVIDENCE.md for review only; nothing fetched is compiled or executed this session |
| review doc -> future upgrade | the document recommends actions; actual JUCE/dep changes are deferred to a separate, gated session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-hn4-01 | Tampering | curl'd upstream source used to size the patch re-base | low | mitigate | Fetch only at pinned version tags (8.0.14 / v2.2.1 / v1.10.0 / v3.3.2), never branch tips; content is diffed/read, never compiled or executed in this session |
| T-hn4-02 | Information disclosure | EVIDENCE.md + research doc | low | accept | Both are local planning/research artifacts containing only public upstream facts; no secrets |
| T-hn4-SC | Tampering | npm/pip/cargo/CMake FetchContent installs | high | mitigate | No package installs occur this session — the deliverable is a paper review only; the document flags ANIRA/ORT + header-only bumps as changes to be verified in a later gated upgrade session, not installed now |
</threat_model>

<verification>
- research/framework-updates-2026-07.md exists and contains all 8 required H2 sections.
- The document covers all five pinned dependencies (JUCE, ANIRA + ORT, nanoflann, umappp, pluginval).
- The note-expression patch re-base risk cites concrete diff numbers from EVIDENCE.md, not assumptions.
- JUCE breaking-change verdicts cite the repo-exposure grep counts.
- No JUCE, vendored/, CMake, or plugin build file was modified (git status shows only the two new/edited doc files).
</verification>

<success_criteria>
- A decision-ready research/framework-updates-2026-07.md that the developer can act on to schedule the JUCE 8.0.14 (and optional ANIRA/header-only) upgrade.
- Highest-risk item (note-expression patch re-base) is quantified and has a concrete re-base + verification + rollback procedure.
- Up-to-date (pluginval) and deferred items are explicitly stated.
- Zero changes to build-affecting files this session.
</success_criteria>

<output>
Create `.planning/quick/260719-hn4-check-juce-and-framework-updates-review-/260719-hn4-SUMMARY.md` when done.
</output>