# 260907-bgx MANIFEST — reference-grep gate and deletion record

Generated by the executor at Task 1. All observations re-measured live; none inherited
from the plan.

## Live counts (re-verified, Task 1 Step 2)

| Glob | Disk | Tracked (`git ls-files`) | Plan expected |
|---|---|---|---|
| `plugins/*/.planning/i18n-index-draft.html` | 33 | 33 | 33 |
| `plugins/*/.planning/i18n-labels-skeleton.js` | 37 | 37 | 37 |
| `plugins/*/.planning/i18n-inventory.tsv` | 37 | 37 | 37 |

All three sets are **fully tracked** — `.planning/` is not gitignored. Deletion mechanism is
therefore `git rm` (never `git rm --cached`), one path-scoped commit per plugin.

Skeleton set and inventory set are **identical** (verified by `diff` of the two derived
plugin lists). The live draft list is **identical** to the plan's hardcoded 33 (verified by
`diff`); the derived list, not the hardcoded one, drives the deletions.

Sibling-only plugins (skeleton + inventory present, draft already gone) — derived by
`comm -13`: **O-AnalogSaturation, O-Bitrot, O-Emulator, O-SimpleReverb**. Out of scope.

## check-i18n baseline (Task 1 Step 1)

Captured **before** any deletion, to `check-i18n-before.txt`.
Exit code **0**, 1949 lines, final line `ALL CHECKS PASS — 43 localized plugin(s)`.

## Classified reference grep (Task 1 Step 3)

Command (unfiltered by extension — reaches `scripts/`, `tests/`, every `CMakeLists.txt`,
`.claude/`, `.github/`, any `Makefile`, and every script at repo root and under `plugins/*/`):

```
grep -rIn -e 'i18n-index-draft\.html' -e 'i18n-labels-skeleton\.js' -e 'i18n-inventory\.tsv' . \
  --exclude-dir=.git --exclude-dir=node_modules --exclude-dir=build --exclude-dir=backups \
  --exclude-dir=Builds --exclude-dir=libs
```

**124 hits across 50 paths.** Every hit classified into exactly one bucket; the buckets sum
to 124, so no hit is unclassified.

| Class | Hits | Paths | Verdict |
|---|---|---|---|
| SELF | 37 | `plugins/*/.planning/i18n-labels-skeleton.js` L2 (all 37) | not a block — the target file's own generated header |
| NARRATIVE | 77 | 13 `.md` files (below) | not a block — record only |
| GENERATOR/DOC | 10 | `scripts/i18n-extract.js` (7), `scripts/i18n-extract-README.md` (3) | not a block — proved write-only in Step 4 |
| **BLOCK** | **0** | — | — |

### NARRATIVE hits (prose; recorded, never blocking)

| Path | Hits |
|---|---|
| `.planning/quick/260907-bgx-.../260907-bgx-PLAN.md` | 30 |
| `.planning/quick/260901-c3s-.../260901-c3s-PLAN.md` | 15 |
| `.planning/quick/260901-c3s-.../260901-c3s-SUMMARY.md` | 7 |
| `.planning/quick/260901-akh-.../260901-akh-IMPLEMENTATION-PLAN.md` | 5 |
| `.planning/quick/260826-ieq-.../260826-ieq-STAGE-L-BRIEF.md` | 4 |
| `research/i18n-zh-hans-localization.md` | 3 |
| `.planning/quick/260901-akh-.../260901-akh-RESEARCH.md` | 3 |
| `.planning/quick/260904-g5l-.../260904-g5l-PLAN.md` | 2 |
| `.planning/quick/260901-akh-.../260901-akh-PLAN.md` | 2 |
| `.planning/quick/260826-ieq-.../260826-ieq-SUMMARY.md` | 2 |
| `.planning/quick/260826-ieq-.../260826-ieq-PLAN.md` | 2 |
| `.planning/STATE.md` | 1 |
| `.planning/quick/260826-ieq-.../260826-ieq-STAGE-L-EXECUTOR-REPORT.md` | 1 |

**Zero hits** in any `CMakeLists.txt`, `.github/`, `.claude/`, test, hook, `Makefile`, `*.sh`,
`*.py`, or any script other than the generator. There is no `package.json` in the repo.

## Step 4 — generator proved WRITE-ONLY (independently, not on the plan's word)

Read-construct grep:

```
grep -rIn -E "(readFileSync|readFile|createReadStream|require|import|fetch|open)\s*\(?[^)]*i18n-(index-draft|labels-skeleton|inventory)" . \
  --exclude-dir=.git --exclude-dir=node_modules --exclude-dir=build --exclude-dir=backups
```

→ **no output** (exit 1). No read construct anywhere in the repo targets these basenames.

Each of the three path joins in `scripts/i18n-extract.js` is **immediately followed by
`fs.writeFileSync`** — verified by reading L688-706:

| Line | Construct | Next line |
|---|---|---|
| 692 | `const tsv = path.join(outDir, 'i18n-inventory.tsv')` | `fs.writeFileSync(tsv, …)` |
| 696 | `const draft = path.join(outDir, 'i18n-index-draft.html')` | `fs.writeFileSync(draft, …)` |
| 700 | `const skel = path.join(outDir, 'i18n-labels-skeleton.js')` | `fs.writeFileSync(skel, …)` |

The file's other three `readFileSync` calls (L570 `indexPath`, L662 scanned js sources,
L1182 a generic `f`) are covered by the read-construct grep's empty result — none resolves to
a target basename. L65-67 is a usage doc-comment; L1264 is the generated header template
emitted *into* the skeleton. **GENERATOR/DOC confirmed: not a consumer.**

## Step 5 — no glob dependency

`grep -rIn "\.planning" scripts/` returns 16 lines. Exactly **three** are path *resolutions*;
the rest are comments, READMEs, and patch context:

| Path resolution | Target |
|---|---|
| `scripts/serve-ui.js:335` | `path.join(pluginRoot(name, repoRoot), '.planning', 'params.tsv')` — **LIVE READ** |
| `scripts/verify-suite-battery.sh:39` | a fixed `260719-m5p` quick dir |
| `scripts/i18n-extract.js:679` | `outDir` — the **write** side of the generator |

**No script globs `.planning/*.tsv`.** Nothing resolves a target basename by wildcard.

### ⚠ The live neighbour

`plugins/<P>/.planning/params.tsv` is read by `serve-ui.js` and sits in the **same directory**
as our target `i18n-inventory.tsv`. Every deletion in this task names `i18n-inventory.tsv`
**basename-exact**; no command in this task may ever glob `.planning/*.tsv`.

Baseline: **22 on disk, 22 tracked.** Re-asserted equal in Task 3 Step 4.
(O-Gain, the tracer plugin, has no `params.tsv` — its absence after the tracer commit is not
evidence of a slip.)

## Per-plugin verdict — 33 CLEAR, 0 BLOCKED

No plugin is named by a BLOCK-class hit, and no generic-path BLOCK hit exists that would
disqualify all 33. Every plugin below is **CLEAR**.

| # | Plugin | Verdict | Commit |
|---|---|---|---|
| 1 | O-AnalogEQ | CLEAR | `287927fa` |
| 2 | O-Bassoon | CLEAR | `0229df50` |
| 3 | O-Bells | CLEAR | `d50a4fc1` |
| 4 | O-Bowed | CLEAR | `3f5e777e` |
| 5 | O-Chorus | CLEAR | `afd92412` |
| 6 | O-Comp | CLEAR | `60ff3b9f` |
| 7 | O-Contrabass | CLEAR | `fc9c41e6` |
| 8 | O-Detune | CLEAR | `9e54d841` |
| 9 | O-Formant | CLEAR | `58ee8ed6` |
| 10 | O-Freeze | CLEAR | `f9289a1a` |
| 11 | O-FreqPulse | CLEAR | `dd65fb0f` |
| 12 | O-Gain | CLEAR | `c268f72a` |
| 13 | O-GrainScatter | CLEAR | `ed521aed` |
| 14 | O-Lyrica | CLEAR | `adcc5739` |
| 15 | O-MicrotonalSampler | CLEAR | `1efc9ff4` |
| 16 | O-MultiBandCompressor | CLEAR | `42e7c6d7` |
| 17 | O-Octagon | CLEAR | `3841a8fa` |
| 18 | O-Orbit | CLEAR | `cc5cd8f1` |
| 19 | O-Prism | CLEAR | `6e52a976` |
| 20 | O-Reed | CLEAR | `c646b80c` |
| 21 | O-ReverseDelay | CLEAR | `4e082828` |
| 22 | O-simpleAdditive | CLEAR | `47d55276` |
| 23 | O-simpleBeatmaker | CLEAR | `f578d1f5` |
| 24 | O-simpleFM | CLEAR | `f99c44d9` |
| 25 | O-simpleGrain | CLEAR | `371c87f1` |
| 26 | O-simplePhysicalModelSynth | CLEAR | `7b016ad9` |
| 27 | O-simpleSampler | CLEAR | `10c8ecd1` |
| 28 | O-simpleSubtractive | CLEAR | `de2dc7d2` |
| 29 | O-Tapestop | CLEAR | `25f371b6` |
| 30 | O-Texture | CLEAR | `48feb7da` |
| 31 | O-TextureForge | CLEAR | `4989a968` |
| 32 | O-Tremolo | CLEAR | `be645be2` |
| 33 | O-Wind | CLEAR | `5d093179` |

**33 commits, 33 CLEAR, 0 BLOCKED, 0 SKIPPED.** Every commit was asserted to touch exactly 3
files by Guard 6 inside the loop; no guard tripped at any iteration. No plugin required the
`N BLOCKED` count substitution the plan's Task 2 `<done>` allows for.

## Post-deletion results (Task 3)

### Final glob counts

| Glob | Before | After | Expected |
|---|---|---|---|
| `i18n-index-draft.html` | 33 | **0** | 0 |
| `i18n-labels-skeleton.js` | 37 | **4** | 4 |
| `i18n-inventory.tsv` | 37 | **4** | 4 |

The 4 survivors are exactly the out-of-scope sibling-only set:
`O-AnalogSaturation`, `O-Bitrot`, `O-Emulator`, `O-SimpleReverb`.

### Gates

| Gate | Result |
|---|---|
| `node scripts/check-i18n.js` before vs after | **byte-identical** (`diff` empty), exit **0** both |
| `node scripts/check-ui-labels.js --plugin O-Gain` | exit **0** — `== ALL CHECKS PASSED ==`, 28/28 `[data-i18n]` elements visible, every requested resource served |

The check-ui-labels run is **not** INCONCLUSIVE: it served the correct plugin (O-Gain's own
350 x 500 shell, its own decoration image) with no port-bind failure, so the known
concurrent-session port-clash hazard did not fire.

99 tracked files were removed and the repo-level i18n gate — which covers all 43 localized
plugins — produced identical output. No assertion anywhere was reading the removed files.

### Live neighbour survived

`plugins/*/.planning/params.tsv`: **22 on disk, 22 tracked** — unchanged from baseline.
No wildcard slip. (O-Gain never had one, so its absence there is not evidence of a slip.)

### Working tree

`git status --porcelain -- plugins/` is **empty**. `plugins/O-Orbit/libs/SAF` is **silent** —
the submodule was never staged; the mandatory submodule guard passed before all 33 commits.
The 8 `260906-uu7` files are still untracked and untouched.

**Environment note (not caused by this task):** `research/wavetable-synthesis-3d-geometry.md`
appeared as a new untracked file partway through the run — it was absent at the precondition
check and lies outside every pathspec used here. A concurrent session in this shared checkout
wrote it. Left alone.
