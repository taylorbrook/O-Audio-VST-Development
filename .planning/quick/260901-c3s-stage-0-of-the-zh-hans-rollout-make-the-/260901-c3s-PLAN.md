---
phase: quick-260901-c3s
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [D-F, P1, P2, P3, P4, P5, P6, P7]
files_modified:
  - scripts/i18n-canon.js
  - scripts/check-i18n.js
  - scripts/check-ui-labels.js
  - scripts/i18n-extract.js
  - "plugins/*/{Source/ui/public,Resources/ui}/js/{app.js,main.js,i18n_init.js,sampler-app.js} and plugins/*/{Source/ui/public,Resources/ui}/index.html — the 43 shipping canon copies enumerated at runtime (see Task 1)"
files_deleted:
  - plugins/O-AnalogSaturation/.planning/i18n-index-draft.html
  - plugins/O-Bitrot/.planning/i18n-index-draft.html
  - plugins/O-Emulator/.planning/i18n-index-draft.html
  - plugins/O-SimpleReverb/.planning/i18n-index-draft.html

estimate:
  tokens: 110000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A plugin whose i18n.js table still declares two languages passes `node scripts/check-i18n.js` unchanged (exit 0, ALL CHECKS PASS, 43 localized plugins)."
    - "A plugin whose i18n.js table declares three languages ('en','fr','zh-Hans') also passes check-i18n — proven on a scratch copy outside the repo, per P3/P4."
    - "A tooltip or LABELS entry carrying reviewed: 'mt' passes assertion [5]; one carrying reviewed: 'oops' fails it — at BOTH reviewed sites, per P5."
    - "fr semantics are unchanged at every site: fr still demands a boolean, and the unreviewed-French worklist still counts the same numbers."
    - "check-ui-labels reads the plugin's own LANGUAGES rather than a hard-coded pair, in all six language loops, per P6."
    - "The extractor skeleton emits a zh-Hans row alongside the fr row, per P7."
    - "Every UI still boots: boot-all-uis reports clean 43 / 43 with 0 DEAD bindings."
    - "Zero plugin i18n.js tables changed; all 43 still declare exactly two languages."
  artifacts:
    - scripts/i18n-canon.js (canon region line 216 language-agnostic)
    - scripts/check-i18n.js (assertions [1], [5] and the three language loops language-agnostic)
    - scripts/check-ui-labels.js (six language loops language-agnostic, failure text relabelled)
    - scripts/i18n-extract.js (skeleton emits a zh-Hans row)
    - "43 rewritten shipping canon copies under plugins/"
  key_links:
    - "check-i18n.js assertion [6] byte-compares each plugin's extracted applyI18n/initI18n region (comment-stripped, whitespace-collapsed) against the canon region in scripts/i18n-canon.js. P1 without P2 turns 43 plugins red simultaneously — this is why Task 1 is ONE commit."
    - "check-ui-labels.js has no access to LANGUAGES today: the canon exposes only window.__setLanguage / __setLabel / __reapplyI18n. P6 therefore needs a NEW disk-side read of the plugin's i18n.js — it cannot come from the page without changing the canon (which would mean a second 43-file sweep)."
---

<objective>
Stage 0 of the zh-Hans rollout: make every repo-level i18n gate language-agnostic so that a
2-language plugin and a 3-language plugin both pass while the rollout is mid-flight.

Purpose: this is a hard prerequisite for every later stage. Until the gates stop hard-coding a
two-language pair, the first plugin to gain a zh-Hans entry fails check-i18n, and the assertions
that were supposed to protect the zh content go green vacuously instead.

Output: prerequisites P1 through P7 from
`.planning/quick/260901-akh-research-and-plan-chinese-localization-a/260901-akh-IMPLEMENTATION-PLAN.md`
§ "Stage 0 — Repo-wide prerequisites", plus locked decision (f). Two path-scoped commits.

HARD SCOPE BOUNDARY — non-negotiable:
- ZERO plugin `i18n.js` tables change. No `zh-Hans` entry is added anywhere in this stage.
- No version bumps. No CHANGELOG entries. No builds. No `/improve` invocation.
- P8 (the three `scripts/i18n-zh-*.js` tools) is Stage 1 work and is NOT in this plan.
</objective>

<locked_decisions>
All six decisions in the implementation plan's "Decisions to confirm before Stage 0" section were
CONFIRMED by the developer on 2026-09-01. They are given. Do not re-open, re-litigate, or ask
about them:

| id | Decision | Bearing on this plan |
|----|----------|----------------------|
| (a) | Simplified first | context only |
| (b) | schema key `zh-Hans` | the exact string P3 accepts and P7 emits |
| (c) | `reviewed` is an enum `'mt' \| 'bt' \| 'native'` for zh-Hans ONLY; `fr` keeps its boolean | P5, both sites |
| (d) | ship at `'bt'`; `'native'` is never a gate | P5 accepts all three; none is privileged |
| (e) | pilot O-Chorus | the plugin used for the dry runs in Task 3 |
| (f) | DELETE the four unserved `i18n-index-draft.html` drafts before the P1/P2 sweep | Task 1, step 1 |
</locked_decisions>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260901-akh-research-and-plan-chinese-localization-a/260901-akh-IMPLEMENTATION-PLAN.md
@research/i18n-zh-hans-localization.md
</context>

<verified_ground_truth>
Everything below was re-verified against the working tree on 2026-09-01, at plan time. Where the
implementation plan and the research document cite a line, the live file agrees — no drift found.

**P1 site — `scripts/i18n-canon.js`**
- Line 216 is the only occurrence of the clamp in the file. Line 162 already holds the correct
  guard: `uiLanguage = LANGUAGES.includes(lang) ? lang : 'en';`
- Line 216 sits INSIDE the canon comment block (sentinels at `:113` `<<<I18N_CANON_V2_BEGIN>>>` and
  `:226` `<<<I18N_CANON_V2_END>>>`), so editing it changes the canon that assertion [6] compares
  against. That is exactly why P2 must ride in the same commit.

**P2 target list — 43 shipping copies**
```
grep -rl "applyI18n(code === 'fr'" plugins            # -> 47 files
grep -rl "applyI18n(code === 'fr'" plugins | grep -v i18n-index-draft.html   # -> 43 files
```
The 43 are NOT all named `app.js`. Measured filename distribution:
`app.js` 20 · `index.html` 19 (inline `<script type="module">` plugins) · `main.js` 2 ·
`i18n_init.js` 1 · `sampler-app.js` 1.
Each of the 43 contains the clamp literal **exactly once** (verified per file). The substitution is
therefore an unambiguous 1-for-1 replacement, not a pattern match that could over-fire.

**There is NO canon sync script.** Verified: `ls scripts/` contains no `sync-*` / `i18n-canon-sync*`;
`scripts/check-i18n.js` has no `--write` / `--fix` / `--sync` flag (its only flags are `--plugin`,
`--root`, and an ignored `--strict-v2`); `scripts/i18n-canon.js` has no CLI entry point at all
(it ends at `module.exports`). The sweep must be performed by the executor, driven off the grep
list above.

**Assertion [6] normalizes before comparing** (`check-i18n.js:776-777`,
`extractI18nRegion(...)` then `normalise(...)` — comments stripped, whitespace collapsed, compared
to `CANON_REGION_V2`). A single-line substitution is therefore sufficient and correct; no
whole-region re-paste is needed or wanted.

**Submodule guard.** `plugins/O-Orbit/libs/SAF` is a git submodule (currently pinned at
`b6fe188 (v1.3.4)`). O-Orbit's canon copy lives at `plugins/O-Orbit/Resources/ui/js/app.js`, which
is OUTSIDE the submodule. Nothing under `libs/SAF` is touched, staged, or committed by this plan.

**P3/P4/P5 sites — `scripts/check-i18n.js`** (all confirmed at the cited lines)
- `:43` — header assertion text, line 5 of the summary block
- `:497-498` — assertion [1], the hard-coded two-language join
- `:516` — assertion [1] `I18N` en/fr field loop
- `:548` — assertion [2] `tipBodied` body-presence loop
- `:576-578` — assertion [5], `I18N` (tooltip) reviewed flag
- `:605` — assertion [1] `LABELS_EARLY` field loop
- `:623-625` — assertion [5], `LABELS_EARLY` reviewed flag
- OUT OF SCOPE for P5, leave alone: `:581` and `:628-629`, the `reviewed === false` counters that
  feed the "unreviewed French worklist" report. P5 changes flag VALIDITY, not the worklist count.

**P6 sites — `scripts/check-ui-labels.js`**
Six language loops, confirmed by grep: `:598, :658, :669, :703, :717, :851`. Line **635** is
`await page.evaluate((l) => window.__setLanguage(l), 'en');` inside the `const enControl = []`
block that starts at `:634` — an EN→EN control spread, NOT a language loop. Editing it corrupts
the vacuity control. It is not in the six.

**P6 blocker discovered at plan time — read this before starting Task 2.**
`check-ui-labels.js` cannot currently obtain a plugin's `LANGUAGES`. The canon exposes only
`window.__setLanguage` (`i18n-canon.js:183`), `window.__setLabel` (`:187`) and
`window.__reapplyI18n` (`:201`) — `LANGUAGES` is not on `window`. Exposing it would edit the canon
and force a SECOND 43-file sweep, which is forbidden here.
The disk-side read is the intended path, and the pieces are already in scope:
`const built = S.buildRoot(plugin, { repoRoot });` at `:487` yields `built.uiRoot`
(`serve-ui.js:463`), and all 43 plugins hold their table at exactly `<uiRoot>/js/i18n.js`
(verified: 33 under `Source/ui/public/js/`, 10 under `Resources/ui/js/`, and all 43 declare the
identical `export const LANGUAGES = ['en', 'fr'];`). Parse it from that file.
`check-ui-labels.js:39` already names `check-i18n.js` as the precedent for exactly this.

**P7 site — `scripts/i18n-extract.js:1252`**, the sole `reviewed:` emit in the file:
`body.push(\`        fr: { t: 'TODO', b: '', reviewed: false },\`);`

**Gate invocations and their success text** (all confirmed from the sources)
- `node scripts/check-i18n.js` — exits with the failure COUNT (`:1198 process.exit(failed)`);
  final line on success is `ALL CHECKS PASS — 43 localized plugin(s)`; a `-- canon` block prints
  `canon v2    43  <names>` and an `OFF CANON` line only when a plugin has drifted.
- `node scripts/check-i18n.js --root <DIR> --plugin <Name>` — `--root` works on a tree OUTSIDE the
  repo. Verified at plan time: copying `plugins/O-Chorus` to a scratch `<DIR>/plugins/O-Chorus` and
  running with `--root <DIR>` produced `ALL CHECKS PASS — 1 localized plugin(s)`, exit 0. This is
  the mechanism for every scratch test in Task 3.
- `node scripts/check-ui-labels.js --plugin <Name> [--root DIR] [--verbose]`
- `node scripts/boot-all-uis.js --strict-tips` — prints `clean:  N / N` and
  `DEAD bindings:  N across M plugin(s)`; exits 2 on a DEAD binding under `--strict-tips`.
</verified_ground_truth>

<tasks>

<task type="tracer">
  <name>Task 1: decision (f) + P1 + P2 — delete the four drafts, make the canon language-agnostic, sweep all 43 shipping copies, ONE atomic commit</name>

  <files>
scripts/i18n-canon.js
plugins/O-AnalogSaturation/.planning/i18n-index-draft.html (DELETE)
plugins/O-Bitrot/.planning/i18n-index-draft.html (DELETE)
plugins/O-Emulator/.planning/i18n-index-draft.html (DELETE)
plugins/O-SimpleReverb/.planning/i18n-index-draft.html (DELETE)
the 43 shipping canon copies enumerated by the grep in step 2 below
  </files>

  <precondition>
`git branch --show-current` reports `main`, and `git status --short` shows no other session's
staged or modified work under `plugins/` or `scripts/`. Another session shares this checkout's
index and HEAD. Halt and report if either check surprises you.
  </precondition>

  <action>
<!-- planner-discipline-allow: applyI18n(code === 'fr' ? 'fr' : 'en') -->
<!-- planner-discipline-allow: applyI18n(code) -->

Do these four steps in order. They land as ONE commit because assertion [6] byte-compares every
plugin's canon region against `scripts/i18n-canon.js`: the moment step 2 changes the canon, all 43
copies are stale until step 3 finishes.

**Step 1 — decision (f), delete the four unserved drafts.**
Remove `plugins/O-AnalogSaturation/.planning/i18n-index-draft.html` and the same path under
O-Bitrot, O-Emulator and O-SimpleReverb. All four are tracked in git (verified). They are not
served by any `getResource()` branch and not covered by any gate; their only live effect is to
inflate the sweep's grep from 43 to 47. Delete them FIRST so the sweep's target list in step 2 is
unambiguous by construction.

**Step 2 — P1, `scripts/i18n-canon.js:216`.**
Inside the canon comment region, replace the `.then(...)` callback so the resolved language code is
passed straight through to `applyI18n` instead of being clamped to a two-value ternary. The
resulting line is exactly:

`            .then((code) => applyI18n(code))`

(twelve leading spaces, matching the surrounding `.then` / `.catch` chain). Change NOTHING else in
the file — not the surrounding comment prose, not the indentation of neighbouring lines, not the
sentinels. The guard this relies on already exists at `:162`
(`uiLanguage = LANGUAGES.includes(lang) ? lang : 'en';`) and is what makes the clamp redundant.
Getting this form right is the whole risk of the task: it is what means a fourth language never
requires another 43-file sweep. Do not memorialize the old expression in a comment anywhere in
this file — the gate in `<verify>` counts occurrences across the whole file, comments included,
and that is deliberate.

**Step 3 — P2, the 43-copy sweep.**
Derive the target list at run time; do not hand-type it:

```bash
grep -rl "applyI18n(code === 'fr'" plugins | grep -v 'i18n-index-draft.html' | sort
```

After step 1 the `grep -v` is belt-and-braces — the drafts are already gone — but keep it so the
command is correct regardless of step order. Assert the list has exactly **43** entries before you
touch anything; if it does not, STOP and report the count rather than sweeping a list you cannot
account for.

In each of those 43 files perform the identical 1-for-1 substring replacement made in step 2: the
clamped ternary form becomes `applyI18n(code)`. Each file contains that substring exactly once
(verified at plan time across all 43), so a literal substring replacement is exact — do not use a
loose regex, do not reformat, do not rewrite surrounding lines, do not re-paste the whole canon
region. The files span five shapes (`app.js` x20, `index.html` x19 with the canon inline in a
`<script type="module">`, `main.js` x2, `i18n_init.js` x1, `sampler-app.js` x1); the replacement is
the same in all of them.

There is no sync script to lean on (verified: none in `scripts/`, no `--write` mode on
`check-i18n.js`, no CLI on `i18n-canon.js`), so drive the sweep yourself from the grep list —
a short node or shell loop over that exact list, doing a literal replace-once per file.

Assertion [6] normalises (comments stripped, whitespace collapsed) before comparing, so a
one-line substitution is sufficient — no whitespace-perfect re-paste is required.

Touch nothing under `plugins/O-Orbit/libs/SAF` — it is a git submodule. O-Orbit's canon copy is at
`plugins/O-Orbit/Resources/ui/js/app.js`, outside it. Do not stage the submodule pointer.

**Step 4 — commit, path-scoped, per CLAUDE.md.**
Immediately before committing (not earlier in the session), re-run:

```bash
git branch --show-current
git status --short
```

Confirm you are on `main` and that everything modified/deleted under `plugins/` and `scripts/` is
yours. If another session's changes appear under those paths, do NOT widen the commit — narrow it
to the explicit file list instead and say so in the summary. Then:

```bash
git commit -- scripts/i18n-canon.js plugins
```

`git commit -- <pathspec>` commits the working-tree state of those paths and ignores whatever else
sits in the shared index — that bypass is the point. It records the four deletions because all
four files are tracked. If `git status --short` showed foreign work under `plugins/`, substitute
the explicit 48-path form (`scripts/i18n-canon.js` + the 43 swept files + the 4 deleted drafts)
rather than the bare `plugins` pathspec.

Commit message, one line plus body:
`fix(i18n): canon passes the stored language code through unchanged (P1+P2)`
Body must state: 43 shipping copies re-synced in the same commit because assertion 6 byte-compares
them; four unserved i18n-index-draft.html drafts deleted per locked decision (f); zero i18n.js
tables changed.
  </action>

  <verify>
    <automated>
# 1. the clamp is gone from the canon, comments included
test "$(grep -c "code === 'fr'" scripts/i18n-canon.js)" = "0"
# 2. the pass-through form is present, at line 216
grep -n 'applyI18n(code))' scripts/i18n-canon.js | grep -q '^216:'
# 3. the clamp is gone from every plugin copy AND the four drafts are deleted
test -z "$(grep -rl "applyI18n(code === 'fr'" plugins)"
# 4. exactly 43 copies now carry the pass-through form
test "$(grep -rl 'applyI18n(code))' plugins | wc -l | tr -d ' ')" = "43"
# 5. the four drafts are gone
test "$(ls plugins/*/.planning/i18n-index-draft.html 2>/dev/null | wc -l | tr -d ' ')" = "0"
# 6. THE gate: canon drift across the whole suite
node scripts/check-i18n.js | tee /dev/stderr | grep -q '^ALL CHECKS PASS — 43 localized plugin(s)$'
node scripts/check-i18n.js | grep -qE '^  canon v2 +43 '
node scripts/check-i18n.js | grep -q 'OFF CANON' && exit 1 || true
# 7. no i18n.js table changed, and none gained a third language
test -z "$(git diff HEAD~1 --name-only -- 'plugins/*/js/i18n.js' 'plugins/*/*/js/i18n.js' 'plugins/*/*/*/js/i18n.js')"
test "$(grep -rh 'export const LANGUAGES' $(find plugins -name i18n.js) | sort -u | wc -l | tr -d ' ')" = "1"
# 8. the submodule pointer was not staged
test -z "$(git diff HEAD~1 --name-only -- plugins/O-Orbit/libs/SAF)"
    </automated>
  </verify>

  <done>
`node scripts/check-i18n.js` exits 0 with `ALL CHECKS PASS — 43 localized plugin(s)` and
`canon v2   43`, no `OFF CANON` line. The clamped ternary appears nowhere under `scripts/` or
`plugins/`. The four drafts are deleted. All 43 `i18n.js` tables are byte-unchanged and still
declare exactly two languages. One commit on `main`; `plugins/O-Orbit/libs/SAF` untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: P3–P7 — make check-i18n, check-ui-labels and the extractor language-agnostic, second commit</name>

  <files>
scripts/check-i18n.js
scripts/check-ui-labels.js
scripts/i18n-extract.js
  </files>

  <action>
<!-- planner-discipline-allow: ['en', 'fr'] -->
<!-- planner-discipline-allow: en,fr -->

Five edits across three files, one commit. Every one of them has the same shape: a hard-coded
two-language literal becomes something derived from the data. Nowhere does `fr` behaviour change.

**P3 — `scripts/check-i18n.js:497-498`, assertion [1].**
The joined-array equality currently admits one shape only. Widen it to admit exactly two shapes:
the current two-language pair, or that pair followed by `zh-Hans` as a third element in that
position. Nothing else passes — an arbitrary third code, a reordering, or a superset must still
fail. Update the message string on `:498` in the same edit so a failure reports both accepted
shapes rather than the single old one; a message that names only one shape is a gate that lies
about its own contract. Keep the `Array.isArray(LANGUAGES)` guard.

Accepting BOTH shapes rather than migrating to the three-language shape is the entire point: it is
what lets plugins convert one at a time through Stages 2-5 instead of in a big bang.

**P4 — `scripts/check-i18n.js:516, 548, 605`.**
Three loops iterate a literal language pair. Derive each from the `LANGUAGES` value already
destructured out of the plugin's own module at `:495` — it is in scope at all three sites. After
this, a two-language plugin iterates two and a three-language plugin iterates three, with no
further edit ever needed.

Do not touch `:570-573` (assertion [4], the fr-passthrough check) or `:612-618` (its LABELS twin)
in this task — they read `e.fr` by name and generalizing them is Stage 1+ work, not P4. P4 is
precisely the three field-presence/body-presence loops named above.

**P5 — `scripts/check-i18n.js:576-578` AND `:623-625`. Both sites. This is the one that bites.**
`:576-578` guards `I18N` (tooltips); `:623-625` guards `LABELS_EARLY`. Patch only the first and
every one of the 2,367 zh label entries fails assertion [5] the moment translation starts — the
worst possible time to discover it.

At each site, the reviewed-flag validity test becomes per-language:
- for `fr`: unchanged — still `typeof ... === 'boolean'`, still required, still the same failure
  when absent. Locked decision (c).
- for `zh-Hans`: valid iff the value is one of the three strings `'mt'`, `'bt'`, `'native'`.
  None is privileged over the others as a gate — locked decision (d) makes `'bt'` the ship bar,
  but `'native'` and `'mt'` are both structurally valid here. Any other value, including a
  boolean, including `undefined`, fails.
- a language present in `LANGUAGES` other than `en`/`fr`/`zh-Hans` is not expected in this stage;
  fail closed rather than silently skipping it.

Since no `zh-Hans` entry exists yet anywhere, the zh branch is unexercised by the suite run —
which is exactly why Task 3 exercises it on a scratch copy. A branch that has never once been
executed is not a gate.

Update the three assertion strings so they describe the new contract: the header summary line at
`:43`, the tooltip message at `:578`, and the LABELS message at `:625`.

Leave `:581` and `:628-629` alone — those count `reviewed === false` for the unreviewed-French
worklist. P5 is about flag VALIDITY; the worklist counters keep their current fr semantics and
their current numbers.

**P6 — `scripts/check-ui-labels.js`, six loops: `:598, :658, :669, :703, :717, :851`.**
Read the P6 blocker note in `<verified_ground_truth>` before starting. `LANGUAGES` is not reachable
from the page, and putting it there would edit the canon and force a second 43-file sweep. So:

1. Add a disk-side read. `const built = S.buildRoot(plugin, { repoRoot });` already exists at
   `:487`; `built.uiRoot` is exported by `serve-ui.js:463`. Read `<built.uiRoot>/js/i18n.js` — the
   table sits at exactly that path in all 43 plugins — and parse its `export const LANGUAGES = [...]`
   declaration into an array. Follow `check-i18n.js`'s precedent, which `check-ui-labels.js:39`
   already names. If the file is missing or the declaration does not parse, FAIL the run with a
   named assertion; do not silently fall back to a default pair, because a silent fallback is
   indistinguishable from the vacuous pass this whole prerequisite exists to prevent.
2. Replace the literal pair in each of the six loops with that parsed array. Assertions 5/6/7
   become per-non-English-language deltas against EN: for each non-`en` language, compare that
   language's geometry to EN's, and report which language failed.
3. Relabel the failure/report text at those assertions so it names the language that failed rather
   than saying "French" unconditionally. `check-ui-labels.js:28-30` warns in its own header that an
   unlabelled language failure "reads as a mysterious regression in a file that never mentions
   French" — with a second language in play, hard-coded "French" wording becomes actively
   misleading. Relabel while extending; do not defer it.

**DO NOT EDIT LINE 635.** It is `window.__setLanguage(l), 'en'` inside the `const enControl = []`
block opening at `:634` — an EN→EN control spread that measures how much the page moves on its own
between samples. Turning it into a language loop destroys the vacuity control, and the resulting
gate would go green for the wrong reason. Six loops, not seven.

**P7 — `scripts/i18n-extract.js:1252`.**
The generated skeleton emits one `fr` placeholder row per new key. Give it a sibling `zh-Hans` row
immediately after, quoted because of the hyphen, with the same TODO title, the same empty body, and
`reviewed: 'mt'` — the machine-draft level from locked decision (c), which is the correct starting
state for a key nobody has translated yet. The emitted line is:

`        'zh-Hans': { t: 'TODO', b: '', reviewed: 'mt' },`

matching the eight-space indentation of the `fr` row above it. The `fr` row stays exactly as it is.
This is the only `reviewed:` emit in the file. Miss it and every key added after Stage 0 is born
zh-less, silently, forever.

**Commit.** Re-check `git branch --show-current` and `git status --short` immediately before
committing — not earlier — then:

```bash
git commit -- scripts/check-i18n.js scripts/check-ui-labels.js scripts/i18n-extract.js
```

Message: `fix(i18n): gates and extractor read LANGUAGES instead of a hard-coded pair (P3-P7)`
Body must name all five prerequisites and state that fr semantics are unchanged at both reviewed
sites and that no plugin file was touched.
  </action>

  <verify>
    <automated>
# P3/P4 — no hard-coded language-pair literal survives in either gate
test "$(grep -c "\['en', *'fr'\]" scripts/check-i18n.js)" = "0"
test "$(grep -c "\['en', *'fr'\]" scripts/check-ui-labels.js)" = "0"
# P3 — the widened assertion accepts the three-language shape by name
grep -q "zh-Hans" scripts/check-i18n.js
# P5 — the enum branch exists at BOTH reviewed sites
test "$(grep -c "'mt'" scripts/check-i18n.js)" -ge "2"
test "$(grep -c "'native'" scripts/check-i18n.js)" -ge "2"
# P5 — fr still demands a boolean at both sites
test "$(grep -c "'boolean'" scripts/check-i18n.js)" -ge "2"
# P6 — line 635 is still the EN control-spread line, unedited
sed -n '635p' scripts/check-ui-labels.js | grep -q "'en'"
sed -n '634p' scripts/check-ui-labels.js | grep -q 'enControl'
# P6 — check-ui-labels now reads the table from disk
grep -q 'LANGUAGES' scripts/check-ui-labels.js
# P7 — the extractor emits the zh sibling row
grep -q "'zh-Hans': { t: 'TODO', b: '', reviewed: 'mt' }," scripts/i18n-extract.js
grep -q "fr: { t: 'TODO', b: '', reviewed: false }," scripts/i18n-extract.js
# regression: the suite still passes unchanged at two languages
node scripts/check-i18n.js | tee /dev/stderr | grep -q '^ALL CHECKS PASS — 43 localized plugin(s)$'
# no plugin file touched by this commit
test -z "$(git diff HEAD~1 --name-only -- plugins)"
    </automated>
  </verify>

  <done>
Neither gate contains a hard-coded language-pair literal. `check-i18n.js` accepts both the
two-language and the `zh-Hans` three-language shape at assertion [1], derives its three field loops
from the file's own `LANGUAGES`, and validates the reviewed flag per-language at BOTH the tooltip
and LABELS sites with `fr` semantics untouched. `check-ui-labels.js` parses `LANGUAGES` from the
plugin's `i18n.js` on disk, drives all six language loops from it, names the failing language in its
messages, and line 635's EN control spread is intact. The extractor emits a `zh-Hans` sibling row.
`node scripts/check-i18n.js` still reports `ALL CHECKS PASS — 43 localized plugin(s)`. Second commit
on `main`, touching only the three script files.
  </done>
</task>

<task type="auto">
  <name>Task 3: exit gates — prove language-agnostic on a scratch 3-language copy, and prove nothing regressed on the real 43</name>

  <files>
(no repo files modified unless a gate fails; scratch trees live only under the session scratchpad)
  </files>

  <precondition>
Tasks 1 and 2 are committed. Playwright is resolvable (`serve-ui.js:524 resolvePlaywright()`) —
`check-ui-labels` and `boot-all-uis` both need a browser. If it is not installed, report that and
run the non-browser gates; do not install anything.
  </precondition>

  <action>
Run the full Stage 0 exit battery. Nothing here writes to the repo. Every scratch tree lives under
`/private/tmp/claude-501/-Users-taylorbrook-Dev-VST-development/66d17596-1699-47b1-ae34-4879ca426d7a/scratchpad`
and NOTHING from it is ever committed — this is the whole reason `check-i18n.js` and
`check-ui-labels.js` accept `--root`.

**Gate A — the real suite, unchanged at two languages.** `node scripts/check-i18n.js`. This is the
Stage 0 exit criterion in its exact intended form: the gates became language-agnostic while every
table still holds two languages. Confirm `ALL CHECKS PASS — 43 localized plugin(s)`, `canon v2  43`,
no `OFF CANON`, and that the unreviewed-French worklist TOTAL is unchanged from before Task 2 (P5
must not have perturbed the worklist counters at `:581` / `:628-629`).

**Gate B — the scratch three-language copy (proves P3 and P4).**
Build a scratch root OUTSIDE the repo:

```
SCRATCH=/private/tmp/claude-501/-Users-taylorbrook-Dev-VST-development/66d17596-1699-47b1-ae34-4879ca426d7a/scratchpad/zh-scratch
rm -rf "$SCRATCH" && mkdir -p "$SCRATCH/plugins"
cp -R plugins/O-Chorus "$SCRATCH/plugins/O-Chorus"
```

Copy it AFTER Task 1, so the scratch carries the post-P1 canon and passes assertion [6]. Then in
the SCRATCH copy only, edit `plugins/O-Chorus/Source/ui/public/js/i18n.js`:
- `LANGUAGES` gains `'zh-Hans'` as a third element;
- every `I18N` key gains a `'zh-Hans'` entry with a non-empty `t`, a `b`, and `reviewed: 'bt'`;
- every `LABELS` key gains a `'zh-Hans'` entry with a non-empty `t` and `reviewed: 'bt'`.
O-Chorus has 28 entries (10 tooltip, 18 label) — small enough to do exhaustively, and it is the
locked pilot (decision (e)). The `t` values need not be real Chinese; they must merely differ from
the English, since assertion [4]'s passthrough check will fire on an exact copy.

Run `node scripts/check-i18n.js --root "$SCRATCH" --plugin O-Chorus`. It must print
`ALL CHECKS PASS — 1 localized plugin(s)` and exit 0. `--root` on an out-of-repo tree was verified
working at plan time. A failure here means P3 or P4 is incomplete.

**Gate C — the reviewed enum, both sites (proves P5).** Four one-line perturbations of the Gate B
scratch, re-running the same command each time:
1. a TOOLTIP entry's `reviewed` set to `'mt'` -> PASS (exit 0)
2. that same entry set to `'oops'` -> FAIL, and the failure text must be assertion [5]
3. a LABELS entry's `reviewed` set to `'mt'` -> PASS
4. that same entry set to `'oops'` -> FAIL on assertion [5]
Steps 3 and 4 are the ones that matter — they are what proves the `:623-625` site was patched and
not just the tooltip site. Restore to `'bt'` after each. Also confirm that setting a `fr` entry's
`reviewed` to `'mt'` still FAILS, which proves fr's boolean requirement survived.

**Gate D — check-ui-labels reads LANGUAGES (proves P6).** `node scripts/check-ui-labels.js --plugin
O-Chorus --verbose` against the REAL repo (two languages) must pass exactly as it did before. Then
run it against the Gate B scratch with `--root "$SCRATCH" --plugin O-Chorus`: the run must visibly
exercise three languages rather than two — capture the concrete evidence, whether that is a printed
language list, three language-switch passes, or per-language assertion 5/6/7 lines naming
`zh-Hans`. A run that merely exits 0 is NOT evidence that the loops read `LANGUAGES`; the whole
failure mode P6 guards against is a vacuous pass. If the scratch run cannot be made to demonstrate
three passes, say so plainly rather than claiming the gate.

**Gate E — every UI still boots.** `node scripts/boot-all-uis.js --strict-tips`. Must report
`clean:  43 / 43` and `DEAD bindings:  0`. This is the gate that catches P1 having broken the
runtime language load — the canon is live code, and a botched substitution in any of the 43 copies
surfaces here as a boot failure or a dead binding rather than in check-i18n.

**Gate F — scope containment.** Confirm across both commits that no `i18n.js` changed, that all 43
still declare two languages, that no `CMakeLists.txt` / `CHANGELOG.md` / `STATUS.md` was touched,
and that `plugins/O-Orbit/libs/SAF` is unmoved.

**If a gate fails:** fix it and land a third path-scoped commit rather than amending either of the
first two — Task 1's commit is the atomic canon sweep and must stay legible as such. Re-check
`git branch --show-current` and `git status --short` immediately before that commit too.

**Cleanup:** `rm -rf "$SCRATCH"` and any other scratch tree when done. Verify `git status --short`
shows nothing new under the repo.
  </action>

  <verify>
    <automated>
SCRATCH=/private/tmp/claude-501/-Users-taylorbrook-Dev-VST-development/66d17596-1699-47b1-ae34-4879ca426d7a/scratchpad/zh-scratch
# Gate A
node scripts/check-i18n.js | tee /dev/stderr | grep -q '^ALL CHECKS PASS — 43 localized plugin(s)$'
node scripts/check-i18n.js | grep -qE '^  canon v2 +43 '
# Gate B — three-language scratch passes
node scripts/check-i18n.js --root "$SCRATCH" --plugin O-Chorus | grep -q '^ALL CHECKS PASS — 1 localized plugin(s)$'
# Gate D — real repo dry run still passes
node scripts/check-ui-labels.js --plugin O-Chorus > /dev/null
# Gate E — all UIs boot, zero dead bindings
node scripts/boot-all-uis.js --strict-tips | tee /dev/stderr | grep -qE '^  clean: +43 / 43$'
node scripts/boot-all-uis.js --strict-tips | grep -qE '^  DEAD bindings: +0'
# Gate F — scope containment across both commits
test -z "$(git diff HEAD~2 --name-only -- '*i18n.js')"
test "$(grep -rh 'export const LANGUAGES' $(find plugins -name i18n.js) | sort -u | wc -l | tr -d ' ')" = "1"
test -z "$(git diff HEAD~2 --name-only -- '*CMakeLists.txt' '*CHANGELOG.md' '*STATUS.md')"
test -z "$(git diff HEAD~2 --name-only -- plugins/O-Orbit/libs/SAF)"
# scratch never entered the repo
test -z "$(git status --short | grep -v dispatch-isolation-sentinel)"
    </automated>
    <human-check>
Gate C's four enum perturbations and the fr-boolean negative control are one-line scratch edits and
cannot be scripted from a fixed command — report each of the five outcomes explicitly (which entry,
which value, PASS or FAIL, and which assertion number fired on the failures). Gate D's three-pass
evidence must likewise be quoted from the run output, not asserted.
    </human-check>
  </verify>

  <done>
All six gates pass with evidence: check-i18n `ALL CHECKS PASS — 43 localized plugin(s)` with
`canon v2 43` and an unchanged unreviewed-French TOTAL; the out-of-repo three-language O-Chorus
scratch also passes; `reviewed: 'mt'` passes and `reviewed: 'oops'` fails at BOTH the tooltip and
LABELS sites, while `fr` still rejects a non-boolean; a check-ui-labels run demonstrably exercises
three languages on the scratch and still passes two on the real repo; `boot-all-uis --strict-tips`
reports `clean: 43 / 43` and 0 DEAD bindings; and across both commits no `i18n.js`, CMakeLists,
CHANGELOG, STATUS.md or submodule pointer moved. All scratch trees deleted; working tree clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared git index / HEAD | Another session in this same checkout can stage work between your `git status` and your `git commit` (CLAUDE.md, "Commit discipline for concurrent sessions"). |
| submodule boundary | `plugins/O-Orbit/libs/SAF` is a separate repo whose pointer can be moved by an over-broad pathspec. |
| scratch tree ↔ repo | Scratch fixtures carrying invented `zh-Hans` content must never cross into the repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-c3s-01 | Tampering | shared git index | medium | mitigate | Path-scoped `git commit -- <paths>` only (bypasses the shared index); `git branch --show-current` + `git status --short` re-run immediately before each of the two commits, never once at session start. |
| T-c3s-02 | Tampering | `plugins/O-Orbit/libs/SAF` submodule | medium | mitigate | Never stage under `libs/SAF`; Task 1 and Task 3 both assert `git diff --name-only -- plugins/O-Orbit/libs/SAF` is empty. |
| T-c3s-03 | Tampering | 43 plugin canon copies | high | mitigate | Sweep driven off a grep-derived list asserted at exactly 43; literal replace-once per file (each contains the target substring exactly once, verified); assertion [6] byte-compare over 43/43 is the acceptance gate; P1 and P2 in ONE commit so no intermediate state exists where copies are stale. |
| T-c3s-04 | Information disclosure | scratch fixtures | low | mitigate | All scratch trees under the session scratchpad, driven via `--root`; Task 3 asserts a clean `git status --short` and deletes them. |
| T-c3s-05 | Denial of service | gate vacuity (P6) | high | mitigate | P6's disk read FAILS closed on a missing/unparseable `LANGUAGES` rather than defaulting to a pair; Gate D requires observed three-language evidence, not merely exit 0. |
| T-c3s-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs in this plan. No new dependency is added; every tool invoked (`node scripts/*.js`, `git`, `grep`) already exists in the repo. |
</threat_model>

<verification>
Stage 0 is complete when the gates became language-agnostic **without any zh content existing yet**.
The falsifiable form of that: `node scripts/check-i18n.js` passes 43/43 with every table still at
two languages (Gate A), AND the same binary passes a three-language plugin on a scratch root
(Gate B). Either alone is insufficient — Gate A alone is satisfied by changing nothing, and Gate B
alone would be satisfied by breaking the two-language path.
</verification>

<success_criteria>
- Four unserved `i18n-index-draft.html` drafts deleted (locked decision (f)).
- `scripts/i18n-canon.js:216` passes the resolved code through; the clamped ternary appears nowhere
  under `scripts/` or `plugins/`; all 43 shipping copies re-synced in the SAME commit.
- `check-i18n.js` assertion [1] accepts both the two-language and the `zh-Hans` three-language shape;
  its three field loops derive from the file's own `LANGUAGES`; the reviewed flag is validated
  per-language at BOTH `:576-578` and `:623-625` with `fr` semantics unchanged.
- `check-ui-labels.js`: all six language loops (`:598, 658, 669, 703, 717, 851`) read the plugin's
  `LANGUAGES`; line 635's EN→EN control spread untouched; failure text names the failing language.
- `i18n-extract.js:1252` emits a `'zh-Hans'` sibling row at `reviewed: 'mt'`.
- `boot-all-uis --strict-tips` clean 43/43, 0 DEAD bindings.
- Zero `i18n.js` tables changed; zero `zh-Hans` entries added to any plugin; no version bumps, no
  builds, no CHANGELOG entries.
- Exactly two path-scoped commits on `main` (a third only if a gate failed); submodule untouched.
</success_criteria>

<output>
Create `.planning/quick/260901-c3s-stage-0-of-the-zh-hans-rollout-make-the-/260901-c3s-SUMMARY.md`
when done. Record: the exact 43-file sweep list and its derivation command; the five filename shapes
encountered; the concrete Gate C outcomes (all five perturbations, by entry and value); the Gate D
three-language evidence quoted from the run; and any place where the live file disagreed with the
implementation plan's cited line numbers.

Then present the CLAUDE.md two-step handoff and STOP:
- **Step 1:** `/clear`
- **Step 2:** Stage 1 of the zh-Hans rollout (P8 — `scripts/i18n-zh-glossary.js`,
  `i18n-zh-lint.js`, `i18n-zh-backtranslate.js`), per
  `.planning/quick/260901-akh-research-and-plan-chinese-localization-a/260901-akh-IMPLEMENTATION-PLAN.md`
  § "Stage 1 — zh-Hans glossary and lint".
</output>
