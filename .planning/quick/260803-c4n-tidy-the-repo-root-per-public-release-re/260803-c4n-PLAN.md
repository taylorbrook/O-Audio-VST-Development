---
phase: quick-260803-c4n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - PUBLIC-RELEASE-READINESS.md
autonomous: true
requirements: [C4N-01, C4N-02, C4N-03, C4N-04, C4N-05]
must_haves:
  truths:
    - "The repo root contains no loose scratch render, screenshot, Finder metadata file, or tool-output directory — `ls` at root shows only project files and real project directories."
    - "The O-Bowed golden render survives on disk, byte-exact, next to the `.sha256` that attests it — sha256 still equals `93124fb8dd8223caafac5948c988a226230363d79a17323d386e9a1db34c8891`."
    - "Every file removed from the root exists outside the repository first, MD5-matched against the original, before it is removed."
    - "Nothing new becomes committable: `git add -An` over the whole repository stages zero paths after the tidy, exactly as it did before."
    - "The relocated golden render resolves to a real ignore rule via `git check-ignore -v`, and that rule is scoped to the single path — O-Contrabass's 19 tracked golden `.wav` files stay tracked."
    - "Section 6 step 13 reads as done in the same visual style and density as the thirteen already-ticked steps, naming what moved, what was archived, what was deleted, and what was deliberately left alone."
    - "Section 4.4 carries a `Resolved 2026-08-03` note whose body corrects the record on the root `e1-max-sustain` pair — that it is a superseded harness run, not the artifact `RESEARCH.md` cites."
    - "`backups/` and `logs/` are untouched, and the readiness doc says out loud that they were considered and ruled out of scope."
    - "No commit rewrites history, force-pushes, or pushes at all."
  artifacts:
    - plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav
    - "~/VST-development-root-scratch-20260803/README.txt"
    - "~/VST-development-root-scratch-20260803/contrabass-e1-superseded-run/ (wav + json)"
    - "~/VST-development-root-scratch-20260803/screenshots/ (6 PNGs)"
    - "~/VST-development-root-scratch-20260803/scratch-pv/ (6 text logs)"
    - "~/VST-development-root-scratch-20260803/o-bowed-harness-json/"
    - "~/VST-development-root-scratch-20260803/playwright-mcp/"
    - .gitignore
    - PUBLIC-RELEASE-READINESS.md
  key_links:
    - "The ignore rule for the golden's destination lands BEFORE the move, so the 1.3 MB wav is never simultaneously present-and-unignored under a directory where wav files ARE tracked — the exact window that reopened step 6 and that step 12 designed out."
    - "Archive MD5s are compared against the on-disk originals before anything is removed, so the preservation gate is proven, not assumed."
    - "The golden's sha256 is re-verified against the tracked sha256 sibling at its new path, so the relocation is proven to have preserved the bytes that make the file load-bearing."
    - "`git add -An` staging zero paths is the single gate proving the tidy added no new committable surface to a now-public repository."
---

<objective>
Close readiness checklist step 13: get the loose scratch renders, screenshots, and tool output out of the repository root so the root reads as a project rather than a workspace — while preserving the one file in that pile that is load-bearing.

Purpose: the repository is now public at `github.com/taylorbrook/O-Audio-VST-Development`. The root currently carries ~21 MB of orphaned harness renders, debug screenshots, Finder metadata, and browser-tool output. None of it is tracked, so none of it is published — but it is the first thing anyone sees in a checkout, and one of the files in it is the only byte-exact copy on disk of O-Bowed's golden reference render. Deleting the pile wholesale would destroy that. This plan separates the load-bearing file from the genuinely disposable, relocates the former, archives the latter outside the repo, and proves with runnable gates that nothing new became committable in the process.

Output: one file relocated inside the repo, one dated external archive directory, an emptied root, two modified files (`.gitignore`, `PUBLIC-RELEASE-READINESS.md`), and two commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@PUBLIC-RELEASE-READINESS.md

Read only the parts of the readiness doc you need: section 4.4 (line ~268), section 4.6 (line ~280), and the section 6 checklist entries for steps 12 and 13 (lines ~400-401). Step 12 at line 400 is the STYLE MODEL — match its voice, density, and the shape of its resolution note.
</context>

<execution_environment>
**This plan runs on the main working tree. Worktree isolation is OFF and must stay off.**

Every file this plan operates on is UNTRACKED. In a fresh worktree checkout none of them exist at all — an isolated executor would find an already-clean root, move nothing, and report success falsely. Do not create a worktree. Do not `git stash`. Work in `/Users/taylorbrook/Dev/VST-development` directly.

**Consequence for commits:** because everything in scope is untracked, no file move or removal produces a commit. The only committed deliverables are `.gitignore` (Task 2) and `PUBLIC-RELEASE-READINESS.md` (Task 3). There is nothing to `git rm --cached` — do not plan or attempt an untracking step.
</execution_environment>

<verified_findings>
These were measured before planning. Re-verify the destructive-gate ones in Task 1; do not re-derive the rest.

1. **Everything in scope is already untracked AND ignored.** `git add -An .` over the whole repo stages ZERO paths right now. Root census, all `tracked=no ignored=YES`: `e1-max-sustain.wav` (17,199,104 B), `e1-max-sustain.json`, `o-bowed-pre-extraction-canonical.wav` (1,323,104 B), `o-bowed-pre-extraction-canonical.json`, `mbc-v150.png`, `o-reversedelay-484.png`, `tooltip-knob.png`, `after-header.png`, `after-tips-off.png`, `before-full.png`, `.DS_Store`, `scratch-pv/` (56 K, 6 text logs), `.playwright-mcp/` (2.2 MB), `backups/`.

2. **`o-bowed-pre-extraction-canonical.wav` IS the O-Bowed golden render — LOAD-BEARING.** Its sha256 is `93124fb8dd8223caafac5948c988a226230363d79a17323d386e9a1db34c8891`, matching `plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav.sha256` exactly. That golden directory tracks only the sha256 file, never the wav, so this root file is the sole byte-exact copy on disk. It must be RELOCATED, never deleted.

3. **The relocation destination is not covered by any ignore rule.** `.gitignore:225` is `/*.wav` — root-anchored, so it stops covering the file the moment it moves. `git check-ignore` on `plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav` currently returns nothing. Worse, wav files ARE legitimately tracked in sibling golden directories: O-Contrabass tracks 19 of them. So the destination is a directory where a stray wav looks native and would be swept up by a `git add -A` into a public repo. A rule must land there FIRST.

4. **The root `e1-max-sustain` pair is a superseded run, and the readiness doc's provenance claim is stale.** `plugins/O-Contrabass/.planning/stages/2-dsp/RESEARCH.md:1390` (tracked) cites the root json as numerical evidence with `rmsMid_s5_s6 = 0.0353`, `rmsFinal` approximately 0, `pass_rms = false`. Measured, the root file does NOT carry those numbers:
   - ROOT json: peak 0.09926, rmsMid 0.02767, rmsFinal 0.006335, ratio 0.2289
   - PLUGIN-LOCAL `plugins/O-Contrabass/e1-max-sustain.json`: peak 0.068542, rmsMid 0.035618, rmsFinal 0.012675, ratio 0.35586

   `VERIFICATION.md:68` cites "peak 0.0683, rmsMid 0.0353, rmsFinal 0.0127, ratio 0.36" — that is the plugin-local copy. The wavs differ too (root md5 `ba8d5b37ba5fc613b00dbe313c4f00e5` vs plugin-local `d585110e4f7738a15aaca397c17c0fe1`, same byte length, different runs). The cited evidence artifact is the plugin-local pair, which is TRACKED and survives regardless; the "(project root)" parenthetical in RESEARCH.md is simply wrong.

5. **The root `o-bowed-pre-extraction-canonical.json` is redundant with tracked content.** Every audio-bearing field matches the tracked `golden/canonical-preset.json` verbatim — `totalSamples` 220500, `peak` 0.052906461060047, `nanCount` 0, all three pass flags. Only the per-run timing telemetry (`blockMicros_*`) and the `outputWav` path string differ. It is a second invocation of the same canonical render, carrying no unique evidence.

6. **README.md references none of these files.** `grep -n "\.png\|\.wav" README.md` returns zero hits. The only tracked docs naming them are `PUBLIC-RELEASE-READINESS.md` itself (as the thing to be cleaned) and the O-Contrabass plugin-local `.planning/` stage docs covered by finding 4.

7. **Baselines to hold constant:** tracked-but-ignored census = **9** (all vendored `preset-manager.js`); total tracked wav files = **31**; O-Contrabass golden tracked files = **70**, of which **19** are wav.

8. **Archive naming precedent:** `~/VST-development-releases-backup-20260803/` (step 12) and `~/VST-development-pre-rewrite-20260802.bundle` (step 3b). Follow that shape — do not invent a new convention.
</verified_findings>

<dispositions>
The decision for every item, settled at plan time. Task 1 proves the gate; Task 2 executes.

| Item | Size | Disposition | Why |
|---|---|---|---|
| `o-bowed-pre-extraction-canonical.wav` | 1.3 MB | **RELOCATE in-repo** to `plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav` | Byte-exact golden; belongs beside the sha256 file that attests it. Ignore rule lands first. |
| `e1-max-sustain.wav` + json | 17.2 MB | **ARCHIVE externally** | Superseded harness run. Fails a strict delete gate both ways — not byte-identical to the plugin-local copy, and named in a tracked doc — so it is preserved rather than destroyed. |
| `o-bowed-pre-extraction-canonical.json` | 472 B | **ARCHIVE externally** | Audio-bearing fields fully duplicated by the tracked `golden/canonical-preset.json`; only run telemetry is unique. |
| `mbc-v150.png`, `o-reversedelay-484.png`, `tooltip-knob.png` | 856 KB | **ARCHIVE externally** | Debug screenshots, no tracked reference. |
| `after-header.png`, `after-tips-off.png`, `before-full.png` | 523 KB | **ARCHIVE externally** | Aug 2 before/after screenshots. Not in section 4.4's original list — same class, decided explicitly here rather than swept in silently. |
| `scratch-pv/` | 56 KB | **ARCHIVE externally** | Six pluginval log dumps from a past validation run. |
| `.playwright-mcp/` | 2.2 MB | **ARCHIVE externally** | Browser-tool output traces. Not in section 4.4's original list — same class, decided explicitly. Regenerable, but archiving costs nothing and keeps one uniform rule. |
| `.DS_Store` | 6 KB | **DELETE** | Finder metadata. Regenerates on its own, carries no content, cannot be referenced by anything. The only outright deletion in this plan. |
| `backups/` | 1.3 GB | **OUT OF SCOPE — leave alone** | An established working directory, not a scratch render or image. Say so in the doc rather than expanding scope silently. |
| `logs/` | 9.8 MB | **OUT OF SCOPE — leave alone** | Same. Its build logs were already handled by step 7. |
| `build/`, `build-release/` | — | **OUT OF SCOPE — leave alone** | Build trees; `build-release/` was closed by step 6. |

**Uniform rule:** nothing is deleted except Finder metadata. Everything else is archived, so every disposition is reversible.
</dispositions>

<tasks>

<task type="tracer">
  <name>Task 1: Preservation gate — prove the golden, then archive everything leaving the root</name>
  <files>~/VST-development-root-scratch-20260803/ (new external archive; no repo files modified)</files>
  <precondition>Working tree is clean (`git status --porcelain` is empty) and cwd is `/Users/taylorbrook/Dev/VST-development`, NOT a worktree. If the tree is dirty, halt and report rather than archiving over uncommitted work.</precondition>
  <action>
Establish the preservation gate before anything moves or is removed. Nothing is deleted in this task; originals stay in place.

Re-verify the two facts that make a later destructive step safe or unsafe. Compute `shasum -a 256 o-bowed-pre-extraction-canonical.wav` and compare it to the first field of `plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav.sha256`. If they do not match, HALT — the relocation premise is void and the disposition table must be revisited. Then confirm the root and plugin-local e1 wav copies differ, by comparing `md5 -q` on each; a match would mean the root copy is redundant rather than a distinct superseded run, which changes the write-up in Task 3. Record both results.

Capture the four baselines Task 2 must hold constant, so they are measured rather than assumed: the tracked-but-ignored census (`git ls-files -i -c --exclude-standard | wc -l`, expect 9), total tracked wav files (`git ls-files '*.wav' | wc -l`, expect 31), and the O-Contrabass golden tracked counts (all files, expect 70; wav only, expect 19). Also run `git add -An . | wc -l` and confirm it is 0 — that is the pre-state the tidy must not disturb.

Create the dated external archive following the step-12 naming precedent: `~/VST-development-root-scratch-20260803/`, with subdirectories `contrabass-e1-superseded-run/`, `screenshots/`, `scratch-pv/`, `o-bowed-harness-json/`, and `playwright-mcp/`. Copy into it, with `cp` and not a move, since originals stay until Task 2: the e1 wav and json pair; the six root PNGs; the six `scratch-pv` text logs; the root o-bowed harness json; and the whole `.playwright-mcp` tree.

Verify the copies. For every regular file copied, compare `md5 -q` of the archive copy against `md5 -q` of the original and require equality. Any mismatch HALTS the plan. Report the count of files verified.

Write `~/VST-development-root-scratch-20260803/README.txt` recording, in plain prose: the date and that this is readiness step 13 / section 4.4; that the O-Bowed golden was relocated INTO the repo at `plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav` rather than archived here, plus its sha256; that the e1 pair is a superseded harness run whose numbers appear in no document, that the cited evidence is the tracked plugin-local pair, and both md5 values; that the o-bowed harness json duplicates the tracked golden json in every audio-bearing field; and that the screenshots and pluginval logs had no tracked reference. Someone finding this directory in a year must be able to tell what is safe to throw away without reopening the repo.
  </action>
  <verify>
    <automated>ARCH="$HOME/VST-development-root-scratch-20260803"; test -f "$ARCH/README.txt" &amp;&amp; test -f "$ARCH/contrabass-e1-superseded-run/e1-max-sustain.wav" &amp;&amp; test -f "$ARCH/contrabass-e1-superseded-run/e1-max-sustain.json" &amp;&amp; test -f "$ARCH/o-bowed-harness-json/o-bowed-pre-extraction-canonical.json" &amp;&amp; test "$(ls "$ARCH"/screenshots/*.png | wc -l | tr -d ' ')" = "6" &amp;&amp; test "$(ls "$ARCH"/scratch-pv/*.txt | wc -l | tr -d ' ')" = "6" &amp;&amp; test -d "$ARCH/playwright-mcp" &amp;&amp; test "$(shasum -a 256 o-bowed-pre-extraction-canonical.wav | awk '{print $1}')" = "$(awk '{print $1}' plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav.sha256)" &amp;&amp; test "$(md5 -q "$ARCH/contrabass-e1-superseded-run/e1-max-sustain.wav")" = "$(md5 -q e1-max-sustain.wav)" &amp;&amp; test "$(git add -An . | wc -l | tr -d ' ')" = "0" &amp;&amp; echo GATE-PASS</automated>
  </verify>
  <done>The archive exists with all five subdirectories populated and every copied file MD5-matched to its original. The golden wav's sha256 is re-confirmed against the tracked sha256 sibling. The root still contains every original file — nothing has been removed yet. `git add -An .` still stages zero paths. Baselines 9 / 31 / 70 / 19 recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Ignore rule first, then relocate the golden and clear the root</name>
  <files>.gitignore, plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav</files>
  <precondition>Task 1 reported GATE-PASS. The archive at `~/VST-development-root-scratch-20260803/` exists and is MD5-verified. Do not begin this task otherwise — it is the first point at which files leave the root.</precondition>
  <action>
Order matters here and is the whole point of the task. The ignore rule lands and is committed BEFORE the wav moves, so the file is never simultaneously present in the repo and unignored. That is the window that reopened step 6, and step 12 designed it out; do the same.

**Step A — add the ignore rule and commit it.** Append to the "Render-Harness / Test-Capture Scratch Output" block in `.gitignore` (the block that currently ends with the `plugins/*/.planning/evidence` line, around line 48). Write a comment explaining three things, then the rule itself on one line:

- what the file is: O-Bowed's canonical golden render, whose sha256 sibling in the same directory is the tracked audit trail, so the binary is kept on disk as a local byte-exact reference and deliberately not committed;
- why it is not tracked when O-Contrabass tracks its own golden renders: section 4.2 identifies the tracked audio goldens as the largest single contributor to repository size and step 14 exists to reclaim exactly that, so adding another binary to a now-public repo would work against the standing direction, and O-Bowed's golden directory was authored sha256-only from the start;
- why the rule names one exact path rather than a directory wildcard: O-Contrabass tracks nineteen golden renders in its own golden directory and a broader rule would silently stop future intentional goldens from being tracked. This mirrors the reasoning already written into the installer-package block at the end of the file — match the artifact, never broaden to the containing directory.

The rule is the full repo-relative path to O-Bowed's canonical golden render binary. Commit `.gitignore` alone with a message in the project's conventional form, scoped `chore(quick-260803-c4n)`.

**Step B — verify the rule before moving anything.** Run `git check-ignore -v` on the destination path and confirm it resolves to the new `.gitignore` line. Confirm the O-Contrabass golden counts are unchanged at 70 tracked files and 19 tracked renders. Confirm the tracked-but-ignored census is still 9 — if the new rule shadowed a tracked file, that number would rise, and it must not.

**Step C — relocate the golden.** Move `o-bowed-pre-extraction-canonical.wav` from the root to `plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav` with `mv`. Immediately re-run `shasum -a 256` at the new path and require it to equal the tracked sha256 sibling. A move that did not preserve the bytes is a failure, not a warning.

**Step D — clear the root.** Only now remove the archived originals: the e1 wav and json pair, the six PNGs, the `scratch-pv` directory, the root o-bowed harness json, and the `.playwright-mcp` directory. Then remove the root `.DS_Store` — the single outright deletion, and the only item in this plan with no archive copy.

Do not touch `backups/`, `logs/`, `build/`, or `build-release/`. Do not remove the plugin-local `plugins/O-Contrabass/e1-max-sustain.wav` — it is tracked and is the actual cited evidence artifact.

**Step E — prove nothing new is committable.** Run `git add -An .` over the whole repository and require it to stage zero paths, matching the pre-state Task 1 recorded. Run `git status --porcelain` and confirm the only entry is the modified readiness doc if Task 3 has started, or nothing at all if it has not. This is the gate that matters most: the repository is public, and the relocated render sits in a directory where its siblings are tracked.
  </action>
  <verify>
    <automated>test -f plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav &amp;&amp; test "$(shasum -a 256 plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav | awk '{print $1}')" = "$(awk '{print $1}' plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav.sha256)" &amp;&amp; git check-ignore -q plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav &amp;&amp; test "$(git ls-files plugins/O-Contrabass/tests/render-harness/golden/ | wc -l | tr -d ' ')" = "70" &amp;&amp; test "$(git ls-files 'plugins/O-Contrabass/tests/render-harness/golden/*.wav' | wc -l | tr -d ' ')" = "19" &amp;&amp; test "$(git ls-files -i -c --exclude-standard | wc -l | tr -d ' ')" = "9" &amp;&amp; test "$(git ls-files '*.wav' | wc -l | tr -d ' ')" = "31" &amp;&amp; test "$(ls -A | grep -c -i -E '^(e1-max|o-bowed-pre|mbc-|o-reversedelay-|tooltip-|after-|before-|\.DS_Store$|scratch-pv$|\.playwright-mcp$)')" = "0" &amp;&amp; test "$(git add -An . | wc -l | tr -d ' ')" = "0" &amp;&amp; test -f plugins/O-Contrabass/e1-max-sustain.wav &amp;&amp; test -d backups &amp;&amp; test -d logs &amp;&amp; echo ROOT-CLEAN</automated>
  </verify>
  <done>The root holds no scratch render, screenshot, Finder metadata file, or tool-output directory. The golden render lives at `plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav`, byte-exact against its tracked sha256, and resolves to an ignore rule. O-Contrabass's 70 tracked golden files including 19 renders are untouched, the tracked-but-ignored census is still 9, the tracked render total is still 31, and `git add -An .` stages zero paths. `backups/` and `logs/` still exist. One commit landed: the `.gitignore` rule.</done>
</task>

<task type="auto">
  <name>Task 3: Record the result in PUBLIC-RELEASE-READINESS.md — section 4.4 body and step 13</name>
  <files>PUBLIC-RELEASE-READINESS.md</files>
  <precondition>Task 2 reported ROOT-CLEAN. Write only what was actually measured — every figure in this task must come from Task 1's or Task 2's recorded output, not from this plan's prose.</precondition>
  <action>
Both edits are required. Step 12 at line 400 is the style model for density and voice; section 4.3's resolution note at lines 261-266 is the model for the section-body note. Match them.

**Edit A — section 4.4 heading and body.** Mark the heading resolved in the same form section 4.3 uses, then add a `Resolved 2026-08-03` note to the body and re-tense any surrounding prose so no present-tense claim about files still sitting in the root survives beneath a resolved heading. The note must carry four things:

1. **What happened to each item**, grouped by disposition rather than listed flat: one file relocated inside the repository, the rest copied to a dated archive outside it, and one Finder metadata file deleted outright as the sole item with no preservation copy. Name the archive directory and say the copies were MD5-verified against the originals before anything was removed.

2. **The correction to this section's own record — this is the part a future reader most needs.** Section 4.4 lists the root e1 render as plain scratch alongside the screenshots. It is more specific than that: it is a superseded harness run whose numbers appear in no document. The tracked `RESEARCH.md` at `plugins/O-Contrabass/.planning/stages/2-dsp/RESEARCH.md:1390` cites the root json as its numerical evidence, and that citation is stale — the root file's measured values differ from the ones quoted, while the tracked plugin-local pair at `plugins/O-Contrabass/` matches the figures in `VERIFICATION.md:68` exactly. Give both sets of numbers so nobody has to re-measure. State plainly that the cited evidence artifact is the plugin-local pair, that it is tracked and unaffected, and that the root pair was archived rather than deleted precisely because a tracked document names it. Note that `RESEARCH.md` itself was deliberately not edited: it is a historical stage document and this correction belongs here, not in a rewrite of the record.

3. **Why the O-Bowed render was relocated and then ignored rather than committed.** Its sha256 matched the tracked sibling exactly, making the root copy the only byte-exact copy on disk — deletion was never an option. But the destination directory is one where renders ARE tracked (O-Contrabass has nineteen), so the ignore rule had to land first, before the move, or the file would have sat one `git add -A` away from a public commit. Say that the rule names one exact path rather than a directory wildcard, and why. Point at section 4.2 and step 14 for the size reasoning that makes not-committing the right default.

4. **What was considered and ruled out of scope.** `backups/` at 1.3 GB and `logs/` at 9.8 MB are established working directories, not scratch renders or images, and were left untouched. Say so explicitly so the next reader does not assume they were missed. Also record that three before/after screenshots and the browser-tool output directory postdate this section's original assessment and were decided explicitly rather than swept in silently.

**Edit B — section 6, step 13.** Convert the unticked entry at line 401 to the ticked form the other entries use: checked box, strikethrough on the bolded step title, then a bold done-date, then the substantive note. Match step 12's density. It must state what moved where, that the preservation gate ran before anything was removed, that the ignore rule landed before the move so the step-6 window never opened, and the verification that proves it — the destination resolving to a rule under `git check-ignore`, the tracked-but-ignored census holding at 9, the tracked render total holding at 31, and a whole-repository `git add -An` staging zero paths. Note that nothing was tracked to begin with, so unlike steps 5, 6, 7 and 12 this was a working-directory tidy with no untracking and no history implications at all. Close by naming what was left alone and why.

Then update the checklist tally wherever the document states it. Step 12's own note and the STATE.md record put it at 13 ticked with 4 open (4, 11, 13, 14); after this it is 14 ticked with 3 open (4, 11, 14). Grep for the count rather than assuming where it appears, and fix every occurrence you find.

Commit `PUBLIC-RELEASE-READINESS.md` alone, scoped `docs(quick-260803-c4n)`. Do not push.
  </action>
  <verify>
    <automated>grep '^- \[x\] ~~\*\*13\.' PUBLIC-RELEASE-READINESS.md | grep -q 'Done 2026-08-03' &amp;&amp; test "$(grep -c 'Resolved 2026-08-03' PUBLIC-RELEASE-READINESS.md)" -ge 5 &amp;&amp; grep -q '4\.4 Root-level scratch files.*RESOLVED' PUBLIC-RELEASE-READINESS.md &amp;&amp; grep -q '0\.0353' PUBLIC-RELEASE-READINESS.md &amp;&amp; grep -q '0\.02767' PUBLIC-RELEASE-READINESS.md &amp;&amp; grep -q 'VST-development-root-scratch-20260803' PUBLIC-RELEASE-READINESS.md &amp;&amp; test "$(grep -c '^- \[ \] ' PUBLIC-RELEASE-READINESS.md)" = "3" &amp;&amp; test "$(grep -c '^- \[x\] ' PUBLIC-RELEASE-READINESS.md)" = "14" &amp;&amp; test "$(git diff --numstat HEAD~1 -- PUBLIC-RELEASE-READINESS.md | awk '{print $1}')" -gt 0 &amp;&amp; echo DOC-OK</automated>
    <human-check>Read the rewritten section 4.4 top to bottom. Confirm no sentence beneath the resolved heading still describes the root files in the present tense, and that the provenance correction reads as a correction to the record rather than as a new claim — a reader who has never seen this plan should finish it knowing which e1 pair is the real evidence and why the other one was kept anyway.</human-check>
  </verify>
  <done>Section 4.4's heading is marked resolved, its body carries a `Resolved 2026-08-03` note covering all four required points including the provenance correction with both sets of measured numbers, and no stale present-tense prose survives under the resolved heading. Step 13 is ticked in step 12's style. Exactly three unticked checklist boxes remain (4, 11, 14). One commit landed, touching only `PUBLIC-RELEASE-READINESS.md`. Nothing was pushed and no historical `.planning/` or plugin `.planning/` document was edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree to public git remote | The repository is public at `github.com/taylorbrook/O-Audio-VST-Development`. Anything that becomes tracked here is published permanently and becomes fetchable by SHA even if later removed. |
| repository to external filesystem | Files archived to `$HOME` leave the repository's access controls and backup story entirely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-c4n-01 | Information Disclosure | relocated golden render in a directory where sibling renders are tracked | high | mitigate | Ignore rule lands and is committed in Task 2 Step A, before the move in Step C. Verified by `git check-ignore -q` on the destination plus a whole-repository `git add -An` staging zero paths. This is the step-6 residual shape, designed out rather than fixed after the fact. |
| T-c4n-02 | Denial of Service (loss of evidence) | O-Bowed golden render, the only byte-exact copy on disk | critical | mitigate | Task 1 halts unless the render's sha256 matches the tracked sibling; Task 2 Step C re-verifies after the move. Relocation, never deletion. |
| T-c4n-03 | Denial of Service (loss of evidence) | archived scratch files with a stale tracked-doc citation | medium | mitigate | Nothing is deleted except Finder metadata. Every other item is MD5-verified into a dated external archive before removal, and a README records provenance so the archive is intelligible without the repo. |
| T-c4n-04 | Tampering | broadened ignore rule silently untracking real goldens | medium | mitigate | Rule is scoped to one exact path, not a directory wildcard. Verified by O-Contrabass golden counts holding at 70 files / 19 renders and the tracked-but-ignored census holding at 9. |
| T-c4n-05 | Repudiation | readiness doc claiming a resolution the tree does not support | medium | mitigate | Every figure written in Task 3 must come from Task 1 or Task 2 recorded output. Task 3's gate greps for the measured values, and its human-check reads the section for stale present-tense prose. |

No package-manager installs occur in this plan, so no legitimacy gate applies.
</threat_model>

<verification>
Run from the repository root after all three tasks:

1. `ls -A` at root shows no scratch render, screenshot, Finder metadata file, `scratch-pv`, or `.playwright-mcp`.
2. `shasum -a 256 plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav` equals the tracked sha256 sibling.
3. `git check-ignore -v plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav` resolves to the new `.gitignore` line.
4. `git add -An .` stages zero paths.
5. `git ls-files -i -c --exclude-standard | wc -l` is 9; `git ls-files '*.wav' | wc -l` is 31.
6. `git ls-files plugins/O-Contrabass/tests/render-harness/golden/ | wc -l` is 70.
7. `git log --oneline -2` shows exactly two new commits, one touching `.gitignore` and one touching `PUBLIC-RELEASE-READINESS.md`.
8. `git reflog | head -20` shows no force-push, no filter-repo, no filter-branch, no push.
9. `git submodule status plugins/O-Orbit/libs/SAF` is unchanged.
10. `ls ~/VST-development-root-scratch-20260803/` shows README.txt and five populated subdirectories.
</verification>

<success_criteria>
- Repository root reads as a project: no loose renders, screenshots, Finder metadata, or tool output.
- O-Bowed's golden render preserved byte-exact at its new in-repo path and provably ignored.
- Every removed file preserved outside the repo first and MD5-verified, except Finder metadata.
- No new committable surface: whole-repo `git add -An` stages zero paths, all four baselines unchanged.
- Section 4.4 resolved with the provenance correction recorded; step 13 ticked in step 12's style; three unticked boxes remain.
- `backups/` and `logs/` untouched and documented as out of scope.
- Two commits, no push, no history rewrite, no edits to historical `.planning/` documents.
</success_criteria>

<non_scope_observation>
Report but do NOT act on: section 4.2 states "32 `.wav` files are tracked in total" while the measured count is 31. That figure belongs to step 14's section and went stale independently of this work. Surface it in the SUMMARY as a one-line note for whoever opens step 14; do not edit section 4.2 here.
</non_scope_observation>

<output>
Create `.planning/quick/260803-c4n-tidy-the-repo-root-per-public-release-re/260803-c4n-SUMMARY.md` when done.

Per the known `gsd-quick` cleanup trap: commit the SUMMARY before any cleanup wave runs, or worktree cleanup blocks on `worktree_dirty`. This plan runs on the main tree so there is no worktree to clean, but the SUMMARY still needs committing.
</output>
