---
phase: quick-260719-m5p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-suite-battery.sh
  - .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/260719-m5p-RESULTS.md
  - .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/battery-results.tsv
  - .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/harness-results.tsv
  - plugins/O-Bowed/tests/render-harness/CMakeLists.txt
  - plugins/O-Contrabass/tests/render-harness/CMakeLists.txt
  - plugins/O-simpleFM/tests/render-harness/CMakeLists.txt
  - plugins/O-simpleGrain/tests/render-harness/CMakeLists.txt
  - plugins/O-simpleAdditive/tests/render-harness/CMakeLists.txt
  - plugins/O-simpleBeatmaker/tests/render-harness/CMakeLists.txt
  - plugins/O-simpleSubtractive/tests/render-harness/CMakeLists.txt
  - plugins/O-simplePhysicalModelSynth/tests/render-harness/CMakeLists.txt
autonomous: true
requirements: [BATTERY-AUVAL, BATTERY-PLUGINVAL, BATTERY-HARNESS, BATTERY-PRESET, BATTERY-MANUAL]

must_haves:
  truths:
    - "A pass/fail matrix exists with one row per suite plugin covering auval (AU-link), pluginval, and (where applicable) render-harness columns"
    - "verify-au-link.sh (auval) was actually run for every buildable plugin and its exit code recorded"
    - "pluginval was run across the suite at strictness >=5 with per-plugin exit codes recorded (its state save/restore test covers the 8.0.11 var deep-equality watch)"
    - "Every offline render harness was rebuilt (WebView pitfall fixed where present) and executed, with pass/fail recorded"
    - "O-TextureForge is recorded as KNOWN-FAIL (umappp/irlba drift, DEF-L26-01), not silently dropped or reported as fixed"
    - "The results doc ends with a manual checklist: Dorico 3-point microtonal smoke + Logic/Ableton smoke with the mandatory cache-clear sequence"
  artifacts:
    - ".planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/260719-m5p-RESULTS.md"
    - "scripts/verify-suite-battery.sh"
    - ".planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/battery-results.tsv"
    - ".planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/harness-results.tsv"
  key_links:
    - "scripts/verify-au-link.sh reused verbatim (auval gate) — not rewritten"
    - "CMake target resolved per plugin (11/37 have target != folder) via the build-and-install.sh resolve_cmake_target pattern"
    - "pluginval CLI at /Applications/pluginval.app/Contents/MacOS/pluginval with --skip-gui-tests + --timeout-ms (per-plugin exit code captured; sweep never aborts on one failure)"
    - "macOS lacks timeout/gtimeout — sweep uses a portable per-plugin wall-clock guard"
---

<objective>
Run the post-JUCE-8.0.14 verification battery from `research/framework-updates-2026-07.md` §Verification Strategy and produce a single pass/fail matrix document with the manual smoke-test checklists appended.

The battery covers the automated gates that must pass after the 8.0.9 → 8.0.14 bump: AU-link (auval) per plugin, pluginval across the suite, and the offline render harnesses (rebuilt with the WebView pitfall fixed). The 8.0.11 `var` deep-equality change is watched via pluginval's state save/restore test. The Dorico 3-point microtonal smoke and Logic/Ableton DAW smoke are DOCUMENTATION ONLY — the executor cannot drive DAWs, so the final task writes those checklists into the results doc for the user to run by hand.

Purpose: prove the JUCE bump did not regress the suite, with recorded evidence rather than assertion.
Output: `scripts/verify-suite-battery.sh` (reusable resilient sweep), machine-readable per-plugin result TSVs, and `260719-m5p-RESULTS.md` (pass/fail matrix + manual checklists).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@research/framework-updates-2026-07.md
@scripts/verify-au-link.sh
@scripts/build-and-install.sh

# Facts established during planning (do not re-derive):
# - Suite = 38 dirs with plugins/<name>/CMakeLists.txt. `plugins/tache_plugins/` has NO
#   top-level CMakeLists and is NOT part of the suite — enumerating plugins/*/CMakeLists.txt
#   self-selects the 38 O-* plugins and naturally excludes tache_plugins.
# - O-TextureForge is a KNOWN-FAIL fresh build (umappp<->irlba transitive drift, JUCE-independent,
#   deferred DEF-L26-01). It is recorded as KNOWN-FAIL, never "fixed" and never allowed to abort the sweep.
# - The 8.0.14 full-suite fresh build already ran 36/37 clean (quick task l26). build-and-install.sh
#   incremental rebuild is therefore a near-no-op per plugin ("ninja: no work to do") — it exists here
#   to guarantee a FRESH 8.0.14 install (with the dual-variant sweep) is what auval/pluginval gate against.
# - 8 render harnesses exist under plugins/<name>/tests/render-harness/ (targets named "<Plugin>-render-test",
#   built with -DOUARICON_BUILD_TESTS=ON). All 8 compile PluginEditor.cpp; the editors are WebView.
#   O-simpleFM's harness handles this by setting JUCE_WEB_BROWSER=1 and linking <Plugin>_UIResources
#   (the working pattern). Others set JUCE_WEB_BROWSER=0 while compiling PluginEditor.cpp (the pitfall).
# - pluginval CLI = /Applications/pluginval.app/Contents/MacOS/pluginval (JUCE v8.0.3). Flags confirmed:
#   --validate <path>, --strictness-level 1-10, --timeout-ms, --skip-gui-tests, --output-dir. Non-zero on fail.
# - macOS has NO `timeout`/`gtimeout`. Use pluginval's own --timeout-ms and a portable wall-clock guard
#   (background PID + kill watchdog) for auval/build steps.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the resilient battery sweep and run AU-link (auval) + pluginval across the suite</name>
  <files>scripts/verify-suite-battery.sh, .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/battery-results.tsv</files>
  <action>
Create `scripts/verify-suite-battery.sh` — a reusable, resilient sweep that gates the whole O-* suite and writes machine-readable results. Behavior:

1. Enumerate plugins by globbing `plugins/*/CMakeLists.txt` (this excludes `tache_plugins/`, which has no top-level CMakeLists). Iterate in sorted order.
2. For each plugin, in a per-plugin subshell that NEVER aborts the outer loop (capture exit codes; do not use `set -e` at the loop level):
   a. Special-case O-TextureForge: do NOT attempt to build it. Record it as `KNOWN-FAIL` (reason: umappp/irlba drift, DEF-L26-01) in every column and continue.
   b. Ensure a FRESH 8.0.14 install: invoke `scripts/build-and-install.sh <plugin>` in a subshell with a wall-clock watchdog (background the command, record its PID, kill it after a bounded timeout since macOS has no `timeout`). Because the l26 fresh build already produced 8.0.14 artefacts, the incremental ninja step is a near-no-op; the value is the fresh dual-variant install. Capture the install exit code as `install_rc`. On non-zero, record `BUILD-FAIL`/`INSTALL-FAIL` and continue to the next plugin (do not run downstream gates for that plugin).
   c. AU-link gate: run `scripts/verify-au-link.sh <plugin>` verbatim (reuse — do not reimplement auval logic). It resolves the AU triple and PLUGIN target and runs `auval`. Capture `auval_rc`.
   d. pluginval gate: locate the installed VST3 at `~/Library/Audio/Plug-Ins/VST3/<Product>*.vst3` (the `-dev` suffixed bundle is what build-and-install.sh produces locally). Run `/Applications/pluginval.app/Contents/MacOS/pluginval --strictness-level 8 --skip-gui-tests --timeout-ms 60000 --output-dir <logdir> --validate <vst3path>`. Use `--skip-gui-tests` so WebView editors do not hang a headless run; strictness 8 exercises the thorough state/parameter restoration test that surfaces the 8.0.11 `var` deep-equality change. If the full-suite wall-clock is excessive, the script may fall back to `--strictness-level 5`. Capture `pluginval_rc`.
   e. Append one tab-separated line `plugin<TAB>cmake_target<TAB>install_rc<TAB>auval_rc<TAB>pluginval_rc<TAB>note` to `.planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/battery-results.tsv` (write a header row first on a fresh run). Resolve `cmake_target` with the same juce_add_plugin first-token + one-level ${VAR} indirection logic used by `resolve_cmake_target()` in build-and-install.sh (11/37 plugins have target != folder).
3. Accept a `--plugin <name>` flag to gate a single plugin (for re-runs) and default to the full sweep. Keep pluginval per-run logs under a `logdir` inside the quick directory or scratchpad.

Run the sweep to completion. It is long-running (auval + pluginval across ~37 plugins) — launch it so it self-manages the loop and writes the TSV incrementally; do not hold per-plugin build/pluginval logs in context. After it finishes, read only the compact `battery-results.tsv`.

Do not modify any plugin DSP or CMake in this task — this is a read-only gate over installed binaries plus the fresh reinstall.
  </action>
  <verify>
    <automated>bash -n scripts/verify-suite-battery.sh && test -x scripts/verify-suite-battery.sh && test -s .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/battery-results.tsv && awk -F'\t' 'NR>1{n++} END{exit !(n>=37)}' .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/battery-results.tsv && grep -q "O-TextureForge" .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/battery-results.tsv && grep -q "KNOWN-FAIL" .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/battery-results.tsv</automated>
  </verify>
  <done>scripts/verify-suite-battery.sh exists, is syntactically valid, and resilient (per-plugin exit-code capture, portable wall-clock guard, no whole-sweep abort). battery-results.tsv has one row per suite plugin (>=37) with install_rc/auval_rc/pluginval_rc populated; O-TextureForge is recorded KNOWN-FAIL; a single plugin failure did not stop the sweep.</done>
</task>

<task type="auto">
  <name>Task 2: Audit + fix the render-harness WebView pitfall, then rebuild and run all 8 harnesses</name>
  <files>plugins/O-Bowed/tests/render-harness/CMakeLists.txt, plugins/O-Contrabass/tests/render-harness/CMakeLists.txt, plugins/O-simpleGrain/tests/render-harness/CMakeLists.txt, plugins/O-simpleAdditive/tests/render-harness/CMakeLists.txt, plugins/O-simpleBeatmaker/tests/render-harness/CMakeLists.txt, plugins/O-simpleSubtractive/tests/render-harness/CMakeLists.txt, plugins/O-simplePhysicalModelSynth/tests/render-harness/CMakeLists.txt, plugins/O-simpleFM/tests/render-harness/CMakeLists.txt, .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/harness-results.tsv</files>
  <action>
The 8 offline render harnesses live at `plugins/<name>/tests/render-harness/` (O-simplePhysicalModelSynth, O-Bowed, O-simpleFM, O-simpleGrain, O-simpleSubtractive, O-Contrabass, O-simpleAdditive, O-simpleBeatmaker), built with `-DOUARICON_BUILD_TESTS=ON`, targets named `<Plugin>-render-test`. All compile PluginEditor.cpp so `createEditor()` resolves at link time — the known WebView-vs-harness pitfall (a WebView editor compiled under `JUCE_WEB_BROWSER=0` fails to build/link).

1. Audit each harness CMakeLists for the pitfall combination: compiles `PluginEditor.cpp` AND sets `JUCE_WEB_BROWSER=0`. O-simpleFM already uses the working pattern (`JUCE_WEB_BROWSER=1` + links `<Plugin>_UIResources` for BinaryData) — treat it as the reference. From the planning grep, the `JUCE_WEB_BROWSER=0` harnesses are O-simpleSubtractive, O-simpleBeatmaker, O-Contrabass, and O-simplePhysicalModelSynth; re-confirm by reading each CMakeLists rather than trusting the list.
2. For each pitfall harness, apply the minimal fix that matches the plugin's own working sibling — do NOT change DSP or the plugin's shipping editor:
   - Preferred where the plugin exposes a `<Plugin>_UIResources` BinaryData target: flip the harness to `JUCE_WEB_BROWSER=1` and link that resources target (the O-simpleFM pattern), so PluginEditor.cpp compiles and links cleanly.
   - Where a resources target is not readily linkable: guard `createEditor()`'s WebView body with `#if JUCE_WEB_BROWSER` and drop `PluginEditor.cpp` from the harness `target_sources`, providing a trivial `createEditor()` fallback so the console app links (the pattern captured in project memory `pattern_render_harness_breaks_on_webview_editor`).
3. Configure once with `cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DOUARICON_BUILD_TESTS=ON` (reuse the existing build dir; add the flag). Build each `<Plugin>-render-test` target and run the resulting executable, capturing its exit code and a one-line pass/fail summary. Batch this with the same resilience as Task 1: per-harness exit-code capture and a wall-clock guard, never aborting the batch on one failure.
4. Write one tab-separated line per harness `plugin<TAB>target<TAB>build_rc<TAB>run_rc<TAB>note` (with header) to `.planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/harness-results.tsv`.

Keep harness edits scoped to the harness CMakeLists (and, only if the guard route is chosen, a `#if JUCE_WEB_BROWSER` guard around the WebView editor body). The shipping plugin builds must remain untouched.
  </action>
  <verify>
    <automated>test -s .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/harness-results.tsv && awk -F'\t' 'NR>1{n++} END{exit !(n>=8)}' .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/harness-results.tsv && ! grep -rlE 'JUCE_WEB_BROWSER=0' plugins/*/tests/render-harness/CMakeLists.txt | xargs -I{} grep -l 'PluginEditor\.cpp' {} 2>/dev/null | grep -q . </automated>
  </verify>
  <done>All 8 render-harness targets configured with -DOUARICON_BUILD_TESTS=ON, built, and run; harness-results.tsv has one row per harness with build_rc/run_rc populated. No harness CMakeLists still combines JUCE_WEB_BROWSER=0 with a compiled PluginEditor.cpp. Shipping plugin builds unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble the pass/fail matrix and append the manual smoke-test checklists</name>
  <files>.planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/260719-m5p-RESULTS.md</files>
  <action>
Aggregate `battery-results.tsv` (Task 1) and `harness-results.tsv` (Task 2) into a single human-readable document at `.planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/260719-m5p-RESULTS.md`.

Structure:
1. Header: title, date (2026-07-19), JUCE 8.0.9 → 8.0.14 context, one-line summary counts (N pass / N fail / KNOWN-FAIL). Link the source strategy `research/framework-updates-2026-07.md` §Verification Strategy.
2. AUTOMATED RESULTS — a markdown matrix table, one row per suite plugin with columns: Plugin | CMake Target | Install | auval | pluginval | Harness | Verdict. Fill auval/pluginval/install from battery-results.tsv exit codes (0 = PASS, non-zero = FAIL); fill Harness from harness-results.tsv for the 8 plugins that have one (blank/"n/a" otherwise). O-TextureForge row MUST read KNOWN-FAIL with the DEF-L26-01 umappp/irlba reason — never PASS, never omitted.
3. 8.0.11 var deep-equality (preset round-trip) watch — a short subsection stating this is covered by pluginval's strictness-8 state save/restore test, and calling out any plugin whose pluginval state test failed (candidate deep-equality regression) for manual preset round-trip follow-up. If all state tests passed, say so explicitly.
4. Windows VST3 note — a one-line pointer that the Windows half of the build matrix is validated via CI `build-and-release.yml` (out of local macOS scope), not run here.
5. MANUAL CHECKLIST (DOCUMENTATION ONLY — executor cannot drive DAWs; user runs these by hand). Two subsections:
   a. Dorico 3-point microtonal smoke test — the true note-expression acceptance gate proving the patch re-base preserved microtonal playback: (1) quarter-sharp C4 plays at ≈ 269 Hz (+50 cents above C4 261.63 Hz); (2) no attack zipper on note onset; (3) polyphonic isolation — the detuned note is isolated while other held notes play 12-TET. Give per-point PASS/FAIL checkboxes. Note O-Lyrica is the validated reference plugin.
   b. Logic / Ableton DAW smoke — instantiate the AU (Logic) and VST3 (Ableton), confirm load, play a note, confirm no crash/silence. Precede it with the MANDATORY cache-clear sequence copied from CLAUDE.md verbatim (killall AudioComponentRegistrar; rm AudioUnitCache + com.apple.audiounits.cache; remove old AND alternate-variant `-dev`/unsuffixed bundles before reinstall), because auval/Logic will otherwise gate a stale cached instance.

Do not attempt to run Dorico/Logic/Ableton — only write the checklist for the user.
  </action>
  <verify>
    <automated>test -f .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/260719-m5p-RESULTS.md && grep -qi "pluginval" .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/260719-m5p-RESULTS.md && grep -q "KNOWN-FAIL" .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/260719-m5p-RESULTS.md && grep -qi "quarter-sharp" .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/260719-m5p-RESULTS.md && grep -qi "AudioComponentRegistrar" .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/260719-m5p-RESULTS.md && grep -qi "deep-equality" .planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/260719-m5p-RESULTS.md</automated>
  </verify>
  <done>260719-m5p-RESULTS.md contains a per-plugin pass/fail matrix (auval + pluginval + harness columns) with O-TextureForge as KNOWN-FAIL, a 8.0.11 var deep-equality watch subsection, and the two manual checklists (Dorico 3-point microtonal + Logic/Ableton with the verbatim CLAUDE.md cache-clear sequence). No DAW was driven by the executor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → installed AU/VST3 bundles | Verification reinstalls and gates locally-built bundles; no network, no untrusted input, no new package installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-m5p-01 | Tampering | stale cached AU instance masks a real regression | low | mitigate | sweep forces fresh dual-variant install (build-and-install.sh Phase 4) + verify-au-link.sh clears AudioComponentRegistrar; manual checklist repeats the CLAUDE.md cache-clear before DAW smoke |
| T-m5p-02 | Denial of Service | one plugin (e.g. O-TextureForge) hanging/failing aborts the whole sweep | low | mitigate | per-plugin subshell + captured exit codes + portable wall-clock guard + pluginval --timeout-ms; O-TextureForge explicitly skipped as KNOWN-FAIL |
| T-m5p-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this task — verification-only over pre-existing tools (auval, pluginval, JUCE 8.0.14 already present) |
</threat_model>

<verification>
- `battery-results.tsv` has >=37 data rows with install/auval/pluginval exit codes; O-TextureForge = KNOWN-FAIL.
- `harness-results.tsv` has 8 rows; no harness CMakeLists combines `JUCE_WEB_BROWSER=0` with a compiled PluginEditor.cpp.
- `260719-m5p-RESULTS.md` renders a complete matrix + the 8.0.11 deep-equality watch + both manual checklists (Dorico 3-point, Logic/Ableton with the verbatim cache-clear).
- The sweep and harness batch each ran to completion without a single failure aborting the run.
</verification>

<success_criteria>
The post-JUCE-8.0.14 verification battery is complete: auval and pluginval were actually run across the suite and their exit codes recorded; every render harness was rebuilt (WebView pitfall fixed where present) and run; the 8.0.11 var deep-equality watch is addressed via pluginval state tests; a single pass/fail matrix document exists with O-TextureForge marked KNOWN-FAIL; and the manual Dorico + DAW smoke checklists (with the mandatory cache-clear sequence) are written for the user to execute.
</success_criteria>

<output>
Create `.planning/quick/260719-m5p-post-juce-8-0-14-verification-battery/260719-m5p-SUMMARY.md` when done.
</output>
