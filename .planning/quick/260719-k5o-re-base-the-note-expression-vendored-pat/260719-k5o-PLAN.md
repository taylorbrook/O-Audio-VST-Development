---
phase: quick-260719-k5o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vendored/JUCE-overrides/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h
  - vendored/JUCE-overrides/modules/juce_audio_processors/utilities/juce_VST3ClientExtensions.h
  - vendored/JUCE-overrides/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp
  - .github/workflows/build-and-release.yml
  - modules/tuning/note-expression/module.cmake
  - scripts/juce-patches/note-expression-juce-8.0.4.patch
  - scripts/juce-patches/note-expression-juce-8.0.14.patch
  - scripts/apply-juce-patches.sh
autonomous: true
requirements:
  - QUICK-260719-k5o-NE-REBASE
tags:
  - juce
  - vendored-fork
  - note-expression
  - ci

must_haves:
  truths:
    - "Vendored JUCE overrides are re-stitched against PRISTINE 8.0.14: the header lives only at the headless module path with the NE struct + virtual inserted, and the .cpp one-liner if is expanded to the braced event-forward block — both still carry the JUCE-NE-PATCH marker."
    - "O-Lyrica_VST3 configures and compiles clean against a scratch pristine JUCE 8.0.14 tree overlaid with the re-based vendored overrides, using a JUCE_DIR env override (local /Users/taylorbrook/JUCE untouched)."
    - "CI copy step + both grep gates + module.cmake guard resolve the header at its new 8.0.14 headless location, while local 8.0.9 dev still resolves the old path (both-paths-tolerant guard)."
    - "Both patch files exist: note-expression-juce-8.0.9.patch (renamed from 8.0.4) and note-expression-juce-8.0.14.patch (regenerated); the 8.0.14 patch dry-run-applies onto a pristine 8.0.14 tree."
  artifacts:
    - vendored/JUCE-overrides/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h
    - vendored/JUCE-overrides/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp
    - scripts/juce-patches/note-expression-juce-8.0.9.patch
    - scripts/juce-patches/note-expression-juce-8.0.14.patch
  key_links:
    - "module.cmake _NE_FILE1 both-paths guard: EXISTS(new headless path) → use it; else fall back to pre-8.0.14 path — passes on 8.0.9 local AND scratch 8.0.14."
    - "CI cp -R (yml:102/:451) walks the new vendored headless subtree unchanged; grep gates yml:104/:453 point at the headless header path."
    - "JUCE_DIR env override sends CMake at the scratch 8.0.14 tree; CLAUDE.md /Users/taylorbrook/JUCE and CI JUCE_VERSION stay at 8.0.9."
---

<objective>
Re-base the note-expression vendored patch (JUCE-NE-PATCH) from JUCE 8.0.9 onto pristine JUCE 8.0.14 on branch `quick/260719-k5o-juce-ne-rebase-8014`. The header moved modules at 8.0.14 (`juce_audio_processors` → `juce_audio_processors_headless/utilities/`); the `.cpp` NE anchor survives verbatim. Re-vendor both pristine 8.0.14 files with the NE additions re-stitched, repoint the five path touchpoints (CI copy is path-stable, two `.h` grep gates + `module.cmake` guard move), keep both patch files (rename old → 8.0.9, add regenerated 8.0.14), and prove it by compiling O-Lyrica_VST3 against a scratch pristine 8.0.14 tree.

Purpose: Land the mechanical JUCE-fork re-base ahead of the future JUCE_VERSION bump, without touching the local JUCE install or CI JUCE_VERSION. The branch is intentionally unmergeable-by-design (its CI is broken against 8.0.9) until the bump follows.
Output: Re-based `vendored/JUCE-overrides/` layout, repointed CI + module.cmake paths, both patch files, and a passing scratch-8.0.14 compile-check of O-Lyrica_VST3.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260719-k5o-re-base-the-note-expression-vendored-pat/260719-k5o-CONTEXT.md
@.planning/quick/260719-k5o-re-base-the-note-expression-vendored-pat/260719-k5o-RESEARCH.md

# Source-of-truth for the two NE additions to re-stitch verbatim (both files are 8.0.9-based today):
@vendored/JUCE-overrides/modules/juce_audio_processors/utilities/juce_VST3ClientExtensions.h
@vendored/JUCE-overrides/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp

# Touchpoints to repoint:
@.github/workflows/build-and-release.yml
@modules/tuning/note-expression/module.cmake
@scripts/apply-juce-patches.sh

Scratch root (ALL downloads/builds go here — NEVER /Users/taylorbrook/JUCE):
SCRATCH=/private/tmp/claude-501/-Users-taylorbrook-Dev-VST-development/bae91ebe-fa18-45f2-a71d-9bb523f3c6a2/scratchpad

Verified facts (do not re-derive):
- The two NE additions are self-contained inline code. Header block = current vendored `.h` lines 63–95 (`struct Vst3RawEvent` + `virtual void onVst3RawEvent (const Vst3RawEvent&) {}`), inserted between `virtual ~VST3ClientExtensions() = default;` and `virtual int32_t queryIEditController`. `.cpp` block = current vendored `.cpp` lines 3696–3749, replacing the pristine one-liner `if (isMidiInputBusEnabled && data.inputEvents != nullptr) MidiEventList::toMidiBuffer(...)` with the braced event-forward loop, inside `#if JucePlugin_WantsMidiInput`.
- The companion `modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.cpp` at 8.0.14 defines only `getCompatibleParameterIds` + `convertJuceParameterId` — NO NE code. DECISION (locked, RESEARCH-verified): do NOT patch it, do NOT vendor it.
- Pristine 8.0.14 source: release zip `https://github.com/juce-framework/JUCE/releases/download/8.0.14/juce-8.0.14-osx.zip` (mirrors what CI overlays onto; unzips to `JUCE/`).
- OUT OF SCOPE (never touch): `/Users/taylorbrook/JUCE`, CI `JUCE_VERSION` env (yml:39), and the OLD 8.0.9 header path inside `apply-juce-patches.sh` (:39 stays — local install is still 8.0.9).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-vendor pristine 8.0.14 files with NE re-stitched (header → headless path)</name>
  <files>vendored/JUCE-overrides/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h, vendored/JUCE-overrides/modules/juce_audio_processors/utilities/juce_VST3ClientExtensions.h, vendored/JUCE-overrides/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp</files>
  <action>
    Download pristine JUCE 8.0.14 into the scratch root and re-vendor the two override files from that pristine base (NOT from the current 8.0.9-based vendored files — those must be overwritten with 8.0.14 content plus only the NE additions).

    Step 1 — scratch download: create $SCRATCH, fetch the 8.0.14 macOS release zip (see context URL) to $SCRATCH/juce.zip, unzip into $SCRATCH producing $SCRATCH/JUCE, then rename to $SCRATCH/JUCE-8.0.14-pristine. Make a second copy $SCRATCH/JUCE-8.0.14 (this second copy is the compile-check overlay target used by the phase verification). Verify $SCRATCH/JUCE-8.0.14-pristine/modules/juce_core/juce_core.h reports version 8.0.14 and that the pristine header exists at modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h and does NOT exist at the old modules/juce_audio_processors/utilities/ path.

    Step 2 — header (new headless path): copy the PRISTINE 8.0.14 header from $SCRATCH/JUCE-8.0.14-pristine/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h into a NEW vendored location vendored/JUCE-overrides/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h, then insert the NE additions verbatim. The exact insertion text is the current vendored `.h` lines 63–95 (the JUCE-NE-PATCH comment block, `struct Vst3RawEvent`, and `virtual void onVst3RawEvent (const Vst3RawEvent&) {}`); place it immediately after the line `virtual ~VST3ClientExtensions() = default;` and before `virtual int32_t queryIEditController`. Preserve surrounding pristine-8.0.14 context exactly.

    Step 3 — remove the stale old-path header: delete vendored/JUCE-overrides/modules/juce_audio_processors/utilities/juce_VST3ClientExtensions.h and remove the now-empty `.../juce_audio_processors/utilities/` and `.../juce_audio_processors/` dirs under vendored (use git rm so the rename is tracked as delete+add).

    Step 4 — VST3 wrapper .cpp (path unchanged): overwrite vendored/JUCE-overrides/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp with the PRISTINE 8.0.14 copy, then re-stitch the NE braced block. In pristine 8.0.14 the anchor is a one-liner inside `#if JucePlugin_WantsMidiInput`: an `if (isMidiInputBusEnabled && data.inputEvents != nullptr)` guard whose body is the single `MidiEventList::toMidiBuffer (midiBuffer, *data.inputEvents);` call (~lines 3589–3592). Replace that one-liner body with the braced block that is the current vendored `.cpp` lines 3696–3749 (the JUCE-NE-PATCH comment, the `getVST3ClientExtensions()` fetch, the per-event `Vst3RawEvent` build loop for kNoteOn/kNoteOff/kNoteExpressionValue, `ext->onVst3RawEvent(raw)`, and the trailing `MidiEventList::toMidiBuffer` call — all inside braces, still guarded by `#if JucePlugin_WantsMidiInput`). Copy that block verbatim; do not paraphrase the identifiers.

    Both re-vendored files MUST retain the literal JUCE-NE-PATCH marker string (CI grep gate depends on it). The full verbatim NE text also lives in RESEARCH.md §Code Examples — use the current vendored files as the byte-exact source.
  </action>
  <verify>
    <automated>test -f vendored/JUCE-overrides/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h && test ! -e vendored/JUCE-overrides/modules/juce_audio_processors/utilities/juce_VST3ClientExtensions.h && grep -q 'JUCE-NE-PATCH' vendored/JUCE-overrides/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h && grep -q 'virtual void onVst3RawEvent' vendored/JUCE-overrides/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h && grep -q 'JUCE-NE-PATCH' vendored/JUCE-overrides/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp && grep -q 'ext->onVst3RawEvent (raw)' vendored/JUCE-overrides/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp && grep -q 'version:.*8\.0\.14' "$SCRATCH/JUCE-8.0.14-pristine/modules/juce_core/juce_core.h"</automated>
  </verify>
  <done>Vendored header exists only at the headless path with the NE struct + virtual inserted; old-path header removed; vendored `.cpp` is 8.0.14-based with the braced NE block re-stitched; both files carry the JUCE-NE-PATCH marker; scratch pristine 8.0.14 tree in place.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint CI grep gates + module.cmake to the headless header path (both-paths-tolerant)</name>
  <files>.github/workflows/build-and-release.yml, modules/tuning/note-expression/module.cmake</files>
  <action>
    Repoint the header-path touchpoints so CI resolves the 8.0.14 headless location while local 8.0.9 dev keeps working.

    CI (`.github/workflows/build-and-release.yml`): change ONLY the two `.h` grep-gate lines — macOS gate (~line 104) and Windows gate (~line 453) — from `JUCE/modules/juce_audio_processors/utilities/juce_VST3ClientExtensions.h` to `JUCE/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h`. Do NOT touch: the `.cpp` grep gates (~105/~454, path unchanged), the `cp -R vendored/JUCE-overrides/modules/. JUCE/modules/` copy step (~102/~451 — it walks whatever subtree exists, so it already carries the new headless subtree), the JUCE download steps, or `JUCE_VERSION` (yml:39).

    module.cmake (`modules/tuning/note-expression/module.cmake`, the `_NE_FILE1` guard at ~line 22): replace the single hard-coded old-path assignment with a BOTH-PATHS-TOLERANT resolution — define the new headless path and the old pre-8.0.14 path, then set `_NE_FILE1` to the new headless path if it EXISTS, else fall back to the old path. This makes the configure-time marker check pass on BOTH a local 8.0.9 tree (header at old path) AND a scratch/overlaid 8.0.14 tree (header at headless path), and it survives the future JUCE bump. Keep `_NE_FILE2` (the `.cpp`) unchanged. Also fix the stale FATAL_ERROR wording that says "Ensure JUCE 8.0.4 is installed" — make it version-neutral (it currently misstates the version). This is a real code guard, not a verify-only override.
  </action>
  <verify>
    <automated>test "$(grep -c 'juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h' .github/workflows/build-and-release.yml)" = "2" && test "$(grep -c 'juce_audio_processors/utilities/juce_VST3ClientExtensions.h' .github/workflows/build-and-release.yml)" = "0" && grep -q 'juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h' modules/tuning/note-expression/module.cmake && grep -q 'juce_audio_processors/utilities/juce_VST3ClientExtensions.h' modules/tuning/note-expression/module.cmake && grep -Eq 'if\s*\(\s*EXISTS' modules/tuning/note-expression/module.cmake</automated>
  </verify>
  <done>CI `.h` grep gates (macOS + Windows) point at the headless path with zero references to the old header path remaining; `.cpp` gates + cp -R + JUCE_VERSION untouched; module.cmake `_NE_FILE1` resolves the marker via an EXISTS(new)-else-old fallback and the FATAL_ERROR message no longer hard-codes a wrong version.</done>
</task>

<task type="auto">
  <name>Task 3: Rename old patch → 8.0.9, add regenerated 8.0.14 patch, update apply-juce-patches.sh</name>
  <files>scripts/juce-patches/note-expression-juce-8.0.4.patch, scripts/juce-patches/note-expression-juce-8.0.14.patch, scripts/apply-juce-patches.sh</files>
  <action>
    Keep both patch files (locked decision) and regenerate the 8.0.14 one.

    Rename: `git mv scripts/juce-patches/note-expression-juce-8.0.4.patch scripts/juce-patches/note-expression-juce-8.0.9.patch`. Note in the SUMMARY the RESEARCH A1 caveat — the renamed file retains 8.0.4-vs-8.0.9 drift hunks and is not a clean pristine-8.0.9 NE-only diff; it is out of scope to re-generate here (local install is already patched and the script skips via marker). Honor the locked rename decision literally.

    Regenerate the 8.0.14 patch: produce scripts/juce-patches/note-expression-juce-8.0.14.patch as a `patch -p1`-applyable unified diff (from the JUCE root) whose ONLY hunks are the two NE additions — the header hunk on `modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h` and the `.cpp` braced-block hunk on `modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp`. Generate it by diffing the pristine 8.0.14 tree ($SCRATCH/JUCE-8.0.14-pristine) against the re-based vendored files from Task 1: diff each of the two files (pristine vs the vendored override), normalize the diff header paths to `a/modules/...` and `b/modules/...` (so `-p1` from a JUCE root applies), and concatenate. Update the patch header comment to state Target: JUCE 8.0.14. The diff must be LF-normalized so it also applies on the Windows CI download.

    Update `scripts/apply-juce-patches.sh`: change the `PATCH_FILE` string (~line 22) from `note-expression-juce-8.0.4.patch` to `note-expression-juce-8.0.9.patch` so the local-dev applier still finds its patch after the rename. Do NOT change the OLD 8.0.9 header path at ~line 39 (`HEADER_FILE=.../juce_audio_processors/utilities/...`) — the local install is still 8.0.9 (RESEARCH pitfall #4). Update the header-comment reference to the patch filename to match.
  </action>
  <verify>
    <automated>test -f scripts/juce-patches/note-expression-juce-8.0.9.patch && test -f scripts/juce-patches/note-expression-juce-8.0.14.patch && test ! -e scripts/juce-patches/note-expression-juce-8.0.4.patch && grep -q 'note-expression-juce-8.0.9.patch' scripts/apply-juce-patches.sh && grep -q 'juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h' scripts/juce-patches/note-expression-juce-8.0.14.patch && rm -rf "$SCRATCH/patch-drycheck" && cp -R "$SCRATCH/JUCE-8.0.14-pristine" "$SCRATCH/patch-drycheck" && ( cd "$SCRATCH/patch-drycheck" && patch -p1 --dry-run < "$OLDPWD/scripts/juce-patches/note-expression-juce-8.0.14.patch" )</automated>
  </verify>
  <done>Old patch renamed to `-8.0.9.patch` (git-tracked as rename); new `-8.0.14.patch` contains only the two NE hunks (header on the headless path), Target header says 8.0.14, and dry-run-applies cleanly onto a pristine 8.0.14 tree; apply-juce-patches.sh PATCH_FILE points at the 8.0.9 filename with the OLD 8.0.9 header path preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub JUCE release → scratch dir | External source tarball (pristine 8.0.14) crosses into the local build; overlaid vendored files re-enter as compiled code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-k5o-01 | Tampering | Scratch 8.0.14 download from GitHub releases | low | accept | Fetched over HTTPS from the official `juce-framework/JUCE` release URL (same source CI uses); scratch-only, never overwrites `/Users/taylorbrook/JUCE`. Version asserted via juce_core.h `version: 8.0.14` in Task 1 verify. |
| T-k5o-02 | Tampering | NE re-stitch drifts from verified block | medium | mitigate | NE text copied verbatim from the current vendored files (byte-exact source of truth); JUCE-NE-PATCH marker + `onVst3RawEvent` presence grep-gated per file; scratch-8.0.14 compile-check catches any surrounding-context drift. |
| T-k5o-03 | Denial of Service | Local 8.0.9 dev build regresses (module.cmake FATAL_ERROR) | high | mitigate | `_NE_FILE1` guard is both-paths-tolerant (EXISTS(new headless) else old path) — verified to pass on both 8.0.9 local and scratch 8.0.14; no local-dev regression. |
</threat_model>

<verification>
## End-to-end compile-check (the locked verification, run after all three tasks)

Prove the re-stitched 8.0.14 code actually compiles — not just that paths line up — without touching the local JUCE install:

1. Overlay the re-based vendored overrides onto the scratch 8.0.14 tree (mirrors the CI cp -R step):
   `cp -R vendored/JUCE-overrides/modules/. "$SCRATCH/JUCE-8.0.14/modules/"`
2. Confirm the marker landed at the headless path in the overlaid tree:
   `grep -q 'JUCE-NE-PATCH' "$SCRATCH/JUCE-8.0.14/modules/juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h"`
   `grep -q 'JUCE-NE-PATCH' "$SCRATCH/JUCE-8.0.14/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp"`
3. Configure with JUCE_DIR pointed at the scratch tree and a temp build dir (local /Users/taylorbrook/JUCE stays 8.0.9, untouched):
   `JUCE_DIR="$SCRATCH/JUCE-8.0.14" cmake -B "$SCRATCH/ne-rebase-build" -S . -DCMAKE_BUILD_TYPE=Release`
   (module.cmake's both-paths guard must find the marker at the headless path → no FATAL_ERROR.)
4. Build the validated NE consumer:
   `cmake --build "$SCRATCH/ne-rebase-build" --target O-Lyrica_VST3`
   Expect exit 0 and a produced O-Lyrica VST3 artefact. A clean compile confirms the re-stitched header + wrapper are valid against pristine 8.0.14.

If configure fails at the note-expression marker guard → module.cmake path fallback is wrong (Task 2). If the `.cpp`/`.h` fail to compile → NE re-stitch drifted from the verified block (Task 1).
</verification>

<success_criteria>
- Vendored header exists only at `.../juce_audio_processors_headless/utilities/juce_VST3ClientExtensions.h` with the NE struct + virtual; old-path header removed.
- Vendored `.cpp` is pristine-8.0.14 + the braced NE block; both files carry JUCE-NE-PATCH.
- CI `.h` grep gates (macOS + Windows) repointed to the headless path; `.cpp` gates, cp -R, and JUCE_VERSION unchanged.
- `module.cmake` `_NE_FILE1` resolves the marker on both 8.0.9 (old path) and 8.0.14 (headless path) via EXISTS-fallback; version-neutral FATAL_ERROR message.
- `note-expression-juce-8.0.4.patch` renamed → `-8.0.9.patch`; new `-8.0.14.patch` (two NE hunks, headless header path, Target 8.0.14) dry-run-applies onto pristine 8.0.14.
- `apply-juce-patches.sh` PATCH_FILE → 8.0.9 filename; old 8.0.9 header path at :39 preserved.
- O-Lyrica_VST3 builds exit 0 against the scratch 8.0.14 overlay via JUCE_DIR override; `/Users/taylorbrook/JUCE` and CI `JUCE_VERSION` never modified.
- All work committed on branch `quick/260719-k5o-juce-ne-rebase-8014` (unmergeable-by-design; do not merge to main).
</success_criteria>

<output>
Create `.planning/quick/260719-k5o-re-base-the-note-expression-vendored-pat/260719-k5o-SUMMARY.md` when done. Record: the scratch download path used, the RESEARCH A1 caveat on the renamed 8.0.9 patch (retains 8.0.4-vs-8.0.9 drift hunks — not regenerated here), the O-Lyrica_VST3 compile-check result, and a reminder that this branch is unmergeable until the JUCE_VERSION bump lands.
</output>
