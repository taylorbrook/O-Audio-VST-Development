---
phase: 01-agent-contracts
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - .claude/skills/contract-validation/SKILL.md
  - .claude/schemas/CHANGELOG.md
  - .planning/phases/01-agent-contracts/MISSING-AGENT-SPECS.md
  - .claude/skills/plugin-workflow/SKILL.md
  - .claude/skills/build-automation/SKILL.md
  - .claude/skills/plugin-ideation/SKILL.md
  - .claude/skills/plugin-planning/SKILL.md
  - .claude/skills/plugin-testing/SKILL.md
  - .claude/skills/plugin-improve/SKILL.md
  - .claude/skills/ui-mockup/SKILL.md
  - .claude/skills/plugin-lifecycle/SKILL.md
  - .claude/skills/deep-research/SKILL.md
autonomous: true

must_haves:
  truths:
    - "Contract validation protocol is documented and ready to enforce"
    - "Each core agent SKILL.md includes validation enforcement instructions"
    - "Validation produces actionable error messages (field + expected + received + fix)"
    - "Missing agents are fully specified and ready to implement"
    - "Contract changes are tracked in centralized changelog"
  artifacts:
    - path: ".claude/skills/contract-validation/SKILL.md"
      provides: "Validation protocol and error format"
      contains: "CONTRACT VIOLATION"
    - path: ".claude/schemas/CHANGELOG.md"
      provides: "Centralized contract version tracking"
      contains: "## Version History"
    - path: ".planning/phases/01-agent-contracts/MISSING-AGENT-SPECS.md"
      provides: "Full specs for missing agents identified in audit"
      contains: "## Agent Specifications"
  key_links:
    - from: ".claude/skills/contract-validation/SKILL.md"
      to: ".claude/schemas/agent-contracts/*.json"
      via: "validation loads schemas from directory"
      pattern: "agent-contracts"
    - from: ".planning/phases/01-agent-contracts/MISSING-AGENT-SPECS.md"
      to: ".planning/phases/01-agent-contracts/01-01-AUDIT.md"
      via: "specs address gaps identified in audit"
      pattern: "Gaps Identified"
    - from: ".claude/skills/plugin-workflow/SKILL.md"
      to: ".claude/skills/contract-validation/SKILL.md"
      via: "agent references validation protocol"
      pattern: "contract-validation"
---

<objective>
Implement contract validation protocol, embed enforcement in all 9 core agents, and create full specifications for missing agents.

Purpose: Validation ensures contracts are enforced, not just documented. Each core agent must reference and follow the validation protocol at entry. Missing agent specs prepare the system for future expansion to cover workflow gaps.

Output: Contract validation skill, 9 updated agent SKILL.md files with validation enforcement, centralized changelog, and complete specs for all missing agents identified in the audit.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-agent-contracts/01-CONTEXT.md
@.planning/phases/01-agent-contracts/01-RESEARCH.md
@.planning/phases/01-agent-contracts/01-01-SUMMARY.md
@.planning/phases/01-agent-contracts/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create contract validation skill</name>
  <files>
    .claude/skills/contract-validation/SKILL.md
  </files>
  <action>
Create a new skill that defines the validation protocol for agent contracts.

This is a **protocol skill** - it documents how Claude should validate contracts, not a runtime validator. Claude reads this at agent entry to know how to validate.

Create `.claude/skills/contract-validation/SKILL.md`:
```markdown
---
name: contract-validation
description: Protocol for validating agent input/output contracts. Read at agent entry to enforce contracts. Not invoked directly - embedded in agent startup sequence.
---

# Contract Validation Protocol

## Purpose

Enforce agent contracts by validating inputs at entry and outputs at exit. Per CONTEXT.md decisions: fail fast with actionable errors.

## When to Validate

**Input Validation:**
- At agent entry, before any processing
- Load: `.claude/schemas/agent-contracts/{agent-name}.input.json`
- Validate invocation context against schema

**Output Validation:**
- Before agent exit, after processing complete
- Load: `.claude/schemas/agent-contracts/{agent-name}.output.json`
- Validate return value against schema

## Validation Process

### Step 1: Load Schema
```
schema_path = ".claude/schemas/agent-contracts/{agent-name}.{input|output}.json"
schema = read_json(schema_path)
```

### Step 2: Validate Against Schema
For each required field:
- Check field exists
- Check type matches
- Check constraints (enum, pattern, minLength, etc.)

For additionalProperties: false:
- Reject any fields not in schema

### Step 3: On Failure - Produce Actionable Error

Error format (MUST follow exactly):
```
CONTRACT VIOLATION: {agent-name} {input|output} invalid

Field: {json_path_to_field}
Expected: {schema_expectation}
Received: {actual_value_or_type}

Fix: {specific_instruction_to_resolve}
```

Examples:

```
CONTRACT VIOLATION: plugin-workflow input invalid

Field: plugin_name
Expected: string matching pattern ^[A-Z][a-zA-Z0-9-]*$
Received: "my-plugin" (lowercase start)

Fix: Capitalize plugin name: "My-plugin" or "MyPlugin"
```

```
CONTRACT VIOLATION: build-automation input invalid

Field: invoker
Expected: one of ["plugin-workflow", "plugin-improve", "plugin-lifecycle", "manual"]
Received: "user"

Fix: Use "manual" for direct user invocation
```

```
CONTRACT VIOLATION: plugin-ideation output invalid

Field: brief_path
Expected: string (required field)
Received: undefined

Fix: Ensure BRIEF.md path is returned in output
```

### Step 4: On Success - Continue

If validation passes, proceed with agent logic.

## Validation Strictness

Per CONTEXT.md decisions:
- **Required fields**: Most fields required (strict)
- **Unknown fields**: Rejected (additionalProperties: false)
- **Type coercion**: None - types must match exactly

## Embedding in Agents

Each core agent SKILL.md should include at startup:
```
## Contract Validation

Before processing, validate inputs against contract:
- Schema: `.claude/schemas/agent-contracts/{agent-name}.input.json`
- Protocol: `.claude/skills/contract-validation/SKILL.md`
- On violation: Stop and report error per contract-validation protocol
- On success: Continue to main logic
```

## Error Severity

All contract violations are **blocking**. Do not:
- Attempt to guess missing values
- Coerce types
- Proceed with partial data
- Warn and continue

Stop immediately and report the violation.
```
  </action>
  <verify>
File exists at `.claude/skills/contract-validation/SKILL.md` with:
- "CONTRACT VIOLATION" error format documented
- Field/Expected/Received/Fix structure
- Examples of validation errors
- Step-by-step validation process
- "Embedding in Agents" section with template
  </verify>
  <done>
Validation protocol documented. Agents can reference this for consistent error handling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Embed validation enforcement in all 9 core agent SKILL.md files</name>
  <files>
    .claude/skills/plugin-workflow/SKILL.md
    .claude/skills/build-automation/SKILL.md
    .claude/skills/plugin-ideation/SKILL.md
    .claude/skills/plugin-planning/SKILL.md
    .claude/skills/plugin-testing/SKILL.md
    .claude/skills/plugin-improve/SKILL.md
    .claude/skills/ui-mockup/SKILL.md
    .claude/skills/plugin-lifecycle/SKILL.md
    .claude/skills/deep-research/SKILL.md
  </files>
  <action>
Update each of the 9 core agent SKILL.md files to include a "Contract Validation" section that enforces the validation protocol at agent entry.

For each agent SKILL.md, add the following section near the top (after frontmatter, before main logic):

```markdown
## Contract Validation

Before processing any request, validate inputs against the contract:

1. **Load schema:** `.claude/schemas/agent-contracts/{agent-name}.input.json`
2. **Validate:** Check all required fields present, types match, constraints satisfied
3. **On violation:** Stop immediately. Report error using format from `.claude/skills/contract-validation/SKILL.md`:
   ```
   CONTRACT VIOLATION: {agent-name} input invalid

   Field: {field_name}
   Expected: {from_schema}
   Received: {actual_value}

   Fix: {actionable_instruction}
   ```
4. **On success:** Proceed to main agent logic

See `.claude/skills/contract-validation/SKILL.md` for full validation protocol.
```

This ensures every core agent:
- References the validation protocol
- Knows to fail fast on invalid inputs
- Produces consistent error messages

Note: Plan 01-02 Task 3 already adds a "Contract" section with schema paths. This task adds the enforcement instructions that tell the agent HOW to validate, not just WHERE the schemas are.
  </action>
  <verify>
All 9 SKILL.md files contain:
- A "## Contract Validation" section
- Reference to `.claude/skills/contract-validation/SKILL.md`
- Instructions to stop on violation
- The CONTRACT VIOLATION error format template
  </verify>
  <done>
All 9 core agents now include validation enforcement instructions. Agent invocation will fail fast with clear error when inputs are invalid.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create centralized changelog and missing agent specifications</name>
  <files>
    .claude/schemas/CHANGELOG.md
    .planning/phases/01-agent-contracts/MISSING-AGENT-SPECS.md
  </files>
  <action>
**Part A: Create centralized changelog**

Create `.claude/schemas/CHANGELOG.md`:
```markdown
# Schema Changelog

Centralized version history for all agent contract schemas.

## Versioning Policy

Per CONTEXT.md: Full semver (MAJOR.MINOR.PATCH)
- **MAJOR**: Breaking changes (removed/renamed required fields, type changes)
- **MINOR**: New optional fields, loosened constraints
- **PATCH**: Documentation, formatting, examples

## Version History

### v1.0.0 - {date}

**Initial Release**

Created input/output schemas for 9 core agents:
- plugin-workflow (orchestrates implementation stages)
- build-automation (handles builds and installation)
- plugin-ideation (gathers creative vision)
- plugin-planning (creates architecture and roadmap)
- plugin-testing (runs tests and validation)
- plugin-improve (post-completion improvements)
- ui-mockup (generates UI previews)
- plugin-lifecycle (manages status transitions)
- deep-research (investigates technical topics)

Common types:
- plugin-reference (standard plugin identifier)

All schemas:
- Use JSON Schema draft 2020-12
- Set additionalProperties: false
- Include descriptive field documentation

### Schema Index

| Agent | Input Schema | Output Schema | Version |
|-------|--------------|---------------|---------|
| plugin-workflow | plugin-workflow.input.json | plugin-workflow.output.json | 1.0.0 |
| build-automation | build-automation.input.json | build-automation.output.json | 1.0.0 |
| plugin-ideation | plugin-ideation.input.json | plugin-ideation.output.json | 1.0.0 |
| plugin-planning | plugin-planning.input.json | plugin-planning.output.json | 1.0.0 |
| plugin-testing | plugin-testing.input.json | plugin-testing.output.json | 1.0.0 |
| plugin-improve | plugin-improve.input.json | plugin-improve.output.json | 1.0.0 |
| ui-mockup | ui-mockup.input.json | ui-mockup.output.json | 1.0.0 |
| plugin-lifecycle | plugin-lifecycle.input.json | plugin-lifecycle.output.json | 1.0.0 |
| deep-research | deep-research.input.json | deep-research.output.json | 1.0.0 |

## Future Changes

When modifying schemas:
1. Determine change type (MAJOR/MINOR/PATCH)
2. Update affected schema files
3. Add changelog entry with:
   - Version number
   - Date
   - Description of changes
   - Migration notes for MAJOR changes
4. Update Schema Index table
```

**Part B: Create missing agent specifications**

Using the prioritized gap list from 01-01-AUDIT.md, create full specifications for missing agents.

Per CONTEXT.md: For each gap, produce full agent spec ready to implement:
- Name
- Purpose
- Inputs (JSON schema format)
- Outputs (JSON schema format)
- Boundaries (does/doesn't)

AGNT-07 specifically mentions: music theory, aesthetics

Create `.planning/phases/01-agent-contracts/MISSING-AGENT-SPECS.md`:
```markdown
# Missing Agent Specifications

Specifications for agents identified as gaps in the workflow audit. Each spec is complete enough to implement as a new skill.

## Agent Specifications

### music-theory-agent

**Priority:** {from audit}
**Gap Addressed:** {from audit}

**Purpose:**
Provides music theory calculations and guidance for tuning, temperament, intervals, and harmonic relationships. Consulted during DSP implementation for plugins involving pitch, harmony, or intonation.

**Input Contract:**
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "music-theory-agent.input",
  "type": "object",
  "required": ["query_type"],
  "properties": {
    "query_type": {
      "type": "string",
      "enum": ["interval_ratio", "temperament_frequencies", "chord_voicing", "scale_degrees"],
      "description": "Type of music theory calculation needed"
    },
    "parameters": {
      "type": "object",
      "description": "Query-specific parameters"
    },
    "context": {
      "type": "object",
      "properties": {
        "plugin_name": { "type": "string" },
        "use_case": { "type": "string" }
      }
    }
  },
  "additionalProperties": false
}
```

**Output Contract:**
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "music-theory-agent.output",
  "type": "object",
  "required": ["status", "result"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["success", "error"]
    },
    "result": {
      "type": "object",
      "description": "Query-specific result data"
    },
    "explanation": {
      "type": "string",
      "description": "Human-readable explanation of the calculation"
    },
    "code_snippet": {
      "type": "string",
      "description": "Optional C++ code implementing the calculation"
    }
  },
  "additionalProperties": false
}
```

**Boundaries:**

*This Agent DOES:*
- Calculate just intonation ratios for any interval
- Generate frequency tables for various temperaments
- Provide chord voicing suggestions
- Explain scale/mode relationships
- Generate C++ code snippets for pitch calculations

*This Agent DOES NOT:*
- Implement DSP code directly (use dsp-agent)
- Make architectural decisions (use plugin-planning)
- Handle audio buffer processing
- Design user interfaces

**Handoffs:**
- Receives from: dsp-agent (for tuning calculations)
- Outputs to: dsp-agent (calculation results and code snippets)

---

### aesthetics-agent

**Priority:** {from audit}
**Gap Addressed:** {from audit}

**Purpose:**
Provides design guidance for plugin UI aesthetics, visual consistency, and user experience. Consulted during GUI implementation for color schemes, layout principles, and professional polish.

**Input Contract:**
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "aesthetics-agent.input",
  "type": "object",
  "required": ["query_type"],
  "properties": {
    "query_type": {
      "type": "string",
      "enum": ["color_scheme", "layout_review", "typography", "visual_hierarchy", "polish_suggestions"],
      "description": "Type of aesthetic guidance needed"
    },
    "context": {
      "type": "object",
      "properties": {
        "plugin_name": { "type": "string" },
        "plugin_type": { "type": "string" },
        "existing_mockup": { "type": "string" },
        "brand_guidelines": { "type": "string" }
      }
    }
  },
  "additionalProperties": false
}
```

**Output Contract:**
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "aesthetics-agent.output",
  "type": "object",
  "required": ["status", "recommendations"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["success", "error"]
    },
    "recommendations": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "category": { "type": "string" },
          "suggestion": { "type": "string" },
          "rationale": { "type": "string" },
          "priority": { "type": "string", "enum": ["high", "medium", "low"] }
        }
      }
    },
    "color_palette": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Recommended hex colors if applicable"
    }
  },
  "additionalProperties": false
}
```

**Boundaries:**

*This Agent DOES:*
- Recommend color schemes based on plugin purpose
- Review layouts for visual balance
- Suggest typography improvements
- Identify visual hierarchy issues
- Provide polish suggestions for professional look

*This Agent DOES NOT:*
- Implement CSS/HTML code (use gui-agent)
- Create mockup images (use ui-mockup)
- Make functional decisions
- Override user's creative vision

**Handoffs:**
- Receives from: gui-agent (for design review)
- Outputs to: gui-agent (recommendations to implement)

---

{Additional agents as identified in audit...}

## Implementation Priority

| Agent | Priority | Rationale | Phase |
|-------|----------|-----------|-------|
| music-theory-agent | HIGH | Multiple plugins need tuning math | Future |
| aesthetics-agent | MEDIUM | Improves polish but not blocking | Future |
| ... | ... | ... | ... |

## Notes

These specs are ready for implementation but deferred to future phases. Phase 1 focuses on contracts for existing agents.
```

Reference the actual gaps from AUDIT.md to determine which agents to spec and their priorities.
  </action>
  <verify>
File exists at `.claude/schemas/CHANGELOG.md` with:
- Version history section showing v1.0.0
- Schema index table listing all 9 agents
- Versioning policy documented

File exists at `.planning/phases/01-agent-contracts/MISSING-AGENT-SPECS.md` with:
- At least 2 agent specifications (music-theory, aesthetics as mentioned in AGNT-07)
- Each spec has input/output JSON schemas
- Each spec has boundaries (does/doesn't)
- Implementation priority table
  </verify>
  <done>
Changelog created tracking all v1.0.0 schemas. Missing agents fully specified with contracts and boundaries. Ready for future implementation.
  </done>
</task>

</tasks>

<verification>
- [ ] contract-validation/SKILL.md exists with error format and embedding instructions
- [ ] All 9 core agent SKILL.md files include "## Contract Validation" section
- [ ] Each agent SKILL.md references the validation protocol
- [ ] CHANGELOG.md exists with v1.0.0 entry
- [ ] MISSING-AGENT-SPECS.md exists with at least 2 agent specs
- [ ] Each spec has input/output schemas
- [ ] Each spec has boundaries
- [ ] All gaps from audit are addressed
</verification>

<success_criteria>
1. Validation protocol documented with actionable error format
2. All 9 core agents have validation enforcement embedded in SKILL.md
3. Changelog tracks all v1.0.0 schemas
4. Every gap from audit has a complete agent spec
5. Specs include full JSON schemas (not just descriptions)
6. Ready for phase completion
</success_criteria>

<output>
After completion, create `.planning/phases/01-agent-contracts/01-03-SUMMARY.md`
</output>
