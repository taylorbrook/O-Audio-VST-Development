---
phase: 15-context-persistence
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/implement.md
  - .claude/skills/plugin-workflow/references/workflow-mode.md
  - .claude/skills/plugin-workflow/SKILL.md
autonomous: true

must_haves:
  truths:
    - "Running /implement --auto PluginName auto-generates discuss, research, and plan phases without user interaction"
    - "--auto mode is distinct from --express mode"
    - "Auto mode falls back to manual on any error"
  artifacts:
    - path: ".claude/commands/implement.md"
      provides: "Implement command with --auto flag documentation"
      contains: "--auto"
    - path: ".claude/skills/plugin-workflow/references/workflow-mode.md"
      provides: "Auto mode detection and handling logic"
      contains: "auto"
    - path: ".claude/skills/plugin-workflow/SKILL.md"
      provides: "Orchestration with auto mode discuss/research/plan generation"
      contains: "auto"
  key_links:
    - from: ".claude/commands/implement.md"
      to: ".claude/skills/plugin-workflow/SKILL.md"
      via: "skill invocation with --auto flag"
      pattern: "--auto"
    - from: ".claude/skills/plugin-workflow/references/workflow-mode.md"
      to: ".claude/skills/plugin-workflow/SKILL.md"
      via: "mode detection function"
      pattern: "auto.*mode|getWorkflowMode"
    - from: ".claude/skills/plugin-workflow/SKILL.md"
      to: "discuss phase"
      via: "auto-generate CONTEXT.md from contracts instead of interactive questions"
      pattern: "auto.*discuss|auto_generate"
---

<objective>
Add an `--auto` flag to `/implement` that auto-generates discuss, research, and plan phases without user interaction.

Purpose: The current `--express` mode auto-advances between phases but still runs each phase normally. There is no way to fully auto-generate all planning artifacts (CONTEXT.md, RESEARCH.md, PLAN.md) for a plugin without user interaction. The `--auto` flag enables true hands-off plan generation, which is critical for express plugin creation where the user wants to go from completed Stage 0 to implementation with a single command.

Output: Updated implement.md with --auto flag, updated workflow-mode.md with auto mode logic, updated SKILL.md with auto-generate behavior for discuss/research/plan phases.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-context-persistence/15-RESEARCH.md
@.claude/commands/implement.md
@.claude/skills/plugin-workflow/references/workflow-mode.md
@.claude/skills/plugin-workflow/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --auto flag to implement command and workflow-mode reference</name>
  <files>.claude/commands/implement.md, .claude/skills/plugin-workflow/references/workflow-mode.md</files>
  <action>
**Part A: Update `.claude/commands/implement.md`**

1. Add `--auto` to the `argument-hint` frontmatter: `"[PluginName?] [--express] [--auto] [--skip-discuss] [--skip-research] [--skip-verify]"`

2. Add `--auto` to the Flags table:
   ```
   | `--auto` | Auto-generate discuss/research/plan without user interaction |
   ```

3. Add a new section after "Workflow Mode" explaining the three modes:
   ```markdown
   **Auto mode (`--auto`):**
   - Auto-generates CONTEXT.md from existing contracts (BRIEF.md, parameter-spec.md)
   - Auto-runs research phase non-interactively
   - Auto-generates PLAN.md from research output
   - Execute and verify phases run normally
   - Falls back to manual mode on any error
   - Different from express: express auto-advances but still runs each phase normally; auto generates planning artifacts without interaction
   ```

4. Add an example:
   ```bash
   # Fully automated: generate plans and implement without interaction
   /implement O-IntonationPad --auto
   ```

**Part B: Update `.claude/skills/plugin-workflow/references/workflow-mode.md`**

> **NOTE:** workflow-mode.md is a Claude Code skill reference document (prose instructions for Claude), not executable code. Code examples below are ILLUSTRATIVE — they show Claude what behavior to implement when following the instructions. The actual "implementation" is the prose description that Claude reads and follows.

1. Add "auto" to the valid mode values section:
   ```
   WORKFLOW_MODE="express" | "manual" | "auto"  # Default: "manual"
   ```

2. Update the mode validation prose to accept "auto" as a valid mode alongside "express" and "manual". If an unrecognized mode is encountered, default to "manual".

3. Add an "Auto Mode Behavior" section after the existing "Express Mode" section. This section describes (in prose) what Claude should do when auto mode is active:

   **Auto-Generate Context (autoGenerateContext):**
   Describe that in auto mode, Claude generates CONTEXT.md from existing contracts instead of running interactive questioning. The prose should instruct Claude to:
   - Read BRIEF.md, parameter-spec.md, research/ARCHITECTURE.md, and any previous stage VERIFICATION.md
   - Compile a CONTEXT.md that references these source documents, lists requirements extracted from contracts, and notes constraints (follow ARCHITECTURE.md, implement all parameters, real-time safe)
   - Mark the CONTEXT.md as "Auto-Generated from existing contracts (no interactive session)"

   Include an illustrative code example showing the contract-reading and CONTEXT.md structure (JavaScript pseudocode), clearly labeled as illustrative:
   ```markdown
   <!-- Illustrative: shows Claude what behavior to implement -->
   ```

   **Auto-Generate Research (autoGenerateResearch):**
   Describe that in auto mode, Claude invokes the research phase non-interactively — the research agent reads CONTEXT.md and produces RESEARCH.md without asking clarifying questions. Include an illustrative example of the non-interactive prompt.

   **Error Handling in Auto Mode:**
   Describe that auto mode drops to manual mode on ANY error (same fallback behavior as express mode). Claude should log the error and switch to manual for the remainder of the workflow.
  </action>
  <verify>
1. implement.md contains `--auto` in argument-hint and flags table
2. implement.md describes auto mode as distinct from express mode
3. workflow-mode.md accepts "auto" as a valid mode value in its prose instructions
4. workflow-mode.md has "Auto Mode Behavior" section describing autoGenerateContext and autoGenerateResearch behavior in prose (with illustrative code examples clearly labeled as illustrative)
5. Both files describe the fallback-to-manual behavior on error
  </verify>
  <done>The --auto flag is documented in implement.md. workflow-mode.md contains prose instructions describing auto mode behavior (context generation, non-interactive research, error fallback) with illustrative code examples. Auto mode is clearly distinct from express mode.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate auto mode into SKILL.md orchestration loop</name>
  <files>.claude/skills/plugin-workflow/SKILL.md</files>
  <action>
Update `.claude/skills/plugin-workflow/SKILL.md` to handle the `auto` workflow mode in the orchestration loop.

> **NOTE:** SKILL.md is a markdown instruction file that Claude reads and follows — it contains prose orchestration instructions, not executable Python/JavaScript code. All updates below are prose additions to SKILL.md's existing markdown sections.

**1. Update the Discuss Phase section**

In the prose section that describes how the discuss phase runs, add a third behavioral path for auto mode alongside the existing express and manual paths. The updated prose should read approximately:

- **Manual mode:** Run interactive questioning via plugin-discuss-agent to produce CONTEXT.md
- **Express mode:** Auto-advance but still run each phase normally (may pause at decisions)
- **Auto mode:** Auto-generate CONTEXT.md directly from existing contracts (BRIEF.md, parameter-spec.md, ARCHITECTURE.md, previous stage VERIFICATION.md) without any user interaction. No questions are asked. CONTEXT.md is compiled from existing documents and marked as auto-generated.

**2. Update the Research Phase section**

In the prose section that describes how the research phase runs, add instructions for auto mode behavior:

- **Auto mode:** Invoke the research agent with an explicit non-interactive directive. The research agent reads CONTEXT.md and produces RESEARCH.md without asking clarifying questions. Add the instruction: "When workflow mode is auto, append to the research agent prompt: 'Mode: Non-interactive (auto mode) — do not ask clarifying questions.'"

**3. Update the Main Orchestration Loop prose**

In the section that describes phase-to-phase flow and checkpoint behavior:
- Add "auto" alongside "express" in every instruction where express mode auto-advances between phases
- State that auto mode always auto-advances (like express) at phase checkpoints
- Add the note: "CTXP-03: Auto mode generates all planning artifacts without user interaction"

**4. Add an "Auto Mode Output" section**

After the existing Express Mode output example, add a new section showing what auto mode output looks like. Include an example showing the `[auto]` tag prefix on each phase and the standard handoff at the end:

```
/implement O-IntonationPad --auto

--- Stage 1: Foundation ---
  [auto] discuss -> Context auto-generated from BRIEF.md + contracts
  [auto] research -> Research generated non-interactively
  [auto] plan -> PLAN.md auto-generated
  [auto] execute -> foundation-shell-agent running...
  [auto] verify -> Build successful, parameters validated
  Stage 1 complete

--- Handoff ---
Next Up: Stage 2: DSP
Step 1: /clear
Step 2: /implement O-IntonationPad --auto
```

**5. Update the Skip Flags section**

Add a prose note clarifying the distinction: "`--auto` auto-generates discuss and research phases (producing CONTEXT.md and RESEARCH.md from contracts), unlike `--skip-discuss`/`--skip-research` which skip those phases entirely without producing artifacts."
  </action>
  <verify>
1. SKILL.md mentions "auto" mode in the orchestration loop prose instructions
2. The discuss phase section describes auto mode behavior: auto-generate CONTEXT.md from existing contracts
3. The research phase section describes auto mode behavior: invoke research agent non-interactively
4. "Auto Mode Output" section exists showing example auto mode output
5. "CTXP-03" note exists in the orchestration prose
6. Auto mode is described as auto-generating (not skipping) planning artifacts
7. All additions are prose instructions (not Python/JavaScript code blocks presented as implementations)
  </verify>
  <done>SKILL.md prose orchestration instructions describe auto mode: auto-generates CONTEXT.md from contracts, runs research non-interactively, auto-generates plans. Auto mode is clearly documented as generating (not skipping) artifacts, distinct from express mode which auto-advances existing phases.</done>
</task>

</tasks>

<verification>
1. /implement --auto is documented in implement.md with clear description
2. workflow-mode.md detects and handles "auto" as a valid mode
3. SKILL.md orchestration auto-generates discuss/research/plan in auto mode
4. Auto mode falls back to manual on error (verified in workflow-mode.md)
5. Auto mode is clearly distinct from express mode in all three files
</verification>

<success_criteria>
- `/implement --auto PluginName` is fully documented and the orchestration logic auto-generates discuss, research, and plan phases without user interaction
- Auto mode is distinct from express mode: auto generates planning artifacts, express auto-advances existing phases
- Error fallback to manual mode is preserved
</success_criteria>

<output>
After completion, create `.planning/phases/15-context-persistence/15-04-SUMMARY.md`
</output>
