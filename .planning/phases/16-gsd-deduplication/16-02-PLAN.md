---
phase: 16-gsd-deduplication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/16-gsd-deduplication/VALIDATOR-CLASSIFICATION.md
  - /Users/taylorbrook/.claude/agents/gsd-plan-checker.md
  - /Users/taylorbrook/.claude/get-shit-done/workflows/verify-phase.md
  - /Users/taylorbrook/.claude/get-shit-done/workflows/execute-phase.md
autonomous: true

must_haves:
  truths:
    - "A validator classification document explicitly categorizes every PFS validator as either 'structural (replaced by gsd-tools)' or 'domain (preserved)' with rationale for each"
    - "validate-research-frontmatter.py is classified as a domain validator, NOT a structural validator — and this classification is documented with reasoning about its semantic validation vs gsd-tools presence-only validation"
    - "All inline structural verification checks in agent/workflow files are replaced with gsd-tools verify commands — no ad-hoc plan parsing, file counting, or reference checking remains"
    - "All 6 domain validators (DSP, APVTS, WebView, checksums, cross-contract, resource accountability) plus 3 additional (silent-failures, foundation, research-frontmatter) are untouched"
  artifacts:
    - path: ".planning/phases/16-gsd-deduplication/VALIDATOR-CLASSIFICATION.md"
      provides: "Authoritative classification of all PFS validators as structural or domain"
      contains: "domain validator"
      min_lines: 30
    - path: "/Users/taylorbrook/.claude/agents/gsd-plan-checker.md"
      provides: "Plan checker using gsd-tools verify commands for structural checks"
      contains: "gsd-tools"
    - path: "/Users/taylorbrook/.claude/get-shit-done/workflows/verify-phase.md"
      provides: "Phase verification workflow using gsd-tools verify commands"
      contains: "verify plan-structure"
  key_links:
    - from: ".planning/phases/16-gsd-deduplication/VALIDATOR-CLASSIFICATION.md"
      to: ".claude/hooks/validators/"
      via: "classifies each validator file"
      pattern: "validate-.*\\.py"
    - from: "/Users/taylorbrook/.claude/agents/gsd-plan-checker.md"
      to: "/Users/taylorbrook/.claude/get-shit-done/bin/gsd-tools.js"
      via: "gsd-tools verify commands for structural checks"
      pattern: "gsd-tools.*verify"
---

<objective>
Classify all PFS validators and replace inline structural verification checks with gsd-tools verify commands.

Purpose: The PFS has two categories of validators: structural (plan format, phase completeness, references, commits, artifacts, key-links) and domain (DSP safety, APVTS matching, WebView bindings, checksums, cross-contract, resource accountability). GSD 1.18.0 provides a verification suite that covers all 6 structural validators. Some agents/workflows already use gsd-tools verify commands, but others still perform ad-hoc inline structural checks. This plan creates the authoritative classification document (per P34: classify before removing) and migrates remaining inline structural checks to gsd-tools verify calls, while explicitly preserving all domain validators untouched.

Output: VALIDATOR-CLASSIFICATION.md documenting the boundary, updated agent/workflow files with inline structural checks replaced by gsd-tools verify calls. Domain validators and SubagentStop.sh dispatch completely untouched.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-gsd-deduplication/16-RESEARCH.md
@/Users/taylorbrook/.claude/get-shit-done/bin/gsd-tools.js
@/Users/taylorbrook/.claude/agents/gsd-plan-checker.md
@/Users/taylorbrook/.claude/get-shit-done/workflows/verify-phase.md
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-phase.md
@.claude/hooks/SubagentStop.sh
@.claude/hooks/validators/validate-parameters.py
@.claude/hooks/validators/validate-dsp-components.py
@.claude/hooks/validators/validate-gui-bindings.py
@.claude/hooks/validators/validate-checksums.py
@.claude/hooks/validators/validate-cross-contract.py
@.claude/hooks/validators/validate-resource-accountability.py
@.claude/hooks/validators/validate-silent-failures.py
@.claude/hooks/validators/validate-foundation.py
@.claude/hooks/validators/validate-research-frontmatter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create validator classification document (P34 compliance)</name>
  <files>.planning/phases/16-gsd-deduplication/VALIDATOR-CLASSIFICATION.md</files>
  <action>
Create `.planning/phases/16-gsd-deduplication/VALIDATOR-CLASSIFICATION.md` — the authoritative classification document that categorizes every PFS validator as structural (replaced by gsd-tools) or domain (preserved).

This satisfies prior decision P34: "Classify all custom code as duplicate/extension/workaround BEFORE removing."

The document must contain:

**Header:** Title, date, purpose statement referencing P34 and GSDD-02/GSDD-03.

**Section 1: Structural Validators (Replaced by GSD)**

List each structural validation concern with its gsd-tools replacement:

| Structural Check | gsd-tools Command | Status |
|-----------------|-------------------|--------|
| Plan structure (frontmatter, tasks, waves) | `verify plan-structure` | Replaced — used by gsd-plan-checker |
| Phase completeness (plans vs summaries) | `verify phase-completeness` | Replaced — used by verify-phase |
| Reference resolution (@-refs, paths) | `verify references` | Replaced — used by verify-phase |
| Commit verification (hash validation) | `verify commits` | Replaced — used by verify-work |
| Artifact existence + content checks | `verify artifacts` | Replaced — used by verify-phase |
| Key link wiring verification | `verify key-links` | Replaced — used by verify-phase |

Note: These were never separate Python scripts — they were inline instructions in agent/workflow markdown files. The "replacement" means migrating callsites from inline grep/parse to gsd-tools verify commands.

**Section 2: Domain Validators (Preserved — No GSD Equivalent)**

List each domain validator with classification rationale:

| Validator | File | Domain | Why Preserved |
|-----------|------|--------|---------------|
| DSP Components | validate-dsp-components.py | Audio/DSP | Verifies architecture.md DSP components exist in PluginProcessor — requires audio domain knowledge |
| Parameter Matching | validate-parameters.py | Audio/APVTS | Verifies parameter-spec.md IDs match APVTS code — plugin-specific schema |
| GUI Bindings | validate-gui-bindings.py | WebView | Verifies HTML parameter IDs match C++ WebView relay — cross-language binding |
| Checksums | validate-checksums.py | Contract integrity | SHA256 checksums of contract files vs STATUS.md — PFS-specific contract system |
| Cross-Contract | validate-cross-contract.py | Contract consistency | Cross-references params/components across multiple contracts — PFS-specific |
| Resource Accountability | validate-resource-accountability.py | Agent behavior | Verifies agents reported consulting injected resources — PFS-specific |
| Silent Failures | validate-silent-failures.py | DSP safety | JUCE 8 runtime failure pattern detection — audio-specific |
| Foundation | validate-foundation.py | Build safety | CMakeLists.txt and source file existence — could be generic but PFS-specific format |
| Research Frontmatter | validate-research-frontmatter.py | Research quality | 10-field YAML schema with enum values, date patterns, JUCE version format — NOT replaceable by gsd-tools frontmatter validate (presence-only) |

**Section 3: Frontmatter Boundary (GSDD-02)**

Document the boundary between gsd-tools frontmatter validate and PFS validate-research-frontmatter.py:

- gsd-tools `frontmatter validate --schema plan`: Checks 8 fields EXIST (presence only) — phase, plan, type, wave, depends_on, files_modified, autonomous, must_haves
- gsd-tools `frontmatter validate --schema summary`: Checks 6 fields EXIST (presence only)
- gsd-tools `frontmatter validate --schema verification`: Checks 4 fields EXIST (presence only)
- PFS `validate-research-frontmatter.py`: Checks 10 fields with SEMANTIC validation — enum values for `type` and `domain`, date format for `created` and `last_verified`, JUCE version pattern, list content for `keywords`/`stages`/`agents`

Conclusion: gsd-tools handles structural frontmatter validation. PFS Python validator handles semantic research frontmatter validation. No overlap — both are needed.

**Section 4: SubagentStop.sh Dispatch (DO NOT TOUCH)**

State explicitly that SubagentStop.sh dispatches domain validators based on subagent type and is OUT OF SCOPE for this phase. No changes to the hook or its validator invocations.

**Section 5: contract_validator.py (Shared Library — Preserved)**

Note that contract_validator.py is a shared library used by validate-checksums.py and validate-cross-contract.py. It is part of the domain validation infrastructure and is preserved.
  </action>
  <verify>
1. File exists at `.planning/phases/16-gsd-deduplication/VALIDATOR-CLASSIFICATION.md`
2. File has at least 30 lines
3. All 6 structural checks listed with gsd-tools replacements
4. All 9 domain validators listed with preservation rationale
5. Frontmatter boundary section clearly distinguishes gsd-tools (presence) from PFS (semantic)
6. SubagentStop.sh explicitly marked as out of scope
7. validate-research-frontmatter.py classified as domain validator with semantic validation rationale
  </verify>
  <done>Authoritative validator classification document created. Every PFS validator classified as structural (6 — replaced by gsd-tools verify) or domain (9 — preserved). Frontmatter boundary documented. P34 compliance achieved.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline structural checks with gsd-tools verify commands in agents and workflows</name>
  <files>/Users/taylorbrook/.claude/agents/gsd-plan-checker.md, /Users/taylorbrook/.claude/get-shit-done/workflows/verify-phase.md, /Users/taylorbrook/.claude/get-shit-done/workflows/execute-phase.md</files>
  <action>
Audit all agent and workflow files for inline structural verification checks, then replace them with gsd-tools verify commands. The goal is zero ad-hoc structural checking — all structural validation routes through gsd-tools verify suite.

**Step 1: Audit for inline structural checks**

Search these patterns across all agent/workflow files:
- `grep -c` for task/plan counting
- `grep "must_haves"` for manual frontmatter parsing
- Manual `ls` + comparison for plan/summary counting
- Manual `git cat-file` or `git log --oneline` for commit verification
- Manual file existence checks that duplicate `verify artifacts`
- Manual source-target reference checking that duplicates `verify key-links` or `verify references`

Focus on these files (primary suspects from research):
- `/Users/taylorbrook/.claude/agents/gsd-plan-checker.md` — already uses `verify plan-structure` but may have inline checks for other dimensions
- `/Users/taylorbrook/.claude/get-shit-done/workflows/verify-phase.md` — already uses `verify artifacts` and `verify key-links` but may have inline checks too
- `/Users/taylorbrook/.claude/get-shit-done/workflows/execute-phase.md` — may do inline commit verification or plan counting

**Step 2: Replace identified inline checks**

For each inline structural check found, replace with the equivalent gsd-tools verify command:

| Inline Pattern | Replace With |
|---------------|-------------|
| Manual plan/summary count comparison | `node gsd-tools.js verify phase-completeness "${phase}"` |
| Manual @-reference file existence checks | `node gsd-tools.js verify references "${file}"` |
| Manual `git cat-file -t` loops | `node gsd-tools.js verify commits ${hashes}` |
| Manual file existence + line count | `node gsd-tools.js verify artifacts "${plan}"` |
| Manual source→target pattern matching | `node gsd-tools.js verify key-links "${plan}"` |
| Manual frontmatter field grep | `node gsd-tools.js frontmatter get "${file}" --field fieldname` |

IMPORTANT constraints:
- Do NOT modify SubagentStop.sh or any Python domain validator
- Do NOT change any verification LOGIC in verify-phase.md that uses domain-specific checks (e.g., manual grep for JUCE patterns, WebView bindings)
- ONLY replace structural checks that have direct gsd-tools verify equivalents
- If a check is ambiguous (could be structural or domain), leave it as-is and note it in the summary

**Step 3: Verify plan-checker completeness**

Ensure gsd-plan-checker.md's Step 2 ("Load All Plans") uses gsd-tools verify plan-structure consistently for all structural dimensions (Dimensions 1-5). If any dimension still does ad-hoc parsing that verify plan-structure already covers, remove the duplicate and reference the verify plan-structure result instead.

Dimension 7 (Context Compliance) uses agent judgment — leave as-is. This will be enhanced by GSDD-04 in Plan 16-03.

**Step 4: Verify execute-phase completeness**

Check execute-phase.md for any inline structural verification (plan counting, summary verification) that could use gsd-tools. The workflow likely already uses `verify-summary` — confirm and migrate any remaining inline checks.
  </action>
  <verify>
1. Search all agent files for inline structural patterns: `grep -rn "grep.*task\|wc.*PLAN\|cat-file\|ls.*SUMMARY.*PLAN" /Users/taylorbrook/.claude/agents/gsd-*.md /Users/taylorbrook/.claude/get-shit-done/workflows/{verify-phase,execute-phase,verify-work}.md` — structural check hits should be zero (replaced by gsd-tools verify)
2. gsd-plan-checker.md uses `verify plan-structure` for structural dimensions, not ad-hoc grep
3. verify-phase.md uses `verify artifacts`, `verify key-links`, `verify phase-completeness`, `verify references` — not manual file existence loops
4. All 9 domain validators still present and untouched in `.claude/hooks/validators/`
5. SubagentStop.sh is unmodified (diff shows no changes)
6. VALIDATOR-CLASSIFICATION.md exists and is accurate
  </verify>
  <done>All inline structural verification checks replaced with gsd-tools verify commands. Six structural validators fully migrated to gsd-tools verify suite. Nine domain validators preserved untouched. GSDD-02 (frontmatter classification) and GSDD-03 (structural verification replacement) satisfied.</done>
</task>

</tasks>

<verification>
1. VALIDATOR-CLASSIFICATION.md exists with complete classification of all validators
2. Grep for inline structural patterns in agents/workflows returns 0 matches for ad-hoc plan parsing
3. All 9 PFS domain validators are present and unchanged in .claude/hooks/validators/
4. SubagentStop.sh is unmodified
5. gsd-plan-checker.md uses gsd-tools verify plan-structure for structural checking
6. verify-phase.md uses gsd-tools verify commands for all structural verification
7. validate-research-frontmatter.py is classified as domain validator and preserved
</verification>

<success_criteria>
- Authoritative validator classification document exists (P34 compliance)
- validate-research-frontmatter.py classified as domain validator (semantic validation preserved)
- All inline structural verification replaced with gsd-tools verify commands
- Six domain validators (DSP, APVTS, WebView, checksums, cross-contract, resource accountability) plus three additional (silent-failures, foundation, research-frontmatter) completely untouched
- SubagentStop.sh unmodified
- gsd-tools frontmatter validate used for structural presence checks; PFS Python validator retained for semantic research schema
</success_criteria>

<output>
After completion, create `.planning/phases/16-gsd-deduplication/16-02-SUMMARY.md`
</output>
