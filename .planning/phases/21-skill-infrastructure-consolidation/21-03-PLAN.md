---
phase: 21-skill-infrastructure-consolidation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agent-profiles.json
  - .claude/preferences-README.md
  - .claude/references/agent-profiles.json
  - .claude/references/preferences-README.md
  - .gitignore
autonomous: true
requirements:
  - INFR-02
  - INFR-03
  - INFR-04

must_haves:
  truths:
    - "agent-profiles.json lives at .claude/references/agent-profiles.json, not .claude/agent-profiles.json"
    - "preferences-README.md lives at .claude/references/preferences-README.md, not .claude/preferences-README.md"
    - "No ui-test.html files are tracked by git"
    - ".gitignore contains the *ui-test.html pattern"
  artifacts:
    - path: ".claude/references/agent-profiles.json"
      provides: "Agent profile documentation (relocated from .claude/ root)"
    - path: ".claude/references/preferences-README.md"
      provides: "Workflow preferences documentation (relocated from .claude/ root)"
    - path: ".gitignore"
      provides: "Updated ignore patterns including *ui-test.html"
      contains: "ui-test.html"
  key_links:
    - from: ".gitignore"
      to: "plugins/*/.planning/mockups/*ui-test.html"
      via: "gitignore pattern prevents future tracking"
      pattern: "\\*ui-test\\.html"
---

<objective>
Relocate documentation-only files out of .claude/ root and exclude aesthetic test HTML files from the repo.

Purpose: agent-profiles.json and preferences-README.md are pure documentation with no runtime effect, cluttering the .claude/ root alongside active configuration. Moving them to .claude/references/ (where handoff-protocol.md already lives) keeps the root clean. The 73 tracked ui-test.html files are dev artifacts that should not be in version control.

Output: 2 files relocated to .claude/references/, 73 ui-test.html files removed from git tracking, .gitignore updated.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-skill-infrastructure-consolidation/21-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate doc files and remove ui-test.html from tracking</name>
  <files>
    .claude/agent-profiles.json
    .claude/preferences-README.md
    .claude/references/agent-profiles.json
    .claude/references/preferences-README.md
    .gitignore
  </files>
  <action>
**Step 1: Relocate files**

Move agent-profiles.json and preferences-README.md to .claude/references/:
```bash
git mv .claude/agent-profiles.json .claude/references/agent-profiles.json
git mv .claude/preferences-README.md .claude/references/preferences-README.md
```

Per user decision: clean break, no symlinks or backwards-compatibility shims.

After moving, grep the entire .claude/ directory for references to the old paths (`agent-profiles.json` or `preferences-README.md` at `.claude/` root level). If any references found, update them to point to `.claude/references/`. Based on pre-planning grep, no active references exist, but verify at execution time.

**Step 2: Add ui-test.html to .gitignore**

Add the following entry to .gitignore, in a new section after the "Legacy Paths" section at the bottom:

```
# =============================================================================
# Aesthetic Test Previews
# =============================================================================
*ui-test.html
```

Per user decision: scope the ignore pattern specifically to `*ui-test.html` -- do not use broader patterns that could catch legitimate HTML files.

**Step 3: Remove ui-test.html files from git tracking**

Remove all 73 tracked ui-test.html files from the git index without deleting them from disk:
```bash
git ls-files '*ui-test.html' | xargs git rm --cached
```

This keeps the files on disk (developers can still open them locally) but removes them from version control. The .gitignore pattern prevents them from being re-added.

Do NOT delete the files from disk. Do NOT use a broader rm pattern. Only target files matching `*ui-test.html`.
  </action>
  <verify>
    Run: test -f .claude/references/agent-profiles.json && test -f .claude/references/preferences-README.md && echo "PASS: files relocated" || echo "FAIL"
    Run: test ! -f .claude/agent-profiles.json && test ! -f .claude/preferences-README.md && echo "PASS: old locations empty" || echo "FAIL"
    Run: git ls-files '*ui-test.html' | wc -l
    Expected: 0 (no ui-test.html files tracked)
    Run: grep 'ui-test.html' .gitignore
    Expected: *ui-test.html pattern present
  </verify>
  <done>agent-profiles.json and preferences-README.md relocated to .claude/references/. All 73 ui-test.html files removed from git tracking. .gitignore updated with *ui-test.html pattern. Files still exist on disk for local use.</done>
</task>

</tasks>

<verification>
1. `ls .claude/references/agent-profiles.json .claude/references/preferences-README.md` -- both exist
2. `ls .claude/agent-profiles.json .claude/preferences-README.md 2>/dev/null` -- neither exists
3. `git ls-files '*ui-test.html' | wc -l` returns 0
4. `grep 'ui-test.html' .gitignore` returns the pattern
5. `find plugins/ -name '*ui-test.html' | head -5` -- files still exist on disk (not deleted)
</verification>

<success_criteria>
- agent-profiles.json is at .claude/references/agent-profiles.json (not .claude/ root)
- preferences-README.md is at .claude/references/preferences-README.md (not .claude/ root)
- Zero ui-test.html files are tracked by git
- .gitignore contains *ui-test.html pattern
- ui-test.html files still exist on disk (not deleted)
</success_criteria>

<output>
After completion, create `.planning/phases/21-skill-infrastructure-consolidation/21-03-SUMMARY.md`
</output>
