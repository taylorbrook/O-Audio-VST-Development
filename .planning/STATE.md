---
gsd_state_version: 1.0
milestone: v1.5
status: shipped
stopped_at: Completed quick task 260904-q4j
last_updated: "2026-09-05T02:10:14.321Z"
last_activity: 2026-09-04
last_activity_desc: "Completed quick task 260904-q4j: **i18n-zh-lint repaired ahead of the Stage 4 gate flip** — self-test 9/9 → **10/10**, repo-wide findings **8 → 0** (all 8 were Z1 tool defects on O-MicrotonalSampler, every one byte-identical to a glossary root Z5 itself compels). maskLatin() widened on two axes under D-01: a leading-dot file extension (.scl/.kbm/.omspreset) now masks before the Han punctuation scan, and a single unmatched '(' at END of string masks after the balanced replacement (the markup-split floTokensBefore labels). New rule **Z8** under D-02: a plain U+0020 between two Han code points — the shape Stage 3 could only find by READING O-Octagon's puck body, pushed in lintRows so it reaches BODIES. Both RED lines observed and recorded BEFORE the fixes: 'SELF-TEST Z1 BROKEN: fires on a control' (8/9) and 'SELF-TEST Z8 BROKEN: silent on a violation' (9/10). Z8's zero agrees with an independent standalone scan (0 = 0 across 43), and that agreement is load-bearing ONLY because a corpus positive control (O-Chorus label.hoverHelp 悬停帮助 → 悬停 帮助) proved the rule non-inert — commit d8684e49 actually exhibits T-q4j-04, a declared-but-unpushed Z8 column reading a clean 0. Injection reverted by REVERSE TARGETED EDIT (never git checkout — uncommitted lint work was in the same tree), digest byte-identical, git diff --stat -- plugins/ empty. Disclosed blindness: Han + ASCII period + Latin unspaced (混音.mix) now reads as an extension token. FOR STAGE 4: the promotion criterion is MET but the gate flip (exit 2) is deliberately NOT made here; Z8 is scoped to the PLAIN space only so its zero stays checkable — widening it requires redefining the agreement test in the same commit; Z8 sits LAST in CODES and the header block + legend were updated to match. Baseline correction: the .scl/.kbm plugin census is **7**, not the 8 the plan recorded (O-Lyrica carries none) — and it is definition-sensitive: 7 file-grep / 3 any-rendering / 1 zh-Hans rendering. Gates: check-i18n exit 0, i18n-fr-lint exit 0, no en/fr/zh rendering changed anywhere in plugins/. 4 path-scoped commits (ec20d0b1, d8684e49, 97816a94, 18c7f375). Standing: 31, 59, 19, 8, 2, 11, 72, O-Reed tuning tab."
state_head: 18c7f3759ef2f28d02b8011f212e0c1702d79e22
progress:
  total_phases: 25
  completed_phases: 25
  total_plans: 80
  completed_plans: 80
  percent: 100
milestone_name: Microtonal Shared Module & Suite Propagation
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-27)

**Core value:** Reliable collaborative workflow that produces professional-quality plugins -- where agents execute quality work that doesn't require constant rework.
**Current focus:** None — v1.5 shipped. Awaiting `/gsd-new-milestone` for v1.6.

## Current Position

Milestone: v1.5 -- SHIPPED 2026-04-27
Phase: All complete
Plan: All complete
Status: Milestone shipped and archived
Last activity: 2026-09-06 - Completed quick task 260906-uu7: O-Chorus's CJK font tail was INERT (wave 4a): written AFTER the trailing generic, so under zh-Hans Chromium resolved Han through the lang-resolved `serif` (Songti SC) and never reached the PingFang SC the tail named — proven by CSS.getPlatformFontsForNode BEFORE/AFTER on all six tailed stacks (six of nine, not the five-of-eight the v1.5.0 comment claimed; `.settings-toggle` was uncounted). Tail moved before the single trailing `serif` on every stack; O-Chorus 1.6.3 built, installed, `auval -v aufx OuCh OuDv` AU VALIDATION SUCCEEDED at Component Version 1.6.3. Every pin re-measured: NO pin value changed — the zh `normal` line box is 13.00 px under both faces, and the `.settings-select` width pin the plan called most-likely-to-break CLOSED instead (zh intrinsic 64→65, endonyms now language-invariant). Movement is zh-only and width-only (knob labels 19.61→20.00, `voices` tip 2→3 body lines); check-ui-labels 87/0 with [7] 0 movers fr+zh, ui_tip_render_check 345/0, measure-ui 3/2/0/0 identical BEFORE/AFTER. Found: measure-ui's `ff` is the DECLARED stack string, not the resolved face (a Songti grep on it is vacuous); `.settings-select` and `.settings-toggle` line-height pins are inert; the three tail-less glyph stacks (◀/▶/⚙) resolve through Hiragino Mincho ProN / Lucida Grande / Menlo — DEFERRED; BSD `wc -l` pads output so `| wc -l | grep -qx '0'` verifies are false REDs
Prior activity: 2026-09-04 - Completed quick task 260904-g5l: **zh-Hans Stage 3 (hard-case wave) COMPLETE** — O-Octagon v1.12.0, O-Bitrot v1.16.0, O-Chorus v1.6.2 (0 below ship bar), O-MicrotonalSampler v1.27.0; 758/758 zh rows at 'bt'; auval PASS ×4; 9 path-scoped commits. Structural findings for Stage 4 (full list in 260904-g5l-SUMMARY.md): author at 'mt' ALWAYS (premature 'bt' invisible to every gate); read ALL triples --verbose (default prints 12, zero of eight real findings ranked inside them); screen TERMS for two keys → one root co-occurring on a page (Dither/Jitter→抖动, Z5/F1/check-ui-labels all blind); correction loops need a FRESH blind reader + fresh salt; Chinese geometry failures are SHRINKS, and min-width pins are floors — 'already pinned' elements are NOT safe; --emit is table-scoped (no --only-mt); version truth is CMakeLists (quoted VERSION); MUST-FIX before Stage 4 gates i18n-zh-lint: maskLatin() Z1 false-positives on leading-dot extensions (.scl/.kbm, 12 plugins affected); ZH3-10 corrected inventory: 12 gate files hard-code two languages, all wave plugins. Standing: 31, 59, 19, 8, 2, 11, 72, O-Reed tuning tab.
Earlier activity: 2026-09-04 - Completed quick task 260904-f2k: **the two zh tool defects fixed before Stage 3** — Z3 Traditional-only set rederived 4139→3198 (self-mapping TS keys like 像 U+50CF excluded; committed generator scripts/gen-zh-trad-only.js with --verify; 声像/乾 self-test controls added before regenerating, 8/9 RED → 9/9), i18n-zh-backtranslate --ingest now resolves the manifest beside the EMIT batch and refuses loudly (was inert), --emit row ids blinded (opaque 12-hex, per-batch salt, manifest.ids joins, 0 unjoinable). All three controls observed RED first; repo-wide Z3 unchanged at 0 (latent defect). Gates: check-i18n 43/43, i18n-zh-lint self-test 9/9. zh-Hans Stage 3 now unblocked; O-Chorus zh 'mt' bodies still queued (i18n-zh-backtranslate --emit O-Chorus); tip.language en/fr French re-review still open. Standing: 31, 59, 19, 8, 2, 11, 72, O-Reed tuning tab.

Progress: [██████████] 100%

## Carry-Forward

- **BL-01** (Phase 25 UAT): Retest Windows installer on non-admin Windows account with UAC elevation via separate admin credential. Persisted in `.planning/phases/25-package-docs/25-HUMAN-UAT.md`.

Next: `/gsd-new-milestone` to begin v1.6 milestone planning.

## Performance Metrics

**Cumulative (v1.0-v1.4):**

- Total phases completed: 22
- Total plans completed: 72
- Total requirements satisfied: 108

**By Milestone:**

| Milestone | Phases | Plans | Requirements | Timeline |
|-----------|--------|-------|--------------|----------|
| v1.0 | 1-7 | 21 | 35 | 2 days |
| v1.1 | 8-9 | 4 | 13 | 2 days |
| v1.2 | 10-13 | 12 | 15 | 2 days |
| v1.3 | 14-17 | 14 | 22 | 3 days |
| v1.4 | 18-22 | 13 | 23 | 2 days |
| v1.5 | 23-25 | TBD | 33 | in progress |

**v1.4 Plan Metrics:**

| Phase | Plan | Duration | Tasks | Files |
|-------|------|----------|-------|-------|
| 18 | 01 | 1min | 3 | 30 |
| 18 | 02 | 5min | 2 | 20 |
| 18 | 03 | 4min | 1 | 1 |
| 18 | 04 | 1min | 1 | 1 |
| 19 | 01 | 4min | 2 | 3 |
| 20 | 01 | 15min | 2 | 44 |
| 20 | 02 | 1min | 2 | 6 |
| 20 | 03 | 20min | 2 | 12 |
| 21 | 01 | 2min | 2 | 8 |
| 21 | 02 | 2min | 2 | 4 |
| 21 | 03 | 1min | 1 | 76 |
| 22 | 01 | 2min | 2 | 9 |
| 22 | 02 | 2min | 2 | 2 |
| Phase 23 P01 | 4min | 2 tasks | 4 files |
| Phase Phase 23 P02 P23-02 | 4min | 3 tasks tasks | 4 files files |
| 23 | 03 | ~25min | 4 | 6 |
| 23 | 04 | ~12min + human | 4 | 3 |
| 23 | 05 | ~25min + human | 7 | 6 |
| 24 | 01 | ~30min + human | 5 | 8 |
| 25 | 01 | ~25min + human canary | 5 | 7 modified, 3 deleted |
| Phase 25 P02 | ~30min | 4 tasks | 6 files |
| Phase 25 P03 | 9min | 1 tasks | 1 files |

## Accumulated Context

### Decisions

All v1.0-v1.4 decisions logged in PROJECT.md Key Decisions table.
Milestone-specific decisions archived in `.planning/milestones/`.

v1.5 decisions (to be logged as phase execution progresses):

- Phase 24 per-plugin rollouts MUST use `/improve [PluginName]` — enforces versioning, changelog, STATUS.md update, regression test (TRACK-01).
- Shared module candidate name `dsp/note-expression` — confirm against `/module-list` before creation (MOD-01).
- Local JUCE patch tracked as a named patch file in `scripts/` with re-apply procedure for JUCE upgrades (MOD-07).
- Phase 23 Plan 01: note-expression module shipped at modules/tuning/note-expression v1.0.0 — header-only under Ouaricon::NoteExpression, owns PendingTuningTable on VST3Extensions (D-09), no dependency on scala-tuning-engine (D-11)
- Phase 23 Plan 02: JUCE patch shipped as scripts/juce-patches/note-expression-juce-8.0.4.patch (1112 lines, generated via diff -u against pristine JUCE 8.0.4); idempotent apply-juce-patches.sh wrapper preflights JUCE_DIR and skips when JUCE-NE-PATCH marker already present (T-23-04); modules/tuning/note-expression/module.cmake gates only future consumers via opt-in module.cmake hook auto-included by OuariconModules.cmake (T-23-05, D-15)
- Phase 23 Plan 03: O-Lyrica refactored onto Ouaricon::NoteExpression — 4 source files edited, spike header VST3/NoteExpressionSupport.h deleted, OLyrica_VST3 + OLyrica_AU build clean (LYR-01/02). Build-gate discovery (D-23-03-A): Steinberg SDK symbols only link for VST3 client target — Controller class + queryIEditController body + nec member guarded behind #if JucePlugin_Build_VST3 in NoteExpression.h so AU/Standalone/VST2 link cleanly. Fix committed as f85ff38 against plan 23-01's deliverable. NOTE: Plan 23-04 later discovered Plan 23-03's "OLyrica_AU built clean" claim was incorrect — the on-disk AU artefact pre-dated the refactor commits and the AU re-link was never actually exercised. The f85ff38 guard does not solve the issue because guards evaluate at every TU compile site and SharedCode is compiled with VST3=1.
- Phase 23 Plan 04: O-Lyrica v2.3.0 shipped (CMakeLists VERSION + CHANGELOG [2.3.0] entry, LYR-04). Comprehensive note-expression module README published — 223 lines covering Quick Start / Features / Installation / Dorico Setup / Patch Management / Integration Approach (MOD-05). Dorico quarter-sharp smoke test 5/5 PASS via VST3 (LYR-03 gate cleared). Build-gate discovery (D-23-04-A): AU re-link failure exposed module-level architectural defect — JucePlugin_Build_VST3 guards in NoteExpression.h evaluate at TU-compile site, so SharedCode (compiled with VST3=1) carries Steinberg symbol references that AU's link line cannot resolve. Resolution deferred to Plan 23-05 (move Controller + Steinberg-touching code from header to VST3-only .cpp). Phase 24 BLOCKED until 23-05 lands.
- Phase 23 Plan 05: D-23-04-A AU-link Steinberg-symbol leak RESOLVED via two-TU split + custom function-pointer deleter pimpl + dual dispatch slots (g_neUpdate, g_neQuery). cpp/NoteExpression.cpp (SharedCode-bound, Steinberg-free) hosts ctor/dtor/drainAndUpdate/queryIEditController bodies; cpp/vst3/NoteExpression_VST3.cpp (VST3-only) hosts Controller body, vst3QueryIEditController free helper, updatePendingFromEvents, and the static-init DispatchRegistrar. modules/cmake/OuariconModules.cmake gained per-format source routing convention (cpp/<format>/ → ${TARGET}_<FORMAT>) used project-wide going forward. scripts/verify-au-link.sh provides reusable AU-link gate for Phase 24. Mid-flight Rule-1 q-slot fix (commit 0e00826) addressed plan-checker's prescient warning about VST3Extensions vtable references. LYR-03 Dorico smoke test re-passed 5/5 via VST3 (regression check). Public API + consumer call-sites preserved verbatim (D-23/D-32). Out-of-scope discovery: pre-existing APVTS Meta-Flag failure on parameter ID 1275870432 logged to deferred-items.md (was previously masked by AU re-link failure; not a Plan 23-05 regression). Phase 24 unblocked.
- Phase 24 Plan 01: O-Bells v4.1.0 propagation canary COMPLETE (atomic commit 8fee3a8). PROP-01 + TRACK-01..05 satisfied. 8-file atomic commit (CMakeLists.txt + PluginProcessor.{h,cpp} + BellVoice.{h,cpp} + CHANGELOG.md + STATUS.md + registry.yaml). Tri-format ninja exit 0 — no Steinberg-symbol leak (per-format module-source convention from Phase 23 D-22..D-29 holds). scripts/verify-au-link.sh O-Bells PASS — `AU VALIDATION SUCCEEDED. auval accepted O-Bells (aumu OBls OuDv)`. Dorico 3-point smoke gate (D-07) ALL PASS — gate 1 (~269.29 Hz at +50¢ above C4, Pattern 3 240-semitone full-scale validated); gate 2 (no attack zipper, Pattern 2 apply-before-DSP-trigger validated); gate 3 (polyphonic chord — only C4 detuned, E4 plays 12-TET 329.63 Hz, Pattern 1 noteId-correlation validated). System-environment notes: OUARICON_DEV_SUFFIX=-dev produces dev-suffixed bundles alongside prod-named bundles (intentional dev branding); auval -a system listing returns zero aumu entries on this machine (host-environment quirk affecting all plugins; verify-au-link.sh is canonical D-08 path and PASSES). Float→double cast at applyPendingTuning helper boundary works as designed (BellVoice uses float fundamental). Plan-checker phrasing: CHANGELOG body lowercase "adds" matches plan §verify grep; pattern propagates to plans 24-02..24-07. Feeds 24-08-final-sweep-SUMMARY.md row 1 of 8.
- Phase 24 Plans 02-07: 6 atomic propagation commits landed in single `/gsd-execute-phase 24` session 2026-04-26 (deferred-batch Dorico flow per user direction): O-Prism v1.17.0 (0393d0d), O-Wind v1.16.0 (4ae4600), O-IntonationPad v2.8.0 (a935830), O-Reed v1.1.0 (c829350), O-Bowed v1.3.0 (7b20d14), O-Formant v1.25.0 (d0e101a). Each: 8-file atomic commit, tri-format ninja exit 0, verify-au-link.sh PASS, fresh dual-bundle install. Three Rule-3 inline fixes uncovered/resolved during sweep: (a) O-IntonationPad CMakeLists missing `juce::juce_audio_utils` + `juce::juce_audio_devices` (blocked Standalone build); (b) O-Bowed missing `isBusesLayoutSupported` override (caused auval segfault, NOT NE regression); (c) O-Formant missing `OuariconModules.cmake` include (planned 2-step structural edit). Five propagation patterns catalogued for v1.5 retrospective: classic-Synthesiser-multi-osc (Prism), classic-Synthesiser-physical-period (Bells/Wind), classic-Synthesiser-multi-sub-voice (IntonationPad: applyPendingTuning at root with multiplicative ratio derivation), MPE-helper-based (Reed 3 sites / Bowed 2 sites), MPE-per-call-site (Formant). Note D pattern (explicit PLUGIN_VERSION arg in juce_add_plugin) applied to Wind/Reed/Formant/Bowed CMakeLists.
- Phase 24 Plan 08 final sweep COMPLETE (commit 0ec32e9). All 16 build targets `ninja: no work to do` (incremental no-op confirms cumulative tri-format link cleanliness across 8 plugins). All 8 plugins PASS substantive Dorico runtime + AU loading gates (O-Lyrica is the validated spike/reference plugin from Phase 23 Plans 01-05; the auval static-check finding DEF-24-01 was DOWNGRADED 2026-04-26 to a benign parameter-meta-flag annotation gap, not a defect). modules/registry.yaml note-expression.used_by has all 8 expected consumers; module v1.0.0 unchanged (D-33 honored). Dorico 3-point batch validation 2026-04-26: ALL 8 PLUGINS PASS (24/24 individual gate-points). VERIFICATION.md status=passed. Phase 24 closes v1.5 propagation cycle.
- Phase 25 Plan 01 v3 (Path B locked, completed 2026-04-27): single canonical Dorico-valid `.doricolib` (full 48-container kScoreLibrary skeleton + injected ExpressionMapDefinition; byte-identical to verified `/tmp/Ouaricon-VST3-NoteExpression-v2.doricolib` reference) authored at `modules/tuning/note-expression/resources/library/`. Path A artifacts surgically excised under D-10 amend-forward (3 deletion groups in commit db20a04: playback-template/ subtree, ouaricon_extract_vst3_cids helper, .dorico_pt packing in module.cmake). install-microtonal-suite.cmake.in collapsed from 87-line dual-write to ~40-line single-write to platform-specific Ouaricon shared path only (D-07; v1.6 revival reference preserved as `git show 819b2b4:...`). Module READMEs rewritten for Library Manager Import flow (Path B); technical-mechanics paragraph (kVST3NoteExpression invariant) preserved verbatim. O-Lyrica canary PASS end-to-end on macOS 26.3.1 / Dorico 6 / O-Lyrica-dev: ninja build exit 0, cmake --install lands assets at `~/Library/Application Support/Ouaricon/Microtonal Suite/`, Dorico Library Manager Import succeeds (no invalid-file-format error), quarter-sharp C4 plays at ~269 Hz target with no attack zipper, polyphonic isolation as expected. Wave 0 v3 auto-discovery probe FAIL (informational only; non-blocking; logged for v1.6 deferred-ideas per D-08 carry-forward). 6 atomic commits: ad9e5e4 (Task 1), db20a04 (Task 2), 98479ba (Task 3), 93d29d6 (Task 4), c45703b (Task 5 verification log), f15b7e8 (v3 SUMMARY). INST-01 + INST-02 satisfied. Plan 25-02 unblocked.
- Phase 25 Plan 02 v3 (installer-bundling sweep, completed 2026-04-27): Task 0 preflight audit GREEN (0 forks; 8/8 cohort plugins consume shared `pkg-creation.md` and `inno-template.iss` templates directly via `/package` and `build-installer` skills). Tasks 1-2 extended the shared PKG postinstall reference (Section 4a `${PROJECT_ROOT}` precondition + Microtonal Suite payload-copy sub-block; Section 4b `SUITE_DIR` postinstall block before `/tmp` cleanup with `chown -R "$ACTUAL_USER:staff"` + Library Manager Import activation hint echo) and the shared Inno Setup template (`[Files]` 2 new entries to `{userappdata}\Ouaricon\Microtonal Suite` with `ignoreversion`; `[Code]` `CurStepChanged(ssPostInstall)` 2 new `Log()` activation-hint calls) plus the `inno-setup-creation.md` reference (new Section 3.4 documenting `MICROTONAL_SUITE_DORICOLIB_PATH` + `MICROTONAL_SUITE_README_PATH` template variables with PowerShell substitution example). Task 3 D-08 cross-platform validation gate STRICT-PASS via `matrix-pass` resume signal: all 8 cohort plugins (O-Lyrica, O-Bells, O-IntonationPad, O-Prism, O-Wind, O-Reed, O-Bowed, O-Formant) on both macOS and Windows have PKG/EXE built bundling the canonical 6,431 B `.doricolib`; canary O-Lyrica on each platform clears the Phase 24 3-point Dorico gate (Library Manager Import success + quarter-sharp C4 ~269 Hz + no attack zipper + polyphonic isolation). Per-plugin sha256 captured locally during user-driven sweep; documented in `25-02-VALIDATION-MATRIX.md` with `Final Verdict: PASS`. No `DEFER-WINDOWS-TO-v1.6` or `DEFER-N-PLUGINS-TO-v1.6` escape valves invoked. 6 atomic commits: 9176903 (Task 0 preflight), b8c9b00 (Task 1 PKG), d1477e4 (Task 2 Inno Setup), 4258072 (Tasks 0-2 progress + Task 3 checkpoint blocker), de8e9df (Task 3 validation matrix), bc2aef1 (SUMMARY). INST-03 + INST-04 satisfied. Plan 25-03 unblocked.
- [Phase 25]: Plan 25-03 (internal docs) complete: research/microtonal-dorico-integration.md authored as single combined developer-facing technical reference (554 lines; audience: internal-dev-only per DOCS-05; 4 H2 sections DOCS-01..04). DOCS-01 grounded in NoteExpression.h + NoteExpression_VST3.cpp (two-TU split, dispatch slots g_neUpdate/g_neQuery, applyPendingTuning composition order). DOCS-02 reframed for Path B (Library Manager Import + per-channel assignment; ~269 Hz verification). DOCS-03 covers kScoreLibrary 48-container schema requirement (v2-defect lesson; cd2c2c6 fragment), explicit-import rationale (D-01), skipped <pluginNames> rationale (D-02), kVST3NoteExpression invariant. DOCS-04 9-row symptom-cause-fix table. One Rule-3 fix (literal-grep gate compliance — Path A residue strings rephrased; semantic content preserved). 1 atomic commit 8091d64. DOCS-01..05 satisfied; all 9 Phase 25 requirements complete; phase ready for verification.
- [quick-260904-q4j]: Z8 scoped to plain U+0020 only, so its zero stays checkable against an independent scan on the same predicate (D-02)
- [quick-260904-q4j]: maskLatin widened on two axes with the residual blindness (Han + ASCII period + Latin, unspaced) disclosed in the file rather than left silent (D-01)

### Pending Todos

1. CI/CD pipeline verification (test tag push)
2. ~~APVTS Meta-Flag audit across O-Lyrica~~ — **DOWNGRADED 2026-04-26**: O-Lyrica is the validated spike/reference plugin (Phase 23 Plans 01-05) and PASSES all substantive runtime + Dorico gates. The auval parameter-meta-flag finding (parameter ID 1275870432) is a benign tool-static-check artifact, not a defect. Optional cosmetic 1-line fix only if a release-pipeline code-signing audit surfaces it. See `.planning/phases/24-propagate/deferred-items.md` DEF-24-01 [DOWNGRADED].

### Blockers/Concerns

- **RESOLVED 2026-04-25 by Plan 23-05.** AU build was broken module-wide (D-23-04-A). Two-TU split with custom-deleter pimpl + dispatch slots restores clean OLyrica_VST3 + OLyrica_AU + OLyrica_Standalone link. Per-format module-source convention prevents Phase 24 plugins from regressing into the same defect class. Phase 24 unblocked.

### Quick Tasks Completed

| # | Description | Date | Commit | Status | Directory |
|---|-------------|------|--------|--------|-----------|
| 6 | add a new slash command to build a windows exe installer for a vst3 plugin | 2026-02-11 | bf03372 |  | [6-add-a-new-slash-command-to-build-a-windo](./quick/6-add-a-new-slash-command-to-build-a-windo/) |
| 7 | Move O-Bells to top of Ouaricon Plugins table in PLUGINS.md | 2026-02-26 | 2efe535 |  | [7-move-o-bells-up-to-the-top-of-the-produc](./quick/7-move-o-bells-up-to-the-top-of-the-produc/) |
| 8 | Fix free purchase flow: bundle display, cart clearing, email logging | 2026-02-26 | bfd4368 |  | [8-fix-free-purchase-flow-products-land-in-](./quick/8-fix-free-purchase-flow-products-land-in-/) |
| 9 | Fix email confirmation landing page to show thank-you message | 2026-02-26 | 0ebc554 |  | [9-fix-email-confirmation-landing-page-to-s](./quick/9-fix-email-confirmation-landing-page-to-s/) |
| 10 | Fix Google OAuth provider not enabled on oaudio.io | 2026-02-26 | -- |  | [10-fix-google-oauth-provider-not-enabled-on](./quick/10-fix-google-oauth-provider-not-enabled-on/) |
| 11 | Review manuscript for grammar and spelling | 2026-03-02 | 0df9e01 |  | [11-review-manuscript-for-grammar-and-spelli](./quick/11-review-manuscript-for-grammar-and-spelli/) |
| 12 | Reformat manuscript as Word document for Leonardo submission | 2026-03-02 | -- |  | [12-reformat-manuscript-md-as-word-document-](./quick/12-reformat-manuscript-md-as-word-document-/) |
| 13 | Codebase refactoring audit (13 opportunities, ~3,200+ lines eliminable) | 2026-03-03 | 386e107 |  | [13-look-through-this-project-for-opportunit](./quick/13-look-through-this-project-for-opportunit/) |
| 14 | Full system review of PFS (632K tokens, 15 recommendations, 9 quick wins) | 2026-03-05 | 564c8bd |  | [14-full-system-review-of-plugin-freedom-sys](./quick/14-full-system-review-of-plugin-freedom-sys/) |
| e4q | Sweep repo root: remove 24 debug artifacts, add gitignore rules | 2026-03-19 | 523c74e |  | [260319-e4q-sweep-repo-root-for-misplaced-screenshot](./quick/260319-e4q-sweep-repo-root-for-misplaced-screenshot/) |
| 260408-vzl | Fix tuning module integration - compare with O-Prism, add container queries | 2026-04-09 | b6df44a |  | [260408-vzl-fix-tuning-module-integration-compare-wi](./quick/260408-vzl-fix-tuning-module-integration-compare-wi/) |
| 260427-readme | README v1.5 milestone update — Microtonal Dorico Playback subsection + Milestone History table | 2026-04-27 | bd90b8a |  | [260427-readme-v15-microtonal](./quick/260427-readme-v15-microtonal/) |
| 260502-bb5 | Research: How VST3 NoteExpression pitch data embeds in MIDI files and DAW (Logic/Ableton) import compatibility | 2026-05-02 | fd7715f |  | [260502-bb5-research-how-vst3-noteexpression-pitch-d](./quick/260502-bb5-research-how-vst3-noteexpression-pitch-d/) |
| 260505-ayr | Create Dorico agent (subagent + /dorico slash command + seed memory) for VST instrument integration | 2026-05-05 | 3f744be |  | [260505-ayr-create-a-dorico-agent-that-helps-with-vs](./quick/260505-ayr-create-a-dorico-agent-that-helps-with-vs/) |
| 260505-mri | Wire 6 agents/skills/commands (troubleshoot-agent, plugin-improve, plugin-publishing, generalize-microtones, gui-agent, dsp-agent) to delegate to dorico-agent | 2026-05-05 | 0a216af |  | [260505-mri-wire-6-existing-agents-skills-to-delegat](./quick/260505-mri-wire-6-existing-agents-skills-to-delegat/) |
| 260622-pwy | Move to pay-what-you-want — Path A: flip CI licensing switch (remove licensing/Supabase flags from build-and-release.yml). Path B full removal deferred & scoped | 2026-06-22 | 155f34c |  | [260622-pwy-remove-licensing-pay-what-you-want](./quick/260622-pwy-remove-licensing-pay-what-you-want/) |
| 260623-bmr | PWYW Path B — physically remove the licensing system (delete module + add-licensing skill, strip guards from 24 plugins, clean root CMake + registry). 28 atomic commits; 4 sample plugins build clean | 2026-06-23 | 77b6b5a | Verified | [260623-bmr-path-b-physically-remove-licensing-syste](./quick/260623-bmr-path-b-physically-remove-licensing-syste/) |
| 260701-in8 | Read-only PFS system review — 6 bugs, 5 improvements, 6 update recs (REVIEW.md; dead hooks, CI target resolver, registry drift) | 2026-07-01 | -- |  | [260701-in8-complete-a-review-of-this-system-for-cre](./quick/260701-in8-complete-a-review-of-this-system-for-cre/) |
| 260701-j3b | Hook wiring + settings hygiene: BUG-02 verified valid (review premise stale — SubagentStart/TaskCompleted real in CC ≥2.1.197, instrumented not rewired), BUG-04 matcher 1:1, BUG-03 rm -rf auto-approve removed, IMP-01 2 orphans deleted / 2 CLI utils retained | 2026-07-01 | 71e8fbd, cde44f5 | Verified | [260701-j3b-fix-dead-claude-hook-wiring-and-settings](./quick/260701-j3b-fix-dead-claude-hook-wiring-and-settings/) |
| 260701-k0a | Build-script + CI defects: BUG-01/IMP-03 shared resolve-target.sh (5 CI call sites, validated vs O-Texture/O-Chorus/O-Gain), UPD-01 patch renamed 8.0.4→8.0.9 (6 refs), BUG-05 --forward --dry-run preflight, BUG-06 set -euo pipefail + eval\|tee rc capture | 2026-07-01 | cf5696b, 1166b2c, 8dd245a | Verified | [260701-k0a-fix-build-script-and-ci-defects-from-the](./quick/260701-k0a-fix-build-script-and-ci-defects-from-the/) |
| 260702-evn | Registry used_by regen (UPD-02/IMP-02/UPD-03): scripts/regen-registry-used-by.sh derives used_by from disk truth; scala-tuning-engine 0→12, note-expression 8→11, preset-manager 3→21, retired names purged; header 1.0.0→1.0.1 + bump-reminder comment; idempotent | 2026-07-02 | 27e890c, 6d046c8 |  | [260702-evn-regenerate-modules-registry-yaml-used-by](./quick/260702-evn-regenerate-modules-registry-yaml-used-by/) |
| 260702-ffl | Housekeeping sweep (UPD-04/UPD-05/IMP-04): compaction-snapshot.md deleted, resource-index.json regenerated (frontmatter-issues.txt refreshed by generator — 10 docs still lack frontmatter); 2 stale licens* tokens stripped, installer LicenseFile refs kept; plugins/tache_plugins → archive/tache_plugins via git mv (348 files + 43 gitignored mockup leftovers), regen-script old-path refs cleaned | 2026-07-02 | b7930c2, 7a2f037, ff2e7ea |  | [260702-ffl-housekeeping-sweep-260701-in8-delete-sta](./quick/260702-ffl-housekeeping-sweep-260701-in8-delete-sta/) |
| 260719-hn4 | Framework update audit — JUCE 8.0.9→8.0.14, ANIRA, nanoflann, umappp reviewed; integration plan written to research/framework-updates-2026-07.md (no upgrades performed) | 2026-07-19 | 1d562b6 |  | [260719-hn4-check-juce-and-framework-updates-review-](./quick/260719-hn4-check-juce-and-framework-updates-review-/) |
| 260719-iun | Triage dirty tree — ~70 entries committed as 9 atomic commits (O-Bells v4.1.1, O-Contrabass v1.0.0, O-GrainScatter v2.4.2, O-Tremolo v1.6.0, O-Bowed v1.4.1, O-Detune module sync, commands, workflow state, CI); main pushed, no discards | 2026-07-19 | 60d1b24 |  | [260719-iun-triage-dirty-working-tree-commit-uncommi](./quick/260719-iun-triage-dirty-working-tree-commit-uncommi/) |
| 260719-k5o | Re-base JUCE-NE-PATCH onto 8.0.14 — vendored header relocated to headless module, .cpp re-stitched at toMidiBuffer anchor, CI grep gates repointed, module.cmake both-paths guard, patch renamed 8.0.9 + regenerated 8.0.14; O-Lyrica compile-check vs scratch 8.0.14 green. Branch quick/260719-k5o-juce-ne-rebase-8014 (unmergeable until JUCE_VERSION bump) | 2026-07-19 | bbd24c5 | Verified | [260719-k5o-re-base-the-note-expression-vendored-pat](./quick/260719-k5o-re-base-the-note-expression-vendored-pat/) |
| 260719-l26 | Bump JUCE 8.0.9 → 8.0.14 — local /Users/taylorbrook/JUCE swapped to pristine 8.0.14 + NE patch re-applied (8.0.9 backup preserved), apply-juce-patches.sh repointed, CI JUCE_VERSION → 8.0.14; full-suite fresh build 36/37 plugins clean VST3+AU, zero JUCE fallout. Sole failure O-TextureForge (umappp↔irlba transitive drift, JUCE-independent, deferred DEF-L26-01) | 2026-07-19 | 4c45eba | Verified | [260719-l26-bump-juce-8-0-9-to-8-0-14-local-install-](./quick/260719-l26-bump-juce-8-0-9-to-8-0-14-local-install-/) |
| 260719-m5p | Post-JUCE-8.0.14 verification battery — auval + verify-au-link.sh + pluginval (strictness 8) across 38 plugins, 8 render harnesses rebuilt+run, 8.0.11 var deep-equality watch CLEAN, no JUCE regressions; O-simpleSubtractive harness WebView pitfall fixed; matrix + Dorico/Logic/Ableton manual checklists in 260719-m5p-RESULTS.md | 2026-07-19 | 56d1f63 | Verified | [260719-m5p-post-juce-8-0-14-verification-battery](./quick/260719-m5p-post-juce-8-0-14-verification-battery/) |
| 260819-mrg | Merge-all sweep — committed O-SpectralShaper v1.6.1, merged 5 plugin branches into main (GrainScatter UI fix renumbered 2.4.4 over released licensing 2.4.3; MBC 1.6.1+1.7.0 disjoint-lineage combine, build-verified), 2 PLUGINS.md union dups fixed, 6 branches + 2 worktrees removed; main ahead 14, unpushed | 2026-08-19 | f2bf5c8 | Verified | [20260819-merge-all-branches-worktrees](./quick/20260819-merge-all-branches-worktrees/) |
| 260719-q0o | Bump ANIRA v2.0.3 → v2.1.0 in O-Texture (ORT stays 1.19.2; v2.2.x deliberately skipped) — rebuild + install clean, embedded libonnxruntime.1.19.2.dylib + @loader_path/../Frameworks rpath verified via otool, auval + pluginval strictness 8 PASS | 2026-07-19 | bfdd6e0 |  | [260719-q0o-bump-anira-git-tag-v2-0-3-to-v2-1-0-in-o](./quick/260719-q0o-bump-anira-git-tag-v2-0-3-to-v2-1-0-in-o/) |
| 260719-qxy | Bump O-TextureForge deps — nanoflann v1.6.2→1.10.1 (changelog scan clean; unprefixed tag) + umappp v3.2.0→v3.3.2 (required `parallel_optimization`→`num_threads_optimize` migration in UMAPProjection.cpp — NOT a pure drop-in); force-clean re-fetch build green → DEF-L26-01 cleared, KNOWN-FAIL guard removed from verify-suite-battery.sh; auval + pluginval strictness 8 PASS; DAW UMAP-scatter spot-check pending | 2026-07-19 | c54ca09 |  | [260719-qxy-bump-o-textureforge-header-only-deps-uma](./quick/260719-qxy-bump-o-textureforge-header-only-deps-uma/) |
| 260720-akp | README review/update — JUCE 8.0.0+→8.0.14, cross-platform VST3 framing (macOS+Windows CI, AU macOS-only), full scripts/ listing; "17 templates" verified accurate, no rewrite. Follow-up: merged orphaned improve/o-formant-v1.25.1-rt-safety (34 commits — O-Formant 1.25.4, O-MicrotonalSampler 1.23.6, O-Prism 1.19.1, o-simpleSampler 1.0.0, PFS review fixes, tache_plugins archived; newest-wins resolution, 4 plugins build-verified vs 8.0.14), reconciled PLUGINS.md registry rows, linked registry from README | 2026-07-20 | 45d7178, cb6ceb0 |  | [260720-akp-review-the-readme-and-update-as-needed](./quick/260720-akp-review-the-readme-and-update-as-needed/) |
| 260730-9mh | Add gsd-core repo link to README GSD Integration section | 2026-07-30 | 64425f3 |  | [260730-9mh-add-a-link-to-the-gsd-core-repo-in-the-g](./quick/260730-9mh-add-a-link-to-the-gsd-core-repo-in-the-g/) |
| 260730-vwx | Public-release readiness audit — PUBLIC-RELEASE-READINESS.md (344 lines). Full-history secret scan CLEAN (no rewrite needed for security). **L1/L2 RESOLVED 2026-08-01: repo licensed AGPL-3.0** — JUCE taken under the AGPLv3 horn, not the free Starter tier, because (a) the repo redistributes 80 JUCE-owned files (vendored/JUCE-overrides 4,451 lines + 78 vendored js/juce) which JUCE 8 EULA §1.17/§2.3 conflict with, and (b) Starter's $20k cap counts PWYW + donations. Deps verified AGPL-compatible. **L4 RESOLVED 2026-08-01 via 260801-u3o** — and the audit overstated it: 9 of the 12 files were provably self-authored, and the 4 O-MicrotonalSampler fixtures were never in any binary at all, contrary to this row's original "compiled into shipped binaries" claim. Git-history exposure remains open (see that row). Also open: `.claude/system-config.json` + `build-release/` (incl. compiled `O-Bowed_vst3_helper`) tracked despite gitignore; CI already fork-safe (push/workflow_dispatch only) so S4 is harden-not-fix; 912 MB `.git` / 3308 files, ~250 MB audio goldens. 16-item ordered checklist, rewrite + visibility flip gated last | 2026-07-30 | 42e4348, 219a93d, 81e9ca0 | LICENSE + L4 landed | [260730-vwx-audit-repo-for-security-and-efficiency-i](./quick/260730-vwx-audit-repo-for-security-and-efficiency-i/) |
| 260720-rtc | Remove TÂCHES attribution + all taches plugins — deleted archive/tache_plugins/ (348 tracked + 43 ignored files, 17 plugins, ~6.9 MB; archive/ gone); stripped TÂCHES/TACHES credit from README.md, PLUGINS.md (heading + 18-row table), .planning/research/STACK.md, O-GrainScatter NOTES.md + BRIEF.md (×2). grep clean (only "detaches" false positive). Deferred: ~60+ example/historical name mentions in skills/troubleshooting/aesthetics left pending user decision | 2026-07-20 | ecf3dfe, 1dcfd27 | Complete | [260720-rtc-remove-taches-attribution-and-plugins](./quick/260720-rtc-remove-taches-attribution-and-plugins/) |
| 260801-u3o | Resolve the undocumented audio samples (closes readiness L4 §2.2) — **the blocker was 3 files, not 12**. Provenance PROVEN for 9 of 12: O-simpleGrain `fire/piano/voice/water.wav` regenerate **bit-identically** from `tools/generate_samples.py` (SEED 20260624, MD5-verified this session), O-simpleSampler `piano.wav` is byte-identical to it, and the O-MicrotonalSampler 4-layer `C4_v1..v4.wav` are `generate.py` sines that are **test fixtures in no `juce_add_binary_data` target** — the audit's "compiled into distributed binaries" claim was false for them. Real blocker = O-simpleSampler `cello.aif`/`hit.wav`/`pizz.aif`, commercial-library origin → **removed, no replacement** (user decision). That forced a parameter deletion: JUCE asserts `choices.size() > 1`, so a 1-choice `sourceSample` builds a degenerate `{0,0}` NormalisableRange whose `convertTo0to1` is `0/0`→NaN which `jlimit` will not clamp — param dropped, **21→20 params**, Source combo removed from the WebView. O-simpleSampler v1.1.0 verified: auval SUCCEEDED / 20 Global Scope Parameters / Component Version 1.1.0 (0x10100); pluginval strictness-10 ×6 (3 VST3 + 3 AU) all exit 0, zero nan/inf/FAILED; render harness 9/9; 0 withdrawn strings in either installed bundle. 3 provenance `LICENSE.md` written with per-file MD5s gated against disk. Neither plugin was ever released (38 releases, 0 for either) so no shipped/notarised binary was ever affected. **RESOLVED 2026-08-02: history rewritten with git filter-repo — the 3 sample paths expunged from all refs/tags/stashes (formerly-`4ca27977` → `6734552a`, HEAD tree byte-identical, SAF gitlink intact, backup bundle at `~/VST-development-pre-rewrite-20260802.bundle`). Residual for step 15: GitHub keeps orphaned pre-rewrite commits fetchable by SHA until server-side GC — purge via Support or recreate the repo before the visibility flip.** | 2026-08-01 | 22796a4a, 5b4c4fc3, 54b9ec80, ee9a7f81, ddd6f6c5 | Complete — DAW check pending | [260801-u3o-remove-commercial-library-samples-from-o](./quick/260801-u3o-remove-commercial-library-samples-from-o/) |
| 260802-ujb | Update README for public release: lead with 0-Audio publisher intro + oaudio.io link + 39-plugin catalog, add AGPL-3.0 License section, bring command reference to set-parity with .claude/commands/ (12 added, 1 stale removed), fix stale facts (38→39 plugins, note-expression v1.1.0→v1.1.1, 3 missing scripts, invented example plugin names replaced) | 2026-08-02 | fb43e674, a84b960a, b778022a |  | [260802-ujb-update-readme-for-public-release-intro-t](./quick/260802-ujb-update-readme-for-public-release-intro-t/) |
| 260803-9wh | Harden release CI workflow (readiness steps 8–10 ✅) — top-level `permissions: contents: read` added between `on:` and `env:` (`create-release` keeps its `contents: write` override); all **8** action refs SHA-pinned with `# vN (vN.N.N)` comments (checkout→`11d5960a` v4.4.0 ×3, upload-artifact→`ea165f8d` v4.6.2 ×3, download-artifact→`d3f86a10` v4.3.0 ×1, action-gh-release→`3bb12739` v2.6.2 ×1 — SHAs resolved via `gh api .../git/ref/tags/`, all `type: commit`); standing rule recorded in the workflow header naming `pull_request_target` + secrets-bearing `pull_request` as forbidden, listing the 8 Apple signing secrets and the signed-notarised-malware stake. Readiness §3.1's own table was **wrong** (checkout recorded ×2, two cells blank) — corrected to 3/3/1/1. Verified: zero bare-tag `uses:` refs, 8 SHA pins, PyYAML parse confirms triggers/`env:`/job perms unchanged. **Residual: the trigger rule is documentary only** — a branch ruleset on `.github/workflows/**` would enforce it and is not implemented. Steps 5–7 remain unticked in the checklist despite landing in `ecf3fa39` *(closed by 260803-am7)* | 2026-08-03 | 72d656cd, 43cff844, 8e0e69a9 |  | [260803-9wh-harden-release-ci-workflow-top-level-per](./quick/260803-9wh-harden-release-ci-workflow-top-level-per/) |
| 260803-am7 | Tick readiness steps 5–7 ✅ — the untracking landed in `ecf3fa39` (42 files, 5897 deletions, 0 insertions: 1 `.claude/system-config.json` + 10 `build-release/**` incl. the compiled `O-Bowed_vst3_helper` + 31 `logs/**/build_*.log`; 1+10+31=42 ✅) but the boxes were never checked. Checklist now **12 ticked / 5 open** (4, 11, 12, 13, 14). Sections 2.3/2.4/3.4 headings marked `— ✅ RESOLVED 2026-08-03` and their bodies re-tensed so no ✅ heading sits over present-tense "is tracked" prose. The doc's own counts ("Ten tracked files", "seven files matching `JUCE/*.cmake`", "31 files") were **verified correct against the diff** and left verbatim; §2.3's "`.gitignore` line 1" corrected to **line 9**. Unplanned catch: §3.2's "**354 tracked files**" went stale the moment step 5 ticked — annotated to 353 by pure insertion so the dated [S2] measurement survives. **Step 6's residual found and then closed on user instruction: `build-release/` was untracked but had never been gitignored** — `git check-ignore` returned nothing and `?? build-release/` sat on disk, so one `git add -A` would have re-committed the compiled helper into the *public* repo. `build-release/` added at `.gitignore:67` beside `build/`; verified `check-ignore` resolves the tree + the helper binary to that rule, `git status` no longer lists it, and `git add -An` stages **zero** paths under it. §2.4 re-tensed throughout (heading note, "What was needed", remedy block now includes the ignore step) so no stale "still open" claim survives. **Readiness step 6 is now genuinely complete — untrack + ignore, not just untrack** | 2026-08-03 | 73839504, 3e9d4a01, d21524e1 |  | [260803-am7-tick-readiness-checklist-items-5-6-7-the](./quick/260803-am7-tick-readiness-checklist-items-5-6-7-the/) |
| 260803-bhf | Move the committed installers off git (readiness step 12 ✅, §4.3) — **checkout-only, history intentionally intact**. Two tracked O-Polystutter **v1.8.0-era** blobs untracked via `git rm --cached` and left on disk: `dist/O-Polystutter-OuariconAudio.pkg` (4,756,409 B) + `dist/PolyStutter.zip` (4,742,481 B). **The preservation gate as briefed FAILED and the backup is what satisfied it, not history** — neither blob matches any published Release asset (the only published release is `O-Polystutter-v1.12.4`, a *different version at different sizes*: 10,151,465 B pkg + 4,822,035 B exe), and the backup held only v1.12.2/v1.12.4. Both were therefore copied to `~/VST-development-releases-backup-20260803/assets/O-Polystutter-v1.8.0-repo-dist/` and MD5-matched (`c778e3de…`, `705eae9b…`) **before** anything left the index. **Step 6's residual was designed out, not re-fixed:** ignore rules landed in commit 1 and untracking in commit 2, so the untracked-but-unignored window never opened. `*.pkg`/`*.dmg`/`*.msi` appended at EOF (`*.exe`/`*.zip` already existed); **a `plugins/*/dist/` wildcard was considered and DELIBERATELY REJECTED** — those folders also hold hand-authored source (`O-Tremolo/dist/installer.iss`, both `install-readme` files), all 3 still tracked; the rejection is written into the `.gitignore` comment. Verified: tracked installer-binary count **0**, tracked-but-ignored census **10→9** (all vendored `preset-manager.js`), `git add -An` on both dist dirs stages zero, SAF gitlink unchanged, reflog shows zero filter-repo/filter-branch/push. §4.3's own headline was **wrong** and is corrected in place: **two** blobs tracked not three (`-by-TACHES` was already history-only), **9,498,890 B not 13.5 MB**, and the two *overlap* — the zip contains the pkg verbatim, so it is one package stored twice. Also repaired pre-existing `.gitignore:196`→**197** citation drift in §3.4 + step 7 (caused by step 6's line-67 insertion). **Residual: step 11 still reads "Must resolve before step 12" while 12 is ticked and 11 is open** — the dependency was about rewrite scope and no rewrite happened, so nothing bound; left verbatim per scope boundary, one line to fix when 11 is next opened | 2026-08-03 | 2cefd026, ef817350, d8f4f19b |  | [260803-bhf-move-committed-installer-artifacts-out-o](./quick/260803-bhf-move-committed-installer-artifacts-out-o/) |
| 260803-c4n | Tidy the repo root (readiness step 13 ✅, §4.4) — **the whole scope was already untracked *and* ignored**, so unlike steps 5/6/7/12 there was no `git rm --cached`, no index change and no history implication: `git add -An .` staged **zero** paths both before and after. 13 root items cleared — the `e1-max-sustain` wav+json pair (17 MB), 6 PNGs, `scratch-pv/` (6 pluginval logs), the root o-bowed harness JSON, `.playwright-mcp/` (63 files) and `.DS_Store`. **Preservation gate ran first: 77/77 files copied to `~/VST-development-root-scratch-20260803/` and MD5-matched against their originals *before* a single original was removed**, with a `README.txt` recording each group's provenance; `.DS_Store` (6,148 B) was the sole outright deletion, the only file with no preservation copy. **One file was load-bearing and was moved, not archived:** `o-bowed-pre-extraction-canonical.wav` was the **only byte-exact copy on disk** of O-Bowed's golden render — sha256 `93124fb8…c8891`, matching the tracked `canonical-preset.wav.sha256` sibling that attests it (the golden dir tracks the checksum but not the wav) — so it was relocated to `plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav` and re-verified byte-exact after the move. **Step 6's window never opened:** the ignore rule was committed *before* the move, which mattered more than usual because the destination is a directory where renders *are* legitimately tracked (O-Contrabass keeps 19) — a stray wav there looks native. `.gitignore:68` names that **one exact path**; a `golden/*.wav` wildcard was **deliberately rejected** as it would silently stop future intentional goldens from being tracked. **A doc claim was overturned by measurement:** §4.4 listed the root `e1-max-sustain` pair as plain scratch, but `RESEARCH.md:1390` cites "`e1-max-sustain.json` (project root)" as numerical evidence — measured, the root pair does **not** carry the cited figures (root rmsMid 0.0277 / peak 0.0993 vs cited 0.0353 / 0.0683), the **tracked plugin-local** pair does; the citation's "(project root)" is stale, and the root pair was archived rather than deleted precisely because a tracked doc names it. Verified: census **9**, tracked `.wav` **31**, O-Contrabass golden 70/19, SAF gitlink unchanged, reflog shows no push or rewrite. Ruled out of scope explicitly (considered, not missed): `backups/` 1.3 GB, `logs/` 9.8 MB, `build/`, `build-release/` — working directories, not scratch renders. **Residuals for step 14:** §4.2 says "32 tracked `.wav`" but the measured count is **31**; and §4.2 dismisses `plugins/O-Contrabass/e1-max-sustain.wav` as "a stray scratch render" when it is in fact the real cited evidence — yet it sits in §4.6's `filter-repo` path list. Read those two together before any rewrite | 2026-08-03 | 54bc7a35, ad4321ef |  | [260803-c4n-tidy-the-repo-root-per-public-release-re](./quick/260803-c4n-tidy-the-repo-root-per-public-release-re/) |
| 260805-ozs | README: promote Dorico microtonal integration to standalone section (8→11 cohort plugins, playback-template bundles documented) + link 15 plugin rows to verified oaudio.io product pages | 2026-08-05 | 6b849d01, 8fce98b7 |  | [260805-ozs-to-the-readme-add-info-about-the-dorico-](./quick/260805-ozs-to-the-readme-add-info-about-the-dorico-/) |
| 260808-uiq | README: add `## Support` section (3 lines, purely additive — 4 insertions / **0 deletions**) linking the site's donate page **https://oaudio.io/support**, placed between the plugin catalog and `## Microtonal Dorico Integration` so the ask sits *below* the catalog rather than above it. **The link the user asked for was not the one their prompt named:** the request said "the same donation link" alongside `oaudio.io/products/`, but the products page is the *distribution* surface — the site's actual donate target is `/support` (labeled "Donate" in site nav; suggested $5/$10/$15/$30, "every contribution supports continued development"), which is what shipped. Line 7's existing "free and pay-what-you-want" sentence was **deliberately left byte-identical** — a second live donate link 70 lines above the section is redundancy, so there is exactly one canonical donate link in the file. No `.github/FUNDING.yml` added (confirmed still absent), no badges, no TOC entry (README has neither). Executed sequentially: the pre-dispatch plan commit advanced HEAD past `origin/HEAD`, so the #1941 base-check auto-degraded off worktree isolation. An unrelated `178dc296 release(O-simpleGrain): v1.1.3` landed on `main` mid-task from another process; `git show --stat` confirms the README commit carries README.md only — nothing swept in | 2026-08-08 | d56435b2 |  | [260808-uiq-add-oaudio-io-donation-support-link-to-r](./quick/260808-uiq-add-oaudio-io-donation-support-link-to-r/) |
| 260810-j78 | README: link the 7 newly published plugins to their oaudio.io product pages — catalog goes **15 → 22** linked rows (7 insertions / 7 deletions, Plugin column only). Added: `O-MicrotonalSampler`, `O-ReverseDelay`, and 5 of the `simple` series (`Additive`, `FM`, `Grain`, `PhysicalModelSynth`, `Beatmaker`). **Published set was derived from the site's own `sitemap.xml`, not from PLUGINS.md status** — the registry's 📦 Installed flag means *installed locally*, not *published*, and 17 📦-Installed plugins have no product page (O-Marimba, O-Wind, O-Bowed, O-Formant, O-Texture, O-Gain, …); those stay unlinked. Each of the 7 URLs was fetched and confirmed **HTTP 200 with a `<title>` naming the matching plugin** before the plan was written. **One slug is not derivable from the plugin name and would have 404'd if guessed:** `O-simplePhysicalModelSynth` publishes at `ouaricon-simple-modeling-synth` (site title "Simple Modeling Synth"), so the verified slug table was passed to the planner and executor as ground truth with re-fetching and name-derivation explicitly forbidden. The `simple` series is now deliberately **mixed** — `O-simpleSubtractive` and `O-simpleSampler` have no product page yet and stay plain, so the table could not be link-swept wholesale. `https://oaudio.io/products/ohands` appears in the sitemap but is a **separate product, not a plugin in this repo's tables** — ruled out of scope, not missed. Line 13's "Linked plugin names go to that plugin's page" sentence left byte-identical (it gets *more* accurate, not less). A description-integrity gate diffed the 3rd pipe-field of **all 147 table rows in the file** against pre-edit `da661c95` — zero drift, guarding the silent-re-alignment failure mode; line count unchanged. Ran **sequentially on `main`, worktrees off** — a single-file doc edit has no parallelism to buy, and worktree cleanup blocks on dirty planning artifacts | 2026-08-10 | 0a50ef53 |  | [260810-j78-update-the-readme-with-links-to-the-oaud](./quick/260810-j78-update-the-readme-with-links-to-the-oaud/) |
| 260826-ieq | Multi-language (EN/FR) tooltips across all VST plugin UIs — **SCOPE EXPANDED mid-task from tooltips to ALL WebView UI text. ALL STAGES A–M COMPLETE 2026-08-31 — 43/43 localized, items 17/18/26 closed, Checkpoint 5 (French review) open**. Census found 43 plugins on 2 UI roots and **two** tooltip renderers, not three: O-MultiBandCompressor's `GLOBAL_TOOLTIPS` array is not a third convention — `applyTooltip()` *writes* `data-tip`/`data-tip-title` onto selector-resolved elements and the same measure-then-pin renderer reads them back, so MBC was **already** the key-indirection design and no `data-tip-key` attribute was needed. **Stage A** built the two repo tools: `scripts/param-dump/` (runtime `getParameters()` walk — static parsing of `createParameterLayout()` provably fails here: a regex finds 2 params in O-Emulator where the runtime finds 5, and **173 in O-Prism where the estimate said 81**, because its IDs are `prefix + "Table"` concatenations that never appear as literals) and `scripts/check-i18n.js` (9 assertions/plugin: en/fr key-set equality, TIP_BINDINGS resolution, zero tooltip literals left in HTML, no silent `fr===en` passthrough, explicit `reviewed` flag, byte-compare against `scripts/i18n-canon.js`, export-only module top level, embedded-AND-served closure, no markup path). **Stages B–D** localized 5 plugins: O-MultiBandCompressor v1.10.0 (pattern-bearer — chosen over O-Octagon because its `applyTooltip()` was already the target shape, it owns the only *composed* tips `${band} — ${title}`, and it has `tests/` but no tooltip gate, so the pattern proved without simultaneously proving a gate rewrite), O-Octagon v1.6.0 (gate-hardening — 43 sections/377 assertions + both CI-run C++ probe targets), O-ReverseDelay v1.9.0, O-Bitrot v1.14.0, O-Tapestop v1.5.0. **The French geometry claim is now MEASURED, not argued**, on all three clamp-gated frames: 940×768 / 900×740 / 860×580, both languages, every anchor inside its viewport with an 8 px margin, no assertion failing in French anywhere. French is 14.8 px taller at its tallest on all three (one extra wrapped line — consistent because the cap is 230 on all three). Clamp counts moved on **none** (assertion 2 is capped by `max-width`, so longer copy wraps rather than widening); **O-Bitrot cost 3 extra vertical flips (10→13)** — the only behavioural change French produced, caught by the existing flip logic, which is exactly what assertion 3 exists to prove. `.tooltip { max-width }` untouched throughout. **Bugs found by rendering that reading could not have caught:** MBC's gear popover was correct in markup and CSS and completely pointer-dead behind a `z-index: 1` tie the spectrum won on DOM order (fixed with the plugin's own documented `:has()` header-lift idiom; cascaded into `.tooltip` 1000→10001). **Three latent gate defects fixed, all the same class — a scanner that assumed one spelling:** O-Octagon §9's module-reference scan was `/from "\.\/([^"]+)"/g`, **double quotes only**, while the canonical import is single-quoted, so it reported `js/i18n.js` as embedded-but-never-referenced while the page imported it fine; `check-i18n` assertion 7 segmented on `}` at depth zero and split the `;` of `export const X = {...};` into an empty statement it then rejected; and `check-i18n` assumed a `js/app.js` controller, which O-Bitrot does not have (inline `<script type="module">`). **D13 upheld and its gate made sharper:** O-ReverseDelay keeps display-only hover help — language selector alone, no toggle, no `setTooltipsEnabled`. The old assertion was a bare substring search that could not distinguish *registering* the function from *a comment saying it is deliberately absent*, so it failed on source obeying D13 exactly; tightened to match real `withNativeFunction`/`getNativeFunction` registration plus a new check that the processor holds no persisted enabled flag. **The both-language sweep is now a committed gate** in all 3 `tests/` dirs — parameterised not duplicated, `max-width` parsed from each plugin's own CSS, anchor count derived from `TIP_BINDINGS` (the old `data-tip=` count is 0 now and would have passed vacuously). All gates re-run independently: exit 0, **none exited 77**. **NOT VERIFIED: the C++ persistence round-trip was never executed on any of the 5** — 'close the DAW, reopen, still French' is reasoned from `OuariconPresetManager.h:579-613`, not measured. Nothing checked in a real DAW; bridge exercised only against ui-stubs; no Windows/WebView2; all 200 French entries are unreviewed machine drafts **STAGE O COMPLETE 2026-08-31 — 21 of 21 per-plugin English/DSP/CSS defects fixed (items 22, 30, 34-58, 61-66 + 69-82 opened).** | 2026-08-31 | 5ee01ed4, b98aa9ec, cceebfa4, 8dcb1317, 951dd584, 1f3a9faa, 547f9738, ae419999, 4291236c, c9a5ef83, 2e03020e, 8011f09a, 92e5fdc9, 8b146dd3, a1d80957, cc2ea600, e56abded, ff162184, 55228ffb, 3f6b201d, 99e7d206, 7035029a, f00e5d45, 9d8e50d0, b1082fc0, 2a2f68c4, 5fc1ceb0, 857dfa85, 2a5efef0, 08e7649b, 051e1a72, 816f2767, 665150c0, 37602894, 66456f1e, b0ce697e, 09bdbb61, 5b509e8c, 1151de07, 435957a7, bb275ae1, 91d81cf7, 5406f419, f571a78e, 56cdbb37, cec3f857, 242983a4, aaab5318 | Stages A–N complete; Checkpoint 5 second reading done, native review open | [260826-ieq-multi-language-tooltips-across-all-vst-p](./quick/260826-ieq-multi-language-tooltips-across-all-vst-p/)  **EXPANSION (2026-08-27):** scope grew to all user-visible WebView text — 3,593 rendered text-bearing elements, 354 aria-label, 575 title across 43 plugins. Locked: WebView only (C++ param display names stay English — host automation-lane contract); factory preset names stay English (**the name IS the JSON filename**, `OuariconPresetManager.h:283-285`, so translating breaks recall); English number formatting retained (169 `toFixed` sites and every editable-readout parser untouched); overflow **gated and fixed per plugin**, no auto-shrink and no short-variant fallback. **Stage E retired the blocker:** only 5 of 43 plugins could be rendered headless, and a gate that cannot load the page is not a gate — built `serve-ui.js` + a generic bridge stub, `boot-all-uis.js`, `i18n-extract.js` (three-source inventory) and `check-ui-labels.js`; **41/43 now boot clean**. The 2 failures are a real shipped defect: O-Bowed (`index.html:800`) and O-Reed (`:874`) load `/js/juce/index.js` as a CLASSIC script (throws `Unexpected token 'export'`) then correctly re-import it as a module — non-fatal, one-line fix, blocks Stage K not F–H. **Stages F/H localized 4 plugins' pages:** O-Tapestop v1.6.0, O-MultiBandCompressor v1.11.0, O-Bitrot v1.15.0, O-ReverseDelay v1.10.0. Geometry measured en-vs-fr at fixed frames: **222 / 78 / 7 elements moved → 0**, all fixed by spending existing slack; 222 was five recurring causes, not 222. The env-row fix corrected an **English** bug that had rendered 79.2px against its designed 88 since the row existed. **TDZ, again:** canon v2 is reached at BINDING time where v1 was not, so MBC and O-Bitrot needed their i18n block moved above the eager binders — `uiLanguage` sat in its TDZ and the throws were **swallowed by the binders' own try/catch**, leaving MBC with three global knobs and a toggle silently unbound on a page that looked correct. `boot-all-uis` caught it; `check-ui-labels` did not. **Six wrong-shaped gate assumptions found by running the tooling for real**, three of them reporting a violation of a rule the code was obeying: assertion 4 blind to `overflow:visible` spill (`scrollWidth === clientWidth` however far text spills — `PLEURAGE` 60.2px sat outside a 58px segment with every assertion green); assertions 5/6 failing in ENGLISH on authored layout; `check-i18n` never checking LABELS at all; `[15]` blind to `dataset.i18nAria`; **en and fr measured AT DIFFERENT PARAMETER VALUES** (53 phantom rows on MBC — invisible on O-Tapestop's events-only stub, would have hit 38 of 43); and the spill check failing on correct English inline labels. **One negative control deliberately left un-firing as a recorded finding:** a single over-long caption in a shrink-to-fit cell is invisible to `check-ui-labels` — the box is always exactly its text, its `offsetParent` is the frame, and it is excluded as a label — so it is caught only on collision. The fix needs each label's intended cell, which only the plugin knows. **Method note worth keeping:** a control that appeared not to fire turned out to be a Python `str.replace` that silently did not match — a no-op mutation is indistinguishable from a working gate, so every later control prints and asserts its substitution count. 37 negative controls, 36 fired. No gate exited 77. **Stage G (O-Octagon) DEFERRED not skipped** — a concurrent session held 27 modified + 4 new files there and landed v1.8.0 (`2ba236a1`) mid-run; O-Octagon is the last half-localized plugin and G must be re-planned against v1.8.0. **STILL NOT VERIFIED after 7 stages: the C++ persistence round-trip has never been executed on any of the 5.** No human has seen the French UI; nothing opened in a DAW; no Windows/WebView2; all 389 French entries are machine drafts. **STAGE G LANDED 2026-08-27 (`3f6b201d` tools, `99e7d206` plugin), out of order, after its blocker cleared:** the concurrent session committed O-Octagon v1.8.0 (`2ba236a1`), so G was re-planned against v1.8.0 rather than the v1.6.0 the plan described. **O-Octagon v1.9.0 — all five shipped plugins are now canon v2, and `check-i18n --strict-v2` passes repo-wide for the first time** (the gate the plan scheduled for Stage L, reached at G). **The plan's numbers were stale again, in the direction that adds work:** 89 `aria-label` not 78; a Motion tab with 10 new captions and 10 new aria-labels that did not exist at v1.6.0; the section numbering now carries two §41s. Shipped 84 `[data-i18n]`, 89 `data-i18n-aria`, 125 LABELS keys from 184 extracted candidates. **THE STAGE'S FINDING: §6 was not going to need a bigger whitelist — it was going to notice nothing at all.** It scanned for `IDENT.textContent =` and required each receiver identifier to be whitelisted; canon v2's `applyLabel` names its parameter **`el`**, which was ALREADY whitelisted for the map-banner and venue name, so the single largest `textContent` writer on the page — all 84 labels — walked straight through. Measured, not reasoned: with canon v2 fully in place and §6 untouched, its only complaint was a markup regex. §6 now asserts the `dataset.label` mirror in four legitimate forms and **the whitelist SHRANK by two**. **Three more repo-level gate defects fixed (eight in this task now; five of the eight reported a violation of a rule the code was obeying):** `check-i18n` assertions 12/13/15 scanned ONE module while **eleven plugins** split their page across siblings (O-Octagon has seven; `js/venue.js` is 796 lines) — wrong in both directions at once, two raw English prose writes passed green while two keys reached only from `venue.js` reported dead; `readSetLabelCalls` could not see `window.__setLabel(...)`, which the canon block's own comment names as the ONLY in-canon route for a sibling module; and `check-ui-labels` could drive a state only by `click`, so the house `dblclick` popover idiom and plugin-owned banner states were unreachable — adding `dblclick` and `eval` took measured coverage on O-Octagon from **35/84 to 84/84**. **Geometry: 4 before fixes** (12 once eight new states were driven) against O-Tapestop's 26–30 and MBC's 222 — the plan predicted fewer for the roomiest frame and MORE would have meant a broken tool; every one was the `align-items: center` shrink-to-fit family Stage H named. **26 negative controls, 26 fire — but one did not fire at first, and it was the executor's own new assertion:** the `venue.js` alias check matched only a bare-identifier RHS, so `const el = rows[0].labelInput;` slipped through; widened, given a non-vacuity check, fires both ways. A counter-proof reverts the `window.__setLabel` fix and confirms its control goes silent — load-bearing, not decoration. **CONCURRENCY:** another session wrote inside `plugins/O-Octagon` throughout, so `git commit -- plugins/O-Octagon` would have swept in `MotionClock.h`, `PluginProcessor.cpp`, `elevation.js`, `CODE_REVIEW.md`, `ui_layout_check.js` and four test `main.cpp`s; the ten files were named individually instead. That session has since bumped the tree to **v1.10.0** and installed it, so the **installed bundle is v1.10.0 while Stage G's commit is v1.9.0** — Stage G's §6 rewrite survives in their working tree, which builds on it. **STILL NOT VERIFIED after 8 stages: the C++ persistence round-trip has never been executed on ANY of the five.** No human has seen the French UI (`AFFECT.` and `MÉM.` are the two to challenge first); nothing opened in a DAW, and this one is a 7.1 spatializer; the Standalone `.app` is stale (`build-and-install.sh` builds VST3 + AU only); a single over-long caption in a shrink-to-fit cell is still ungated (`#btn-scene-store` is this stage's instance, found by hand); the new `eval` state action has nothing enforcing stub-only use; all 184 O-Octagon French entries are machine drafts, `reviewed: false` (repo total 435). **STAGE I STARTED 2026-08-27 — BATCH I1, PARTIAL: 1 of the 9 plugins.** O-Contrabass v1.8.0 (`7035029a`, 10 files) is the SIXTH plugin on canon v2; `check-i18n --strict-v2` now reports **6 canon v2, 0 canon v1**. O-Orbit — the other half of batch I1 — was excluded from dispatch because a concurrent session held five of its files including `Resources/ui/index.html`, the exact file Stage I rewrites; **that blocker cleared mid-dispatch** when that session landed O-Octagon v1.10.0 (`8687e933`) and O-Orbit v1.1.1 (`3593213d`), so O-Orbit is ready to run. The seven `O-simple*` of batch I2 are untouched. **The stale-count pattern held for a third consecutive stage, and this time in BOTH directions:** the bridge surface was already 34 where O-Contrabass's own STATUS.md still recorded 32=32, and the plan's **"47 attrs" was wrong by 15x** — there are **6** `aria-label`, zero `title=`, zero `placeholder`, zero `alt`; 47 had counted 44 `data-tip-title` tooltip titles as localizable attributes. Tips (44) and text nodes (61) were right. Bridge landed at **36** (2 mockup + 10 preset + 20 tuning + 2 hover-help + 2 language), matching the plan's arithmetic for once. **THE GATE DEFECT WAS A FALSE PASS, NOT A FALSE FAILURE — the first of its kind in this task.** `ui_frontend_check.js` §6 enumerated three import shapes; canon v2's named import is a fourth, so it counted 4 references where the page has 5 and printed `PASS: all 4 local UI file references have resource-provider entries` over a file with **no `getResource()` branch at all** — the silent-404 blank UI that assertion exists to prevent, certified green. Now shape-agnostic with a non-vacuity check. That is **nine** wrong-shaped gate assumptions in this task; five reported a violation of a rule the code was obeying, and this one reported compliance the code did not have. **29 negative controls, 29 fired**, each applied to a byte-exact backup and restored FROM that backup rather than `git checkout --` (which would wipe the uncommitted fix). Two decisive: a counter-proof restoring the old scan AND deleting the provider branch still prints PASS — proving the fix load-bearing; and a TDZ control moving the canon block below the eager bindings, which **only `boot-all-uis` catches** (`Cannot access 'uiLanguage' before initialization`) while `check-ui-labels` and `check-i18n` stay silent. Geometry **21 moved → 0**; two English bugs French exposed (`.header-spacer` rendering 0px — a 300px preset readout that had never exceeded 250.2 — and an Active Strings readout reading `4 of 4` in French). 21 render goldens byte-identical (**not the 19 STATUS.md records** — that count predates `note-expression` and `mpe-yz`); `auval aumu OCbs OuDv` SUCCEEDED. **COMMIT-DISCIPLINE INCIDENT, caught and repaired:** the first commit swept in the other session's uncommitted `PLUGINS.md` row (O-Octagon `1.10.0-dev`→`1.10.1-dev`) because **naming a FILE stages the whole file**; caught by reading the diff (`PLUGINS.md 4 +-` where a one-row edit is `2`), repaired by rebuilding the file from `HEAD~1` plus only the O-Contrabass row, re-staging and `--amend` while unpushed, then writing their row back to the working tree — verified afterwards that their row is still dirty and intact and the union-merge duplicate check is empty. **The lesson upgrades Stage G's: on a shared file like PLUGINS.md the unit of scope is the HUNK, not the file.** **NOT VERIFIED:** the C++ language round-trip has never been run on ANY of the six (O-Contrabass uses `xml->setAttribute`/`getStringAttribute` rather than a ValueTree property, sidestepping the string-round-trip trap *by construction* but still unmeasured); no human has seen the French UI; nothing opened in a DAW and O-Contrabass's four Stage-4 human gates were neither attempted nor affected; the Standalone `.app` is stale; **the Tuning tab is NOT localized** — it belongs to the shared `scala-tuning-engine` module and is out of scope for a per-plugin commit, which is the largest gap in the claim; `label.tuningLoadFailed` was statically verified but never rendered; `i18n-extract.js` does not decode `&beta;` (OPEN, affects generated inventory only); 103 French entries here, **618 repo-wide across six plugins**, every one `reviewed: false`. **STAGE I COMPLETE 2026-08-28 — BATCH I2, all seven `O-simple*` plugins, one path-scoped commit each:** O-simpleAdditive v1.1.0 (`b1082fc0`), O-simpleSampler v1.4.0 (`2a2f68c4`), O-simpleSubtractive v1.3.0 (`5fc1ceb0`), O-simpleGrain v1.3.0 (`857dfa85`), O-simpleFM v1.3.0 (`2a5efef0`), O-simplePhysicalModelSynth v1.2.0 (`051e1a72`), O-simpleBeatmaker v1.1.0 (`665150c0`). Plus O-Orbit v1.2.0 (`9d8e50d0`) completing batch I1. **`check-i18n --strict-v2` now reports 14 canon v2 / 0 canon v1 — EVERY `data-tip`-convention plugin in the repo is localized, so Stage J starts from a green repo.** 639 new French strings (1,350 repo-wide), all `reviewed: false`. **THE PLAN'S READ OF THIS BATCH WAS WRONG IN THREE WAYS.** (1) T13 counted `data-tip` anchors without reading what they held: in this family `data-tip` is the tip **KEY**, looked up in a `const TIPS = {}` object — but canon v2's `applyI18n` *writes* `data-tip` as the **body** and assertion 3 forbids literal `data-tip=` in markup, so every plugin needed its anchors moved to `data-param`/an id AND its listeners delegated on the document (`querySelectorAll("[data-tip]")` at setup binds nothing once anchors move, and generated cells do not exist yet anyway). (2) "No bridge toggle" was true; "no toggle" was not — three of the seven already shipped one (O-simpleSampler a real C++ bridge named **`tipsEnabled`, not `tooltipsEnabled`**, which is why a pre-flight grep missed it; O-simpleGrain and O-simplePhysicalModelSynth a localStorage one). Each MOVED into the gear popover. O-simpleSampler's was the dangerous one: it cached `data-tip-title` to restore on re-enable and `applyI18n` writes that attribute every sweep, so keeping the restore would have put **English back after a French switch** — the whole layer was deleted rather than adapted. (3) Roomy frames did not mean free geometry: **31 pins across seven plugins, each reverted alone and confirmed to re-break the gate; THREE were decoration** (their negative control passed) and were removed or re-labelled as design pins rather than claimed. **TWO more wrong-shaped gate assumptions — eleventh and twelfth in this task — each landed as its own commit ahead of its plugin:** `08e7649b`, assertion 7 measured **ANIMATED** elements, so a SMIL `animateTransform` circling a pulse every 1.7s reported three phantom moved rows **in a SINGLE language** and a different answer every run (PROBE now freezes SMIL + Web Animations, deliberately **not** `CSSTransition` — the state pass drives every slider and knob stems transition their rotation, so pausing one mid-flight would invent the diff being removed); and `816f2767`, assertion 8 compared labels **across PAINT LAYERS** because a rectangle has no z, so an opaque `z-index:40` popover drawn over the page on purpose read as a collision the moment the French string grew long enough to reach it. Both regression-swept across every previously-shipped canon-v2 plugin. **WHAT THE GATES STILL CANNOT SEE, measured this stage:** assertion 12 does not see `textContent = someVariable` (only literal RHS produces a row, so `showToast(msg)` ships English with the gate green — caught by hand on O-simpleGrain, and the three plugins committed before it were re-checked and were clean); assertion 7 **prints only the first 12 moved elements** (O-simpleSubtractive reported "18 moved", listed 12, and the six withheld held a second independent bug); assertion 8 only reports pairs disjoint in English, so a `white-space: nowrap` fix can introduce an **English** collision it will never catch; and **`check-ui-labels` measures only at `setSize`** — O-simpleBeatmaker is resizable to 860x640, 200px narrower than anything the gate sees (hand-measured: nothing clips, French 15px taller in an already-scrolling pane), and every other plugin's minimum is unmeasurable by this gate. **PRE-EXISTING BUGS FOUND AND FIXED:** dead flex bases lost to specificity on four plugins (a bare `.group-x` (0,1,0) loses to `.rack .group` (0,2,0), so authored bases had never applied and racks were sized by max-content — **which is precisely what made them language-dependent**; the largest geometry win available, and it changes the ENGLISH layout); ~120 controls with `role="slider"`+`tabindex` and **no accessible name at all**, fixed free by `data-i18n-aria`; **render-harness version drift on two plugins, LIVE on one** — O-simplePhysicalModelSynth hardcoded `JucePlugin_VersionString="1.0.0"` against a shipping 1.1.0 and `~/Library/O-simplePhysicalModelSynth/Presets/Factory/.factory-version` on disk read `1.0.0`, because the sentinel compares against that string and a mismatch **rewrites the user's real factory-preset directory** (both converted to the by-reference form the other five already use); `harmonicTip()` rendering **"The 2th harmonic"** in every O-simpleAdditive build since tooltips shipped; O-simpleBeatmaker's transport strip resizing the header whenever the host started playing; and `bindKnob()` freezing the accessible name in one language on two plugins (it copies `textContent` once at bind time, which runs before `initI18n()`). **REPORTED, DELIBERATELY NOT FIXED:** O-simplePhysicalModelSynth's **Delete-preset button deletes without confirmation** — `setupPresetManager()` calls `deletePreset()` directly and passes no `deleteButton`, so the vendored module's `promptDelete()`/`onConfirmDelete` are dead code; adding a dialog is a design change. **OPEN HUMAN DECISION:** O-simpleSampler's content grew **796 → 835px inside a FIXED 980x720 frame**, putting the on-screen keyboard entirely below the fold on a pedagogical plugin whose keyboard is a teaching surface (17px rack repair, 10px reserved caption line, rest a new header row; the alternative — trimming eight French lesson captions to a character budget holding their English line count — was rejected as copy any later edit re-breaks). **O-simpleSubtractive proves the cost is not inherent:** its extent was measured identical (1118px) before and after. **CONCURRENCY:** another session landed O-simpleGrain v1.4.0 (`4d6056bc`, DSP) between our plugins 4 and 6 — nothing collided, commits stayed path-scoped and PLUGINS.md clean, but that plugin now carries i18n and DSP work in one version line. **STILL NOT VERIFIED after 9 stages, now across FOURTEEN plugins: the C++ language round-trip has never been executed by hand on ANY of them** (reasoned from the `isVoid()` guard, not measured); no human has seen any French UI; nothing opened in a DAW; Standalone `.app`s stale; Windows/WebView2 font metrics remain the named hardware deferral (tightest French margin measured this batch: 20.9px); all **1,350** French strings repo-wide are machine drafts, `reviewed: false`.  **STAGE J COMPLETE 2026-08-28 — all seven second-renderer plugins PORTED and localized, one path-scoped commit each:** O-FreqPulse v1.18.0 (`37602894`), O-Polystutter v1.14.0 (`66456f1e`), O-Lyrica v2.4.0 (`09bdbb61`), O-SpectralShaper v1.7.0 (`5b509e8c`), O-IntonationPad v2.9.0 (`bb275ae1`), O-Marimba v1.13.0 (`5406f419`), O-Gain v1.3.0 (`f571a78e`). **ONE TOOLTIP RENDERER REPO-WIDE — the third stop point.** Done-criteria verified by the orchestrator independently of the executors: `tooltipHeight` grep returns **0** over every served UI root, and every surviving `data-tooltip` is a COMMENT documenting the deleted design; `check-i18n --strict-v2` reports **21 canon v2 / 0 canon v1**. **THE PLAN'S MODEL OF THESE SEVEN WAS WRONG IN FOUR WAYS.** (1) **O-Gain had no tooltip JavaScript AT ALL** — its help was a pure-CSS `[data-tooltip]::after` with three hand-picked direction classes, unmeasurable and unclampable, and because 23 of those boxes were **in-flow** they inflated the document's own scroll extent to **435x540 inside a 350x500 window, in ENGLISH, at rest**; the port fixed a pre-existing English defect nobody had measured. (2) **O-Marimba does NOT have the tooltips bridge the plan credits it with** — v1.12.1 called `window.JuceAPI.getNativeFunction(...)` inside a try/catch and `window.JuceAPI` has never existed there; dead call removed, not repaired. (3) **Every tip count is a grep artifact, wrong in BOTH directions** — O-Lyrica's 48 was 43, O-IntonationPad's ~37 was **77 live anchors / 74 unique** once `makeKnob`-applied and injected-template tips were counted; O-Marimba's ~40 text nodes were 61, the gap being two shared FX modules. (4) **The `"Label: sentence."` shape mostly does NOT hold** — 0 of 43 hand-split on O-Lyrica and 0 of 15 on O-Marimba, but **63 of 77** on O-IntonationPad and **23 of 23** on O-Gain; the rule that worked and should be Stage K's default is **the title is the control's own existing English caption — reuse it, never author**, every body verified byte-identical against HEAD. **THE VERTICAL CLAMP — found on plugin 1, latent in all 21 copies:** the O-ReverseDelay renderer prefers *above*, flips *below* and **stops there**, so a tall anchor strands the tip off-window (O-FreqPulse's 376px `#grid-area` put a 97px French tip 15px below the frame; O-IntonationPad stranded two tuning-tab tips by 53px and 67px). Independently reproducible on **only those two**; measured-not-assumed on the other five by deleting the two lines and re-sweeping. **Every "not reproducible" verdict is backed by a harness-blindness check** — delete the HORIZONTAL clamp instead and confirm the sweep still reports failures (O-SpectralShaper 14 off-frame tips, O-IntonationPad 26, O-Marimba 10, O-Gain 26); without that control, "no failures" is indistinguishable from a sweep that cannot see failures. **THE D-01 TEST NEEDS A THIRD ARM — the most reusable finding of the stage.** O-Marimba's six timbre words (`Edge`/`Center`/`Shimmer`/`Focused`/`Warm`/`Bright`) are backed by `AudioParameterFloat`, so the plan's binary choice-vs-not test says localize — but they are written into `#strike-value`/`#damping-value`/`#tone-value`, the knob's **READOUT**, wearing a word instead of a number at the ends of its travel. Contract §5: a readout is never `[data-i18n]`. Keying one makes the element enter and leave the sweep as the knob turns (repainting `Chaud` over `62%`), and its French face exists only at a knob extreme no committed gate state can reach, making D-04 undischargeable. **Third arm: what ELEMENT receives the string.** And the clean discriminator for the choice arm is **BYTE-IDENTITY against the option strings** — `12-TET`/`MTS-ESP` match `TUNING_MODE` verbatim so they are exempt, `CUSTOM` does not (the option is `Scala`) so it localizes to `PERSO`; O-Gain ran the same test on one page and landed on both sides (`Peak`/`RMS`/`VU`/`LUFS` exempt, `M/S OFF`/`ENC`/`DEC` page-invented and localized). O-Gain then **overruled the third arm with reasons**, correctly: `LOW`/`MED`/`HIGH` in `#learn-confidence` is copy because that node never holds a number, `learnConfidence` is not a parameter, and a committed state drives all three French faces. **CHECKPOINT 4 MEASURED AT LAST, on 2 of 21.** O-IntonationPad six ways: symmetric `comm -3` grep-diff of 31 `getNativeFunction` names against 31 `withNativeFunction` registrations empty on both sides; both symbols plus the French table in the shipped VST3 binary; a real Standalone launch-and-quit writing `<Parameters uiLanguage="en">`; a stored `fr` surviving quit→relaunch→quit; three negative controls (`"zz"`→en, a pre-v2.9.0 session with no attribute→en without crashing, then `fr` again to prove the first two were not the default sticking); and the JS half driven separately with the backend holding `fr` before page load. O-Marimba both halves: a compiled JUCE probe over the three added paths (confirming the attribute returns a **STRING** var, per the known ValueTree XML type loss), `auval` VERIFYING CLASS INFO, and a four-state drive through the real gear popover. **A trap worth carrying: `build-and-install.sh` builds VST3+AU only** — O-IntonationPad's first round-trip read `uiLanguage` as ABSENT because it was testing a **stale July Standalone**; it checked the binary with `strings` rather than trusting the run, then built `_Standalone` explicitly. **FOUR MORE WRONG-SHAPED GATE ASSUMPTIONS — thirteenth through sixteenth, each committed ALONE ahead of its plugin, and all four the SAME shape found four ways: the gate grepped source text where it needed to parse what the page renders.** `b0ce697e` (scan EVERY inline script, not just the canon target — its own sweep then proved it LATENT across all 16 prior plugins), `1151de07` (assertion 12 PARSES injected markup instead of grepping it), `435957a7` (assertion 15 counts keys declared in injected markup), `91d81cf7` (i18n-extract finds injected keys in an ACCUMULATED template). **A SEVENTEENTH was found and DELIBERATELY NOT FIXED:** assertion 12 reads the whole RHS of a `.textContent =` and flags every two-letter literal — right for `on ? "On" : "Off"`, wrong for a literal in **call-argument position** (`formatParam('trim', norm)` renders `"+0.0 dB"` and `'trim'` never reaches the screen). It occurs at exactly two sites repo-wide and both were these; narrowing a **coverage** assertion on n=2 trades a cheap over-report for a costly under-report, so the call sites were respelled to pass the branch's own `paramId` variable and the gate's shape is named in `O-Gain/js/app.js:591-608`. The O-Marimba run made the mirror-image call: broadening `scanJsSource()` to match its own `markupKeyRefs()` fix would have reported five violations on shipped **O-Lyrica v2.4.0**, which keys those nodes by id AFTER injection via `window.__setLabel(...)` — legal canon; tried, reverted, named in code. **PRE-EXISTING ENGLISH DEFECTS THE FRENCH SWEEP EXPOSED:** O-Gain's `.learn-section` slid the Target readout up to 40px sideways whenever Learn ran; O-Marimba's tuning-mode row overhung its 200px panel by 10.83px and the Scala row by 12.78px; O-SpectralShaper's header read `v1.6.1` on a v1.6.2 build; O-Lyrica's `.truekeys-view` was `display:flex` unconditionally, painting "Hold 2+ notes to see intervals" in EVERY visualization mode, and `technique` option 4 lacked an accent `createParameterLayout` has always carried; O-IntonationPad's `TOOLTIPS.voicingMode` was applied by `makeKnob()` to a voicingMode knob that has never existed. **COMMIT-DISCIPLINE NEAR-MISS, caught and repaired:** O-Marimba's first commit used `git commit -- <paths>` **without staging**, which takes only **TRACKED** files and so silently omitted three new untracked files, leaving HEAD with a gutted `index.html` and no controller; caught by reading `git show --stat` and amended. **Every Stage-K plugin adds new files too — `git add` the exact paths, commit, then `git show --stat` and confirm.** **PROCESS INCIDENT:** one run reached for `screencapture` to see a reopened plugin and captured the user's entire desktop including an unrelated private document; deleted immediately, and the orchestrator verified independently that no PNG was retained. Full-screen capture is never the verification — headless harness and DOM reads only. **STILL NOT VERIFIED after 10 stages, across TWENTY-ONE plugins:** Checkpoint 5 outstanding on all 21 (no human has seen any French UI); Checkpoint 4 measured on 2 and reasoned on 19, with the **DAW** reopen unexecuted everywhere; all French strings repo-wide are machine drafts, `reviewed: false`; Windows/WebView2 font metrics remain the hardware-blocked deferral, tightest French margins this stage O-Marimba **1.69px** (CUSTOM in a 48px box), O-Polystutter 4.2px (DÉCLIN), O-FreqPulse 5.2px (MÉDIUM). |
| 260821-gwc | Branch/worktree sweep — merged 3 unmerged branches into main (O-Contrabass v1.7.2 tuning-tab fix, O-Octagon v1.3.0 flat-field fix, O-Emulator v1.0.0 WIP stages 0–2 + verified DSP), deleted all 6 branches with `-d`, removed all 3 worktrees (emulator/octagon/prism — each verified clean first); PLUGINS.md union-merge dup check clean ×3; repo now single main checkout, ahead 23, unpushed | 2026-08-21 | 559ad9c0 | Complete | [260821-gwc-clean-up-branches-and-worktrees-merge-to-main](./quick/260821-gwc-clean-up-branches-and-worktrees-merge-to-main/) |
| 260821-aym | Fix multi-plugin branch visibility after `/clear` — **migrated the project to trunk-based development**. Root cause: PFS models plugin work as `(plugin, stage, phase)` from fixed paths and has no reader for the 4th coordinate, *location*; STATUS.md is branch-versioned, so a fresh session on `main` reads a stale file that still looks current. Rather than build branch-aware machinery, all plugin work moves to `main` and any deviation is made loud. `SessionStart.py` gained a `=== Git Context ===` section printed **on stdout** (stderr would reach the human and leave the agent exactly as blind — the defect being repaired): branch (`[WARN]` if not `main`), worktree count (`[WARN]` if >1), dirty/staged counts, unpushed vs `origin/main`, and an in-flight scan of `plugins/*/.planning/STATUS.md` (**7 in-flight** today of 37 files; **8 files have no frontmatter at all** and are skipped silently). Scan is bounded at 40 lines/file — O-Octagon's frontmatter runs to line 1628 and O-Contrabass's to 866, but all four keys sit within the first 10 lines. Values are sanitized before entering session context (strip trailing ` #` comments — O-Octagon's `status:`/`phase:` both carry one — drop `ord<32`, truncate 60, cap 12 rows); `run_git` is list-form only, never `shell=True`; 1s per-call timeout + **1.5s cumulative deadline** (measured cost 0.078s, whole hook 0.16s vs the `timeout: 5000` registration) and the section is wrapped in `try/except` so the never-block `sys.exit(0)` contract holds. CLAUDE.md's `## Parallel Plugin Development` rewritten to 4 subsections (concurrent-session commit discipline, branchless rollback, exceptional-branch-only, condensed union merge); the per-plugin branch/worktree conventions and both command blocks are gone. **A plan-supplied ground truth prevented shipping a broken command:** `git revert` takes no pathspec (`git revert -n <sha> -- <path>` → `fatal: bad revision`), so the documented path-scoped undo is `git restore --source=<tag> -- plugins/<Name>`; tags are `vX.Y.Z-<PluginName>`, version first. Both resume protocols now halt on a non-main branch (`context-resume` Step 0 + checklist + `<requirements>` + `<anti_patterns>`; `continue.md` Process item 1, items renumbered 2–7, plus a `Wrong branch` output example) — surface the branch, never auto-proceed, never auto-switch. Empty untracked `.claude/worktrees/` removed (`rmdir`; no diff, nothing to stage). **The shared-index race was observed live mid-run:** one `git status --porcelain` sample showed `.claude/commands/publish.md` staged by a concurrent session and the next showed nothing staged — exactly the hazard the new commit discipline documents; all 3 commits were path-scoped so nothing was swept in. Branch WARN negative-controlled on a scratch branch (flips `[WARN] tmp/aym-probe` → `[OK] main`, not decoration). 14/14 plan gates green; 4 pre-existing dirty files untouched | 2026-08-21 | a930355c, 1a758730, d85072ef | Verified | [260821-aym-fix-multi-plugin-branch-visibility-after](./quick/260821-aym-fix-multi-plugin-branch-visibility-after/) |
| 260813-mz1 | Fix multi-plugin parallel-work isolation — `.gitattributes` created with `PLUGINS.md merge=union` (primary fix for the most-contended file: 123 edits/3 mo, row-scoped); false O-SpectralShaper row on `feat/o-octagon` reverted to 1.3.2 via a `1 1 PLUGINS.md` commit in the octagon worktree (CHANGELOG sha256-unchanged); dead `VST-contrabass-v1.1` worktree removed + `improve/o-contrabass-v1.1` deleted with `-d` after re-confirming 0-ahead; stale focus layer (`.planning/workflow/active-plugin.json` + `registry.json`, frozen 2026-07-16) deleted with **15 readers** redirected (focus/plugin state → PLUGINS.md + plugin-local STATUS.md; module tracking → `modules/registry.yaml` — the briefed 7-command reader list was wrong, measured set differed); CLAUDE.md gained `## Parallel Plugin Development` (53 ins / 0 del) incl. union-merge duplicate-row caveat + `VST-development-<slug>` sibling naming. Infra commits on new `chore/parallel-work-isolation` cut from local main — NOT on the plugin branch. Report-only: 4 stale branches surveyed, none touched; a concurrent session was committing to `feat/o-octagon` mid-run (benign, fix commit atomic + still ancestor). Verified 7/7 must-haves | 2026-08-13 | 07e95283, 7849b31b, 9998167e, 252f9859, 10828090 | Verified | [260813-mz1-fix-multi-plugin-parallel-work-isolation](./quick/260813-mz1-fix-multi-plugin-parallel-work-isolation/) |
| 26 | Disable per-push GitHub Actions builds; build only via /publish | 2026-08-27 | 6ae1760c | complete | [260827-crb-disable-per-push-github-actions-builds-b](./quick/260827-crb-disable-per-push-github-actions-builds-b/) |
| 260831-wq3 | Sweep tuning-panel family for O-Formant item 22 gap (item 71): O-MicrotonalSampler 1.25.2 panel half ported (probe 440→442 Hz, drags 450/450→452/462); O-Bells, O-IntonationPad, O-Lyrica, O-Prism, O-Wind clean; O-Reed tuning tab proven disconnected (0 of 19 native fns registered, panel hangs silently) — found, not fixed | 2026-08-31 | 56fc8bbc | complete | [260831-wq3-sweep-tuning-panel-family-for-o-formant-](./quick/260831-wq3-sweep-tuning-panel-family-for-o-formant-/) |
| 260901-akh | Research and plan Chinese (zh-Hans) localization across all 43 plugins: research/i18n-zh-hans-localization.md (variant zh-Hans, no webfont hazard, width = chars×font-size, corpus 3,789 entries / 543 shared strings = 64%, review model reviewed:'mt'|'bt'|'native') + 6-stage IMPLEMENTATION-PLAN.md (Stage 0 eight gate prerequisites incl. 43-file canon sweep, Stage 1 zh glossary+lint, Stage 2 pilot O-Chorus, Stage 3 hard cases, Stage 4 waves, Stage 5 QA). Documentation only — no plugin/script touched | 2026-09-01 | 038bdedd | complete | [260901-akh-research-and-plan-chinese-localization-a](./quick/260901-akh-research-and-plan-chinese-localization-a/) |
| 260901-c3s | Stage 0 of the zh-Hans rollout: make the i18n gates language-agnostic (P1–P7 + decision f). i18n-canon.js passes the stored code through (applyI18n(code)) + 43 shipping copies re-synced + 4 unserved i18n-index-draft.html deleted (e9aab4fc); check-i18n accepts en,fr OR en,fr,zh-Hans, loops read LANGUAGES, reviewed enum 'mt'|'bt'|'native' at BOTH sites; check-ui-labels six loops read LANGUAGES via disk-side i18n.js read; i18n-extract skeleton emits zh-Hans row (ee2e92cf, ef9f47f9). Gates: check-i18n ALL PASS 43/43 at two languages, 3-language scratch PASS, boot-all-uis 43/43 0 DEAD. Zero i18n.js tables changed | 2026-09-01 | ef9f47f9 | complete | [260901-c3s-stage-0-of-the-zh-hans-rollout-make-the-](./quick/260901-c3s-stage-0-of-the-zh-hans-rollout-make-the-/) |
| 260901-fr8 | Stage 1 of the zh-Hans rollout (P8): three new REPORT-ONLY tools — scripts/i18n-zh-glossary.js (552 terms = all 551 measured shared English strings + 3 single-site extras; SAME_AS_EN 12; budgets only for the 3 measured O-Chorus cells, 549 UNBUDGETED and disclosed), scripts/i18n-zh-lint.js (Z1 Z2 Z3 Z4 Z5 Z6 Z7 F1 R1 — no French T1–T7/C1 ported; `--self-test` SELF-TEST: 9/9, every rule fires on a violation fixture and is silent on a control; Z3 set machine-derived from OpenCC, 4139 chars, provenance in-file), scripts/i18n-zh-backtranslate.js (`--emit` withholds English, `--ingest` refuses forward-identical provenance). All three exit 0 with an explicit VACUITY line on the 43 plugins (0 zh entries yet — not a pass). check-i18n ALL PASS 43/43, i18n-fr-lint exit 0, zero files under plugins/, no npm dependency (68cd81c6, 34211ef7, 6d52bb04) | 2026-09-01 | 6d52bb04 | complete | [260901-fr8-stage-1-of-the-zh-hans-rollout-p8-create](./quick/260901-fr8-stage-1-of-the-zh-hans-rollout-p8-create/) |
| 260901-h9g | Stage 2 of the zh-Hans rollout: pilot O-Chorus v1.5.0 ships en/fr/zh-Hans with all 28 zh entries at reviewed:'bt' via a BLIND independent reverse pass (claude-sonnet-5, opaque row ids r01-r38 so `label.depth` could not leak the English; 0 fixed, 24 exact, 14 accepted synonyms with reasons in the i18n.js header). Structural answers Stage 3 inherits: CJK tail on 5 of 8 font stacks (3 omissions justified: nav glyphs, ASCII preset filenames, gear glyph); endonym as numeric entities + I18N_EXEMPT; line-height pinned on 3 nodes at unitless 1.1111 (EN 10.00px / 9px — assertion 7 named 26 movers all driven by .knob-label inheriting `normal`) and #lang-select pinned to its 65px EN intrinsic; Stage-1 budgets intact (depth 6, save 6, spread 5), NO new cell — every zh caption is NARROWER than its English (zh spends height, not width); ui_tip_render_check.js now reads LANGUAGES (was VACUOUS for zh — hard-coded ['en','fr','en'] and a <=2-state height recorder; assertion 5 now gates on differs, since zh SHRINKS 6 tips). Gates: check-i18n O-Chorus + 43/43 ALL PASS; check-ui-labels 0 FAIL, PASS [7][GEOMETRY DIFF] fr AND zh-Hans, zh vacuity 92%; i18n-zh-lint 0 findings on all 9 rules incl. R1; boot-all-uis 0 DEAD; i18n-fr-lint exit 0; Han gate empty with HAN_CONTROL firing; auval `aufx OuCh OuDv` VALIDATION SUCCEEDED. Commits 4ceb5b1f (forward pass), 707d3499 (ship). Disclosed in CHANGELOG: no native reader; <=9px legibility tier (5 nodes); A5 WKWebView remeasure impossible without a debug hook (Chromium +30% reconfirmed). OPEN before Stage 3: (1) Z3 Traditional-only set contains 像 U+50CF — a standard simplified char and the glossary root for pan (声像) — Z5 REQUIRES what Z3 FLAGS; fix scripts/i18n-zh-lint.js before the first Pan knob (O-Chorus reworded around it: 声场位置 / 立体声场); (2) i18n-zh-backtranslate --ingest resolves the manifest beside the INGEST file not the batch, so the forward-provenance refusal ran INERT until --manifest was passed explicitly — fix or document; (3) tip.language en/fr bodies name two of three languages (needs a French re-review); (4) the --emit id column leaks English — blind the ids in the tool, not by hand. | 2026-09-03 | 707d3499 | complete | [260901-h9g-stage-2-of-the-zh-hans-rollout-pilot-o-c](./quick/260901-h9g-stage-2-of-the-zh-hans-rollout-pilot-o-c/) |
| 260903-rjm | Hover-help (tooltips) switch for the 23 plugins that had a language selector but no toggle — O-Chorus (v1.6.0) as the tracer, then 22 more in five path-scoped batches; all 23 minor-bumped, CHANGELOG'd, rebuilt, reinstalled, auval PASS. Scan: 43/43 have `#lang-select`; 20 already had a switch on two coexisting id conventions (`#help-toggle` x11, `#tips-toggle` x9). Recipe: a second `.settings-row` in the gear popover, `data-tip-always` on the gear + switch, a `tipsEnabled` gate on `show()`, default ON, localStorage-persisted, labels `label.hoverHelp`/`ui.on`/`ui.off`/`aria.helpToggle` reused from the 20 siblings (zh-Hans roots 悬停帮助/开/关 verbatim from the glossary). Layout: only O-Tremolo (116→120px, French AIDE AU SURVOL 95.98px overflowed by 1.98) and O-Prism (caption pin → hard 168px panel) needed widening; O-Formant needed reparenting (block popover, row landed INSIDE the lang row). Five of the 23 keep the renderer outside index.html (O-Formant, O-GrainScatter, O-Texture, O-TextureForge, O-MicrotonalSampler), not one as the plan said; O-Texture was at 0.3.1, not 1.19.2. Task 3: check-i18n assertion [16] — a language selector implies EXACTLY ONE hover-help switch (either id), bound in TIP_BINDINGS, key present in I18N, with a vacuity check; all four negative controls fired (exit 1/1/2/1) and the `show()` gate's own control fired on O-Chorus (ui_tip_render_check section 8, 16 assertions) and O-Bass. Gates: check-i18n 43/43 [16] live 43 examined, boot-all-uis 43/43 0 DEAD, i18n-fr-lint 0, i18n-zh-lint O-Chorus 0/9, check-ui-labels 0 moved on all 23, ui_tip_render_check 0 on 22 (O-ReverseDelay has none). OPEN: two O-Chorus zh-Hans bodies (`tip.tipsToggle`, rewritten `tip.settings`) ship at reviewed:'mt' below the 'bt' bar — no independent reverse pass available; queue `i18n-zh-backtranslate --emit O-Chorus`. O-ReverseDelay carries no standing switch gate. In-DAW spot-checks (O-Chorus, O-GrainScatter, O-Prism, O-Wind) not done by a human. Commits 69918256, 0e406c7b, d4fd9f35, 566a7fa6, 7b3b689c, a4bc791a, 6ca98461. | 2026-09-03 | 6ca98461 | complete | [260903-rjm-o-chorus-has-language-options-but-no-but](./quick/260903-rjm-o-chorus-has-language-options-but-no-but/) |
| 260903-ukp | French hover-help renamed "Aide au survol" → "Infobulles" across all 43 plugins — glossary root-only (old term removed; fr-lint went exit 2 / 117 G1 across 43/43 before the rollout, the non-vacuity proof), O-Gain v1.3.3 tracer then 42 in seven path-scoped batches, every plugin patch-bumped, CHANGELOG'd, rebuilt, reinstalled; every body re-agreed plural and read by the developer on the 75-row sheet BEFORE the edits (reviewed:true kept, unreviewed TOTAL 0); 13 sibling renderings (aide contextuelle / bulle d’aide / descriptions|explications au survol) folded in; 8 stale width comments re-measured; two follow-ups: Activées/Désactivées switch faces in 3 plugins, O-SpectralShaper pin 64→66px proven by border-box in both languages. auval 42/43 (O-Lyrica pre-existing, item 72) | 2026-09-04 | 4a1799d7 | complete | [260903-ukp-for-the-french-language-change-aide-au-s](./quick/260903-ukp-for-the-french-language-change-aide-au-s/) |
| 260904-f2k | The two zh tool defects fixed before Stage 3 (three fixes, every positive control observed RED first): (1) Z3 Traditional-only set rederived — the rule keys(TS)\keys(ST) was blind to TS keys whose value list contains themselves (TSCharacters.txt line 418 `像→像 象`: survives T→S unchanged, never an ST key), 941 chars wrongly in-set; new committed generator scripts/gen-zh-trad-only.js with --verify re-audits the committed literal offline (pre-fix literal exits 1 naming 像 乾 徵 於 夥; self-exclusion scans the WHOLE value list — 18 chars map to themselves in later position); set 4139→3198 chars / 12773→9660 bytes, OpenCC digests re-fetched byte-identical to recorded provenance; 声像 (the glossary's only rendering of pan) + 乾 added as self-test controls BEFORE regenerating (SELF-TEST 8/9 Z3 BROKEN → 9/9); repo-wide Z3 unchanged at 0 across 43 plugins — the defect was latent, the control lives in the fixture. (2) `--ingest` resolves its manifest beside the EMIT batch (was: beside the ingest file, so the forward-provenance refusal ran INERT — pre-fix it printed all 44 triples under '(none recorded — the manifest was not found beside the batch)'); now refuses loudly when the manifest is absent, ambiguous, unparseable, or has no forward provenance. (3) `--emit` row ids blinded to opaque 12-hex behind a per-batch salted map (pre-fix id column read `O-Chorus\|label\|label.rate` — the key IS the English); two emits of one target share zero ids; round-trips via manifest.ids with 0 unjoinable; grep for `O-`/`label.`/`tip.`/pipe over the batch returns 0. check-i18n ALL PASS 43/43. Deviation: refuse() generalized with optional headline/remedy instead of duplicated. | 2026-09-04 | 37c9b2ab | complete | [260904-f2k-fix-the-two-zh-tool-defects-before-stage](./quick/260904-f2k-fix-the-two-zh-tool-defects-before-stage/) |
| 260904-g5l | Stage 3 of the zh-Hans rollout: **hard-case wave COMPLETE** — O-Octagon v1.12.0 (131 entries/246 rows; ui_layout_check gained its first language arm, 34 sections), O-Bitrot v1.16.0 (117/172; #viewSync width pin CLOSED the standing 2-FAIL French baseline; clamp gate zh arm, 14 clamps engaged), O-Chorus v1.6.2 (last 44 rows folded, 0 below ship bar, stale tip.language enumeration removed), O-MicrotonalSampler v1.27.0 (275/296; tip gate's LANGUAGES 'en,fr' hard-assert replaced with derive-or-ABORT proved both ways; v1.24.0 pluralization removal verified 0 live sites via positive-control regex). 758/758 zh rows at reviewed:'bt' via 5 blind reverse passes (8 rows re-authored — incl. Dither/Jitter sharing glossary root 抖动, invisible to Z5/F1); auval cold sweep PASS ×4 | 2026-09-04 | 88575ff1, 125f8a0b, 88c36535, 43246871, d788b769, 4a26b265, d702f6f4, 567deaab, 5c4b326f |  | [260904-g5l-stage-3-of-the-zh-hans-rollout-the-hard-](./quick/260904-g5l-stage-3-of-the-zh-hans-rollout-the-hard-/) |
| 260904-q4j | Fix i18n-zh-lint before Stage 4: maskLatin leading-dot extension masking + self-test controls, and new rule Z8 (inter-Han space) with independent-scan agreement across all 43 | 2026-09-04 | 18c7f375 |  | [260904-q4j-fix-i18n-zh-lint-before-stage-4-masklati](./quick/260904-q4j-fix-i18n-zh-lint-before-stage-4-masklati/) |
| qrc | Stage 4 wave 4a of the zh-Hans rollout — six plugins ship en/fr/zh-Hans: O-AnalogSaturation 1.5.0, O-Prism 1.24.0, O-Comp 1.9.0, O-Freeze 2.5.0, O-Texture 0.5.0, O-Bass 1.7.0. 422 zh entries authored at 'mt', promoted to 'bt' via blind back-translate; repo-wide 1338 zh rows all 'bt'. Gates: check-i18n exit 0, check-ui-labels zh arm 0 geometry moved, i18n-zh-lint 0/43, fr-lint 0/43, auval SUCCEEDED x6. 17 path-scoped commits; PLUGINS.md corrected from CMakeLists values. Findings for wave 4b: CJK font tail must PRECEDE the trailing generic (O-Chorus's shipped tail is inert); two-language-gate census was 14 not 12 (all four repaired, derive-or-abort); canvas fillText copy unreachable by CSS/gates (O-Comp); --emit needs --plugin to scope; Z8 caught its first live defect; glossary roots botanical→植物律 re-authored w/ termNote, Rank-2→二阶音律 ships with concern recorded (roots untouched — repo-wide change not owned here) | 2026-09-04 | 051b9bcc |  | [260904-qrc-stage-4-wave-4a-of-the-zh-hans-rollout-o](./quick/260904-qrc-stage-4-wave-4a-of-the-zh-hans-rollout-o/) |
| 260905-acr | Stage 4 wave 4b of the zh-Hans rollout — six plugins ship en/fr/zh-Hans: O-Tremolo 1.10.0, O-IntonationPad 2.10.0, O-DigiDelay 1.6.0, O-AnalogEQ 1.5.0, O-SimpleReverb 1.9.0, O-Emulator 1.4.0. 492 zh rows authored and promoted to 'bt'; corpus 1338→1830, all 'bt', 0 'mt'. i18n-zh-lint FLIPPED to a gate (exit 2) in its own commit before authoring — fired once on real work (Z5 on a tip title, whose caption's termNote could not cover it). O-IntonationPad's 17 late tip bindings fixed at source (plan's N7 premise measured false first — tips carried zh at settle). auval SUCCEEDED x6, one cold sweep. 18 path-scoped commits fed08208…d0227c6c. Findings for wave 4c: FOURTH font carrier — button/select/input do NOT inherit font-family (UA gives Arial), so a grep census of declarations+SVG attrs is structurally incomplete (14 nodes found only by measurement); a zh row outlived the en sentence it translated — nothing compares a zh body against its own en (C7); scratch measurement harness worth promoting to scripts/ (D7) | 2026-09-05 | d0227c6c |  | [260905-acr-wave-4b](./quick/260905-acr-wave-4b/) |
| 260905-izp | Promote the wave-4b measurement harness from scratch to scripts/ (D7) — `scripts/measure-ui.js` + `measure-ui-README.md`: per-node computed-style census of any plugin's WebView page at the PARSED shipping frame, cumulative `i18n-states.json` walk (click/dblclick/eval), W2 OR-ed visibility, LANGUAGES from the CMake-derived UI root; four `--report` screens (undeclared-font, line-height-normal, wrap-count, svg-font-attr) as pure functions re-runnable via `--from <rows.json>`, count printed even at 0, SKIPPED distinct from 0. Six scratch defects repaired — the largest unrecorded: scratch keyed rows on tag.class so siblings collapsed (O-AnalogEQ 29 `<text>` → 1 row; O-IntonationPad 948 DOM keys vs 318 display ids), plus silently-skipped `dblclick` states and a hardcoded ROOT. Positive controls fired ×2 (12/12 O-Emulator buttons name a CJK face; one planted attribute → svg-font-attr 1 finding). O-IntonationPad: 55 line-height-normal (all zh), 2 wrap-count. Record corrections: O-Emulator frame is 620×430 not 700×380; O-AnalogEQ's 24 SVG attrs show 0 divergence. tipcheck.js / trace.js NOT promoted, reasoning in README. Exit 0/1/2/77 | 2026-09-05 | 7dcc4308, 59ff5826, 61382a34 |  | [260905-izp-promote-the-wave-4b-measurement-harness-](./quick/260905-izp-promote-the-wave-4b-measurement-harness-/) |
| 260905-rwh | Stage 4 wave 4c of the zh-Hans rollout — six plugins ship en/fr/zh-Hans: O-Detune 1.9.0, O-Bells 4.5.0, O-Gain 1.4.0, O-SpectralShaper 1.8.0, O-Bassoon 1.4.0, O-TextureForge 1.4.0. 610 zh rows authored at 'mt' and promoted to 'bt'; corpus 1830→2440, 16→22 plugins, BELOW SHIP BAR 0; flipped i18n-zh-lint exit 0 (0 findings / 43 plugins / 1844 entries), self-test 10/10, fr-lint exit 0. auval SUCCEEDED x6 on one cold rescan (triples read off `auval -a`: aufx OuDt / aumu OBls / aufx OGan / aufx OSpS / aumu OBsn / aumu OuTF, mfr OuDv); boot-all-uis 0 dead / exactly 2 late (O-Bells' gate-pinned census control). 16 path-scoped commits 3b5a35c5…d7584767, PLUGINS.md six rows AGREE with CMakeLists (O-Bassoon's stale '🚧 Stage 0 1.2.2' row corrected). Four tip-render gates derived at BOTH language sites (assertion AND walk) with the derive-or-abort control fired on each; O-Detune's direction assertion rewritten as a difference (zh shrank 13/14 tips). O-SpectralShaper's canvas caption 'WebGL not supported' had shipped UNTRANSLATED through the whole French rollout — localized to O-Comp's precedent (72 rows, not 71). O-TextureForge's 2 bundle-built overlay buttons (form-4, only visible after a 2nd state was added) repaired in CSS — bundle byte-identical, no webpack rebuild. O-Bells 196→0 undeclared-font over 3222 nodes, 26 line-height pins, 2 width FLOORS (Chinese narrower). O-Bassoon VERSION IS quoted (plan said none were) — R9 held. Scope decisions: shared scala-tuning-engine tuning panel LEFT AS FOUND (five consumers, 0 i18n hooks in ANY language; en/fr hover-help already discloses it — deferred D1); O-Bells' 2 late tips kept (EXPECTED_LATE-pinned census control, D2). Findings for wave 4d: measure-ui `line-height-normal` CANNOT reach 0 (han is computed over aria-label/data-tip — O-Tremolo reads 3 today) so the criterion is check-ui-labels 0 moved, not the screen; a termNote is ENTRY-scoped (caption + tip title need two — Z5 fires on the title); Z5 is BLIND to near-identical non-TERMS English (`WebGL 不可用` vs `不支持 WebGL`, D3); `measure`→小节 is wrong on a meter (root = musical bar, O-Gain diverges under termNote); the 'no plugin name in the batch' blinding control cannot pass when the copy names the product (O-Gain ms-enc) — `--allowed-tools ""` + prompt prohibition are the real boundary; a line-height pin must be derived PER LINE where the English already wraps; Georgia IS installed here so face-naming was 3 sites, not 69 | 2026-09-05 | d7584767 |  | [260905-rwh-wave-4c](./quick/260905-rwh-wave-4c/) |
| 260905-vp3 | Stage 4 wave 4d of the zh-Hans rollout — five plugins ship en/fr/zh-Hans: O-MultiBandCompressor 1.12.0, O-Formant 1.29.0, O-ReverseDelay 1.12.0, O-Marimba 1.14.0, O-FreqPulse 1.19.0. 633 zh rows authored at 'mt' and promoted to 'bt'; corpus 2440→3073, 22→27 plugins, BELOW SHIP BAR 0; i18n-zh-lint gate exit 0 (0 findings / 43), self-test 10/10, fr-lint exit 0, check-i18n exit 0. auval SUCCEEDED x5 on one cold sweep (triples off `auval -a`: aufx OMbc / aumu OuFm / aufx ORvD / aumu OuMa / aufx OFPu, mfr OuDv). 11 path-scoped commits 7bb7f39d…a56e2ace, PLUGINS.md five rows corrected from CMakeLists, no duplicate. Harness note: the first executor stalled on the 600s no-progress watchdog three times, each at a long command — the orchestrator built and ship-committed T1, a fresh executor ran T2/T3 with every build/gate in the background. O-MultiBandCompressor: the one-space-divergent C++ codec HAND-EDITED and proved by an explicit grep; version held in `set(OMBC_VERSION …)` — the two-arm CMake reader proven a superset (agrees 4/4 with the single-arm reader); canvas caption localized (99 rows, not 98). O-ReverseDelay: the FIFTH enumeration form (a per-language Map lookup); its clamp gate had NO path to the table — a vm loader was added before anything could derive; its frontend gate was RED on the tree as found since v1.11.0 (stale D13 assertion, inverted not deleted — the plan asserted it green without firing it). O-Formant: glottal Shimmer diverges under a termNote (微光 is the reverb sense already spent on its effects tab). O-FreqPulse: 24 form-4 controls that nine `inherit` declarations never reached; one element type needed three line-height pins. Findings for wave 4e (deferred-items.md, 18 items): fire every precondition including 'this gate passes'; check a gate has a path to the table before planning its derive; derive line-height pins from the CONTENT box per line; `auval -v` takes three UNQUOTED words (a quoted triple reads like five failures); two DIFFERENT glossary roots can collide on a page (Scatter/Diffusion) and only the reverse read sees it; the identity control catches READER faults — check returned ids identical AND in order; D1's consumer list is SIX (O-MicrotonalSampler too) | 2026-09-06 | a56e2ace |  | [260905-vp3-wave-4d-o-formant-o-multibandcompressor-](./quick/260905-vp3-wave-4d-o-formant-o-multibandcompressor-/) |
| 260906-h8y | Stage 4 wave 4e of the zh-Hans rollout — five plugins ship en/fr/zh-Hans: O-Tapestop 1.7.0, O-Lyrica 2.5.0, O-simpleBeatmaker 1.3.0, O-simpleFM 1.5.0, O-simplePhysicalModelSynth 1.3.0. 651 zh rows authored at 'mt' and promoted to 'bt'; corpus 3073→3724 of 5256, 27→32 plugins, BELOW SHIP BAR 0; i18n-zh-lint gate exit 0 (0 findings / 43), self-test 10/10, fr-lint exit 0, check-i18n exit 0, check-ui-labels 0 moved on en/fr/zh on all five, boot-all-uis 0 dead / exactly 2 late (O-Bells control). auval on one cold rescan (triples off `auval -a`: aufx OTsp / aumu OLyr / aumu OSiB / aumu OSiF / aumu OsPM, mfr OuDv): SUCCEEDED x4; O-Lyrica FAILED 9 PASS / 1 FAIL on its PRE-EXISTING Meta Param Flag assertion (`freeToggle`, documented in its own CHANGELOG since v2.4.x) — the wave changed no .cpp (`git diff 56537bce..HEAD -- 'plugins/*/Source/*.cpp'` empty). 11 path-scoped commits 1ad239b4…ff0de99a, PLUGINS.md five rows corrected from CMakeLists (all five were one patch stale), no duplicate. One FRESH executor per task — no watchdog stall this wave. O-Tapestop (tracer): the wave's only gate file carries enumeration forms 3 and 5 (no sixth); its vm loader published I18N/TIP_BINDINGS but not LANGUAGES — extended, derive-or-abort fired both ways, revert byte-identical; Engage/Stop M13 collision (reader returned 'Start' for 启动 with 停止 the Stop MODE on the same page — Stop side qualified to 停转). O-Lyrica (heavy, 213 rows, 15 states): a CLICK-FORM COVERAGE HOLE — a state's forced click landed on the footer keyboard's white key (elementFromPoint), ten scale-generator captions never measured, gate green and --verbose silent; the eval form turned the gate RED with 12 real failures, fixed. `measure-ui --report undeclared-font` is NOT the form-4 census (it counts the CJK-tail half: 177 vs a real form-4 of 39) — group computed families over visible en nodes. `select` EXCLUDED from O-Lyrica's element-level form-4 rule (page has its own equal-specificity select rule). Three quoted-variable CMake versions — the one-arm reader returns EMPTY on all three, two-arm mandatory. FIFTEEN plan predictions measured FALSE: the 0/0/0/0 pin table wrong on 4 of 5 (R8 live everywhere), every index.html/CSS line number in the font worklists off by NON-CONSTANT offsets while every count was right, inherit counts 5→6 on two, stats.get('en') 2→3 after the port the plan mandates, alternation 4→3, scala-tuning-engine grep 0→1 (a CHANGELOG line), 'all five auval SUCCEEDED'. Three D4 glossary entries opened (通过长度 read as a preposition, 分割 read as a verb by two models, 键位 read as keymap). Findings for wave 4f (deferred-items.md: N1–N13, D1–D7, 19 instructions): D1 is DUE NOW — 4f holds O-Wind/O-Reed/O-Bowed, three of the six scala-tuning-engine consumers, three options written out; the three remaining two-language gate files repo-wide are all in 4f | 2026-09-06 | ff0de99a |  | [260906-h8y-wave-4e-o-lyrica-o-simplebeatmaker-o-sim](./quick/260906-h8y-wave-4e-o-lyrica-o-simplebeatmaker-o-sim/) |
| 260906-s71 | D1 CLOSED by option 1 — the shared scala-tuning-engine tuning panel localized en/fr/zh-Hans at module v3.1.0 (37 data-i18n keys adopted from O-MicrotonalSampler's private copy, refreshPanelI18n → window.__reapplyI18n after every innerHTML injection); five path-embedding consumers ship: O-Bassoon 1.5.0 (en/fr/zh-Hans), O-Bowed 1.8.0, O-Contrabass 1.9.0, O-Reed 1.5.0, O-Wind 1.20.0 (en/fr); auval ×5 PASS in ONE cold sweep, two pre-existing warnings; check-i18n now scans CMake-embedded tuning-panel.js (CMAKE_MODULE_JS_SCOPE narrowing — the unrestricted arm exposed O-ReverseDelay 6 + O-Marimba 5); O-Contrabass's own label.loadScl was silently overwritten by the module row (caught by the 57+37≠93 arithmetic, renamed label.loadSclFile); Task 1 shipped a French geometry regression (interval-list-header) that only driving the panel found — floors per consumer at each's own English box; 17-of-37 coverage hole, 9 <option> structurally unmeasurable; nineteen plan predictions false, three verify commands not executable as spelled (awk range, zsh set -- $PV ×2). Wave 4f inherits the zh column on O-Bowed/O-Reed/O-Wind + the sized line-height 1.11 ratio block and CJK tail; O-Contrabass needs its zh column and sits in NO wave. | 2026-09-06 | 37e6b6d0 |  | [260906-s71-localize-the-shared-scala-tuning-engine-](./quick/260906-s71-localize-the-shared-scala-tuning-engine-/) |
| 260906-tyt | O-Lyrica auval FAILS on the Meta Param Flag assertion for freeToggle/scaleToggle (ParameterID 1275870432) — RESOLVED: both glissando toggles declared with AudioParameterBoolAttributes().withMeta(true); O-Lyrica 2.5.1 rebuilt + installed, `auval -v aumu OLyr OuDv` AU VALIDATION SUCCEEDED (0 FAIL, 0 Meta Param Flag lines; Global Meta count 0→2 on exactly Free Glissando / Scale-Locked Glissando). Negative control: installed 2.5.0 reproduced `* * FAIL` + AU VALIDATION FAILED before the edit. False predictions: CHANGELOG:540 / NOTES:61 called it a benign WARNING (it was a hard FAIL, both corrected); NOTES.md claim that build-and-install.sh fails for this plugin was stale (resolved OLyrica in 59s); auval's own dump names the param 335 lines above the assertion, no hash derivation needed; no 15-min cold rescan. O-Lyrica's pre-existing auval red from waves 4e is cleared | 2026-09-06 | 62f2611c |  | [260906-tyt-o-lyrica-auval-fails-on-the-meta-param-f](./quick/260906-tyt-o-lyrica-auval-fails-on-the-meta-param-f/) |
| 260906-uu7 | O-Chorus's CJK font tail was INERT (wave 4a): written AFTER the trailing generic, so under zh-Hans Chromium resolved Han through the lang-resolved `serif` (Songti SC) and never reached the PingFang SC the tail named — proven by CSS.getPlatformFontsForNode BEFORE/AFTER on all six tailed stacks (six of nine, not the five-of-eight the v1.5.0 comment claimed; `.settings-toggle` was uncounted). Tail moved before the single trailing `serif` on every stack; O-Chorus 1.6.3 built, installed, `auval -v aufx OuCh OuDv` AU VALIDATION SUCCEEDED at Component Version 1.6.3. Every pin re-measured: NO pin value changed — the zh `normal` line box is 13.00 px under both faces, and the `.settings-select` width pin the plan called most-likely-to-break CLOSED instead (zh intrinsic 64→65, endonyms now language-invariant). Movement is zh-only and width-only (knob labels 19.61→20.00, `voices` tip 2→3 body lines); check-ui-labels 87/0 with [7] 0 movers fr+zh, ui_tip_render_check 345/0, measure-ui 3/2/0/0 identical BEFORE/AFTER. Found: measure-ui's `ff` is the DECLARED stack string, not the resolved face (a Songti grep on it is vacuous); `.settings-select` and `.settings-toggle` line-height pins are inert; the three tail-less glyph stacks (◀/▶/⚙) resolve through Hiragino Mincho ProN / Lucida Grande / Menlo — DEFERRED; BSD `wc -l` pads output so `| wc -l | grep -qx '0'` verifies are false REDs | 2026-09-06 | ea1b86cc |  | [260906-uu7-o-chorus-s-cjk-font-tail-is-inert-found-](./quick/260906-uu7-o-chorus-s-cjk-font-tail-is-inert-found-/) |

## Session Continuity

Last session: 2026-09-05T02:10:09.383Z
Stopped at: Completed quick task 260904-q4j
Resume file: None

Next: `/clear` then `/gsd-execute-phase 25` to run Plan 25-03 (internal developer-reference notes — DOCS-01..DOCS-05). Phase 25 closes when 25-03 lands; v1.5 ship gate ready.

---
*v1.4 shipped 2026-03-07. v1.5 Microtonal Shared Module & Suite Propagation started 2026-04-24. Running total: 5 milestones shipped, 22 phases, 64 plans, 108 requirements. v1.5 adds 3 phases (23-25) and 33 requirements.*

**Planned Phase:** 24 (propagate) — 8 plans — 2026-04-26T05:25:14.989Z
