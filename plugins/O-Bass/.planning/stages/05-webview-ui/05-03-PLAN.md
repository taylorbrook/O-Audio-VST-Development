---
phase: 05-webview-ui
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Plugin builds successfully with ninja OBass_VST3 OBass_AU"
    - "WebView displays 4 controls: Frequency, Enhance, Output, Mode toggle"
    - "Knob movements update DSP parameters in real-time"
    - "Parameter automation from host reflects in UI immediately"
    - "UI renders at 500x450 with botanical aesthetic"
  artifacts: []
  key_links: []
---

<objective>
Build the plugin and verify the WebView UI displays correctly with functional parameter binding.

Purpose: This plan confirms all Phase 5 success criteria through a build test and human visual/functional verification in the standalone application.

Output: Verified working WebView UI with all 4 controls functional and botanical aesthetic matching Ouaricon suite.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/O-Bass/.planning/BRIEF.md
@plugins/O-Bass/.planning/ROADMAP.md
@plugins/O-Bass/.planning/STATUS.md
@plugins/O-Bass/.planning/stages/05-webview-ui/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconfigure CMake and build plugin</name>
  <files></files>
  <action>
    1. Reconfigure CMake to pick up new BinaryData sources:
       ```bash
       cd /Users/taylorbrook/Dev/VST-development/build
       cmake ..
       ```

    2. Build OBass plugin (all formats):
       ```bash
       ninja OBass_VST3 OBass_AU OBass_Standalone
       ```

    3. If build fails, analyze error messages:
       - BinaryData not found -> CMakeLists.txt issue
       - WebSliderRelay not found -> missing juce_gui_extra module
       - Parameter not found -> wrong parameter ID string

    Fix any build errors before proceeding to verification.
  </action>
  <verify>ls -la build/plugins/OBass/OBass_artefacts/Release/Standalone/OBass.app/Contents/MacOS/OBass</verify>
  <done>OBass Standalone, VST3, and AU all build successfully</done>
</task>

<task type="auto">
  <name>Task 2: Clear AU cache and install plugin</name>
  <files></files>
  <action>
    Per CLAUDE.md build requirements, clear AU cache and install fresh binaries:

    ```bash
    # Kill AudioComponentRegistrar
    killall -9 AudioComponentRegistrar 2>/dev/null || true

    # Clear AU cache
    rm -rf ~/Library/Caches/AudioUnitCache/
    rm -rf ~/Library/Caches/com.apple.audiounits.cache

    # Remove old plugin binaries
    rm -rf ~/Library/Audio/Plug-Ins/VST3/OBass.vst3
    rm -rf ~/Library/Audio/Plug-Ins/Components/OBass.component

    # Install fresh
    cp -R build/plugins/OBass/OBass_artefacts/Release/VST3/OBass.vst3 ~/Library/Audio/Plug-Ins/VST3/
    cp -R build/plugins/OBass/OBass_artefacts/Release/AU/OBass.component ~/Library/Audio/Plug-Ins/Components/
    ```

    Verify AU registration:
    ```bash
    auval -a | grep -i obass
    ```
  </action>
  <verify>auval -a | grep -i obass</verify>
  <done>OBass AU and VST3 installed and registered</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>WebView UI for O-Bass with 4 controls (Frequency, Enhance, Output, Mode) and limit indicator</what-built>
  <how-to-verify>
    1. Launch OBass standalone:
       ```bash
       open build/plugins/OBass/OBass_artefacts/Release/Standalone/OBass.app
       ```

    2. **Visual verification:**
       - [ ] Window is approximately 500x450 pixels
       - [ ] Paper texture background visible
       - [ ] Botanical illustration overlay visible (~35% opacity)
       - [ ] 2x2 grid layout with 4 controls
       - [ ] Brown/green earth tone color scheme
       - [ ] Serif font (Garamond or similar)

    3. **Control layout verification:**
       - [ ] Top row: FREQUENCY knob (left), ENHANCE knob (right)
       - [ ] Bottom row: OUTPUT knob and MODE toggle
       - [ ] Labels visible above knobs
       - [ ] Values displayed below knobs

    4. **Functional verification:**
       - [ ] Frequency knob: drag changes value (should show 40-200 Hz range)
       - [ ] Enhance knob: drag changes value (should show 0-100% range)
       - [ ] Output knob: drag changes value (should show -18 to +18 dB range)
       - [ ] Mode toggle: click switches between CLEAN and COLORED
       - [ ] Double-click any knob resets to default value

    5. **Limit indicator verification:**
       - [ ] LED element visible in UI
       - [ ] When Enhance is at maximum (100%), LED may glow during loud audio
       - [ ] LED responds smoothly (no jitter)

    6. **Parameter binding verification:**
       - Open Audio/MIDI Settings in standalone
       - Enable some audio input
       - Verify DSP responds to control changes (bass enhancement audible when Enhance > 0)

    Report any issues or type "approved" if all criteria pass.
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 5 success criteria (from ROADMAP.md):
1. [x] WebView displays 4 controls: Frequency, Enhance, Output, Mode toggle
2. [x] UI matches Ouaricon visual language (paper texture, botanical style)
3. [x] Knob movements update DSP parameters in real-time without glitches
4. [x] Parameter changes from host (automation) reflect in UI immediately
5. [x] UI is responsive and renders correctly at default plugin size
</verification>

<success_criteria>
- Plugin builds without errors (VST3, AU, Standalone)
- Standalone launches and displays WebView UI
- All 4 controls visible and functional
- Visual style matches Ouaricon botanical aesthetic
- Parameter binding works bidirectionally
- Human verification approved
</success_criteria>

<output>
After completion, create `plugins/O-Bass/.planning/stages/05-webview-ui/05-03-SUMMARY.md`
</output>
