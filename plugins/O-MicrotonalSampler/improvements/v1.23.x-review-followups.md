# O-MicrotonalSampler — v1.23.x review follow-ups

Batched `/improve` commands covering the **18 WARNING + 22 INFO** findings deferred
from the 2026-06-30 full-instrument review (`.planning/review/2026-06-30/`). The
3 CRITICALs (C1/C2/C3) already shipped in **v1.23.1**.

Grouped **one command per review domain / source file** so each is a single
coherent unit that builds + tests together. Ordered by **highest-risk item
contained** (run top-to-bottom). Each ends with the same deliverables you used for
v1.23.1 — the skill auto-bumps the patch number from whatever's current, so the
order is reorder-safe (no hard-coded versions).

Legend: **Wn** = warning, **IN** = info, from the sibling `REVIEW-*.md` files.

---

## 1 — Voice RT-safety + loop-crossfade  (`MicrotonalSamplerVoice.cpp`) — ✅ SHIPPED v1.23.2
Highest risk: a hard audio-thread **lockup** and an RT-thread **free**.

_Done 2026-06-30: W11 (hostSR guard + finite-safe wrap), W10 (`RetiredMapReaper`
message-thread SPSC ring), W9 (continuous crossfade phase), IN-01/02/03. Real-code
regression test `loop_crossfade_check` (21/21). W10 stayed localized — no MINOR split
needed._

```
/improve O-MicrotonalSampler — voice RT-safety + loop-crossfade fixes from .planning/review/2026-06-30/REVIEW-dsp-voice.md. W11: guard computePlayRateForVariant against hostSR==0 (clamp divisor to 44100) and make wrapLoopPosition finite-safe (bail to lpStart if !isfinite) — kills the +Inf-playRate non-terminating loop / hard lockup. W10: hand retired `prevMap` to a message-thread reaper (SPSC to-release queue drained on the message thread, or processor retains prior map in a bounded ring) so a reload-boundary voice-steal never drops the last SampleMap ref (frees vector<SampleCell> + audio buffers) on the audio thread — related to the shipped C1. W9: drive the 8-sample loop crossfade with a continuous phase that reaches 1.0 at the wrap point (or size the equal-power LUT [0..1]-inclusive) so sustained looped samples stop clicking once per cycle. Also fold in the DSP INFOs: settle/document the equal-power +3 dB correlated-layer bump, stop advancing the round-robin counter for skipped degenerate layers, and fix the stale "squared CC gain" comment. As a PATCH with backup + regression test + changelog.
```

## 2 — Keyswitch default absorbs low notes  (`PluginProcessor.cpp`, params) — ✅ SHIPPED v1.23.3
Product decision needed — isolate it so /improve's discuss phase surfaces the call.

_Done 2026-06-30: product call = **(a)** keyswitches opt-in (`ks_enabled` default →
false), so a fresh instance never absorbs MIDI 0–9 (W3/WR-03). Also fixed IN-04
(default KS range → 0–7, exactly `kMaxTech` slots, no 8/9 collapse) and IN-03
(vocabulary drift → all sites now reference the Dorico-aligned `stacc`/`trem`).
New `TechniqueDefaults.h` is the single source of truth (params +
resetTechniqueNames + processBlock + tests). New `ks_default_check` regression
(27/27); `state_migration_check` + `cc_pc_trigger_check` re-run green; auval PASS._

```
/improve O-MicrotonalSampler — fix the keyswitch default that silently eats low notes (review W3 / REVIEW-processor-state.md WR-03). Fresh-instance defaults are technique_count=8, ks_enabled=true, ks_low=0, ks_high=9, so processBlock absorbs every note-on in MIDI 0–9 as a keyswitch and never forwards it to the synth — any library mapped into that range loses those notes with no visible cause, and the adjacent comment claims the opposite (ks_enabled=false "reproduces v1.13.0"). Decide during discuss: (a) restore ks_enabled=false so keyswitches are opt-in, or (b) keep KS-on but gate absorption on a loaded map that actually populates technique slots and/or default the KS range outside the playable region — then correct the misleading comment either way. Also resolve the related INFOs: reconcile the default technique vocabulary that disagrees between resetTechniqueNames and the header docstrings, and the KS clamp that saturates notes 8 and 9 onto the same technique. As a PATCH with backup + regression test + changelog.
```

## 3 — Processor state race + preset bleed-through  (`PluginProcessor.cpp`, state) — ✅ SHIPPED v1.23.4
W1 is a real crash on off-message-thread saves (Reaper).

_Done 2026-07-01: W1 (WR-01) — snapshot `techniqueNames` under `persistenceLock` +
guard both setters + restore mutation; hold the lock around `captureTuningValueTree`
and in the editor's 8 tuning-WRITE native fns (reads left lock-free); new
`getPersistenceLock()` accessor. W2 (WR-02) — reset technique names /
CC / PC to defaults before applying the loaded tree (mirrors the trims pattern);
restore is now tree-deterministic, no bleed. W4 (WR-04) — memoise the 24-bit base64
WAV on `SampleVariant` (`cachedBase64Wav`); encode once, reuse every save. New
`preset_determinism_check` (15/15); state_migration / cc_pc_trigger / ks_default
re-run green; auval PASS._

```
/improve O-MicrotonalSampler — state-serialization threading + preset determinism from .planning/review/2026-06-30/REVIEW-processor-state.md. W1: captureStateValueTree reads techniqueNames (juce::StringArray) and the TuningEngine off the message thread (Reaper's getStateInformation, the codebase's own HG-08 case) WITHOUT persistenceLock — a concurrent rename/tuning edit tear-reads → crash. Snapshot techniqueNames under persistenceLock (and guard setTechniqueName/resetTechniqueNames with the same lock); snapshot or lock the tuning capture too. W2: restoreStateValueTree resets trims-before-apply but NOT techniqueNames or the CC/PC tables, so a partial/older .omspreset bleeds the previous session's values through — mirror the trims pattern (reset to resetTechniqueNames()/defaultCcMapping()/defaultPcMapping() before applying the loaded tree). W4: cache the embedded-library 24-bit WAV blobs at load time (or encode on a background step) instead of re-encoding a 250 MB library on every getStateInformation save. As a PATCH with backup + regression test + changelog.
```

## 4 — WebView C++ bridge: UAF + silent saves  (`PluginEditor.cpp`) — ✅ SHIPPED v1.23.5
W12 is a conditional use-after-free across ~9 async file-chooser callbacks.

_Done 2026-07-01: W12 (`Component::SafePointer` guard on all 9 `this`-capturing
`launchAsync` completions — bail WITHOUT calling `complete`, since JUCE's native-fn
completion is owned by the dead WebView `Impl`; verified against JUCE 8.0.9). W13
(`saveScalaFile`/`saveKBMFile`/`exportTuningHTML` now propagate `replaceWithText`'s
bool). IN-01 (removed 3 dead native fns; accessors retained). IN-02
(`reportCellLayout` resets `cellLayout`+`folderZoneRect` up-front). IN-03
(`existsAsFile()` guard on the cell-hit drop path). Hit-test geometry extracted to
`DropRouting.h`; new `drop_routing_check` (13/13). auval PASS; pluginval strictness-10
PASS incl. editor open/close cycles._

```
/improve O-MicrotonalSampler — WebView C++ bridge hardening from .planning/review/2026-06-30/REVIEW-editor-bridge.md. W12: the ~9 chooser->launchAsync completions capture raw `this` and can fire after editor teardown (the chooser is kept alive by shared_ptr but the editor/WebView are not) — conditional UAF; wrap each completion body with juce::Component::SafePointer and bail if null. W13: saveScalaFile (~1599), saveKBMFile (~1631), and exportTuningHTML (~1888) discard the replaceWithText bool and always report success — return/report the real result like saveCurrentPreset (~1756) already does. Also clean up the editor INFOs: remove the 3 dead registered native fns (getEmbeddedTuningCategories, getSkippedFiles, resetTechniqueNames), reset folderZoneRect when reportCellLayout gets an omitted payload, and add an existsAsFile() guard on the filesDropped cell-hit load path. As a PATCH with backup + regression test + changelog.
```

## 5 — Loader robustness + parser edge cases  (`SampleLoader.cpp`, `FilenameParser.cpp`) — ✅ SHIPPED v1.23.6
W5 silently aborts an entire folder load with zero feedback.

_Done 2026-07-01: WR-01 (per-file try/catch → recorded skip + `kMaxSamplesPerFile`
ceiling before allocating), WR-02 (validate `lengthInSamples` as int64 before
narrowing), WR-03/IN-05 (one cleared guard sample kills the resampler over-read;
dead `<1` clamp removed — chose padding over `floor` so no tail loss), WR-04
(RR split-form skips the note token → `take_60` no longer fabricates rr 59),
IN-01 ("N subfolder(s) ignored" note; kept flat scan), IN-02
(`oms::isAmbiguousRrGroup` flags mixed explicit/no-token + duplicate RR indices),
IN-03 (two-char `mp`/`mf` required pre-note; bare `p`/`f` post-note only). Pure
helpers extracted to `LoaderSupport.h`. New `loader_robustness_check` (46/46);
technique/merge_rr/find_cell/dynamics/state/cc_pc re-run green; auval PASS. IN-04
(lowest/highestNote in PluginProcessor.cpp) deferred; CR-01 already fixed v1.23.1._

> **NB (verified 2026-07-01):** `REVIEW-loading-parsing.md` is dated later than
> this batch doc and lists a **CR-01** (worker force-kill via 500 ms
> `stopThread`→`pthread_cancel`). That finding is **already fixed in v1.23.1** —
> the loader now does block reads with `shouldExit()` checkpoints
> (`SampleLoader.cpp:148-162, 183-188`) + a 5000 ms signal-first `stopThread`
> (`:31, 46, 69, 90`). **Do NOT re-address CR-01.** WR-01..04 + INFOs below are
> still live. Line refs below are against current (v1.23.5) source.

```
/improve O-MicrotonalSampler — loader/parser robustness from .planning/review/2026-06-30/REVIEW-loading-parsing.md (WR-01..04 + INFOs; CR-01 worker force-kill is ALREADY fixed in v1.23.1 — block reads + shouldExit() checkpoints + 5000ms signal-first stopThread, do NOT re-address). W5/WR-01: SampleLoader.cpp processOneFile allocates AudioBuffer(srcChannels, srcSamples) at :139 and make_shared<AudioBuffer>(2, outNumSamples) at :190 straight from the file header with NO upper bound and NO per-file try/catch — a corrupt/huge length throws std::bad_alloc, which JUCE's Release threadEntryPoint catch(...) silently swallows, so the worker exits mid-folder and neither completionCallback nor failureCallback fires (UI spinner hangs, every already-decoded sample discarded). Wrap the per-file body in try/catch so a throw becomes a recorded skip (outSkipReason) + continue in folder mode / callback in single-variant mode, and cap srcSamples against a kMaxSamplesPerFile ceiling before allocating. W6/WR-02: :130 `const int srcSamples = (int) reader->lengthInSamples;` truncates an int64 — a >2^31-sample or corrupt-header file wraps to a small positive (passes the :133 srcSamples<=0 guard → silent under-read) or negative (false "invalid header" reject of a real file). Validate reader->lengthInSamples as int64 against the same ceiling BEFORE narrowing. W7/WR-03: :171 outNumSamples = ceil(srcSamples / srcRatio) makes LagrangeInterpolator::process consume ~srcRatio*outNumSamples input samples, up to ~1 past sourceBuf's srcSamples on any odd-length down-convert (44.1→48 kHz) — a real 1-sample heap over-read on most SR-mismatched loads. Floor it (accept the ≤1-sample tail) or pad one guard sample; and remove the now-dead outNumSamples<1 clamp at :172-173 (IN-05 — unreachable since srcSamples>=1 and srcRatio>0). W8/WR-04: FilenameParser.cpp RR split-form (:497-522) scans all tokens without excluding the token already claimed as the note, so `take_60.wav` uses "60" as both the MIDI note AND parseAsRrIndex("take60")→rrIndex=59, fabricating a spurious explicit RR that suppresses the ambiguous-duplicate modal — require the digit token index != noteTokenIndex (and skip i==noteTokenIndex). Also the loader INFOs: IN-01 offer recursive folder enumeration (RangedDirectoryIterator recursive=true at :332) or report "N subfolder(s) ignored" in the completion payload; IN-02 flag groups that mix an explicit-RR file and a no-token file (or share a resolved RR index) as ambiguous instead of silently merging the -1 file as a variant; IN-03 gate single-letter pre-note dynamics (F-C3 → forte layer-3 false positive) behind a two-char requirement (mp/mf) or a "trust filename dynamics" toggle. Skip IN-04 (lowest/highestNote metadata lives in PluginProcessor.cpp, outside these files — note it but don't chase). As a PATCH with backup + regression test + changelog.
```

## 6 — WebView frontend polish  (`sampler-app.js`, CSS) — ✅ SHIPPED v1.23.7
Cosmetic/UX + latent boot TypeError; lowest risk, do last.

_Done 2026-07-03: W14 (readouts now computed from `SliderState.getScaledValue()`
— JUCE pushes the real range/skew via `propertiesChanged`, so NO ranges are
hard-coded in JS anymore; also caught polyphony claiming 1–32 vs real 1–16),
W15 (`.backend` guards), W16 (trigger tables build-once + in-place
activeElement-guarded updates; IN-01 KS fields folded in), W17 (new
`getParameterDefaults` native fn; dblclick → real APVTS default), W18 (wheel
gesture: open on first tick, close after 250 ms idle). INFOs: IN-02 comments,
IN-03 one-shot marker suppression, IN-04 modal-Esc `stopPropagation`, IN-05
confirm fallbacks → explicit fail-safe (error + toast + cancel). IN-06/IN-07
deferred (naming/defensive, per brief). New `ui_frontend_check.js` (node,
19/19) incl. bridge-closure guard; all 15 C++ checks green; auval PASS.
**Batch complete — all 6 items shipped.**_

```
/improve O-MicrotonalSampler — WebView frontend fixes from .planning/review/2026-06-30/REVIEW-ui-frontend.md. W14: ADSR knob readouts are wrong — KNOB_FORMATS claims attack/decay/release are 0.001–5.0 s linear but the real NormalisableRange is (0.0, 10.0, 0.001, 0.5f) (max 10 s, skew 0.5), so the label is off by up to 2×; match the format to the real range. W15: subscribeTechniqueStateUpdates / subscribeTriggerStateUpdates dereference `.backend` without the guard every other subscriber uses → latent boot-time TypeError; add the guard. W16: the CC/PC trigger tables innerHTML=''-rebuild on every state echo with no activeElement guard, clobbering in-progress edits/focus — reuse the v1.23 trim-panel reconciliation pattern. W17: knob double-click reset snaps to normalised 0.5 instead of the parameter default (moves attack to ~2.5 s) — reset to the real default. W18: wrap knob wheel edits in sliderDragStarted/Ended so hosts record them as automation. Also the frontend INFOs: fix stale octave-label / one-shot-marker comments, the modal-Esc vs loop-editor-Esc bubble collision, and the dead window.confirm fallbacks in WKWebView. As a PATCH with backup + regression test + changelog.
```

---

### Notes
- Run **`/clear` between commands** (each is a full discuss→implement→verify cycle).
- **#2 (keyswitch default)** carries a genuine product decision — expect the discuss
  phase to ask before changing the shipped default.
- **#1 W10 (message-thread reaper)** is the one non-trivial architectural item; the
  rest are localized fixes. If it balloons, split W10 into its own MINOR.
- Nothing here changes state format or parameter IDs → all PATCH, no breaking changes.
