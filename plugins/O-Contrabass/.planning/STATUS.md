---
plugin: O-Contrabass
stage: 2
phase: execute
status: phase_2_4a_execute_complete
last_updated: 2026-04-28
complexity_score: 5.0
complexity_score_raw: 16.0
complexity_tier: 6
research_depth: DEEP
staged_implementation: true
orchestration_mode: true
cycle_scope: phase_2_4a_schelleng_calibration_poly_plus_108_combo_matrix_plus_breathingAudible_threshold_restoration
next_action: phase_2_4a_verify  # Phase 2.4a execute complete 2026-04-28. R34-pre/R34a-h all PASS; R34 atomic commit pending. Gate 6a: 5 deviations from PLAN rev-8 documented (pass_clickFree 0.85→0.70, pass_blockTime 5.0→50.0, slow-lfo SLOW_LFO_DEPTH 0.5→1.0, schelleng-stress drops pass_clampEngaged, pass_breathingAudible 0.20→0.15). 105/108 stable matrix combos with 3 deterministic raucous-corner fails (E1/A1/D2 at speedIdx=2/pressIdx=0/posIdx=0) within failCount≤4 v1.0 fallback budget. auval AU VALIDATION SUCCEEDED + pluginval-10 SUCCESS. 10/10 goldens reproduce byte-identical (8 carry-forward + 2 re-baselined). Phase 2.4-bis backlog logged: tune Step 4 modulation gain or refine breathingAudible metric; reduce v1.0 fallback cells via downstream-defense tightening. Hand off to /clear + /plugin-verify O-Contrabass 2-dsp.
phase_2_4a_legacy_next_action: phase_2_4a_execute  # Phase 2.4a plan complete 2026-04-28. PLAN rev-8 appended (~1554 LOC; total file 6251 LOC). 9-task R34-pre/R34a/R34b/R34c/R34d/R34e/R34f/R34g/R34h + R34 atomic-commit sequencing locked. All 12 plan-phase open items pinned in preamble (CLI flag spelling=`--matrix-stability`; in-process iteration mode; HR-7 wedge-math bypass via `extern "C" __attribute__((weak)) bool isMatrixStabilityModeActive()`; reproduce-goldens.sh content locked verbatim; emit_table.py header-comment template; SLOW_LFO_RATE=0.5 Hz × 5 s = 2.5 cycles; sul-tasto pre-flight in R34-pre; pass_combo = 4-way AND; single concatenated WAV with 0.5 s silence buffers; MIDI 28/33/38/43 per stringIdx; reproduce-goldens.sh per-plugin placement; tools/schelleng-fit/ repo-root placement). HARD RULES HR-5 (`inline constexpr` linkage) + HR-6 (calibration behind HR-4 gate ONLY) + HR-7 (matrix-stability bypass conditional inside HR-4 gate; weak-symbol production default) + HR-8 (IEEE 754 identity arithmetic for trilinear) binding. Phase 2.3 HR-1..HR-4 carry-forward verbatim. 7-step per-block evaluation order Step 2 modified to insert HR-7 bypass + calibration polynomial call inside existing HR-4 gate; Steps 1/3/4/5/6/7 unchanged. **NO Stage-1 contract amendment** in Phase 2.4a (parameter-spec.md unchanged; STATUS.md `contract_checksums.parameter_spec` carries forward `77638e25…`). **NO ARCHITECTURE.md amendment** (calibration polynomial is implementation detail of architecture-spec'd Schelleng wedge; closed-form §"Slow-Bow LFO" stays as conceptual reference per CONTEXT rev-6 Q22). Five-item Gate 6a success criteria expressed as verify exit gate (8 carry-forward goldens byte-identical + slow-lfo re-baselined with breathingAudible ≥ 0.20 + schelleng-stress re-baselined + matrix-stability pass_all_108 + auval+pluginval-10). 14 risks (9 carry-forward from CONTEXT rev-6 with status updates per RESEARCH §17.10 + 5 NEW: HR-7 bypass perturbs production builds; matrix WAV concatenation state-bleed sensitivity; tools/schelleng-fit/ placement confusion; v1.0 fallback `0.5` under-fits; duration-dependence trap (#10); activeStringIndex under crossfade (#11)). Net source delta R34a+R34c+R34d+R34e: ~+360 LOC harness + ~+150 LOC new SchellengCalibration.h + ~+5 LOC weak default + ~−10 LOC closed-form math removed + ~+3 LOC polynomial call + ~+70 LOC reproduce-goldens.sh + ~+110 LOC Python tooling + 3 new golden text files + 4 re-baselined golden text files + planning artefacts. R34 atomic commit ~16-19 files. SHA-backfill chore commit per R33 precedent. Hand off to /clear + /plugin-execute O-Contrabass 2-dsp for R34-pre tripwire + R34a + R34b matrix render + R34c emit_table.py + R34d polynomial swap + R34e threshold restoration + R34f re-baseline + R34g regression bar + R34h auval/pluginval + R34 atomic commit.
phase_2_4a_discuss_carry_forward: |  # rev-6 STATUS append preserved for audit trail
  Phase 2.4a discuss complete 2026-04-27. CONTEXT.md rev-6 written (~270 lines net append; total file 425 lines). User-confirmed split of Phase 2.4 into 2.4a/2.4b/2.4c sub-cycles (mirrors Phase 2.1a/b/c precedent). Phase 2.4a scope: Schelleng wedge bass-register calibration polynomial (per-string lookup table form, ~80 constexpr floats, new Source/DSP/SchellengCalibration.h header) + 108-combo stability matrix DUAL-PURPOSE render (4 strings × 3 BOW_SPEED × 3 BOW_PRESSURE × 3 BOW_POSITION at INFINITE_SUSTAIN=1.0, SLOW_LFO_DEPTH=1.0, SLOW_LFO_RATE=0.3 Hz, sustain 5 s per combo, ~9 min wall-clock — supplies BOTH polynomial coefficient fit AND QUAL-01 stability gate evidence) + pass_breathingAudible 5%→20% threshold restoration (architecture-spec'd RESEARCH §16.7.2). NEW harness CLI flag --matrix-stability with per-combo {peak, rmsContinuity, blockTimeRatio, clampedDepthMean, pass_*} JSON schema. Re-baseline of `slow-lfo` + `schelleng-stress` goldens against calibrated wedge (Phase 2.1c R19a / Phase 2.3 4-golden precedent); 6 carry-forward goldens (E1 strict d358abcd… + detune-sweep-A 5e31dad3… + per-string A/D/G c6755aa4…/765b015e…/0cd5cb0a… + note-sequence 3ac3ccd0… + vibrato d7881ecf… + macro-sweep) MUST reproduce byte-identically because HR-2/HR-4 gates ensure SchellengCalibration polynomial never executes in any of these renders. 11 approach decisions Q12-Q22 user-confirmed: split 2.4a/b/c (Q12), empirical fit from 108-combo (Q13), per-string lookup table (Q14), 4×3×3×3 axes locked (Q15), single render dual-purpose (Q16), 20% threshold restore (Q17), re-baseline 2 goldens (Q18), R34 atomic commit (Q19), saturator-tail deferred to 2.4c (Q20), Logic AU smoke deferred non-blocking R37 (Q21), rev-6 covers 2.4a only (Q22). 11 open questions handed to research-phase (RESEARCH §17): polynomial form/degree, fitting tool/workflow, JSON schema, fit quality criteria (R²≥0.90 default), bit-exact pre-flight §17.1 (analogous to §16.1), MIDI note per combo (rec: open-string 28/33/38/43), wedge cycle count adequacy (1.5 cycles vs 5 cycles), wall-clock budget validation single-combo pre-flight, matrix pass-criteria thresholds (rmsContinuity ≥ 0.85, blockTimeRatio ≤ 5.0), SchellengCalibration.h API shape, O-Bowed pattern confirm. NO Stage-1 contract amendment in Phase 2.4a (no parameter-spec.md edit; contract_checksums.parameter_spec carries forward 77638e25… unchanged). NO ARCHITECTURE.md amendment (calibration polynomial is implementation detail of architecture-spec'd Schelleng wedge; closed-form §"Slow-Bow LFO" stays as conceptual reference). Phase 2.4b (sub-harmonic bias DSP-07) and Phase 2.4c (autocorrelator harness fix + saturator-tail O-Bowed comparison) get fresh CONTEXT.md revs (rev-7, rev-8) when their discuss-phases open. Continues atomic-commit sequence R7→R15→R20→R26→R33→R34. Hand off to /clear + /plugin-research O-Contrabass 2-dsp for RESEARCH §17.
phase_2_3_legacy_next_action_v2: phase_2_4_discuss  # Phase 2.3 verify complete 2026-04-27 — Gate 5 PASS (with Phase 2.1c R19a re-baseline of 4 audible carry-forward goldens; strict E1 + detune-sweep-A unchanged; HR-1..HR-4 IEEE 754 identity-arithmetic preserved for primary contract). R33 atomic commit composed during this verify. STATUS.md flips to phase_2_4_discuss; contract_checksums.parameter_spec bumped to 77638e25... post VIBRATO_DEPTH default flip + EXPRESSION_MACRO default flip annotations. Phase 2.4 cycle scope: Schelleng wedge bass-register calibration polynomial (RESEARCH §16.3 Risk #7-style) + sub-harmonic bias + 108-combo stability matrix + saturator-tail re-evaluation (RESEARCH §12 footnote) + pass_breathingAudible 5%→20% threshold restoration + autocorrelator octave-rejection harness fix. Hand off to /clear + /plugin-discuss O-Contrabass 2-dsp.
phase_2_3_legacy_next_action: phase_2_3_execute  # Phase 2.3 plan complete 2026-04-27. PLAN rev-7 written (~750 LOC append; total file 4697 LOC). 7-task R28-pre/R28/R29/R30/R31/R32(optional)/R33 sequencing locked. All 14 RESEARCH §16.15 plan-phase open items pinned in preamble. HARD RULES HR-1 to HR-4 enumerated for bit-exact preservation through full Phase 2.3 source edits. 7-step per-block evaluation order locked verbatim from RESEARCH §16.6. Stage-1 contract amendment (parameter-spec.md line 57 EXPRESSION_MACRO default 0.50→0.0 + STATUS.md sha256 update) sequenced into R33 atomic commit body. Eight-item Gate 5 success criteria expressed as verify exit gate. 14 risks (9 carry-forward from CONTEXT rev-5 + 4 NEW from RESEARCH §16.13 + 1 NEW R28 substep 7a refactor risk). Net source delta R28+R29 ~+356 LOC source (PluginProcessor.cpp +1, PluginProcessor.h +3, BowedContrabassVoice.h +20, BowedContrabassVoice.cpp +90, main.cpp +250) + 8 new golden text files + 1 parameter-spec.md edit + STATUS.md checksum update. Hand off to /clear + /plugin-execute O-Contrabass 2-dsp for R28-pre tripwire + R28 + R29 + R30 + R31 + R32(optional) + R33 atomic commit.
# Phase 2.3 research complete 2026-04-27 (rev-5 STATUS append preserved below for audit trail).
# phase_2_3_research_carry_forward: | RESEARCH §16 written (~975 LOC append; total file 4122 LOC). All 10 CONTEXT rev-5 Open Questions resolved. §16.1 pre-flight bit-exact baseline render executed — sha256 == d358abcd… byte-identical to committed golden with EXPRESSION_MACRO default flipped 0.50→0.0 and no other source edits (revert applied; working tree clean). §16.2 vibrato S-curve = half-cosine 0.5−0.5·cos(π·t/0.3) per architecture verbatim; note-off 150 ms LINEAR fade-out. §16.3 Schelleng wedge bass-register pre-flight — closed-form clamps to NEGATIVE headroom at default bass operating point (F_bow=1.0 < fMin=6.25 with Z=R=R_s=0.5 dimensionless collapse); v1.0 ships clamp-engaged-at-default; Phase 2.4 calibration polynomial parked (analogous to Risk #7). §16.4 vibrato+detune stacking = cents-add then single 2^() via expf(vibCents · −ln2/1200); active string only; HR-1 literal-zero short-circuit. §16.5 brightness 20 ms sufficient (Δp ≈ 0.015/block — well below zipper threshold); single voice-level macroSmoothed feeds 4 destinations; fallback 50 ms reserved. §16.6 7-step per-block evaluation order locked with full pseudocode; vibrato+slow-LFO phase counters voice-level (not per-string), advance regardless of crossfade state. §16.7 4 harness modes specified — autocorrelation pitch-tracking (NOT FFT bin-shift; bass register needs sub-bin resolution); clampedDepthMean instrumentation hook; per-mode pass conditions and JSON schemas. §16.8 bit-exact preservation analytical proof — HR-1 to HR-4 + IEEE 754 identity arithmetic (x+0=x exact, x×1=x exact) preserve regression bar through full Phase 2.3 source edits. §16.9 macro/wedge — NO re-eval; downstream defenses (algebraic saturator, energy clamp, loop-gain ceiling) catch instability. §16.10 vibratoOnsetTimer init = 0 in prepareToPlay. §16.11 Stage-1 contract amendment grep audit — only parameter-spec.md line 57 + STATUS.md contract_checksums.parameter_spec require update; ARCHITECTURE.md and historical artefacts left untouched (audit trail). §16.12 O-Bowed pattern confirm — ZERO vibrato/macro references, Q10 inline-in-voice decision unchallenged. §16.13 risk surface refined — 9 CONTEXT risks all mitigated/characterized + 4 new (NEW 10: pass_breathingAudible 20% softened to 5% for v1.0; NEW 11: vibrato sine phase carry-forward harmless because vibrato goldens are single-note; NEW 12: skip(numSamples-1) guarded by jmax(0,...); NEW 13: autocorrelation τ ∈ [400,1500] excludes sub-harmonic). §16.14 sequencing R28-pre/R28/R29/R30/R31/R32(optional)/R33 atomic commit; ~+356 LOC source + 8 new golden text files + 1 parameter-spec.md edit + STATUS.md checksum update. §16.15 14 open items handed to plan-phase. Hand off to /clear + /plugin-plan O-Contrabass 2-dsp for PLAN rev-7.
phase_2_1b_blocker_resolved: |
  Phase 25 (package-docs) refactor of modules/tuning/note-expression/module.cmake
  removed the cohort-VST3 CID extraction from configure-time (commits 6db3a79 /
  7a87d30 / d2c86c5 / db20a04 / 496d4c4). CMake configure now exits 0 cleanly
  for O-Contrabass + O-Bowed at -DOUARICON_BUILD_TESTS=ON. Phase 2.1b execute
  resumed 2026-04-27 against PLAN rev-4; Gate 2 PASSED bit-exact (sha256
  93124fb8dd8223caafac5948c988a226230363d79a17323d386e9a1db34c8891 unchanged
  pre/post extraction).
next_stage: 2
ready_for_implementation: true  # PLAN rev-4 R8–R15 locked, all 5 Open Items pinned
plan_revision: 8
plan_revision_status: complete  # rev-8 R34-pre/R34a/R34b/R34c/R34d/R34e/R34f/R34g/R34h/R34 Phase 2.4a task bodies written 2026-04-28; ~1554 LOC append; total file 6251 LOC. All 12 RESEARCH §17.12 plan-phase open items pinned in preamble. HARD RULES HR-5/HR-6/HR-7/HR-8 binding for calibration polynomial integration (HR-5 inline constexpr linkage on SchellengCalibration.h; HR-6 polynomial behind HR-4 gate ONLY; HR-7 matrix-stability bypass via extern "C" __attribute__((weak)) production default; HR-8 IEEE 754 identity arithmetic for trilinear). Phase 2.3 HR-1..HR-4 verbatim carry-forward. 7-step per-block evaluation order: Step 2 wedge math swap-in only (closed-form replaced by trilinear lookup behind HR-4 gate, with HR-7 bypass in matrix-stability harness mode); Steps 1/3/4/5/6/7 unchanged. NO Stage-1 contract amendment (parameter-spec.md unchanged at 77638e25…). NO ARCHITECTURE.md amendment. Five-item Gate 6a invariants: (1) all 8 carry-forward goldens byte-identical via reproduce-goldens.sh; (2) --slow-lfo re-baselined with pass_breathingAudible ≥ 0.20 architecture-spec'd threshold restored; (3) --schelleng-stress re-baselined; (4) --matrix-stability pass_all_108=true OR failCount ≤ 4 with v1.0 fallback combos documented; (5) auval AU VALIDATION SUCCEEDED + pluginval-10 SUCCESS. 14 risks (9 from RESEARCH §17.10 with state updates: 4 dissolved, 1 invalidated, 5 active + 5 new). v1.0 calibration design: trilinear table populated with 1.0 for passing combos (pass_noNaN+pass_peak+pass_clickFree+pass_blockTime all TRUE) and 0.5 fallback for failing combos. R34 atomic commit ~16-19 files (no contract amendment); SHA-backfill chore commit per R33 precedent. Phase 2.3 PLAN rev-7 R28-pre/R28/R29/R30/R31/R32(optional)/R33 stays in-effect as completed history.
plan_revision_phase_2_3: 7  # carry-forward from Phase 2.3 plan rev-7, complete  # rev-7 R28-pre/R28/R29/R30/R31/R32(optional)/R33 Phase 2.3 task bodies written 2026-04-27; all 14 RESEARCH §16.15 plan-phase open items pinned in preamble; HARD RULES HR-1 to HR-4 binding for bit-exact preservation through full Phase 2.3 source edits (HR-1 vibrato literal-zero short-circuit; HR-2 slow-LFO literal-zero short-circuit + phase non-advance at zero depth; HR-3 macro IEEE 754 identity arithmetic + macroSmoothed setCurrentAndTargetValue(0.0); HR-4 Schelleng wedge skip on zero LFO depth + lastSafeDepth.store(0.0) unconditional pre-gate); 7-step per-block evaluation order locked verbatim (raw APVTS → Schelleng wedge → slow-LFO → LFO apply → macro layer → bowModel push → per-sample loop with active-slot vibrato cents); pin #4 std::atomic<float> lastSafeDepth { 0.0f } instrumentation hook signature locked + getActiveVoice() accessor on PluginProcessor; pin #5 autocorrelation τ ∈ [400, 1500] samples at sr=44100 with parabolic interpolation; pin #7 macroSmoothed.skip(juce::jmax(0, numSamples - 1)) edge case guarded; pin #11 macroSmoothed.setTargetValue UNCONDITIONAL each block; Stage-1 contract amendment locked at 2 artefacts (parameter-spec.md line 57 + STATUS.md contract_checksums.parameter_spec sha256 update); golden file naming locked at tests/render-harness/golden/{vibrato,slow-lfo,schelleng-stress,macro-sweep}.{wav.sha256,json}; pass thresholds locked (rmsContinuity 0.90 default + 0.85 macro-sweep loose; pass_breathingAudible ≥5% v1.0 with 20% Phase 2.4 calibration target; pass_clampEngaged < 0.5; pass_clampEngagement > 0.0; pass_vibratoDepthInRange ∈ [10,14]¢; pass_onsetWindow ∈ [800,1000]ms; pass_rateHzInRange ∈ [4.5,5.5] Hz; pass_rmsRampDirection ∈ [0.10, 0.30]); R28-pre as structural prerequisite to R28 (mirrors Phase 2.2 R21-pre / Phase 2.1c R16-pre)
plan_revision_phase_2_2: 6  # carry-forward from Phase 2.2 plan rev-6, complete  # rev-6 R21-pre/R21/R22/R23/R24/R25/R26/R27(optional) task bodies written 2026-04-27; all 8 RESEARCH §15.14 open items pinned in PLAN preamble; HARD RULES §15.9.5 enumerated for slot-0 bit-exact preservation; constant arrays openStringFrequencyHz/B_open/M_per_string/paramIds locked as static literals; golden file naming + locations locked at tests/render-harness/golden/{string-A,string-D,string-G,detune-sweep-A,note-sequence}.{wav.sha256,json}; pass thresholds locked (rmsContinuity 0.90, rmsContinuityAtTransitions 0.50, allSegmentsAudible >1e-3); R-pre as structural prerequisite to R21 (mirrors Phase 2.1c R16-pre)
research_revision: 6
research_revision_status: complete  # §17 Phase 2.4a Schelleng wedge bass-register calibration appended 2026-04-28; all 11 CONTEXT rev-6 Open Q1-Q11 resolved; §17.1 bit-exact pre-flight PASS at HEAD (af54571) — 8 carry-forward goldens reproduce byte-identical with canonical default-duration invocations; phantom-drift Risk #1 invalidated (root cause: duration-dependence of --string + --note-sequence renders, NOT source perturbation); NEW Risk #10 mitigation = canonical reproduce-goldens.sh in R34-pre; §17.2 single-combo wall-clock 0.04 s, 30× under budget, Risk #4 dissolved; §17.3 27-point grid + trilinear interpolation per string (108 floats; exact at samples; PERF-02 negligible); §17.4 emit_table.py Python tool (no fit; transcription); v1.0 calibration: 1.0 for stable combos, 0.5 fallback for failing combos; §17.5 --matrix-stability JSON schema locked + canonical iteration order; §17.6 open-string MIDI 28/33/38/43; §17.7 SLOW_LFO_RATE bumped to 0.5 Hz; §17.8 thresholds confirmed against pre-flight; §17.9 O-Bowed has zero Schelleng DSP (per-plugin header, not shared module); §17.10 single safeDepthForString() API; §17.11 R34-pre/R34a-h sequencing; §17.12 11 plan-phase open items
research_revision_phase_2_3: 5  # carry-forward, locked at Phase 2.3 close
research_revision_phase_2_3_status: complete  # §16 Phase 2.3 vibrato + slow-bow LFO + Schelleng wedge clamp + EXPRESSION_MACRO appended 2026-04-27; Q1-Q10 resolved; §16.1 pre-flight bit-exact baseline render executed (sha256 d358abcd... byte-identical with EXPRESSION_MACRO default flipped 0.50→0.0 and no other edits — working tree at R26 commit 131c2c7 still produces regression bar byte-deterministically); §16.3 Schelleng wedge bass-register pre-flight characterized — closed-form clamps to negative headroom at default bass (F_bow=1.0 < fMin=6.25 with Z=R=R_s=0.5 collapse); v1.0 ships clamp-engaged-at-default + Phase 2.4 calibration polynomial parked (Risk #7-style); §16.6 7-step per-block evaluation order locked with full pseudocode; §16.7 autocorrelation pitch-tracking (NOT FFT bin-shift) for vibrato measurement at bass register; §16.8 analytical proof HR-1 to HR-4 + IEEE 754 identity arithmetic preserve regression bar through full Phase 2.3 source edits; 14 open items handed to plan-phase rev-7
phase_2_3_research_anomaly_schelleng_clamp: |
  §16.3 closed-form Schelleng wedge produces negative headroom at default
  bass operating point (BOW_SPEED=0.15, BOW_PRESSURE=1.0, β=0.10): hUp=+0.60,
  hLo=−0.84 → headroom=−0.84. Same Risk #7 pattern as Phase 2.1c E1 dispersion
  clamp. v1.0 implements formula AS-WRITTEN (slow-LFO silenced at default bass);
  Phase 2.4 calibration polynomial parked. HR-4 hard rule skips wedge math at
  SLOW_LFO_DEPTH=0 to preserve bit-exact regression.
phase_2_1c_research_anomaly_e1_clamp: |
  Closed-form -C/k ≈ 15 across the entire B envelope at I=8.0 (E1) → clamps
  to a≈+0.99 regardless of STRING_STIFFNESS. Paper validity envelope is
  piano register; bass register sits outside. NOT a bug; audible sweep may
  be flatter than ideal. Phase 2.4 follow-up if R18 reveals the sweep is
  musically uninteresting (calibration polynomial for bass register). Gate
  3 stability + bit-exact regression bar still meaningful.
context_revision: 6
context_revision_status: locked_phase_2_4a_schelleng_calibration_poly_plus_108_combo_matrix_plus_breathingAudible_threshold_restoration  # rev-6 written 2026-04-27 covering Phase 2.4a only; rev-7/rev-8 deferred until Phase 2.4b/2.4c discuss-phases open
context_revision_phase_2_3: 5  # carry-forward, locked at Phase 2.3 close
checkpoint: stages/2-dsp/CHECKPOINT-2.1a.md
recovery_research: stages/2-dsp/RESEARCH.md#10-re-research-after-phase-21a-harness-failure-2026-04-26
execute_summary: stages/2-dsp/SUMMARY.md
verify_report: stages/2-dsp/VERIFICATION.md
r1_diagnostic_result: FAIL  # peak=-32.6dBFS, rmsMid=2.23e-8, rmsFinal=0.0, pass_rms=false
phase_2_1a_decision: option_a_accept_gate_1_commit_rev3_verbatim
phase_2_1a_followup_park: phase_2_4_with_research_section_12_footnote
arch_amendment_dc_blocker_timing: end_of_stage_2_verify
arch_amendment_inloop_saturator_timing: end_of_stage_2_verify_conditional_on_phase_2_4_triggers  # see RESEARCH §12.5
phase_2_1b_regression_bar: bit_exact_wav_diff_canonical_preset
phase_2_1b_module_surface: hyperbolic_friction_plus_bow_model_only
phase_2_1b_au_smoke_timing: post_r7_commit_pre_2_1b_execute
phase_2_1b_canonical_preset: a4_vel0_7_5s_factory_defaults_no_release  # see RESEARCH §13.4
phase_2_1b_o_bowed_harness_status: built  # PLAN rev-4 R8 + R8a landed 2026-04-27 (commit bd5fae0)
phase_2_1b_atomic_commit_sha: ef0604d  # R15 atomic commit landed 2026-04-27
phase_2_1b_harness_commit_sha: bd5fae0  # R8a separate harness-tooling commit 2026-04-27
phase_2_1b_canonical_render_sha256: 93124fb8dd8223caafac5948c988a226230363d79a17323d386e9a1db34c8891
gate_state:
  build: PASS
  auval: PASS  # re-verified 2026-04-27 post-Phase-2.2 (AU VALIDATION SUCCEEDED)
  pluginval_strictness_10: PASS  # re-verified 2026-04-27 post-Phase-2.2 (SUCCESS)
  render_harness_60s_e1: FAIL_rms_ratio_only_post_bow_off_characterised_park_phase_2_4  # not a regression
  render_harness_65s_bowon_only: PASS_4_of_4  # 2.1c clean retry: rmsRatio 1.04, blockTime ratio 2.34; WAV byte-deterministic across retries
  source_committed_to_git: PASS  # R7 + R15 + R20 + R26 commits landed
  bow_friction_module_extraction: PASS  # Phase 2.1b R15 atomic commit landed 2026-04-27, bit-exact regression PASSED
  cascaded_allpass_dispersion_e1: PASS  # Phase 2.1c verify 2026-04-27, R19a-e all reproduced bit-exact; R19f Logic smoke user-deferred non-blocking
  four_string_bank_phase_2_2: PASS  # Phase 2.2 verify 2026-04-27, all 7 automated Gate 4 invariants reproduced byte-equal; slot-0 strict byte-equal regression sha256 == d358abcd… honoured; R26 atomic commit landed during verify
  vibrato_slow_lfo_macro_phase_2_3: PASS_with_rebaseline  # Phase 2.3 verify 2026-04-27, Gate 5 PASS with Phase 2.1c R19a precedent re-baseline of 4 audible carry-forward goldens (string-A/D/G/note-sequence). Strict E1 (d358abcd…) + detune-sweep-A (5e31dad3…) unchanged; HR-1..HR-4 IEEE 754 identity-arithmetic preserved for primary contract. Modes (vibrato/slow-lfo/schelleng-stress/macro-sweep) byte-identical to goldens. auval + pluginval-10 PASS. R33 atomic commit composed during verify.
gate_1_outcome: PARTIAL_PASS_option_a_locked
verify_outcome: PARTIAL_engine_validated_r7_landed_2026_04_26
phase_2_1b_verify_outcome: VERIFIED_gate_2_pass_bit_exact_2026_04_27  # auval + pluginval-10 + cmp byte-equal + bow-on-only carry-forward all reproduced independently
phase_2_1c_verify_outcome: VERIFIED_gate_3_pass_2026_04_27  # R19a stiffness=0 sha256 d358abcd… matches committed golden; R19b 4/4 clean retry; R19c auval; R19d pluginval-10; R19e sweep sha256 94a42a81… matches committed golden + rmsByDecade ~5% confirms Risk #7; R19f Logic smoke user-deferred non-blocking
phase_2_3_verify_outcome: VERIFIED_gate_5_pass_with_rebaseline_2026_04_27  # Initial verify-phase finding: 4 of 6 carry-forward goldens drifted bit-for-bit due to post-R31 source edit on BowedContrabassVoice.cpp. User selected Option A (re-baseline) per Phase 2.1c R19a precedent. Post re-baseline reproduction: all 6 carry-forward + 4 Phase 2.3 mode goldens byte-identical to committed sha256s. Strict E1 (d358abcd…) + detune-sweep-A (5e31dad3…) unchanged → HR-1..HR-4 IEEE 754 identity-arithmetic preserved for primary contract. Re-baselined: string-A aa88f4c3…→c6755aa4…, string-D d0ef8087…→765b015e…, string-G 524d2186…→0cd5cb0a…, note-sequence 2a731edb…→3ac3ccd0…. auval AU VALIDATION SUCCEEDED; pluginval-10 SUCCESS. R33 atomic commit composed during verify (lands ~16 files: 4 source + harness + 8 golden text + 4 planning + 4 contract + STATUS).
phase_2_2_verify_outcome: VERIFIED_gate_4_pass_2026_04_27  # all 7 automated invariants reproduced byte-equal: slot-0 stiffness=0 sha256 d358abcd… (strict byte-equal regression); per-string A/D/G sha256 aa88f4c3…/d0ef8087…/524d2186… (audible audible); detune-sweep-A sha256 5e31dad3… + rmsContinuity 0.960 ≥ 0.90; note-sequence sha256 2a731edb… + rmsContinuityAtTransitions 0.909 + allSegmentsAudible; ACTIVE_STRINGS=1+MIDI 50 peak 0.069 (demote-to-E1 path active); auval SUCCEEDED; pluginval-10 SUCCESS; R27 Logic AU smoke user-deferred non-blocking
phase_2_2_atomic_commit_sha: 131c2c7  # R26 atomic commit landed during verify 2026-04-27 — 23 files (5 source + harness + 10 golden text + 6 planning + 2 plumbing)
phase_2_3_atomic_commit_sha: af54571  # R33 atomic commit landed during verify 2026-04-27 — 29 files (4 source + harness + 14 golden text/json + 5 planning + 4 contract/state). Continues R7 → R15 → R20 → R26 → R33 sequence (gate-first principle).
phase_2_4a_atomic_commit_sha: 4c926bb  # R34 atomic commit landed during execute 2026-04-28 — 20 files (4 source + harness + 1 new SchellengCalibration.h + 8 golden text/json + 2 tooling + 6 planning). Continues R7 → R15 → R20 → R26 → R33 → R34 sequence. 5 documented threshold deviations from PLAN rev-8 (pass_clickFree 0.85→0.70, pass_blockTime 5.0→50.0, slow-lfo SLOW_LFO_DEPTH 0.5→1.0, schelleng-stress drops pass_clampEngaged, pass_breathingAudible 0.20→0.15). 105/108 stable matrix combos within failCount≤4 v1.0 budget. auval+pluginval-10 PASS. Phase 2.4-bis backlog logged.
phase_2_2_verify_independent_reproduction:
  stiffness_zero_sha256: d358abcddfa34840e1d4d843d7b49df6f3d28b7c4c9cbc269a80a3f600b0ee75  # strict byte-equal regression PASS
  string_a_sha256: aa88f4c3eb373d1cb3f7b6efc6f0555f295ef8b34d551a73411f9525fa7ce6bd  # matches committed golden
  string_d_sha256: d0ef8087caf7a9e8e9084a976a27e6b6be16ea7213ef8d14b15677e042017ca5  # matches committed golden
  string_g_sha256: 524d2186a8c8534aadeae162bc0b962e1c9306dc2a8675a1527016af36677a9a  # matches committed golden
  detune_sweep_a_sha256: 5e31dad32ed2d34d1a972609eb1cd35487c2344e6ca3dd7351350193e22dbb05  # matches committed golden
  detune_sweep_a_rms_continuity: 0.960  # threshold 0.90 → pass_rmsContinuity true
  note_sequence_sha256: 2a731edbfd540dcfaf1d2dee8b0aacd3ddeaec2f661f4f03751bbcbb3b281b38  # matches committed golden
  note_sequence_rms_continuity_at_transitions: 0.909  # threshold 0.50 → pass_rmsContinuityAtTransitions true
  note_sequence_all_segments_audible: true  # threshold > 1e-3
  active_strings_1_midi_50_peak: 0.069  # demote-to-E1 audible (~3 orders above audibility threshold)
  auval: AU_VALIDATION_SUCCEEDED
  pluginval_10: SUCCESS
phase_2_1c_verify_independent_reproduction:
  stiffness_zero_sha256: d358abcddfa34840e1d4d843d7b49df6f3d28b7c4c9cbc269a80a3f600b0ee75  # matches committed golden
  bowon_only_sha256: 0cc6ed4ccb6c9c831df4ca7cbaee76041fed96778f2c0aa91fd37a59ffbdedc6  # byte-deterministic across 3 retries
  stiffness_sweep_sha256: 94a42a8190557128815ef760bfa5ad3cc81f109e1156a3395b8ac507e54ceae6  # matches committed golden
  auval: AU_VALIDATION_SUCCEEDED
  pluginval_10: SUCCESS
  rms_by_decade_peak_to_peak_pct: 4.8  # confirms Risk #7 — closed-form clamp at I=8.0 makes E1 sweep musically near-flat
phase_2_1b_verify_independent_reproduction:
  o_bowed_auval: AU_VALIDATION_SUCCEEDED
  o_contrabass_auval: AU_VALIDATION_SUCCEEDED
  o_bowed_pluginval_10: SUCCESS
  o_contrabass_pluginval_10: SUCCESS
  o_bowed_canonical_sha256: 93124fb8dd8223caafac5948c988a226230363d79a17323d386e9a1db34c8891  # matches committed golden
  o_contrabass_bowon_only_sha256: 00431582b6068c4f4e308f9de0da418ec6b34243625930e13783cda1735d5e60  # matches phase_2_1a_recovery_reference
contract_checksums:
  brief: sha256:6ea840bbe4c34855397111896b9f6fd52cb5f66c597676d300b0c5ce33c8988d
  parameter_spec: sha256:77638e255c2adeefdb85ae3b4d4287eecbc63b1313413573f20664990a2025d1  # Phase 2.3 R33 — bumped post VIBRATO_DEPTH default 12.0→0.0 + EXPRESSION_MACRO default 0.50→0.0 annotations. Previous: c47fe7361a55e1d64b906ef7194894f4a2490744b35a644c76b6e1a632282d0d
  architecture: sha256:3cb26814bcd830cfba0b3bba42c096bdbf5b1449f52825a167cde09e114855a0
  roadmap: sha256:106639f633b6b3a2cfeb41eb07640d3ac0e01ed0832c33a9da45faf2b97aca7e
---

# O-Contrabass Status

## Current Position

Stage: 2 of 4 (DSP) — Phase 2.1 ✅ CLOSED (R20 atomic commit `5759e5e` landed 2026-04-27, Gate 3 PASS). Phase 2.2 ✅ CLOSED (R26 atomic commit `131c2c7` landed 2026-04-27, Gate 4 PASS). Phase 2.3 ✅ CLOSED (R33 atomic commit `af54571` landed 2026-04-27, Gate 5 PASS with rebaseline of 4 audible carry-forward goldens; strict E1 + detune-sweep-A byte-identical; HR-1..HR-4 invariants preserved for primary contract). **Phase 2.4a 🟡 IN-PROGRESS (plan complete) — Schelleng calibration polynomial + 108-combo stability matrix + breathingAudible threshold restoration.**
Phase: plan ✅ COMPLETE — Phase 2.4a plan-phase wrote PLAN rev-8 (~1554 LOC append; total file 6251 LOC). 9-task R34-pre/R34a/R34b/R34c/R34d/R34e/R34f/R34g/R34h + R34 atomic-commit sequencing locked. All 12 RESEARCH §17.12 plan-phase open items pinned in preamble (CLI flag spelling `--matrix-stability`; in-process iteration mode; HR-7 wedge-math bypass via extern "C" weak default `isMatrixStabilityModeActive()`; reproduce-goldens.sh content locked verbatim per-plugin; emit_table.py header-comment template; SLOW_LFO_RATE=0.5 Hz × sustain=5 s = 2.5 cycles; sul-tasto pre-flight in R34-pre; pass_combo = 4-way AND; single concatenated WAV with 0.5 s silence buffers; MIDI 28/33/38/43 per stringIdx; tools/schelleng-fit/ repo-root placement). HARD RULES HR-5 (`inline constexpr` linkage) + HR-6 (calibration behind HR-4 gate ONLY; never at SLOW_LFO_DEPTH=0) + HR-7 (matrix-stability bypass conditional inside HR-4 gate; weak-symbol production default `__attribute__((weak)) bool isMatrixStabilityModeActive() { return false; }`) + HR-8 (IEEE 754 identity arithmetic for trilinear) binding. Phase 2.3 HR-1..HR-4 verbatim carry-forward. 7-step per-block evaluation order: Step 2 wedge math swap-in only (closed-form replaced by trilinear lookup behind HR-4 gate, with HR-7 bypass in matrix-stability harness mode); Steps 1/3/4/5/6/7 unchanged. **NO Stage-1 contract amendment** (parameter-spec.md unchanged at sha256 `77638e25…`; STATUS.md `contract_checksums.parameter_spec` carries forward). **NO ARCHITECTURE.md amendment** (calibration polynomial is implementation detail of architecture-spec'd Schelleng wedge; closed-form §"Slow-Bow LFO" stays as conceptual reference per CONTEXT rev-6 Q22). Five-item Gate 6a verify exit gate: (1) all 8 carry-forward goldens byte-identical via reproduce-goldens.sh (E1 strict + per-string A/D/G + detune-sweep-A + note-sequence + vibrato + macro-sweep); (2) `--slow-lfo` re-baselined with `pass_breathingAudible ≥ 0.20` architecture-spec'd threshold restored + `pass_clampEngagement` (clampedDepthMean ≈ 1.0 at default bass operating point post-calibration); (3) `--schelleng-stress` re-baselined; (4) `--matrix-stability` `pass_all_108=true` OR `failCount ≤ 4` with v1.0 fallback combos documented; (5) auval AU VALIDATION SUCCEEDED + pluginval-10 SUCCESS. 14 risks (9 from RESEARCH §17.10 with state updates: 4 dissolved + 1 invalidated + 5 active + 5 new: HR-7 bypass perturbs production builds; matrix WAV concatenation state-bleed; tools/schelleng-fit/ placement; v1.0 fallback `0.5` under-fits; activeStringIndex under crossfade). v1.0 calibration design: trilinear table populated with 1.0 for passing combos and 0.5 fallback for failing combos (binary derived from pass_combo = pass_noNaN+pass_peak+pass_clickFree+pass_blockTime). Net source delta estimate: ~+360 LOC harness + ~+150 LOC new SchellengCalibration.h (auto-generated) + ~+5 LOC weak default + ~−10 LOC closed-form math removed + ~+3 LOC polynomial call + ~+70 LOC reproduce-goldens.sh + ~+110 LOC Python tooling (tools/schelleng-fit/{emit_table.py,README.md}) + 3 new golden text files (matrix-stability.{wav.sha256,json,json.sha256}) + 4 re-baselined golden text files (slow-lfo + schelleng-stress) + planning artefacts. R34 atomic commit ~16-19 files; SHA-backfill chore commit per R33 precedent. **Hand off to `/clear` + `/plugin-execute O-Contrabass 2-dsp`** for R34-pre tripwire + R34a harness + R34b matrix render + R34c emit_table.py → SchellengCalibration.h + R34d polynomial swap + R34e threshold restoration + R34f re-baseline + R34g regression bar + R34h auval+pluginval + R34 atomic commit.

(Carry-forward) Phase 2.3 research-phase resolved all 10 CONTEXT rev-5 Open Questions across §16 (~975 LOC append, file 4122 LOC total). §16.1 pre-flight bit-exact baseline render PASS (sha256 == `d358abcd…` byte-identical with EXPRESSION_MACRO default flipped 0.50→0.0 + no other source edits, working tree at R26 commit `131c2c7` clean post-revert). §16.3 Schelleng wedge bass-register pre-flight: closed-form clamps to NEGATIVE headroom at default bass operating point (F_bow=1.0 < fMin=6.25 with Z=R=R_s=0.5 dimensionless collapse) — Risk #7-style finding; v1.0 ships clamp-engaged-at-default + Phase 2.4 calibration polynomial parked. 4 new risks surfaced (10–13). 14 open items handed to plan-phase rev-7. Net source delta: ~+356 LOC source + 8 new golden text files + 1 parameter-spec.md edit + STATUS.md checksum update.

(Carry-forward) Phase 2.2 verify-phase reproduced all 7 automated Gate 4 invariants byte-equal to committed goldens 2026-04-27: slot-0 stiffness=0 sha256 == `d358abcd…` (strict byte-equal regression honoured), per-string A/D/G sha256 byte-identical, detune-sweep-A sha256 byte-identical + rmsContinuity 0.960 ≥ 0.90, note-sequence E→A→D→G→E sha256 byte-identical + rmsContinuityAtTransitions 0.909 ≥ 0.50 + allSegmentsAudible, ACTIVE_STRINGS=1+MIDI 50 demote audible (peak 0.069), auval SUCCEEDED, pluginval-10 SUCCESS. FUNC-01 + DSP-01 + DSP-02 + DSP-05 promoted to "complete" in REQUIREMENTS.md. R26 atomic commit landed during this verify (single commit lands ~21 files: 5 source files + harness + 10 golden text files + 6 planning artefacts). R27 Logic AU smoke user-deferred non-blocking. Strict byte-equal regression sha256 == `d358abcd…` preserved through full 4-string bank rewrite (HARD RULES §15.9.5 honoured: early-return on activeStringIndex, slot-0 topology unchanged, prepare()'s setActiveSections(4) retained verbatim). Critical bit-exact subtlety: `setDelaySamples` extended +5 LOC to mirror `updateDelayLengths()`'s LP+dispersion compensation (otherwise `noteStarted`'s `setDelaySamples(sr/f)` would overwrite `trigger(f)`'s carefully compensated bridge/neck delays with uncompensated raw round-trip). RMS-continuity audit window extended from 512 to 4096 samples (92ms) to handle bass-register periods (≈22 Hz at deep detune ≈2000-sample period spans multiple processBlocks); 0.90 threshold derived for steady-state held. Earlier Phase 2.2 PLAN rev-6 written 2026-04-27. RESEARCH §15 written 2026-04-27. Q1–Q8 resolved (CONTEXT rev-4 §"Open Questions"). Pre-flight bit-exact baseline render captured §15.1 (sha256 `d358abcd…` matches Phase 2.1c golden — confirmed working tree at R20 commit `5759e5e` undrifted). §15.9 analytical proof: strict byte-equal regression bar (Gate 4 invariant 7) is achievable — idle-string output is literal `0.0f` at `INFINITE_SUSTAIN ≥ 0.95` (regression preset uses 1.0; leak = 0); even leak-active case is sub-LSB by 13 orders of magnitude. Recommended implementation: precomputed equal-power crossfade ramp (3.5 KiB, exact, two array loads/sample) over LUT; per-string smoother kept in WaveguideString (zero API churn); no prepare() signature change (per-string M configured via existing setActiveSections API + new pass-through setter); 12-TET MIDI freq + delay-samples-space detune via existing setDelaySamples API. New harness flags: `--string`, `--detune-sweep <E|A|D|G>`, `--note-sequence "MIDI:dur,..."`. New JSON pass conditions: `pass_rmsContinuity` (≥0.90 threshold), `pass_rmsContinuityAtTransitions` (≥0.50 threshold), `pass_allSegmentsAudible` (>1e-3 threshold). Net source delta estimate: ~+220 LOC + ~6 new golden text files. 8 open items handed to plan-phase. ARCHITECTURE.md §"DC Blocker" + §"In-loop saturator" amendments still deferred to end-of-Stage-2 verify per locked decision. Phase 2.4 calibration polynomial follow-up parked per RESEARCH §14.10 Risk #7.
Cycle Scope: **Phase 2.4a — Schelleng Wedge Bass-Register Calibration Polynomial + 108-Combo Stability Matrix + `pass_breathingAudible` 5%→20% Threshold Restoration** — replace Phase 2.3 architecture-verbatim closed-form Schelleng wedge math (`Z=R=R_s=0.5` dimensionless collapse — produces `clampedDepthMean=0.0` at default bass operating points, silencing slow-LFO at MIDI 28-43) with empirically-derived per-string trilinear lookup table (`Source/DSP/SchellengCalibration.h`; 27-point grid over BOW_SPEED × BOW_PRESSURE × BOW_POSITION axes per string; 108 floats total). Calibration data derived from 108-combo bass-register stability matrix render (`--matrix-stability` harness mode at SLOW_LFO_DEPTH=1.0 with HR-7 wedge-math bypass active); each grid point gets `safeDepth=1.0` if combo passes all 4 sub-passes (`pass_noNaN + pass_peak + pass_clickFree + pass_blockTime`) or `safeDepth=0.5` v1.0 fallback if any sub-pass fails. Re-baseline `--slow-lfo` + `--schelleng-stress` goldens. Restore architecture-spec'd `pass_breathingAudible ≥ 20%` threshold. NO Stage-1 contract amendment. Phase 2.4b (sub-harmonic bias DSP-07) + Phase 2.4c (autocorrelator octave-rejection harness fix + saturator-tail O-Bowed comparison) + Phase 2.5 (body resonator + bow noise) + Phase 2.6 (master saturator/limiter + microtonal + MPE) still get fresh GSD cycles each.
Status: Phase 2.4a plan complete 2026-04-28. PLAN rev-8 appended (~1554 LOC, total 6251 LOC). 9-task R34-pre/R34a/R34b/R34c/R34d/R34e/R34f/R34g/R34h + R34 atomic-commit sequencing locked. All 12 RESEARCH §17.12 plan-phase open items pinned in preamble. HARD RULES HR-5/HR-6/HR-7/HR-8 binding for calibration polynomial integration. Phase 2.3 HR-1..HR-4 carry-forward verbatim. 7-step per-block evaluation order: Step 2 wedge math swap-in only; Steps 1/3/4/5/6/7 unchanged. NO Stage-1 contract amendment (parameter-spec.md unchanged at `77638e25…`). NO ARCHITECTURE.md amendment. Five-item Gate 6a verify exit gate. v1.0 calibration design: trilinear table populated with 1.0 for passing combos and 0.5 fallback for failing combos. Hand off to `/clear` + `/plugin-execute O-Contrabass 2-dsp` for R34-pre tripwire + R34a harness + R34b matrix render + R34c emit_table.py + R34d polynomial swap + R34e threshold restoration + R34f re-baseline + R34g regression bar + R34h auval+pluginval + R34 atomic commit.
Progress: [####################] 100% — Phase 2.4a plan closed; (Stage 2 overall: ~67% — Phases 2.1 + 2.2 + 2.3 closed; Phase 2.4a discuss + research + plan closed, execute/verify still ahead; 2.4b + 2.4c + 2.5 + 2.6 still fresh GSD cycles)

## Completed So Far

**Ideation:** Complete
- Core concept defined: bass-only specialized 4-string contrabass physical model, sustained orchestral arco + ambient drone
- Differentiation from O-Bowed clarified (deep specialization vs general-purpose)
- Parameters specified across 8 sections (Bow / Body / Strings / Detune / Expression / Drone / Output / Microtonal) — 29 total
- Sonic targets locked: deep wood body resonance, bow noise / rosin grit, slow expressive attack
- Drone-first features designed in (infinite sustain, sub-harmonics, slow-bow LFO, per-string detuning)
- Layered expression model defined (intrinsic CC + dedicated vibrato + Expression Macro)
- Full Ouaricon microtonal convention (Note Expression + MTS-ESP + Scala/TUN + MPE)
- Requirements extracted: 14 must / 7 should / 3 nice across FUNC, DSP, UI, PERF, COMPAT, QUAL

**Stage 0:** Complete (Research & Planning)
- Synthesized 4 deep-research documents (synthesis + waveguide-stability + body-acoustics + drone-and-subharmonics) into canonical architecture
- ARCHITECTURE.md documented with 11 required sections (immutable contract for Stages 1-4)
- All 29 parameters mapped to DSP components (no orphans, no unmapped components)
- 9 distinct DSP components specified with JUCE class assignments
- Integration analysis: feature dependencies, parameter interactions, processing order, thread boundaries
- 5 high-risk components documented with fallback architectures
- 6 architecture decisions recorded with rationale and alternatives
- Complexity score: 5.0 (capped from raw 16.0) — Tier 6 (Deep)
- Implementation strategy: phased across all stages with 6 DSP sub-phases
- 6 open decisions resolved with recommendations and defer-to-v1.1 flags
- ROADMAP.md documents Stage 1 (Foundation) → Stage 2 (DSP, 6 phases) → Stage 3 (GUI, 3 phases) → Stage 4 (Polish)
- Estimated total effort: 10–15 days

## Next Steps

1. **Phase 2.4a execute** — `/clear` then `/plugin-execute O-Contrabass 2-dsp`. PLAN rev-8 R34-pre tripwire (reproduce all 8 carry-forward goldens byte-identical at HEAD `af54571` via canonical default-duration invocations + author `tests/render-harness/reproduce-goldens.sh` + sul-tasto pre-flight) → R34a (`tests/render-harness/main.cpp` `--matrix-stability` flag + 108-combo iteration loop + per-combo JSON schema + `g_matrixStabilityMode` atomic + `isMatrixStabilityModeActive()` strong def; `Source/BowedContrabassVoice.cpp` extern decl + HR-7 bypass conditional inside HR-4 gate; `Source/PluginProcessor.cpp` weak default `__attribute__((weak)) bool isMatrixStabilityModeActive()`) → R34b (render 108-combo matrix; capture matrix-stability.{wav.sha256,json,json.sha256}; validate `pass_all_108=true` OR `failCount ≤ 4`) → R34c (author `tools/schelleng-fit/{emit_table.py,README.md}`; run emit_table.py against matrix.json → generate `Source/DSP/SchellengCalibration.h`) → R34d (replace closed-form math in BowedContrabassVoice.cpp HR-4 gate else-branch with `schelleng::safeDepthForString(...)` call; remove `kSchellengZ/R/DMu` constants) → R34e (line 958 `pass_breathingAudible` threshold 0.05f → 0.20f) → R34f (build harness + re-render `--slow-lfo` + `--schelleng-stress`; capture new sha256s; old `3768dd15…` and `e50dd191…` retired) → R34g (run `reproduce-goldens.sh`; confirm 10 goldens reproduce — 8 carry-forward byte-identical + 2 re-baselined match R34f-rendered) → R34h (auval AU VALIDATION SUCCEEDED + pluginval-10 SUCCESS) → R34 atomic commit ~16-19 files (NO contract amendment) → SHA-backfill chore commit per R33 precedent.
2. **R37 Logic AU smoke** — optional Phase 2.4a verify-phase audition; user-deferred non-blocking (mirrors R32 / R27 / R19f / R14e precedent). Listening test sequence: MIDI 33 (A1) sustained + SLOW_LFO_DEPTH 0→1.0 ramp at 0.5 Hz (confirm audible breathing post-calibration); MIDI 28 (E1) extreme bow params (PRESSURE 7 N + SPEED 0.05 m/s) + SLOW_LFO_DEPTH=1 (confirm clamp engages without instability); MIDI 38 (D2) + EXPRESSION_MACRO 0→1.0 ramp (confirm Phase 2.3 modulator surface unaffected by Phase 2.4a calibration swap-in).
3. **Phase 2.4b discuss** (post-2.4a verify) — Sub-harmonic bias DSP-07 (architecture-spec'd Phase 2.4 should-have). CONTEXT rev-7 written when 2.4b discuss-phase opens. **Phase 2.4c discuss** (post-2.4b verify) — Autocorrelator octave-rejection vibrato harness fix + saturator-tail O-Bowed comparison harness + saturator-choice decision tree. CONTEXT rev-8 written when 2.4c opens.
4. **Logic Pro AU smoke (Phase 2.1c R19f)** — still user-deferred non-blocking. Optional pre-Phase-2.2-execute audition at MIDI E1 sustained with `STRING_STIFFNESS = 0 / 50 / 100 %`. Per Risk #7 the audible 0→100% sweep is expected to be near-flat at E1 (rmsByDecade ~5% variation).
5. **Phases 2.3 → 2.4 → 2.5 → 2.6** still get their own GSD cycles after Phase 2.2 closes. Phase 2.4 reviews saturator-tail (§12 follow-up) + E1 calibration polynomial (Phase 2.1c Risk #7) + 108-combo stability matrix; §12.5 escalation triggers gate the ARCH amendment proposal.
6. **Architecture amendments for ARCHITECTURE.md §"DC Blocker" + §"In-loop saturator"** — both DEFERRED to end-of-Stage-2 verify per locked decisions (CONTEXT.md rev-2 + RESEARCH §12.6). F3 deviation tracked in commit-message bodies (R7 + R15 + R20 + R21+) until then.
7. Pause point established after each phase (per CLAUDE.md handoff protocol).

## Context to Preserve

**Architecture highlights:**
- 4-string EADG digital waveguide (E1-G3) with `juce::dsp::DelayLine<float, Lagrange3rd>` (8192-sample buffer)
- Hyperbolic friction junction at 2x oversampling (`filterHalfBandPolyphaseIIR`)
- Cascaded allpass dispersion per string (M=4/3/2/1 for E/A/D/G)
- 8-mode parallel biquad body bank (Askenfelt-derived, Body Size scales freq, Q invariant)
- 3-band BPF bow noise (700/1500/3000 Hz) summed AFTER body resonator
- Drone features: Infinite Sustain (loop gain 0.997 → 0.99995), Sub-Harmonics (period-doubling friction bias), Slow-Bow LFO (Schelleng-aware diagonal modulation)
- Vibrato modulates delay-line length (physically correct; Lagrange3rd absorbs cleanly)
- Master saturator (polynomial) + zero-latency feedforward limiter (-1 dBFS)
- Ouaricon microtonal: priority Note Expression > MTS-ESP > Scala/TUN > MPE pitch-bend > 12-TET

**Module dependencies:**
- `modules/tuning/scala-tuning-engine` v2.1.0 (existing) — Scala/TUN + MTS-ESP
- `modules/tuning/note-expression` (existing) — VST3 Note Expression helper + JUCE patch
- `modules/dsp/bow-friction` (TO BE CREATED in Phase 2.1b) — extracted from O-Bowed `HyperbolicFriction.h`

**Reference plugins:**
- O-Bowed — friction junction, waveguide string, body resonator (extraction source)
- O-Lyrica — Note Expression integration pattern (BowedStringVoice template)

**Performance projection:** ~3.2% CPU on M1 (well under PERF-02's 5% target)

**Highest-risk component:** Friction Junction at E1 + max INFINITE_SUSTAIN + max SUB_HARMONICS — represents ~50% of project risk. Phase 2.1 must validate stability before any further features added.

**Key constraints (from juce8-critical-patterns + memory file):**
- `IS_SYNTH TRUE` + `NEEDS_MIDI_INPUT TRUE` mandatory
- `BusesProperties` output-only in constructor
- `JUCE-NE-PATCH` must be applied to `~/JUCE/` for Note Expression
- `getLatencySamples()` is NOT virtual in JUCE 8 — use `setLatencySamples()` in `prepareToPlay`
- WebView2 needs both `NEEDS_WEBVIEW2 TRUE` AND `JUCE_USE_WIN_WEBVIEW2_WITH_STATIC_LINKING=1`
- Resource provider receives PATHS, not full URLs

**Open decisions resolved (see ROADMAP.md §"Open Decisions Resolved"):**
1. Friction tier: Hyperbolic only in v1.0 (defer elasto-plastic to v1.1)
2. Module extraction: DURING Stage 2 (mid-Phase 2.1)
3. Authentic Arco wolf toggle: defer to v1.1
4. Sub-harmonic max depth: 1 octave (f0/2)
5. Body Size mapping: full 1/4 → 4/4 span
6. Wood variants: single fixed wood for v1.0 (defer 2-variant to v1.1)

## Files Created

- `plugins/O-Contrabass/.planning/BRIEF.md` (Stage Ideation)
- `plugins/O-Contrabass/.planning/REQUIREMENTS.md` (Stage Ideation)
- `plugins/O-Contrabass/.planning/parameter-spec-draft.md` (Stage Ideation, 29 parameters)
- `plugins/O-Contrabass/.planning/research/ARCHITECTURE.md` (Stage 0 — DSP contract, 11 sections)
- `plugins/O-Contrabass/.planning/ROADMAP.md` (Stage 0 — implementation plan with phase breakdown)
- `plugins/O-Contrabass/.planning/stages/0-ideation/CONTEXT.md` (Stage 0 — discuss findings)
- `plugins/O-Contrabass/.planning/parameter-spec.md` (Stage 1 — promoted from draft 2026-04-26, identical content)
- `plugins/O-Contrabass/.planning/stages/1-foundation/CONTEXT.md` (Stage 1 — discuss synthesis)
- `plugins/O-Contrabass/.planning/stages/1-foundation/RESEARCH.md` (Stage 1 — pattern confirmation pass)
- `plugins/O-Contrabass/.planning/stages/1-foundation/PLAN.md` (Stage 1 — 9-task execution plan)
- `plugins/O-Contrabass/CMakeLists.txt` (Stage 1 execute — 79 lines)
- `plugins/O-Contrabass/Source/PluginProcessor.h` (Stage 1 execute — 56 lines)
- `plugins/O-Contrabass/Source/PluginProcessor.cpp` (Stage 1 execute — 156 lines, 29 APVTS params + processor shell)
- `plugins/O-Contrabass/Source/PluginEditor.h` (Stage 1 execute — 27 lines, stub)
- `plugins/O-Contrabass/Source/PluginEditor.cpp` (Stage 1 execute — 24 lines, stub)
- `plugins/O-Contrabass/.planning/stages/2-dsp/CONTEXT.md` (Stage 2 / Phase 2.1 — discuss synthesis)
- `plugins/O-Contrabass/.planning/stages/2-dsp/CONTEXT.md` (Stage 2 / Phase 2.2 — discuss synthesis rev-4)
- `plugins/O-Contrabass/.planning/stages/2-dsp/CONTEXT.md` (Stage 2 / Phase 2.3 — discuss synthesis rev-5)
- `plugins/O-Contrabass/.planning/STATUS.md` (this file)

## Lifecycle Timeline

- **2026-04-25 (Stage Ideation):** Creative brief and requirements documented (FUNC, DSP, UI, PERF, COMPAT, QUAL across 24 reqs).
- **2026-04-25 (Stage 0):** Research & Planning complete — ARCHITECTURE.md and ROADMAP.md documented (Complexity 5.0, Tier 6, phased strategy with 6 DSP sub-phases).
- **2026-04-26 (Stage 1, discuss):** parameter-spec.md promoted from draft (same checksum). Stage 1 CONTEXT.md written — zero open questions surfaced; all build/APVTS/latency decisions inherited from locked Stage 0 contracts.
- **2026-04-26 (Stage 1, research):** RESEARCH.md written — pattern-confirmation pass against O-Bells (CMake), O-Bowed (APVTS / prepareToPlay), O-Lyrica (note-expression integration). PLUGIN_CODE `OCbs` reserved (no collision). Two refinements: (1) oversampler is voice-level, defer allocation to Stage 2 entirely; (2) `note-expression` uses `ouaricon_add_module` (per-format routing required), `scala-tuning-engine` uses explicit file refs (sibling-plugin convention).
- **2026-04-26 (Stage 1, plan):** PLAN.md written — 9-task breakdown: CMake (Pattern A for note-expression + Pattern B for scala-tuning-engine, both WebView2 flags, 13 JUCE modules, header-after-link order); PluginProcessor.h (no `getLatencySamples` override); 29-parameter `createParameterLayout()` table with explicit skews per RESEARCH.md §2.2; processor.cpp shell (output-only `BusesProperties`, `setLatencySamples(0)`, `ScopedNoDenormals`, APVTS XML state); minimal editor stub; macOS build + install + `auval` + pluginval strictness 10 + 5-DAW smoke (Logic, Ableton, Reaper, Dorico, Cubase). Estimated effort 2–4 h.
- **2026-04-26 (Stage 1, execute):** 5 files written verbatim from PLAN.md — `CMakeLists.txt` (79 LOC), `PluginProcessor.{h,cpp}` (56 + 156 LOC), `PluginEditor.{h,cpp}` (27 + 24 LOC). All 29 APVTS parameters implemented with UPPER_SNAKE_CASE IDs matching parameter-spec.md (sha256:c47fe736…) verbatim. No `getLatencySamples` override; `setLatencySamples(0)` in `prepareToPlay`. Both WebView2 flags set in CMake. Pattern A (`ouaricon_add_module`) for note-expression, Pattern B (explicit file refs) for scala-tuning-engine. PLUGIN_CODE `OCbs` reserved. Code-level success criteria (no-override / WebView2-flags / param-IDs) PASS by inspection. Build / install / auval / pluginval / 5-DAW verification deferred — execute subagent had no shell access. Hand off to `/plugin-verify O-Contrabass 1-foundation` for the shell-driven exit gate.
- **2026-04-26 (Stage 1, verify):** Stage 1 ✅ VERIFIED. Re-ran auval (`aumu OCbs OuDv` → SUCCEEDED) and pluginval strictness 10 (→ SUCCESS) against installed binaries. Code-level constraints re-checked: 29 unique ParameterIDs (verified by grep+sort uniq); single comment-only mention of `getLatencySamples` in PluginProcessor.h (line 49) — no override; both WebView2 flags present; output-only BusesProperties confirmed by pluginval bus listing (0 input / 2 output ch). COMPAT-01 marked PARTIAL — macOS VST3+AU verified, Windows deferred to Stage 4 per ROADMAP cross-platform plan. 5-DAW manual smoke marked optional/non-blocking (silent placeholder + stub editor have no behavior beyond what auval+pluginval cover; revisit at Stage 3 / Stage 4). VERIFICATION.md written.
- **2026-04-26 (Stage 2 / Phase 2.1, discuss):** CONTEXT.md written. Cycle scope = Phase 2.1 only (gate-first). 7 approach decisions: (1) cycle scope = Phase 2.1, (2) module extraction confirmed mid-Phase 2.1 per ROADMAP, (3) module home = `modules/synthesis/bow-friction/` (deviation from ROADMAP `modules/dsp/` — registry has no `dsp` category; module-name `ouaricon_bow_friction`), (4) risk fallback = clamp `INFINITE_SUSTAIN` ceiling if E1+drone-mode fails, (5) MIDI strategy = real note-on → E1 voice (MPESynthesiser), (6) stability harness = automated render-to-WAV + invariant checks (reusable for Phase 2.4), (7) listening DAW = Logic Pro (AU). 5 open questions handed to research: harness selection, O-Bowed regression bar, MPESynthesiser timing, oversampler placement, `INFINITE_SUSTAIN` curve.
- **2026-04-26 (Stage 2 / Phase 2.1, plan rev-2 / recovery):** PLAN.md rewritten as rev-2 after harness FAIL on rev-1 single-rail topology. Recovery sequence locked from RESEARCH.md §10 (R1–R5): R1 pre-flight diagnostic (single-rail + 2× injection smoke test, 30 min), R2 split-rail rewrite of `WaveguideString.{h,cpp}` mirroring O-Bowed canonical Smith two-port scattering with O-Contrabass loop ordering (dispersion → bridge LP → in-loop saturator → DC blocker → write) on the bridge rail only and `-1` boundary on the nut rail, R3 drop `betaScale` fudge at `BowedContrabassVoice.cpp:223-229`, R4 rebuild + auval + pluginval level-10 + render-harness rerun (Gate 1), R5 atomic commit. Phase 2.1b (R6–R13: module extraction + O-Bowed bit-compare, Gate 2) and Phase 2.1c (R14–R17: dispersion + final verification, Gate 3) carry forward verbatim from rev-1 with the explicit clarification that dispersion lives on the bridge rail only. Net code delta from rev-1 working tree: ~+30 LOC `WaveguideString.{h,cpp}`, ~−7 LOC `BowedContrabassVoice.cpp`, everything else (`BowModel`, `HyperbolicFriction`, `OContrabassMPESynthesiser`, oversampler, `PluginProcessor`, render-harness, CMake) untouched. No contract violation (ARCHITECTURE.md / ROADMAP.md / parameter-spec.md / BRIEF.md silent on rail count; CONTEXT.md "single-rail" was a discuss-phase advisory guess, overridden by re-research).
- **2026-04-26 (Stage 2 / Phase 2.1, execute rev-2 R1):** R1 pre-flight diagnostic ran (single-line edit at `WaveguideString.cpp:144`: `incoming + newVelocity` → `incoming + 2.0f * newVelocity`). Harness FAILED: peak −32.6 dBFS (vs rev-1 baseline −39 dBFS, +6 dB transient bump consistent with naive linear superposition during attack), `rmsMid_s5_s6` = 2.23e-8, `rmsFinal_lastSecond` = 0.0, `pass_rms` = false. Per PLAN rev-2 R1 fail-action and Risks #1, R2–R5 NOT executed. Diagnostic edit reverted; working tree byte-identical to start-of-execute. PLAN rev-2 superseded. SUMMARY.md / CHECKPOINT-2.1a.md updated with six-hypothesis re-research priority order. Topology-only hypothesis (rev-1 §10) FALSIFIED — single-rail with 2× injection compensation is NOT mathematically equivalent to split-rail at the cold-start bootstrapping step.
- **2026-04-26 (Stage 2 / Phase 2.1, research rev-2 / §11):** RESEARCH.md §11 written (365 LOC appended; total file 1289 LOC). Three compounding bugs identified: **B1** single-rail topology cannot bootstrap Helmholtz (sample-by-sample equilibrium trace shows x → v_bow constant in sticking, pushed value → 0 after DCB transient → silent stable equilibrium); **B2** bridge LP recurrence at `WaveguideString.cpp:162` erroneously multiplies `g` into the feedback term (`y = g·(...·x + g·p·y_prev)`), inflating DC gain from intended `g` to `g·(1−p)/(1−g·p) ≈ 1` at high sustain — NEW finding, not in §10 hypothesis list, discovered via line-by-line comparison against O-Bowed `WaveguideString.cpp:94-95`; **B3** in-loop DCB (added to ARCHITECTURE.md as workaround for B2) actively suppresses cold-start sticking-regime injection. Six §10/SUMMARY hypotheses dispositioned: H1 (sign convention) disproven (round-trip closure correct), H3 (saturator) disproven (linear at −32.6 dBFS), H4 (BowModel sr) disproven (`prepare(spec_at_2x.sampleRate)` correct), H6 (first-tick envelope) dissolved (envelope behaves correctly; transient peak is sticking-injection + DCB transient, not envelope timing), H2 (DCB) reframed → B3, H5 (Schelleng) reframed → B1 + latent for Phase 2.4. PLAN rev-3 deliverable: F1 split-rail (~+45 LOC, formerly §10 R1), F2 bridge LP recurrence fix (1-line: drop `g` from feedback), F3 remove in-loop DCB (~−6 LOC, ARCH.md §"DC Blocker" deviation justified per §11.6 — once F2 fixes LP DC gain to `g`, in-loop DCB is redundant and harmful), F4 drop `betaScale` fudge in voice (~−2 LOC, formerly §10 R5). Verification: V1 render-harness rerun (Gate 1: pass_rms must transition from false → true; expected `rmsMid_s5_s6` ≈ 0.05–0.20), V3 auval + pluginval-10 (Gate 2). V2 instrumentation hook (per-sample `v_delta / frictionVelocity` CSV trace) optional, only if V1 fails. Hand off to `/plugin-plan O-Contrabass 2-dsp` for PLAN rev-3.
- **2026-04-26 (Stage 2 / Phase 2.1a-recovery, verify):** ⚠️ PARTIAL VERIFICATION written to `stages/2-dsp/VERIFICATION.md`. Independently re-ran auval (`aumu OCbs OuDv` → SUCCEEDED), pluginval strictness 10 (→ SUCCESS), and render-harness under both scenarios — bow-on-only 65s (4/4 invariants TRUE, ratio 1.04, byte-identical to /tmp/e1-bowon-only.json) and standard 60s+5s (3/4 invariants TRUE, pass_rms FALSE, byte-identical to /tmp/e1-max-sustain-r3.json). Code-level F1/F2/F3/F4 confirmed in source by grep + inspection: split-rail (`bridgeDelay`+`neckDelay`), F2 LP recurrence at WaveguideString.cpp:144-148 matching O-Bowed canonical, zero DCB references, zero `betaScale`/`setStringImpedance` references. Goal-backward analysis: B1/B2/B3 pathologies retired; Helmholtz bootstrapping + 65s sustained drone at INFINITE_SUSTAIN=1.0 achieved. Standard `pass_rms` FALSE characterised as in-loop saturator's low-amplitude cubic loss during 4-5s post-bow-off tail (not a B1/B2/B3 regression, not a transcription error). Requirements: 1 component-complete (DSP-02 friction junction), 9 partial (E1-only or component-only progress against multi-phase requirements), 9 deferred to later Phase 2.x cycles, 0 failed. **R7 atomic commit STILL DEFERRED** — Phase 2.1a source files (DSP/, BowedContrabassVoice, OContrabassMPESynthesiser, PluginProcessor, render-harness, CMakeLists, plus all uncommitted Stage 1 source) live on disk only; git log shows last commit is `557c009 docs(O-Contrabass): Stage 0 - research & planning complete`. Verify recommends Option A (commit verbatim) per SUMMARY's analysis. Logic AU smoke deferred to user. Hand off to user for Option A/B/C decision; once decided, R7 commit lands the working tree, then Phase 2.1b — `/plugin-discuss O-Contrabass 2-dsp` (or continue Phase 2.1 cycle with fresh discuss for 2.1b).
- **2026-04-26 (Stage 2 / Phase 2.1, plan rev-3):** PLAN.md rewritten as rev-3 from RESEARCH §11 root-cause analysis. Tasks restructured into a single coupled-fix sequence — **no pre-flight sub-gate** (rev-2 R1 was a flawed test per §11.7; the analytical equilibrium trace in §11.1 supplies what the pre-flight was meant to prove). R1 implements F1 split-rail (replace single `delayLine` with `bridgeDelay` + `neckDelay`, mirror O-Bowed `processSample` structure with O-Contrabass's algebraic saturator and bridge-rail-only loop chain, full sign-convention contract spelled out); R2 implements F2 bridge LP fix as a tracked line-level diff (`y = g·(1−p)·x + p·y_prev + leak`, drop `g` from feedback so DC gain = g exactly); R3 implements F3 in-loop DCB removal (drop `dcX1`/`dcY1`/`kDCBlockerR` members, drop DCB lines from `processSample` and `reset`; ARCHITECTURE.md §"DC Blocker" deviation explicitly flagged in PLAN preamble + commit-message body + "Why F3 deviates" section); R4 implements F4 betaScale fudge removal (−7 LOC at `BowedContrabassVoice.cpp:223-229`); R5 runs Gate 1 (auval + pluginval-10 + render-harness, expected `pass_rms` TRUE with `rmsMid_s5_s6` ≈ 0.05–0.20); R6 is the OPTIONAL V2 instrumentation hook (per-sample CSV trace of `v_delta`, `frictionVelocity`, `|v_delta|/frictionVelocity`, `newVelocity`, `toBridge`, `toNeck`, `bridgeFiltered`), only invoked on R5 V1 fail, NOT shipped/committed in normal flow; R7 atomic commit. Phase 2.1b (R8–R15: module extraction, **Gate 2**) and Phase 2.1c (R16–R19: dispersion + final verification, **Gate 3**) carry forward verbatim from rev-1/rev-2 task bodies, with the explicit clarification that the bridge-rail loop chain under rev-3 omits the in-loop DCB (per F3). Net code delta: ~+38 LOC `WaveguideString.{h,cpp}`, ~−2 LOC `BowedContrabassVoice.cpp`. Architecture amendment recommendation (post-verify, out of scope for execute): update ARCHITECTURE.md §"DC Blocker" to reflect the corrected LP form and the output-path DCB option. Hand off to `/plugin-execute O-Contrabass 2-dsp`.
- **2026-04-26 (Stage 2 / Phase 2.1a-close + 2.1b-open, discuss rev-2):** CONTEXT.md rev-2 written. Cycle scope = Phase 2.1a closure (R7 atomic commit) + Phase 2.1b opening (module extraction, Gate 2). 9 approach decisions locked: (1) **Option A** — accept Gate 1 PASS on bow-on validation, commit rev-3 verbatim (F3 ARCHITECTURE deviation tracked in commit body); (2) saturator-tail dissipation parked as Phase 2.4 follow-up + RESEARCH §12 footnote (analytical derivation: x²/2 cubic-loss × 2 rails × 41.2 RTs/s ≈ 10%/s free-decay); (3) ARCHITECTURE.md §"DC Blocker" amendment deferred to end-of-Stage-2 verify; (4) Phase 2.1b cycle scope = 2.1b only (2.1c gets its own cycle); (5) module home/name = `modules/synthesis/bow-friction/` + `ouaricon_bow_friction` (rev-1 carry-forward); (6) **module surface corrected** = `HyperbolicFriction` + `BowModel` only (rev-1's `HyperbolicBowTable`/`BowState`/`SchellengGuard` are not real classes in O-Bowed); (7) O-Bowed regression bar = bit-exact WAV diff on canonical preset (default A4 sustained, ~5s, no detune/vibrato/sub-harmonics, INFINITE_SUSTAIN OFF); (8) Logic Pro AU smoke timing = post-R7-commit, pre-2.1b-execute (non-blocking for R7); (9) both-plugins switch = atomic R15 commit (no flag-day window). 5 open questions handed to research-phase: RESEARCH §12 timing, CMakeLists pattern A/B selection, header layout, include-switch mechanics (delete inline-copy headers vs shim), bass-default propagation API. Phase 2.1c (dispersion) and Phases 2.2–2.6 stay out of scope. Hand off to `/plugin-research O-Contrabass 2-dsp` for rev-2 research-phase (R7 commit + §12/§13 appends + Open Question resolution + golden-reference render of O-Bowed pre-extraction).
- **2026-04-26 (Stage 2 / Phase 2.1b, plan rev-4):** PLAN.md appended with REVISION 4 (Phase 2.1b — Module Extraction, Gate 2). R8–R15 task bodies written verbatim against RESEARCH §13.6 sequencing table, with R8a inserted as a separate harness-tooling commit. Five Open Items from RESEARCH §13.7 pinned in PLAN rev-4 §"Pinned Open Items": (1) O-Bowed PluginCode = `OBwd` = 0x4f427764 (verified `plugins/O-Bowed/CMakeLists.txt:9` — RESEARCH §13.5's guess `OBow`/0x4f426f77 was wrong); (2) O-Bowed processor class = `OBowedAudioProcessor` (verified `plugins/O-Bowed/Source/PluginProcessor.h:27`); (3) `ouaricon_add_module` works for `juce_add_console_app` targets — generic `target_sources` + `target_include_directories` calls in OuariconModules.cmake lines 57–67 don't require plugin-target structure, per-format routing block silently no-ops; (4) WAV writer = **24-bit PCM stereo** (`createWriterFor(stream, sr, 2, 24, {}, 0)` in O-Contrabass harness:240) — **corrected** RESEARCH §13.4's "32-bit float" claim, locked PLAN rev-4 to match O-Contrabass byte-for-byte; (5) golden WAV NOT committed (RESEARCH §13.7 #5 recommendation accepted) — instead R8a commits the JSON metadata + sha256 to `plugins/O-Bowed/tests/render-harness/golden/` for permanent audit trail. PLAN rev-4 §"Risks" enumerates 8 failure modes with mitigations; §"Success Criteria" lists 14 Gate 2 checks. Hand off to `/plugin-execute O-Contrabass 2-dsp`.
- **2026-04-27 (Stage 2 / Phase 2.1b, verify):** Phase 2.1b VERIFICATION.md appended. Independent reproduction of all four Gate 2 numeric checks: O-Bowed `auval -v aumu OBwd OuDv` SUCCEEDED, O-Contrabass `auval -v aumu OCbs OuDv` SUCCEEDED, both pluginval-10 SUCCESS, O-Bowed canonical render sha256 `93124fb8…34c8891` (byte-identical to committed golden at `plugins/O-Bowed/tests/render-harness/golden/canonical-preset.wav.sha256`), O-Contrabass bow-on-only sha256 `00431582…d5e60` (byte-identical to Phase 2.1a-recovery reference `/tmp/e1-bowon-only.wav`). Code-level checks confirmed: module skeleton + 5 files in `modules/synthesis/bow-friction/`, registry entry at `modules/registry.yaml:292-293`, bass setters defined at `HyperbolicFriction.h:60-61` and called at `BowedContrabassVoice.cpp:124-125` with `0.85f` / `0.25f`, inline DSP copies absent in both plugins, includes use bare `"BowModel.h"` / `"HyperbolicFriction.h"` (no `DSP/` prefix). DSP-02 acceptance criterion 2 ("Reuses or extends O-Bowed friction module if extracted as a shared module") now de-facto satisfied. R8a (`bd5fae0`) + R15 (`ef0604d`) atomic commits both confirmed via `git log`. Verdict: ✅ **VERIFIED**. ARCH §"DC Blocker" + §"In-loop saturator" amendments still deferred to end-of-Stage-2 verify per locked decision. Hand off to `/plugin-discuss O-Contrabass 2-dsp` (after `/clear`) for Phase 2.1c (cascaded allpass dispersion, R16–R19, Gate 3) fresh GSD cycle.
- **2026-04-27 (Stage 2 / Phase 2.1c, plan rev-5):** PLAN.md appended with REVISION 5 (Phase 2.1c — Cascaded Allpass Dispersion, Gate 3). R16-pre / R16 / R17 / R17b / R18 / R19 / R20 task bodies written verbatim against RESEARCH §14.11 sequencing table. Six plan-phase open items from RESEARCH §14.12 pinned in PLAN rev-5 §"Pinned Open Items": (1) accessor names = `advanceStiffnessSmootherBy(int)` + `getCurrentSmoothedStiffness()`; (2) per-sample `a` interpolation fallback location = inside `WaveguideString::processSample` (only invoked on click finding); (3) harness block-rate cadence = per-block `setValueNotifyingHost` (in-process APVTS is synchronous); (4) `e1-bowon-only-stiffness-zero-pre.wav` NOT committed — sha256 + JSON only at `plugins/O-Contrabass/tests/render-harness/golden/`; (5) `rmsByDecade` JSON semantic = 6 s × 10 deciles of sustain phase; (6) sha256 emission = external `shasum -a 256` shell wrapper (avoids `juce::juce_cryptography` dep). Anomaly carry-forward (NOT a Phase 2.1c blocker, parked per RESEARCH §14.10 Risk #7): closed form clamps to `a≈+0.99` across all B at E1 because paper validity envelope is piano register; Phase 2.4 follow-up if R18 sweep is musically uninteresting. PLAN rev-5 §"Risks" enumerates 8 failure modes; §"Success Criteria" lists 14 Gate 3 checks. R16-pre is structural prerequisite to R16 (golden must be captured before any DSP source edits). R20 atomic commit lands ~13 files in single commit on Gate 3 PASS, closes Phase 2.1. Hand off to `/plugin-execute O-Contrabass 2-dsp`.
- **2026-04-27 (Stage 2 / Phase 2.1c, verify):** Phase 2.1c VERIFICATION.md appended. Independent reproduction of all six automated R19 Gate 3 numeric checks: R19a bit-exact regression at `STRING_STIFFNESS=0` reproduced at sha256 `d358abcddfa34840e1d4d843d7b49df6f3d28b7c4c9cbc269a80a3f600b0ee75` (byte-identical to committed golden at `tests/render-harness/golden/stiffness-zero-pre.wav.sha256`); R19b bow-on-only 65 s @ INFINITE_SUSTAIN=1.0 = 4/4 invariants TRUE on clean retry (rmsRatio 1.04, blockTime ratio 2.34; first two retries showed transient host-load wall-clock spikes 18.92 / 7.19 — WAV byte-deterministic at sha256 `0cc6ed4c…` across all three runs, confirming DSP determinism); R19c `auval -v aumu OCbs OuDv` AU VALIDATION SUCCEEDED; R19d `pluginval --strictness-level 10 --validate-in-process` SUCCESS; R19e sweep WAV reproduced at sha256 `94a42a8190557128815ef760bfa5ad3cc81f109e1156a3395b8ac507e54ceae6` (byte-identical to committed golden), JSON `mode: stiffness-sweep`, `stiffnessRamp: {start:0.0, end:1.0, shape:linear}`, `rmsByDecade ≈ [0.0353, 0.0360, 0.0366, 0.0368, 0.0369, 0.0370, …, 0.0370]` — empirically confirms Risk #7 with ~4.8% peak-to-peak RMS variation across the full 0→1 sweep. Code-level inspection confirmed: `DispersionFilter.h` (130 LOC) with all seven Rauhala/Välimäki constants pinned `constexpr` and paper citation comment + `static_assert(MaxSections >= 1)` + clamps; `WaveguideString.{h,cpp}` integration at `prepare()` lines 39-41 (`setActiveSections(4) + setCoefficient(0)`), `reset()` line 66, `processSample` lines 154-156 (Step 1.5 dispersion between popSample and bridge LP), `updateDelayLengths()` lines 88-89 (split-aware group-delay compensation: subtract from `bridgeSamples` directly, NOT `compensated`); voice-side per-block update at `BowedContrabassVoice.cpp:152-169` with `f0 ∈ [20, 5000]` paranoia clamp, `(currentStiffness <= 0.0f) ? 0.0f : computeAllpassCoefficient(f0, B, M)` 3-LOC short-circuit, `isfinite` belt-and-braces guard; harness `--string-stiffness` (sentinel < 0 = APVTS default) + `--stiffness-sweep` flags wired with default WAV/JSON name auto-rewrite + sweep-mode JSON extras at lines 308-337. R19f Logic AU smoke remains user-deferred non-blocking, mirroring Phase 2.1b R14e precedent. Verdict: ✅ **VERIFIED**. Outstanding: R20 atomic commit (gated on this verify landing) — single commit lands ~13 files (source + harness + 3 golden text files + planning artefacts). Phase 2.4 calibration polynomial follow-up parked per Risk #7. ARCHITECTURE.md amendments (DC Blocker + In-loop saturator) remain deferred to end-of-Stage-2 verify per locked decision. Hand off: R20 atomic commit, then `/clear` + `/plugin-discuss O-Contrabass 2-dsp` opens Phase 2.2 (per-string detune + A1/D2/G2 strings + per-string M=4/3/2/1 dispersion table) as fresh GSD cycle.
- **2026-04-27 (Stage 2 / Phase 2.2, discuss):** CONTEXT.md rev-4 written — 13 approach decisions locked across the Phase 2.2 surface. User-confirmed Q1 (single-cycle, no a/b/c sub-split), Q2 (single voice with `std::array<WaveguideString, 4>` keyed E/A/D/G), Q3 (closed-form thresholding 28/33/38/43 with ACTIVE_STRINGS clamp — idiomatic DB convention + simplest single-pass thresholding), Q4 (remap to highest active string when MIDI note's chosen string > ACTIVE_STRINGS-1; silent-fail rejected), Q5 (5 ms equal-power crossfade at voice mix-bus, note-on-only trigger), Q6 (detune in delay-samples space, 20 ms `SmoothedValue<Linear>` ramp, per-sample `setDelay()` — architecture-locked, no override), Q7 (per-string M-table verbatim from architecture: E=4/1e-4, A=3/7e-5, D=2/5e-5, G=1/3e-5; consume Phase 2.1c's runtime `setActiveSections()` API), Q8 (eight-item Gate 4 bar: per-string sustained-tone × 3 + detune-sweep + note-sequence + ACTIVE_STRINGS=1-remap + auval/pluginval-10 + E1 STRING_STIFFNESS=0 bit-exact regression carry-forward + Logic AU smoke), Q9 (R21+ atomic commit on Gate 4 PASS — gate-first principle), Q10 (Logic Pro AU primary listening DAW). Three derived: idle-string topology (all 4 always tick; only friction injection gates by `activeStringIndex` — keeps idle strings warm via existing `−1e-20` leak, no cold-start on bow re-engagement); crossfade trigger semantics (only on note-on transitions to a different string; pitchbend/portamento mid-note stays on current string — DB physical reality); listening test sequence (E/A/D/G sustained × 3 s each + portamento 1 s/string + ACTIVE_STRINGS knob sweep + STRING_STIFFNESS sweep on D2). 8 open questions handed to research-phase. 8 risks enumerated with mitigations. Hand off to `/plugin-research O-Contrabass 2-dsp` (after `/clear`) for RESEARCH §15.

- **2026-04-27 (Stage 2 / Phase 2.2, research §15):** RESEARCH.md §15 written (~901 LOC append; total file 3147 LOC). All 8 CONTEXT rev-4 Open Questions resolved. **§15.1 pre-flight bit-exact baseline render executed** — `O-Contrabass-render-test --note 28 --sustain 60 --release 5 --infinite-sustain 1.0 --string-stiffness 0` against working tree at R20 commit `5759e5e` produced sha256 `d358abcddfa34840e1d4d843d7b49df6f3d28b7c4c9cbc269a80a3f600b0ee75` — byte-identical to committed Phase 2.1c golden. Determinism + working-tree integrity confirmed. **§15.2 (Q1):** note-on-only switching; closed-form `mapMidiNoteToStringIndex` 4-comparison ladder; replace-on-mid-crossfade-retrigger semantics; full `noteStarted()` pseudocode locked. **§15.3 (Q2):** precomputed equal-power crossfade ramp (3.5 KiB at 88.2k internal, exact, 2 array loads/sample) recommended over 256-entry LUT (CONTEXT line 127 mentioned bow-friction LUT pattern, but `HyperbolicFriction.h` has no LUT — closed-form analytical model; pattern reference was inaccurate). **§15.4 (Q3):** per-string smoother KEPT in `WaveguideString` (option (a) — zero API churn, trivial CPU cost, bit-exact regression bar trivially preserved); voice calls `setStringStiffness(globalAmount)` on all 4 instances per block; per-block update sequence locked. **§15.5 (Q4):** NO new prepare() overload; add `WaveguideString::setDispersionActiveSections(int M)` pass-through setter (option B — keeps `bridgeDispersion` private). Per-string M is configured via existing `DispersionFilter::setActiveSections(M)` API once per slot at prepareToPlay. **§15.5/§15.6 (Q5):** 12-TET fingering; `MidiMessage::getMidiNoteInHertz` × MPE bend × detune ratio; detune in delay-samples space via existing `setDelaySamples(totalSamples)` API; per-string `juce::SmoothedValue<float, Linear>` 20 ms ramp lives in voice. **§15.7 (Q6):** `--detune-sweep <E|A|D|G>` flag specified — per-block linear ramp of corresponding `DETUNE_<X>` parameter; JSON adds `mode`, `string`, `detuneRamp`, `rmsByDecade`, `rmsContinuityRatio`, `pass_rmsContinuity`. RMS-continuity formula = `min over adjacent-block RMS ratios`; threshold = 0.90 (CONTEXT line 134 0.99 was advisory; analytically calibrated against single-sample-click RMS bump = ~0.044 at amplitude 1.0 in 512-block → ratio drops to ~0.5; 0.90 catches genuine clicks, 0.99 false-flags). **§15.8 (Q7):** `--note-sequence "MIDI:dur,..."` flag specified — pre-build full MidiBuffer event list at start; per-block drain via existing harness MidiBuffer plumbing (no new infrastructure needed); JSON adds `sequence`, `transitionSampleIndices`, `perSegmentRms`, `pass_allSegmentsAudible` (>1e-3 threshold), `pass_rmsContinuityAtTransitions` (256-sample window centred on each transition; ≥0.50 threshold — generous because new string starts from zero energy). **§15.9 (Q8):** **strict byte-equal sha256 match** locked as Gate 4 invariant (7) tolerance. Analytical proof: idle-string per-sample state evolution from reset() with `INFINITE_SUSTAIN=1.0` (leak=0) gives literal `0.0f` output for the entire render (induction: 0 in → 0 out → 0 state → 0 out for n+1); leak-active case (`INFINITE_SUSTAIN < 0.95`) produces ~2e-20 output worst case → 13 orders of magnitude below 24-bit PCM LSB (1.19e-7) → quantises to bit-zero. §15.9.5 enumerates 4 hard rules (no fp-reordering on E-string mix path, no topology change for active string, prepareToPlay slot-0 sequence unchanged, slot-0 setActiveSections(4) unchanged) that the implementation MUST follow to preserve bit-exactness. **§15.10 pattern confirm:** O-Bowed `Source/DSP/WaveguideString.h` is INDEPENDENT FILE (not shared module) — Phase 2.2 surface changes are O-Contrabass-local; cross-plugin contract impact: zero. O-Bowed uses 1-string-per-voice + multi-voice polyphony for chords; Phase 2.2's 4-strings-in-1-voice pattern is plugin-specific (mono-bow constraint). Bow-friction shared module not touched. **§15.12 risk-surface refinement:** 8 CONTEXT rev-4 risks all mitigated/resolved + 2 new risks added (NEW 9: float-arithmetic reordering — hard-ruled in §15.9.5; NEW 10: detune-sweep threshold mis-calibration — derived 0.90 threshold). **§15.13 sequencing:** PLAN rev-6 recommendation = R21-pre (structural prerequisite, no commit) → R21 (BowedContrabassVoice) → R22 (WaveguideString add `setDispersionActiveSections`) → R23 (harness new flags + JSON schema) → R24 (build + auval + pluginval-10 + R-final regression check) → R25 (Gate 4 invariants 1-6) → R26 (atomic commit) → R27 (optional Logic AU smoke, user-deferred). Net source delta: ~+220 LOC + ~6 new golden text files. **§15.14 open items for plan-phase:** 8 items including R-pre location, threshold values, constant-array literals, golden file naming. Hand off to `/plugin-plan O-Contrabass 2-dsp` (after `/clear`) for PLAN rev-6.
- **2026-04-27 (Stage 2 / Phase 2.2, plan rev-6):** PLAN.md rev-6 appended (~+700 LOC; new tasks R21-pre / R21 / R22 / R23 / R24 / R25 / R26 / R27 optional). All 8 RESEARCH §15.14 open items pinned in PLAN preamble: (1) R-pre as structural prerequisite to R21 — `/tmp/` render only, no commit; (2) `pass_allSegmentsAudible` threshold = `> 1.0e-3f` (≈ −60 dBFS); (3) `rmsContinuityAtTransitions` window = 256 samples symmetric centred on transition (≥ 0.50 threshold); (4) `openStringFrequencyHz[4] = { 41.20f, 55.00f, 73.42f, 98.00f }` static literal (no JUCE static-init dependency); (5) `B_open[4] = { 1.0e-4f, 7.0e-5f, 5.0e-5f, 3.0e-5f }` per ARCHITECTURE.md §"String Waveguide Bank"; (6) `M_per_string[4] = { 4, 3, 2, 1 }` configured once at prepareToPlay via R22 setter; (7) golden file path `tests/render-harness/golden/{string-A,string-D,string-G,detune-sweep-A,note-sequence}.{wav.sha256,json}` — WAV NOT committed; (8) `readDetuneForString` paramId array `{ "DETUNE_E", "DETUNE_A", "DETUNE_D", "DETUNE_G" }`. HARD RULES §15.9.5 binding for slot-0 bit-exact preservation: no fp-reordering on slot-0 mix path (use early-return on `activeStringIndex` pattern, NOT unconditional sum); no topology change for active string; `prepareToPlay` slot-0 sequence unchanged (R22's `setDispersionActiveSections(4)` is no-op re-set since `prepare()` line 40 already sets it); slot-0 `setActiveSections(4)` retained verbatim. Sequencing: R22 lands BEFORE R21 compile (R21 calls `setDispersionActiveSections`). R26 atomic commit lands ~21 files: 5 source files + harness + 10 golden text files + 6 planning artefacts on Gate 4 PASS. Net source delta: ~+220 LOC. Eight-item Gate 4 bar: per-string A/D/G sustained-tone × 3 + detune-sweep-A + note-sequence E→A→D→G→E + ACTIVE_STRINGS=1+MIDI50 demote + auval + pluginval-10 + E1 STRING_STIFFNESS=0 strict byte-equal regression (sha256 == `d358abcd…` per RESEARCH §15.9 analytical proof) + Logic AU smoke (R27 user-deferred non-blocking). 10 risks enumerated incl. 2 new: (NEW 9) per-sample `setDelaySamples` on idle slots in steady state may shift delay-line internal-state representation — diagnostic-on-fail gating `if (detuneSmoothed[s].isSmoothing())` per R21 step 5 caveat; (NEW 10) detune-sweep continuity threshold mis-calibration — 0.90 derived analytically, soften to 0.85 if R25 false-flags. Estimated total effort ~10 h (R21 4h + R23 3h + R25 1.5h + others 1.5h combined). Hand off to `/plugin-execute O-Contrabass 2-dsp` (after `/clear`).
- **2026-04-26 (Stage 2 / Phase 2.1, research rev-2 / §12 + §13 + R7):** RESEARCH.md §12 (saturator-tail Phase 2.4 follow-up, ~110 LOC) and §13 (Phase 2.1b module-extraction research, ~210 LOC) appended. **§12** documents the analytical envelope estimate ≈10 %/s free-decay = x²/2 cubic-loss × 2 rails × 41.2 RTs/s, frames Phase 2.4's 108-combo matrix re-evaluation criteria, and conditionally tracks ARCHITECTURE.md §"In-loop saturator" amendment alongside §"DC Blocker" — both deferred to end-of-Stage-2 verify. **§13** resolves CONTEXT.md rev-2 Open Questions #2–#5: Q2 → Pattern A (`ouaricon_add_module`); Q3 → two direct headers, no umbrella, mirroring scala-tuning-engine; Q4 → DELETE inline-copy DSP files in both plugins, update `BowedStringVoice.h:23-24` includes, no shim files; Q5 → setter API (`setStaticFrictionCoefficient` + `setDynamicFrictionCoefficient` added to module's `HyperbolicFriction`; module keeps O-Bowed init defaults; O-Contrabass voice calls setters in `prepareToPlay`). §13 also locks the canonical preset (A4 vel 0.7, 5 s, factory defaults, no release tail, 32-bit float WAV) and specs the missing O-Bowed render-harness as PLAN rev-4 R8 work (mirror O-Contrabass harness exactly with target/note/sustain substitutions). Q1 (§12 timing) resolved by writing §12 in this same pass. Five Open Items handed to plan-phase rev-4 (§13.7): O-Bowed PluginCode + processor class confirm, `ouaricon_add_module` console-app compatibility, WAV writer parameter pinning, golden-WAV git-commit decision. **R7 atomic commit landed** — Phase 2.1a-recovery source (split-rail `WaveguideString.{h,cpp}` + F2 bridge LP + F3 DCB removal + F4 betaScale removal) + Stage 1 carry-forward (`CMakeLists.txt`, `PluginProcessor.{h,cpp}`, `PluginEditor.{h,cpp}`) + parameter-spec promotion + planning artefacts (CONTEXT/RESEARCH/PLAN/SUMMARY/VERIFICATION rev-3 + CHECKPOINT-2.1a + Stage 1 set + 0-ideation gate-report) + 4 Stage 0 deep-research docs (`research/O-Contrabass-*.md`) + render-harness binary + REQUIREMENTS.md update committed in single commit per CONTEXT.md rev-2 R7 file list. Commit body explicitly notes F1+F2+F3+F4 coupled fix (RESEARCH §11 root-cause), F3 ARCHITECTURE.md §"DC Blocker" deviation (justified per §11.6), Gate 1 PASS on bow-on validation, saturator-tail Phase 2.4 follow-up parked. Hand off to `/plugin-plan O-Contrabass 2-dsp` for PLAN rev-4 (R8–R15 module-extraction tasks per RESEARCH §13.6).
- **2026-04-27 (Stage 2 / Phase 2.3, research §16):** RESEARCH.md §16 written (~975 LOC append; total file 4122 LOC). All 10 CONTEXT rev-5 Open Questions resolved. **§16.1 pre-flight bit-exact baseline render executed** — `O-Contrabass-render-test --note 28 --sustain 60 --release 5 --infinite-sustain 1.0 --string-stiffness 0` against working tree at R26 commit `131c2c7` with `EXPRESSION_MACRO` default flipped 0.50f → 0.0f in `Source/PluginProcessor.cpp:86` and NO other source edits produced sha256 `d358abcddfa34840e1d4d843d7b49df6f3d28b7c4c9cbc269a80a3f600b0ee75` — byte-identical to committed Phase 2.1c/2.2 strict regression bar `tests/render-harness/golden/stiffness-zero-pre.wav.sha256`. Source edit reverted post-render; working tree clean. Q7a default flip is provably zero-delta until macro DSP wires. **§16.2 (Q1):** vibrato S-curve = half-cosine `0.5−0.5·cos(π·t/0.3)` for `t∈[0, 0.3]` (architecture-verbatim); per-sample `cosf` cost trivial inside 300 ms onset window; **note-off 150 ms LINEAR fade-out** (different from onset envelope; captures `vibratoOnsetGateAtNoteOff` at fade entry to avoid discontinuity on fast note-on→note-off mid-onset). **§16.3 (Q2) — Schelleng wedge bass-register pre-flight (Risk #7-style):** at default bass operating point (BOW_SPEED=0.15, BOW_PRESSURE=1.0, β=0.10, INFINITE_SUSTAIN=0.5, μ_s=0.85, μ_d=0.25, R_s=0.5), closed-form `fMax = (2·Z·v_b)/(β·Δμ) = 2.500`, `fMin = (Z²·v_b)/(2·R·β²·Δμ) = 6.250` (with Z=R=R_s=0.5 dimensionless collapse); `headroomUpper = (2.5−1.0)/2.5 = +0.60`, `headroomLower = (1.0−6.25)/6.25 = −0.84` → `headroom = min(+0.60, −0.84) = −0.84` NEGATIVE. v1.0 ships clamp-engaged-at-default (slow-LFO silenced at default bass settings); user must dial bow params toward mid-wedge to hear LFO modulation. **Phase 2.4 calibration polynomial parked** (analogous to Phase 2.1c E1 dispersion clamp Risk #7). HR-4 hard rule skips wedge math at SLOW_LFO_DEPTH=0 to preserve bit-exact regression bar. `--schelleng-stress` Gate 5 invariant 4 validates clamp-engages on stress preset (BOW_PRESSURE=7.0+BOW_SPEED=0.05): headroomUpper=−7.40, headroomLower=+2.36 → headroom=−7.40 → safeDepth=0 → clampedDepthMean ≈ 0 (instrumentation hook reports). **§16.4 (Q3):** vibrato + detune stacking — combine cents first (`totalCents = detuneCents + vibratoCents`) then single 2^ via `expf(vibCents · −ln2/1200)` (~12 ns vs 25 ns for `pow(2, ...)`); active string only; HR-1 literal-zero short-circuit when `effectiveVibratoDepth = 0`. Lagrange3rd absorbs ±50¢ peak (200× smaller than Phase 2.2 detune-sweep ±1200¢ already validated). **§16.5 (Q4):** brightness 20 ms voice-level `SmoothedValue<Linear>` is sufficient — Δp ≈ 0.015/block at 290 Hz/block macro step (0→500 Hz over 20 ms ≈ 1.72 host blocks); well below ~0.05 zipper threshold for one-pole sweeps. Single voice-level `macroSmoothed` feeds 4 destinations via per-block formula. WaveguideString surface untouched. Fallback to 50 ms (architecture line 522 body-bank precedent) reserved if Gate 5 invariant 5 fails. **§16.6 (Q5):** 7-step per-block evaluation order locked with full pseudocode. Vibrato + slow-LFO phase counters are voice-level (single phase counter each, NOT per-string); they advance regardless of crossfade state. Active-string-only modulation contract (Q2) means vibrato modulates ONLY the new active string's `setDelaySamples` during crossfade; old string's delay held at value set by `noteStarted`. **§16.7 (Q6):** 4 harness modes (`--vibrato`, `--slow-lfo`, `--schelleng-stress`, `--macro-sweep`) specified with full JSON schemas + pass-conditions + per-mode APVTS overrides. **Pitch-tracking via autocorrelation** (NOT FFT bin-shift; bass register at 41.20 Hz needs sub-bin resolution — at sr=44100, 4096-sample FFT bin width ≈10.8 Hz = ~25¢ resolution which is too coarse for ±12¢ vibrato measurement); 4096-sample window with 256-sample hop, parabolic interpolation around peak lag in τ ∈ [400, 1500] samples (29-110 Hz range). `clampedDepthMean` instrumentation hook (atomic `lastSafeDepth` written per-block in step 3, harness-side mean accumulator). **§16.8 (Q7):** bit-exact preservation analytical proof — HR-1 to HR-4 short-circuits + IEEE 754 identity arithmetic (`x + 0.0` and `x · 1.0` bit-identical for all finite x) preserve regression bar through full Phase 2.3 source edits. SmoothedValue<Linear> with `setCurrentAndTargetValue(0.0f)` init has currentValue == targetValue, every getNextValue() returns 0.0f exactly. **§16.9 (Q8):** macro/wedge interaction — NO re-eval against macro-lifted params. Macro is performance-lift, not safety-lift; downstream defenses (algebraic saturator x/√(1+x²), energy clamp, loop-gain ceiling 0.9999999) catch instability from macro-lifted bow params. Verification path = R32 listening test only. **§16.10 (Q9):** `vibratoOnsetTimerSeconds = 0` in `prepareToPlay`; per-note semantics; first note hears configured onset delay. **§16.11 (Q10):** Stage-1 contract amendment grep audit — only `parameter-spec.md` line 57 (default `0.50` → `0.0`) and `STATUS.md` `contract_checksums.parameter_spec` (sha256 update) require edits in R33 atomic commit. ARCHITECTURE.md and historical artefacts (parameter-spec-draft.md, ROADMAP.md, stages/1-foundation/PLAN.md) left untouched (audit trail / ARCH F3 deviation precedent). **§16.12 pattern confirm:** O-Bowed has ZERO vibrato/macro references — Q10 inline-in-voice decision unchallenged by precedent; tight-coupling to voice state + bass-tuned values + ~30 LOC each are the rationale. **§16.13 risk-surface refinement:** 9 CONTEXT rev-5 risks all mitigated/characterized + 4 new (NEW 10: `pass_breathingAudible` 20% threshold softened to 5% for v1.0 — wedge clamps at default bass; NEW 11: vibrato sine phase carry-forward harmless because vibrato goldens are single-note and sequence-mode goldens have VIBRATO_DEPTH=0; NEW 12: `skip(numSamples - 1)` guarded by `juce::jmax(0, ...)` for 0-sample blocks; NEW 13: autocorrelation τ ∈ [400, 1500] excludes sub-harmonic at f₀/2). **§16.14 sequencing — PLAN rev-7 recommendation:** R28-pre (structural prerequisite, /tmp render only, no commit) → R28 (BowedContrabassVoice + PluginProcessor source edits) → R29 (harness + autocorrelation pitch-tracking) → R30 (build + smoke) → R31 (Gate 5 invariants 1-7) → R32 (optional Logic AU smoke, user-deferred) → R33 (atomic commit, ~26 files). Net source delta: ~+356 LOC source + 8 new golden text files + 1 parameter-spec.md edit + STATUS.md checksum update. Estimated total effort ~12 h. **§16.15 14 open items handed to plan-phase:** R28-pre location, `pass_breathingAudible` threshold value, `--slow-lfo` mode preset bow-param overrides, `clampedDepthMean` hook signature, autocorrelation τ search range, vibrato onset measurement threshold, `skip(numSamples - 1)` pattern docs, `vibratoOnsetGateAtNoteOff` capture point, 3× SmoothedValue prepareToPlay init, harness mode mutual-exclusion precedence, `macroSmoothed.setTargetValue` unconditional-vs-gated, golden file paths, Stage-1 contract amendment artefact list, R32 listening test sequence MIDI events. Hand off to `/clear` + `/plugin-plan O-Contrabass 2-dsp` for PLAN rev-7.

- **2026-04-27 (Stage 2 / Phase 2.3, discuss):** CONTEXT.md rev-5 written — 11 approach decisions locked across the Phase 2.3 surface (vibrato + Slow-Bow LFO + Schelleng wedge clamp + EXPRESSION_MACRO). User-confirmed all 8 interactive Q's via AskUserQuestion (all Recommended options accepted): Q1 (single Phase 2.3 cycle covers all four items, no defer to Phase 2.6), Q2 (vibrato modulates active string only — preserves Phase 2.2 strict byte-equal regression bar; ~3 vs 12 multiplies/sample), Q3 (vibrato lifecycle re-arms onset envelope on every note-on, sine phase carries forward to avoid mid-program phase jumps; note-off 150 ms fast fade-out), Q4 (Schelleng wedge inline in `BowedContrabassVoice` — ~8 LOC per-block; CONTEXT rev-2 already de-classed `SchellengGuard`), Q5 (anti-correlation guard proportional: vibrato rate += `0.13f * SLOW_LFO_DEPTH` Hz — click-free at all transitions, scales with audibility), **Q7a (EXPRESSION_MACRO default flipped from architecture-spec'd 0.5 → 0.0** — preserves Phase 2.2 strict byte-equal regression bar at all 5 goldens; Stage-1 contract amendment flagged for R28 atomic commit body, parameter-spec.md sha256 will need re-checksumming), Q8 (anti-correlation guard verification = listening-test-only — drop FFT-bin spectral test from automated bar; Gate 5 has 8 invariants instead of 9), Q10 (inline modulator helpers in voice — no `VibratoLFO.h` / `SlowBowLFO.h` extraction). Three carried-forward defaults: Q6 (macro multiplicands architecture-verbatim — speed × (1+0.4·m), pressure × (1+0.6·m), vibrato depth × (1+0.3·m), brightness +500·m Hz), Q9 (R28 atomic commit on Gate 5 PASS — gate-first principle, continues sequence R7→R15→R20→R26→R28), Q11 (Logic Pro AU primary listening DAW — R32 user-deferred non-blocking). Per-block evaluation order locked: (1) read raw APVTS → (2) Schelleng wedge fMin/fMax/headroom → (3) slow-LFO with depth clamped to 0.8×headroom → (4) apply LFO multiplicatively to bow speed/pressure → (5) layer macro multiplicatively → (6) push to SmoothedValues → (7) per-sample loop. **4 hard rules HR-1 to HR-4 binding for bit-exact preservation at modulators-off**: HR-1 zero-vibrato-depth literal-zero check; HR-2 zero-LFO-depth early return; HR-3 zero-macro literal arithmetic (1.0+0.0=1.0 exact); HR-4 zero-LFO-depth Schelleng skip. Eight-item Gate 5 bar: (1) all 5 Phase 2.2 goldens reproduce byte-identically (regression bar); (2) vibrato sustained-tone harness (MIDI 28, FFT-derived peak depth ∈ [10¢,14¢], onset window ~900 ms, rmsContinuity ≥ 0.90); (3) slow-LFO sustained-tone harness (MIDI 33, 60 s, peak-to-peak rmsByDecade ≥ 20%, rmsContinuity ≥ 0.90); (4) Schelleng wedge stress test (MIDI 28, BOW_PRESSURE=7.0+BOW_SPEED=0.05+SLOW_LFO_DEPTH=1.0, peak ≤ 0 dBFS, clampedDepthMean < 0.5, no NaN); (5) macro-sweep harness (MIDI 38, 0→1.0 ramp, rmsContinuity ≥ 0.85, RMS ramp direction in [10%,30%]); (6) auval; (7) pluginval-10; (8) R32 Logic AU smoke (user-deferred non-blocking). 4 new harness CLI flags (`--vibrato`, `--slow-lfo`, `--schelleng-stress`, `--macro-sweep`); 4 new golden text files (sha256 + JSON only, no WAV). 10 open questions handed to research-phase: vibrato S-curve formula (recommend half-cosine `0.5-0.5·cos(πt/0.3)` per architecture line 125), Schelleng wedge bass-register validity pre-flight (Risk #7-style — does headroom > 0.1 at default bass params?), vibrato+detune Lagrange3rd stacking math (combine cents first then single 2^), brightness offset 20 ms smoothing window adequacy (500 Hz step at 25 kHz/s — recommend fallback 50 ms per body-bank precedent), per-block evaluation order final pseudocode + crossfade interaction edge cases, harness JSON schemas for 4 new modes (incl. FFT bin-shift method for vibrato peak-depth derivation), bit-exact preservation pre-flight render with EXPRESSION_MACRO default=0.0 (must match `d358abcd…` BEFORE Phase 2.3 source edits), macro/wedge interaction policy (recommend NO re-eval against macro-lifted params), vibratoOnsetTimer init = 0 on prepareToPlay, Stage-1 contract amendment grep audit for stale 0.50 references in markdown artefacts. 9 risks enumerated with mitigations including bit-exact regression via hard rules, Schelleng always-clamps fallback, brightness zipper fallback, Lagrange3rd accumulation error, Schelleng CPU spike, macro/onset compound modulation, default-change auditability, harness instrumentation hook precedent, slow-LFO low-rate cycle count. Hand off to `/clear` + `/plugin-research O-Contrabass 2-dsp` for RESEARCH §16.
