# Stage 4 — Polish / Validation (persistence, factory presets, CI Windows, listening pass, release): RESEARCH

**Plugin:** O-Strata
**Stage:** 4 of 4 — Polish / Validation; one RESEARCH.md for **Round A** (Phase 4.1) and **Round B** (Phase 4.2) per CONTEXT D1
**Phase:** research ✓
**Date:** 2026-09-12
**Input:** `stages/4-polish/CONTEXT.md` (D1–D4, findings 1–11, research items 1–10), `ROADMAP.md` v2 Stage 4, `REQUIREMENTS.md` v2.0.0, `research/ARCHITECTURE.md` v2, `Source/` at `5b34b5cf` (working tree `1eb1edb1`, `Source/` unchanged since the Stage 3 verdict), `tests/render-harness/`, `.github/workflows/ci-tests.yml`, the JUCE 8.0.14 tree at `/Users/taylorbrook/JUCE`, `modules/persistence/preset-manager`
**Method:** four parallel investigations (A persistence + preset hook + Locate… + H10, B factory bank + H2 over presets, C `ci-tests.yml` + MSVC survey + docs / install, D Chebyshev evaluator bench + listening protocol); every claim read from source or measured on this Mac (Apple M4 Max, Apple clang 17, `-O3 -arch arm64` = the plugin's ninja flags, node v24 local / v22 on `macos-14`). Scratch under the session scratchpad `strata-r4/` (`A-persistence.md`, `B-factory-bank.md`, `C-ci-msvc-docs.md`, `D-chebyshev-listening.md`, `bench.cpp` (JUCE base64 / SHA / XML / JSON costs), `main_h2cli.cpp` + `h2cli` + `h2-batch.log` (scratch H2 harness over the draft bank), `cheb_bench.cpp` + `bench_O3_final.txt`). `[VERIFIED: path:line]` = read from source; `[MEASURED: how]` = run here; `[ASSUMPTION]` = neither.

---

## 1. Summary

All ten items resolve. Six findings change what the planner writes; one is a Round A scope change (the fallback is DSP work, not a notice).

1. **The "Sine Product fallback" does not exist — an Imported… oscillator with no image is silent.** `y = image != nullptr ? image->sample (…) : 0.0f` on every quality path `[VERIFIED: dsp/TerrainOscillator.cpp:397]`, the Bandlimited route falls to the same line `[VERIFIED: :258-266]`, the header says "nullptr = none → silent" `[VERIFIED: TerrainOscillator.h:134]`. CONTEXT Finding 2(a)'s "analytic fallback meanwhile, no silent gap" is wrong: every restore has a ≈ 150 ms gap (debounce + job), and QUAL-03's fallback is a new `TerrainOscillator` branch in Round A, pinned by an H10 row that asserts non-silence and SHA-equality with a Sine Product instance (§2.1, §2.5).
2. **Persistence has one cheap shape.** One `terrainImports` child, `<slot osc form=bytes|path name sha256 size data|path/>`, slots written only for oscillators that hold an import (the default session still serialises an empty child, smoke [3] stays green); the preset `customState` carries the same fields as `{v, slots:[…]}` so one writer / reader pair serves both. `juce::Base64` (standard alphabet — the editor already forbids `MemoryBlock::toBase64Encoding` `[VERIFIED: PluginEditor.cpp:567-569]`), every XML property read as a string. Measured for a 2 MiB slot: encode 3.3 ms, decode 15.6 ms, SHA 6.5 ms, two-slot host blob 5.6 MB, preset JSON write 94 ms / read 14 ms; a two-2-MiB restore is ≈ 55 ms + the image jobs — the < 1 s budget is comfortable `[MEASURED: bench.cpp]` (§2.1).
3. **`customLoad` is only called when the preset has a `customState` key** `[VERIFIED: OuariconPresetManager.h:273]` — loading an image-less preset after an image preset leaves the slots live and a user save then carries the wrong image. Patch the O-Strata copy to call `customLoad (var())` when the key is absent (file as preset-manager v1.0.7); the `stateGeneration` bump needs no move — both paths already run import-then-bump, and the async publish gap is closed by the `imageGeneration`-keyed heightmap re-push (§2.2).
4. **The bank is drafted and measured.** 18 presets (Init + 17) cover 6 / 6 terrains, all 11 orbits, feedback × 3, Bandlimited × 3 and the five BRIEF use cases; run through a scratch copy of the harness at C4, 16 pass at the default centre and **Squarcle Storm** fails (−6.9 dB / 0 %) until its centre moves to (0.23, 0.21) `[MEASURED: h2-batch.log]`. Two harness blind spots surfaced: the preset loop takes f0 from the note and ignores `oscACoarse` / `Fine` (a Coarse −12 bass reads FAIL for the wrong reason), and `cleanPatch()` runs after the preset and zeroes osc B, so presets are gated on A only. Regeneration needs a content stamp (`1.0.0+sha256(bank)[0:12]`) plus `Factory/.deleteRecursively()` — `.factory-version` already reads `1.0.0` so nothing regenerates otherwise `[MEASURED]` (§2.4).
5. **The literal D4 evaluator is slower than Clenshaw.** "153 independent FMAs" into one float accumulator measures 102 ns vs 85 ns dependency-carried (no `-ffast-math`, the compiler cannot reassociate). The win needs per-row partial sums **and** a padded 17 × 20 coefficient copy: portable plain-C **29.1 / 18.2 ns** (2.9× / 3.5×), bit-identical to the NEON form (26.7 / 12.2 ns) `[MEASURED: cheb_bench.cpp]`. The H1 identity row is **vacuous** for D4 — `c = δ₁,₀` returns `x` bit-exactly for every evaluator; the gate becomes `max |Δ| ≤ 2e-5` against `clenshaw2D` plus an H6 band of ± 0.5 dB (today's band is zero width). Predicted H7 Bandlimited delta ≈ 4.8 % (portable) vs the ≈ 6.2–6.4 % ceiling (§2.8).
6. **`ci-tests.yml` takes a `plugin` dispatch input, not duplicated jobs.** O-Octagon is hard-coded in 11 places; the two job skeletons differ only in `PLUGIN`, target names and run steps. `resolve-target.sh cmake-target O-Strata` resolves cleanly with no fallback warning `[MEASURED]` (Finding 7 wrong), the O-Strata harness derives its manufacturer code from the target property (Finding 7 wrong again), `orbit-golden.mjs` is plain Node and Node 22 is on `macos-14`. Estimated job times 5–6.5 min each; three rows inside `--gate all` carry wall-clock verdicts (H7 ratio, scheduler ≤ 120 ms, import ≤ 100 ms) — a runner flake source the plan must own (§2.6).
7. **MSVC survey: zero hits on every known trap** — the three memory traps and a 25-token census all read 0 in `Source/**` `[MEASURED]`; the WebView2 `Options` chain is O-Prism's line for line and O-Prism has shipped Windows builds since v1.19.3. Residual risk is unknown-unknowns; the dispatch run is the oracle. The harness TU (`posix_memalign` at `main.cpp:130`) is macOS-only by omission, not by an `APPLE` guard (§2.7).
8. **The listening pass has a format and a sampling rule.** `round-b/LISTENING.md` with three tables: A every preset × Logic + Live 12, B the 66 C4 grid rows plus gate-flagged C2 / C6 pairs, C the Stage 3 human rows folded in; verifier = Taylor. The export grid is **2× only** (no Bandlimited WAVs — the presets are the Bandlimited material); `golden/round-a-grid.sha256` is CRLF (198 / 198 match with `tr -d '\r'`) and `--gate export` re-records without comparing (§2.9).

Confirmed as the CONTEXT expects: `ImportSlot` + `getImportSlotCopy` + `importTerrainImage` / `importTerrainFile` are the whole data side; `applyPresetJson` resets → applies → `customLoad` (Finding 2b); a second load inside the 150 ms debounce is safe by the scheduler's key / cancel design; `#terrain-notice` + the three keys + the `terrain-source-missing` gate state exist; `SafePointer` sites are all the hoisted form; `juce_cryptography` linked; `getPresetListWithCategories` groups and the ◀ / ▶ walker uses the alphabetical flat list (parity safe); `build-and-install.sh O-Strata` resolves its target and sweeps both variants; the `view-perf.log` path and the ⌥-click burst exist and the log is still absent; `gl.finish()` runs per big-view frame at `terrain-view.js:685`.

---

## 2. Open items resolved

### 2.1 Item 1 — `terrainImports` schema and reads

**Today** `[VERIFIED]`: `getStateInformation` = `copyState` → `tuningEngine` child → `uiLanguage` → **empty** `terrainImports` child → `createXml` → `copyXmlToBinary` (`PluginProcessor.cpp:1214-1238`); `setStateInformation` = `getXmlFromBinary` → `replaceState` → tuning caches → `uiLanguage` (string read with `isVoid()` guard — the house pattern) → `tuningEngine` → `ignoreUnused (terrainImports)` → `stateGeneration.fetch_add` last (`:1240-1287`). `getTerrainStatus` hard-sets `sourceMissing = false` (`:1165`); `makeStatusEvent` already serialises it (`TerrainViewFeed.cpp:201-213`). `importTerrainImage` asserts the message thread, decodes inline, bumps `slot.revision` and `importRevision[osc]` (`:1179-1199`); the scheduler keys `{importRevision, blurQ, edge}` whenever `importRevision > 0`, submits after a 150 ms debounce (first submit immediate — `lastSubmitMs = -1e9`), `buildImage` requires `slot.decoded != nullptr` (the job never decodes), `publishImage` swaps `imagePtr` and bumps `imageGeneration` (`TerrainScheduler.cpp:446-470, 557-623`). **Silent when Imported… has no image** — Summary 1.

**JUCE API** `[VERIFIED: juce_core/text/juce_Base64.h:50-62; juce_cryptography/hashing/juce_SHA256.h:71-102]`: `Base64::toBase64 (const void*, size_t)`, `convertFromBase64 (OutputStream&, StringRef)`; `SHA256 (const void*, size_t) / (const MemoryBlock&) / (const File&)`, `toHexString()` (already used at import, `PluginProcessor.cpp:1193`).

**Costs** `[MEASURED: bench.cpp, 2 MiB pseudo-random block, best of 3]`:

| Step | ms |
|---|---|
| `Base64::toBase64` 2 MiB | 3.3 (2 796 204 chars) |
| `Base64::convertFromBase64` | 15.6 |
| `SHA256` 2 MiB | 6.5 |
| `ValueTree::createXml` (205 PARAM + 2 slots) / `XmlElement::toString` / parse + `fromXml` | 0.1 / 15.4 (**5 599 536 B**) / 3.4 |
| `JSON::toString` (customState, 2 slots) / `JSON::parse` | 94.3 / 13.7 |

Restore of two 2 MiB slots ≈ parse 3 + 2 × (decode 16 + SHA 7 + PNG decode 3) ≈ 55 ms before the image jobs (25 ms sync / 72 ms pumped at 1024², `--gate import` (d) / (g)). Save ≈ 22 ms per host autosave — memoising the base64 string per slot revision is cheap insurance (O-MicrotonalSampler's WR-04 precedent). After an XML round trip every property `isString()` `[MEASURED]`; after JSON ints come back as ints — read both with `toString().getIntValue()`. Host blob limit: **unknown**; the in-repo precedent is O-MicrotonalSampler shipping 250–350 MB states through the same route `[VERIFIED: O-MicrotonalSampler/CHANGELOG.md:3082-3086]`.

**Recommended layout:**
```xml
<terrainImports>
  <slot osc="0" form="bytes" name="lava.png" sha256="…" size="1834211" data="…base64…"/>
  <slot osc="1" form="path"  name="big.png"  sha256="…" size="4102233" path="/Users/…/big.png"/>
</terrainImports>
```
- Slots written only for oscillators holding an import → smoke [3] `getNumChildElements() == 0` stays green `[VERIFIED: tests/render-harness/main.cpp:493]`. ARCH's draft names (`oscAImport`, `embedded="1"`) are superseded — record the final layout in ARCH "State Persistence".
- Form chosen **by size at save time**; bytes form stores no path (the bytes are the identity); path form stores the absolute path — on another machine `existsAsFile()` is false → `sourceMissing` → Locate… (CHANGELOG "Known limits").
- `kMaxImportBytes` moves from `PluginEditor.h:123` to the processor (the page duplicates it as `IMPORT_MAX_BYTES`, `index.html:5131`); editor and natives alias it.
- SHA check for the path form runs where the restore runs: `File::loadFileAsData` + `SHA256`, then `importTerrainImage` — which **asserts the message thread** (`:1181`). The harness calls `setStateInformation` from the message thread (`main.cpp:2384, 498`); Logic / VST3 call `setState` on the main thread in practice `[ASSUMPTION — not a JUCE guarantee]`. Guard with `MessageManager::existsAndIsCurrentThread()`, else `callAsync` with an alive token. Mismatch = `sourceMissing`, slot keeps `name / sha256 / path` for Locate….
- `ImportSlot` gains `String path` and `bool sourceMissing`; `getTerrainStatus` reads it through `getImportSlotCopy` (uncontended lock in the 30 Hz poll).

### 2.2 Item 2 — preset-manager hook order

- Setter `setCustomStateCallbacks (std::function<var()>, std::function<void (const var&)>)` `[VERIFIED: OuariconPresetManager.h:54-67]`; O-Strata never calls it. **Call it in the constructor right after `excludedParameterIds` (`PluginProcessor.cpp:566-569`) and before `initializeFactoryPresets` (`:575-577`)** — O-Bells / O-Contrabass / O-Lyrica order `[VERIFIED: O-Bells PluginProcessor.cpp:585-616; O-Contrabass :240-262; O-Lyrica :601]`.
- JSON: `preset->setProperty ("customState", customSave())` — a `var`, serialised inline `[VERIFIED: :209-233, :294]`. Recommended `{ "v": 1, "slots": [ { "osc", "form", "name", "sha256", "size", "data" | "path" } ] }` — the XML attribute set, `var ↔ ValueTree` adapters of ≈ 20 lines each. The `getStateAsXml` / `CustomState` path (`:514-554`) is unused by O-Strata — ignore.
- Load order: reset non-excluded params → apply → `if (customLoad && preset->hasProperty ("customState")) customLoad (…)` `[VERIFIED: :244-276]`. **Absent key → no call** (Summary 3). Fix (ii) recommended: the O-Strata copy calls `customLoad (var())` when absent (2-line change, other plugins' callbacks all null-check `getDynamicObject()` `[VERIFIED: O-Bells :600, O-Contrabass :258]`), filed as preset-manager v1.0.7; fallback (i) = every factory def carries `customState = {}` (written when `!isVoid()`, `:586-587`). `clearImportSlot (osc)` = empty bytes / name / sha / path / decoded, `importRevision = 0`, `imagePtr.exchange (nullptr)` retired through the reaper — the constructor state.
- Re-entrancy: the natives run on the message thread (`PluginEditor.cpp:639-676`); a pending `ImageJob` for an older key is cancelled at the next poll and its result dropped when `key != lastSeen` `[VERIFIED: TerrainScheduler.cpp:454-461, 604-609]` — no extra state.
- `stateGeneration`: bumped last in `setStateInformation` (`:1285`) and by `notifyStateChanged()` after a `true` result in the four preset natives (`PluginEditor.cpp:645, 657, 668, 676`); `savePreset` does not bump. `customLoad` runs inside `loadPreset…` **before** the native's bump — already import-then-bump on both paths. The forced push at the bump shows the pre-publish heightmap (zeros) and re-pushes when `imageGeneration` moves (`heightmapKey` folds it, `3-gui/SUMMARY.md:49`). **Nothing moves** (CONTEXT Finding 2 resolved).
- `initializeFactoryPresets` writes parameters + optional `customState` + `category` + `factory: true` (`:562-596`); the bank carries none. Smoke [5] `--with-disk` asserts exactly `Init/Init.json` + two files `[VERIFIED: main.cpp:539-549]` — rewrite with the bank.

### 2.3 Item 3 — Locate… flow

**Exists** `[VERIFIED]`: `<div class="terrain-notice" id="terrain-notice" data-i18n="label.sourceMissing">` (`index.html:2417`), CSS `.terrain-notice { display:none … }` + `.visible` (`:1596-1602`); `STATUS[A|B].sourceMissing` from `terrainStatus` (`:4988-5010`), `refreshInert` toggles `.visible` unless `NOTICE.transient` (`:5081`), `showTerrainNotice (key, holdMs)` / `restoreTerrainNotice` / `window.__showTerrainNotice` seam (`:5137-5157`), `#btn-terImport` → `chooseTerrainImage (ACTIVE_OSC)` (`:5165-5168`) with a `min-width: 90.39px` pin (`:1643`). Keys `label.sourceMissing / importTooLarge / importFailed` with fr `reviewed: false`, zh `'mt'` (`i18n.js:2383-2389`). **`#btn-terImport` has no tip row** and `check-i18n` does not require one (`scripts/check-i18n.js:35-38, 627-659`). Native `chooseTerrainImage` = `FileChooser` on `userPicturesDirectory`, `*.png`, hoisted `SafePointer`, cap check, `importTerrainFile`, `selectImportedTerrain`, `repushPending`, `complete (importResult)` (`PluginEditor.cpp:522-556`). Gate states `terrain-source-missing` / `terrain-source-ok` / `terrain-import-notice` / `terrain-notice-clear` already in `tests/i18n-states.json:28-36`; stub override `chooseTerrainImage → {ok:false, reason:'cancelled'}`.

**Recommendations:**
1. **Button inside the notice**: `<div id="terrain-notice"><span data-i18n="label.sourceMissing"/> <button id="btn-terLocate" class="terrain-import-btn" data-i18n="label.locate">Locate…</button></div>`; `showTerrainNotice` adds `.transient` and CSS hides the button under it; `setLabel` targets the **span** (today it writes the div, `:5142, 5149` — memory `pattern_js_state_updater_overwrites_html_labels`); pin `#btn-terLocate` at its fr / zh width.
2. **New native `locateTerrainImage (osc)`**, factoring the `launchAsync` block into a helper: reads `getImportSlotCopy (osc).sha256`, hashes the chosen file, match → `importTerrainFile` + `{ok:true}`, mismatch → `{ok:false, reason:'hashMismatch'}` and touches nothing. Hoisted-`SafePointer` form (MSVC trap 2, §2.7).
3. **Refuse on mismatch** with `label.locateMismatch` (transient 4 s, sticky notice returns): a preset promises a specific terrain, silent re-stamping makes FUNC-08's "identical bytes ⇒ identical render" lie; Import… is one click away and re-stamps by definition.
4. **After a locate** the slot re-enters the size rule: ≤ 2 MiB is saved as `bytes` next time, above stays `path` with the new path; `sourceMissing` clears on the next status tick.
5. **Keys** (fr `reviewed: false`, zh `'mt'` → `i18n-zh-backtranslate.js`; lints first): `label.locate` "Locate…" / "Localiser…"; `label.locateMismatch` "Different file — hash does not match the preset" / "Fichier différent — l’empreinte ne correspond pas au préréglage"; `tip.terLocate` (+ optional `tip.terImport`) with `TIP_BINDINGS` rows.
6. **Gate state** `terrain-locate-mismatch` = `window.__showTerrainNotice ('label.locateMismatch', 0)` (extend the key whitelist `:5149-5151`); the existing `terrain-source-missing` state already renders the button for `check-ui-labels` + `ui_layout_check.js`; stub override `"locateTerrainImage": {"ok": false, "reason": "cancelled"}` in `generic-overrides.json` (natives block is hand-maintained).
7. **The fallback sound is DSP** (Summary 1): `terrainKind == Imported && image == nullptr` → `terrain (TerrainKind::SineProduct, …)` at `TerrainOscillator.cpp:394-399`; the Bandlimited route already falls to the analytic call once `candidate == nullptr`.

### 2.4 Item 4 — factory bank authoring

- **Today**: `Init` at `getDefaultValue()` (normalised), category "Init" `[VERIFIED: FactoryPresets.cpp:37-48]`; regeneration when `.factory-version != JucePlugin_VersionString` (`PluginProcessor.cpp:574-577`); `initializeFactoryPresets` only creates / overwrites the defs it is given — **no `deleteRecursively`, no orphan sweep, no hash facility** in the O-Strata copy or in module v1.0.6 `[VERIFIED: OuariconPresetManager.h:562-604; modules/registry.yaml:138-160]`. `.factory-version` on disk already reads `1.0.0`; `Factory/Init/Init.json` (198 keys) is the only file `[MEASURED]`. `getPresetListWithCategories` walks every `Factory/*` directory, so a stale `Init/` stays a category (`:435-453`).
- **Orphan mechanism**: stamp `JucePlugin_VersionString + "+" + sha256(canonical bank)[0:12]` (category, name, id = value in map order — IEEE floats, no text drift); build the vector every launch (18 × ≈ 200 entries, trivial); on mismatch `getFactoryPresetsDirectory().deleteRecursively()` then `initializeFactoryPresets (defs, stamp)`. `User/` is a sibling, never touched. A hash catches the failure mode in front of us (content changed, version did not) and any defaults change that moves `Init`.
- **Authoring**: the JSON stores **normalised** values (`createPresetJson` writes `getValue()`, `applyPresetJson` calls `setValueNotifyingHost` raw `[VERIFIED: :218-222, :262-269]`), so author in engineering units and convert once in `build()` with `param->convertTo0to1` — O-Prism's `RawMap → merge (completeBase(), …) → normalize → makePreset` pattern verbatim `[VERIFIED: O-Prism/FactoryPresets.cpp:38-185]`; `completeBase()` from `getDefaultValue()` via `convertFrom0to1` so Init and the base agree by construction. `osc?TerFreq` is the 3-lambda exact-log range (`convertTo0to1 (1.0) = 0.4`, `(2.0) = 0.6`) `[VERIFIED: PluginProcessor.cpp:135-138]`; eleven other skewed ranges (`amp*`, `filt*` env, `filt?Cutoff`, `reverbPredelay / ModRate`, `delayTime`, `eqMidFreq`, `lfo?Rate`) need the same conversion. Tuning IDs in a def are dropped at write time (`:579-580`) — the bell names its tuning in a description only; **`FactoryPresetDef` has no description field** (decision).
- **Surface** (`[VERIFIED: PluginProcessor.cpp:74-171; Terrains.h:46-56; Orbits.h:49-62]`): Terrain 0 Sine Product, 1 Radial Rings, 2 Saddle, 3 Ridged Cosines, 4 Mitsuhashi, 5 Cosine Wells, 6 Imported…; Orbit 0 Ellipse, 1 Superellipse, 2 Limaçon, 3–5 Epitrochoid 3 / 5 / 7, 6–8 Hypocycloid 3 / 5 / 7, 9 Butterfly, 10 Squarcle; `TerFreq` 0.25–8 (1.0), `OrbAspect` 0.1–1 (0.7), `OrbRot` 0–360, `OrbCX / CY` −1–1 (0.13 / 0.21), `OrbMod` (0.5), `OrbFeedback` (0) / `OrbFbDamp` (0.5), `TerSat` (0) / `TerBlur` (0.2), `TerEdge` Mirror / Window, `TerTrack` (1), `Quality` 0 Bandlimited / 1 2× / 2 4×; mod destinations 26–35 OscA {Aspect, Rot, CX, CY, Mod, TerFreq, ModX, ModY, Feedback, Sat}, 36–45 OscB (`ModulationMatrix.h:38-113`); CX / CY offset = amt·source in unit space, Rot 180°·off, TerFreq 2^(2·off), Aspect 0.9·off (`StrataVoice.cpp:207-220`).
- **Draft bank** — §5.1 (18 presets, coverage matrix, measured H2). Categories = the page's `CATEGORY_ORDER` names: **Init, Pads, Drone, Lead, Bass, Pluck, Keys, Sequence, FX** — `'Init'` must be prepended to `CATEGORY_ORDER` (unknown categories sort after User, `index.html:2891-2985`); headers are rendered raw (`:2969`), not localised.
- **Dropdown**: ◀ / ▶ walk the manager's alphabetical flat list (`PluginEditor.cpp:664-678; OuariconPresetManager.h:404-433, 476-512`) — parity holds, grouping is harmless; names must stay unique across categories (`loadPreset (name)` takes the first hit, `:312-325`); `Init` is not pinned.

### 2.5 Item 5 — H10 full, H2 over presets, preset criteria

**H10 today** = three instances through `importTerrainImage` (A == B, A != C), 0.1 s `[VERIFIED: main.cpp:2173-2189; MEASURED]`; fixtures are 9–16 KB; `pump (ms)` is the single `runDispatchLoopUntil` site (`:285`); `getStateGeneration()` is public (`PluginProcessor.h:169`); `createPresetJson` / `applyPresetJson` are private, `loadPresetFromFile (File)` is public (`OuariconPresetManager.h:70-73, 371-385`).

**Rows to add inside `--gate H10`** (≈ +2–3 s on `all`):
- (a) state round trip, bytes form: A imports → `getStateInformation` → assert one `<slot osc="0" form="bytes">` → B `setStateInformation` → `sha256` equal, `decoded != nullptr`, no phantom slot 1, render SHA equal.
- (b) cap form with a new incompressible fixture (`makeNoisePng`, per-channel LCG noise, size asserted 1.5–2 MiB) in `tempDirectory`: present → `form="path"`, no `data`, render SHA equal; deleted → `sourceMissing` true on slot + status, `imagePtr == nullptr`, **rms > 1e-3 and SHA == a Sine Product instance** (pins the fallback); rewritten with different bytes → `sourceMissing` (mismatch row).
- (c) preset JSON round trip via public `saveTerrainImportsVar()` / `loadTerrainImportsVar (const var&)` → render SHA equal; then a hand-built preset JSON through `getPresetManager().loadPresetFromFile` in the temp dir (no `~/Library`) → same SHA; then a JSON **without** `customState` → both slots cleared (the §2.2 regression row).
- (d) two 2 MiB slots, pumped: `while (imageGeneration[0] == 0 || [1] == 0) pump (10)` → both published < 1.0 s, split printed.
- (e) UI-04 ordering, headless: `getStateGeneration() == g0 + 1` **and** `importRevision[0] > 0` after `setStateInformation`; preset path moves `importRevision` without a bump, `notifyStateChanged()` then bumps. Live half stays human.

**H2 over presets** `[VERIFIED: main.cpp:770-845]`: 66-pair analytic grid (neighbour print on FAIL around the *default* centre) → 66-pair Bandlimited → factory presets (fresh `Instance`, reset, `def.parameters` applied, `cleanPatch()`, `h2Render` at 60) → negative control; whole gate 2.5 s `[MEASURED]`. No CLI / fixture path reaches the preset loop (`--set` only under `--print-only`, `--export NAME` is a dead option `[VERIFIED: :210, :260-266, :2358, :2403]`), so the draft was measured through a scratch TU (`main_h2cli.cpp`: `--real id=eng` + `--gate h2cli`, validated against the shipped gate — defaults 0.0 dB / negative control −109 dB identical). **Defects**: (1) `h2Render` takes f0 from `getFrequency (note)` (`:767`), ignoring `oscACoarse / Fine` — Folded Bass at Coarse −12 reads −8.7 dB / 0 % and 0.0 dB / 100 % at Coarse 0 or note 48 `[MEASURED]`; fix `f0 *= exp2 ((coarse + fine/100)/12)` in the preset loop; (2) `cleanPatch()` after the preset forces `oscMix 0, oscBLevel 0` (`:241-250, 826`) — gate a second pass with A / B roles swapped when `oscBLevel > 0`, reported `preset X (B)`; (3) no neighbour print for presets — port the scratch form around the preset's own centre. Upstream `--real` and `--gate h2cli` so the next bank edit needs no rebuild.

### 2.6 Item 6 — `ci-tests.yml` extension

- **Today** `[VERIFIED: ci-tests.yml]`: `workflow_dispatch` with no inputs (`:44-45`), `permissions: contents: read`, no secrets; header claims list at `:142-151` ("CAN claim: the code compiles under MSVC; the VST3 loads; pluginval 10 OPENS THE EDITOR … CANNOT claim: that the UI is CORRECT on Windows … Named deferral, owner none, blocked on hardware") — keep verbatim; the "THE GATE IS THE BUILD STEP" paragraph (`:33-39`) is O-Octagon-only. macOS job (`macos-14`, `:57-139`): JUCE from `.github/juce-version.txt` (8.0.14), overrides (two files) with the two `JUCE-NE-PATCH` greps, `PLUGIN="O-Octagon"` + `SKIP` loop, `-DOUARICON_BUILD_TESTS=ON`, no `OUARICON_RELEASE`. Windows job (`windows-latest`, `:153-241`): WebView2 NuGet 1.0.1901.177, no `OUARICON_BUILD_TESTS`, `resolve-target.sh`, `${TARGET}_VST3`, pluginval 1.0.3 zip (URL-pinned, no SHA), strictness 10, log artefact. Actions SHA-pinned (checkout v4.4.0, upload-artifact v4.6.2). O-Octagon hard-coded in **11** places (`:1, 57, 102, 130, 135, 139, 153, 195, 212-214, 229, 240`). `SKIP_PLUGINS` = every other `plugins/*/` with a CMakeLists, so `OUARICON_BUILD_TESTS=ON` reaches one plugin (37 carry the option; the header's "eleven" is stale). O-Strata's block adds `O-Strata-param-dump` + `O-Strata-render-test` (`CMakeLists.txt:118-147`); `--target O-Strata-render-test` builds only the latter.
- **Corrections**: `bash scripts/resolve-target.sh cmake-target O-Strata` → `O-Strata`, exit 0, no warning `[MEASURED]`; the harness's manufacturer code comes from the target property (`ParamDump.cmake:33-36, :206`), not a literal — still never pass `OUARICON_RELEASE` (parity, every figure taken on dev codes, the root force-OFF does not apply under `$ENV{CI}`, `CMakeLists.txt:16-21`). `orbit-golden.mjs` is plain Node ESM, exit 2 on a miss (`tests/orbit-golden.mjs:1-46`); Node 22.23.1 on `macos-14` `[VERIFIED: runner-images README]`. Fixtures resolve via `STRATA_FIXTURES_DIR`, cwd-independent (`CMakeLists.txt:134-136`).
- **Timing** `[MEASURED: gh run view; scratch CI-shaped build]`: O-Octagon macOS 2 m 36 s (configure 37 s, build 91 s), Windows 5 m 44 s; O-Prism v1.26.0 Windows 6 m 48 s; O-Strata harness local 19.6 s wall / 205 s CPU → **estimate macOS 5–6.5 min, Windows 5.5–6.5 min**, in parallel.
- **Recommendation (i)** — a `plugin` `choice` input (`default: O-Octagon`, options `[O-Octagon, O-Strata]`) with per-plugin `if: inputs.plugin == '…'` steps; jobs renamed `probes-macos` / `windows-vst3`; O-Strata steps = build `O-Strata-render-test`, `--gate all`, `--gate orbits --out $RUNNER_TEMP/orbits.json` + `node plugins/O-Strata/tests/orbit-golden.mjs`; Windows = `PLUGIN="${{ inputs.plugin }}"`, artefact folder and log name parameterised. YAML sketch in report C §6.3. Dispatch `gh workflow run ci-tests.yml -f plugin=O-Strata`; record the run URL + `headSha` = the freeze commit (O-Octagon's evidence rule).
- **Wall-clock rows inside `all`** (memory `pattern_wallclock_inside_a_stability_verdict`): H7 ≤ 12 % (ratio, best of 3), scheduler (c) ≤ 120 ms, import (d) ≤ 100 ms `[VERIFIED: README.md:73, :84, :85]` — decision 12.

### 2.7 Item 7 — MSVC first compile

C++17 from JUCE's `cxx_std_17` (`JUCEUtils.cmake:522`), `/W4` no `/WX`; MSVC sees the 23 `target_sources` TUs + note-expression + BinaryData only (`CMakeLists.txt:21-46`); the harness TU is behind `OUARICON_BUILD_TESTS`, never passed on Windows (`main.cpp:130` `posix_memalign` would not compile). Survey `[MEASURED: token census over Source/**]`:

| Trap | Result |
|---|---|
| C3493 non-static `constexpr` local in a lambda | 10 non-static `constexpr` locals, **none referenced from a lambda** |
| C2440 `SafePointer` init-capture | 6 sites, all hoisted `SafePointer<…> safeThis (this);` (`PluginEditor.cpp:273, 305, 328, 356, 483, 530`); `= juce::Component::SafePointer` = 0 — **Round A's Locate… native must keep the form** |
| C3615 in `constexpr` functions | `chebRowStart`, `orbitK`, `orbitKNominal` — no bare declarations |
| `M_PI`, `<numbers>`, designated init, `ssize_t`, `__builtin_*`, `__attribute__`, VLAs, alt tokens in code, nested `std::array` single-brace, `[[maybe_unused]]` bindings, `long double`, `<unistd.h>`, `%zu`, `<bit>` / `std::span` / `std::lerp` / `std::bit_cast`, `[[likely]]`, `sprintf` … | all 0 |
| `min` / `max` macro, `<cmath>` via transitive headers | JUCE defines `NOMINMAX`; every DSP header without `JuceHeader.h` includes `<cmath>` itself |
| `static_cast<long>` (`TerrainScheduler.cpp:183`) | cosmetic (`ptrdiff_t` when next touched) |
| Non-ASCII bytes | 41 files, **all in comments**, zero in string literals (C4819 warning at most; O-Prism has 32 such files and builds green) — Round A rule: no non-ASCII in C++ literals |
| WebView2 `Options` chain (`PluginEditor.cpp:758-794`) | line for line O-Prism's (`:970-1004`) + 8 combo relays + `terrain-view.js` / PNG routes; no `juce://` hard-coded; PNG served as `image/png` (`:83-131`) like 20+ Windows-released plugins |
| `view-perf.log` path (`PluginEditor.cpp:1030-1032`) | creates `%USERPROFILE%\Library\Logs\O-Strata\` on Windows — runtime cosmetic, `#if JUCE_MAC` in Round B |

Verdict: nothing matches a known trap; the dispatch run is the oracle. Re-run at Round B verify: the C3493 grep, the `SafePointer` grep, a bare-declaration scan of new `constexpr` functions, the non-ASCII-literal grep. Recommend `if(OUARICON_BUILD_TESTS AND APPLE)` at `CMakeLists.txt:118`.

### 2.8 Item 8 — vectorised Chebyshev basis (D4)

- **Today** `[VERIFIED]`: 153-float triangle row-major by n, `chebRowStart (n) = 17n − n(n−1)/2` (`ChebyshevSet.h:50-59`); `clenshaw2D` = inner Clenshaw over m per row → outer over n (`:142-169`); `float chebW[2][kChebCoeffs]` (`TerrainOscillator.h:253`), taper applied in `buildChebWeights` (`.cpp:214-225`). **One evaluation per `scan`** (`:395`) — the feedback path reads `fbY1/fbY2` from the previous sample, a second `scan` only during the ≤ 64-sample shadow crossfade; `px, py` are `jlimit`-clamped just before (`:384-385`); result is `float`. Off-thread consumers (projector fit `ChebyshevProjector.cpp:103`, scheduler readout `TerrainScheduler.cpp:355-363`, view feed `TerrainViewFeed.cpp:325-333`) use the unpadded `set->c` and can keep `clenshaw2D`. Harness: `--gate clenshaw` 1e6 dependency-carried evals, reported only (`main.cpp:1693-1711`); H1 identity = `c = δ₁,₀` through the real path, gain-fitted, ≤ −80 dB, measured −128.3 dB (`:703-735`); H7 Bandlimited row **printed, not checked** (`:1585-1597`); H6 two checks (`:1543-1545`) with identical figures run to run.
- **Bench** `[MEASURED: cheb_bench.cpp, -O3 -arch arm64 no fast-math, best of 5 × 1e6; dep = gateClenshaw loop verbatim; indep = 4096 precomputed pairs]`:

| Evaluator | dep ns | indep ns | vs shipped |
|---|---|---|---|
| `clenshaw2D` (shipped) | 84.9 | 64.4 | 1× |
| (a) recurrence, 1 float acc, 153 products | 102.3 | 57.8 | **slower** |
| (b) 1 double acc | 109.4 | 65.8 | slower |
| (c) per-row dot, unpadded | 38.7 | 31.7 | 2.2× / 2.0× |
| (d2) NEON, rows padded to 20, 2 vector accs | 26.7 | 12.2 | 3.2× / 5.3× |
| **(e) portable padded 20, plain C 4-lane** | **29.1** | **18.2** | **2.9× / 3.5×**, bit-identical to (d2) |

- **Accuracy vs double** (100 k points per set): shipped 1.32e-5 / −131.5 dB on the harness random set; (e) 3.5e-6 / −137.7 dB; on projected Sine Product / Radial Rings / Mitsuhashi sets every candidate ≤ 1.7e-7…4.6e-7 (−138…−144 dB); **identity set 0.0 for all eight evaluators** — the H1 row cannot move and is vacuous for D4. Bound to write: `max |chebEvalPadded − clenshaw2D| ≤ 2e-5` on the harness set (measured 1.19e-5, dominated by the shipped Clenshaw's own error), ≤ 1e-6 on a projected set.
- **H7 model**: 1× row = 1.536 M evals / s → 1 ns = 0.154 % delta; ≈ 11.9 % ⇒ ≈ 77 ns per kernel call, near the independent figure because Feedback = 0 on the default patch `[ASSUMPTION — consistent with the three numbers]`. Predicted delta: (c) 6.9 % (misses the ≈ 6.2–6.4 % ceiling), **(e) 4.8 %**, (d2) 3.9 %.
- **Recommendation**: ship (e) as `chebEvalPadded (const float* cp, float x, float y) noexcept` — `kChebPadRow = 20`, `alignas (16) float chebW[2][340]`, `buildChebWeights` writes zeros for the pad by loop index (branch-free on data, DSP-05); `clenshaw2D` stays for the off-thread consumers, no processor switch; `--gate clenshaw` prints both ns figures + `check (maxΔ ≤ 2e-5)` + a projected-set row (≤ 1e-6); H6 band ± 0.5 dB with the same 13 equality / 17 muted rows; H7 Bandlimited row promoted to `check (delta_BL ≤ delta_2x + 2.0)`; NEON path only if the row misses. `-Wconversion -Wsign-conversion -Wshorten-64-to-32` are on the plugin TU — index arithmetic in `int`.

### 2.9 Item 9 — listening pass protocol (QUAL-04)

- **Material** `[VERIFIED: main.cpp:1655-1690; README:77; MEASURED]`: `--gate export` = 6 × 11 × {C2, C4, C6} = 198 WAVs (2 s, defaults, **2× only**, `oscAPhase 0.25`) and **overwrites** the golden without comparing; the golden is **CRLF** — `tr -d '\r' < golden | shasum -a 256 -c` → 198 / 198 OK today. No Bandlimited WAV rows: the ≥ 3 Bandlimited presets are the Bandlimited listening material (or `export` grows a Quality-0 variant — decision). Hosts installed: Logic Pro, Live 12 (and 11), REAPER `[MEASURED: ls /Applications]`. Precedent: O-MicrotonalSampler `4-polish/VERIFICATION.md:110-136` (`# | Test | Verdict | Notes`, `Verifier | User`, deferral rows) — no plugin has signed a per-preset table before.
- **Record**: `stages/4-polish/round-b/LISTENING.md` (VERIFICATION §QUAL-04 carries counts + link); header date / sha / hosts / device; verdicts **PASS · NOTE** (to CHANGELOG Known limits) **· CHANGE** (defaults change → §5.4 commands) **· FAIL** (bug → full Decision 46 list). **Table A** every preset × Logic (AU) + Live 12 (VST3), Bandlimited ones also with a held chord above C6. **Table B** the 66 C4 rows + every C2 / C6 of a gate-flagged pair (H6 2× worst Mitsuhashi × Epi 7 −70.7 dB, H2 worst Radial Rings × Ellipse −2.3 dB, any C4 NOTE / CHANGE, the Bandlimited-muted pairs' 2× rows) — `afplay` loop or a Logic track. **Table C** the Stage 3 human rows 1–6 folded in. Sitting: Standalone (row 2 + ⌥-click ×3) → Logic (Table A, ⌘Z, ⌥-click ×3) → Live 12 (Table A) → WAV grid.
- **Library defaults** (constants, never parameters) and what each re-opens — §5.4 table: Orbits.h `:194 / 197 / 205 / 216 / 225 / 231` (also the JS mirror `terrain-view.js:102-120, 142` + orbit golden), `Terrains.h:95-131` (also the `scheduler` fit table → ARCH rows), damp law `TerrainOscillator.cpp:311-312`, clamp `:372 / 376`, size `:358`, saturation `:419`; the taper law is the contract (D4). One CHANGE per commit.
- **PERF-03 step + `gl.finish()`** `[VERIFIED: index.html:5018-5025; PluginEditor.cpp:591-594, 1025-1032; terrain-view.js:292-315, 685]`: ⌥-click the HUD ×3 → one JSON line per report `{mode, dpr, w, h, n, mean, max, submitMean, burstMean}`; pass `mean ≤ 2` at `mode: "webgl2"`, DPR 2, Standalone + Logic. `gl.finish()` is the `finishFn` passed by `makeSceneGL` into `instrument`, called after every big-view draw; a flag = module-level `perfOptions.finishPerFrame` read in `scene.render`, burst finish unconditional, `finish` field added to the log line, `setFinish` on `window.__strataPerf`. Rule: row 1 lands before Round B plans; ≤ 2 ms and no jank → nothing; else add the flag, off in Release, record both figures.

### 2.10 Item 10 — docs and install

- **Today** `[VERIFIED]`: `CHANGELOG.md` = `## v1.0.0 (unreleased)` with reverse-chronological stage entries and "Known limits — Bandlimited mode"; `NOTES.md` says Stage 1, timeline ends 2026-09-10, Known Issues = Init-only bank + `stereoWidth` no UI binding, nothing on attribution; `PLUGINS.md:52` row as in STATUS. Attribution facts: ARCH Decision 10 clean-room (Mitsuhashi 1982 via Mills & de Souza 1999, standard curves; *Terrain* GPL-3.0 inspiration only, no code) and §Licence AGPL-3.0-or-later (`ARCHITECTURE.md:475-476, 511-512`; `Terrains.h:24-30`; SPDX headers everywhere); the botanical plate is the O-Prism-inherited asset (credit line as O-Prism's NOTES `[ASSUMPTION — copy at write time]`).
- **Stage 0 decisions to state** (`ARCHITECTURE.md:460-479`): D5 constant +1-sample latency on top of the distortion oversampler's figure; D6 no wavetable mode (baked sources v1.1); D8 2× is the default Quality, Bandlimited the showcase; D11 PERF-02 = oscillator delta (already in REQUIREMENTS — record as done). Lift the Stage 2 Round B "Known limits — Bandlimited mode" paragraph; add the two D4 deferrals as v1.1 (REQUIREMENTS `:168-169`).
- **Templates**: O-Emulator `## [1.0.0] — 2026-08-21` (prose + `### Added` + Validation line, `O-Emulator/CHANGELOG.md:429-470`); O-Octagon's CI bullet (`CHANGELOG.md:1806-1857`) and the named-deferral sentence **"Named deferral — owner none, blocked on hardware. No human sees the Windows UI this milestone."** (`O-Octagon/.planning/stages/4-polish/SUMMARY-4.1.md:218`; REQUIREMENTS `:1112-1114`) — reuse verbatim for UI-01 / FUNC-07 / PERF-03 WebView2 halves.
- **Outlines** (report C §10.4–10.5): CHANGELOG `## v1.0.0 (date)` tagline → `### Added` (oscillator, Bandlimited, PNG import + persistence + Locate…, bank, 3D view, tuning inherited, i18n status, CI dispatch URL) → `### Behaviour to know` (D5 / D6 / D8) → `### Known limits` (Bandlimited, Windows deferral, PERF-03 figure or "pending row 1", path form on another machine) → `### Technical notes` (harness counts, D4 before / after, validation) → `### Stage history` (existing entries collapsed, not deleted). NOTES: Status 📦 Installed (not published), timeline + Stage 2 / 3 / 4 rows, Attribution & licence, Known Issues, Human rows still open, v1.1 list.
- **Install**: `./scripts/build-and-install.sh O-Strata` — Phase 1 resolves `O-Strata` (`:233-237`), Phase 4 sweeps `O-Strata` ↔ `O-Strata-dev` VST3 + AU with the ⚠ warning (`:351-400`), Phase 5 clears the AU cache (`:496, :526-532`); then `auval -a | grep -i strata` twice (memory `pattern_cold_auval_after_install_rescans_registry`) and `auval -v aumu OuSt OuDv`; the Standalone is not installed by the script (`pattern_build_install_skips_standalone_stale_ui`) — hands-on rows use `build/…/O-Strata-dev.app`. PLUGINS.md target `| O-Strata | 📦 Installed | 1.0.0 | Synth (Microtonal Wave Terrain) | date |` + the duplicate check.
- **Human-row checklist** to close at Round B verify (verbatim in report C §10.7): Stage 3 rows 1–6 (`3-gui/VERIFICATION.md:84-93`) incl. Round A rows 3–10 (`round-a/VERIFICATION.md` §Human), plus Round A / B additions: Locate… by hand (missing → notice → Locate… → map restored; mismatch refused), a PNG preset round-tripping through Logic save / reload, LISTENING.md, Windows rows as named deferrals unless a machine appears.

---

## 3. Corrections to the CONTEXT (recorded, contracts not edited)

| # | CONTEXT says | Finding |
|---|---|---|
| C1 | Finding 2(a): with Terrain = Imported… the oscillator "follows the voice's set rule meanwhile (analytic fallback …), so there is no silent gap"; inherited row + ROADMAP 4.1: "Sine Product fallback" | **Silent** on every quality path when `image == nullptr` `[VERIFIED: TerrainOscillator.cpp:397, :258-266; .h:134]`; ≈ 150 ms gap per restore; the fallback is **Round A DSP work** (a `TerrainOscillator` branch), pinned by an H10 row |
| C2 | Inherited "Persistence layout": `imageGeneration++` on load | `imageGeneration` is bumped by `publishImage`; the load bumps `importRevision` via `importTerrainImage` and the scheduler publishes — nothing to add by hand |
| C3 | Finding 2 / item 2: place the `stateGeneration` bump after the import is queued | Already import-then-bump on both paths (`PluginProcessor.cpp:1285`; `PluginEditor.cpp:645-676`); the async publish gap is closed by the `imageGeneration`-keyed re-push. **Nothing moves** |
| C4 | Item 2 (implicit): `customLoad` runs on every preset apply | Only when the JSON has `customState` `[VERIFIED: OuariconPresetManager.h:273]` — slots leak across presets without a fix (§2.2) |
| C5 | Finding 3: the Locate… button "needs its own key **and a tip row**" | No terrain button has a tip (`#btn-terImport` unbound); `check-i18n` does not require one — a tip is a choice |
| C6 | Finding 4: "the harness prints the eight ±0.1 neighbours" for a failing preset | Grid-only, around the *default* centre; the preset loop prints PASS / FAIL alone. Plus two blind spots: f0 ignores `oscACoarse / Fine`, `cleanPatch()` zeroes osc B after the preset |
| C7 | Finding 4 / item 4: the bank regenerates on a version bump; "the dropdown already groups by category through the manager" | `.factory-version` already reads `1.0.0` → nothing regenerates; a content stamp + `deleteRecursively` is needed. Grouping exists but `CATEGORY_ORDER` lacks `Init` (sorts after User) and headers are not localised |
| C8 | Item 5: smoke [5] just needs a re-run | Asserts exactly `Init/Init.json` + two files + `.factory-version == 1.0.0` `[VERIFIED: main.cpp:539-549]` — rewrite for the bank + stamp |
| C9 | Finding 7: `resolve-target.sh` "warns and falls back to the folder name for O-Strata"; "harness hard-codes the dev manufacturer code" | Resolves `O-Strata` cleanly, exit 0, no stderr `[MEASURED]`; the code is derived from the target property (`ParamDump.cmake:33-36, :206`). Never passing `OUARICON_RELEASE` still stands |
| C10 | Finding 7: `--gate all` ≈ 110 s | 111.5 s locally; on the runner expect 130–220 s, and three rows carry wall-clock verdicts |
| C11 | Item 7: "the harness TU being macOS-only (no MSVC)" | macOS-only by **omission** (no `OUARICON_BUILD_TESTS` on Windows), not by an `APPLE` guard; `posix_memalign` at `main.cpp:130` |
| C12 | Finding 5 / D4: "153 independent FMAs … est. 2–3×"; "reproduce it to the H1 identity floor (−128.3 dB)"; "every H6 figure inside the run-to-run band" | The literal form is **slower** (102 vs 85 ns); the win needs per-row sums + a padded 17 × 20 copy (`chebW` 153 → 340 floats). The identity set is bit-exact for any evaluator — the row is vacuous; bound = `max |Δ| ≤ 2e-5`. The H6 band is zero width today — define ± 0.5 dB |
| C13 | Listening material: "the 198-file terrain × orbit grid" covers Bandlimited | 2× only; Bandlimited has no WAV rows. `golden/round-a-grid.sha256` is CRLF; `--gate export` re-records without comparing |
| C14 | Finding 10 / ROADMAP 4.2: PLUGINS.md → "✅ Working / 📦 Installed" | D3 fixes it at **📦 Installed** |
| C15 | `ci-tests.yml` header: "eleven other harnesses" | 37 plugins carry `OUARICON_BUILD_TESTS` today; harmless (SKIP excludes them), refresh when edited |

---

## 4. Decisions the plan must take (with recommendations)

**Round A (Phase 4.1)**

| # | Decision | Recommendation |
|---|---|---|
| 1 | State child layout | One `terrainImports` with `<slot osc form name sha256 size data\|path/>`, slots only where an import exists; same fields as `customState.slots[]`; `var ↔ ValueTree` adapters; record the layout in ARCH "State Persistence" |
| 2 | Base64 | `juce::Base64::toBase64` / `convertFromBase64` on both sides; never `MemoryBlock::toBase64Encoding`; memoise the string per slot revision (optional) |
| 3 | Cap constant + form | `kMaxImportBytes` moves to the processor; form by size at save time; bytes form stores no path; absolute path documented in Known limits |
| 4 | Fallback sound | New `TerrainOscillator` branch: Imported… with `image == nullptr` → Sine Product on every quality path; `sourceMissing` in `ImportSlot` → status; pinned by H10 (b) (non-silent, SHA == Sine Product instance) |
| 5 | No-`customState` preset | Patch the O-Strata manager copy to call `customLoad (var())` when the key is absent (file preset-manager v1.0.7); fallback = `customState = {}` on every factory def; `clearImportSlot` resets `importRevision` to 0 and retires the image |
| 6 | Thread guard on restore | Inline when `MessageManager::existsAndIsCurrentThread()`, else `callAsync` with an alive token; keep `importTerrainImage`'s assert |
| 7 | Locate… | Button inside the notice (span for the label, `.transient` hides the button), new native `locateTerrainImage (osc)` with the hoisted `SafePointer` form, **refuse on SHA mismatch** (`label.locateMismatch`, transient), located file re-enters the size rule; keys `label.locate` / `label.locateMismatch` / `tip.terLocate` (+ optional `tip.terImport`), fr drafts §2.3, zh `'mt'` + back-translate; pin `#btn-terLocate`; state `terrain-locate-mismatch`; stub override |
| 8 | Orphan bank | Stamp `JucePlugin_VersionString + "+" + sha256(bank)[0:12]`, `Factory/.deleteRecursively()` on mismatch, bank built every launch; smoke [5] rewritten (N + 1 files, stamp prefix `1.0.0+`) |
| 9 | Authoring + content | O-Prism `RawMap → merge → convertTo0to1` pattern; the 18 presets of §5.1 with Squarcle Storm at (0.23, 0.21); ASCII names; categories Init, Pads, Drone, Lead, Bass, Pluck, Keys, Sequence, FX with `'Init'` prepended to `CATEGORY_ORDER`; category strings stay English like file names; add a `description` property to the JSON (harmless to the loader) for the Bohlen-Pierce and drop-a-PNG notes — or CHANGELOG only |
| 10 | H2 harness | f0 corrected for `oscACoarse / Fine`; B-oscillator pass when `oscBLevel > 0`; neighbour print around the preset's centre; `--real id=eng` + `--gate h2cli` upstreamed; contract stays "C4, osc A (and B when present)" — the C3 / C5 rows are informational |
| 11 | H10 rows | (a)–(e) of §2.5; public `save/loadTerrainImportsVar` + tree twins; `makeNoisePng` fixture with a size assertion; ≈ +3 s on `all` |

**Round B (Phase 4.2)**

| # | Decision | Recommendation |
|---|---|---|
| 12 | `ci-tests.yml` | `plugin` `choice` input (`default: O-Octagon`) + per-plugin `if:` steps; orbit golden as a third O-Strata step; header claims list kept verbatim + a two-plugin gate statement + the stale count refreshed; wall-clock rows: **accept and re-dispatch on a timing miss** for v1.0 (a `STRATA_CI=1` band is v1.1); `if(OUARICON_BUILD_TESTS AND APPLE)`; dispatch command + run URL + `headSha` recorded in the verify |
| 13 | Evaluator (D4) | **(e) portable padded** `chebEvalPadded`, `chebW` 17 × 20; `clenshaw2D` kept for the off-thread consumers, no processor switch; `--gate clenshaw` = both ns figures + `check (maxΔ ≤ 2e-5)` + projected-set row ≤ 1e-6; H6 band ± 0.5 dB with the same equality / muted sets; H1 identity row identical; H9 Bandlimited row 0.000e+00; H7 Bandlimited row promoted to `check (delta_BL ≤ delta_2x + 2.0)`; NEON only if it misses |
| 14 | MSVC sweep | The four greps of §2.7 as a Round B verify row after Round A's native lands; no non-ASCII in C++ literals; `#if JUCE_MAC` on the `view-perf.log` path |
| 15 | Listening record | `round-b/LISTENING.md` Tables A / B / C, verifier Taylor, verdict vocabulary PASS / NOTE / CHANGE / FAIL; Bandlimited via the presets (no Quality-0 export); one CHANGE per commit with the §5.4 command list; sitting order Standalone → Logic → Live 12 → WAV grid |
| 16 | `gl.finish()` flag | Conditional on Stage 3 human row 1's number (≤ 2 ms + no jank → nothing; else `perfOptions.finishPerFrame`, off in Release, `finish` field in the log) |
| 17 | CHANGELOG / NOTES / PLUGINS.md | O-Emulator `[1.0.0] — date` shape with the existing stage entries collapsed under "Stage history"; O-Octagon's deferral sentence verbatim in CHANGELOG, REQUIREMENTS (UI-01 / FUNC-07 / PERF-03) and NOTES; PERF-03 = the WKWebView number or "pending, human row 1"; PLUGINS.md 📦 Installed; dated, no tag (D3) |

No blocking question. Taylor-facing: the PERF-03 ⌥-click row (needed before Round B plans the flag); the hash-mismatch policy (refuse) if a different feel is wanted; whether a Windows machine appears before Round B verify.

---

## 5. Fixtures and run lists the planner can lift

### 5.1 Draft factory bank — 18 presets, engineering units, unlisted = defaults (measured H2 at C4 through the scratch harness; centre (0.13, 0.21) unless stated)

| # | Name | Cat | Terrain × Orbit | F | Key values | H2 worst / windows |
|---|---|---|---|---|---|---|
| 1 | Init | Init | SP × Ellipse | 1.0 | all defaults | 0.0 dB / 100 % |
| 2 | Breathing Pad *(use case 1)* | Pads | SP × Ellipse | 1.0 | LFO1 0.08 Hz→OscA Size +0.35; LFO2 0.05 Hz→OscA CX +0.15 (keep ≤ 0.15); osc B SP × Ellipse level 0.5, Fine +6 c, centre (−0.17, 0.24); amp 1.5/1.0/0.9/3.0; filtA LP24 4 kHz; chorus 0.25; reverb 0.35 size 0.7 | −1.9 / 100 |
| 3 | Chatter Lead *(use case 2)* | Lead | RC × Epi5 | 1.0 | Feedback 0.4 Damp 0.3; ModWheel→OscA TerFreq +0.4; amp 0.005/0.3/0.8/0.3; filtA LP24 8 kHz res 0.2; delay 1/8D mix 0.2 fb 0.35 | 0.0 / 100 |
| 4 | Pierce Bell *(use case 3)* | Keys | CW × Hypo3 | 1.5 | **Bandlimited**; amp 0.002/2.5/0.0/2.5; filtA LP24 12 kHz; reverb 0.3 size 0.6; description "Tuning tab → Bohlen-Pierce" | 0.0 / 100 |
| 5 | Your Terrain Here *(use case 4)* | FX | Mits × Limaçon | 1.0 | Blur 0.2, Edge Mirror, OrbMod 0.5; description "drop a greyscale PNG on the terrain view — the preset keeps it" | 0.0 / 100 |
| 6 | PD Organ *(use case 5)* | Keys | Saddle × Squarcle | 1.0 | FilterEnv→OscA Rot +0.5 (90° sweep); filt env 0.01/0.4/0.3/0.5; amp 0.005/0.3/1.0/0.08; chorus 0.3 rate 0.8 | −3.3 / 100 |
| 7 | Radial Keys | Keys | RR × Superellipse | 1.5 | Sat 0.2; amp 0.003/1.2/0.4/0.6; filtA LP24 6 kHz keytrack 0.5 envDepth 0.3 | 0.0 / 100 |
| 8 | Folded Bass | Bass | RC × Hypo5 | 0.7 | Feedback 0.25 Damp 0.6; Coarse −12; sub −1 oct 0.3; filtA LP24 1.2 kHz envDepth 0.4 decay 0.3; amp 0.003/0.4/0.7/0.2 | 0.0 / 100 at the played pitch (needs the Coarse fix) |
| 9 | Wells Drone | Drone | CW × Epi7 | 2.0 | **Bandlimited**; LFO1 0.03 Hz→OscA TerModY +0.3; amp 3/1/1/4; reverb 0.45 size 0.85; osc B CW × Ellipse level 0.4 Coarse −12 | 0.0 / 100 |
| 10 | Saddle Pluck | Pluck | Saddle × Hypo7 | 3.0 | amp 0.001/0.5/0/0.4; filtA LP24 3 kHz envDepth 0.6 decay 0.25; Velocity→FiltA Cut +0.3 | 0.0 / 100 |
| 11 | Butterfly Choir | Pads | Mits × Butterfly | 1.0 | Unison 3 Detune 0.3 Width 0.8; amp 1.2/1/0.9/2.5; chorus 0.35; reverb 0.4 | −4.5 / 100 (closest to −6) |
| 12 | Limacon Lead | Lead | SP × Limaçon | 2.0 | OrbMod 0.7 Sat 0.4; dist Soft drive 0.3 mix 0.4; delay 1/4 mix 0.15; amp 0.01/0.3/0.9/0.25 | 0.0 / 100 |
| 13 | Squarcle Storm | FX | RR × Squarcle | 1.0 | Feedback 0.6 Damp 0.2; **centre (0.23, 0.21)**; LFO1 S&H 4 Hz→OscA CY +0.05; delay ping-pong 0.3; dist 0.3 | FAIL at default centre −6.9 / 0 → **0.0 / 100** at (0.23, 0.21) |
| 14 | Glass Rings | Keys | RR × Hypo3 | 2.5 | **Bandlimited**; amp 0.002/1.8/0/1.8; reverb 0.35 | 0.0 / 100 |
| 15 | Mitsuhashi Organ | Keys | Mits × Ellipse | 1.0 | **centre (0, 0), Aspect 1.0** — the deliberate centred case (Mitsuhashi is odd → odd harmonics, h1 present); amp 0.005/0.3/1.0/0.08; chorus 0.2 | 0.0 / 100 |
| 16 | Superellipse Bass | Bass | CW × Superellipse | 1.0 | Coarse −12; OrbMod 0.3; filtA LP24 900 Hz envDepth 0.5; sub 0.25 | 0.0 / 100 at the played pitch |
| 17 | Epi Keys | Keys | SP × Epi3 | 1.5 | OrbMod 0.6; amp 0.003/1.5/0.3/0.5; Velocity→OscA Orbit Mod +0.3; chorus 0.2 | 0.0 / 100 |
| 18 | Saddle Sweep | Sequence | Saddle × Epi7 | 1.0 | LFO1 0.2 Hz (sync 1/2)→OscA TerFreq +0.3; amp 0.005/0.3/0.5/0.2; delay 1/8 mix 0.25 | 0.0 / 100 |

Coverage (preset numbers): Sine Product 1, 2, 12, 17 · Radial Rings 7, 13, 14 · Saddle 6, 10, 18 · Ridged Cosines 3, 8 · Mitsuhashi 5, 11, 15 · Cosine Wells 4, 9, 16; all 11 orbits; feedback 3, 8, 13; Bandlimited 4, 9, 14. `[MEASURED: h2-batch.log]`

### 5.2 H10 skeletons (harness + processor)
```cpp
// harness
juce::MemoryBlock makeNoisePng (int side, bool rgb);      // incompressible, size asserted 1.5–2 MiB
juce::String renderShaOf (Instance& in);                  // tapRender 1 s C4 → SHA-256 hex
std::unique_ptr<juce::XmlElement> stateXml (const juce::MemoryBlock& blob);   // strip the 9-byte frame (as smoke [3])
void gateH10();                                           // today's row + (a)…(e)
// processor (public, message thread)
juce::var       saveTerrainImportsVar() const;            // both forms, slots with an import only
void            loadTerrainImportsVar (const juce::var&); // clears both slots first; path form → SHA check → sourceMissing
juce::ValueTree saveTerrainImportsTree() const;  void loadTerrainImportsTree (const juce::ValueTree&);
void            clearImportSlot (int osc);
static constexpr juce::int64 kMaxImportBytes = 2 * 1024 * 1024;   // moved from PluginEditor.h:123
```

### 5.3 Gate run lists
- **Round A verify**: `--gate all` (≤ 3 min; H2 `presets: 18`, both-osc rows, H10 (a)–(e), smoke [5] with the stamp), `--gate H2` neighbour print on the bank, `check-ui-labels` over default + 42 (the new `terrain-locate-mismatch` state), `ui_layout_check.js` (button inside the notice in fr / zh), `check-i18n`, `i18n-fr-lint` / `i18n-zh-lint`, `boot-all-uis --strict-tips` (no `stub invented:` — `locateTerrainImage` overridden), tip gate with the new binding count, `ui_shell_diff_check.js` 0 px, pluginval ×2 + auval, `params.tsv` diff empty, warning census 0 new, the MSVC `SafePointer` grep, `ls -R ~/Library/O-Strata/Presets` (no `Init/` orphan, N + 1 files), `git tag -l '*Strata*'` empty.
- **Round B verify**: the same list + `--gate clenshaw` (three lines, `maxΔ` check), H6 ± 0.5 dB band, H7 Bandlimited `check`, H1 / H9 unchanged, `--gate orbits` + `orbit-golden.mjs`, `gh workflow run ci-tests.yml -f plugin=O-Strata` green (run URL + `headSha`), the four MSVC greps, `build-and-install.sh O-Strata` + `auval -a | grep -i strata` ×2 + `auval -v aumu OuSt OuDv`, PLUGINS.md duplicate check, LISTENING.md counts, human-row checklist.

### 5.4 Defaults-change commands (after any CHANGE in the listening pass)
```
H=/Users/taylorbrook/Dev/VST-development/build/plugins/O-Strata/O-Strata-render-test_artefacts/Release/O-Strata-render-test
ninja -C /Users/taylorbrook/Dev/VST-development/build O-Strata-render-test O-Strata_Standalone O-Strata_VST3 O-Strata_AU
$H --gate H2 --gate H3 --gate H6 --gate H7            # + --gate scheduler for Terrains.h, --gate saturation for :419
$H --gate orbits --out /tmp/orbits.json && node plugins/O-Strata/tests/orbit-golden.mjs /tmp/orbits.json   # Orbits.h / :358 only, after porting the JS mirror
$H --gate export                                       # re-records tests/exports + golden/round-a-grid.sha256 (not in `all`)
git diff --stat -- plugins/O-Strata/tests/render-harness/golden/round-a-grid.sha256   # rows for the changed terrain / orbit only; empty for a damp-law change
(cd plugins/O-Strata/tests/exports && tr -d '\r' < ../render-harness/golden/round-a-grid.sha256 | shasum -a 256 -c - | grep -vc ': OK')   # expect 0
$H --gate all
```
Then re-listen the touched Table A / B rows and commit code + golden together (`git commit -- plugins/O-Strata`), one CHANGE per commit.

---

## 6. References

- CONTEXT: `stages/4-polish/CONTEXT.md` (D1–D4, findings 1–11, items 1–10); `ROADMAP.md` v2 Stage 4; `REQUIREMENTS.md` v2.0.0 (FUNC-08, FUNC-11, QUAL-03, QUAL-04, COMPAT-02, UI-01 / FUNC-07 / PERF-03 rows, §Out of Scope `:168-169`); `research/ARCHITECTURE.md` ("State Persistence" `:238`, Decisions 5 / 6 / 8 / 10 / 11 `:460-479`, §Licence `:511-512`); `BRIEF.md` §Use Cases
- Source: `PluginProcessor.{h,cpp}` (`ImportSlot` `:205-217`, import API `:1179-1210`, `getTerrainStatus :1165`, state `:1214-1287`, ctor `:566-577`, TerFreq range `:135-138`), `PluginEditor.{h,cpp}` (`kMaxImportBytes` `.h:123`, choosers `:273-556`, natives `:639-684`, Options chain `:758-794`, `reportViewPerf :1025-1032`), `OuariconPresetManager.h` (`:54-67, :129-130, :209-276, :312-325, :371-385, :404-453, :476-512, :514-604`), `FactoryPresets.{h,cpp}`, `TerrainViewFeed.cpp:201-213`, `dsp/TerrainOscillator.{h,cpp}` (`:258-266, :311-312, :349-419`; `.h:134, :253`), `dsp/TerrainScheduler.{h,cpp}` (`:446-470, :557-623`; `.h:75-77, :124`), `dsp/ChebyshevSet.h:50-169`, `dsp/ChebyshevProjector.cpp:103`, `dsp/Terrains.h:46-131`, `dsp/Orbits.h:49-231`, `dsp/ModulationMatrix.h:38-113`, `StrataVoice.cpp:207-220`, `ui/public/index.html` (`:1596-1643, :2417, :2891-3042, :4988-5168`), `ui/public/js/i18n.js:2379-2389`, `ui/public/js/terrain-view.js:102-142, :292-315, :685`, `CMakeLists.txt:16-147`
- Harness: `tests/render-harness/main.cpp` (`:210-285, :493-551, :703-845, :1543-1711, :2123-2189, :2358-2403`), `README.md:38-85`, `golden/round-a-grid.sha256`, `tests/orbit-golden.mjs`, `tests/i18n-states.json:28-36`, `tests/ui_layout_check.js:199-203`, `tests/ui-stub/generic-overrides.json`
- CI / scripts: `.github/workflows/ci-tests.yml`, `build-and-release.yml`, `.github/juce-version.txt`, `vendored/JUCE-overrides/`, `scripts/resolve-target.sh:42-47`, `scripts/param-dump/ParamDump.cmake:33-36, :206`, `scripts/build-and-install.sh:233-237, :351-400, :496-532`, `scripts/check-i18n.js:35-38, :627-659`; O-Octagon run 32905553745, O-Prism run 34302666294
- Precedents: O-Prism `FactoryPresets.cpp:38-185`, `PluginEditor.cpp:970-1004`; O-Bells `PluginProcessor.cpp:585-616`; O-Contrabass `:240-262`; O-Lyrica `:601`; O-MicrotonalSampler `CHANGELOG.md:808-816, :3082-3086`, `4-polish/VERIFICATION.md:110-136`; O-Emulator `CHANGELOG.md:429-470`; O-Octagon `CHANGELOG.md:1806-1857`, `4-polish/SUMMARY-4.1.md:218`, `REQUIREMENTS.md:1112-1114`, `VERIFICATION-4.1.md:48`
- JUCE 8.0.14: `juce_core/text/juce_Base64.h:50-62`, `juce_core/memory/juce_MemoryBlock.h:263-281`, `juce_cryptography/hashing/juce_SHA256.h:71-102`, `juce_core/native/juce_BasicNativeHeaders.h:98`, `extras/Build/CMake/JUCEUtils.cmake:522`, `JUCEHelperTargets.cmake:50`
- Memory: `critical_valuetree_xml_roundtrip_loses_type`, `pattern_factory_preset_normalized_ignores_skew`, `critical_preset_name_slash_path_separator`, `pattern_grouping_preset_dropdown_breaks_prev_next`, `pattern_preset_migration_per_param_version_gate`, `pattern_js_state_updater_overwrites_html_labels`, `pattern_language_width_pins_content_sized_boxes_move`, `critical_msvc_constexpr_lambda_capture`, `critical_msvc_safepointer_init_capture_nested_lambda`, `critical_msvc_c3615_uninitialised_constexpr_local`, `critical_juce_string_char_ctor_is_ascii_only`, `critical_webview_resource_provider_and_schemes`, `critical_webview2_static_linking`, `pattern_wallclock_inside_a_stability_verdict`, `pattern_release_tag_format_must_match_ci_glob`, `feedback_never_tag_unless_publish`, `build_script_target_name_vs_folder`, `pattern_cold_auval_after_install_rescans_registry`, `pattern_build_install_skips_standalone_stale_ui`, `pattern_golden_tracked_as_checksum_only`, `pattern_reanchor_cross_version_digest_probe`
- Investigator reports (session scratchpad `strata-r4/`): `A-persistence.md`, `B-factory-bank.md`, `C-ci-msvc-docs.md`, `D-chebyshev-listening.md` + `bench.cpp`, `main_h2cli.cpp` / `h2-batch.log`, `cheb_bench.cpp` / `bench_O3_final.txt`
