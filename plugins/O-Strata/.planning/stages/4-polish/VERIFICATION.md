# Stage 4: Polish / Validation — Round B (Phase 4.2: D4 evaluator, CI Windows, listening material, v1.0.0) — Verification

## Verification Date

2026-09-14

**Plugin:** O-Strata · **Stage:** 4 of 4 (Polish) · **Round:** B = ROADMAP Phase 4.2 (CONTEXT D1) — **this file is the Stage 4 verdict**; `round-a/VERIFICATION.md` is the Round A verdict · **Mode:** manual
**Inputs:** `CONTEXT.md` (D1–D4, Findings 1–11, requirements map), `RESEARCH.md` (§5.3 Round B gate list), `PLAN.md` (12 tasks / 4 waves, Decisions 27–45, gates B1–B20, success criteria, requirements traceability), `SUMMARY.md` (execute report, commits `76a90874` · `36a6c1d4` · `5e044fd1` · `dcbc609a` · `005cbcf7`; 7 deviations), `round-a/VERIFICATION.md` (Issue 1 — the S&H LFO), `REQUIREMENTS.md` v2.0.0.
**Method:** every SUMMARY figure was re-measured in this session from the committed tree at `005cbcf7` (`git status --short` clean under `plugins/O-Strata`, `modules/`, `PLUGINS.md`, `.github/`). A **forced recompile** of the seven Round B translation units reproduced a VST3 binary bit-identical to the installed `-dev` bundle (sha256 `c86546cd…`, the digest SUMMARY records), so every gate below ran on the release code. Then B1–B20 were re-run from the repo root: the harness (`--gate all`, `clenshaw`, `H7`, `H1`/`H9`, `H2` ×3, `H10`, `smoke --with-disk`, `H8`, `orbits` + golden, `exportPresets`, the grid `shasum -c`), the i18n gates, the Playwright gates, the grep gates, the param-dump diff, the warning census, `auval` + pluginval ×2 on the installed bundles, the on-disk bank, and the three CI runs re-read from `gh` (job conclusions, step conclusions and raw logs — not from SUMMARY). Nothing below is copied from SUMMARY without a fresh run. Machine: Apple M4 Max, macOS 26 / Darwin 25.6, Apple clang 17, node v24, Release build. Logs in the session scratchpad.

> **Measurement note (drives Issue 1).** The first two harness runs of this session landed while `XprotectService` was at 134 % CPU. `--gate all` took **2125.2 s wall-clock against 122.7 s of user CPU** — the process spent ~94 % of that descheduled — and H7's percentages inflated with it (`clenshaw2D` read 163.6–240.7 ns against the expected ~84). Those runs are reported in Issue 1 and **excluded** from the tables below. Every figure in this document is from a quiet machine, confirmed by a sustained single-core control (8 × 0.85–0.87 s, no thermal warning) and by each run's own duty cycle (`--gate all` 108.6 s real / 76.5 s user; H7 27.1 s real / 26.8 s user).

## Goal-Backward Analysis

### Original Goals (CONTEXT.md "Goal" + requirements map, Round B scope per D1; PLAN success criteria)

1. **D4 padded basis evaluator (PERF-02 Bandlimited item):** replace `clenshaw2D` on the audio thread with a padded-layout evaluator that reproduces the Chebyshev contract to the H1 identity floor, leaves every H6 Bandlimited figure inside its band, and brings the H7 Bandlimited delta from ≈ 12 % down to the 2× path's own delta + 2 %.
2. **COMPAT-02 — CI Windows:** `ci-tests.yml` gains a `plugin` dispatch input; a Windows job builds `O-Strata_VST3` under MSVC with WebView2 and runs pluginval strictness 10, and a macOS job builds and runs the harness + the orbit golden — dispatched green from `origin/main`, with the hands-on WebView2 half recorded as a named deferral (Finding 6 ceiling).
3. **Round A Issue 1 — the S&H LFO:** the Squarcle Storm H2 figure made reproducible by seeding the LFO's `juce::Random` from the harness phase seed, with the production clock-seeded path and O-Prism's copy untouched.
4. **QUAL-04 listening material:** `LISTENING.md` Tables A / B / C with every verdict blank, the 198-WAV grid matching its golden by `shasum -c`, and 18 non-silent post-FX preset renders.
5. **Release-ready v1.0.0 (D3):** CHANGELOG / NOTES / ARCHITECTURE / PLUGINS.md current, a local install through `build-and-install.sh` with the dual-variant sweep, pluginval ×2 + auval green on the **installed** bundles, every page and grep gate at its pinned number, `params.tsv` unchanged, 0 new warnings, path-scoped commits, **no tag**.

### Deliverables (SUMMARY.md, confirmed in the tree at `005cbcf7`)

1. `Source/dsp/ChebyshevSet.h`: `kChebPadRow` 20, `kChebPadded` 340, `chebBasis17`, `chebEvalPadded` + the pad-row `static_assert`; `clenshaw2D` retained as the reference / off-thread form. `Source/dsp/TerrainOscillator.h`: `chebW` is `alignas (16) float[2][kChebPadded]`, `buildChebWeights` writes the padded layout on loop indices only; `grep -c clenshaw2D Source/dsp/TerrainOscillator.cpp` = **0** (re-verified).
2. `Source/dsp/LFO.h`: a one-line `seed (juce::int64)`; `StrataVoice.cpp` seeds the four LFOs from `seedFor (2..5)` under `harnessSeed != 0u`. `LFO.cpp` and O-Prism's copy untouched.
3. `plugins/O-Strata/CMakeLists.txt`: `if(OUARICON_BUILD_TESTS AND APPLE)`; `logViewPerf` via `juce::FileLogger::getSystemLogFileFolder()`.
4. Harness (`tests/render-harness/main.cpp` + README): `--gate clenshaw` four lines / three checks, H7's Bandlimited row promoted to a check against the same run's 2× delta + 2.0 with a `machine:` line from `SystemStats::getCpuModel()`, the new `--gate exportPresets`, the shared `writeWav24` helper, and the LF-terminated grid golden.
5. `.github/workflows/ci-tests.yml`: required `plugin` choice input, jobs `probes-macos` / `windows-vst3`, the O-Octagon steps byte-identical behind `if:`, the shared steps parameterised, the two-plugin CAN / CANNOT contract block, and (`dcbc609a`) `if: ${{ !cancelled() && … }}` on the orbit golden so it no longer skips when `--gate all` fails.
6. `stages/4-polish/LISTENING.md`: Tables A (18 presets × Logic / Live 12 + 3 Bandlimited chord cells), B (66 C4 rows + 19 flagged pairs), C (15 human rows), the sitting order, the run-the-material block and the Decision 38 CHANGE protocol.
7. Docs: `CHANGELOG.md` `## v1.0.0 — 2026-09-13` in the O-Emulator shape, `NOTES.md` with **Attribution & licence** and the v1.1 list, `research/ARCHITECTURE.md:342-346` the D4 evaluator paragraph, `PLUGINS.md` → 📦 Installed. `REQUIREMENTS.md` untouched by the execute phase (`git diff --name-only 26599292 005cbcf7 -- …/REQUIREMENTS.md` empty).

### Goal Achievement

| Goal | Status | Evidence (re-measured 2026-09-14) |
|---|---|---|
| 1 D4 padded evaluator | ✅ Achieved | `--gate clenshaw`: `clenshaw2D` **81.4 ns** / `chebEvalPadded` **28.0 ns** (**2.91×**; 2.8–3.4× across the session's runs); random maxΔ **9.775e-06** ≤ 2e-5, projected **2.980e-07** ≤ 1e-6, double-reference 3.41e-06 / 2.49e-07 — **character-identical to SUMMARY on every run, contended or quiet**. `--gate H1` Chebyshev identity **−128.3 dB** (the pre-D4 floor, unmoved); `--gate H9` Bandlimited pairwise **0.000e+00**. H6 within band: 2× **198 / 198** rows ≤ −60 dB, worst **−70.7 dB** at Mitsuhashi × Epitrochoid 7 A2; Bandlimited **127 / 127** sounding rows ≤ −90 dB, worst **−98.4 dB**, **17** muted by truncation (the same 17-row list), `h_max ≤ D_max·K` with **equality on 13**. `--gate H7` on a quiet machine: 2× delta **4.13 %** (≤ 12), Bandlimited **4.24 % ≤ 4.13 + 2.0 PASS**, `machine: Apple M4 Max (Release build)` |
| 2 COMPAT-02 CI Windows | ✅ Achieved (at the Finding 6 ceiling) | Run 3 [34809565500](https://github.com/taylorbrook/O-Audio-VST-Development/actions/runs/34809565500), `headSha` `dcbc609a`, re-read from `gh`: **`windows-vst3` conclusion `success`, every one of its 13 steps `success`** — MSVC compiled the plugin TUs (only C4458 hides-member warnings from the VST3 SDK / NE module), WebView2 linked, the VST3 was produced with its `moduleinfo.json`, and pluginval 10 completed **Editor**, **Open editor whilst processing**, **Automation**, **Editor Automation** and **Fuzz parameters** with **0 `FAIL` lines in 191 log lines**. The step's `shell: bash` on `windows-latest` is `bash -e -o pipefail`, so `pluginval.exe … \| tee` cannot mask a nonzero exit: green = exit 0. `probes-macos` built the harness and ran **162 checks — the same count as local** — and the orbit golden **44 / 44 within 1e-4, worst 2.93e-6, the same figure as local** |
| 3 S&H LFO determinism (Round A Issue 1) | ✅ Achieved — **Issue 1 closed** | `--gate H2` ×3: **20 / 20 preset rows** on every run; every measurement row identical across the three runs (the sole textual difference is the trailing wall-clock line, 2.7 / 2.8 / 2.7 s — Issue 2). **Squarcle Storm −3.6 dB / 100 % of windows on all three runs**, against Round A's −2.2 / −2.8 / −4.0 spread. `--gate all` carries the same 20 / 20 with `presets: 18` |
| 4 QUAL-04 material | ✅ Material ready · ⏳ **verdicts unfilled — the sitting is Taylor's** | `LISTENING.md` re-counted from the file: Table A **18 data rows** with **39 blank verdict cells** (18 Logic + 18 Live 12 + 3 Bandlimited chord cells; the other 15 chord cells are "—"), Table B **85 rows** (66 + 19), Table C **15 rows**. Grid: `(cd tests/exports && shasum -a 256 -c ../render-harness/golden/round-a-grid.sha256)` → **0 non-`OK` lines with no `tr`**, golden **0 CR bytes** over 198 lines. `--gate exportPresets` re-run: **18 files, 0 silent** (4 checks PASS), every file **864 104 B** = exactly 3 s, and the grid golden still clean afterwards |
| 5 release-ready v1.0.0 | ✅ Achieved | Table below — every B1–B20 threshold met. Installed = built (`diff -r` identical both formats, VST3 `c86546cd…` / AU `89ba772f…`), pluginval 10 **`SUCCESS`** on both installed bundles, `auval -v aumu OuSt OuDv` **AU VALIDATION SUCCEEDED**, `auval -a` lists the triple **once**. On-disk bank 18 JSONs / 9 folders / stamp `1.0.0+18c17735821c`. `params.tsv` diff **empty** (209 lines). Warning census **26 lines / 19 distinct, 0 new**. `git tag -l '*Strata*'` **empty**; PLUGINS.md duplicate check **empty**; every commit path-scoped |

## Requirements Verification

**Stage:** stage-4, Round B — **the stage verdict.** Requirements closed here (CONTEXT requirements map): COMPAT-02 (must), QUAL-04 (nice), PERF-02 Bandlimited item (must, annotation), COMPAT-01 regression (must), the WebView2 halves of UI-01 / FUNC-07 (should / must), PERF-03 (should). Round A's rows (FUNC-08, FUNC-11, QUAL-03, UI-04) are re-checked as regressions.

| Requirement | Priority | Status | Acceptance evidence |
|---|---|---|---|
| COMPAT-02: Windows VST3 build via CI (WebView2, static linking) | must | ✅ **complete** (Finding 6 ceiling) | Run 3 `dcbc609a`: `windows-vst3` `success`, all 13 steps `success`; MSVC + WebView2 built `O-Strata-dev.vst3`; pluginval strictness 10 exited 0 under `-e -o pipefail` with 0 `FAIL` lines across every suite incl. Editor Automation. **Named deferral — owner none, blocked on hardware:** no human sees the 3D view render, the `dataTransfer.files` drop, or a WebView2 frame time on Windows |
| PERF-02 (Bandlimited item, D4) | must | ✅ **complete** (annotation — the 2× row closed at Stage 2) | `--gate clenshaw` accuracy contracted (9.775e-06 ≤ 2e-5 random, 2.980e-07 ≤ 1e-6 projected); H6 band held with `h_max` / 17-muted / 13-equality sets identical; H7 Bandlimited **4.24 % ≤ 4.13 + 2.0** on a quiet M4 Max, against ≈ 11.9 % before D4. Gate-robustness caveat in Issue 1 |
| COMPAT-01 (regression): pluginval 10 VST3 + AU, auval | must | ✅ **held complete** | pluginval 10 `SUCCESS` on the installed VST3 **and** AU; `auval -v aumu OuSt OuDv` `AU VALIDATION SUCCEEDED`; `auval -a` once; installed byte-identical to a forced-recompile build |
| UI-01 (WebView2 half): per-oscillator 3D terrain view | should | ✅ **complete at the Finding 6 ceiling** | The CI Windows build compiles the view's TUs under MSVC and pluginval 10 **opens the editor** (a silently-blank WebView2 would surface as an Editor / Editor Automation failure or timeout; neither occurred). **That the view renders correctly on Windows is a named deferral — owner none, blocked on hardware.** The macOS half was closed at Stage 3 |
| FUNC-07 (WebView2 half): PNG import, chooser + drag-and-drop | must | ✅ **complete at the Finding 6 ceiling** | Same CI evidence: MSVC compiles the chooser / drop path, pluginval opens the editor. **The `dataTransfer.files` drop on WebView2 is a named deferral — owner none, blocked on hardware.** The DSP and macOS halves were closed at Stage 2 / Stage 3 |
| QUAL-04: listening pass on the factory presets and the terrain × orbit grid | nice | ⏳ **pending** — material complete, sign-off outstanding | Per PLAN Decision 41: complete only if Tables A + B are signed at this verify. They are not — **39 / 39 Table A verdict cells blank, 85 Table B rows blank**. The material itself is verified present and correct: 198 WAVs `shasum -c` clean, 18 preset renders 0 silent. **Named step:** Taylor's sitting against `stages/4-polish/LISTENING.md` |
| PERF-03: 3D view ≤ 2 ms per frame at DPR 2 | should | ⏳ **pending** (both halves) | `~/Library/Logs/O-Strata/` **does not exist** — no WKWebView figure. Decision 36's scripted route is unreachable (SUMMARY Task 5: four attempts, two mechanisms, 0 AX windows; the occluded-WKWebView rAF behaviour). No `gl.finish()` flag was added, so `terrain-view.js` is untouched and the layout gate stays at 607. WebView2 half = named deferral. Neither the page nor the instrumentation is implicated (shell diff 0 px, layout 607 / 0) |
| DSP-06 (regression): symmetry rule | must | ✅ **held complete** — and strengthened | `--gate H2` ×3 all 20 / 20 with **identical figures across runs**; Squarcle Storm now a stable −3.6 dB / 100 % instead of Round A's −2.2 … −4.0 |
| FUNC-08 / FUNC-11 / QUAL-03 / UI-04 (regressions) | must / should / nice / must | ✅ **held complete** | `--gate H10` **24 / 24**; `smoke --with-disk` **39 / 39** incl. [5] (18 JSONs, stamp `1.0.0+18c17735821c` = `FactoryPresets::stamp`, 19 files); `--gate all` 162 / 0 carries `scheduler` and the H2 preset loop; on-disk bank 18 / 9 folders |

**Requirements Summary:** ✅ complete **6** (COMPAT-02, PERF-02, COMPAT-01, UI-01, FUNC-07 — the last two at the Finding 6 ceiling with a named deferral — plus the DSP-06 / FUNC-08 / FUNC-11 / QUAL-03 / UI-04 regressions held) · ⚠️ partial **0** · ⏳ pending **2** (QUAL-04, PERF-03 — both human, neither `must`) · ❌ failed **0**.

**Every `must` requirement in the project is now complete.** The two open rows are `nice` (QUAL-04) and `should` (PERF-03).

## Automated Checks

| # | Check | Result (re-measured) | Threshold (PLAN B1–B20) |
|---|---|---|---|
| — | forced recompile of the 7 Round B TUs + `ninja O-Strata_VST3 O-Strata-render-test` | 0 errors; VST3 **bit-identical** to the installed bundle (`c86546cd…`) — the build is deterministic and installed = built = `005cbcf7` | build clean; installed = built |
| B1 | `$H --gate all` | **ALL GATES PASSED — 162 checks, 0 failures, 108.6 s** (76.5 s user); `presets: 18`; `[H2] factory presets: 20 / 20`; `liveCount 2 / 2`; storm 0 allocations | exactly 162 / 0 / ≤ 180 s |
| B2 | `$H --gate clenshaw` | `clenshaw2D` **81.4 ns** / `chebEvalPadded` **28.0 ns** (2.91×); random **9.775e-06** (≤ 2e-5); projected **2.980e-07** (≤ 1e-6); double-ref 3.41e-06 / 2.49e-07 | ≤ 2e-5 / ≤ 1e-6 |
| B3 | `$H --gate H6` | 2× **198 / 198** ≤ −60 dB, worst **−70.7 dB** (Mitsuhashi × Epitrochoid 7, A2); rate rows within 3 dB; Bandlimited **127 / 127** sounding ≤ −90 dB, worst **−98.4 dB** (8 quiet rows ≤ −100 dBFS, worst −124); **17** muted by truncation, same list; `h_max ≤ D_max·K` **equality on 13** | ≤ −60 / ≤ −90; sets identical |
| B4 | `$H --gate H7` (quiet machine) | 2× delta **4.13 %** (≤ 12; total 10.76, baseline 6.63); Bandlimited **4.24 % ≤ 4.13 + 2.0 PASS** (total 10.75, baseline 6.51); unison 4 row 16.08 %, 4× row 6.99 %; `machine: Apple M4 Max (Release build)` | ≤ 12; ≤ 2× + 2.0 |
| B5 | `$H --gate H1` · `--gate H9` | Chebyshev identity **−128.3 dB** (need ≤ −80); Bandlimited pairwise max\|Δ\| **0.000e+00** (need ≤ 1e-5) | identity unmoved; 0 |
| B6 | `$H --gate H2` ×3 | **20 / 20** on every run; every measurement row identical across runs; **Squarcle Storm −3.6 dB / 100 %** on all three (Round A Issue 1 closed). Only textual difference: the wall-clock line (Issue 2) | 20 / 20; runs identical |
| B7 | `$H --gate H10` · `smoke --with-disk` · `--gate H8` | **24 / 24** in 1.0 s; **39 / 39** in 0.2 s (18 JSONs, stamp `1.0.0+18c17735821c` = `FactoryPresets::stamp (bank)`, 19 files); **8 / 8**, 0 allocations across 60 and 100 changes + the image row (foreign-thread tallies printed, not counted) | all PASS; 0 allocations |
| B8 | `$H --gate orbits` + `node tests/orbit-golden.mjs` | `PASS [orbits] 44 / 44 curves × 512`; golden **ALL PASS — 44 / 44 within 0.0001 (worst 2.93e-6)** | 44 / 44 |
| B9 | grid `shasum -c` · `$H --gate exportPresets` | **0** non-`OK` lines with no `tr`; golden **0 CR bytes** / 198 lines; **18 files, 0 silent** (4 checks PASS), all 864 104 B; grid golden still clean after the preset re-render | 0 non-OK; 18 / 0 silent |
| B10 | page + i18n gates | `check-i18n` `ALL CHECKS PASS`, exit 0 · fr-lint `CLEAN — exit 0` (0 findings) · zh-lint `GATE PASSED — exit 0`, **53 at `reviewed:'mt'`** · `check-ui-labels` **`== ALL CHECKS PASSED ==` 1809 PASS / 0 FAIL** over **`default + 42`** states, `seed=param-dump (205 rows) + overrides` · boot **`clean: 1 / 1`**, warn 0, failed 0, **DEAD 0 / late 0**, no `stub invented:` line, `i18n=203`, text 1268 / aria 12; 34 `withNativeFunction` sites · layout **`ALL PASS — 607 passed, 0 failed`** (budget 666 / 666), `--viewport 1200x780` **`FAILED — 526 passed, 81 failed`, exit 2** (documented liveness) · tips **`ALL CHECKS PASSED (3215 passed)`**, table **122** I18N / **122** TIP_BINDINGS · shell diff **`ALL CHECKS PASSED`**, **0 px outside masks** (3655 inside), self-diff 0, planted control **1576 px** · `measure-ui --mode box --report all` exit 0: undeclared-font **5**, line-height-normal **3**, wrap-count **0**, svg-font-attr **0** · CDP probe **16 runs, 0 nodes missing, 0 Han runs off the declared face** | every pinned number |
| B11 | `O-Strata-param-dump` vs `.planning/params.tsv` | **diff empty** (209 / 209 lines) | empty |
| B12 | warning census (forced recompile of 7 TUs) | **26 warning lines, 19 distinct, 0 new** — every row on Round A's A15 list, line drift only. `createWriterFor` deprecation appears **once** (`main.cpp:1792`), confirming Deviation 1's `writeWav24` refactor removed the duplicate; no `-Wsign-compare` from `exportPresets` | 0 new |
| B13 | MSVC greps | `= juce::Component::SafePointer` **0**; C3493 `constexpr`-local-captured-by-lambda **0** (six `constexpr` local sites exist — `TuningEngine.cpp:420`, `TerrainViewFeed.cpp:42-43`, `TerrainImage.cpp:170`, `TerrainScheduler.cpp:354`, `ChebyshevProjector.cpp:43` — **none captured**, see Issue 4); non-ASCII inside a C++ string literal **0** (the regex's 5 hits are all `//` or `/** */` comment lines); `grep -c clenshaw2D Source/dsp/TerrainOscillator.cpp` **0** | 0 / 0 / 0 |
| B14 | CI runs (re-read from `gh`, not from SUMMARY) | Three dispatches, all `-f plugin=O-Strata`; the O-Octagon steps show `skipped` in every run (the `if:` gating works). **`windows-vst3` `success` on all three** (6m35s / 6m01s / 6m12s). `probes-macos` `failure` on all three, **162 checks** each — run 1: 2 failures; run 2: 1; run 3: 2 ([H7] Bandlimited 10.37 ≤ 10.23, [sched] 154 ms ≤ 120), `machine: Apple M1 (Virtual)`, `--gate all` 292.0 s vs 108.6 s local. **Orbit golden 44 / 44 worst 2.93e-6 on run 3** (the `dcbc609a` `if:` fix) | Windows green; macOS count = local |
| B15 | validators (installed bundles) | pluginval strictness 10 VST3 **`SUCCESS`**, AU **`SUCCESS`**; `auval -v aumu OuSt OuDv` **`AU VALIDATION SUCCEEDED`**; `auval -a \| grep -i strata` → `aumu OuSt OuDv  -  Ouaricon Audio Development: O-Strata-dev`, **once**; `diff -r` built vs installed **identical** for both formats | green; once; identical |
| B16 | on-disk bank | **18 JSONs** across 9 category folders (Bass, Drone, FX, Init, Keys, Lead, Pads, Pluck, Sequence) + `.factory-version` **`1.0.0+18c17735821c`** | 18 + stamp |
| B17 | tags / duplicates / scoping | `git tag -l '*Strata*'` **empty (0)**; PLUGINS.md duplicate check **empty**; the release commit touches `plugins/O-Strata` + `PLUGINS.md` only (7 files, all docs) | empty; path-scoped |
| B18 | `LISTENING.md` counts | Table A **18 rows / 39 blank verdict cells**; Table B **85 rows**; Table C **15 rows** — recounted from the file, all verdicts blank | 39 / 85 / 15, blank |
| B19 | `view-perf.log` | `~/Library/Logs/O-Strata/` **absent** — PERF-03 recorded pending (Issue 3 in SUMMARY Task 5) | recorded |
| B20 | docs | CHANGELOG `## v1.0.0 — 2026-09-13` with **Added / Behaviour to know / Known limits / Technical notes / Stage history**, the Windows deferral sentence and the PERF-03 / S&H paragraphs present; NOTES **Attribution & licence** (AGPL-3.0-or-later, fork base O-Prism v1.24.0) + v1.1 list; ARCHITECTURE `:342-346` the D4 evaluator + accuracy + `clenshaw2D`-retention paragraphs; PLUGINS.md **📦 Installed** with the D3 wording; `REQUIREMENTS.md` untouched by the execute phase | all present |

## Human Verification

The 15 rows of `stages/4-polish/LISTENING.md` Table C (Stage 3 rows 1–6, Round A rows 1–6, three Round B rows). **None blocks the automated verdict**; two of them carry the only two non-`must` requirement rows still open.

- [ ] **QUAL-04 — the sitting.** Table A: the 18 presets in Logic and Live 12 (39 verdict cells); Table B: the 66 C4 grid rows plus the 19 flagged pairs (85). Material is ready and verified: `plugins/O-Strata/tests/exports/` (198 grid WAVs, golden-clean) and `tests/exports/presets/` (18 renders, 0 silent). Defaults changes follow the Decision 38 CHANGE protocol — one CHANGE per commit, and any change re-runs H2 / H3 / H6 / H7 and re-records the export golden.
- [ ] **PERF-03 — the WKWebView number.** ⌥-click the Terrain HUD ×3 in the Standalone and again in Logic → `~/Library/Logs/O-Strata/view-perf.log`. ~30 seconds of hands-on; the scripted route is proven unreachable. Build the Standalone from `build/plugins/O-Strata/O-Strata_artefacts/Release/Standalone/O-Strata-dev.app` (the install script does not install it).
- [ ] Round A rows 1–6 (Locate… by hand, Logic bytes-form preset, live session restore, dropdown order + ◀ / ▶, first listen, the French read of the five new rows + the 53 zh flips).
- [ ] Stage 3 rows: chooser → Imported… with a real PNG through Blur / Edge; Logic ⌘Z count for one gesture; lose-context in the Standalone.
- [ ] The three Round B rows: open the CI run URL, acknowledge the Windows named deferral, the PERF-03 Logic half.

**Named deferral — owner none, blocked on hardware:** the WebView2 hands-on halves of UI-01 (3D view renders), FUNC-07 (`dataTransfer.files` drop) and PERF-03 (WebView2 frame time). No Windows machine is in the loop. The CI build + pluginval-10 editor open is the ceiling this stage can reach (CONTEXT Finding 6).

## Issues Found

1. **The H7 Bandlimited check is load-flaky *everywhere*, not just on the runner — and this session proved it on the local M4 Max.** While `XprotectService` was at 134 % CPU, the **same binary** produced 2× delta 2.68 % / Bandlimited **10.42 % → FAIL** (ratio **3.9×**), and `--gate all` ran 2125.2 s wall-clock against 122.7 s of user CPU. Ten minutes later, quiet, the same two commands gave 4.13 / **4.24 % PASS** and 108.6 s. SUMMARY Deviation 4 had already retired Decision 31's premise (the two deltas move together) by measuring 1.15–1.97× on the M1 Virtual runner against 1.02× locally; this verify extends it — the local machine reaches 3.9× under contention, so a green local run is evidence **only when the machine is quiet**, and "runner-flaky" understates the failure mode. **Not a v1.0 blocker:** the D4 evaluator's own contract is machine-stable — the `clenshaw` accuracy rows printed character-identical figures (9.775e-06 / 2.980e-07 / 3.41e-06 / 2.49e-07) in every run including the contended ones, and the `clenshaw2D`/`chebEvalPadded` **ratio** held at 2.9–3.4× throughout even as the absolute ns inflated 3×. The row remains a sound local regression guard — it is what pinned 11.69 % → 4.2 % and would catch a regression back. **Carried to v1.1** (already in NOTES): a ratio form (`delta_BL ≤ 1.3 × delta_2x`, say) or a `STRATA_CI` band, plus a duty-cycle assertion so the gate refuses to report a timing verdict when user CPU is far below wall-clock. Confirms memory `pattern_absolute_slack_threshold_is_not_runner_portable`.
2. **SUMMARY's "`--gate H2` ×3 byte-identical full output" is one line too strong.** The gate's trailing verdict line carries its own wall-clock (`2.7 / 2.8 / 2.7 s`), so the files differ by exactly that line. Every *measurement* row — all 20 preset rows, the 66 analytic and 66 Bandlimited rows, Squarcle Storm at −3.6 dB / 100 % — is identical across the three runs, which is the claim Decision 32 actually needed. Bookkeeping only; Round A Issue 1 is properly closed.
3. **SUMMARY's Windows "pluginval … reports `SUCCESS`" is not literally in the log.** The tee'd 191-line log ends at `Completed tests in pluginval / Fuzz parameters`; there is no `SUCCESS` line (the local macOS runs do print one). The claim that survives is stronger than it needs to be anyway: the step ran under `bash -e -o pipefail`, so a green step means pluginval exited 0, and 0 `FAIL` lines appear across every suite including Editor Automation. Same verdict, more precise wording — worth fixing if the CHANGELOG ever quotes it.
4. **B13's `constexpr` wording is narrower than the tree.** SUMMARY says "the only `constexpr` locals found are namespace-scope in `TerrainViewFeed.cpp`"; there are in fact six sites (listed in B13 above). The **verdict is unchanged** — C3493 fires only on a `constexpr` local *captured by a lambda*, and none of the six is captured — and three green MSVC builds settle the question independently of the grep.
5. **The release commit `005cbcf7` is not pushed** (`origin/main` is at `dcbc609a`). It is docs-only — CHANGELOG, NOTES, ARCHITECTURE, LISTENING, SUMMARY, STATUS, PLUGINS.md — so the CI evidence at `dcbc609a` still covers every line of code and workflow in the tree, and no gate above is affected. Recorded so it is not mistaken for a missing dispatch; the next push (or `/publish`) closes it.
6. **No defect in the delivered code.** Every SUMMARY figure reproduced within run-to-run noise on a quiet machine: `--gate all` 108.6 s (111.3), clenshaw 81.4 / 28.0 ns (83.9 / 30.3), H7 4.13 / 4.24 % (4.46 / 4.54), H6 worst −70.7 / −98.4 dB and the 17-muted / 13-equality sets identical, H1 −128.3 dB, H9 0.000e+00, H10 24 / 24, smoke 39 / 39, H8 8 / 8, orbit golden 44 / 44 worst 2.93e-6, every Playwright and i18n count on its pin, `params.tsv` empty, 0 new warnings, validators green, installed byte-identical to a deterministic rebuild.

## Stage Verdict

**Status:** ✅ **VERIFIED** (Stage 4 — automated) — the stage goal is met. The padded Chebyshev evaluator is on the audio path under a contracted accuracy gate, reproducing the Chebyshev law to the unmoved −128.3 dB H1 identity floor with the H6 `h_max` / muted / equality sets character-identical, and it takes the H7 Bandlimited oscillator delta from ≈ 11.9 % to 4.24 % — level with the 2× path's own 4.13 %. `ci-tests.yml` carries a `plugin` dispatch input and a green Windows job: MSVC + WebView2 built the VST3 and pluginval strictness 10 exited 0 across every suite including Editor Automation, which is COMPAT-02 at its Finding 6 ceiling, with the hands-on WebView2 halves of UI-01 / FUNC-07 / PERF-03 recorded as a named deferral (owner none, blocked on hardware). Round A's Issue 1 is closed — the S&H LFO now renders bit-stably under the harness seed, Squarcle Storm at a repeatable −3.6 dB. `--gate all` is 162 / 162, every page, i18n, grep and param gate sits on its pinned number, 0 new warnings, and pluginval ×2 + `auval` are green on installed bundles byte-identical to a deterministic rebuild. **Every `must` requirement in the project is complete.**

**Requirements written to REQUIREMENTS.md:** COMPAT-02 → complete · PERF-02 → complete (Bandlimited annotation) · UI-01 → complete (Finding 6 ceiling + deferral) · FUNC-07 → complete (Finding 6 ceiling + deferral) · COMPAT-01 → complete (held) · QUAL-04 → **pending** (material ready, sitting outstanding) · PERF-03 → **pending** (both halves human).

**Ready for next step:** Yes. The plugin is installed locally at v1.0.0 and releasable per CONTEXT D3 — **no tag, no `/publish` in this stage** (`git tag -l '*Strata*'` empty). `/install-plugin O-Strata` is already satisfied by Task 10 and re-verified here.

**Blockers:** none. The two pending rows are `nice` (QUAL-04) and `should` (PERF-03), both waiting on a human sitting of ~30 minutes and ~30 seconds respectively; neither carries a figure the automated rows do not already pin, and neither gates the local install. Issue 1 is a v1.1 gate-robustness item, not a code defect.
