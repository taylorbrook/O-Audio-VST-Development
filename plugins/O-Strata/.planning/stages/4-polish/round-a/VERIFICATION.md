# Stage 4: Polish / Validation — Round A (Phase 4.1: persistence, fallback + Locate…, factory bank) — Verification

## Verification Date

2026-09-13

**Plugin:** O-Strata · **Stage:** 4 of 4 (Polish) · **Round:** A = ROADMAP Phase 4.1 (CONTEXT D1) — Round B (Phase 4.2: D4 evaluator, `ci-tests.yml`, listening pass, CHANGELOG v1.0.0, local install) carries the **stage** verdict; this file is the Round A verdict · **Mode:** manual
**Inputs:** `CONTEXT.md` (D1–D4, Findings 1–11, requirements map), `RESEARCH.md` (§4 Decisions 1–11, §5.1 bank, §5.3 gate list), `PLAN.md` (12 tasks / 4 waves, Decisions 1–26, Task 10 gates A1–A16, success criteria, requirements traceability), `SUMMARY.md` (execute report, commits `f2fe26ed` preset-manager v1.0.7 · `1a1ff54e` Phase 4.1a · `29803cb5` Phase 4.1; 11 deviations; Decision 25 checklist), `REQUIREMENTS.md` v2.0.0.
**Method:** every SUMMARY figure was re-measured in this session from the committed tree at `29803cb5` (`git status` clean under `plugins/O-Strata`, `modules/`, `PLUGINS.md`). `ninja` for the five targets re-ran CMake (a globbed directory changed elsewhere in the checkout) and relinked the Standalone / param-dump, but the VST3 and AU binaries it produced are **byte-identical** (`cmp`) to the installed `-dev` bundles — sha256 `798d103f…` (VST3) / `5899e47b…` (AU), the same VST3 digest SUMMARY records — so every gate below ran on the 4.1 code. Then the Task 10 list A1–A16 was re-run from the repo root: harness (`--gate all`, `H2` ×3, `H10`, `smoke --with-disk`, `H8`, `orbits` + golden), the i18n gates, the Playwright gates (check-ui-labels, boot, tip gate, layout + liveness, shell diff, measure-ui, CDP face probe), the grep gates, the param-dump diff, a forced-recompile warning census of the six Round A translation units, `auval` + pluginval ×2 on the installed bundles, the on-disk bank listing. Nothing below is copied from SUMMARY without a fresh run. No plugin source changed in this phase. Machine: Apple M4 Max, macOS 26 / Darwin 25.6, Apple clang 17, node v24, Release build. Logs in the session scratchpad.

## Goal-Backward Analysis

### Original Goals (CONTEXT.md "Goal" + requirements map, Round A scope per D1; PLAN success criteria)

1. **Persistence (FUNC-08):** the imported PNG survives plugin state (`terrainImports` child of the APVTS state) and presets (preset-manager `customState`) — raw bytes as base64 when ≤ 2 MiB per oscillator, `path` + SHA-256 + name above the cap; a restore re-imports through the Stage 2 API *before* the `stateGeneration` bump so the view shows the restored map within one push interval (UI-04 regression); an absent child or a preset without `customState` clears both slots.
2. **Fallback + Locate… (QUAL-03):** a preset whose PNG is missing or changed (SHA mismatch) degrades to a library terrain (Sine Product — RESEARCH C1 found the `Imported…` oscillator with no image was *silent*), never silence or a crash; the sticky "Source missing" notice carries **Locate…**, which re-links only a hash-equal file and refuses a different one with a localised notice.
3. **Factory bank (FUNC-11):** ≥ 15 presets covering all 6 terrains, all 11 orbits, feedback ≥ 3, Bandlimited ≥ 3, the five BRIEF use cases, authored in engineering units, every one passing H2 at C4 individually (DSP-06 — not inferred from a neighbour, Coarse / Fine honoured, osc B where present); the on-disk bank regenerated with the Stage 1 `Init`-only orphan gone (`.factory-version` stays at 1.0.0 → content stamp).
4. **Harness + gates:** H10 (a)–(e), smoke [5] over the bank, the H2 preset loop fixed (C6: Coarse / Fine + osc B), the layout gate `locate` section, every Decision 46 / Task 10 gate green, COMPAT-01 regression, `params.tsv` unchanged, 0 new warnings, MSVC greps 0, three path-scoped commits, no tag.

### Deliverables (SUMMARY.md, confirmed in the tree at `29803cb5`)

1. `PluginProcessor.h/.cpp`: `kMaxImportBytes` 2 MiB + `kMaxImportFileBytes` 8 MiB (`:213-214`), `ImportSlot { …, path, sourceMissing, sourceSize }`, `sourceMissingFlag[2]` atomics mirrored into `getTerrainStatus` (`:1186`), `restoreAlive` token reset in the destructor (`:688`), `saveTerrainImportsTree` / `loadTerrainImportsTree` (`:1366 / :1385`) + the `Var` pair, `clearImportSlot`, the `SlotRecord` adapters; `getStateInformation` replaces the child (`removeChild` + `appendChild`, `:1470-1471`); `setStateInformation` restores inline on the message thread, else `callAsync` under the weak token with its own bump, then the Stage 3 bump last (`:1523-1544`); the constructor wires `setCustomStateCallbacks` after `excludedParameterIds` (`:566 / :577`) and sweeps `Factory/` on a stamp mismatch (`deleteRecursively` + `initializeFactoryPresets (defs, stamp)`, `:590-595`).
2. `TerrainOscillator.cpp:399`: the `Imported` branch with `image == nullptr` evaluates `terrain (TerrainKind::SineProduct, px, py, terrainFreq * rTrack, terrainModX, terrainModY)`; `TerrainViewFeed.cpp:336` mirrors it in `copyHeightmap`.
3. preset-manager v1.0.7 (`modules/persistence/preset-manager/cpp/OuariconPresetManager.h`, `module.yaml` 1.0.7, README, `modules/registry.yaml` 1.0.7 + O-Strata `used_by`): `customLoad` fires on every apply with `juce::var()` when the preset has no `customState`; the O-Strata copy (`Source/OuariconPresetManager.h`, a diverged fork — SUMMARY deviation 4) carries the identical line.
4. `PluginEditor.cpp`: `launchPngChooser` (`:1048`, the one `launchAsync` site; `safeThis (this)` hoisted form ×6, `= juce::Component::SafePointer` 0), `chooseTerrainImage` at `kMaxImportFileBytes`, `locateTerrainImage` (`:560-598`: cancel / `tooLargeFile` / `undecodable` / **`hashMismatch` with nothing touched** / `importTerrainFile` + `repushPending`); 34 `withNativeFunction` sites.
5. Page + i18n: `#terrain-notice` = `<span id="terrain-notice-text">` + `<button id="btn-terLocate">` (`index.html:2430`), flex row, `.transient` hides the button (`:1612`), `min-width: 93.41px` (`:1656`); `'Init'` first in `CATEGORY_ORDER` (`:2915`); keys `label.locate` / `label.locateMismatch` / `label.importTooLargeFile` (`i18n.js:2416-2419`, fr `reviewed: false`, zh `'mt'`) + `tip.terImport` / `tip.terLocate` with two `TIP_BINDINGS` rows (`:2142-2143`); state `terrain-locate-mismatch` (`i18n-states.json:36`); stub override `locateTerrainImage` (`generic-overrides.json:14`).
6. `FactoryPresets.cpp`: 18 `makePreset` calls (`Init` + 17 authored; comments 1–18 carry each preset's measured H2 figure), `RawMap → completeBase() → convertTo0to1`, `choiceIndex` for the division names, `stamp (defs)` = `JucePlugin_VersionString + "+" + sha256 (…)[0:12]`.
7. Harness (`tests/render-harness/main.cpp` + README): H10 (a)–(e), smoke [5], the H2 preset loop (Coarse / Fine, `(B)` rows, `bypassFx()`, preset-centred neighbours), `--real id=eng`, `--gate h2cli`; `tests/ui_layout_check.js` `locate` section + the `locateTerrainImage` native grep + the editor-cap grep (`:181-190, :540-557`); `tests/ui_tip_render_check.js` `#btn-ter*` mapping + `setSourceMissing` (`:521-542`), 122 bindings (`:1029`).
8. Docs: ARCHITECTURE "State Persistence" as built, CHANGELOG Round A block + "Known limits — persistence", NOTES status, STATUS, PLUGINS.md row, SUMMARY.

### Goal Achievement

| Goal | Status | Evidence (re-measured 2026-09-13) |
|---|---|---|
| 1 persistence: state child, preset `customState`, cap form, ordering | ✅ Achieved (automated) · ⏳ Logic bytes-form + live-restore rows (Decision 25 rows 2–3) | `--gate H10` **24 / 24 in 1.0 s**: (a) exactly one `<slot osc="0" form="bytes">` with data / no path / SHA / size (blob 21 098 B), restored slot 0 SHA equal + decoded + `sourceMissing` false, slot 1 empty, **render SHA equal through `getStateInformation` → `setStateInformation`**; (c) `saveTerrainImportsVar` = `{v, slots:[…]}`, `loadTerrainImportsVar` SHA equal + render SHA equal, `loadPresetFromFile` with `customState` → render SHA equal to the same preset + a direct import (0 parameters differ), image published after the sync poll, **a preset without `customState` clears both slots** (sha256 empty, `imagePtr` null, `importRevision` 0 — v1.0.7); (d) fixtures 1 908 942 / 1 860 715 B, **both slots published in 64 ms** (restore 52 + jobs 11; blob 5.0 MB; < 1000); (e) `setStateInformation` bumps `stateGeneration` 0 → 1 with `importRevision[0]` = 1 (import queued before the bump), the preset path moves `importRevision` without a bump, `notifyStateChanged()` then bumps. Code: the inline / `callAsync` split at `PluginProcessor.cpp:1523-1544` with the Stage 3 bump as the last statement |
| 2 fallback + Locate… | ✅ Achieved (automated) · ⏳ Locate… by hand (Decision 25 row 1) | H10 (b): noise fixture 3 652 762 B in (2 MiB, 8 MiB]; `importTerrainFile` accepts it; state `form="path"` + absolute path + size, no data (blob 8 917 B); present → re-imported, render SHA equal; **deleted → `sourceMissing` on the slot copy and `getTerrainStatus`, `imagePtr` null, name / sha256 / path kept for Locate…, the oscillator is NOT silent (rms 0.2420) and renders SHA-equal to a Sine Product instance at the same patch**; rewritten with different bytes → SHA mismatch → `sourceMissing`, the preset's SHA kept. Layout gate `locate` section (in the 607): button inside the notice in en / fr / zh-Hans, text left of it, `.transient` hides it, click → one `locateTerrainImage (['A'])` then `(['B'])` after the Osc B switch, mismatch transient + restore, `sourceMissing` false hides. Native: `hashMismatch` returns before any state is touched (`PluginEditor.cpp:584`) |
| 3 factory bank ≥ 15, coverage, H2 per preset, orphan gone | ✅ Achieved | `--gate H2` **20 / 20 preset rows** (18 A + Breathing Pad (B) + Wells Drone (B)), every row 100 % of windows, `presets: 18`; figures: Init 0.0, Breathing Pad −1.9 (B 0.0 at (−0.17, 0.24), f0 × 1.003), Chatter Lead 0.0, Pierce Bell 0.0, Your Terrain Here 0.0, PD Organ −3.3, Radial Keys 0.0, Folded Bass 0.0 (f0 × 0.5), Wells Drone 0.0 / (B) 0.0 (f0 × 0.5), Saddle Pluck 0.0, Butterfly Choir −4.5, Limacon Lead 0.0, **Squarcle Storm −2.8 / −4.0 / −2.2 across three runs** (Issue 1), Glass Rings 0.0, Mitsuhashi Organ 0.0 at (0, 0), Superellipse Bass 0.0 (f0 × 0.5), Epi Keys 0.0, Saddle Sweep 0.0. Coverage from the source: osc A terrains {SineProduct, RadialRings, Saddle, RidgedCosines, Mitsuhashi, CosineWells} = **6 / 6**; orbits {Ellipse, Superellipse, Limacon, Epi3, Epi5, Epi7, Hypo3, Hypo5, Hypo7, Butterfly, Squarcle} = **11 / 11**; `oscAOrbFeedback` 0.4 / 0.25 / 0.6 = **3**; `Q_Bandlimited` ×3 (Pierce Bell, Wells Drone, Glass Rings) = **3**; the five BRIEF use cases = presets 2–6. On disk: `~/Library/O-Strata/Presets/Factory/` = Bass 2 · Drone 1 · FX 2 · Init 1 · Keys 6 · Lead 2 · Pads 2 · Pluck 1 · Sequence 1 = 18 JSONs + `.factory-version` `1.0.0+18c17735821c` (19 files, no `User/`); smoke [5] 7 / 7 (stamp equals `FactoryPresets::stamp (bank)`, 198 keys each, 0 with `customState`, 9 folders / 0 stray) |
| 4 harness, gates, regression, commits | ✅ Achieved | Table below — every A1–A16 threshold met on re-run; pluginval ×2 `SUCCESS`, `auval` `AU VALIDATION SUCCEEDED`; installed = built (`cmp`); `params.tsv` diff empty (209 / 209); 0 new warnings; `git diff --name-only 9e5f6f85 29803cb5` outside `plugins/O-Strata` = `PLUGINS.md` + the four preset-manager module files of `f2fe26ed` only; `git tag -l '*Strata*'` empty |

## Requirements Verification

**Stage:** stage-4, Round A · **Requirements for this round (CONTEXT requirements map):** FUNC-08 (must), FUNC-11 (should), QUAL-03 (nice), UI-04 regression (must), COMPAT-01 regression (must). Round B rows (COMPAT-02, QUAL-04, PERF-02 Bandlimited item, the WebView2 halves of UI-01 / FUNC-07 / PERF-03) are not touched here.

| Requirement | Priority | Status | Acceptance evidence |
|---|---|---|---|
| FUNC-08: imported PNG persists in state and presets (bytes ≤ 2 MB; path + SHA-256 above) | must | ✅ **complete** (automated; Logic bytes-form + live-restore rows for Taylor) | H10 (a) state round trip render-SHA-identical; (b) path form present / deleted / rewritten; (c) preset `customState` through the public file loader + the no-`customState` clear; (d) two ≈ 1.9 MB slots in 64 ms; (e) ordering. Acceptance bullet "Save/reload a preset with an embedded PNG: identical rendered output; above the cap: path + SHA; missing file → fallback + notice" → ticked |
| QUAL-03: missing PNG → library fallback + visible notice, never silence or a crash | nice | ✅ **complete** (macOS automated; host-independent — no Windows half) | H10 (b missing) rms 0.2420 = Sine Product SHA; `sourceMissing` in the status push; layout gate `locate` (notice + button + mismatch notice + restore); Decision 25 row 1 by hand |
| FUNC-11: factory presets ≥ 15 covering terrains / orbits / feedback / Bandlimited, every one passing DSP-06 | should | ✅ **complete** | 18 presets; 6 / 6 · 11 / 11 · 3 · 3 · five use cases; H2 20 / 20 at C4 with Coarse / Fine honoured, osc B rows; smoke [5]; on-disk bank + stamp. DSP-06 acceptance "every factory preset" half → ticked (the library grid half was Stage 2) |
| UI-04 (regression): re-push after preset apply / session restore | must | ✅ unchanged, complete (live row 3 for Taylor) | H10 (e): the restore's import is queued before the `stateGeneration` bump, the preset path relies on `notifyStateChanged()`; `--gate scheduler` (f) unchanged inside `--gate all` 159 / 159 |
| COMPAT-01 (regression) | must | ✅ complete | pluginval strictness 10 VST3 + AU `SUCCESS`; `auval -v aumu OuSt OuDv` `AU VALIDATION SUCCEEDED`; `auval -a` lists `aumu OuSt OuDv` once |

**Requirements Summary:** ✅ complete 3 new (FUNC-08, FUNC-11, QUAL-03) + 2 regressions held (UI-04, COMPAT-01) · ⚠️ partial 0 · ⏸️ deferred to Round B 2 (COMPAT-02, QUAL-04) + the Stage 3 partial / pending rows (UI-01, FUNC-07, PERF-03) · ❌ failed 0.

## Automated Checks

| # | Check | Result | Threshold (PLAN Task 10) |
|---|---|---|---|
| — | `ninja O-Strata_VST3 O-Strata_AU O-Strata_Standalone O-Strata-render-test O-Strata-param-dump` | CMake re-ran (glob change outside the plugin), Standalone + param-dump relinked; VST3 / AU **byte-identical to the installed `-dev` bundles** (`cmp`, sha256 `798d103f…` / `5899e47b…`); second `ninja -n` = no work | build clean; installed = built |
| A1 | `$H --gate all` (repo root) | **ALL GATES PASSED — 159 checks, 0 failures, 111.4 s** (`presets: 18`, `[H2] factory presets: 20 / 20`, H1 identity −128.3 dB, H8 0 allocations ×3 rows, `liveCount` 2 / 2) | ≥ 150 / 0 / ≤ 180 s; `presets: 18` |
| A2 | `$H --gate H2` ×3 | 66 / 66 analytic + 66 / 66 Bandlimited + **20 / 20 preset rows** on every run; the 19 deterministic rows identical across runs (figures in Goal 3); Squarcle Storm −2.8 / −4.0 / −2.2 (Issue 1); negative control fails as expected | 18 / 18 + B rows |
| A3 | `$H --gate H10` | **24 / 24 in 1.0 s**; (d) 64 ms = restore 52 + jobs 11 (< 1000) | all PASS; (d) < 1000 ms |
| A4 | `$H --gate smoke --with-disk` | **39 / 39 in 0.2 s** incl. [5] 7 rows: 18 JSONs, `Init/Init.json`, stamp `1.0.0+18c17735821c` = `FactoryPresets::stamp`, 19 files, 9 folders / 0 stray, 198 keys / 0 `customState` (SUMMARY's "61 / 61" is not this gate's count — the gate prints 39; the [5] rows and the verdict are the same) | [1]–[6] PASS incl. [5] |
| A5 | `$H --gate H8` (standalone) · storm / import inside A1 | H8 **8 / 8**: 0 allocations across 60 terrain / orbit changes, 0 across 100 Quality changes under 16 notes, image row 0 counted (+82 foreign-thread, not counted), output finite | 0 allocations |
| A6 | `check-i18n.js --plugin O-Strata` | `ALL CHECKS PASS — 1 localized plugin(s)`, exit 0 | exit 0 |
| A7 | `i18n-fr-lint.js` / `i18n-zh-lint.js` | `CLEAN — exit 0` / `GATE PASSED — exit 0`, 0 findings, **53 at `'mt'`** | exit 0 / exit 0, 53 |
| A8 | `check-ui-labels.js --plugin O-Strata` | `== ALL CHECKS PASSED ==`, **1809 PASS / 0 FAIL**, `states: default + 42 from tests/i18n-states.json`, `seed=param-dump (205 rows) + overrides`; the only NOTEs are the Stage 1 inline-box rows | `default + 42`, ≥ 1767 / 0 |
| A9 | `boot-all-uis.js --plugin O-Strata --strict-tips --verbose` | `clean 1 / 1`, warn 0, failed 0, **DEAD 0 / late 0**, no i18n runtime diagnostics, 1268 text-bearing elements / 12 aria / `i18n=203` (202 at Stage 3 + the notice span); **no `stub invented:` line** (the script prints one only when the stub had to invent a native); native parity = the layout gate's five-name grep + the editor's 34 `withNativeFunction` sites | DEAD 0 / late 0 / invented empty / 34 natives |
| A10 | `tests/ui_layout_check.js` · `--viewport 1200x780` | **`ALL PASS — 607 passed, 0 failed`** (source greps incl. the `locateTerrainImage` native + editor-cap rows, Round A rows, `webgl` / `fallback` / `interaction` / `drop` / `locate`; budget 666 / 666, 0 page errors, 0 404s); liveness at 1200 × 780 → **`FAILED — 526 passed, 81 failed`, exit 2** (78 baseline + the three `locate` button-box rows) | ALL PASS incl. `locate`; liveness fails |
| A11 | `tests/ui_tip_render_check.js` | **`ALL CHECKS PASSED (3215 passed)`** — check [8] asserts `TIP_BINDINGS.length === 122` (103 parameter + 16 terrain + 3 chrome), `#btn-terLocate` reached through `setSourceMissing (on)` | 122 bindings, ≥ 3163, 0 fail |
| A12 | `tests/ui_shell_diff_check.js` · `measure-ui.js --mode box --report all` · `tests/tools/cdp-font-probe.js` | shell diff **`ALL CHECKS PASSED`** — 0 px outside the masks on every tab (effects inside 3655), self-diff 0, planted control 1576 px; measure-ui exit 0: `undeclared-font: 0`, `wrap-count: 0`, `svg-font-attr: 0`, `line-height-normal: 3` (zh-Hans ★ / WebGL placeholder / drop overlay — inherited rows, a report not a gate); CDP probe **16 runs, 0 nodes missing, 0 Han runs off PingFang SC** | 0 px; ≤ 5 / 0; 0 off-face |
| A13 | `$H --gate orbits --out <scratch>/orbits.json` + `node tests/orbit-golden.mjs` | `PASS [orbits] 44 / 44 curves x 512`; golden **44 / 44 within 1e-4, worst 2.93e-6** | 44 / 44 |
| A14 | `O-Strata-param-dump` vs `.planning/params.tsv` | **diff empty** (209 / 209 lines) | empty |
| A15 | warning census (forced recompile of `PluginProcessor.cpp`, `PluginEditor.cpp`, `TerrainOscillator.cpp`, `TerrainViewFeed.cpp`, `FactoryPresets.cpp`, harness `main.cpp`) | 19 distinct warnings, **0 new** — every row is on SUMMARY A15's list, only line numbers drifted with the 4.1 edits: `PluginProcessor.cpp:813-814` float-equal (was 802–803), `TerrainOscillator.cpp:175 / 321 / 497`, `PluginEditor.cpp:378-417` unused lambda capture ×4, harness `:713 / :998` sign-conversion (was 678 / 963), `:1802` deprecated `createWriterFor` (was 1767), `:1913` float-equal (was 1878), `maxStep` / `mean` unused, `theta_reference.h:115` switch-enum; `FactoryPresets.cpp` + `TerrainViewFeed.cpp` silent | 0 new |
| A16 | MSVC greps | `= juce::Component::SafePointer` **0**; `[…] constexpr` in lambdas **0**; non-ASCII inside a C++ string literal **0** (the regex's five hits — `PluginEditor.cpp:39`, `PluginProcessor.h:244-245`, `TerrainViewFeed.h:116`, `ChebyshevProjector.h:28` — are all `//` or `/** */` comment lines, not literals) | 0; 0; 0 |
| — | validators (installed bundles) | `auval -a` → `aumu OuSt OuDv - Ouaricon Audio Development: O-Strata-dev` (once); `auval -v aumu OuSt OuDv` **AU VALIDATION SUCCEEDED**; pluginval strictness 10 VST3 **SUCCESS**, AU **SUCCESS** | green |
| — | on-disk bank | 18 JSONs in nine category folders + `.factory-version` `1.0.0+18c17735821c`; the Stage 1 `Init`-only orphan is gone (`Init/` holds one file, the bank's `Init`); no `User/` | 18 + stamp |
| — | commits / tag | three path-scoped commits (`f2fe26ed` → the four `modules/persistence/preset-manager` files + `modules/registry.yaml`; `1a1ff54e` + `29803cb5` → `plugins/O-Strata` + `PLUGINS.md`); `git tag -l '*Strata*'` empty | path-scoped; no tag |

## Human Verification

The Decision 25 rows (SUMMARY "Hands-on checklist") — none blocks the automated verdict; rows 1–3 are the live halves of FUNC-08 / QUAL-03 / UI-04. Use `build/plugins/O-Strata/O-Strata_artefacts/Release/Standalone/O-Strata-dev.app` (the install script does not install the Standalone) and Logic for row 2.

- [ ] **1. Locate… by hand (QUAL-03):** Import… a 3–8 MiB PNG, save a User preset, move the file, reload the preset → the "Source missing" notice with **Locate…**, the oscillator sounds Sine Product; Locate… the moved file → map restored, notice gone; Locate… a *different* PNG → "Different file — hash does not match the preset" for 4 s, the sticky notice returns.
- [ ] **2. Logic, bytes form (FUNC-08):** a User preset with an embedded ≤ 2 MiB PNG survives save / close / reopen of the project and the map re-pushes within one push interval.
- [ ] **3. Live restore (UI-04):** save a Standalone session with an imported PNG, quit, relaunch → the map shows the picture within one interval; load `Init` → the map returns to Sine Product and the notice is hidden.
- [ ] **4. Dropdown:** `Init` first, then Pads … FX; ◀ / ▶ walk all 18 alphabetically.
- [ ] **5. First listen** to the 18 presets for gross faults only — the sign-off is Round B's LISTENING.md. Pierce Bell wants the Tuning tab at Bohlen-Pierce.
- [ ] **6. French read of the five new rows** (`label.locate` "Localiser…", `label.locateMismatch`, `label.importTooLargeFile`, `tip.terImport`, `tip.terLocate` — `reviewed: false`) and the **zh flips** (53 at `'mt'`).

**Carried unchanged:** Stage 3's six human rows (`stages/3-gui/VERIFICATION.md` §Human — PERF-03 ⌥-click ×3 → `view-perf.log` still absent; hands-on; Logic ⌘Z; lose-context; fr / zh; Round A rows 3–10). The listening pass (Round B) is the natural moment for all of them.

## Issues Found

1. **Squarcle Storm's H2 figure is not reproducible run-to-run** (−3.8 in SUMMARY; −2.8 / −4.0 / −2.2 in three runs here, all 100 % of windows, all PASS). Cause, verified in code: the preset routes LFO1 **S&H** 4 Hz → Centre Y ± 0.05 (`FactoryPresets.cpp:269-270`), and `LFO.h:61` holds a default-constructed `juce::Random random` — seeded from the clock, outside the harness phase seed (`seed=1592590337` printed identically on every run). The other 19 preset rows are bit-stable. The row still holds DSP-06 with ≥ 2 dB of margin on the worst run seen, and the LFO is O-Prism's inherited section (FUNC-09: carried unchanged), so this is **not a Round A defect and re-opens nothing**. Recorded for Round B: either seed the S&H `Random` from the harness phase seed (a one-line `LFO` change, O-Prism-inherited — needs its own regression check) or drop the S&H route from the shipped def; until then any per-preset export golden must exclude this preset. (SUMMARY deviation 2's "−3.8, not the research's 0.0" is this same variance, not a centre error.)
2. **`--gate smoke --with-disk` counts 39 checks, not SUMMARY's 61.** The [5] rows (7), their content and the verdict match SUMMARY exactly; the 61 was not this gate's count (the gate has printed 39 since Stage 2 Round B's inversion of [4]). Bookkeeping only.
3. **No defect in the delivered code.** Every other SUMMARY figure reproduced within run-to-run noise: `--gate all` 111.4 s (115.9), H10 (d) 64 ms (62), every H2 deterministic row identical, every Playwright count identical, every validator green.
4. **Carried into Round B (unchanged from CONTEXT / SUMMARY):** the D4 padded Chebyshev evaluator (H7 Bandlimited ≈ 12 % → ≤ 2× + 2 %), `ci-tests.yml` O-Strata macOS harness + Windows VST3 / pluginval-10 jobs by manual dispatch (COMPAT-02), the listening pass (QUAL-04, `round-b/LISTENING.md`), CHANGELOG v1.0.0 / NOTES / PLUGINS.md 📦 Installed, `build-and-install.sh` with the dual-variant sweep, the WebView2 halves of UI-01 / FUNC-07 / PERF-03 at the Finding 6 ceiling; H10 (c)'s one-ulp skewed-parameter note (SUMMARY deviation 7, CHANGELOG "Known limits — persistence"); the Stage 3 human backlog.

## Stage Verdict

**Status:** ✅ VERIFIED (Round A — automated) — every Task 10 gate re-measured green from the committed tree at `29803cb5`: the `terrainImports` state child and the preset `customState` round-trip a PNG render-SHA-identically in both forms, a missing or changed source plays Sine Product with `sourceMissing` in the status push and a working Locate… that refuses a hash mismatch, the 18-preset bank covers 6 / 6 terrains, 11 / 11 orbits, feedback 3, Bandlimited 3 and passes H2 20 / 20 with the `Init` orphan swept, and COMPAT-01 holds. FUNC-08, FUNC-11 and QUAL-03 → complete in REQUIREMENTS.md; UI-04 / COMPAT-01 regressions held. Six human rows are open (Decision 25 + the fr / zh read); none carries a requirement figure that the automated rows do not already pin. **This is not the Stage 4 verdict** — Round B (Phase 4.2) closes the stage.

**Ready for next round:** Yes — `/plugin-plan O-Strata 4-polish` (Round B, Decisions 27+). No contract re-opens: the persistence format (`v=1` slot layout, `customState {v, slots[]}`) is now frozen by H10 and the on-disk bank by its stamp, so Round B tunes defaults on top of them (a defaults change re-runs H2 / H3 / H6 / H7 and re-records the export golden — CONTEXT "Listening material").

**Blockers:** none. Issue 1 is a Round B plan item (seed or drop the S&H route), not a blocker.
