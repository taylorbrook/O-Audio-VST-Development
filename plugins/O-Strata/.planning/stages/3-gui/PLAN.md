# Stage 3 — GUI (3D terrain view, pushes, view interaction, PNG import), Round B: PLAN

**Plugin:** O-Strata · **Stage:** 3 of 4 (GUI) · **Round:** B = ROADMAP Phases 3.2 + 3.3 (CONTEXT D1) · **Phase:** plan
**Date:** 2026-09-12 · **Mode:** manual · **Branch:** `main` (trunk-based; path-scoped commits under `plugins/O-Strata` + `PLUGINS.md`)
**Inputs:** `stages/3-gui/CONTEXT.md` (D1–D4, inherited contracts, requirements map), `stages/3-gui/RESEARCH.md` (§2.2 helpers + heightmap, §2.3 ring, §2.5 push shapes, §2.6 re-push, §2.7 view base + orbit golden, §2.8 PERF-03, §2.9 gesture maths, §2.10 import, §2.11 stubs, §3 C8–C10 / C13 / C14 / C16, §4 items 7 / 9 / 10 / 11–15 / 18–20, §5.1 Round B states, §5.5 gates B1–B15), `round-a/PLAN.md` (Decisions 1–26 — carried, not re-opened), `round-a/SUMMARY.md` (deliverables, deviations 1–10, handoff), `round-a/VERIFICATION.md` (✅ VERIFIED, 10 human rows open), `mockups/v2-ui.yaml` (locked — **the YAML wins**: `view_push_contract`, `interactions.view_3d`, `state.view_mode`), `mockups/v2-ui.html` (the gesture / import handlers `bindView()`, the renderer already spliced in Round A), `v2-PluginEditor-TEMPLATE.cpp` §3 (chooser / import sketches — `submitTerrainImport` is superseded by the Stage 2 API), `ROADMAP.md` v2 Phases 3.2 / 3.3, `REQUIREMENTS.md` (UI-01, UI-02, UI-03, UI-04 partial, PERF-03, FUNC-07), the code at `1bedb53d` (`Source/` as committed by `9699a166`).
**Baseline commit:** `1bedb53d` (Round A ✅ VERIFIED; `--gate all` 139 / 139; check-ui-labels 1431 / 0 over `default + 33`; layout gate 358 / 0; shell diff 0 px; tip gate 3163 at 120; 30 natives; `evaluateJavascript` 0 / 0).
**Line numbers** are not quoted as edit sites; every site is named by a token the executor re-locates by grep (memory `pattern_deferred_item_line_number_drifts_record_a_token`).
**Decision numbering** continues the Stage 3 sequence: Round A closed at 26; this plan is **27–48**.

---

## Goal

Replace the Round A placeholder with the real view and make the view editable and the import reachable. **View (3.2):** the mockup's renderer moves into `Source/ui/public/js/terrain-view.js` with the orbit maths ported verbatim from `Orbits.h` (golden ≤ 1e-4 against a harness dump), fed by four pushed events — the R32F heightmap sampled by C++ from the **active** surface (analytic / Chebyshev / image), the 30 Hz change-gated playhead from the ring's last slot, the last-cycle trail already pushed in Round A, and the status — with every push repeated after a preset apply and a session restore (`stateGeneration`), the Canvas 2D fallback + localised placeholder when WebGL2 is null, context-loss recovery, and PERF-03 instrumentation whose verdict is read from a log file (the Release WKWebView has no inspector). **Interaction + import (3.3):** drag → Orbit Centre X/Y by unprojection, wheel → Orbit Size, ⌥-drag → Rotation, one host undo step per gesture, always on the active oscillator's parameters; `Import…` → native chooser, Drop PNG on the big view or either mini canvas → bytes streamed as base64 → the Stage 2 import API; the editor applies the 2 MiB cap, selects Imported… on success and reports `{ok, reason}` so the page shows a transient notice on refusal. **Gates:** every Round A gate at its pinned number or better, the layout gate grown by WebGL2 / DPR 2 / interaction / drop sections, the orbit golden, `stub invented:` still empty with three new natives, `--gate all` 139 / 139, pluginval × 2 + auval.

**Requirements verified in Round B:** UI-01 (WKWebView half), UI-02, UI-03, UI-04 (re-push half → complete), PERF-03 (WKWebView half), FUNC-07 (chooser / drop / view, macOS half). The WebView2 halves of UI-01 / PERF-03 / FUNC-07 stay Phase 4.2 (CONTEXT D2); `sourceMissing` behaviour, `terrainImports` persistence and factory presets stay Stage 4.1.

**Round B exit:** RESEARCH §5.5 gates B1–B15 at the thresholds of Decision 46, both commits landed (+ the optional stub commit of Decision 44), `stages/3-gui/SUMMARY.md` written, the PERF-03 log and the hands-on rows queued for verify.

---

## Decisions resolved for the executor

These close RESEARCH §4 for Round B (items 7, 9, 10, 11–15, 18–20 and the Round B rows of 21) and settle every ambiguity found while planning. The executor applies them without asking; there is **no blocking question** in this plan.

| # | Question | Decision |
|---|---|---|
| 27 | Round boundary and commits | Round B = ROADMAP 3.2 + 3.3 in one execute session, **two path-scoped commits** (Decision 47): **3.2** after Wave 2 (view module, pushes, re-push, golden, PERF-03 plumbing), **3.3** at close (gestures, import, notices, gate extensions, docs). Round A's artifacts already live in `round-a/`; this plan and the Round B SUMMARY / VERIFICATION are written at `stages/3-gui/`. The checklist's B.6 / B.7 split is superseded by the ROADMAP split (CONTEXT D1). |
| 28 | View code location (RESEARCH §4.11) | **`Source/ui/public/js/terrain-view.js`**, an ES module exporting `makeScene`, `makeScene2D`, `makeSceneGL`, `viewProj`, `baseOrbit`, `orbitPoint`, `HM_N`, `HSCALE`, `sampleH`, `surfaceH`, `perf`. DOM-free at import time (no `document` access above function bodies; `makeScene*` receive the canvas). The page's single `<script type="module">` gains one static `import { … } from './js/terrain-view.js'` beside the two existing imports — still exactly one module script, no bundler. Embedded in the **one** `juce_add_binary_data` target (symbol `BinaryData::terrainview_js` — the hyphen is stripped, memory `critical_binary_data_strips_hyphens`) and served by `getResource` at `/js/terrain-view.js` as `application/javascript` (both halves, mirroring the `i18n.js` rule; the layout gate greps both — Decision 45). The Round A placeholder block (from the header comment token `PLACEHOLDER (Stage 3 Round B replaces)` through `makeScene(`) is **deleted** from `index.html`; `grep -c "PLACEHOLDER (Stage 3 Round B" index.html` = 0. `terrainH()`, `orbitXY()`, `heightmapFor()`'s local branch, `localHeightmap`, `trajectory()` and `cycleWave` remnants go with it. No prose `textContent` writes inside the module (check-i18n [12]); the HUD string stays in `index.html`. |
| 29 | Orbit maths (RESEARCH §4.12, C4, C16) | Port from `Orbits.h` **verbatim**: `rawOrbit` (Ellipse; Superellipse `n = 0.5 + 5.5 m` through the **33 × 129 bilinear LUT** over n ∈ [0.5, 6] × θ ∈ [0, π/2] with the same quadrant folding, built with `Math.pow` once at module load — < 1 ms; Limaçon `r = 1 + 2 m cos θ`; Epitrochoid `p + 1`, `d = 0.1 + 2.9 m`; Hypocycloid `p − 1`, `d = 0.1 + 1.9 m`; Butterfly `e^{cos θ} − 2 cos 4θ + 2 m sin⁵ θ` with the **C++ axis order**; Squarcle `k = 0.3 + 6 m`, Padé tanh with the ±3-clamped argument and `invTanhK`), `maxRadius` (the 64-point scan → `normFactor`), `baseOrbit` = raw × `normFactor`, and the affine in the oscillator's order (aspect on y, rotate, `r = 0.05 + 0.95 · size`, centre, clamp ±1 — `orbitPoint` already has it). Every constant is read from `Orbits.h` / `TerrainOscillator.cpp` at execution time, not from the mockup. `snapshot()` keeps feeding `orbitPoint` the raw parameter values it reads today (aspect `0.1 + 0.9 n`, rot in radians, cx / cy in [−1, 1], m in [0, 1]). |
| 30 | Orbit golden (RESEARCH §4.12) | Harness flag **`--gate orbits [--out PATH]`** under `wantsExact` (never in `--all`; `--out` defaults to `orbits.json` in the cwd; the file is **not committed** — the binary is the oracle). JSON `{ "thetaOffset": 0.5, "n": 512, "m": [0, 0.3, 0.5, 1], "affine": { "aspect": 0.7, "rotDeg": 30, "size": 0.5, "cx": 0.13, "cy": 0.21 }, "orbits": [ { "kind", "name", "m", "base": [[x, y] × 512], "affine": [[x, y] × 512] } × 44 ] }` printed `%.8g`, θ_i = 2π (i + 0.5) / 512 (half-step — the LUT nodes must not be hit, C16), `base` = `baseOrbit (kind, θ, m, scratch, SuperellipseLUT::get())` with `scratch` from `maxRadius`, `affine` = the oscillator's affine of `base` (no feedback, no clamp reached at these values). **`tests/orbit-golden.mjs`**: `import { baseOrbit, orbitPoint } from '../Source/ui/public/js/terrain-view.js'` (node ≥ 22 imports the ESM file directly), reads the JSON path from `argv[2]`, asserts `max |Δ| ≤ 1e-4` per orbit × m over base and affine, prints a 44-row table, exit 2 on any miss. Expected residual ≈ 1e-6 (Padé vs `Math.tanh` 9.4e-7, float32 3e-8). |
| 31 | Heightmap sampling (RESEARCH §4.7, C8) | In `TerrainViewFeed` (processor-only dependency, compiles in the harness console): `uint64_t heightmapKey (processor, osc)` and `void copyHeightmap (processor, osc, float* out4096)`. Grid = the mockup's corner nodes `x_i = −1 + 2 i / 63`, **`j = 0 ⇒ y = −1`**, row-major `out[j · 64 + i]` (matches the page's `sampleH` and the R32F upload). Source by Quality × Terrain: **2× / 4× analytic** → `terrain (kind, x, y, F, mx, my)` from `Terrains.h` with F = raw `osc?TerFreq` (at C4 the track ratio is 1 — Pitch Track is irrelevant to the map), mx / my raw; **2× / 4× Imported…** → `imagePtr->sample (x, y, F, edge)` with `edge` = raw `osc?TerEdge` (zeros when `imagePtr == nullptr` — the voice is silent then); **Bandlimited** → `clenshaw2D (set.c.data(), x, y)` **untapered** over `chebPtr` (analytic terrains) or `imagePtr->cheb` (Imported…), zeros when the pointer is null. Base values are the **unmodulated** `getRawParameterValue` reads; the published set already folds ModWheel / Aftertouch in Bandlimited. Non-finite → 0 (`clenshaw2D` does not guard). `TerrainImage::view` is not read (it is the box-average grid with row 0 = top; delete in Stage 4). Cost ≤ 0.3 ms per map (measured). Key = FNV-1a over `{quality, terrain, F, mx, my, edge}` quantised to 1/1024 (the scheduler's rule, token `1/1024 quantisation`) + `chebGeneration[osc]`, `imageGeneration[osc]`, `importRevision[osc]`, `stateGeneration` (Decision 34). Event **`terrainHeightmap`** = `{"osc":"A","n":64,"data":"<base64 LE Float32 4096>"}` (≈ 21.8 K chars) through `encodeFloat32Base64`. |
| 32 | Playhead event (RESEARCH §4.3 / §4.10, C9) | Name **`terrainState`** (the YAML's). Shape `{"osc":"A","theta":<rad>,"x":,"y":,"h":}` — `x, y` = the ring's displaced `px, py` (after feedback and clamp), **`h`** = the ring's `y` saturated by the oscillator's law `g = 1 + 4 · sat` (the same code path as `copyCycle`'s third column), all four rounded to 1e-4 before boxing (≈ 75 B). One extra field over the YAML's `{osc, theta, x, y}` — an extension, not a contradiction: with feedback the analytic orbit point at θ is **not** where the voice is, and the ring holds the truth. `TerrainViewFeed::getPlayhead (processor, osc, uint32_t& lastWriteIndex, out)` reads `writeIndex` (acquire), returns `false` when it has not advanced (idle — no push, the HUD keeps its last values), else slot `((w − 1) & (kPoints − 1)) · 4`. Pushed **every tick** iff advanced AND the quad changed (compare the rounded values), or forced. The page places the scan point at world `(x, h · HSCALE + 0.012, y)` and uses `theta` only for the ≋ playhead index (`round (θ / 2π · 512)`); `x, y, h` are never localised (HUD is READOUT). |
| 33 | Trail and orbit ribbon | **Trail** = the pushed cycle's `{px, py}` (`CYCLE[osc]`, Round A) lifted onto the pushed heightmap (`surfaceH (hm, p, px, py)`), drawn as the amber ribbon only while raw `osc?OrbFeedback > 0.001` (`snapshot().fb`); the JS feedback simulation `trajectory()` is **deleted** (the ring stores the displaced path — RESEARCH §2.3). **Orbit ribbon** = the ported `orbitPoint` over 320 (big) / 160 (mini) θ steps on the pushed heightmap — the undisplaced curve. Until the first `terrainHeightmap` arrives the page draws the wireframe from a flat zero map (`pushedHeightmap[osc] = null` → a zeroed `Float32Array`), never the local look-alike. After a `terrainCycle` with no heightmap yet the trail still draws at h = 0. |
| 34 | Preset / session re-push (RESEARCH §4.9, §2.6) | `std::atomic<uint32_t> stateGeneration { 0 }` on the processor beside `publishCount`; `fetch_add (1, std::memory_order_release)` as the **last statement inside the `xml != nullptr` block** of `setStateInformation` (after the `terrainImports` `ignoreUnused` line — token `getChildWithName ("terrainImports")`); public `void notifyStateChanged()` (same `fetch_add`) called by the four preset natives **after a `true` result** — `loadPresetFromCategory`, `loadPresetByName`, `selectNextPreset`, `selectPreviousPreset` (tokens `withNativeFunction ("loadPresetFromCategory"` …; `savePreset` does not bump). Editor: `uint32_t lastStateGeneration` read per tick; a moved value **forces all five pushes** (`heldNotes`, `terrainStatus`, `terrainCycle`, `terrainState`, `terrainHeightmap`) exactly like `repushPending`, and the generation sits inside the heightmap key so a restore landing identical values still re-pushes once (memory `pattern_webview_one_shot_state_push_stale_on_preset_load`). The page-side `requestTerrainRepush` handshake (boot + `__refreshAllControls`) stays. |
| 35 | Push cadence and budget (RESEARCH §2.5) | 30 Hz timer unchanged. Per tick: `pushHeldNotes`, `pushState` × 2 (O(1)); even ticks `pushStatus` × 2 + `pushCycle` × 2 (≤ 15 Hz, 128 KB copy each); ticks with `tick % 3 == 0` **`pushHeightmap` × 2** (≤ 10 Hz by construction — a knob under automation moves the key every tick, the cap is the tick filter, not the key). `force` bypasses the change gates but not the tick filters except for the heightmap, which force pushes immediately. No promise-chained pushes; every push `emitEventIfBrowserIsVisible`; `grep -c evaluateJavascript` stays 0 / 0. Nothing new touches the audio thread (`--gate H8` unchanged). |
| 36 | WebGL2 / fallback behaviour (UI-01 / UI-02) | `makeSceneGL` as spliced (R32F `texImage2D` + `texelFetch` in a `#version 300 es` vertex shader, `NEAREST`, no extension; ribbons; no GLSL `flat`; `webglcontextlost` `preventDefault` + `restored` rebuild; Retina backing per draw; dirty-only redraw), with three edits when it moves: `powerPreference: 'low-power'` restored; the 88 per-frame dashed-edge `bufferData` + `drawArrays` folded into **one static VBO** built in `initResources` (44 dashes × 4 edges as a single `LINES` or ribbon batch); `scene.debugLoseContext()` / `debugRestoreContext()` exposed through `WEBGL_lose_context` and `scene.lastGLError()` = `gl.getError()` read once after the first heightmap upload (the gate reads it — RESEARCH B8). **When `getContext ('webgl2')` is null the placeholder `#terrain-webgl-unavailable` stays visible permanently** (the mockup's 6 s auto-hide is dropped — on a 10.13–11 host the 2D path *is* the product and the badge says so); the Synth mini canvases are Canvas 2D always (unchanged). |
| 37 | PERF-03 instrumentation (RESEARCH §4.13, C13) | In the module: `perf` = a 300-frame `Float64Array` ring around the big view's `render`, **two figures** — finish-inclusive (`gl.finish()` before the second `performance.now()`) and submit-only; `perf.burst (k = 300)` = k back-to-back draws with a synthetic θ sweep and one `finish()`, total / k; `perf.snapshot()` → `{mode, dpr, w, h, n, mean, max, submitMean, burstMean}`. Page: `window.__strataPerf = { burst, snapshot }`; ⌥-click on `#terrain-hud` runs a burst (so Logic can be driven without a console); when the ring wraps and after every burst the page calls the native **`reportViewPerf (jsonString)`**. Editor: `reportViewPerf` → `DBG` + append one line `<ISO time> <json>` to `~/Library/Logs/O-Strata/view-perf.log` through `juce::FileLogger` (created lazily on first call; `juce::File::getSpecialLocation (userHomeDirectory).getChildFile ("Library/Logs/O-Strata")`), `complete (true)`. Idle cost zero (the ring is written only when a frame is drawn). Verdict path = the log file (the Release AU shows no Safari ▸ Develop entry — inspector is `JUCE_DEBUG`-only in 8.0.14); Round B verify records Standalone + Logic at DPR 2, 700 × 540, mode `webgl2` and forced 2D, finish-inclusive mean / max, submit-only mean, burst mean, machine + macOS — **partial (WKWebView)**, WebView2 → Phase 4.2 (D2). |
| 38 | View gestures (RESEARCH §4.14, C14) | Splice the mockup's `bindView()` with these changes: **capture the concrete states at mousedown** (`const sx = PROXY.terCX.target, sy = PROXY.terCY.target` / `PROXY.terRot.target` / `PROXY.terSize.target`) and use them for move / up so a gesture never straddles an Osc A ↔ B repoint; document-level `mousemove` / `mouseup`; a `mousemove` with `e.buttons === 0` while dragging ends the gesture (the WebView loses the up outside the host window); `canvas.getBoundingClientRect()` read per move; **drag** → `sx.setNormalisedValue ((clamp (hit[0], −1, 1) + 1) / 2)`, `sy` likewise from `sceneBig.unproject` (null det → ignore the move); **wheel** `{ passive: false }` → Orbit Size with the per-event step clamped `|Δ| ≤ 0.05` (`clamp (−e.deltaY · 0.0012, −0.05, 0.05)`), the burst wrapped in one gesture — first wheel → `sliderDragStarted()`, a 250 ms debounce → `sliderDragEnded()`; **⌥-drag** → Rotation `base + dx · 0.5 / 360`, wrapped `((v % 1) + 1) % 1`; **⌥-click** (mouseup with < 3 px travel) → Rotation reset to 0 inside the same Started / Ended pair; nothing else resets from the view (knob ⌥-click is the reset surface). Only `#terrainView` is editable (a mini canvas pixel is 2.6e-2 in x — too coarse); `e.preventDefault()` on mousedown; hover brightens `#terrain-hud` (CSS, already there). The `TERFREQ_ADAPTER` is untouched (no view gesture writes Terrain Freq). |
| 39 | Import natives (RESEARCH §4.15, C10) | Two natives, names fixed by the page: **`chooseTerrainImage (osc)`** → `FileChooser ("Import Terrain Image", userPicturesDirectory, "*.png")`, `launchAsync (openMode | canSelectFiles, …)` with `Component::SafePointer` captured and a **bare `return` on the null path** (memory `pattern_webview_launchasync_safepointer_no_complete`); cancel (`getResult() == File()`) → `complete ({ok:false, reason:"cancelled"})`, parameter untouched; `file.getSize() > 2 · 1024 · 1024` → `{ok:false, reason:"tooLarge"}`; `importTerrainFile (osc, file)` false → `{ok:false, reason:"undecodable"}`; true → select Imported… (below), `repushPending = true`, `{ok:true}`. **`importTerrainImageData (osc, name, base64)`** → `juce::Base64::convertFromBase64 (MemoryOutputStream&, …)` (never `MemoryBlock::fromBase64Encoding`); decode failure → `undecodable`; size > 2 MiB → `tooLarge`; `importTerrainImage (osc, MemoryBlock, name)` false → `undecodable`; true → select + repush + `{ok:true}`. **Single call**, no chunking (proven up to tens of MB by O-MicrotonalSampler; a 2 MiB PNG is 2.7 MB of base64). `osc` arrives as `"A"` / `"B"`. The five inherited choosers are untouched. Result objects are built with `juce::DynamicObject` (`ok` bool, `reason` string). |
| 40 | Selecting Imported… from C++ | Editor helper `selectImportedTerrain (int osc)` (message thread): `auto* p = apvts.getParameter ("osc" + AB + "Terrain")`; `target = p->convertTo0to1 (6.0f)` (Imported… is index 6 of 7 — token `Imported…` in the choice list); if `std::abs (p->getValue() − target) < 1e-6` return (no spurious undo step); else `beginChangeGesture(); setValueNotifyingHost (target); endChangeGesture()`. The combo relay pushes `valueChanged` → the page's listener → `refreshInert()` un-greys `#terrain-image-row` — no extra page code. The processor API never touches parameters (unchanged). |
| 41 | Page import flow + notices (RESEARCH §4.15, §2.10) | `#btn-terImport` click → `chooseTerrainImage (ACTIVE_OSC)`. Drop targets `#terrain-view-wrap`, `#canvasWrapA`, `#canvasWrapB`: `dragover` → `preventDefault` + `.drag-over` (the `.wt-drop-overlay` shows `label.dropPng`), `dragleave` / `drop` clear it; a document-level `dragover` / `drop` `preventDefault` so a missed drop never navigates the WebView; `readEntryFile` = `webkitGetAsEntry().file()` with the `getAsFile` fallback (Windows `dataTransfer.files`); guards **in this order**: name `/\.png$/i` **and** the 8-byte PNG signature `89 50 4E 47 0D 0A 1A 0A` → else `label.importFailed`; `file.size > 2 · 1024 · 1024` → `label.importTooLarge`, **no native call**; then `arrayBuffer` → base64 (32 K-chunked `String.fromCharCode` → `btoa`) → `importTerrainImageData (osc, file.name, b64)`. Both natives' results: `ok:false` + `tooLarge` → `label.importTooLarge`; `undecodable` → `label.importFailed`; `cancelled` → nothing. **Notice** = `#terrain-notice` reused transiently: `showTerrainNotice (key)` with **two literal `setLabel` calls under `if / else if`** (`'label.importTooLarge'`, `'label.importFailed'` — never a ternary, check-i18n [13]), adds `.visible`, and after 4 s restores `setLabel (el, 'label.sourceMissing')` + the STATUS-driven visibility; a second call restarts the timer. Test seam `window.__showTerrainNotice (key, holdMs)` with `holdMs = 0` meaning sticky (for the state walk) — the same kind of seam as `window.__setLanguage`. Drop on a mini canvas imports into that card's oscillator (`data-osc`); on the big view into `ACTIVE_OSC`. |
| 42 | i18n for Round B (RESEARCH §4.16 tail) | Two LABELS keys: `label.importTooLarge` — en `Image over 2 MB — not imported`, fr `Image de plus de 2 Mo — non importée` (`reviewed: false`), zh `图片超过 2 MB——未导入` (`'mt'`); `label.importFailed` — en `Could not read image — terrain unchanged`, fr `Image illisible — terrain inchangé` (`reviewed: false`), zh `无法读取图片——地形未更改` (`'mt'`). fr / zh lints run **before** the page references the keys (order of RESEARCH §2.12); numerals and `MB` / `Mo` follow the fr unit rule (U+00A0 before the unit). No new tips, no `TIP_BINDINGS` change (the tip gate literal stays 120). `label.hintView` (already tabled and rendered in `.terrain-hint`) now describes real gestures. `aria.view3d` / `aria.viewWaveform` unchanged. 48 zh rows at `'mt'` after this round (46 + 2); flags flipped by Taylor at verify as in Round A. |
| 43 | Stub fixtures + state file (RESEARCH §4.18–19, §5.1) | `tests/tools/gen-stub-overrides.mjs`: extend its **constant-natives table** (token `requestTerrainRepush: true`) with `chooseTerrainImage: { ok: false, reason: 'cancelled' }`, `importTerrainImageData: { ok: true }`, `reportViewPerf: true`, then **re-run the generator** (`O-Strata-render-test --dump-choices | node tests/tools/gen-stub-overrides.mjs`) — never hand-edit `generic-overrides.json` (memory `pattern_test_fixture_mirrors_drift_silently`). `tests/i18n-states.json` appends, after `terrain-cycle-pushed` and before `terrain-wave-view`: `terrain-heightmap-pushed` (eval that **computes** a 64 × 64 sine-product surface in-page, encodes it LE Float32 → base64 and `__stubEmit ('terrainHeightmap', {osc:'A', n:64, data})` — no 22 KB literal), `terrain-playhead-pushed` (`__stubEmit ('terrainState', {osc:'A', theta:1.2, x:0.3, y:0.1, h:0.4})`), `terrain-drop-overlay` (`dispatchEvent (new DragEvent ('dragover', {bubbles:true, cancelable:true, dataTransfer:new DataTransfer()}))` on `#terrain-view-wrap`), `terrain-drop-overlay-off` (`dragleave` likewise), `terrain-import-notice` (`window.__showTerrainNotice ('label.importFailed', 0)`), `terrain-notice-clear` (`window.__showTerrainNotice (null, 0)` restores), `terrain-webgl-unavailable` (`classList.add ('visible')` on `#terrain-webgl-unavailable` — geometry only; the real forced-null path is the layout gate's) and `terrain-webgl-available-back` (remove). → `states: default + 41`. |
| 44 | Native-call spy for the gates (RESEARCH B13 `[ASSUMPTION]`) | The generic stub records nothing per call today. Add **three lines** to `scripts/ui-stub/generic-juce-stub.js`: every wrapper returned by `getNativeFunction` pushes `{ name, args }` onto `window.__stubNativeCalls` (an array created in the preamble block beside `__stubUnknownNativeFns`). Repo-wide but inert for every other plugin (a growing array nobody reads). Committed **separately**, path-scoped: `test(ui-stub): record native calls on window.__stubNativeCalls` (`git commit -- scripts/ui-stub/generic-juce-stub.js`), before commit 3.3. The drop and gesture sections of the layout gate read it; the gesture spy on `sliderDragStarted / Ended` is done by patching the **instances** in `window.__stubStates.sliders` from `page.evaluate` before the gesture (no stub change needed). |
| 45 | Layout-gate extensions (RESEARCH §4.20, B8 / B12 / B13) | `tests/ui_layout_check.js` gains four sections after the existing walk, each with PASS / FAIL rows and the same exit codes: **`webgl`** — in the DPR 1 context and a second `deviceScaleFactor: 2` context: `#terrainView` `clientWidth × clientHeight` = 700 × 540 inside the 704 × 544 wrap, `canvas.width === 700 · DPR`, `canvas.height === 540 · DPR`; `window.__strataScene.kind === 'webgl2'` (the page exposes `sceneBig` as `window.__strataScene`, a seam like `__strataPerf`); `lastGLError() === 0` after `terrain-heightmap-pushed`; `debugLoseContext()` → no `pageerror`, `debugRestoreContext()` → a frame draws (`perf.snapshot().n` advances); **`fallback`** — a third context with `addInitScript` overriding `HTMLCanvasElement.prototype.getContext` to return `null` for `'webgl2'`: `#terrain-webgl-unavailable` visible in en / fr / zh-Hans, `__strataScene.kind === '2d'`, every existing layout assertion re-run on the Terrain tab; **`interaction`** — patch `sliderDragStarted / Ended` on the `oscAOrbCX / CY / Pos / OrbRot` stub instances to count; `page.mouse` drag across `#terrainView` → CX and CY values change and Started / Ended counted **exactly once each**; `page.mouse.wheel (0, 120)` → `oscAPos` moves by ≤ 0.05 and one Started / Ended pair after 300 ms; ⌥-drag (`page.keyboard.down ('Alt')`) → `oscAOrbRot` changes; ⌥-click < 3 px → `oscAOrbRot` = 0; after `terrain-osc-b` the same drag moves `oscBOrbCX / CY` and leaves `oscA*` unchanged; **`drop`** — `page.evaluate` builds a `File` from a 1 × 1 PNG (67-byte literal), dispatches `drop` on `#terrainView` with a `DataTransfer` → `__stubNativeCalls` holds exactly one `importTerrainImageData` with `['A', 'probe.png', <base64>]`; a 2 MiB + 1 byte `File` named `.png` → `#terrain-notice` visible with `label.importTooLarge`, no new call; a `.jpg` name → `label.importFailed`, no new call; drop on `#canvasWrapB` → call with `'B'`. Source greps added: `PLACEHOLDER (Stage 3 Round B` = 0; `terrain-view.js` present in `CMakeLists.txt` **and** in `getResource` (mirror of check-i18n [8]); `reportViewPerf` / `chooseTerrainImage` / `importTerrainImageData` each in both `index.html` and `PluginEditor.cpp`. Liveness stays `--viewport 1200x780`. `tests/ui_shell_diff_check.js` unchanged (re-run). |
| 46 | Round B verify thresholds (RESEARCH §4.21 / §5.5) | check-i18n `ALL CHECKS PASS` 16 / 16; fr lint `CLEAN — exit 0`; zh lint `GATE PASSED — exit 0` (48 at `'mt'`); check-ui-labels exit 0, `states: default + 41`, PASS ≥ 1431; boot-all-uis `DEAD 0 / late 0 / title= 0`, no `pageerror` / `404:` / **`stub invented:`** with 33 natives; tip gate `ALL CHECKS PASSED (3163)` at 120, five tabs; measure-ui `undeclared-font ≤ 5`, `wrap-count 0`; CDP probe 0 Han runs off PingFang SC; layout gate `ALL PASS` incl. the four new sections, liveness fails; shell diff 0 px outside masks, control fires; native parity `comm -3` empty (33 = 33); `evaluateJavascript` 0 / 0; viewport units 0; one module script; **`--gate orbits` + `orbit-golden.mjs` max Δ ≤ 1e-4 over 44 curves × base + affine**; `O-Strata-render-test --gate all` = 139 / 139 ≤ 3 min from the repo root; `--gate H8` 0 allocations; pluginval strictness 10 × 2 SUCCESS; `auval -v aumu OuSt OuDv` SUCCEEDED; `params.tsv` diff empty; 0 new warnings in the touched C++ files (`-Wswitch-enum` at `TerrainOscillator.cpp` is Phase 2.1's, not counted); **PERF-03** `~/Library/Logs/O-Strata/view-perf.log` rows for Standalone and Logic at DPR 2: finish-inclusive mean ≤ 2 ms in `webgl2`, the forced-2D number reported (not gated), recorded partial (WKWebView). |
| 47 | Commits | Two path-scoped commits, no tag (memory `feedback_never_tag_unless_publish`): **3.2** after Wave 2 (`feat(O-Strata): Phase 3.2 — 3D terrain view, playhead, feedback trail, preset re-push`, the ROADMAP message; paths `plugins/O-Strata/Source plugins/O-Strata/CMakeLists.txt plugins/O-Strata/tests`) and **3.3** at close (`feat(O-Strata): Phase 3.3 — view interaction, PNG import via chooser and drag-and-drop`; `git commit -- plugins/O-Strata PLUGINS.md`), with Decision 44's stub commit in between if the spy lands. `git branch --show-current` + `git status --short` **immediately before each**; explicit paths only; the untracked `stages/1-foundation/gate-report.json`, `stages/2-dsp/gate-report.json`, `.claude/agent-memory/*` and the O-Gain working-tree changes belong to other sessions and are **never** added. |
| 48 | Not touched this round | `parameter-spec.md`, `params.tsv` (diff must stay empty), `Orbits.h`, `Terrains.h`, `ChebyshevSet.h`, `TerrainImage.*`, `TerrainOscillator.*`, `TerrainScheduler.*`, `StrataVoice.*`; the nine byte-identical inherited page regions (the shell-diff gate and check-i18n [6] re-prove it); the five inherited choosers; `js/i18n.js` beyond the two keys; the Round A layout / shell-diff / tip-gate assertions (only additions); O-Prism's tree. Member order relays → webView → attachments unchanged; Windows `withUserDataFolder ("OStrata_WebView")` kept; the offline harness still configures with `JUCE_WEB_BROWSER=0` (`TerrainViewFeed.cpp` keeps its processor-only dependency). |

---

## Tasks

Four waves, two feature commits (+ one optional stub commit). Each task ends with the check the executor runs before moving on. Builds, `--gate all`, check-ui-labels and the tip gate go to the background (executor 600 s watchdog; the tip gate alone is ≈ 3 min).

### ━━━ Wave 1 — Feed helpers, state generation, harness dump, the view module (parallel: disjoint files) ━━━

### Task 1 — `stateGeneration` + `notifyStateChanged` (Decision 34)
- **Modify:** `Source/PluginProcessor.h/.cpp` — `std::atomic<uint32_t> stateGeneration { 0 }` beside `publishCount` (token `std::atomic<int>   publishCount`); `uint32_t getStateGeneration() const` (acquire); `void notifyStateChanged()`; the `fetch_add` as the last statement of the `xml != nullptr` block in `setStateInformation` (token `getChildWithName ("terrainImports")`).
- **Modify:** `Source/PluginEditor.cpp` — the four preset natives call `processorRef.notifyStateChanged()` when their result is `true` (tokens `withNativeFunction ("loadPresetFromCategory"`, `("loadPresetByName"`, `("selectNextPreset"`, `("selectPreviousPreset"`); `savePreset` untouched.
- **Check:** plugin + harness targets compile; `--gate scheduler` row (f) (`setStateInformation` at the default patch) still passes — `publishCount` stays 0, only `stateGeneration` moves.

### Task 2 — `TerrainViewFeed`: playhead + heightmap + two event builders (Decisions 31, 32)
- **Modify:** `Source/TerrainViewFeed.h/.cpp` — `struct Playhead { float theta, x, y, h; }`; `bool getPlayhead (OStrataAudioProcessor&, int osc, uint32_t& lastWriteIndex, Playhead& out)` (the saturation law shared with `copyCycle` — lift the `g = 1 + 4 · sat` block into one static helper); `uint64_t heightmapKey (OStrataAudioProcessor&, int osc)` (FNV-1a over the quantised inputs + the four generations); `void copyHeightmap (OStrataAudioProcessor&, int osc, float* out4096)` per Decision 31 (includes `dsp/Terrains.h`, `dsp/ChebyshevSet.h`, `dsp/TerrainImage.h` — all already reachable from `PluginProcessor.h`); `juce::var makeStateEvent (int osc, const Playhead&)` (rounded 1e-4); `juce::var makeHeightmapEvent (int osc, const float* out4096)` (`encodeFloat32Base64`). The header comment's "Round B adds …" sentence becomes the description.
- **Check:** compiles in the plugin **and** `O-Strata-render-test` (`JUCE_WEB_BROWSER=0`); a 6-line scratch in the harness (not committed) or a `DBG` in a debug build shows `copyHeightmap` at the default patch returns 4096 finite values with `|h| ≤ 1` and the centre node `out[32 · 64 + 32]` ≈ `terrain (SineProduct, x_32, y_32, 1, 0.5, 0.5)` — matching the analytic call bit-for-bit.

### Task 3 — Harness `--gate orbits [--out PATH]` (Decision 30)
- **Modify:** `tests/render-harness/main.cpp` — `gateOrbits()` under `wantsExact ("orbits")` (token `if (wantsExact ("export")) gateExport();` — add beside it), `--out` parsed like `--dump-choices` (token `else if (a == "--dump-choices")`), the usage line and `README.md` "Run" list + a "Gates (Round B, Stage 3)" row (`orbits` — never in `all`, writes a file). JSON per Decision 30; `SuperellipseLUT::get()` + `OrbitDetail::maxRadius` for `scratch.normFactor` (and `invTanhK` for Squarcle exactly as `TerrainOscillator::updateBlockRate` computes it — read that site, token `invTanhK`); `%.8g`.
- **Check:** `O-Strata-render-test --gate orbits --out <scratch>/orbits.json` writes ≈ 1.1 MB that `python3 -m json.tool` parses with 44 `orbits` entries × 512 × 2 × 2; `--gate all` still reports 139 checks (orbits excluded).

### Task 4 — `Source/ui/public/js/terrain-view.js` (Decisions 28, 29, 36, 37)
- **Create:** the module from the page's spliced renderer block: `TAU`, `HM_N`, `HSCALE`, `perspective / lookAt / mmul / CAM / viewProjAt / FIT_CORNERS / viewProj`, `sampleH`, `surfaceH`, the **ported** `SuperellipseLUT` (built at module load) + `rawOrbit` + `maxRadius` + `baseOrbit` + `orbitPoint (p, θ)` (Decision 29 — read `Orbits.h` and `TerrainOscillator.cpp`'s affine block while porting, constant by constant), `makeScene2D`, `makeSceneGL` with the three Decision 36 edits + `debugLoseContext / debugRestoreContext / lastGLError`, `makeScene (canvas, grid, step, preferGL, onGLUnavailable)` (the placeholder toggle becomes a callback the page supplies — the module stays DOM-free), the `perf` object (Decision 37). **Trail** signature: `render (hm, p, playhead, cycle, mode, big)` — the trail comes from `cycle` (Float32Array 512 × 3 or null), the scan point from `playhead` `{theta, x, y, h}` (Decision 33); `trajectory()` and `cycleFor()`'s waveform half stay page-side only where the ≋ path needs them (move `cycleFor` into the module too — it is pure). No `document.getElementById` outside `makeScene*` bodies; no i18n strings.
- **Check:** `node -e "import('./plugins/O-Strata/Source/ui/public/js/terrain-view.js').then(m => console.log(Object.keys(m)))"` lists the exports with no DOM access at import; `node tests/orbit-golden.mjs` is Task 6's.

### ━━━ Wave 2 — Page + editor wiring, golden, commit 3.2 ━━━

### Task 5 — Editor: five pushes, three natives, Imported… selection, perf log, resource route, CMake (Decisions 32, 34, 35, 37, 39, 40)
- **Modify:** `Source/PluginEditor.h/.cpp` — members `uint32_t lastWriteIndexState[2]`, `TerrainViewFeed::Playhead lastState[2]` + `hasLastState[2]`, `uint64_t lastHeightmapKey[2]`, `std::array<float, 4096> heightmapOut`, `uint32_t lastStateGeneration`, `int tick`, `std::unique_ptr<juce::FileLogger> perfLog`; `pushState (osc, force)`, `pushHeightmap (osc, force)`, `selectImportedTerrain (osc)`, `logViewPerf (json)`; `timerCallback` per Decision 35 (the `stateGeneration` compare sets `force` exactly like `repushPending`); natives `chooseTerrainImage`, `importTerrainImageData` (Decision 39 — model the chooser on the inherited `loadScalaFile` block, token `withNativeFunction ("loadScalaFile"`), `reportViewPerf` (Decision 37); the `/js/terrain-view.js` route after the `check_native_interop_js` branch (token `check_native_interop_js`). The header comment on `timerCallback` lists all five events.
- **Modify:** `CMakeLists.txt` — `Source/ui/public/js/terrain-view.js` in the one `juce_add_binary_data` (token `shell_conchologiaiconi12reev_0090.png`); the "Round B's terrain view module goes HERE too" comment resolved.
- **Check:** `grep -c withNativeFunction Source/PluginEditor.cpp` = 33; `grep -c evaluateJavascript` = 0; `ninja O-Strata_VST3 O-Strata_AU O-Strata_Standalone` (background) clean, 0 new warnings; `--gate all` 139 / 139 (the harness excludes the editor TU — this proves `TerrainViewFeed` still links without it).

### Task 6 — `tests/orbit-golden.mjs` (Decision 30)
- **Create:** per Decision 30; usage `node plugins/O-Strata/tests/orbit-golden.mjs <orbits.json>`; the affine sample uses `orbitPoint` with `p = {orbit, m, size 0.5, aspect 0.7, rot 30°, cx 0.13, cy 0.21}` in the page's `snapshot()` units.
- **Check:** max Δ ≤ 1e-4 on all 44 rows (expected ≈ 1e-6); a planted `+1e-3` on the JS Limaçon constant → the Limaçon rows FAIL (liveness; revert).

### Task 7 — Page: import the module, delete the placeholder, wire the four events, HUD, perf seam (Decisions 28, 32, 33, 36, 37)
- **Modify:** `Source/ui/public/index.html` (module script) — `import { makeScene, viewProj, orbitPoint, HM_N, HSCALE, sampleH, surfaceH, cycleFor, perf } from './js/terrain-view.js'` beside the two imports; delete the placeholder block (Decision 28); `pushedHeightmap[osc]` null → a shared zeroed `Float32Array (4096)` with `gen 0` for the renderer; the `terrainState` listener stores `{theta, x, y, h}`; `frame()` passes `PLAYHEAD[osc]` and `CYCLE[osc]` to `render`; `makeScene (…, true, () => badge.classList.add ('visible'))` for the big view (no auto-hide); `window.__strataScene = sceneBig`; `window.__strataPerf`; ⌥-click on `#terrain-hud` → `perf.burst (300)` → `reportViewPerf (JSON.stringify (perf.snapshot()))`; ring-wrap report from a `perf.onWrap` hook; the HUD line unchanged (READOUT). `snapshot()` unchanged except `fb` is read raw for the trail gate.
- **Check:** RESEARCH §5.3 one-liner (parity `comm -3` empty at 33 / 33, `evaluateJavascript` 0 / 0, viewport units 0, one module); `node scripts/boot-all-uis.js --plugin O-Strata --strict-tips --verbose` → no `pageerror`, `stub invented:` empty **only after Task 10** (expect three invented names until then — note them, do not "fix" the page); `node scripts/check-i18n.js --plugin O-Strata` 16 / 16 ([8] and [12] cover the new module file: it is embedded, served and prose-free).
- **Commit 3.2** (Decision 47) after `--gate all` 139 / 139, the golden, boot and check-i18n are green — branch / status re-check first.

### ━━━ Wave 3 — Gestures, import flow, i18n, fixtures, gate extensions (parallel where files are disjoint; 8 → 9 sequential on `index.html`) ━━━

### Task 8 — View gestures (Decision 38)
- **Modify:** `Source/ui/public/index.html` — `bindView()` from the mockup with the Decision 38 changes, placed where the Round A comment says "View gestures … are Round B" (token `the view is display-only in Round A`); the comment deleted.
- **Check:** manual under `serve-ui` in a browser: drag moves the point under the cursor, wheel ± ≤ 5 % per notch, ⌥-drag rotates, ⌥-click resets rotation; Task 11's `interaction` section is the gate.

### Task 9 — Import flow + notices + i18n keys (Decisions 41, 42)
- **Modify:** `Source/ui/public/js/i18n.js` — the two LABELS keys of Decision 42 (fr `reviewed: false`, zh `'mt'`); run the lints **first**.
- **Modify:** `Source/ui/public/index.html` — `#btn-terImport` click, the three drop targets, document-level `dragover` / `drop` guards, `readEntryFile`, the guard order, `showTerrainNotice (key, holdMs)` + `window.__showTerrainNotice`; the Round A comment "Import… … are markup only in Round A" (token `markup only in Round A`) deleted.
- **Check (in this order):** `node scripts/i18n-fr-lint.js --plugin O-Strata --verbose` exit 0 → `node scripts/i18n-zh-lint.js --plugin O-Strata --verbose` exit 0 (48 `'mt'`) → `node scripts/check-i18n.js --plugin O-Strata` 16 / 16 ([15]: both keys referenced, [13]: literal keys).

### Task 10 — Stub natives (generated) + state file (Decision 43)
- **Modify:** `tests/tools/gen-stub-overrides.mjs` (three constants) → re-run → `tests/ui-stub/generic-overrides.json` regenerated; `tests/i18n-states.json` + 8 states per Decision 43.
- **Check:** `node scripts/boot-all-uis.js --plugin O-Strata --strict-tips --verbose` → `stub invented:` **empty**, DEAD 0 / late 0; `node scripts/check-ui-labels.js --plugin O-Strata` (background) → exit 0, `states: default + 41`, PASS ≥ 1431, the notice / overlay / placeholder states reachable (their labels measured in three languages).

### Task 11 — Stub call spy + layout-gate sections (Decisions 44, 45)
- **Modify:** `scripts/ui-stub/generic-juce-stub.js` — `window.__stubNativeCalls = []` in the preamble block (token `window.__stubUnknownNativeFns = report.nativeUnknown;`) and a `push ({ name, args })` in each of the three wrapper closures of `getNativeFunction` (token `export function getNativeFunction`). Commit separately (`test(ui-stub): record native calls on window.__stubNativeCalls`, path-scoped to that file) after `node scripts/boot-all-uis.js --plugin O-Prism` and `--plugin O-Strata` both still boot clean.
- **Modify:** `tests/ui_layout_check.js` — the `webgl`, `fallback`, `interaction`, `drop` sections and the added source greps of Decision 45 (the 1 × 1 PNG literal as a base64 string in the file; a `File` of 2 MiB + 1 bytes built from an `ArrayBuffer`, never a literal). Header comment updated.
- **Check:** `node plugins/O-Strata/tests/ui_layout_check.js` → ALL PASS with the new sections listed; `--viewport 1200x780` → FAIL (liveness); a temporarily planted `return;` at the top of `bindView()` makes the `interaction` rows FAIL (liveness; revert) — memory `pattern_zipper_sweep_probe_needs_liveness_gate`.

### ━━━ Wave 4 — Full gate run, install, PERF-03, hands-on, docs, commit 3.3 ━━━

### Task 12 — Gate run B1–B14 (Decision 46)
- Run RESEARCH §5.5 B1–B14 in order (the Round A commands A1–A12 re-run + B8 / B9 / B11–B13 through the new sections), background the long ones, logs under `stages/3-gui/round-b-logs/` (gitignored if large — otherwise commit the summaries only). Fix and re-run until every threshold of Decision 46 holds. Record the numbers (they are the SUMMARY's table).

### Task 13 — Install, validate, PERF-03 log, hands-on (Decision 37, 46; UI-01–04, FUNC-07)
- `./scripts/build-and-install.sh O-Strata` (background); pluginval strictness 10 on VST3 + AU; `auval -v aumu OuSt OuDv`; `params.tsv` diff vs the rebuilt `O-Strata-param-dump` empty.
- **PERF-03:** Standalone (DPR 2 display) → Terrain tab → hold a note (tick the MIDI input under Options → Audio/MIDI Settings first — the macOS Standalone does not auto-enable it, VERIFICATION §Human) → ⌥-click `#terrain-hud` three times → read `~/Library/Logs/O-Strata/view-perf.log`; repeat in Logic (AU) and once with `webgl2` forced off (a Debug Standalone with `getContext` patched from the inspector, or the `fallback` numbers from the layout gate at DPR 2 as the headless stand-in, labelled as such). Record mean / max / submit / burst per row.
- **Hands-on (recorded in SUMMARY as a checklist, the verify phase re-runs it):** the wireframe changes with Terrain / Terrain Freq / Mod X / Mod Y and with Blur / Edge on an imported PNG; the orbit curve matches the ≋ trace's extent; the scan point rides the displaced path with Feedback > 0 and the amber trail appears only then; preset `Init` apply and a session restore (save the Standalone session, quit, relaunch) re-push the map and the playhead within one interval; drag / wheel / ⌥-drag edit the active oscillator's CX / CY / Size / Rotation with **one undo step each in Logic** (⌘Z once undoes a whole drag); after Osc B is selected the gestures edit `oscB*`; `Import…` picks a PNG → Terrain reads Imported…, Blur / Edge un-grey, the map shows the picture; drop a PNG on the big view and on the Osc B mini canvas; a 10 MB PNG → the amber "over 2 MB" notice for 4 s, terrain unchanged; a renamed JPEG `.png` → "could not read" notice; `WEBGL_lose_context` via `window.__strataScene.debugLoseContext()` in a Debug Standalone recovers; the ≋ view, readout and held-notes bar still behave as in Round A.

### Task 14 — Docs, STATUS, SUMMARY, commit 3.3 (Decision 47)
- **Modify:** `CHANGELOG.md` (v1.0.0 unreleased — "Stage 3, Round B — 3D terrain view, playhead, feedback trail, preset re-push (Phase 3.2) and view interaction, PNG import via chooser and drag-and-drop (Phase 3.3), 2026-09-12" with the module, pushes, `stateGeneration`, PERF-03 log path, gestures, import cap + notices, and the Stage 4 carry list), `PLUGINS.md` row (🚧 Stage 3 GUI — Round B executed), `.planning/STATUS.md` (execute (Round B) row ✓, `next_action` → `/plugin-verify O-Strata 3-gui`), `.planning/stages/3-gui/SUMMARY.md` (tasks 1–14, every gate number incl. the PERF-03 rows, deviations, "Open for verify": fr read of the 2 new rows, zh flips (48), the PERF-03 Logic row if the Standalone-only run stood in, the hands-on rows, the Round A human rows still open).
- **Commit 3.3:** `git commit -- plugins/O-Strata PLUGINS.md` after the branch / status re-check (path-scoped; the two untracked `gate-report.json` files, `.claude/agent-memory/*` and the O-Gain working-tree changes are never added).

---

## Parallelism

| Wave | Tasks | Can run together | Blocked by |
|---|---|---|---|
| 1 | 1, 2, 3, 4 | yes (processor / editor-preset-natives vs `TerrainViewFeed` vs harness vs the JS module — the two `PluginProcessor` edits of Tasks 1 and 2 touch different regions; Task 1's editor edit is inside the preset natives, Task 5's is elsewhere) | — |
| 2 | 5, 6, 7 (+ commit 3.2) | 5 ∥ 6; 7 after 5 (the route + natives must exist for `boot-all-uis`) and after 4 | 1–4 |
| 3 | 8 → 9 (`index.html`); 10 ∥ 11 alongside | 10 and 11 with 8 / 9; 9's `i18n.js` half first | 7 |
| 4 | 12 → 13 → 14 | 13's PERF-03 rows and hands-on may interleave with 12's long gates | 8–11 |

---

## Files

**Create:** `Source/ui/public/js/terrain-view.js`, `tests/orbit-golden.mjs`, `.planning/stages/3-gui/SUMMARY.md`.
**Modify:** `Source/ui/public/index.html`, `Source/ui/public/js/i18n.js` (2 keys), `Source/PluginEditor.h/.cpp`, `Source/PluginProcessor.h/.cpp` (`stateGeneration` only), `Source/TerrainViewFeed.h/.cpp`, `CMakeLists.txt`, `tests/render-harness/main.cpp` + `README.md`, `tests/tools/gen-stub-overrides.mjs`, `tests/ui-stub/generic-overrides.json` (regenerated), `tests/i18n-states.json`, `tests/ui_layout_check.js`, `scripts/ui-stub/generic-juce-stub.js` (separate commit), `CHANGELOG.md`, `.planning/STATUS.md`, `PLUGINS.md`.
**Untouched (must stay byte-identical to O-Prism):** the nine inherited page regions of Round A.
**Not touched this round:** Decision 48's list.

---

## Success criteria (ROADMAP 3.2 + 3.3 test criteria as amended by CONTEXT D2 and Decisions 27–48)

- [ ] The big view renders through WebGL2 in the Standalone and in Logic (WKWebView); forcing `getContext ('webgl2')` → null shows the Canvas 2D path with the localised placeholder in en / fr / zh-Hans and every layout assertion holds; `WEBGL_lose_context` recovers (UI-01 macOS half, UI-02)
- [ ] The wireframe is the pushed heightmap of the **active** surface (analytic at F, Chebyshev untapered, image with Blur / Edge honoured); the orbit curve is `Orbits.h` (golden ≤ 1e-4 over 44 curves, base + affine); the scan point rides the ring's displaced path at 30 Hz, unchanged state sends no event; the trail is the pushed cycle and shows only while Feedback > 0 (UI-01)
- [ ] After a preset apply and after a session restore with the editor open, the map and the playhead are re-pushed within one interval (`stateGeneration`) (UI-04 → complete)
- [ ] PERF-03: finish-inclusive mean ≤ 2 ms per frame at DPR 2 in WKWebView (Standalone + Logic rows in `view-perf.log`); recorded partial (WKWebView), WebView2 → 4.2
- [ ] Drag edits Orbit Centre X / Y under the cursor, wheel edits Orbit Size (≤ 5 % per notch, one undo step per burst), ⌥-drag edits Rotation, ⌥-click resets Rotation — one host undo step per gesture, always on the active oscillator (UI-03)
- [ ] PNG loads via the chooser and via drop on the big view and either mini canvas on macOS; a successful import selects Imported… from C++ and un-greys Blur / Edge; Blur and Edge are visible in the view (FUNC-07 macOS half)
- [ ] > 2 MiB refused with the `label.importTooLarge` notice and no native call; an undecodable file keeps the previous terrain with `label.importFailed`; cancel leaves the parameter alone; importing with the editor closed still publishes (API unchanged)
- [ ] Gates at Decision 46: check-i18n 16 / 16; fr / zh lints exit 0 (48 `'mt'`); check-ui-labels `default + 41` / ≥ 1431 / exit 0; boot DEAD 0 / late 0, `stub invented:` empty at 33 natives; tip gate 3163 at 120; measure-ui ≤ 5 / 0; CDP clean; layout gate ALL PASS with `webgl` / `fallback` / `interaction` / `drop`, liveness fails; shell diff 0 px; parity empty; `evaluateJavascript` 0 / 0; placeholder token 0; module embedded **and** served
- [ ] `--gate all` 139 / 139 ≤ 3 min; `--gate H8` 0 allocations; `--gate orbits` + golden green; pluginval × 2 SUCCESS; auval SUCCEEDED; `params.tsv` diff empty; 0 new warnings
- [ ] Two path-scoped commits (+ the stub commit) landed; SUMMARY.md + CHANGELOG + PLUGINS.md + STATUS updated; no tag

---

## Out of scope (do not do in this round)

- Windows / WebView2 evidence for UI-01, PERF-03, FUNC-07 (`dataTransfer.files` ships as the fallback but is measured on the Phase 4.2 CI build — CONTEXT D2).
- `terrainImports` persistence content, the path + SHA form above the cap, "Locate…", `sourceMissing` behaviour (the flag stays hard-false; the notice markup is exercised only by the transient import notices), factory presets, `TerrainImage::view` deletion — Stage 4.1.
- Base64 chunking per native call (optional hardening — single-call is the proven pattern); a keyboard widget for the D3 hide rule; any change to the D3 τ / 108 constants (Taylor's verify feel row).
- Round A's open human rows (fr read of 46 rows, zh triples, MIDI / Logic hands-on 3–10) — they stay open for the Round B verify session, which re-runs them on the finished stage.
- O-Prism `min-width: 0` + `.settings-toggle` port (D4), repo-wide glossary TERMS, promoting `cdp-font-probe.js` to `scripts/`, vectorised Chebyshev basis, muted-row soft knee, ASan re-run — carried unchanged.
- Any APVTS, `parameter-spec.md`, `params.tsv`, DSP-file change (Decision 48).

---

## Requirements traceability (Round B)

| Requirement | Round B evidence | Status after Round B |
|---|---|---|
| UI-01 | layout gate `webgl` section (backing at DPR 1 / 2, `NO_ERROR`, lose / restore), orbit golden, Task 13 hands-on in Standalone + Logic, PERF-03 log | partial (WKWebView) — WebView2 half → 4.2 |
| UI-02 | layout gate `fallback` section (forced-null `webgl2`, placeholder in three languages, all layout rows hold), state `terrain-webgl-unavailable` in check-ui-labels | complete |
| UI-03 | layout gate `interaction` section (values move, Started / Ended once per gesture, A ↔ B repoint), Task 13 undo check in Logic | complete |
| UI-04 | Task 1 `stateGeneration` + editor force; Task 13 preset apply + session restore rows; `--gate scheduler` (f) unchanged | complete (controls half Round A + re-push half Round B) |
| PERF-03 | `view-perf.log` rows Standalone + Logic at DPR 2 (finish-inclusive mean ≤ 2 ms) | partial (WKWebView) — WebView2 half → 4.2 |
| FUNC-07 | chooser + drop (layout gate `drop` section: one call with the right osc / name, cap and signature refusals; Task 13 real PNGs), Imported… selected from C++, Blur / Edge un-greyed and visible in the map | partial (macOS) — WebView2 drop half → 4.2; DSP half already complete |
| COMPAT-01 (regression) | pluginval × 2 + auval (Task 13) | unchanged, complete |

## Handoff to verify

`stages/3-gui/SUMMARY.md` (Round B), the Decision 46 numbers, `~/Library/Logs/O-Strata/view-perf.log`, the orbit golden table, the layout-gate section output, the two new i18n rows (fr `reviewed: false`, zh `'mt'`), the Round A human rows (`round-a/VERIFICATION.md` §Human, 1–10) plus the Round B hands-on rows of Task 13. Stage 3 as a whole is verified by the Round B verify; Stage 4 discuss follows (persistence + fallback notice + factory presets; CI Windows build for the WebView2 halves).
