# Stage 3 — GUI (Terrain tab, oscillator cards, readout, ≋ cycle view, i18n), Round A: PLAN

**Plugin:** O-Strata · **Stage:** 3 of 4 (GUI) · **Round:** A = ROADMAP Phase 3.1 (CONTEXT D1) · **Phase:** plan
**Date:** 2026-09-12 · **Mode:** manual · **Branch:** `main` (trunk-based; path-scoped commits under `plugins/O-Strata` + `PLUGINS.md`)
**Inputs:** `stages/3-gui/CONTEXT.md` (D1–D4, findings 1–10, inherited contracts), `stages/3-gui/RESEARCH.md` (§2.1–2.6, §2.9 TerFreq adapter, §2.11–2.12, §3 corrections C1–C16, §4 decisions, §5 fixtures + run lists), `mockups/v2-ui.yaml` (locked — **the YAML wins**), `mockups/v2-ui.html`, `v2-integration-checklist.md` §B.1–B.5, `parameter-spec.md` v2 (locked — no parameter change), `ROADMAP.md` v2 Phase 3.1, `REQUIREMENTS.md`, the O-Prism page at `4f12ef57` (byte-identical to HEAD).
**Baseline commit:** `92340ddb` (`Source/` unchanged since `6f30137b`; Stage 2 ✅ VERIFIED at `3d1db4e2`, harness `--gate all` 135 / 135).
**Line numbers** are not quoted as edit sites; every site is named by a token the executor re-locates by grep (memory `pattern_deferred_item_line_number_drifts_record_a_token`). Where a RESEARCH line number is cited it is a reading aid only.
**Decision numbering** starts a fresh Stage 3 sequence (1–26 below); Round B continues from 27.

---

## Goal

Replace the fork's v1.24.0 page with the locked v2 shell on the verified DSP, and make every new control real. **Page:** the O-Prism v1.26.0 page re-forked at `4f12ef57` with the mockup's NEW regions spliced in — two oscillator cards, the fifth tab `Terrain` whose one panel repoints through proxies to `osc{A|B}<suffix>`, inert rules, the readout, the ≋ last-cycle view fed by a pushed event, the ⬡ view as the mockup's placeholder renderer (3D proper is Round B), the botanical plate, localised en / fr / zh-Hans. **Editor / processor:** the two dead native stubs removed, the one `evaluateJavascript` converted to a `heldNotes` push, `terrainStatus` and `terrainCycle` pushed through `emitEventIfBrowserIsVisible`, a `requestTerrainRepush` handshake, a display-voice ring that holds a full cycle down to A0 and never freezes, and the D3 "silent above <note>" readout computed by the scheduler. **Gates:** every existing UI gate green at the new counts, plus two new per-plugin gates (layout budget, shell diff against O-Prism), stub fixtures generated from the binary, and the offline harness still 100 % green.

**Requirements verified in Round A:** UI-04 (controls half — every one of the 34 new parameters moves from the UI and follows automation; preset apply updates all controls), FUNC-05 (the 46 destinations listed in the UI). UI-01 / UI-02 / UI-03 / PERF-03 / FUNC-07 (UI halves) and UI-04's re-push half are Round B; WebView2 halves are Phase 4.2 (D2).

**Round A exit:** RESEARCH §5.4 gates A1–A12 at the thresholds of Decision 22, both commits landed, `stages/3-gui/SUMMARY.md` written, Taylor's fr read + zh triples queued for verify.

---

## Decisions resolved for the executor

These close RESEARCH §4 for Round A and fix the round boundary. The executor applies them without asking; there is **no blocking question** in this plan.

| # | Question | Decision |
|---|---|---|
| 1 | Round boundary (STATUS said "§4 items 4–9 are Round B") | The ≋ view and the readout are Round A (CONTEXT D1, ROADMAP 3.1), so Round A also takes RESEARCH §4 **4 (ring), 5 (capture rule), 6 (cycle extraction), 8 (D3 rule)** and the Round A half of 10 (`copyTerrainCycle`, `getTerrainStatus`, `chebTopNote`). Round B keeps 7 (heightmap), 9 (`stateGeneration` re-push), 11–15 (module, orbit port + golden, PERF-03, gestures, import) and the rest of 10 (`getTerrainPlayhead`, `copyTerrainHeightmap`). The STATUS note was an undercount, not a contract. |
| 2 | Shell merge base (RESEARCH §4.1) | **Base (i):** copy the O-Prism `4f12ef57` `index.html` over the fork's, drop the six wavetable ranges, splice the mockup's NEW regions in. Verified by construction: the i18n canon region, `initializeTipsToggle` / `setupTooltips`, the `.tooltip` CSS + trailing div, the Tuning markup + IIFE, the tuning preset / tonic relays and the preset-save modal markup are **byte-identical** to O-Prism after the merge (`diff` of those ranges = empty; check-i18n [6]). |
| 3 | Drop list and `.wt-*` survivors (§2.1) | Drop from O-Prism: CSS `.wt-modal*` block; `#wavetable-tab`; `#wt-save-modal-overlay`; the `WavetableDisplay` / `WavetableSelector` / `showWavetableManager` JS block (tokens `class WavetableDisplay`, `showWavetableManager`); the `.wt-modal-backdrop` markup; the `<link … wavetable-editor.css>` / `<script src … wavetable-editor.js>` lines; `switchTab`'s `WavetableEditor` hooks; `select-osc?Table` + `label.shape` captions; `label.dropWav` → `label.dropPng`; the dead `.wt-*` members inside the zh line-height pin groups. **Keep** `.wt-osc-toggle` / `.wt-osc-btn` (+ the zh `html[lang="zh-Hans"] .wt-osc-btn` pin and `.wt-osc-btn { line-height }`), `.wt-drop-overlay` (reused for Drop PNG), `.wt-op-btn` / `.wt-save-modal*` (the preset modal's classes). The modal's **63 CSS lines come from the fork's current page** (token `.wt-save-modal-overlay`), not from `wavetable-editor.css`. |
| 4 | Synth tab (C4) | Splice **only the two `.osc-*` cards** from `v2-ui.html`; O-Prism's LFO cards (with the four `#lfo?-div-wrap` / `#select-lfo?Division` groups) stay. `data-knob="osc?Pos"` caption → `label.orbitSize`, default `50%`; `unisonFmt` 1–4; `osc?Pos` default 0.5; the card's Terrain▾ / Orbit▾ selects bind straight to `osc{A,B}Terrain` / `osc{A,B}Orbit` through `bindCombo` (`properties.choices` wins over the fallback array). |
| 5 | ⬡ view in Round A (ROADMAP 3.1 "placeholder present") | The mockup's renderer block (`makeScene2D`, `makeSceneGL`, camera, `heightmapFor`, `terrainH`, `orbitXY`, `surfaceH`) is spliced **inline, verbatim, as the placeholder**, under a header comment `// PLACEHOLDER (Stage 3 Round B replaces): orbitXY / terrainH are look-alikes, not Orbits.h / Terrains.h (RESEARCH §2.7)`. No `js/terrain-view.js` in this round — the YAML and the CMake snippet say inline, and RESEARCH §4.11 (module extraction) is Round B's to decide with the view. Round A splices **no** view gestures (`mousedown` / `wheel` on `#terrainView`), **no** import handlers (`#btn-terImport` click, `dragenter` / `drop`, `readEntryFile`), **no** `reportViewPerf`; the Import… button and the drop overlay markup stay for layout. `window.mock.*`, the `m` / `w` keys and the console parameter log are deleted (must not ship). |
| 6 | ≋ view data | `cycleWave()` (JS-computed from the placeholder heightmap) is **deleted**. A page-level `CYCLE = { A: null, B: null }` holds the last pushed `Float32Array(512 × 3)`; the ≋ renderer (Synth-tab mini canvases and the Terrain-tab view in wave mode) draws `y[i]` against `i` with the mockup's styling; with no data it draws the baseline only. Under the stub the `terrain-wave-view` state is preceded by a `__stubEmit('terrainCycle', …)` state (Decision 20). |
| 7 | TerFreq (§4.2, Round A blocker) | Page-side adapter on both `osc?TerFreq` slider states, no APVTS change: `norm = log(state.getScaledValue() / 0.25) / log(32)`; `set(n) = state.setNormalisedValue((0.25 · 32^n − 0.25) / 7.75)` (converts the wanted scaled value into the frontend's linear space so the emitted scaled value is right); the readout, `snapshot().F`, the ⌥-click reset (`0.4` → 1.00×) and dblclick entry all go through it. Every new knob reads `getScaledValue()` for its readout (memory `pattern_webview_knob_readout_scaled_value`). Gate: `#val-terFreq === '1.00×'` at default. |
| 8 | Events in Round A (§4.3) | Pushed: **`terrainStatus`**, **`terrainCycle`** (new), **`heldNotes`**. The mockup's `terrainState` / `terrainHeightmap` listeners are spliced and stay idle until Round B pushes them. Every push goes through `emitEventIfBrowserIsVisible`; `grep -c evaluateJavascript` = 0 in `index.html` and `PluginEditor.cpp`. Native **`requestTerrainRepush`** is registered now (the page calls it at boot and in `__refreshAllControls`): it sets `repushPending`; the next tick forces all three pushes. Page names in this round = the O-Prism set − the 10 wavetable natives + `requestTerrainRepush`; `comm -3` (RESEARCH §5.3) must be empty. |
| 9 | Ring (§4.4) | `CycleCapture::kPoints` 2048 → **8192** (`TerrainOscillator.h`, token `kPoints = 2048`), `static_assert((kPoints & (kPoints − 1)) == 0)` (memory `pattern_ring_invariant_needs_static_assert`), header comment corrected to "per **sub**-sample (fs · OS)". Audio output unaffected — `--gate all` must still read every row green. |
| 10 | Capture rule (§4.5 A′) | `lastPlayedNote` stays the display gate (its only reader is `StrataVoice` — token `getLastPlayedNote`). Add a per-voice `uint32_t startSerial` stamped in `startNote` from a processor counter; at the end of `processBlock` (before `blockGeneration.fetch_add`), if no active voice has `currentMidiNote == lastPlayedNote`, store the note of the active voice with the greatest `startSerial` (≤ 16 relaxed loads, no allocation). The editor treats a non-advancing `writeIndex` as idle (no push; the last cycle stays drawn). |
| 11 | Cycle extraction (§4.6) | Message thread, in a new `Source/TerrainViewFeed.h/.cpp` (Round B adds playhead + heightmap here): `w1 = writeIndex.load(acquire)`; `memcpy` the ring into the caller's scratch; `w2 = load(acquire)`; discard the newest `(w2 − w1) + 1` slots; skip if `w2 − w1 ≥ kPoints` or `w1 == lastWriteIndex` (idle). Walk back from `w1 − 1` while `θ[k] ≤ θ[k+1] + π`; first drop > π = start of the current partial cycle, second = start of the last complete cycle. Resample that span to 512 by linear interpolation on slot index; if θ is non-monotone inside the span (FM warp) fall back to the newest `L = round(fs · OS / f_display)` slots with `f_display = getLastPlayedFrequency() · pitchRatio(coarse, fine)`. Output `{px, py, y}` with `y ← tanh(g·y) / tanh(g)`, `g = 1 + 4 · osc?TerSat` (raw, unmodulated — matches `TerrainOscillator::saturate`); non-finite → 0. |
| 12 | Status helper (§4.10) | `struct TerrainStatus { int quality, terrain, partialsAtC4, fitPercent, topNote; bool approximate, sourceMissing; }` + `TerrainStatus getTerrainStatus (int osc) const` on the processor (`JUCE_ASSERT_MESSAGE_THREAD`): `chebPartialsAtC4`, `chebFit` (or `imageFit` when Terrain = Imported…), `chebApproximate`, `chebTopNote`, raw `osc?Quality` / `osc?Terrain`, `sourceMissing = false` (hard-false until 4.1). Pushed when the packed struct differs from the last sent or on repush. |
| 13 | D3 top-note rule (§4.8) | `std::atomic<int> chebTopNote[2] { −1, −1 }` on the processor beside `chebPartialsAtC4` (−1 = nothing to show). Computed in `TerrainScheduler::refreshReadouts`: only when Quality = Bandlimited and a set is published (`chebPtr`, or `imagePtr->cheb` when Imported…), else −1. Rule: trace the tapered set (`c[d] · chebTaperWeight(d, dc(fs, K_nominal, f(n)))`) along the base orbit (Pos, Aspect, Rot, CX, CY, Orbit Mod — raw values quantised to 1/1024) at 256 θ, 256-point DFT, `A(n) = max_{k≥1} 2|X_k| / 256`; sounding iff `A(n) ≥ τ = 2e-3`; bisection over MIDI 0–127 for the highest sounding n (A non-increasing in n); result ≥ **108** → −1; `f(n) = tuningEngine.getFrequency(n) · pitchRatio(coarse, fine)`. Recomputed when the key `{chebGeneration or imageGeneration, orbit kind, six orbit params, Coarse, Fine, fs}` changes **or every 10 polls (500 ms)** — the periodic refresh covers tuning-table changes without a generation counter. ≈ 0.6 ms on the message thread, ≈ 7 probes. |
| 14 | Harness rows for D3 + a choices dump (§4.18) | New gate `--gate topnote` (in `--all`): Bandlimited at the harness fs, defaults, sets published through `runOnceSynchronously`, four rows — (Radial Rings, Epitrochoid 3) `chebTopNote[0]` ∈ [0, 92]; (Sine Product, Epitrochoid 3) ≥ 93 or −1 (≥ 108); (Mitsuhashi, Ellipse) == −1; (Sine Product, Epitrochoid 3, Quality 2×) == −1. New `--dump-choices` under `wantsExact` (not in `--all`): prints one JSON object `{ "combos": { "<id>": { "choices": [...], "def": <index> } × 8 }, "modDestNames": [46], "modSourceNames": [...] }` read from the live APVTS (`AudioParameterChoice`) and `ModulationMatrix`. |
| 15 | Push encoding (§2.5) | `terrainCycle` = `{"osc":"A","n":512,"data":"<base64>"}` — 512 × {px, py, y} little-endian Float32 raw bytes (`static_assert` LE via `juce::ByteOrder`) through `juce::Base64::toBase64 (const void*, size_t)`; ≤ 15 Hz (every other 30 Hz tick) and only when a new complete cycle exists. `terrainStatus` = `{"osc":"A","quality":1,"terrain":0,"partials":16,"fit":100,"approx":false,"topNote":-1,"sourceMissing":false}`. `heldNotes` = `{"notes":[…],"freqs":[…]}` with the existing `currentNotes == lastSentNotes` gate. Page decode: `atob` → `Uint8Array` → `Float32Array` over the buffer. |
| 16 | Readout (§4.17) | Mockup split kept: `createTextNode` for `2× · `, `setLabel` spans for words; `label.readoutPartials` `{n, note}`; `label.approx`; `label.imageProjected` `{f, pct}`; **`label.silentAbove` `{note}` as a third span under an `if (topNote >= 0)`** (never a ternary — check-i18n [13]); note name from the O-Prism `midiToNoteName` (C4 = 60, sharps). `Bandlimited` segment face is keyed (`label.bandlimited`), `2×` / `4×` are READOUT (no exemption). The JS estimate `round(12·F·K·(0.52 + 0.96·size))` stands until the first `terrainStatus`; the 12 → 16 flip at boot is accepted (C15). |
| 17 | i18n (§4.16, §2.12) | Fork table + **restore `label.oscAShort` / `label.oscBShort`** from O-Prism + **retire `label.position`** + 29 new + 2 changed (`label.subtitle`, `label.modMatrixInfo`) + `label.silentAbove` = 32 keys + 14 `tip.ter*` I18N entries + 14 `TIP_BINDINGS` rows (`['#knob-terFreq', 'tip.terFreq']` …; bodies from the mockup `title=""`), then every `title="` in `#terrain-tab` and the two cards deleted. fr: ’ (U+2019) in `label.subtitle`, `label.modMatrixInfo`, `aria.viewWaveform`; U+00A0 before the three colons of `label.hintView`; `termNote: 'spatial frequency multiplier (×) of the terrain, not a rate'` on fr `label.terrainFreq` (keeps `Fréq.`); `sameAsEn: true` on the 10 straight copies (`tab.terrain`, `label.terrain`, `label.terrainModX/Y`, `label.saturation`, `label.orbitAspect`, `label.orbitRotation`, `label.orbitCentreX/Y`, `label.approx`); `reviewed: false` on every new fr row. zh: `reviewed: 'mt'` on all 46 new rows. `label.silentAbove`: en `silent above {note}`, fr `muet au-dessus de {note}`, zh `{note} 以上静音`. **No glossary additions this round** (they pass without; repo-wide TERMS entries are a separate commit — open item). `I18N_EXEMPT` rows only where check-i18n [10] demands (RESEARCH expects none). Round B adds `label.importTooLarge` / `label.importFailed` (unreferenced keys fail [15] now). |
| 18 | Back-translation and review flags | At Round A close the executor runs `i18n-zh-backtranslate.js --emit`, hands the blinded rows to a fresh agent that never saw the English, ingests, and saves the triples worst-drift-first to `stages/3-gui/round-a-zh-triples.md`. Flags stay `'mt'` / fr `reviewed: false`; **Taylor flips them at Round A verify** (fr read; zh triples). |
| 19 | Botanical plate + CMake (checklist B.4) | `mockups/img/shell_conchologiaiconi12reev_0090.png` → `Source/ui/public/img/`; added to the **one** `juce_add_binary_data` target (symbol `BinaryData::shell_conchologiaiconi12reev_0090_png`) + an `image/png` route in `getResource` (`/img/shell_conchologiaiconi12reev_0090.png`); the CMake comment "Stage 3 adds js/geometry-view.js" corrected. CSS background on the clipped `.terrain-botanical` box (multiply, 0.3 α, `pointer-events: none`), never an `<img>`. `TerrainViewFeed.cpp` joins `target_sources`. |
| 20 | Stub fixtures + state file (§4.18–19, §5.1–5.2) | `tests/ui-stub/generic-overrides.json` gains `combos` × 8 and `natives` (`getModDestNames` / `getModSourceNames` as JSON strings, `requestTerrainRepush: true`) **generated** from `--dump-choices` by a small script `tests/tools/gen-stub-overrides.mjs` (reads the dump on stdin, rewrites the two blocks, leaves everything else) — never typed (memory `pattern_test_fixture_mirrors_drift_silently`); a header field `"_generatedBy": "O-Strata-render-test --dump-choices"`. `tests/i18n-states.json`: `tuning-held-notes` → `window.__stubEmit('heldNotes', {notes:[60,64,67], freqs:[261.6256,329.6276,391.9954]})`; append the 12 states of RESEARCH §5.1 with the selector corrected to **`.osc-view-btn[data-view="wave"]` / `[data-view="3d"]`** (the mockup uses `data-view`, not `data-mode`), and insert `terrain-cycle-pushed` (`window.__stubEmit('terrainCycle', {osc:'A', n:512, data:<base64 of a 512×3 sine>})`) before `terrain-wave-view`. Not needed: `chooseTerrainImage` / `importTerrainImageData` / `reportViewPerf` (Round B natives). |
| 21 | New gates (§4.20) | **`tests/ui_layout_check.js`** (shape: `plugins/O-Octagon/tests/ui_layout_check.js` — serve-ui + Playwright, port 0): for en / fr / zh-Hans × every tab × default + the Terrain states: `.tab-content.active` `scrollHeight === clientHeight` (666) and `scrollWidth === clientWidth`; `#terrain-tab` four-region overlap 0; `.osc-params` one row with the rightmost `right ≤ 1175` (measured with the Synth tab visible); width-pinned `[data-i18n]` border-boxes equal across languages (`.tab` × 5, `.wt-osc-btn` × 2, `.terrain-seg-btn` × 3, `#tips-toggle`); `elementFromPoint` hits `#btn-terImport`, `.osc-view-btn`, `.terrain-seg-btn`, `#select-ter*`, the fifth `.tab`; `#val-terFreq === '1.00×'`; every `.mod-col-dst select` has 46 options; source greps (`[0-9](vh|vw|dvh|svh)\b` = 0, `<script type="module"` = 1, `user-select: none`, `contextmenu`, `html, body { height: 100% }`, `evaluateJavascript` 0 / 0). **`tests/ui_shell_diff_check.js`**: serve O-Prism and O-Strata (two `serve-ui` instances, port 0), screenshot `synth / mod / tuning / effects` at 1200 × 800, DPR 1, en; before the O-Prism screenshot inject the v2 **intended body edits** as a style tag (`.mod-col-dst{flex:1.35}`, `.effect-section .section-header{margin-bottom:12px}`, `.knob-value,.footer-value{font-variant-numeric:tabular-nums}`, `html,body{cursor:default}`, `.octave-stretch-slider{min-width:0}`) so the diff isolates the merge; DOM-derived masks on the O-Strata page: the subtitle, the tab row, `#preset-display`, the mod-matrix info line, the two osc cards; diff via canvas `getImageData` in a Playwright page (no new dependency); assert **0 differing px outside masks**; negative controls: self-diff = 0 and a planted `.section-header{margin-left:1px}` → > 0. `tests/ui_tip_render_check.js`: `'terrain'` added to both tab loops, the `TIP_BINDINGS.length === 106` literal → the new count. `tests/tools/cdp-font-probe.js`: copy of the O-Chorus quick-dir probe with `PLUGIN = 'O-Strata'` and Terrain-tab targets (promotion to `scripts/` = open item). |
| 22 | Round A verify thresholds (§4.21) | check-i18n `ALL CHECKS PASS` 16 / 16; fr lint `CLEAN — exit 0`; zh lint `GATE PASSED — exit 0` (46 at `'mt'` counted); check-ui-labels exit 0, `states: default + 33`, PASS ≥ 885, `seed=param-dump (205 rows) + overrides`; boot-all-uis `DEAD 0 / late 0 / title= 0`, no `pageerror` / `404:` / `stub invented:`; tip gate `ALL CHECKS PASSED` at the new count, five tabs; measure-ui `undeclared-font ≤ 5`, `wrap-count 0`; CDP probe: every Terrain text run on the declared CJK face; layout gate ALL PASS; shell diff 0 px outside masks + negative control fires; native parity `comm -3` empty; `evaluateJavascript` 0 / 0; pluginval strictness 10 × 2 SUCCESS; `auval -v aumu OuSt OuDv` SUCCEEDED; `O-Strata-render-test --gate all` = **135 + 4 topnote rows, 0 failures**, ≤ 3 min. |
| 23 | Commits | Two path-scoped commits, no tag (memory `feedback_never_tag_unless_publish`): **3.1a** after Wave 2 (`feat(O-Strata): Phase 3.1a — cycle ring 8192, display-voice re-election, top-note readout atomic, editor pushes`) and **3.1** at close (`feat(O-Strata): Phase 3.1 — Terrain tab, oscillator panels, readout, cycle view, i18n`, the ROADMAP message). Between the two the held-notes bar is dark (old page has no `heldNotes` listener) — both commits land in the same execute session. `git branch --show-current` + `git status --short` immediately before each; `git commit -- <explicit paths>`; the untracked `stages/1-foundation/gate-report.json` and `.claude/agent-memory/*` are **not** part of either commit. |
| 24 | Inherited traps carried into the code | Every `launchAsync` stays as it is (five choosers already comply); member order relays → webView → attachments unchanged (`grep -n "RELAYS FIRST\|WEBVIEW SECOND\|ATTACHMENTS LAST"` ascending); Windows `withUserDataFolder ("OStrata_WebView")` kept; the offline harness still configures with `JUCE_WEB_BROWSER=0` (the editor TU is excluded — `TerrainViewFeed.cpp` must compile without the editor, so it depends on the processor only). |
| 25 | Budgets the page must meet (BRIEF decision 8) | Terrain tab ≤ 650 tall inside 666 / 1172 wide; `.osc-params` ≤ 980 in en, no wrap; fr / zh-Hans `wrap_fallback` (stack the two dropdowns in one `.dropdown-group`) only if the layout gate shows a wrap; every tab `scrollHeight === clientHeight`; `.octave-stretch-slider { min-width: 0 }` carried (D4). |
| 26 | Contract items recorded, not edited | ROADMAP 3.1's `getTerrainStatus(osc)` **native function** → a pushed event (the YAML's push contract wins); its `terrainModRate` field is dropped (nothing computes it; the readout does not show it); the checklist's "30 new keys" = 29 + 2 + 1; CONTEXT's `I18N_EXEMPT` list shrinks to none; RESEARCH §5.1's `data-mode` → `data-view`. |

---

## Tasks

Five waves, two commits. Each task ends with the check the executor runs before moving on. Builds, `--gate all`, check-ui-labels and the tip gate go to the background (executor 600 s watchdog; the tip gate alone is ≈ 3 min).

### ━━━ Wave 1 — DSP-side helpers (parallel: disjoint files) ━━━

### Task 1 — Ring 8192 + display-voice re-election (Decisions 9, 10)
- **Modify:** `Source/dsp/TerrainOscillator.h` — `kPoints = 2048` → `8192`, add the power-of-two `static_assert`, fix the Core 10 comment ("per sub-sample (fs · OS); the display voice's partial 0 writes; read by `TerrainViewFeed` on the message thread").
- **Modify:** `Source/StrataVoice.h/.cpp` — `uint32_t startSerial` stamped in `startNote` (token `setLastPlayedNote (midiNoteNumber)`) from `processor->nextStartSerial()`; accessor `getStartSerial()`.
- **Modify:** `Source/PluginProcessor.h/.cpp` — `std::atomic<uint32_t> startSerialCounter`, `nextStartSerial()`; at the end of `processBlock` (token `blockGeneration.fetch_add`) the re-election loop of Decision 10 over `synthesiser.getVoice (i)` (cast as the ctor loop does).
- **Check:** `ninja O-Strata-render-test` (background) → `O-Strata-render-test --gate all` from the repo root = 135 / 135 (no new rows yet). `--gate H8` still 0 allocations.

### Task 2 — D3 top note in the scheduler + harness `topnote` + `--dump-choices` (Decisions 13, 14)
- **Modify:** `Source/PluginProcessor.h` — `std::atomic<int> chebTopNote[2] { -1, -1 }` beside `chebPartialsAtC4` with the rule in a comment; a message-thread `double tunedFrequency (int midi, int osc) const` = `tuningEngine.getFrequency (midi) · pitchRatio (coarse, fine)` (reuse the voice's ratio formula — token `pitchRatio` or its inline equivalent in `StrataVoice.cpp`; if it is inline, lift it into a small shared header function rather than duplicating).
- **Modify:** `Source/dsp/TerrainScheduler.h/.cpp` — cache the six orbit params + Coarse / Fine pointers in the ctor block that already caches `pOrbit` (token `pOrbit[osc]    = apvts.getRawParameterValue`); a `TopNoteKey` per osc + a 10-poll counter; `computeTopNote (osc)` per Decision 13 (probe = trace + 256-point DFT, `A(n)`; bisection; hide ≥ 108); called from `refreshReadouts` when the key or the counter says so; stores −1 otherwise. `SuperellipseLUT::get()` / `OrbitDetail::maxRadius` are message-thread safe (RESEARCH §2.4). Uses `chebTaperWeight` / `chebDiagonalCutoff` from `ChebyshevSet.h` — no new law.
- **Modify:** `tests/render-harness/main.cpp` — `gateTopNote()` with the four rows of Decision 14 (registered in `--all`, README row H12 "top note"), and `--dump-choices` (under `wantsExact`; JSON per Decision 14 with `%s`-escaped UTF-8 names — `Limaçon`, `Imported…`).
- **Check:** `--gate topnote` 4 / 4; `--dump-choices | python3 -m json.tool` parses and shows 7 / 2 / 11 / 3 choices and 46 destination names; `--gate all` = 139 / 139.

### Task 3 — `TerrainViewFeed` + `getTerrainStatus` (Decisions 11, 12, 15)
- **Create:** `Source/TerrainViewFeed.h/.cpp` — `namespace TerrainViewFeed`: `struct CycleScratch { std::array<float, CycleCapture::kPoints * 4> ring; uint32_t lastWriteIndex = 0; }`; `int copyCycle (const OStrataAudioProcessor&, int osc, CycleScratch&, float* out512x3)` returning 0 when idle / torn / no complete cycle (Decision 11); `juce::String encodeFloat32Base64 (const float*, size_t)` (LE `static_assert`, non-finite scrub); `juce::var makeCycleEvent (osc, out)` / `makeStatusEvent (osc, const TerrainStatus&)` building the JSON shapes of Decision 15. Depends on the processor only (no editor include).
- **Modify:** `Source/PluginProcessor.h/.cpp` — `struct TerrainStatus` + `getTerrainStatus (int osc) const` (Decision 12; `imageFit` when `osc?Terrain == 6`); `CMakeLists.txt` `target_sources` += `Source/TerrainViewFeed.cpp`.
- **Check:** a harness row is **not** required (the ring reader is exercised by the editor); compile under both configurations: the plugin target and `O-Strata-render-test` (`JUCE_WEB_BROWSER=0`) — both link.

### ━━━ Wave 2 — Editor plumbing, resources, commit 3.1a ━━━

### Task 4 — Editor: stubs out, pushes in, `requestTerrainRepush`, PNG route, CMake (Decisions 8, 15, 19, 23, 24)
- **Modify:** `Source/PluginEditor.h/.cpp` — delete the `getActiveOscInfo` / `getActiveOscFrame` registrations (token `withNativeFunction ("getActiveOscInfo"` and the comment block above it); register `requestTerrainRepush` (sets `repushPending = true`, completes `true`); replace `timerCallback`'s `evaluateJavascript` (token `window.updateHeldNotes`) by `webView->emitEventIfBrowserIsVisible ("heldNotes", …)` keeping the `lastSentNotes` gate; add per-tick `pushStatus (osc)` (struct compare / force) and `pushCycle (osc)` on even ticks (`CycleScratch scratch[2]` members); `repushPending` forces all three and clears. Resource route for `/img/shell_conchologiaiconi12reev_0090.png` → `image/png` (token `check_native_interop_js` — add after it).
- **Modify:** `CMakeLists.txt` — the PNG in the one `juce_add_binary_data` target; comment corrected. Copy the plate to `Source/ui/public/img/`.
- **Check:** `grep -c evaluateJavascript Source/PluginEditor.cpp` = 0; `grep -c 'withNativeFunction' Source/PluginEditor.cpp` = 30 (31 − 2 + 1); `ninja O-Strata_VST3 O-Strata_AU` (background) clean, 0 new warnings in the touched files; `--gate all` 139 / 139 still (the harness excludes the editor TU).
- **Commit 3.1a** (Decision 23): `git commit -- plugins/O-Strata/Source plugins/O-Strata/CMakeLists.txt plugins/O-Strata/tests/render-harness` after the branch / status re-check.

### ━━━ Wave 3 — The page (Tasks 5 → 6 → 7 sequential on `index.html`; 7's `i18n.js` half may start in parallel with 5) ━━━

### Task 5 — Shell merge: markup + CSS (Decisions 2–5, 19, 25)
- **Modify:** `Source/ui/public/index.html` — start from `git show 4f12ef57:plugins/O-Prism/Source/ui/public/index.html`; apply Decision 3's drop list; splice from `v2-ui.html`: the NEW CSS block (from `.wt-osc-toggle` through `.terrain-notice.visible`, incl. `.terrain-botanical`), the 11 body edits RESEARCH §2.1 lists, the fork's 63-line modal CSS, `.octave-stretch-slider { min-width: 0 }`; the tab row with the fifth `data-tab="terrain"`; the two `.osc-*` cards (Decision 4); `#terrain-tab` after `#effects-tab` (with `.terrain-botanical`, `#terrain-osc-toggle`, `#select-ter*`, `#seg-terQuality`, `#btn-terImport`, `#terrain-readout`, the view wrap with `#terrain-hud` + the `.osc-view-btn` pair + `#terrain-webgl-unavailable` placeholder + the drop overlay, the 14 knobs, `#terrain-notice`). Keep the `title=""` attributes for Task 7 to harvest. `html, body { height: 100% }`, `user-select: none`, `contextmenu` handler, no viewport units.
- **Check:** `diff <(sed -n '/let uiLanguage/,/^}/p' …)`-style range diffs of the canon, tips and Tuning IIFE regions against `4f12ef57` = empty; `grep -c '<script type="module"'` = 1; `grep -cE "[0-9](vh|vw|dvh|svh)\b"` = 0; `grep -c "wavetable-editor\|WavetableDisplay\|select-oscATable\|label.dropWav"` = 0; `node scripts/boot-all-uis.js --plugin O-Strata` boots (tips may be late until Task 7).

### Task 6 — Page JS: bindings, proxies, inert rules, readout, listeners, ≋ view, TerFreq adapter (Decisions 5–8, 16)
- **Modify:** `Source/ui/public/index.html` (module script) — splice from `v2-ui.html`: the `stateOverride` arg on `bindKnob` / `bindCombo`; `SliderProxy` / `PROXY` / `notifyAllProxies` / `ACTIVE_OSC` + the A/B toggle; `refreshInert` (Blur / Edge greyed unless Imported…, Orbit Mod greyed for Ellipse, Pitch Track greyed in Bandlimited, readout clamp 2.00×; greyed keeps its column); `#seg-terQuality` bound to `osc?Quality`; `updateReadout` with the `label.silentAbove` third span (Decision 16); the `terrainState` / `terrainHeightmap` / `terrainStatus` listeners + a new `terrainCycle` listener → `CYCLE[osc]` (Decision 15 decode) → `requestDraw()`; the `heldNotes` listener + `__refreshAllControls` (with `requestTerrainRepush`) exactly as the mockup has them; `switchTab`'s terrain branch; the 46-entry mod-destination fallback (native / `properties.choices` win); `unisonFmt` 1–4; the renderer block as the labelled placeholder (Decision 5) with the ≋ mode reading `CYCLE` (Decision 6, `cycleWave` deleted); the TerFreq adapter (Decision 7) and `getScaledValue()` readouts on all 14 new knobs; `freqFmt` / `degFmt` parsers as the mockup; ⌥-click knob resets to the normalised defaults (`terFreq` through the adapter). **Not spliced:** view gestures, import handlers, `reportViewPerf`, `window.mock`, `m` / `w` keys, the console parameter log.
- **Check:** RESEARCH §5.3 one-liner — parity empty, `evaluateJavascript` 0 / 0, 0 viewport units, 1 module; `node scripts/boot-all-uis.js --plugin O-Strata --verbose` → no `pageerror`, no `stub invented:` once Task 8's overrides exist (run it again after Task 8).

### Task 7 — i18n: keys, tips, lints, `title=` deletion (Decisions 17, 26)
- **Modify:** `Source/ui/public/js/i18n.js` — Decision 17 in full (restore 2, retire 1, 32 keys, 14 tips + 14 `TIP_BINDINGS` rows, fr / zh flags, `termNote`, `sameAsEn`, ’ / U+00A0); `label.oscAShort` / `BShort` values from O-Prism's table.
- **Modify:** `Source/ui/public/index.html` — delete every `title="` inside `#terrain-tab` and the two cards (RESEARCH counts 15); make sure `#terrain-webgl-unavailable`, the drop overlay and `#terrain-notice` carry their `data-i18n` keys; no caption outside a `[data-i18n]` except READOUT nodes.
- **Check (in this order):** `node scripts/i18n-fr-lint.js --plugin O-Strata --verbose` exit 0 → `node scripts/i18n-zh-lint.js --plugin O-Strata --verbose` exit 0 (46 `'mt'` counted) → `node scripts/check-i18n.js --plugin O-Strata` 16 / 16 → `node scripts/boot-all-uis.js --plugin O-Strata --strict-tips --verbose` `DEAD 0 / late 0 / title= 0`.

### ━━━ Wave 4 — Fixtures and gates (parallel: disjoint files) ━━━

### Task 8 — Stub overrides generated from the binary + state file (Decision 20)
- **Create:** `tests/tools/gen-stub-overrides.mjs` (stdin = `--dump-choices` JSON → rewrites `combos` / `natives` in `tests/ui-stub/generic-overrides.json`, preserves other blocks, writes `_generatedBy`).
- **Modify:** `tests/ui-stub/generic-overrides.json` — run the generator once and commit the result.
- **Modify:** `tests/i18n-states.json` — Decision 20 (`tuning-held-notes` rewrite; 13 appended states incl. `terrain-cycle-pushed`; `data-view` selectors).
- **Check:** `node scripts/check-ui-labels.js --plugin O-Strata` (background) → exit 0, `states: default + 33`, the Imported… / Bandlimited states reachable (the `#terrain-image-row` un-greys in `terrain-imported`), PASS ≥ 885; `boot-all-uis --verbose` shows no `stub invented:`.

### Task 9 — Tip gate literals + CDP font probe copy (Decision 21)
- **Modify:** `tests/ui_tip_render_check.js` — `'terrain'` in both tab-loop literals (tokens `['synth', 'effects', 'tuning']` and `['synth', 'mod', 'tuning', 'effects']`); the `TIP_BINDINGS.length === 106` literal → the new count (106 − retired + 14; the executor reads the real number from `i18n.js` and writes it as a literal, not an expression).
- **Create:** `tests/tools/cdp-font-probe.js` — copy of `.planning/quick/260906-uu7-o-chorus-s-cjk-font-tail-is-inert-found-/cdp-font-probe.js` with `PLUGIN = 'O-Strata'`, targets = the Terrain-tab text runs (`#terrain-tab .knob-label`, `.terrain-seg-btn`, `#terrain-readout`, `#terrain-notice`, `.wt-osc-btn`, `#btn-terImport`) under zh-Hans; prints `familyName` per run.
- **Check:** `node plugins/O-Strata/tests/ui_tip_render_check.js` (background, 5-min timeout) → `ALL CHECKS PASSED`, five tabs visited; `node plugins/O-Strata/tests/tools/cdp-font-probe.js` → every run on the declared CJK face.

### Task 10 — `tests/ui_layout_check.js` (Decision 21)
- **Create:** the layout gate per Decision 21, structured like O-Octagon's (serve-ui on port 0, Playwright from the npx cache, per-language loop, PASS / FAIL rows, exit 2 on any FAIL, the source greps at the end). Terrain states come from `tests/i18n-states.json` (reuse the same walk — import the file, filter names starting `terrain`).
- **Check:** `node plugins/O-Strata/tests/ui_layout_check.js` → ALL PASS in en / fr / zh-Hans; a deliberate `--viewport 1200x780` run (or a planted 30 px padding via `--inject-css`) FAILs the 666 assertion — proves the gate is live (memory `pattern_zipper_sweep_probe_needs_liveness_gate`).

### Task 11 — `tests/ui_shell_diff_check.js` (Decision 21)
- **Create:** the shell-diff gate per Decision 21 (two servers, injected intended edits on the O-Prism side, DOM-derived masks from the O-Strata page, canvas diff, negative controls, prints px-outside-mask per tab).
- **Check:** `synth / mod / tuning / effects` → 0 px outside masks; self-diff 0; planted control > 0 (both printed).

### ━━━ Wave 5 — Full gate run, install, review material, docs, commit 3.1 ━━━

### Task 12 — Gate run A1–A12 (Decision 22)
- Run RESEARCH §5.4 A1–A12 in order, background the long ones, keep every log under `stages/3-gui/round-a-logs/` (gitignored if large — otherwise commit the summaries only). Fix and re-run until every threshold of Decision 22 holds. Record the numbers (they are the SUMMARY's table).

### Task 13 — Install + validate + hands-on check (COMPAT-01 regression; UI-04 controls half; FUNC-05)
- `./scripts/build-and-install.sh O-Strata` (background); pluginval strictness 10 on VST3 + AU; `auval -v aumu OuSt OuDv`.
- Standalone + Logic (WKWebView), recorded in SUMMARY as a checklist: every new knob / select / segment moves its parameter and follows host automation (spot-check 6 incl. both TerFreq knobs reading `1.00×` at default and `2.00×` at normalised 0.6); preset `Init` apply refreshes every control; Osc A / B toggle repoints (edit B, toggle to A, values differ); Blur / Edge greyed until Imported… is chosen from the dropdown, then un-greyed; readout shows `2× · 16 partials at C4` at default, `Bandlimited · … · approx.` on Superellipse, `… · silent above <note>` on Radial Rings × Epitrochoid 3 in Bandlimited; the ≋ view moves with a held note, holds the last cycle after release, and still updates after the held note ends while an older note sounds (Decision 10); the mod-destination dropdown lists 46 entries; the tuning held-notes bar still tracks keys (heldNotes push).

### Task 14 — zh back-translation triples (Decision 18)
- `node scripts/i18n-zh-backtranslate.js --emit O-Strata --plugin O-Strata --out <scratch>/strata-zh.tsv --forward-provenance "Stage 3 Round A, 2026-09-12"` → a fresh agent (no English, no `i18n.js` access) back-translates the 46 blinded rows → `--ingest … --provenance "…"` → save the printed triples to `stages/3-gui/round-a-zh-triples.md`. Flags untouched.

### Task 15 — Docs, STATUS, SUMMARY, commit 3.1 (Decision 23)
- **Modify:** `CHANGELOG.md` (v1.0.0 unreleased — "Stage 3, Round A — Terrain tab, oscillator panels, readout, cycle view, i18n (Phase 3.1, 2026-09-12)" with the ring / re-election / top-note items and the Round B carry list), `PLUGINS.md` row (🚧 Stage 3 GUI — Round A executed), `.planning/STATUS.md` (execute row ✓, `next_action` → `/plugin-verify O-Strata 3-gui`), `.planning/stages/3-gui/SUMMARY.md` (tasks 1–15, every gate number, deviations, "Open for verify": fr read, zh triples, the τ = 2e-3 / hide-108 feel, the 12 → 16 boot flip).
- **Commit 3.1:** `git commit -- plugins/O-Strata PLUGINS.md` after the branch / status re-check (path-scoped; the untracked `stages/1-foundation/gate-report.json` must not be added — name paths explicitly if `git status` shows it staged by another session).

---

## Parallelism

| Wave | Tasks | Can run together | Blocked by |
|---|---|---|---|
| 1 | 1, 2, 3 | yes (files disjoint: oscillator / voice / processor-block-end vs scheduler + harness vs `TerrainViewFeed` + processor status — the two processor edits touch different regions; merge by hand if one executor does them serially) | — |
| 2 | 4 (+ commit 3.1a) | — | 1–3 |
| 3 | 5 → 6 → 7 (`index.html` is one file); 7's `i18n.js` half alongside 5 | partially | 4 (the PNG route + repush native exist) |
| 4 | 8, 9, 10, 11 | yes | 7 |
| 5 | 12 → 13 → 14 → 15 | 13 and 14 together | 8–11 |

---

## Files

**Create:** `Source/TerrainViewFeed.h`, `Source/TerrainViewFeed.cpp`, `Source/ui/public/img/shell_conchologiaiconi12reev_0090.png`, `tests/ui_layout_check.js`, `tests/ui_shell_diff_check.js`, `tests/tools/gen-stub-overrides.mjs`, `tests/tools/cdp-font-probe.js`, `.planning/stages/3-gui/SUMMARY.md`, `.planning/stages/3-gui/round-a-zh-triples.md`.
**Modify:** `Source/ui/public/index.html` (replaced), `Source/ui/public/js/i18n.js`, `Source/PluginEditor.h/.cpp`, `Source/PluginProcessor.h/.cpp`, `Source/StrataVoice.h/.cpp`, `Source/dsp/TerrainOscillator.h`, `Source/dsp/TerrainScheduler.h/.cpp`, `CMakeLists.txt`, `tests/render-harness/main.cpp` + `README.md`, `tests/ui-stub/generic-overrides.json`, `tests/i18n-states.json`, `tests/ui_tip_render_check.js`, `CHANGELOG.md`, `.planning/STATUS.md`, `PLUGINS.md`.
**Untouched (must stay byte-identical to O-Prism after the merge):** the i18n canon region, tips toggle + `setupTooltips`, `.tooltip` CSS + trailing div, Tuning markup + IIFE, tuning preset / tonic relays, preset-save modal markup.
**Not touched this round:** `parameter-spec.md`, `params.tsv` (diff must stay empty), `Orbits.h`, `Terrains.h`, `ChebyshevSet.h`, `TerrainImage.*`, the five inherited choosers, O-Prism's tree.

---

## Success criteria (ROADMAP 3.1 test criteria as amended by Decisions 1–26)

- [ ] Every one of the 34 new parameters moves from the UI and follows host automation; preset apply updates all controls (Task 13 checklist; the two TerFreq knobs read `1.00×` at default — Decision 7)
- [ ] Readout shows `2× · N partials at C4` / `Bandlimited · N partials at C4 [· approx.] [· silent above <note>]` / `image projected at F = 1 · fit N %` from the pushed `terrainStatus`; Blur / Edge greyed unless Imported…; Orbit Mod greyed for Ellipse; Pitch Track greyed in Bandlimited; readout clamps at 2.00× in Bandlimited
- [ ] ≋ view shows the last complete cycle from `terrainCycle` (512 points, saturated with g = 1 + 4·sat) and keeps updating after the display voice ends while older notes sound; ⬡ placeholder present
- [ ] `--gate topnote` 4 / 4 (Radial Rings × Epi3 muted ≤ 92; Sine Product × Epi3 ≥ 93 or hidden; Mitsuhashi −1; 2× −1); `--gate all` 139 / 139 ≤ 3 min from the repo root — audio unchanged by the ring resize
- [ ] i18n: check-i18n 16 / 16; fr lint exit 0; zh lint exit 0 with 46 `'mt'` counted; no untranslated `data-i18n`; readout nodes never localised; 32 keys + 14 tips landed; `label.oscAShort/BShort` restored, `label.position` retired; zh triples file written
- [ ] UI gates green at Decision 22's thresholds: check-ui-labels `default + 33` / PASS ≥ 885 / exit 0; boot `DEAD 0 / late 0 / title= 0`, no `stub invented:`; tip gate ALL PASS over five tabs at the new literal; measure-ui `undeclared-font ≤ 5`, `wrap-count 0`; CDP probe on the declared CJK face; layout gate ALL PASS (666 on every tab in three languages, `.osc-params` one row, pinned widths equal, `elementFromPoint` hits, 46 destinations)
- [ ] No visual regression on the inherited tabs: shell diff 0 px outside masks vs O-Prism `4f12ef57` on `synth / mod / tuning / effects`, negative control fires
- [ ] Native parity `comm -3` empty; `evaluateJavascript` 0 / 0; 0 viewport units; exactly one `<script type="module">`; member order comment tokens ascending
- [ ] pluginval strictness 10 × 2 SUCCESS; `auval` SUCCEEDED; `params.tsv` diff empty; 0 new warnings in the touched C++ files
- [ ] Two path-scoped commits landed; SUMMARY.md + CHANGELOG + PLUGINS.md + STATUS updated; no tag

---

## Out of scope (do not do in this round)

- The real 3D view (`Orbits.h` port, R32F heightmap texture, `terrainHeightmap` / `terrainState` pushes, orbit golden, `js/terrain-view.js` extraction, PERF-03 ring / `reportViewPerf`) — Round B 3.2.
- View gestures, `chooseTerrainImage` / `importTerrainImageData`, the Drop PNG flow, `label.importTooLarge` / `label.importFailed`, `stateGeneration` — Round B 3.3 / 3.2.
- `terrainImports` persistence content, "Locate…", `sourceMissing` behaviour, factory presets — Stage 4.1. WebView2 evidence — Phase 4.2 (D2).
- O-Prism `min-width: 0` port (D4), repo-wide glossary TERMS additions, promoting the CDP probe to `scripts/` — separate commits / open items.
- Any APVTS, `parameter-spec.md`, `Orbits.h`, `Terrains.h` or `ChebyshevSet.h` change.

---

## Requirements traceability (Round A)

| Requirement | Round A evidence | Status after Round A |
|---|---|---|
| UI-04 (controls half) | Task 13 checklist + check-ui-labels state walk + layout gate `#val-terFreq`; preset apply refreshes through `setValueNotifyingHost` per parameter (RESEARCH §2.6) | partial — re-push half (view + playhead after preset / session) is Round B |
| FUNC-05 (UI list) | layout gate: 46 options in every `.mod-col-dst select`; Task 13 | complete (DSP half already complete Stage 2) |
| COMPAT-01 (regression) | pluginval × 2 + auval (Task 13) | unchanged, complete |
| UI-01 / UI-02 / UI-03 / PERF-03 / FUNC-07 (UI halves) | placeholder ⬡ present; markup for the fallback placeholder and the Import… button in place | pending — Round B |

## Handoff to Round B (inputs the next plan reads)

`round-a/SUMMARY.md` + `round-a/VERIFICATION.md` (after verify moves them), the measured gate numbers of Decision 22, the placeholder renderer block boundaries in `index.html` (header comment token `PLACEHOLDER (Stage 3 Round B replaces)`), `TerrainViewFeed` as the home for playhead / heightmap, `chebTopNote` + `getTerrainStatus` as the status source, the `terrainCycle` decode on the page, the stub `natives` block (add `chooseTerrainImage`, `importTerrainImageData`, `reportViewPerf`), the state file (add the Round B states of RESEARCH §5.1), RESEARCH §4.7, 4.9, 4.11–4.15 and §5.5.
