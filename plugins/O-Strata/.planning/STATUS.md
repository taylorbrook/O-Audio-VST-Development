---
plugin: O-Strata
stage: 4
stage_name: polish
phase: verify
status: stage_4_complete_verified
round: B
last_updated: 2026-09-14
workflow_mode: manual
complexity_score: 5.0
complexity_raw: 25.0
staged_implementation: true
orchestration_mode: true
next_action: "PLUGIN COMPLETE — Stage 4 ✅ VERIFIED 2026-09-14 (stages/4-polish/VERIFICATION.md, the Stage 4 verdict). Every gate re-measured from the release commit 005cbcf7 on a quiet machine: --gate all 162 / 0 in 108.6 s, clenshaw 81.4 -> 28.0 ns (2.9x) with random 9.775e-06 <= 2e-5 and projected 2.980e-07 <= 1e-6, H7 2x delta 4.13 % and Bandlimited 4.24 % <= 4.13 + 2.0 PASS, H1 identity -128.3 dB unmoved, H9 0.000e+00, H6 2x worst -70.7 dB / BL 127 of 127 worst -98.4 dB with the 17-muted and 13-equality sets identical, H2 x3 20 / 20 with every measurement row identical and Squarcle Storm a stable -3.6 dB (Round A Issue 1 CLOSED), H10 24 / 24, smoke 39 / 39, H8 8 / 8, orbit golden 44 / 44 worst 2.93e-6, grid shasum -c 0 non-OK, exportPresets 18 / 0 silent, labels 1809 / 0 over default + 42, boot clean 1 / 1 DEAD 0, layout 607 / 0, tips 3215 at 122, shell diff 0 px, measure-ui 5 / 3 / 0 / 0, CDP 16 runs 0 off-face, params.tsv diff empty, 26 warning lines 0 new, MSVC greps 0, pluginval 10 VST3 + AU SUCCESS and auval SUCCEEDED on installed bundles byte-identical to a forced-recompile rebuild (VST3 c86546cd...), bank 18 / 9 folders / stamp 1.0.0+18c17735821c, no tag. CI re-read from gh: run 34809565500 at dcbc609a, windows-vst3 success with all 13 steps green on three dispatches (MSVC + WebView2 VST3, pluginval 10 exit 0 under -e -o pipefail, 0 FAIL lines incl. Editor Automation) = COMPAT-02 complete at the Finding 6 ceiling; probes-macos 162 checks (= local) and orbit golden 44 / 44 worst 2.93e-6, failing only two wall-clock rows on an Apple M1 (Virtual) runner. REQUIREMENTS: COMPAT-02 / PERF-02 (Bandlimited, D4) / UI-01 / FUNC-07 -> complete (UI-01 + FUNC-07 at the Finding 6 ceiling, WebView2 hands-on halves a NAMED DEFERRAL - owner none, blocked on hardware); COMPAT-01 / DSP-06 / FUNC-08 / FUNC-11 / QUAL-03 / UI-04 held; QUAL-04 pending (LISTENING.md material complete and verified - 39 / 85 / 15 rows, all verdicts blank - the sitting is Taylor's); PERF-03 pending both halves (no view-perf.log; Decision 36 route unreachable). ALL 24 must REQUIREMENTS COMPLETE; the 2 pending are nice + should. VERIFICATION Issue 1: the H7 Bandlimited absolute-slack row is load-flaky everywhere, not just on the runner - under XprotectService contention the same binary read 10.42 % vs 2.68 % (ratio 3.9x) and --gate all took 2125 s wall / 123 s user; a ratio form + a duty-cycle assertion is a v1.1 item. Issue 5: the release commit 005cbcf7 is unpushed (origin/main at dcbc609a) but is docs-only, so the CI evidence still covers all code. Next: /clear then /install-plugin O-Strata (already satisfied by Task 10 and re-verified); /publish O-Strata 1.0.0 stays a separate decision (D3, no tag)."
next_stage: 4
ready_for_implementation: true
contract_checksums:
  brief: sha256:955f20b31a5f99a45b97a989e5c7d1333b8d23b359dd1f8b66ec90af189c4671
  parameter_spec_draft: sha256:cc3631cba8a098103cc88f2b0bd87cffa6fdf1f93ea8f7ea39b7e55cb1f0cc7d
  parameter_spec: sha256:238863d7dcd9f59045e9532e79afbbe9e8b531c7aaab29fc27317c9efb702f61
  architecture: sha256:165ba8e24b43a78e7ea5c5a8e690ca4b056e2f275a73660e5676b5551a2af211
  roadmap: sha256:9d77da57b61937b532148a1df2376e314ed5ce3f373293a6e813211cba343aa9
mockup_latest_version: 2
ui_design_phase_complete: true
mockup_finalized: true
finalized_version: 2
brief_updated_from_mockup: true
mockup_version_synced: 2
brief_update_timestamp: "2026-09-10T15:15:37Z"
ui_scaffolding_phase_complete: true
fork_exists: true
fork_verified: "stages/1-foundation/first-pass-baked/VERIFICATION.md (baked parameter set, 2026-09-07)"
superseded_design: superseded-baked-v1/
stage_0_status: ui_design_complete
---

# O-Strata Status

## Current Position

Stage: 4 — **Polish / Validation ✅ VERIFIED 2026-09-14 — PLUGIN COMPLETE.** Round B (4.2) carries the Stage 4 verdict (`stages/4-polish/VERIFICATION.md`); Round A (4.1) ✅ VERIFIED 2026-09-13 (`stages/4-polish/round-a/VERIFICATION.md`). v1.0.0 installed locally, **no tag, not published** (CONTEXT D3). All 24 `must` requirements complete; QUAL-04 (nice) and PERF-03 (should) pending a human sitting.
Status: `stages/4-polish/VERIFICATION.md` — **✅ VERIFIED (Stage 4, automated)** = the plugin verdict. Every B1–B20 gate re-measured from the release commit `005cbcf7` on a quiet machine (`--gate all` 162 / 0 in 108.6 s, H7 Bandlimited 4.24 % ≤ 4.13 + 2.0, H2 ×3 stable at −3.6 dB, orbit golden 44 / 44, pluginval ×2 + auval green on installed bundles byte-identical to a forced rebuild); CI `windows-vst3` green ×3 → COMPAT-02 complete. **All 24 `must` requirements complete**; QUAL-04 + PERF-03 pending a human sitting. No tag, not published (D3).
Progress: [####################] 100% (Stages 0–4 all VERIFIED; v1.0.0 releasable and installed locally. Open: QUAL-04 listening sign-off + PERF-03 WKWebView number, both human, neither a `must`.)

## Why (evidence)

- `evidence/critique-check-2026-09-08.md` — measured on the prototype: centred orbits over symmetric fields play an octave / a twelfth up; mesh sweeps are spectrally static; fBm is the only rich baked source.
- `evidence/replan-proposal-2026-09-08.md` — the sequence below and what carries over.
- Listening (Taylor, 2026-09-08): the baked tables sound nice but do not warrant a new plugin.

## What carries over from the built fork (`Source/`, verified 2026-09-07; re-parameterised 2026-09-10)

Full `Strata` rename, wavetable library/editor removed, `JUCE_WEB_BROWSER`-guarded editor, `juce_cryptography` linked, UI gate fixtures, headless smoke harness (`stages/1-foundation/smoke/`, checks [1]–[6]), CHANGELOG/NOTES/PLUGINS.md plumbing. The 48 baked-geometry parameters were replaced by the 34 live-oscillator parameters in the Stage 1 second pass; `params.tsv` (205 rows) is the current binary **and** the spec v2 target.

## Phase Progress

### Stage 0: Research & Planning (live terrain design)
| Phase | Status | Date | Notes |
|-------|--------|------|-------|
| re-plan brief | ✓ | 2026-09-08 | BRIEF / REQUIREMENTS v2 / parameter-spec-draft v2 |
| research + plan (`/plan`) | ✓ | 2026-09-08 | `research/ARCHITECTURE.md` v2 (Core 1–11, Algorithms, Decisions D1–D12, harness H1–H11), `ROADMAP.md` v2 (complexity 5.0), `stages/0-ideation/CONTEXT.md` |
| mockup v2 | ✓ finalised + scaffolded | 2026-09-10 | `mockups/v2-ui.yaml` + `v2-ui-test.html` on the O-Prism v1.26.0 card shell; brief §UI Concept synced; brief checksum re-anchored (only §UI Concept changed). Scaffolding: `mockups/v2-ui.html`, `v2-PluginEditor-TEMPLATE.h/.cpp`, `v2-CMakeLists-SNIPPET.txt`, `v2-integration-checklist.md`; **`parameter-spec.md` v2 LOCKED** (205 params, 46 mod destinations; 166 slider + 8 comboBox + 30 toggle relays) |

### Stage 1: Foundation — first pass ✓ (baked params, 2026-09-07 — `stages/1-foundation/first-pass-baked/`); **second pass ✓ verified 2026-09-10** (re-parameterise: 205 params, 46 mod destinations, COMPAT-01 re-verify — ROADMAP "Stage 1: Foundation — second pass")
| Phase | Status | Date | Notes |
|-------|--------|------|-------|
| discuss | ✓ | 2026-09-10 | `stages/1-foundation/CONTEXT.md` — D1 relays only, page untouched (UI criteria → Phase 3.1); D2 factory bank = `Init` only, O-Prism presets dropped, stale on-disk bank must be removed (version stays 1.0.0); D3 `StrataVoice` untouched; D4 params.tsv diff = −2 +34 + 20 in-place rows |
| research | ✓ | 2026-09-10 | `stages/1-foundation/RESEARCH.md` — 7 items resolved (§2.1–2.7); corrections: `~/Library/O-Strata/Presets` path + 192-preset stale bank, no migration hook in fork, 3-arg remap lambdas + 7-decimal text, `params.tsv` baseline = `a774d6d4^`; smoke checks [4] D3 route (with positive control) and [5] on-disk `Init` specified |
| plan | ✓ | 2026-09-10 | `stages/1-foundation/PLAN.md` — 14 tasks / 5 waves; Decisions 1–12 close RESEARCH §5 (preset path + `rm` timing, default TerFreq text, 3-arg lambdas, self-describing `Init`, `Pitch` positive control, harness literals asserted against live values, `a774d6d4^` baseline, host names, 0.001 steps, comment-only count refresh, gates regression-only) |
| execute | ✓ | 2026-09-10 | `stages/1-foundation/SUMMARY.md` — Tasks 1–14 of PLAN.md; 205 params / 46 dests / `terrainImports` / 8 combo relays / `Init` bank; pluginval ×2 + auval green; smoke 62/62; UI gates at baseline; commit `feat(O-Strata): Stage 1 second pass …` |
| verify | ✓ | 2026-09-10 | `stages/1-foundation/VERIFICATION.md` — ✅ VERIFIED; COMPAT-01 → complete in REQUIREMENTS.md; FUNC-09 / FUNC-05 / FUNC-10 Stage 1 evidence recorded, formal verification stays Stage 2; issue: smoke harness cwd-sensitive (carry to the Stage 2 render harness) |

### Stage 2: DSP — two rounds (CONTEXT D1); Round A = 2.1 core + 2.2 feedback/tracking + 2.3 oversampling (H6/H7 decision point), Round B = 2.4 Bandlimited + 2.5 PNG path
| Phase | Status | Date | Notes |
|-------|--------|------|-------|
| discuss | ✓ | 2026-09-10 | `stages/2-dsp/CONTEXT.md` — D1 two rounds split at 2.3 (Round A artifacts move to `round-a/` after verify); D2 PERF-02 amended to oscillator delta ≤ 12 % (REQUIREMENTS.md edited); D3 no *Terrain* listening, ARCH Decisions 1 / 3 stand; D4 F-lattice v1.1. Findings: smoke check [4] inverts in 2.1 (fold smoke into `render-harness --smoke`), bench prototype at repo-root `research/…/terrain-bench/`, harness must exclude the editor TU (not O-Bowed's CMake), M4 Max = §7.2 machine. 12 research items |
| research | ✓ | 2026-09-11 | `stages/2-dsp/RESEARCH.md` — 12 items resolved for both rounds; 16 corrections (harness via `ouaricon_add_processor_console`, 2× decimator = 5 sections / latency 1.26, 4× = 1.74 → +1 vs +2 decision, H6 rows A2/A4/A6 exact-cycle, ramps 22 per voice, `getActiveOscFrame` only page caller, HeapBlock gap in H8, blur 113 ms → 3-pass box, Clenshaw 43–91 ns); §4 = 10 plan decisions; §5 = harness skeleton |
| plan (Round A) | ✓ | 2026-09-11 | `stages/2-dsp/PLAN.md` — 28 tasks / 7 waves / 3 commits (2.1: Tasks 1–12, 2.2: 13–17, 2.3: 18–25, close 26–28); Decisions 1–24 close RESEARCH §4 (latency +1 constant, H6 A2/A4/A6 at fs = 440·65536/600, pre-filter tap, H8 operator-new family + HeapBlock gap, shared ramp rows by absolute index, stubs `[]`/`{}`, seed atomic + `setVoiceIndex`, `theta_reference.h` first, `setUnison` pow cached, Saturation branch skipped at exact 0, Superellipse LUT `call_once`, fb = 0 skip gated on row + `isDestinationRouted`, 64-sample Quality crossfade, H2 grid + `presets: 1 (Init)`, `tests/.gitignore`, smoke [4] inverted / [5] `--with-disk`, `--gate tuning` ≥ 5 keys) |
| execute (Round A) | ✓ | 2026-09-11 | `stages/2-dsp/SUMMARY.md` — Tasks 1–28; `TerrainOscillator` + Orbits / Terrains / HalfbandDecimator, shared ramp rows, feedback, pitch tracking, 2× / 4× + crossfade, latency +1, wavetable path deleted; harness `O-Strata-render-test` `--all` 85 checks / **2 failures (H4 partial counts — design finding)** in 70.8 s; **H6 198/198 ≤ −60 dB (no rung), H7 delta 4.44 % (no rung)**; pluginval ×2 SUCCESS, auval SUCCEEDED; params.tsv diff empty; deviations 1–10 (Cosine Wells πF, H5 excess-step, centroid partial-move, H3 pre-blocker bound + Hann DC, Nyquist / H4 controls swept) |
| verify (Round A) | ✓ | 2026-09-11 | `stages/2-dsp/round-a/VERIFICATION.md` — ✅ VERIFIED; every SUMMARY figure re-measured (`--gate all` 89 / 89 from `/` and the repo root, 68.8 / 70.3 s; H7 delta 4.48 / 4.56 %; H6 2× worst −70.7 dB; pluginval ×2 SUCCESS, auval SUCCEEDED, params.tsv diff empty). **H4 / DSP-01 ruling:** the law (ARCH Decision 2) is the contract; DSP-01 statement + acceptance re-worded in REQUIREMENTS.md (identical below C4, not growing above, tracked C6 < untracked), harness `gateH4` asserts that (only code change); default-Track / soft-knee options need a contract re-open → optional Round B discuss item. 15 stage-2 requirements complete, FUNC-06 / DSP-04 / QUAL-01 partial (Round B halves), DSP-02 pending. Round A artifacts moved to `round-a/` (CONTEXT D1) |
| plan (Round B) | ✓ | 2026-09-11 | `stages/2-dsp/PLAN.md` — Phases 2.4 (Tasks 1–9: `Retirable.h`, `ChebyshevSet.h` triangle + Clenshaw, `ChebyshevProjector`, `TerrainScheduler` 50 ms / 2-thread pool / `callAsync` + weak token / `runOnceSynchronously`, processor pointers + readout atomics, oscillator Chebyshev path with per-oscillator weighted copy + continuous taper + 64-sample set crossfade, harness `pump` / H6 Bandlimited 144 rows ≤ −90 dB / `scheduler` / `storm` / `clenshaw`) and 2.5 (Tasks 10–16: `TerrainImage` decode ≤ 1024² + 3-pass box blur + Mirror / Window / HarnessWrap read, import API `importTerrainImage` / `importTerrainFile` + `importSlot`, image side of the scheduler at 150 ms, oscillator image path, `import` / H11 / H10 / H8 rows, ASan `build-asan/`, README H1–H11, install + validate, CHANGELOG "Known limits — Bandlimited mode", SUMMARY, commit); Decisions 25–46 (nominal K for approximate orbits, taper law, weighted copy, 64-sample fade, set-selection rule, terrain-destination bypass = ignore + ModWheel fold via a scheduler-owned matrix, `TerrainScheduler` single class, `Retirable.h`, readout atomics, image embedded set at F = 1, blur pads by mirror, HarnessWrap control, API never touches parameters, `pump` rule, H5-style storm click metric, in-process PNG fixtures, separate ASan build dir, A-note Bandlimited H6, Round A gate rows). No blocking question |
| execute (Round B) | ✓ | 2026-09-12 | `stages/2-dsp/SUMMARY.md` — Tasks 1–16; commits Phase 2.4 (`ChebyshevSet` / `ChebyshevProjector` / `TerrainScheduler` / `Retirable.h` / `TerrainImage` type, oscillator Chebyshev path + taper + set crossfade, harness pump / H6 Bandlimited / scheduler / storm / clenshaw) and Phase 2.5 (import API, scheduler image side, `import` / H10 / H11 / H8 image rows, README H1–H11, CHANGELOG "Known limits — Bandlimited mode"); H6 Bandlimited 127 / 127 sounding rows ≤ −90 dB (17 A6 rows muted by truncation — finding), H7 Bandlimited delta ≈ 11.7 % (reported, under the 12 % fallback line), storm 0 allocations / click ratio 1.02–1.15 / liveCount 2, import 26 ms at 1024², H10 identical, H11 ≤ −58 dB with the tiling control at −24 dB; pluginval ×2 + auval green; params.tsv diff empty. Deviations: Chebyshev output not clamped; Mitsuhashi fit 98.3 / 87.6 %; ASan runtime hangs at init on this toolchain (instance counters carry the leak verdict); `.gitignore` gained `build-asan/` |
| verify (Round B) | ✓ | 2026-09-12 | `stages/2-dsp/VERIFICATION.md` — ✅ VERIFIED (stage verdict); every SUMMARY figure re-measured (`--gate all` 135 / 135 from `/` and the repo root, 109.1 / 110.0 s; H6 Bandlimited 127 / 127 ≤ −90 dB worst −98.4 dB, 17 muted rows = exactly the law's prediction; H7 2× 4.19 / 4.38 %, Bandlimited 11.61 / 12.06 % reported; storm 0 alloc, ratio ≤ 1.16, liveCount 2; import 26.3 ms; H11 ≤ −58 dB, control −24.4 dB; H10 identical; pluginval ×2 SUCCESS, auval SUCCEEDED; params.tsv diff empty; 0 warnings in the new files). Rulings: muted rows = documented limit (optional Stage 4 / v1.1 discuss item with the soft knee); H7 Bandlimited ≈ 12 % → Stage 4 PERF item (vectorised basis); ASan = toolchain limit, instance counters are the verdict; FUNC-06 acceptance re-worded (≤ D_max·K, A-notes). FUNC-06 / DSP-02 / DSP-04 / QUAL-01 → complete; FUNC-07 DSP half + FUNC-08 bytes half verified for their stages |

### Stage 3: GUI — two rounds (CONTEXT D1); Round A = 3.1 shell re-fork + panels + readout + ≋ + i18n, Round B = 3.2 3D view / pushes + 3.3 interaction / import
| Phase | Status | Date | Notes |
|-------|--------|------|-------|
| discuss | ✓ | 2026-09-12 | `stages/3-gui/CONTEXT.md` — D1 ROADMAP split as two rounds (Round A artifacts → `round-a/` after verify); D2 WebView2 halves of UI-01 / PERF-03 / FUNC-07 → Phase 4.2 (REQUIREMENTS rows annotated); D3 readout form `Bandlimited · N partials at C4 · silent above <note>` from a scheduler-owned top-note atomic + `label.silentAbove`; D4 O-Prism `min-width: 0` port = separate `/improve`. Findings: v1.24.0 page with no terrain controls, stubs at `PluginEditor.cpp:481/486` + `evaluateJavascript` at `:806`, template §7 API absent (map onto the Stage 2 atomics / ring / `TerrainImage::view`), shared ring across voices, O-Prism stable at `4f12ef57`. 12 research items |
| research | ✓ | 2026-09-12 | `stages/3-gui/RESEARCH.md` — 12 items resolved for both rounds (four parallel investigations); 16 corrections: **TerFreq lambda range is invisible to the JUCE JS frontend** (skew stays 1 → mockup knob / readout / reset wrong by the log map; page-side adapter, no APVTS change), **mockup `orbitXY` ≠ `Orbits.h`** (10 / 11 orbits diverge, Butterfly axes swapped; golden grid lands on LUT nodes → half-step θ + m = 0.3), shell merge base (i) = O-Prism page + spliced NEW regions (canon byte-identical, every other v2 "verbatim" region is a stub; v2 Synth tab drops the LFO Division dropdowns — splice only the two osc cards), one-voice ring rule already exists (gap = ring freezes when the display voice ends), ring written per sub-sample (2048 slots = full cycle only ≥ 47 Hz at 2× → `kPoints` 8192), heightmap ≤ 36 µs analytic / 0.28 ms Chebyshev (measured), D3 needs an amplitude rule (strongest harmonic on the orbit, τ = 2e-3 reproduces 17 / 17, hide ≥ 108), playhead event is `terrainState` (YAML), `setStateInformation` bumps nothing → `stateGeneration`, import API never sets Imported… and has no cap (editor's), tabled fr / zh strings fail the lints as written (fixed set passes), state file has 20 states not 23, no committed 666-budget gate, measure-ui has no CDP, combos not seeded by the stub, stub renders the 46-dest fallback by accident; §4 = 21 plan decisions; §5 = states / stub / gate run lists for both rounds |
| plan (Round A) | ✓ | 2026-09-12 | `stages/3-gui/PLAN.md` — 15 tasks / 5 waves / 2 commits (3.1a = ring 8192 + display-voice re-election + `chebTopNote` + `TerrainViewFeed` + editor pushes; 3.1 = page merge + i18n + gates); Decisions 1–26 close RESEARCH §4 for Round A (base (i) merge, only the two osc cards, ⬡ = mockup renderer inline as labelled placeholder, ≋ from a `terrainCycle` push, TerFreq page adapter, `requestTerrainRepush` registered now, `--gate topnote` + `--dump-choices`, generated stub overrides, `data-view` selectors, new `ui_layout_check.js` + `ui_shell_diff_check.js` with injected intended edits, zh triples at close, flags flipped by Taylor at verify). No blocking question |
| execute (Round A) | ✓ | 2026-09-12 | `stages/3-gui/round-a/SUMMARY.md` — Tasks 1–15; commits `9e4a6e65` (3.1a) + `9699a166` (3.1); v2 shell merged, Terrain tab, readout, ≋ view, i18n; every A1–A12 gate at Decision 22 |
| verify (Round A) | ✓ | 2026-09-12 | `stages/3-gui/round-a/VERIFICATION.md` — ✅ VERIFIED; every SUMMARY figure re-measured (`--gate all` 139 / 139 in 114.8 s, labels 1431 / 0, layout 358 / 0 + liveness 51 fails, shell diff 0 px + control 1576 px, tip 3163, CDP 0 off-face, pluginval ×2 + auval, params diff empty, nine inherited regions byte-identical); UI-04 → partial (controls half), FUNC-05 UI half complete; 10 human rows open (fr read, zh flips, ≋ with a note, automation, preset apply, A/B repoint, held-notes bar, Logic pass, D3 feel, readout wrap); `-Wswitch-enum` at `TerrainOscillator.cpp:321` is Phase 2.1's |
| plan (Round B) | ✓ | 2026-09-12 | `stages/3-gui/PLAN.md` — Phases 3.2 (Tasks 1–7: `stateGeneration` + `notifyStateChanged`, `TerrainViewFeed` playhead + heightmap + event builders, harness `--gate orbits [--out]`, `js/terrain-view.js` with the verbatim `Orbits.h` port + perf ring, editor five pushes + `chooseTerrainImage` / `importTerrainImageData` / `reportViewPerf` + Imported… selection + route + CMake, `tests/orbit-golden.mjs`, page wiring; commit 3.2) and 3.3 (Tasks 8–14: gestures with captured proxy targets, import flow + notices + 2 i18n keys, stub natives regenerated + 8 states, `__stubNativeCalls` spy + layout-gate `webgl` / `fallback` / `interaction` / `drop` sections, gate run B1–B14, install + PERF-03 log + hands-on, docs; commit 3.3); Decisions 27–48 close RESEARCH §4 for Round B (module location, orbit port + half-step golden with m = 0.3, heightmap on the mockup corner grid from the active surface, `terrainState` gains `h`, trail = pushed cycle, `stateGeneration` at the end of `setStateInformation` + the four preset natives, 10 Hz heightmap tick filter, placeholder stays visible when WebGL2 is null, PERF-03 log-file verdict partial (WKWebView), gesture capture rule + wheel burst + ⌥-click resets Rotation only, single-call base64 + 2 MiB cap in the editor, `selectImportedTerrain` idempotent gesture, transient `#terrain-notice`, generated stub natives, thresholds, commits). No blocking question |
| execute (Round B) | ✓ | 2026-09-12 | `stages/3-gui/SUMMARY.md` — Tasks 1–14; commits `78f39fc5` (3.2: `js/terrain-view.js` with the verbatim `Orbits.h` port, `--gate orbits` + `orbit-golden.mjs` 44 / 44 at 2.9e-6, `TerrainViewFeed` playhead + heightmap, `stateGeneration`, five pushes, three natives, Imported… from C++, perf log), `f2432353` (stub `__stubNativeCalls` spy), 3.3 (gestures with captured proxy targets + wheel burst, import flow + notices + 2 keys, 8 states, stub natives regenerated, layout gate `webgl` / `fallback` / `interaction` / `drop`); every automated gate at Decision 46 (layout 575 / 0 + liveness 78 / planted 10, labels 1767 / 0 over default + 41, tips 3163, measure-ui 5 / 0, CDP 0 off-face, shell diff 0 px, `--gate all` 139 / 139 in 111.5 s, pluginval ×2 + auval, params.tsv diff empty, 0 new warnings). Deviations: copyHeightmap follows the voice's set rule (analytic fallback, not zeros); `force` bypasses the tick filters; HUD takes pointer events; PERF-03 Standalone / Logic rows and the hands-on rows not driven (Taylor active on the machine) — headless DPR 2 stand-in 0.09 ms webgl2 / 0.11 ms 2D recorded, rows open for verify |
| verify (Round B) | ✓ | 2026-09-12 | `stages/3-gui/VERIFICATION.md` — ✅ VERIFIED (automated) = the Stage 3 verdict; every Decision 46 gate re-measured from `0e245fdb` (`--gate all` 139 / 139 in 111.5 s, orbit golden 44 / 44 at 2.93e-6, H8 0 allocations, labels 1767 / 0, layout 575 / 0, shell diff 0 px, tips 3163, CDP 0 off-face, boot clean at 33 natives, lints exit 0, pluginval ×2 + auval, params diff empty, 0 new warnings, Release WKWebView paints the pushed maps); UI-02 / UI-03 / UI-04 → complete, UI-01 / FUNC-07 partial at the D2 ceiling, PERF-03 pending (WKWebView row); six human rows open |

### Stage 4: Polish / Validation — two rounds (CONTEXT D1); Round A = 4.1 persistence + fallback + factory bank, Round B = 4.2 basis PERF + CI + listening + release-ready
| Phase | Status | Date | Notes |
|-------|--------|------|-------|
| discuss | ✓ | 2026-09-12 | `stages/4-polish/CONTEXT.md` — D1 ROADMAP split as two rounds (Round A artifacts → `round-a/` after verify); D2 COMPAT-02 evidence = `ci-tests.yml` O-Strata macOS harness (`--gate all` + orbit golden) + Windows VST3 / pluginval-10 jobs, run by **manual dispatch** (Taylor asked for on-push; the 2026-08-27 standing rule forbids it — recorded); D3 local install only, no tag / `/publish` in Stage 4; D4 vectorised Chebyshev basis taken (gated by H1 identity + H6 band + H7 ≤ 2× + 2 %), analytic fallback above the muting note + pitch-tracking soft knee → v1.1 (REQUIREMENTS §Out of Scope). Findings: `customSave` / `customLoad` never wired, `terrainImports` child empty, `ImportSlot` already holds bytes + name + SHA, two load orders vs `stateGeneration`, Locate… is new UI + native + 3 keys, `Init` orphan deletion at 1.0.0, `ci-tests.yml` hard-codes O-Octagon + `resolve-target.sh` falls back for O-Strata, CI Windows build cannot prove the WebView2 render / drop / PERF-03 figure (named deferral), PERF-03 log still absent, NOTES.md stale. 10 research items |
| research | ✓ | 2026-09-12 | `stages/4-polish/RESEARCH.md` — 10 items resolved (A persistence / preset hook / Locate… / H10, B bank + H2 over presets, C `ci-tests.yml` + MSVC + docs, D Chebyshev bench + listening); 15 corrections (C1 Imported… with no image is **silent** → the fallback is Round A DSP; C3 `stateGeneration` bump stays; C4 `customLoad` skipped without `customState`; C6 H2 preset loop ignores Coarse/Fine + zeroes osc B; C7 `.factory-version` already 1.0.0 → content stamp + `deleteRecursively`; C9 `resolve-target.sh` resolves cleanly; C12 literal D4 form slower, identity row vacuous, H6 band undefined; C13 export grid 2× only, golden CRLF); 17 plan decisions (1–11 Round A, 12–17 Round B); §5.1 18-preset bank with measured H2 (Squarcle Storm → (0.23, 0.21)); §5.2 H10 skeletons; §5.3 gate run lists; §5.4 defaults-change commands |
| plan (Round A) | ✓ | 2026-09-12 | `stages/4-polish/PLAN.md` — 12 tasks / 4 waves / 3 commits (preset-manager v1.0.7 separate; 4.1a after Wave 2; 4.1 at close); Decisions 1–26 close RESEARCH §4 items 1–11 (D2 two caps: drop / embed 2 MiB unchanged, Import… / Locate… 8 MiB + `tooLargeFile`; D3 slot layout `v=1`; D4 `juce::Base64`, no memoisation; D7 Sine Product fallback + heightmap mirror; D8 `sourceMissingFlag` atomics; D9 `callAsync` guard with `restoreAlive`; D10 `clearImportSlot` retires via `retire()`; D11 v1.0.7 patch + `customSave` always an object; D13 `locateTerrainImage` refuses on SHA mismatch; D15 five keys, tip literal 122; D16 `terrain-locate-mismatch` state, stub override, layout `locate` section; D17–19 §5.1 bank, `Init` first in `CATEGORY_ORDER`, stamp + `deleteRecursively`; D20 H2 f0 / B pass / neighbours / `--real` / `h2cli`; D21 H10 (a)–(e) with the noise fixture in (2 MiB, 8 MiB]; D22 smoke [5]; D25 five human rows). Execute (Round A) next |
| execute (Round A) | ✓ | 2026-09-12 | `stages/4-polish/round-a/SUMMARY.md` — Tasks 1–12; commits `f2fe26ed` (preset-manager v1.0.7: `customLoad (var())` when a preset has no `customState`), `1a1ff54e` (4.1a: `terrainImports` `<slot osc form name sha256 size data|path/>` + `customState {v, slots[]}` through one adapter pair, bytes ≤ 2 MiB inline / path form above with SHA-256 check, `clearImportSlot` retiring through `retire()`, Decision 9 `callAsync` guard, Sine Product fallback + heightmap mirror, `kMaxImportBytes` 2 MiB + `kMaxImportFileBytes` 8 MiB on the processor, `launchPngChooser` + `locateTerrainImage` (refuses on hash mismatch), the notice's Locate… button + 5 keys + 2 tips + `terrain-locate-mismatch` state + stub override, `'Init'` first in `CATEGORY_ORDER`, the 18-preset bank stamped `1.0.0+18c17735821c` with `Factory/` swept on mismatch, H2 preset rows with Coarse / Fine + osc-B + FX bypass + preset-centred neighbours, `--real` + `--gate h2cli`), 4.1 (H10 (a)–(e), smoke [5], layout `locate` section, docs). Gates: `--gate all` 159 / 159 in 115.9 s (repo root; 139 / 139 from `/` at 4.1a), H2 20 / 20 (Breathing Pad −1.9, PD Organ −3.3, Butterfly Choir −4.5, Squarcle Storm −3.8 at (0.23, 0.21)), H10 24 / 24 (two 1.9 MB slots published in 62 ms; missing source rms 0.242 = Sine Product SHA), smoke --with-disk 61 / 61, layout 607 / 0, shell diff 0 px, labels 1809 / 0 over default + 42, tip gate 3215 / 0 at 122, lints 0 with 53 'mt', orbit golden 44 / 44, params diff empty, 0 new warnings, MSVC greps 0, auval SUCCEEDED, install clean, no tag. Deviations (11, SUMMARY): H2 preset rows bypass the FX chain (the pre-filter tap is a voice tap — reverb smeared the h1 windows; research figures were FX-free); Squarcle Storm −3.8 dB with its S&H route (100 % windows); SafePointer count stays 6 (site moved into the helper); manager copy is a diverged fork; notice = flex row (zh height); tip gate TAB_OF + sourceMissing push; H10 (c) reference through the same preset apply (normalised round trip = 1 ulp, ≤ 5e-8); H10 (d) waits on imagePtr; `getpid()` for the temp dir. Five hands-on rows + Stage 3's six carried for Taylor. Verify (Round A) next |
| verify (Round A) | ✓ | 2026-09-13 | `stages/4-polish/round-a/VERIFICATION.md` — ✅ VERIFIED (Round A, automated); every Task 10 gate re-measured from `29803cb5` (`--gate all` 159 / 159 in 111.4 s; H2 20 / 20 preset rows — Squarcle Storm −2.2 … −4.0 dB across three runs through the S&H LFO's clock-seeded `juce::Random`, always inside −6 dB, Round B item; H10 24 / 24, (d) 64 ms; smoke --with-disk 39 / 39 incl. [5]; H8 0 allocations; labels 1809 / 0 over default + 42; boot clean, DEAD 0, no `stub invented:`; tips 3215 at 122; layout 607 / 0 + liveness 81 fails; shell diff 0 px; measure-ui 0 / 0; CDP 0 off-face; lints exit 0 with 53 `'mt'`; orbit golden 44 / 44 at 2.93e-6; params.tsv diff empty; 0 new warnings; MSVC greps 0; pluginval ×2 SUCCESS + auval SUCCEEDED on the installed `-dev` bundles, byte-identical to the build). FUNC-08 / FUNC-11 / QUAL-03 → complete; UI-04 / COMPAT-01 regressions held. Six human rows open (Decision 25 rows 1–5 + fr / zh read). Round A artifacts moved to `round-a/` (CONTEXT D1). **Not the Stage 4 verdict** — Round B closes the stage |
| plan (Round B) | ✓ | 2026-09-13 | `stages/4-polish/PLAN.md` — 12 tasks / 4 waves / 3 commits (4.2a `plugins/O-Strata`; ci `.github/workflows/ci-tests.yml`; release `plugins/O-Strata` + `PLUGINS.md`; a 4.2b only if the Windows run forces a fix); Decisions 27–45 close RESEARCH §4 items 12–17 + Round A verify Issue 1: D28 form (e) `chebEvalPadded` on an `alignas (16)` 17 × 20 copy written by `buildChebWeights` with fixed trip counts, `clenshaw2D` kept for the three off-thread consumers, NEON only if H7 misses; D29 `--gate clenshaw` = both ns figures reported + `maxΔ ≤ 2e-5` on the random set + `≤ 1e-6` on a `projectAnalytic` Sine Product set + double-ref reported; D30 H6 ± 0.5 dB as an executor before / after diff (h_max / muted 17 / equality 13 identical, 2× / 4× / 1× bit-identical), H1 identity −128.3 identical, H9 `0.000e+00`; D31 H7 Bandlimited row → `check (deltaBL ≤ delta2x + 2.0)` + `SystemStats::getCpuModel()`; D32 `LFO::seed()` + four `seedFor (2..5)` calls before the voice's `lfoN.reset()` when `harnessSeed != 0` (production clock-seeded, O-Prism untouched) → H2 ×3 bit-stable; D33 one workflow, `plugin` `choice` input (default O-Octagon), jobs `probes-macos` / `windows-vst3`, O-Octagon steps byte-identical behind `if:`, O-Strata macOS = build `O-Strata-render-test` + `--gate all` + orbit golden, header CAN / CANNOT sentences verbatim, wall-clock rows accepted with one re-dispatch; D34 `git push origin main` (carries the 5 unpushed commits) + `gh workflow run ci-tests.yml --ref main -f plugin=O-Strata` + background `gh run watch`, URL / headSha / durations / pluginval log recorded, MSVC failure → 4.2b; D35 `if(OUARICON_BUILD_TESTS AND APPLE)` + `FileLogger::getSystemLogFileFolder()` (same macOS path); D36 PERF-03 Standalone burst driven by script, flag `perfOptions.finishPerFrame` only if mean > 2 ms, else row stays human; D37 `stages/4-polish/LISTENING.md` (Tables A 18 × 2 hosts, B 66 C4 + flagged C2 / C6 pairs, C = Stage 3 rows 1–6 + Round A rows 1–6 + 3 Round B rows; verdicts blank; PASS / NOTE / CHANGE / FAIL); D38 CHANGE protocol after the sitting, one per commit under §5.4; D39 `--gate exportPresets` (18 post-FX renders, no golden) + LF grid golden (same checksums); D40 CHANGELOG `## v1.0.0 — date` Added / Behaviour to know / Known limits (Windows deferral sentence verbatim) / Technical notes / Stage history; D41 NOTES Attribution & licence + v1.1 list, ARCH D4 paragraph, PLUGINS.md 📦 Installed, REQUIREMENTS untouched; D42 `build-and-install.sh` + `auval -a` ×2 + pluginval ×2 on the installed bundles + `cmp`; D43 four MSVC greps; D44 human rows = LISTENING Table C; D45 not-touched list. Gate run B1–B20 (`--gate all` ≥ 162; page gates unchanged at 1809 / 607 / 3215 / 53 mt). Execute (Round B) next |
| execute (Round B) | ✓ | 2026-09-13 | `stages/4-polish/SUMMARY.md` — Tasks 0–11; commits `76a90874` (4.2a: `chebEvalPadded` on a padded `alignas (16)` 17 × 20 copy written by `buildChebWeights` with fixed trip counts, `clenshaw2D` untouched for the three off-thread consumers; `LFO::seed()` + four `seedFor (2..5)` calls before the voice's LFO resets when `harnessSeed != 0`; `--gate clenshaw` four lines, H7 Bandlimited promoted to a check + `SystemStats::getCpuModel()`, new `--gate exportPresets`, LF grid golden, `writeWav24` shared by both export gates; `if(OUARICON_BUILD_TESTS AND APPLE)`; `logViewPerf` via `FileLogger::getSystemLogFileFolder()`), `36a6c1d4` (ci: `plugin` choice input, `probes-macos` / `windows-vst3`, O-Octagon step bodies byte-identical behind `if:`, O-Strata macOS harness + orbit golden steps), release (docs + LISTENING + SUMMARY + STATUS + PLUGINS.md). No 4.2b — the Windows MSVC build passed first time. **D4:** `clenshaw2D` 83.9–95.2 ns → `chebEvalPadded` 28.0–30.3 ns (2.8–3.4×); H7 Bandlimited delta 11.69 % → 4.46–4.54 % ≤ 2× delta + 2.0; accuracy random 9.775e-06 ≤ 2e-5 / projected 2.980e-07 ≤ 1e-6 / double ref 3.41e-06; H6 worst shift **0.100 dB** at CosineWells × Epitrochoid5 A2 with `h_max` / 17 muted / 13 equality identical and 2× / 4× / 1× bit-identical; H1 −128.3 identical; H9 `0.000e+00`. **D32:** `--gate H2` ×3 byte-identical, Squarcle Storm −3.6 dB / 100 % every run, 20 / 20; H3 9 / 9. **CI (COMPAT-02):** three dispatches from `origin/main` with `-f plugin=O-Strata`. **`windows-vst3` success on all three** (6m35s / 6m01s / 6m12s) — MSVC + WebView2 built the VST3 and pluginval 10 passed every suite incl. Editor Automation (log downloaded; the step's shell carries `pipefail`, so the exit code is pluginval's). `probes-macos` builds the harness, runs all **162** checks (the local count) and the **orbit golden at 44 / 44, worst 2.93e-6 — identical to local**; it fails on wall-clock only, on an `Apple M1 (Virtual)` runner ~2.6x slower (288–304 s vs 111 s): `[sched]` publish latency 264 / 176 / 154 ms against 120 ms on all three, and `[H7]` Bandlimited on two of three (16.89 / 11.02 / 10.37 against a 2x-delta + 2.0 bound of 10.58 / 11.57 / 10.23). Per Decision 33 both are **recorded as runner-flaky; local thresholds unchanged**. Decision 31's "both sides move together" rationale does not hold on this runner (BL / 2x is 1.02x locally, 1.15–1.97x there) — a ratio form or a `STRATA_CI` band is v1.1. Latest run <https://github.com/taylorbrook/O-Audio-VST-Development/actions/runs/34809565500> at `dcbc609a`. Two extra commits beyond the plan's three: `5e044fd1` (harness `writeWav24` — the new export gate had added 3 warnings against the 0-new bar) and `dcbc609a` (the orbit golden was being skipped by an unrelated `--gate all` failure; `!cancelled()` makes it report).  **Local gates:** `--gate all` **162 / 162** in 111.7 s, H10 24 / 24, smoke --with-disk 39 / 39 at stamp `1.0.0+18c17735821c`, H8 0 allocations, orbit golden 44 / 44 at 2.93e-6, export 198 / 198 + `shasum -c` 0 non-OK + exportPresets 18 / 18 0 silent, labels 1809 / 0 over default + 42, boot clean / DEAD 0 / `i18n=203`, tips 3215 at 122, layout 607 / 0 + liveness 526 / 81, shell diff 0 px, measure-ui wrap 0 (undeclared 5 = the inherited `‹ › ★ ⚙` glyphs), CDP 0 off-face, lints exit 0 with 53 `'mt'`, `params.tsv` diff empty, 0 new warnings, MSVC greps 0. **Install:** `build-and-install.sh` clean (no alternate-variant sweep), `auval -a` ×2 lists `aumu OuSt OuDv` once, **AU VALIDATION SUCCEEDED**, pluginval 10 **SUCCESS** on the installed VST3 + AU, installed = built by `cmp`; **no tag**. **Deviations (6):** `writeWav24` extracted so the new gate adds 0 warnings; PERF-03 unreachable (four scripted attempts, no `view-perf.log`, **no `gl.finish()` flag**) → LISTENING Table C row 1; the botanical-plate credit had no existing line to copy and was written from the asset filename; Decision 31's runner-safety rationale did not hold; `--gate all` is exactly 162; the H6 diff needed a ~45-line parser. **QUAL-04 material ready:** `stages/4-polish/LISTENING.md` (A 39 blank verdicts, B 85, C 15 rows). Verify (Round B = the stage verdict) next |
| **verify (Round B = the Stage 4 verdict)** | ✓ | 2026-09-14 | `stages/4-polish/VERIFICATION.md` — **✅ VERIFIED (Stage 4, automated)**. Every B1–B20 gate re-measured from the release commit `005cbcf7`; a forced recompile of the 7 Round B TUs reproduced a VST3 bit-identical to the installed bundle (`c86546cd…`), so the gates ran on the release code. `--gate all` **162 / 0 in 108.6 s**; clenshaw **81.4 → 28.0 ns (2.9×)**, random 9.775e-06 ≤ 2e-5 / projected 2.980e-07 ≤ 1e-6; **H7 2× 4.13 %, Bandlimited 4.24 % ≤ 4.13 + 2.0 PASS**; H1 identity −128.3 dB unmoved, H9 0.000e+00; H6 2× worst −70.7 dB, BL 127 / 127 worst −98.4 dB, 17 muted + 13 equality identical; **H2 ×3 20 / 20 with every measurement row identical, Squarcle Storm a stable −3.6 dB → Round A Issue 1 CLOSED**; H10 24 / 24; smoke 39 / 39; H8 8 / 8; orbit golden 44 / 44 worst 2.93e-6; grid `shasum -c` 0 non-OK; exportPresets 18 / 0 silent; labels 1809 / 0 over default + 42; boot clean 1 / 1, DEAD 0; layout 607 / 0 (liveness 526 / 81, documented); tips 3215 at 122; shell diff 0 px (control 1576); measure-ui 5 / 3 / 0 / 0; CDP 16 runs 0 off-face; `params.tsv` empty; 26 warning lines, **0 new**; MSVC greps 0; pluginval 10 VST3 + AU `SUCCESS` and `auval` `SUCCEEDED` on the installed bundles; bank 18 / 9 folders / stamp `1.0.0+18c17735821c`; **no tag**. CI re-read from `gh`: `windows-vst3` `success` with all 13 steps green on three dispatches → **COMPAT-02 complete** at the Finding 6 ceiling. REQUIREMENTS annotated: COMPAT-02 / PERF-02 / UI-01 / FUNC-07 → complete (the last two at the ceiling, WebView2 hands-on halves a **named deferral — owner none, blocked on hardware**); QUAL-04 + PERF-03 pending (human). **All 24 `must` complete.** Issues: (1) the H7 Bandlimited absolute-slack row is load-flaky *everywhere* — under contention the same binary read 10.42 % vs 2.68 % (3.9×) and `--gate all` took 2125 s wall / 123 s user; v1.1 wants a ratio form + a duty-cycle assertion; (2) SUMMARY's H2 "byte-identical" is one wall-clock line too strong; (3) the Windows pluginval log has no literal `SUCCESS` line (exit 0 under `-e -o pipefail`, 0 FAIL lines, is the claim); (4) B13's `constexpr` wording is narrower than the tree (6 sites, none lambda-captured — verdict unchanged); (5) the release commit `005cbcf7` is unpushed but docs-only |
| post-verify: CI gates | ✓ | 2026-09-21 | quick-260921-j94 (`.planning/quick/260921-j94-*/260921-j94-SUMMARY.md`) — closes VERIFICATION Issue 1 and the two wall-clock rows that failed `probes-macos` on all three 2026-09-14 dispatches. `[H7]` Bandlimited is now a same-run ratio (K 1.8, floor 2.0 %) behind a duty-cycle witness; `[sched]` asserts the deterministic terms only (publish time printed, 2000 ms hang guard). Harness-only — no plugin source touched. `ci-tests.yml` run 35661284591 green on JUCE 8.0.15: `--gate all` 164 / 164, orbit golden, `windows-vst3`. |

## Stage 0 decisions (summary — full table in `stages/0-ideation/CONTEXT.md`)

- Terrain formulas use the **πF convention**; defaults c = (0.13, 0.21), aspect 0.7, r = 0.5, F = 1 measured h1 = 0 dB (symmetry gate passes); the gate is not monotone in F, so every factory preset is gated individually.
- Pitch tracking F_eff = F · min(1, C4/f_note)^track (scales down only, block-rate).
- Bandlimited mode: degree-16 triangle, Terrain Freq clamped ≤ 2, Pitch Track inert, truncation is the mip, terrain destinations block-rate (global sources) / bypassed (per-voice); F-lattice is v1.1.
- Hand-rolled per-sample polyphase-IIR decimator (JUCE `FilterDesign` coefficients in `prepare`), not `juce::dsp::Oversampling`; constant +1 sample latency report.
- `WavetableOscillator` / `WavetableData` / mipmaps **deleted in Phase 2.1**; `TerrainOscillator` replaces in place; reaper generalised.
- Quality default 2×. Feedback: two-sample average + leaky integrator, clamped, DC blocker on the output.
- PERF-02 measured as **oscillator delta** via a harness baseline (open question: amend the requirement wording); full-path model ≈ 15 % → ≈ 11.4 % after the two planned Phase 2.3 savings.

## Next Steps

1. ~~Play Aaron Anderson's *Terrain* before Stage 2~~ **skipped by decision (Stage 2 CONTEXT D3)** — the Phase 2.3 WAV grid and the Stage 4 QUAL-04 listening pass are the listening material; the raw-feedback blend stays an internal fallback.
2. ~~**UI mockup v2:** `design UI for O-Strata` (ui-mockup skill) from BRIEF §UI Concept and ARCHITECTURE "Parameter Mapping" → finalise → `parameter-spec.md` v2 locked; then set `mockup_finalized: true`, `ready_for_implementation: true` here.~~ **Done 2026-09-10** — mockup v2 finalised and scaffolded; `parameter-spec.md` v2 locked (sha256 in `contract_checksums`); `ready_for_implementation: true`.
3. ~~Stage 1 second pass: `/plugin-discuss O-Strata 1-foundation`~~ **verified 2026-09-10** (`stages/1-foundation/VERIFICATION.md`).
4. ~~`/plugin-discuss O-Strata 2-dsp`~~ **done 2026-09-10**; ~~`/plugin-research O-Strata 2-dsp`~~ **done 2026-09-11**; ~~Round A plan / execute / verify~~ **done 2026-09-11**; ~~`/plugin-plan O-Strata 2-dsp` (Round B)~~ **done 2026-09-11**; ~~`/plugin-execute O-Strata 2-dsp` (Round B)~~ **done 2026-09-12** (`stages/2-dsp/SUMMARY.md`, two commits). ~~`/plugin-verify O-Strata 2-dsp` (Round B)~~ **done 2026-09-12** (`stages/2-dsp/VERIFICATION.md` ✅ VERIFIED — Stage 2 complete). ~~`/plugin-discuss O-Strata 3-gui`~~ **done 2026-09-12** (`stages/3-gui/CONTEXT.md`). ~~`/plugin-research O-Strata 3-gui`~~ **done 2026-09-12** (`stages/3-gui/RESEARCH.md`). ~~`/plugin-plan O-Strata 3-gui` (Round A)~~ **done 2026-09-12** (`stages/3-gui/PLAN.md`). ~~`/plugin-execute O-Strata 3-gui` (Round A)~~ **done 2026-09-12** (`stages/3-gui/round-a/SUMMARY.md`, commits `9e4a6e65` + `9699a166`). ~~`/plugin-verify O-Strata 3-gui` (Round A)~~ **done 2026-09-12** (`stages/3-gui/round-a/VERIFICATION.md` ✅ VERIFIED). ~~`/plugin-plan O-Strata 3-gui` (Round B)~~ **done 2026-09-12** (`stages/3-gui/PLAN.md`, Decisions 27–48). ~~`/plugin-execute O-Strata 3-gui` (Round B)~~ **done 2026-09-12** (`stages/3-gui/SUMMARY.md`, commits `78f39fc5` + `f2432353` + Phase 3.3). ~~`/plugin-verify O-Strata 3-gui` (Round B = the Stage 3 verdict)~~ **done 2026-09-12** (`stages/3-gui/VERIFICATION.md` ✅ VERIFIED (automated) — Stage 3 complete). ~~`/plugin-discuss O-Strata 4-polish`~~ **done 2026-09-12**; ~~`/plugin-research O-Strata 4-polish`~~ **done 2026-09-12**; ~~`/plugin-plan O-Strata 4-polish` (Round A)~~ **done 2026-09-12**; ~~`/plugin-execute O-Strata 4-polish` (Round A)~~ **done 2026-09-12**; ~~`/plugin-verify O-Strata 4-polish` (Round A)~~ **done 2026-09-13** (`stages/4-polish/round-a/VERIFICATION.md` ✅ VERIFIED). ~~`/plugin-plan O-Strata 4-polish` (Round B)~~ **done 2026-09-13** (`stages/4-polish/PLAN.md`, Decisions 27–45). ~~`/plugin-execute O-Strata 4-polish` (Round B)~~ **done 2026-09-13** (`stages/4-polish/SUMMARY.md`, commits `76a90874` + `36a6c1d4` + release; `git push origin main` pushed `a2cf6513..36a6c1d4`; CI dispatched with `plugin=O-Strata`). **Next:** `/plugin-verify O-Strata 4-polish` (Round B = the Stage 4 verdict) — re-measure B1–B20 from the release commit, record Taylor's `LISTENING.md` counts (or QUAL-04 pending), annotate COMPAT-02 / QUAL-04 / PERF-02 / UI-01 / FUNC-07 / PERF-03 / COMPAT-01 in REQUIREMENTS.md, write `stages/4-polish/VERIFICATION.md`. **Open for Taylor:** the listening sitting (`stages/4-polish/LISTENING.md` Tables A + B) and the 15 Table C rows — PERF-03 in the Standalone and Logic is row 1. Superseded execute text follows: (Round B — Phase 4.2: Tasks 0–11 in four waves; commit 4.2a after Wave 2, the `ci` commit + `git push origin main` + `gh workflow run ci-tests.yml -f plugin=O-Strata` in Wave 3 (Decision 34 — Taylor may veto the push before execute; COMPAT-02 then stays pending), the release commit at close; no tag). After execute: Taylor's listening sitting fills `stages/4-polish/LISTENING.md`, then `/plugin-verify O-Strata 4-polish` (Round B = the Stage 4 verdict). Superseded text follows: `/plugin-discuss O-Strata 4-polish` — persistence (`terrainImports`, 2 MB cap form, Locate…, `sourceMissing`), factory presets, CI Windows build for the WebView2 halves, PERF items (Chebyshev basis; `gl.finish()` flag if the PERF-03 row asks), listening pass. **Open for Taylor (Stage 3 VERIFICATION §Human rows 1–6):** PERF-03 rows in the Standalone and Logic at DPR 2 (⌥-click the HUD three times → `~/Library/Logs/O-Strata/view-perf.log`; the headless DPR 2 stand-in reads 0.09 ms webgl2 / 0.11 ms 2D), the hands-on rows (wireframe follows Terrain / Freq / Mod X / Mod Y and Blur / Edge on a PNG, scan point + amber trail with Feedback > 0, preset apply / session restore re-push, one undo step per gesture in Logic, Osc B gestures, chooser + drop + the two notices, lose-context recovery), fr read of the 2 new rows, zh flips (48). **Still open (Round A human rows, `round-a/VERIFICATION.md` §Human):** fr read (46 rows incl. `label.orbitSize` "Taille" / `label.pitchTrack` "Suivi haut."), zh triples flip (`stages/3-gui/round-a/round-a-zh-triples.md`), the MIDI / Logic hands-on rows (≋ view with a held note + re-election, automation, preset apply, Osc A/B toggle, held-notes bar — tick the MIDI input under Options → Audio/MIDI Settings first, the macOS Standalone does not auto-enable it); carried items: O-Prism v1.26.1 octave-stretch port (separate `/improve`), WebView2 evidence (Phase 4.2), muted Bandlimited rows + soft knee (optional Stage 4 / v1.1 discuss), vectorised Chebyshev basis (Stage 4 PERF), ASan re-run when the toolchain allows. ~~`/plugin-discuss O-Strata 4-polish`~~ **done 2026-09-12** (`stages/4-polish/CONTEXT.md`, D1–D4). ~~`/plugin-research O-Strata 4-polish`~~ **done 2026-09-12** (`stages/4-polish/RESEARCH.md`). ~~`/plugin-plan O-Strata 4-polish` (Round A)~~ **done 2026-09-12** (`stages/4-polish/PLAN.md`). ~~`/plugin-execute O-Strata 4-polish` (Round A)~~ **done 2026-09-12** (`stages/4-polish/SUMMARY.md`, commits `f2fe26ed`, `1a1ff54e`, 4.1). **Next: `/plugin-verify O-Strata 4-polish` (Round A)** — re-measure A1–A16 from the 4.1 commit, annotate FUNC-08 / FUNC-11 / QUAL-03 in REQUIREMENTS.md, move PLAN / SUMMARY / VERIFICATION to `round-a/`; then `/plugin-plan O-Strata 4-polish` (Round B, Decisions 27+). **Open for Taylor (Round A hands-on, SUMMARY §Hands-on):** Locate… by hand (moved file → notice + button → restored; different PNG refused), a ≤ 2 MiB PNG preset through Logic save / reload, the live restore re-push, the dropdown order (Init first, ◀ / ▶ over 18), a first listen to the bank; plus the fr read of the five new rows and the zh flips (53).
5. ~~Answer the four open questions in `stages/0-ideation/CONTEXT.md`~~ **all closed 2026-09-10**: PERF-02 → oscillator delta (REQUIREMENTS amended); *Terrain* listening skipped (Decisions 1 / 3 stand); F-lattice v1.1; host strings closed by the mockup v2 lock.

## Context to Preserve

- Research: `research/wavetable-synthesis-3d-geometry.md` §1, §7.2, §7.3; benchmark `research/wavetable-synthesis-3d-geometry-prototypes/terrain-bench/`; view prototype `webgl-3d/terrain-proto.html`
- Contracts (v2): `research/ARCHITECTURE.md`, `ROADMAP.md`, `REQUIREMENTS.md`, `BRIEF.md`, **`parameter-spec.md` (locked 2026-09-10)**, `parameter-spec-draft.md` (superseded by the lock); UI: `mockups/v2-ui.yaml`, `v2-ui.html`, `v2-integration-checklist.md`
- Superseded design (for the v1.1 Baked source type): `superseded-baked-v1/` (ARCHITECTURE, ROADMAP, spec v1, mockup v1, Stage 0 + Stage 2 CONTEXT) — its Core 5 / Core 8 / State Persistence / 3D view sections are cited by the v2 ARCHITECTURE
- Fork history: `stages/1-foundation/first-pass-baked/` (CONTEXT D1–D6, RESEARCH, PLAN, SUMMARY, VERIFICATION); `stages/1-foundation/smoke/` (shared harness); second pass: `stages/1-foundation/CONTEXT.md`, `RESEARCH.md`, `PLAN.md`
- Symmetry rule and harness gate: REQUIREMENTS DSP-06, ARCHITECTURE "Symmetry rule", harness H2
- Out of scope v1.0: baked sources (v1.1), wavetable mode (never), RGB terrains (v1.1), dual-orbit stereo (v1.x), Bandlimited F-lattice (v1.1)

## Files
- plugins/O-Strata/Source/** (fork, spec v2 parameter set — 205 params, live terrain oscillator at 2× since Round A: `dsp/TerrainOscillator.*`, `Orbits.h`, `Terrains.h`, `HalfbandDecimator.h`; **Round B:** `dsp/Retirable.h`, `ChebyshevSet.h`, `ChebyshevProjector.*`, `TerrainScheduler.*`, `TerrainImage.*`, the processor import API + readout atomics), CMakeLists.txt (render-test target, `JUCE_MODAL_LOOPS_PERMITTED`, ASan option), tests/render-harness/** (harness H1–H11, reference headers, fixture, golden .sha256), CHANGELOG.md, .planning/params.tsv (205 rows, current binary, unchanged by Rounds A / B); root `.gitignore` (`build-asan/`)
- plugins/O-Strata/.planning/{BRIEF.md, REQUIREMENTS.md, parameter-spec-draft.md} (v2, 2026-09-08); parameter-spec.md (v2 locked, 2026-09-10); mockups/{v2-ui.yaml, v2-ui-test.html, v2-ui.html, v2-PluginEditor-TEMPLATE.h, v2-PluginEditor-TEMPLATE.cpp, v2-CMakeLists-SNIPPET.txt, v2-integration-checklist.md, img/}
- plugins/O-Strata/.planning/research/ARCHITECTURE.md, ROADMAP.md, stages/0-ideation/CONTEXT.md (v2, 2026-09-08); stages/2-dsp/CONTEXT.md (2026-09-10), RESEARCH.md (2026-09-11), round-a/{PLAN,SUMMARY,VERIFICATION}.md (Round A, 2026-09-11), PLAN.md (Round B, 2026-09-11), SUMMARY.md (Round B, 2026-09-12), VERIFICATION.md (Round B + stage verdict, 2026-09-12); REQUIREMENTS.md (DSP-01 amended 2026-09-11; FUNC-06 / DSP-02 / DSP-04 / QUAL-01 complete + FUNC-06 acceptance amended 2026-09-12)
- plugins/O-Strata/.planning/stages/4-polish/CONTEXT.md (2026-09-12), RESEARCH.md (2026-09-12), PLAN.md (Round B, 2026-09-13, Decisions 27–45), **SUMMARY.md (Round B, 2026-09-13)**, **LISTENING.md (Round B, 2026-09-13 — Tables A / B / C, verdicts blank)**, round-a/{PLAN,SUMMARY,VERIFICATION}.md (Round A, 2026-09-12 / 13; VERIFICATION ✅ VERIFIED); REQUIREMENTS.md (FUNC-08 / FUNC-11 / QUAL-03 complete, 2026-09-13 — untouched by Round B)
- **Stage 4 Round B files:** Source/dsp/ChebyshevSet.h (`kChebPadRow` / `kChebPadded` / `chebBasis17` / `chebEvalPadded`), dsp/TerrainOscillator.{h,cpp} (padded `chebW`, padded `buildChebWeights`, the scan site), dsp/LFO.h (`seed()`), StrataVoice.cpp (four seeded LFOs at note-on), FactoryPresets.cpp (the Squarcle Storm comment), PluginEditor.cpp (`logViewPerf` folder), CMakeLists.txt (`AND APPLE`), tests/render-harness/{main.cpp,README.md,golden/round-a-grid.sha256 (LF)}, tests/exports/presets/*.wav (18, gitignored); .github/workflows/ci-tests.yml (the `plugin` dispatch input); CHANGELOG.md (v1.0.0), NOTES.md (rewritten), .planning/research/ARCHITECTURE.md (the evaluator paragraph), PLUGINS.md
- plugins/O-Strata/.planning/stages/3-gui/CONTEXT.md (2026-09-12), RESEARCH.md (2026-09-12), round-a/{PLAN,SUMMARY,VERIFICATION}.md + round-a/round-a-zh-triples.md (Round A, 2026-09-12; VERIFICATION ✅ VERIFIED), PLAN.md (Round B, 2026-09-12); REQUIREMENTS.md (UI-01 / PERF-03 / FUNC-07 WebView2 note; UI-04 partial + FUNC-05 UI half, 2026-09-12)
- **Stage 3 Round A files:** Source/ui/public/index.html (the v2 shell), js/i18n.js (+32 keys, +14 tips), ui/public/img/shell_conchologiaiconi12reev_0090.png, Source/TerrainViewFeed.{h,cpp}, PluginEditor.{h,cpp} (pushes, requestTerrainRepush), PluginProcessor.{h,cpp} (chebTopNote, TerrainStatus, re-election), StrataVoice.{h,cpp} (startSerial), dsp/TerrainOscillator.{h,cpp} (ring 8192), dsp/TerrainScheduler.{h,cpp} (top-note probe), dsp/MathConstants.h (pitchRatio), CMakeLists.txt; tests/render-harness (topnote gate, --dump-choices), tests/ui_layout_check.js, tests/ui_shell_diff_check.js, tests/ui_tip_render_check.js (five tabs, 120), tests/tools/{gen-stub-overrides.mjs, cdp-font-probe.js}, tests/ui-stub/generic-overrides.json (generated), tests/i18n-states.json (+13)
- plugins/O-Strata/.planning/superseded-baked-v1/** ; .planning/evidence/*.md (WAVs gitignored)
- PLUGINS.md (row 📦 Installed 1.0.0, 2026-09-13)
